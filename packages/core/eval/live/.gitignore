# Generated reference blobs (regenerate with gen-blob.mjs):
#   bun gen-blob.mjs blob-spec.md 700   # ~700KB, ~180K tok  (single-turn near-full 200K window)
#   bun gen-blob.mjs blob-mid.md  351   # ~360KB, ~90K tok/turn (long-session, manageable per-turn)
#   bun gen-blob.mjs blob-sm.md   250   # ~256KB, ~64K tok/turn (combined multi-session)
blob-*.md
# Generated large exploratory seed (regenerate with gen-seed.mjs); seed-min/ is committed
seed/
# Run logs / scratch
*.log
