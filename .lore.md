<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019e550f-29b4-768d-ade6-4e46c014effe -->
* **DB schema current version: 26 migrations, key columns per migration**: DB schema: 30 migrations. Key: v8=knowledge.embedding+kv\_meta; v13=metadata; v14=projects.git\_remote+path\_aliases; v19=import\_history; v23=session\_state LTM cache/pin; v24=session identity+gradient state; v25=dedup\_feedback; v26=session\_state.parent\_session\_id+is\_subagent; v27=entities+entity\_aliases+knowledge\_entity\_refs+FTS5+triggers; v28=entity\_relations; v29=knowledge +11 cols (promotion\_status, promoted\_at, approval\_status, approved\_by, approved\_at, source\_user\_id, source\_entry\_id, last\_accessed\_at, created\_by, updated\_by, sensitivity) + team\_knowledge + team\_config; v30=daily\_costs ledger table (day TEXT, bucket TEXT, cost REAL; PRIMARY KEY (day, bucket) — no separate idx\_daily\_costs\_day needed, PK covers day-prefix queries). \`migrate()\` uses \`MIGRATIONS.length\` as version — \`SCHEMA\_VERSION=16\` constant is dead code. \`recoverMissingObjects()\` must cover ALL v30 tables. Always \`git pull --rebase\` before migration work. Cross-check version numbers across PR title, description, commit messages, test assertions, and constants.

<!-- lore:019e54f5-051c-7641-aaad-7688451d5021 -->
* **Gateway complete HTTP endpoint inventory**: Gateway HTTP endpoints. LLM Proxy: POST /v1/messages (Anthropic), POST /v1/chat/completions (OpenAI Chat), POST /v1/responses (OpenAI Responses API), POST /v1/compact, GET /v1/models. Health: GET /health. REST API (/api/v1/): GET projects/stats/knowledge/sessions/distillations/recall/import-history; DELETE knowledge/:id/sessions/:id/distillations/:id/projects/:id; POST projects/merge (disabled hosted), reindex, import/extract, import/record, projects/:id/clear, projects/:id/dedup. UI: GET+POST /ui/\*. Project resolution: 1) :id URL param, 2) ?git\_remote=, 3) ?path=. All pipeline handlers return internal Anthropic-format; translation to OpenAI/Responses format happens after pipeline returns. Total pipeline.ts lines: 4377.

<!-- lore:019e6904-e0e1-7689-bb52-7e8a0cb0edda -->
* **Gateway recall: Marker-and-Expand strategy — tool injection, interception, and round-trip**: Recall tool injected by gateway pipeline (pipeline.ts), NOT OpenCode plugin. Injection condition: \`modifiedReq.tools.length > 0 && !clientHasRecallTool(tools)\`. Strategy: (1) Response: recall \`tool\_use\` replaced with marker text via \`replaceRecallWithMarker()\`; result stored in \`sessionState.recallStore\`. (2) Next request: \`expandRecallMarkers()\` restores \`tool\_use\`+\`tool\_result\` pairs before forwarding. \`buildRecallFollowUp()\`: assistant message = ORIGINAL \`tool\_use\` block; user message = \`tool\_result\` block with recall results (NOT plain text) — prevents LLM echoing raw recall formatting during streaming. \`hasContinuationAfter\` logic: if blocks follow marker, truncates/splices; else prepends tool\_result to next user msg. \`RecallAwareAccumulator\` suppresses recall SSE blocks. MAX\_RECALL\_DEPTH=10. Follow-up requests: \`cacheConversation: false\`.

<!-- lore:019e6afa-26e4-772c-a432-5273b52fdb71 -->
* **Gateway session eviction: per-module cleanup functions and memory profile**: Gateway session eviction: per-module cleanup functions and memory profile: Gateway accumulates per-session state in 13+ Maps/Sets across 8 modules with zero eviction. Memory dominated by \`gradient.prefixCache.cachedText\` (10-50KB), \`distillationSnapshot.rows\`, \`cacheAnalytics.lastRequestBody\`. Per-module cleanup: \`deleteSessionAuth\`, \`deleteSessionCosts\`, \`deleteBillingPrefix\`, \`clearWarmupAuthDisabled\`, \`distillLimiter.evict()\`, \`curatorLimiter.evict()\`, \`evictQuotaCache(fingerprint)\`. Configurable via \`LORE\_SESSION\_EVICTION\_TIMEOUT\` (default 1800s). Sub-agents evict after \`SUBAGENT\_EVICTION\_MS=5min\`. GOTCHA: \`parsePositiveInt\` rejects \`0\` — \`LORE\_SESSION\_EVICTION\_TIMEOUT=0\` silently uses 1800s; use \`parseNonNegativeInt\`. quota.ts: \`fetchOAuthQuotaSnapshot()\` queries \`GET /api/oauth/usage\` with bearer + \`anthropic-beta: oauth-2025-04-20\`. \`utilization\` field may be 0.0–1.0 fraction OR 0–100 percent — normalize with \`>1 ? value : value\*100\`. \`resets\_at\` may be ISO-8601 string or Unix epoch number. 403 may indicate missing \`user:profile\` scope — re-mint token via \`claude /login\`. \`isAnthropicOAuthSession()\` required for quota checks.

<!-- lore:019e5544-4bc6-7ae6-ba2c-8d4493f6e1cf -->
* **gradient.ts: consecutiveBusts lifecycle — init, mutation, persistence, and DB-restore skip**: \`resolvedConversationTTL: '5m'|'1h'\` on \`SessionState\` (translate/types.ts:253). TTL resolution block in pipeline.ts lines 3444–3490: initialized from \`sessionState.resolvedConversationTTL ?? '5m'\`. Config \`'5m'\`/\`'1h'\`: pass through. Config \`'auto'\`: reads \`sessionState.coldCacheWindow\` (requires ≥5 samples). Auto-upgrade: \`coldFraction>0.4 && current==='5m'\` → immediate upgrade to \`'1h'\`, resets \`ttlDowngradeStreak=0\`. Auto-downgrade: \`coldFraction<0.2 && current==='1h'\` → hysteresis: requires 3 consecutive qualifying turns (\`streak≥3\`); rationale: TTL change modifies cached bytes AND drops idle threshold from 60min to 5min, causing compounding cache bust. \`consecutiveBusts\` in gradient.ts is separate from TTL streak. \`resolvedConversationTTL\` used in pipeline.ts, idle.ts, sentry.ts, cache-warmer.ts. Key \`SessionState\` fields (translate/types.ts:240–325): \`ttlDowngradeStreak\` (257), \`isSubagent\` (265), \`outputTokensEMA\` (285, α=0.3), \`consecutiveTextOnlyTurns\` (297), \`warmup: WarmupState\` (300), \`survivalModel: InterTurnHistogram\` (302), \`amnesia\` (318), \`\_dirty\` (324).

<!-- lore:019e7013-a9ea-72e6-8e8c-17ef1848e4e0 -->
* **llm-adapter.ts: retry logic, backoff schedule, and circuit breaker integration**: \`TRANSIENT\_CODES={429,500,502,503,529}\`; \`AUTH\_ERROR\_CODES={401,403}\`. \`MAX\_RETRIES\_RATE\_LIMIT=3\`; \`MAX\_RETRIES\_SERVER=3\`; \`MAX\_RETRIES\_URGENT=2\`. \`RETRY\_AFTER\_CAP\_URGENT\_MS=8\_000\`; \`RETRY\_AFTER\_CAP\_BACKGROUND\_MS=120\_000\`. \`backoffMs()\`: Retry-After present → \`min(retryAfterMs, cap)\`; urgent (no header) → \`min(1000×2^attempt, 4000)\`; 429 non-urgent → \`min(60\_000+attempt×60\_000, 180\_000)\`; 5xx → \`min(1000×2^attempt, 8000)\`. On 429 non-urgent → \`tripCircuitBreaker(pauseSec)\` before retry. Auth errors → \`markAuthStale\`, re-resolve, retry once if \`credentialChanged && attempt===0\`. \`buildAnthropicWorkerRequest()\`: injects billing block only when \`cred.scheme === 'bearer'\`; system sent as block array with \`cache\_control: { type: 'ephemeral', ttl: '1h' }\`; body signed via \`signBody()\`. \`opts.thinking\` intentionally NOT forwarded in bare API fetch. \`activeWorkerCalls=Set\<string>\` tracks worker session IDs.

<!-- lore:019e591d-acda-7f7d-b97b-85ec594a1445 -->
* **LLMClient.prompt() opts: urgent, thinking, workerID, maxTokens, temperature semantics**: \`LLMClient.prompt()\` opts: \`urgent:true\` = bypass batch queue — blocking callers (compaction, overflow recovery, query expansion) must set it; background workers must NOT. \`thinking:false\` = disable extended thinking — ALL background workers always set this. Only gateway's \`BatchLLMClient\` honors \`urgent\`; OpenCode/Pi adapters always process immediately. \`generateCompactionSummary()\`: \`compactMaxTokens = max(2048, min(ceil(inputLen/3 \* 0.5), 20\_000))\`; \`urgent:true, callType:'direct', temperature:0\`. Query expansion (\`search.ts\`): \`urgent:true\`. \`scheduleBackgroundWork()\`: urgent distillation bypasses \`isBackgroundPaused()\`; incremental distillation uses \`callType: batchQueueEnabled ? 'batch' : 'direct'\`; curation multiplier: \`inputCost >= 5 → 3x\`, \`>= 1 → 2x\`, else \`1x\`.

<!-- lore:019e5715-cccd-7be2-a979-407066c526d4 -->
* **log.ts: sink bridge, file rotation, and error visibility rules**: \`packages/core/src/log.ts\` (170 lines). Key rules: (1) \`log.error()\` \*\*always\*\* calls \`console.error()\` — 'Always visible — these indicate real failures.' \`log.info()\`/\`log.warn()\` only print if \`LORE\_DEBUG=1\`. (2) \`sink?.captureException(err)\` called only when an \`Error\` instance found in args via \`findError()\` — gateway bridges this to Sentry via \`registerSink()\`. (3) File logging: all levels written to \`~/.local/share/lore/lore.log\` regardless of \`LORE\_DEBUG\`; rotated at 5 MB (\`LOG\_MAX\_BYTES\`) with single \`.log.1\` backup; disabled in \`NODE\_ENV=test\`. (4) Only one \`LogSink\` supported at a time. Trap: any \`log.error(..., errorObj)\` auto-forwards to Sentry — including expected shutdown/auth errors. Use typed error subclasses (e.g. \`LocalProviderUnavailableError\`) to filter in \`beforeSend\`.

<!-- lore:019e54f5-04be-7a9f-9e62-177d65d5db9f -->
* **LTM forSession() budget, phases, and safety-net constants**: \`forSession()\` (ltm.ts): budgets: \`budget.ltm\`=0.05, \`budget.preferenceLtm\`=0.02. Phase 1: architecture entries up to ARCH\_BUDGET\_FRACTION=0.2. Phase 2: remaining entries by score descending. Phase 3: .lore.md sections. HEADER\_OVERHEAD\_TOKENS=15 subtracted upfront. Hard cap: \`if (used+cost>maxTokens) continue\`. PROJECT\_SAFETY\_NET=5: top-confidence project entries ALWAYS included even with zero relevance score — intentional design, guards against coarse term-overlap scoring excluding important project knowledge. NO\_CONTEXT\_FALLBACK\_CAP=10. Global entries (pid=null) must be cross\_project=1. \`vectorSearch()\` has no project\_id filter; dedup threshold for Nomic v1.5 is 0.935. GOTCHA: \`vectorSearch()\` queries \`knowledge WHERE embedding IS NOT NULL AND confidence>0.2\` — test suites must \`DELETE FROM knowledge WHERE embedding IS NOT NULL\` in beforeEach. Pool SQL: project pool uses \`cross\_project=0\`; cross pool uses \`project\_id IS NULL OR cross\_project=1\`. Promoted entries (cross\_project=1) lose PROJECT\_SAFETY\_NET guarantee in their home project — semantic regression to be aware of.

<!-- lore:019e4c7d-bfae-7c96-a491-e57cef38d84d -->
* **pipeline.ts: per-turn auth binding, billing prefix capture, and session bootstrap sequence**: pipeline.ts key behaviors: (1) \`extractAuth()\` → \`setLastSeenAuth\` + \`setSessionAuth\` every request. (2) \`captureBillingPrefix(sessionID, req.system)\` at line 2945, then \`captureSessionHeaders\`. (3) New sessions: \`fingerprintMessages()\`, \`learnHeaders()\`, \`tryImportKnowledge(projectPath)\`. (4) \`stripContextWarnings()\` before forwarding. (5) 1h TTL → \`effectiveCacheWriteCost = cacheWriteCost \* 2\`. Daily budget throttle (lines 3500–3540): \`getDailyBudget()\` → \`estimateRequestCost\` → \`getDailyThrottleDelay(dailyBudget, estimatedCost, quotaPressure)\` — quota pressure flows through even when \`dailyBudget=0\`; sleep skipped if ≤0.5s; delay capped at 50% remaining cache TTL. Inline workers gated by \`isQuotaPaused(fingerprint)\` when quota pressure ≥ 0.95. \`handlePassthrough\` bypasses cache-TTL resolution block. \`getLLMClient()\` lazy singleton; wraps with \`createBatchLLMClient()\` unless \`LORE\_BATCH\_DISABLED=1\`. Total lines: 4377.

<!-- lore:019e5922-4280-76aa-9b79-5ebeae433d7a -->
* **Sub-agent session detection, isolation, and differential treatment**: Sub-agent sessions detected via \`x-parent-session-id\` header (pipeline.ts:2773-2809). Each sub-agent gets independent session: temporal storage, gradient state, distillation pipeline, LTM injection, cost tracking. Key differential treatment: (1) cache warming ALWAYS skipped — \`shouldWarm()\` returns false unconditionally (cache-warmer.ts:731-733); (2) \`findRotationPredecessor()\` (session.ts:505) skips sub-agents from Tier 1b rotation; (3) idle distillation/curation runs normally (no isSubagent check). \`isSubagent\`/\`parentSessionId\` persisted to DB (migration v26). Dashboard: collapsible cost-rollup tree via \`loadParentChildMap()\` + \`buildLiveSessionRows()\`; \`rollUp()\` propagates grandchildren costs bottom-up.

<!-- lore:019e6f25-51b7-7b3c-8461-dfec7c531902 -->
* **Worker credential routing: LORE\_WORKER\_API\_KEY, LORE\_WORKER\_UPSTREAM, LORE\_WORKER\_MODEL env vars**: Worker credential routing (PR #482): Three env vars routing background worker LLM calls independently from session credentials, wired through \`getLLMClient()\` in pipeline.ts: \`LORE\_WORKER\_API\_KEY\` → replaces \`resolveAuth\` for all worker calls (always returns \`{ scheme: 'api-key' }\`, bearer bypass in batch-queue never triggers); \`LORE\_WORKER\_UPSTREAM\` → overrides both anthropic+openai URLs; \`LORE\_WORKER\_MODEL\` → checked first in \`getWorkerModel()\` before config-file override (format: \`providerID/modelID\`). Zero changes needed in \`@loreai/core\`, \`auth.ts\`, \`llm-adapter.ts\`, or \`batch-queue.ts\`. LOREAI-GATEWAY-Z: OAuth tokens don't support Batch API; bearer bypass added in PR #502 — \`cred.scheme === 'bearer'\` at enqueue time skips queue, goes direct. Circuit breaker only trips on 429, not 401.

<!-- lore:019e4bbd-ade4-7355-8fb1-87df54c8ea69 -->
* **X-Lore-\* headers: project path injection and resolution priority**: X-Lore-\* headers and project path resolution: Priority in \`packages/gateway/src/config.ts\`: 1) \`X-Lore-Project\` header (absolute path, max 1024 chars, strips trailing slashes/control chars), 2) inferred from system prompt (\`cwd\` JSON key, \`Working directory:\` prefix, \`.lore.md\` path, generic \`/home/\`/\`/Users/\` paths), 3) \`process.cwd()\` fallback. Source tagged as \`ProjectPathSource\`: \`"header"|"inferred"|"cwd"\`. Pipeline warns on \`cwd\` source (dedup via \`cwdWarned\` Set). \`agents.ts\` \`envVars()\` injects \`X-Lore-Project\` + \`X-Lore-Git-Remote\` into \`ANTHROPIC\_CUSTOM\_HEADERS\`. Other headers: \`x-lore-session-id\`, \`x-lore-no-store\`, \`x-lore-upstream-url\`, \`x-lore-recall-invoked\` (response). \`lore run\` always runs locally.

### Gotcha

<!-- lore:019e5b19-138a-7e00-ab9e-720769924303 -->
* **bootstrapDailySpend() overcounts multi-day sessions and has double-count risk**: cost-tracker.ts design and gotchas: (1) v30 \`daily\_costs\` ledger (day, bucket, cost; PK=(day,bucket)) replaces session\_state cumulative sum — records cost at exact UTC day incurred. \`bootstrapDailySpend()\` reads ledger only (no session\_state fallback — that was the over-count bug). \`addDailyCost()\` guard: \`!Number.isFinite(cost) || cost <= 0\` (rejects negative+zero). (2) \`computeThrottleDelay\`: \`if (spendFraction>=1.0) return MAX\_THROTTLE\_DELAY\`; pressure²×tanh(overshoot/3) formula. (3) \`getDailyBudget()\` priority: LORE\_DAILY\_BUDGET env → KV \`'daily\_budget'\` → 0. (4) \`aggregateDistillationsBySession\` SQL includes \`SUM(CASE WHEN call\_type='batch' THEN token\_count ELSE 0 END) as batch\_tokens\` — proportional split by call count diverges >10% when token distributions differ. (5) Compaction formula \`1+floor((total-threshold)/stride)\` overestimates for large messages (overshoots not modeled). (6) \`HISTORICAL\_CACHE\_TTL\_MS\`=5min. \`DAILY\_BUDGET\_KV\_KEY='daily\_budget'\`.

<!-- lore:019e6a44-dbf1-7df6-ba60-94f75c21b81d -->
* **consolidationUser() batchTarget frame-of-reference mismatch: global target vs batch count produces negative excess**: Trap: \`batchTarget\` in batched consolidation was computed globally (\`Math.max(cfg.curator.maxEntries, entries.length - CONSOLIDATION\_BATCH\_SIZE/2)\` = 1118 for 1143 entries) but \`consolidationUser()\` computed \`excess = count - targetMax\` where \`count\` is the batch size (50). Result: \`excess = 50 - 1118 = -1068\` — prompt tells LLM 'must remove at least -1068 entries', LLM returns empty ops, idle scheduler enters 1h cooldown, system never converges. Fix: \`batchTarget\` must be batch-relative — \`Math.ceil(CONSOLIDATION\_BATCH\_SIZE / 2)\` so \`excess = 50 - 25 = 25\`. Fixed in e8a73f9. CONSOLIDATION\_BATCH\_SIZE=50 (curator.ts:643). Non-batched path (entries ≤ BATCH\_SIZE) correctly uses \`cfg.curator.maxEntries\` as target.

<!-- lore:019e6a1b-3408-7609-ba6b-d4ba9d552afd -->
* **curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields**: curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields: Trap: \`filterOps()\` (curator.ts:109–117) only checks \`op\` is a string — does NOT validate required fields (\`content\`, \`title\`, \`category\`) for \`create\` ops, casting result as \`CuratorOp\[]\` silencing TypeScript. When LLM emits a malformed \`create\` op missing \`content\`, \`applyOps()\` line 226 accesses \`op.content.length\` without null guard → \`TypeError: undefined is not an object\`. Fix: add null guard at line 226 AND field validation in \`filterOps()\` for \`create\` ops. The \`update\` branch at line 252 already has \`op.content !== undefined &&\` guard — \`create\` branch lacks equivalent. \`applyOps()\` calls \`ltm.syncRefs(id)\` and \`entities.syncEntityRefs(id, content)\` after each create/update; \`skipCreate\` flag skips creates (used by consolidation); cross-project guard prevents mutating foreign-project entries. \`cfg\` is in scope in \`runInner\` at line 414.

<!-- lore:019e50e7-6250-779e-a842-b1b5baf87295 -->
* **ONNX OOM error code 284432024: single oversized text bypasses batch budget guard**: ONNX OOM fix chain (Nomic v1.5 pads all batch texts to longest sequence): (1) \`LOCAL\_MAX\_CHARS=16384\`; (2) Worker OOM-retry: attempt=0 full texts; on OOM, \`maxTokens=4096>>attempt\` → 4096→2048→1024; \`OOM\_MAX\_RETRIES=3\`; (3) \`truncateTexts(texts,maxTokens)\`; (4) \`nextBatch()\` adaptive batching caps \`batch\_size×max\_tokens\` at \`MAX\_BATCH\_TOKEN\_AREA=4096\`; (5) \`safeLocalTruncate()\` backs up 1 char on high surrogate boundary. \`isOomError()\` matches \`/^\d{6,}$/\` and \`/out.of.memory|alloc.\*fail|oom/i\`. WASM \`Aborted()\` NOT caught by \`isOomError()\` — use \`isWasmFatalError()\` matching \`/Aborted\\(\\)/i\`; post \`{type:'init-error'}\`, call \`process.exit(1)\`. \`shutdown()\` must reject with \`LocalProviderUnavailableError\` (not plain Error) to avoid Sentry noise. Bun NAPI crash on \`process.exit()\` with fastembed: use \`safeExit(code)\` from \`packages/gateway/src/cli/exit.ts\` (uses libc \`\_exit()\` via bun:ffi). Do NOT call \`embedding.resetProvider()\` in test teardown — only in \`shutdown()\` in \`start.ts\`.

<!-- lore:019e4eab-59b4-7fe3-be03-874f682cc340 -->
* **openai.ts: array-format content in system/developer messages silently dropped**: (gotcha) openai.ts / openai-responses.ts parsing traps: (1) \`parseOpenAIRequest\` (openai.ts:67–69) coerces non-string content to \`""\` — array-format content in \`system\`/\`developer\` messages silently dropped. Fix: check \`Array.isArray(content)\` and extract text parts. (2) \`buildOpenAIMessages\` (openai.ts:523–529): \`tool\_result\` blocks pushed mid-loop before \`textParts\`/\`toolUses\` flushed — tool\_result appears before text/tool\_use of same message. (3) \`buildOpenAIMessages\` always re-emits system as \`role:'system'\`, never \`'developer'\`. (4) \`parseOpenAIResponsesRequest\` reads system prompt from top-level \`instructions\` field — \`developer\`/\`system\` role messages in input array treated as regular user messages. LTM injection for Responses API: \`\[req.system, cache?.stableLtmSystem, cache?.ltmSystem].filter(Boolean).join('\n\n')\`.

<!-- lore:019e6ff7-3fcd-7cf6-84f3-632073dba9d6 -->
* **Pi plugin baseUrl mismatch causes 404 on /v1/responses — OpenAI SDK appends path to bare gateway root**: Trap: \`pi.registerProvider('openai', { baseUrl: 'http://127.0.0.1:3207' })\` looks correct because it points to the gateway. But OpenAI SDK's \`responses.create()\` concatenates \`baseURL + '/responses'\` → sends to \`http://127.0.0.1:3207/responses\`. Gateway routes only on \`pathname === '/v1/responses'\` (server.ts:316) → falls through to 404: 'No route for POST /responses'. Fix: Pi must set \`baseUrl: 'http://127.0.0.1:3207/v1'\` (with \`/v1\` suffix) so SDK produces the correct \`/v1/responses\` path. The \`/v1/responses\` route has existed since commit b8a4836 (pre-0.18.0) — this is a Pi plugin configuration bug, not a missing gateway route.

<!-- lore:019e6950-8200-7473-87a5-45a861bb84af -->
* **recall.ts: unescaped quotes/ellipsis in marker strings cause store miss and raw marker leak**: Trap: \`buildRecallMarker()\` embeds raw \`query\` between double-quotes and raw \`id\` before \`…\` — looks safe because queries are usually plain text. But if \`query\` contains \`"\`, \`MARKER\_REGEX\` (\`/📚 Searching (.+?) for "(.+?)"…/\`) truncates the captured group at the first inner quote; \`recallStoreKey()\` produces a mismatched key; \`store.get(key)\` returns undefined; the marker is left as raw text in the conversation visible to the upstream LLM. Same issue for \`id\` containing \`…\`. Fix: escape \`"\` → \`\\"\` in \`buildRecallMarker()\` and unescape in \`parseRecallMarker()\`; for IDs, percent-encode or strip \`…\` before embedding.

### Pattern

<!-- lore:019e56d6-9cca-7a77-aa1a-a5850daef395 -->
* **Gateway test patterns: unit vs integration vs subprocess isolation**: Gateway test patterns in \`packages/gateway/test/\`. Three patterns: (1) Unit: import functions directly, build fixtures inline — \`makeRequest()\`/\`makeUserMsg()\` factories. (2) Integration: \`createHarness()\` starts real server on random port with replay interceptor, provides \`chat()\` + \`queryDB()\`. (3) Subprocess isolation: \`mock.module()\` calls isolated in subprocess files to avoid global cache pollution. DB isolation via root \`bunfig.toml\` preloading \`packages/core/test/setup.ts\`. \`GatewayRequest\` minimal required fields: \`protocol, model, system, messages, tools, stream, maxTokens, metadata, rawHeaders\`. Use port \`0\` in tests. Session ID regex requires \`\[a-f0-9]\` hex chars only — alphanumeric IDs with non-hex chars (e.g. \`g-z\`) will fail marker extraction.

### Preference

<!-- lore:019e8be5-0f0b-717b-9ac8-a68505bb4d9b -->
* **Always end planning turns with plan\_exit call**: When the user activates Plan mode, they consistently require the assistant to call \`plan\_exit\` at the end of every planning turn to signal completion. This is a mandatory workflow step — the assistant must never leave a planning turn without either asking a clarifying question or calling \`plan\_exit\`. The user has explicitly stated this directive multiple times across sessions and flags it as a hard requirement (🔴 priority). The plan file path is always provided at session start and should be referenced throughout planning. Plan files are stored at \`/home/byk/Code/opencode-lore/.opencode/plans/\<timestamp>-\<name>.md\`.

<!-- lore:019e2168-2fa4-77bd-a557-9d6dbcb40d81 -->
* **Prefer WASM backend over native onnxruntime-node for compiled binaries**: WASM backend for Bun \`--compile\` binaries with transformers.js: \`binaryExternalsPlugin\` in esbuild redirects \`onnxruntime-node\` → \`onnxruntime-web\` via \`onResolve\` (static imports only — does NOT redirect dynamic \`import()\` calls) and patches transformers.js CDN fallback via \`onLoad\` to read \`wasmPaths\` from \`globalThis.\_\_LORE\_VENDOR\_WASM\_PATHS\_\_\` (object form \`{ mjs, wasm }\` with exact hashed \`$bunfs\` filenames — directory strings fail). WASM files embedded as Bun \`{ type: 'file' }\` assets. For npm/CJS builds, \`onnxruntime-node\` stays external. WASM is ~2x faster on batches than native. Importing \`onnxruntime-web\` explicitly alongside the redirect creates two ort instances — 'cannot register backend cpu using priority 10' error.

<!-- lore:019e5aa0-1e5f-7da9-a1f4-5b3b3f72b8e9 -->
* **Review code before committing**: Code review and workflow preferences: (1) Adversarial pre-merge review — severity-classified (CRITICAL/MEDIUM/LOW), file:line refs, concrete fixes; check PR description accuracy, test coverage, race conditions, type safety, SQL bugs. (2) Investigate Sentry issues by reading full source, tracing call chains, enumerating all root causes before fixes. (3) Run \`bun --filter '\*' typecheck\` (all 4 packages must pass) and full test suite before PR creation; note exact pass/fail/skip counts; distinguish pre-existing failures. (4) Verify all CI checks pass before merging. (5) Prefer update over create for knowledge entries. (6) Prefer minimal viable scope — gradual throttling over hard blocks, EMA signals over step functions. (7) Defer architectural changes when existing mechanisms already handle the case. (8) Always produce a structured implementation plan (file paths, line numbers, tasks) before making code changes — enumerate every affected file, functions/lines, backward-compatibility constraints, new module wiring. (9) Annotate architectural facts with source file, line numbers, and exact values. (10) Trace whether functions are actually called/wired up — flag dead code as a design con \[truncated — entry too long]
