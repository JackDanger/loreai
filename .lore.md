<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019eb931-7257-7ef6-87db-b2dd034c3ea7 -->
* **embedding-worker.ts: self-heal retry for corrupt model — skipped for vendored binaries**: embedding-worker.ts self-heal retry: \`initPromise\` wraps \`loadPipeline()\`; on catch, if \`!vendorModel && isCorruptModelError(msg)\`, calls \`purgeCachedModel()\` then retries once. NEVER post \`{type:'init-error'}\` before the retry — \`embedding.ts\` sets \`localProviderKnownBroken=true\` permanently on init-error. Only the final \`.catch\` may post \`init-error\`. Error classifiers are INLINED — Node ESM resolver cannot map \`./foo.js\` → \`./foo.ts\` for spawned workers. TRAP: \`/load model .\* failed/i\` is too broad — misclassifies 401/403 → purge→redownload→401 loop. \`embedding-worker.ts\` cannot be imported in tests (throws at module top-level if \`!parentPort\`). \`vendorModel=true\`: skip all purge/heal paths (model is read-only inside binary).

<!-- lore:019ebdd7-b5a4-7b44-840d-2bd981d368ff -->
* **gradient.ts: selectDistillations() always preserves meta (gen>=1) entries — never evicted by recency scoring**: gradient.ts selectDistillations() always preserves meta (gen>=1) entries — never evicted by recency scoring. distLimit slots = limit - meta.length; gen-0 scored by recency (70%) and content signal (30%). Meta guarantee retains session history (fixes #417). distilledPrefixCached three-branch: (1) no new rows → cache hit, (2) new gen-0 → append delta (prepends headers, causes duplication in messages\[1]), (3) full re-render on meta rewrite. Cache validity: same sessionID, rowCount grew, last cached row unchanged. Layer 0 (warm-cache) does not inject prefix — only Layer 1+ (compression mode).

<!-- lore:019ecb52-ff6e-7e2a-aac9-f9699285b09d -->
* **gradient.ts: unsustainable conversation detection and warning injection**: gradient.ts unsustainable detection: recordCacheUsage() bust = cacheWrite/(cacheWrite+cacheRead+inputTokens) > 0.5; consecutiveBusts increments on bust, resets on non-bust or zero-write turns. SUSTAINED\_BUST\_THRESHOLD=5. Tier gate: entered when effectiveMinLayer===0 AND layer0Input > layer0Ceiling AND layer0Input <= maxInput\*HARD\_CEILING\_MARGIN (0.95). compressedEstimate = Math.min(distilledBudget + rawBudget, layer0Ceiling) — MUST be clamped; without clamp large-context models produce estimate >> 200K ceiling, shouldCompress skips every turn. If pricing not set (both costs=0), shouldCompress() returns false → unbounded growth.

<!-- lore:019ecab6-f934-757f-a73e-2a14234d8c4f -->
* **lore gateway: thinking parameter handling — never forwarded in bare API calls, lives in req.metadata**: \`thinking\` is NOT in \`KNOWN\_BODY\_FIELDS\` (anthropic.ts:25-36). \`parseAnthropicRequest\` puts unknown fields into \`req.metadata\` (lines 226-229) — \`thinking\` arrives as \`req.metadata.thinking\` with shape \`{type:"enabled", budget\_tokens:N}\`. \`buildAnthropicRequest\` egress (lines 486-488) restores metadata fields back to body. Bare API calls NEVER include \`thinking\` (llm-adapter.ts:897-899). \`cache-warmer.ts\` detects thinking via \`"thinking" in body\`; uses \`max\_tokens:1\` for thinking sessions — LATENT BUG: \`max\_tokens=1\` violates Anthropic's \`max\_tokens > budget\_tokens\` when \`budget\_tokens>1\`, causing 400 on every thinking-session warmup (non-fatal, circuit-breaker protected). Meta request passthrough MUST NEVER enable caching (anthropic.ts:262-263). \`resignBody\` gate (pipeline.ts:2628) uses \`hasBillingHeader(req.system)\` — cache-warmer.ts:1381 calls \`resignBody\` directly, bypassing this gate entirely.

<!-- lore:019ecc2b-da14-7017-a156-5ce6c4201fcc -->
* **Lore Website: Mermaid rendering uses CDN ESM build with client-side init in astro.config.mjs head**: Lore docs site (Astro + Starlight) renders Mermaid via a CDN ESM script in \`astro.config.mjs\` head injection. URL: \`https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.esm.min.mjs\`. Init config: \`{startOnLoad:false, theme:"neutral", securityLevel:"loose", fontFamily:"var(--sl-font)"}\`. Per-block workflow: query \`pre\[data-language="mermaid"]\` → extract source by joining \`.ec-line\` textContent with \`\n\` → \`await mermaid.render(id, source)\` → replace pre with \`\<div class="mermaid">{svg}\</div>\`. Random ID: \`mermaid-\` + base36 slice(2,9). CSS in \`starlight.css\` sets \`.mermaid { display:block; text-align:center; margin:1.5rem auto; max-width:100%; overflow-x:auto; }\`. Theme-aware switching is a known follow-up.

<!-- lore:019ebc36-830a-7307-8a10-9ff12dfa5740 -->
* **openai-responses.ts: Codex delta must live in a dedicated wrapper, not inline conditionals**: openai-responses.ts: Use \`buildOpenAICodexUpstreamRequest\` wrapper and \`applyCodexResponsesDelta\` helper over inline \`req.codex\` guards. Codex has 4+ divergences: URL \`/codex/responses\`, \`store:false\`, control fields passthrough, \`max\_output\_tokens\` suppression. \`applyCodexResponsesDelta\` mutates body post-build. Guard \`if (!extras) return\` runs AFTER setting \`store=false\` — so \`store:false\` always applies even when \`req.extras\` is absent. TRAP: no test that control fields (\`include\`, \`prompt\_cache\_key\`, etc.) are still emitted after refactor — regression could silently drop them. Always add regression tests when fixing Codex builder bugs: (1) suppressed field absent even when client sends it, (2) non-codex path still emits the field, (3) worker-side asserts field absent.

<!-- lore:019ecadd-eaea-7689-896a-280bd489ef3f -->
* **opencode-lore plugin: dev workflow uses bun bundle, not standalone binary**: Burak uses lore locally via the \`@loreai/opencode\` plugin (symlinked from \`~/.config/opencode/node\_modules/opencode-lore\` → \`/home/byk/Code/opencode-lore\`), NOT the standalone \`lore\` binary. Plugin \`main: ./src/index.ts\` (raw TS), depends on \`@loreai/core\` via built dist. Gateway logic lives in \`packages/gateway/dist/index.bun.js\` (ESM/Bun bundle). After merging gateway changes, rebuild bundles via \`pnpm build\` in \`packages/gateway/\` — NOT the SEA binary. Trap: installing a new SEA binary replaces the wrong artifact; the plugin reads the bun bundle, not the binary. PRs: squash merge only after CI is fully green; sync local main and rebuild bundle after merge.

<!-- lore:019ebc96-83c7-7cd5-98d2-ca85724c2a85 -->
* **pipeline.ts: two curation paths — in-flight vs idle — must be gated separately**: pipeline.ts: Two curation paths — in-flight (mid-turn, gated by \`sessionState.curationScheduled\`) and idle (on idle, flush gen-0 then metaDistill) — must be gated separately. In-flight avoids system\[2] cache busts during active conversations. System\[2] churn root causes: context-health note appended after diff-pinning fluctuates by layer, LTM entry selection changes per-turn. In-flight gen-0 distillation mutates messages\[1] in compression mode. Warm-cache DB divergence: when pin reused, \`ltmCacheText=NULL\` while \`ltmPinText\` populated causes spurious re-pin; fix: persist pinned text. Volatile delta channel (PR #747): keep system\[1]/system\[2] pinned, surface changed/new entries as ID-referenced delta in always-rewritten conversation tail.

<!-- lore:019ecb52-ff7c-7515-a435-be511f6fdde7 -->
* **session.ts: 3-tier session ID strategy, generateSessionID format, and marker functions**: 3-tier session ID strategy (session.ts): Tier 1 known headers (priority order): \`x-lore-session-id\`, \`x-claude-code-session-id\`, \`x-session-id\`, \`x-session-affinity\`. ROTATION\_ELIGIBLE\_HEADERS (lines 272-274): ONLY \`x-session-affinity\`. \`x-claude-code-session-id\` is explicitly NOT rotation-eligible — a new value is ALWAYS a genuinely new session, never merged into existing (cross-project contamination risk). \`isClaudeCodeClient(rawHeaders)\` (lines 238-242): returns \`!!rawHeaders\["x-claude-code-session-id"]\`; doc comment (lines 234-236) warns callers to additionally check \`hasBillingHeader()\` on system prompt for edge cases (Claude Code OAuth without session header). \`isCC\` in pipeline.ts:5153 = \`isClaudeCodeClient(req.rawHeaders) || hasBillingHeader(req.system)\`. Tier 2: learned headers promoted after stable 3 turns. Tier 3: SHA-256 of first user message + auth suffix. generateSessionID(): 12 bytes = 8 random + 4-byte big-endian unix timestamp, base62.

<!-- lore:019ec7fc-3bab-7e84-b5b3-e8e9d802a806 -->
* **Supabase auth constraints: email templates blocked on free tier; no device flow**: Supabase auth constraints: Free tier blocks email template customization (HTTP 400); custom SMTP live: \`smtp\_host: mail.smtp2go.com\`, sender \`keeper@withlore.ai\`. Email templates pushed via Management API PATCH (4 fields only). \`supabase config push\` clobbers entire auth config — use Management API PATCH for targeted updates. GitHub OAuth must be enabled separately in Auth → Providers → GitHub. OTP length configurable (not fixed at 6 digits) — observed 8-digit tokens; use \`/^\d{4,12}$/\`. \`lore login --email\` active once custom SMTP configured. Supabase project ref: \`jlwxsrmvomocgngbxmcf\`, region \`us-east-2\`. \`config.toml\` auth settings only affect \`supabase start\` (local stack) — dashboard is authoritative for production. Email templates MUST include \`{{ .Token }}\` in body or users receive only a \`{{ .ConfirmationURL }}\` link. Redirect URLs: must include \`https://\` when not localhost; must include trailing \`/\`.

<!-- lore:019ecb58-7fc8-70ad-85eb-5c27233ec4e3 -->
* **sync engine: Supabase mirror schema (migration 0002) — 5 tables, RLS, triggers**: Supabase migration 0002 mirrors 5 SQLite tables to Postgres (entities, entity\_aliases, entity\_relations, knowledge, knowledge\_entity\_refs) with sync columns (owner\_user\_id, updated\_at, is\_deleted, content\_hash, revision). PKs composite (owner\_user\_id, id) except refs (owner\_user\_id, knowledge\_id, entity\_id). RLS enabled; owner\_all policy scoped to auth.uid() = owner\_user\_id. Migration 0003 adds profiles.tier, plan\_limits, BEFORE INSERT quota triggers (free: knowledge≤500, entities≤30, aliases≤300, relations≤300, refs≤2000). Known issues: profiles.tier user-writable (needs column GRANT + BEFORE UPDATE trigger), uncapped TEXT columns allow GB-scale blobs, sync tables need explicit DML GRANTs to authenticated or PostgREST returns 403.

### Decision

<!-- lore:019ecaa6-0dda-770a-b12c-2b61138af774 -->
* **Sync engine pieces 1+2+3 must land together — piece 1 alone is dead code with migration cost**: Chose to land sync pieces 1+2+3 together over merging piece 1 (commit f42ad40a, v43 migration) alone. Piece 1 has zero syncing capability: \`sync\_outbox\` triggers are gated behind \`sync.enabled='1'\` (nothing sets it yet), \`sync-data.ts\` has no callers, no Supabase client or network code. Merging alone runs the v43 migration for every user on upgrade with no offsetting benefit. Rejected: incremental merge of piece 1 as a foundation PR.

### Gotcha

<!-- lore:019ebc55-ba2e-7271-9906-905e4f5ed9bf -->
* **CCH seed extraction: seed may persist in binary but algorithm/preimage changed across versions**: CCH seed extraction: consult \`quality/CCH.md\`. Seed \`0x4d659218e32a3268\` (SEED\_2\_1\_138) unchanged in 2.1.175. Hash-input ≠ wire body — cch fn scans for \`cch=\` placeholder, splits/transforms before hashing. Binary is EXEC (not PIE), fixed base \`0x200000\`. CCH\_PLACEHOLDER = \`cch=00000\`; signBody replaces with hash; regex \`/cch=(\[0-9a-fA-F]{5});/\` (trailing semicolon required). Work files in stable dir (e.g. \`~/Code/cch-investigation/\`), not \`/tmp\`.

<!-- lore:019ebe5d-132f-7674-a47a-dd41c820fc07 -->
* **cch-seed-check.yml: @loreai/core must be built before scripts run**: Trap: \`scripts/check-cc-version.ts\` and \`scripts/extract-cch-seed.ts\` both import \`packages/gateway/src/cch.ts\`, which imports \`@loreai/core\` — resolving to \`packages/core/dist/node/index.js\`. The root \`postinstall\` only builds \`@loreai/gateway\`, never \`@loreai/core\`. So after \`pnpm install --frozen-lockfile\`, \`dist/\` is missing and the scripts crash with \`ERR\_MODULE\_NOT\_FOUND\`. Looks safe because \`pnpm install\` succeeds. Fix: add \`pnpm --filter @loreai/core run build\` after install in BOTH \`check-version\` and \`extract-seed\` jobs. Precedent: \`ci.yml\` line 501 uses the same pattern before \`check:docs\`.

<!-- lore:019eb4d0-6fd9-7ef5-b22f-0f156eeca4b0 -->
* **cli/main.ts OPTIONS map: undeclared flags silently become booleans under strict:false**: Trap: \`parseArgs\` with \`strict: false\` treats any undeclared \`--flag value\` as \`{flag: true}\` with \`value\` falling into positionals — looks correct because \`--flag=value\` (equals syntax) still works. Fix: every flag used in ANY subcommand MUST be declared in the global \`OPTIONS\` map in \`packages/gateway/src/cli/main.ts\` with its correct type (\`string\` or \`boolean\`). Discovered in PR #696: \`--to\`, \`--project\`, \`--dry-run\`, \`--no-children\`, \`--min-confidence\` were missing, causing \`--to /path\` to parse as \`{to: true}\` with \`/path\` as a positional.

<!-- lore:019ecab6-f928-7927-a39e-d601fb850af6 -->
* **computeMaxTokens: EMA-based floor ignores thinking budget, causing silent agent loop exit**: computeMaxTokens: EMA-based floor ignores thinking budget, causing silent agent loop exit. Trap: \`computeMaxTokens\` (pipeline.ts) floors \`max\_tokens\` at \`max(8192, 3×outputEMA)\`. Anthropic counts \`max\_tokens\` as combined thinking+visible budget. Model consumes entire 8192 on reasoning → \`finish:"length"\` → no text/tool part → agent loop exits silently. Looks like a normal completion. Fix: pass \`thinkingBudget\` to \`computeMaxTokens\`; set \`baseFloor = thinkingBudget + THINKING\_OUTPUT\_HEADROOM (8192)\`, clamped by \`modelOutput\`. \`THINKING\_OUTPUT\_HEADROOM = 8192\` constant; \`MAX\_TOKENS\_FLOOR = 8192\`. Only ONE \`req.maxTokens =\` write in conversation path (pipeline.ts:5056). \`thinking\` lives in \`req.metadata.thinking\` (not KNOWN\_BODY\_FIELD). Gate: \`if (!isCC)\` — CC clients excluded; opencode does NOT send \`x-claude-code-session-id\` so fix applies. EMA not corrupted by thinking truncation (Anthropic includes thinking tokens in output\_tokens).

<!-- lore:019ebb4a-397c-76aa-89c8-93110713f3d4 -->
* **formatKnowledge: title-collision within a category produces non-deterministic render order**: Trap: \`formatKnowledge\` sorts entries within each category by title for byte-stable rendering. If two entries share an identical title, the sort returns 0 and their relative order is non-deterministic — text can swap between turns, busting the prompt cache. This is an edge case (duplicate titles are rare) but worth guarding. Fix: add a secondary sort key (e.g. entry id) as a tiebreak after title comparison, mirroring the id-tiebreak already used in the preference fast-path packing loop.

<!-- lore:019ecc2b-da1e-7e3e-973e-1ed3e9643818 -->
* **JSDOM cannot fully render Mermaid SVG but CAN parse diagram syntax — use it for static verification**: Trap: Mermaid 11 requires \`CSSStyleSheet\` which JSDOM does not implement, so full SVG render throws \`CSSStyleSheet is not defined\`. Looks like a render bug, but it's a JSDOM limitation — Mermaid is browser-only. Fix: use JSDOM to load mermaid and call \`mermaid.parse(source)\` to verify syntax; trust actual rendering to a real browser. Setup gotcha: \`Object.defineProperty(window, 'navigator', ...)\` required (direct assignment throws "has only a getter"). Local \`mermaid\` package must be \`npm install\`ed into the working dir (not in repo node\_modules). Playwright alternative fails without system libs (libatk-1.0.so.0) and \`sudo\` is blocked by \`no new privileges\` flag.

<!-- lore:019ebafd-32b7-7468-bdb2-5c77ff6c4ccf -->
* **opencode-lore plugin: legacy export path causes getLegacyPlugins() to be invoked**: Trap: lore's plugin module exports named functions (\`applyLoreProviderConfig\`, \`probeGateway\`, \`LorePlugin\`) plus \`export default LorePlugin\`. OpenCode's \`getLegacyPlugins()\` iterates \`Object.values(mod)\` and treats ANY function as a server plugin (\`isServerPlugin\` = \`typeof === 'function'\`). So \`applyLoreProviderConfig\` and \`probeGateway\` are invoked as plugins; \`applyLoreProviderConfig\` returns \`undefined\` → pushed into \`hooks\[]\` → all dispatch loops (\`config\`, \`event\`, \`dispose\`) crash: \`j.config\` / \`A.event\` / \`j.dispose\` on undefined. Fix: lore's plugin must \`default export { server: LorePlugin }\` (plain object with \`server\` key) so \`readV1Plugin\` takes the v1 path and \`getLegacyPlugins\` is never called. Additionally, do NOT export helper functions (\`applyLoreProviderConfig\`, \`probeGateway\`) from the plugin's top-level index — they leak into the legacy loader.

<!-- lore:019ecc2b-da05-70bb-a2e8-216232d28cb7 -->
* **Starlight expressive-code breaks Mermaid blocks: per-line divs and class selector mismatch**: Trap: \`\`\`mermaid\`\`\` fences in Starlight get wrapped as \`\<pre data-language="mermaid">\` (not \`\<pre class="mermaid">\`) AND each line is wrapped in \`\<div class="ec-line">\`. So Mermaid 11's \`startOnLoad:true\` (default selector \`.mermaid\`) finds nothing, and even if you add the class, \`.ec-line\` divs contribute no newlines to \`textContent\` — naive source extraction concatenates lines into garbage Mermaid can't parse. Looks like a single bug. Fix: client-side script that queries \`pre\[data-language="mermaid"]\`, joins \`pre.querySelectorAll('.ec-line')\` textContent with \`\n\`, calls \`mermaid.render(id, source)\` per block, then \`pre.replaceWith(\<div class="mermaid">{svg}\</div>)\`. Rejected: disable expressive-code globally (breaks syntax highlighting), custom remark plugin (extra dep), build-time mermaid (browser deps in build env).

### Preference

<!-- lore:019ecbfa-4304-798f-829f-fa2f7e48a25b -->
* **Always assert system invariants as code comments with 'always'/'never' language**: Code style and review standards for the loreai monorepo: (1) Embed authoritative invariants as code comments using 'always'/'never'/'must' language with 🔴 markers — treat as ground-truth specs. (2) All code references must include exact file paths and line numbers. (3) Before merging, commission a critical subagent review (find flaws, not validate) producing MAJOR/Minor/Nit findings; address all MAJOR/Minor before merge. (4) Verify PR/commit claims against actual code — descriptions are often inaccurate. (5) Run Biome (\`biome check --write\`) before committing. (6) When identifying bugs, audit why they escaped existing tests and close coverage gaps. (7) After finishing work, run full test suite (all files, typecheck, zero failures) before closing out.

<!-- lore:019ecb89-06da-74f6-8200-5a3f80991164 -->
* **Always conduct adversarial security reviews with empirical confirmation and severity labels**: Always conduct adversarial security reviews with empirical confirmation and severity labels \[BLOCKER]/🟡/🔴. Enumerate attack vectors, confirm vulnerabilities. Key categories: RLS policy gaps, column-level privilege issues (DEFAULT auth.uid() does NOT prevent forgery — WITH CHECK does), trigger ordering bugs, constraint bypasses, missing GRANTs (PostgREST checks has\_table\_privilege() before RLS — missing grants return 403), DEFAULT vs explicit-column INSERT attacks, storage quota bypasses via uncapped TEXT columns, tier escalation via user-writable tier column. All BLOCKER/SHOULD-FIX findings must be addressed before merging.

<!-- lore:019ecc43-a0fb-7e8f-9d20-8a0fa2aa9808 -->
* **Always create a dedicated branch before raising a PR after completing a fix session**: PR landing workflow: After completing a fix session, create a named branch (pattern: \`fix/\<short-kebab-description>\`) off \`origin/main\` containing only relevant commits. Land all modified files cleanly, open a PR, watch CI until all checks pass, then notify teammate Onur. Handle stash/checkout mechanics automatically. \`.lore.md\` must never be stashed — commit it on the source branch first to clean the working tree before branching.

<!-- lore:019ecc12-dc50-7aa4-a1da-57f08e0548a2 -->
* **Always perform deep read-only code archaeology before making changes**: Always perform deep read-only code archaeology before making changes: Exhaustively read source files, grep for symbols across the codebase, document findings with line numbers and code snippets before proposing edits. Trace call sites, map data flow, identify variable scope, surface potential bug vectors — all read-only. Want full situational understanding before any implementation. PR reviews expect adversarial review with severity-tagged findings (BLOCKER/SHOULD-FIX/NIT). Debugging: systematically eliminate hypotheses by checking env vars, shell rc files, log sources, runtime state. Do not jump to fixes. Demand evidence-based exploration: any claim about the codebase must be backed by concrete file:line evidence. Flag when something isn't in the code. Prefer inspecting request content using existing helpers over refactoring call signatures when branching on session type.

<!-- lore:019ecbaa-2c3a-77f5-9a7d-b1c1e806e4db -->
* **Always request a local working build after merging a PR**: Git and landing workflow: (1) At session start, review git status and full diff before any implementation. (2) Land changes on a new focused branch off \`origin/main\` — never commit directly to working branch. Stash changes, branch from \`origin/main\`, pop stash, commit only relevant files. Exclude \`.lore.md\` and unrelated files. (3) After each commit, verify exact files changed, insertion/deletion counts, and commit hash. (4) After merging a PR, build and smoke-test immediately; check \`git status\` for commits behind origin/main. (5) After any git operation that could corrupt files (stash-pop conflicts, merges), verify actual file state empirically. Squash merge only after CI is fully green. \`.lore.md\` is frequently dirty due to background writes — exclude from commits and stash before pull/rebase.

<!-- lore:019ecc6a-8cb3-7855-966e-fee1529ea2ee -->
* **Always treat billing header position invariants as load-bearing assertions, not assumptions**: The user consistently asserts and re-asserts specific structural invariants about the billing header as hard facts that gate logic must rely on: (1) the real billing header is ALWAYS at system\[0]/offset 0, never elsewhere; (2) content copies can NEVER appear at offset 0; (3) LTM is ALWAYS appended, never prepended. When reviewing or writing code that touches \`hasBillingHeader\`, \`resignBody\`, \`signBody\`, or related gate logic, explicitly acknowledge these invariants in comments and ensure any new code or tests preserve them. Flag any code path that could violate offset-0 placement as a critical bug, not a minor issue.

<!-- lore:019ecc1d-a14b-7f97-b25a-0d1a86489e57 -->
* **Always write e2e tests that assert on specific text content rather than full serialized blocks when cache\_control metadata may vary**: When writing cache-stability e2e tests, the user expects assertions to target the specific text content being validated (e.g., quoted sentinel text, message body text) rather than full serialized message blocks that include \`cache\_control\` metadata. This is because \`cache\_control: {type: 'ephemeral'}\` legitimately moves between turns as cache breakpoints shift, and asserting on the full block causes false failures. The pattern: extract and compare only the \`.text\` field (or equivalent content field) of the relevant message, not the entire serialized block. This applies whenever the test is checking byte-stability of content across turns in the replay harness.

<!-- lore:019ecc24-f26c-792a-9e70-77c75bc216d3 -->
* **Preserve and reinforce architectural invariants exactly as stated**: The user repeatedly asserts specific invariants in their own words and expects them to be preserved verbatim in documentation and code. Examples from this session: 'The agent never knows it's there' (transparent proxy — architecture.md:12), 'distillations and long-term knowledge are derived from \[temporal storage], never the other way around' (three-tier memory data flow), 'always protected from stripping' (last 2 turns in layer 2), 'ALWAYS keeps full current turn' (layer 4 emergency). Diagrams: 'all these as mermaid graphs (we need to make sure we can render them in Astro)' — cross-session directive seen 24+ times. When editing architecture.md or related docs, quote the user's exact phrasing for these invariants rather than paraphrasing. Treat them as load-bearing requirements, not suggestions, and surface them as explicit constraints in plans and edits.

<!-- lore:019ebe56-3cfa-7b49-b786-3d603e608a37 -->
* **Test file conventions: Vitest, kebab-case, import style**: Always write new tests with \`import { describe, it, expect } from 'vitest'\`. Use kebab-case file naming for test files. Prefer writing more tests over lowering coverage thresholds or excluding files. Project migrated from Mocha+Chai+ts-node to Vitest (May 2026; 312ms vs 30s startup). Run tests from repo root, not package subdirectory — \`vitest\` with a filter from a subdirectory returns 'No test files found'.
