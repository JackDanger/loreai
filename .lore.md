<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019e3083-8969-732b-a269-72e6cdd1ff7d -->
* **Background LLM rate limiting: p-limit(2) + 429 circuit breaker in background-limiter.ts**: Global concurrency limit for background LLM work in \`packages/gateway/src/background-limiter.ts\`. Uses \`p-limit(2)\` to cap simultaneous background LLM calls across all idle sessions. Circuit breaker trips on 429 responses and pauses all background work for the \`Retry-After\` duration. Wired into: idle scheduler, pipeline incremental distillation, in-flight curation. Urgent distillation is excluded (client is waiting). Without this, N idle sessions fire N×4 simultaneous background calls causing cascading rate limit failures.

<!-- lore:019e596c-e39c-74ff-879f-f04f2bb625f6 -->
* **Daily budget throttle curve: 3 zones + rate-aware EMA design**: Daily budget throttle: rate-aware EMA in \`cost-tracker.ts\`. State: \`dailySpend\`, \`dailySpendDate\`, \`costRateEMA\` (α=0.15). \`computeThrottleDelay()\`: \`pressure=(spendFraction-0.50)/0.50\`; \`overshoot=min(costRatePerHour/targetRate-1, 10)\`; delay=\`MAX\_THROTTLE\_DELAY(60s) × pressure² × tanh(overshoot/3)\`. Returns 0 if budget≤0, spendFraction<0.50, or rate sustainable. GOTCHA: returns 0 at 100%+ spend when EMA decayed to ~0 after idle — fix: \`if (spendFraction>=1.0) return MAX\_THROTTLE\_DELAY\`. \`getDailyBudget()\` priority: LORE\_DAILY\_BUDGET env → KV \`'daily\_budget'\` → 0. Worker costs update \`dailySpend\` but NOT EMA. Throttle uses proxy sleep (invisible delay), not 429. Only conversation turns throttled; compaction/meta exempt. Background workers excluded from throttle.

<!-- lore:019e550f-29b4-768d-ade6-4e46c014effe -->
* **DB schema current version: 26 migrations, key columns per migration**: DB schema: 32 migrations. Key: v8=knowledge.embedding+kv\_meta; v13=metadata; v14=projects.git\_remote+path\_aliases; v19=import\_history; v23=session\_state LTM cache/pin; v24=session identity+gradient state; v25=dedup\_feedback; v26=session\_state.parent\_session\_id+is\_subagent; v27=entities+entity\_aliases+knowledge\_entity\_refs+FTS5+triggers; v28=entity\_relations; v29=knowledge +11 cols (promotion\_status, promoted\_at, approval\_status, approved\_by, approved\_at, source\_user\_id, source\_entry\_id, last\_accessed\_at, created\_by, updated\_by, sensitivity) + team\_knowledge + team\_config; v30=daily\_costs ledger table (day TEXT, bucket TEXT, cost REAL; PRIMARY KEY (day, bucket)); v31=tool\_calls table (structured tool-call execution trace); v32=rebuild all 6 FTS5 tables with \`tokenize='unicode61 remove\_diacritics 0'\` (drops English \`porter\` stemmer for language-neutral search; remove\_diacritics MUST be 0 — Turkish ç/ğ/ı/ö/ş/ü are distinct letters, not accents; repopulate via FTS5 \`'rebuild'\`). \`migrate()\` uses \`MIGRATIONS.length\` as version — \`SCHEMA\_VERSION=16\` constant is dead code. \`recoverMissingObjects()\` must cover ALL tables (it does not recreate FTS tables — those live in the migration). Always \`git pull --rebase\` before migration work. Cross-check version numbers across PR title, description, commit messages, test assertions, and constants.

<!-- lore:019e5728-916d-7888-9778-224a5fbce34b -->
* **embedding.ts: LocalProvider worker lifecycle — init, crash, restart, and permanent-disable paths**: \`LocalProvider\` worker lifecycle in \`embedding.ts\`: \`workerReady=true\` set after Worker constructor returns — NOT after ONNX pipeline loads. \`workerInitError\` set permanently on \`'init-error'\` message, \`'error'\` event, or non-zero \`'exit'\`. Once set, all \`ensureWorker()\` calls throw \`LocalProviderUnavailableError\` — no restart. \`shutdown()\` resets all fields including \`workerInitError=null\` (allows re-init). \`resetProvider()\` shuts down + sets \`cachedProvider=undefined\`. \`\_shutdownAndDisable()\` sets \`cachedProvider=null\` (prevents new provider — test-only). \`embed()\` auto-fallback: catches \`LocalProviderUnavailableError\`, calls \`pickRemoteFallback()\`, permanently replaces \`cachedProvider\`. Voyage wins ties over OpenAI. GOTCHA: \`shutdown()\` must reject pending requests with \`new LocalProviderUnavailableError(...)\` not plain \`Error\` — plain Error hits Sentry via \`log.error()\` → \`sink.captureException()\`.

<!-- lore:019e69ae-12cf-7bb2-8e09-881e24af8ef3 -->
* **Gateway auth: single credential per session — background workers inherit client key**: Gateway auth: single credential per session — background workers inherit client key. \`resolveAuth(sessionID)\` in auth.ts:131 returns per-session credential → lastSeenAuth fallback. Credentials captured from client's incoming request via \`setSessionAuth\`/\`setLastSeenAuth\` in pipeline.ts. Background workers (distillation, curation, consolidation) always use whatever key the client session sends — no mechanism to route workers to a different upstream with a different credential. PR #482 added \`LORE\_WORKER\_API\_KEY\`, \`LORE\_WORKER\_UPSTREAM\`, \`LORE\_WORKER\_MODEL\` env vars (worker-model.ts) to override this: all wired through \`getLLMClient()\` in pipeline.ts via dependency injection at construction time. Zero changes needed in \`@loreai/core\`, \`auth.ts\`, \`llm-adapter.ts\`, or \`batch-queue.ts\`.

<!-- lore:019e54f5-051c-7641-aaad-7688451d5021 -->
* **Gateway complete HTTP endpoint inventory**: Gateway HTTP endpoints. LLM Proxy: POST /v1/messages (Anthropic), POST /v1/chat/completions (OpenAI Chat), POST /v1/responses (OpenAI Responses API), POST /v1/compact, GET /v1/models. Health: GET /health. REST API (/api/v1/): GET projects/stats/knowledge/sessions/distillations/recall/import-history; DELETE knowledge/:id/sessions/:id/distillations/:id/projects/:id; POST projects/merge (disabled hosted), reindex, import/extract, import/record, projects/:id/clear, projects/:id/dedup. UI: GET+POST /ui/\*. Project resolution: 1) :id URL param, 2) ?git\_remote=, 3) ?path=. All pipeline handlers return internal Anthropic-format; translation to OpenAI/Responses format happens after pipeline returns. Total pipeline.ts lines: 4377.

<!-- lore:019e8d88-e305-76ac-9d62-fa605ab0f2a7 -->
* **Gateway is HTTP-only; reject WebSocket upgrades (Codex Responses-WS)**: The gateway is a translating HTTP proxy — deliberately NO WebSocket support. Fix: server.ts detects GET + \`Upgrade\` + \`Host\` containing \`/v1/responses\`, returns \`101\`+\`Connection: close\` so Codex falls back on first attempt. Codex WS transport details (client.rs analysis): \`responses\_websocket\_enabled()\` gates on \`provider.info().supports\_websockets\` AND \`!disable\_websockets\` (AtomicBool, session-sticky once tripped). \`force\_http\_fallback()\` sets \`disable\_websockets=true\`, clears cached WS session, logs \`warn!("falling back to HTTP")\` + telemetry counter \`codex.transport.fallback\_to\_http\`. Beta header: \`openai-beta: responses\_websockets=2026-02-06\`. Wire protocol lives in \`codex\_api\` crate (\`ResponsesWebsocketClient\`, \`ResponsesWsRequest\`) — NOT in client.rs. WS is OpenAI-provider-specific (gated by \`supports\_websockets\` per provider). Codex NEVER emits \`previous\_response\_id\` (always full input items).

<!-- lore:019e8da3-0790-7538-ad24-64fd1dcdb5fa -->
* **Gateway is stateless full-history for OpenAI Responses API — strips previous\_response\_id, drops item\_reference**: Gateway is stateless full-history for OpenAI Responses API — strips previous\_response\_id, drops item\_reference: The gateway always sends the COMPLETE (gradient-transformed, recall-injected) conversation as \`input\` via \`buildResponsesInput()\` (openai-responses.ts:310-341) — iterates every message, emits input items for each text/tool\_use/tool\_result block. Never relies on server-side response storage. \`buildOpenAIResponsesUpstreamRequest\` intentionally does NOT forward \`previous\_response\_id\` (logs warn when present) — forwarding would cause duplication and defeat compression/recall edits. \`item\_reference\` items dropped at parseInputItems (line 196) — no else/default branch. \`reasoning\` items also dropped (confirmed test at openai-responses.test.ts:156-183). \`store\` not in extras allow-list (lines 72-81) — client-sent \`store\` silently dropped. Upstream \`response.id\` echoed back only (\`resp\_${resp.id}\` at lines 396, 443) — never persisted for continuation. grep for \`storeResponseId|continuation\` finds nothing. Codex: never emits \`item\_reference\`, never populates \`previous\_response\_id\` (hardcoded None in common.rs), sets \`store: provider.is\_azure\_responses\_endpoint()\`.

<!-- lore:019e6904-e0e1-7689-bb52-7e8a0cb0edda -->
* **Gateway recall: Marker-and-Expand strategy — tool injection, interception, and round-trip**: Recall tool injected by gateway pipeline (pipeline.ts), NOT OpenCode plugin. Injection condition: \`modifiedReq.tools.length > 0 && !clientHasRecallTool(tools)\`. Strategy: (1) Response: recall \`tool\_use\` replaced with marker text via \`replaceRecallWithMarker()\`; result stored in \`sessionState.recallStore\`. (2) Next request: \`expandRecallMarkers()\` restores \`tool\_use\`+\`tool\_result\` pairs before forwarding. \`buildRecallFollowUp()\`: assistant message = ORIGINAL \`tool\_use\` block; user message = \`tool\_result\` block with recall results (NOT plain text) — prevents LLM echoing raw recall formatting during streaming. \`hasContinuationAfter\` logic: if blocks follow marker, truncates/splices; else prepends tool\_result to next user msg. \`RecallAwareAccumulator\` suppresses recall SSE blocks. MAX\_RECALL\_DEPTH=10. Follow-up requests: \`cacheConversation: false\`.

<!-- lore:019e6afa-26e4-772c-a432-5273b52fdb71 -->
* **Gateway session eviction: per-module cleanup functions and memory profile**: Gateway session eviction: per-module cleanup functions and memory profile: Gateway accumulates per-session state in 13+ Maps/Sets across 8 modules with zero eviction. Memory dominated by \`gradient.prefixCache.cachedText\` (10-50KB), \`distillationSnapshot.rows\`, \`cacheAnalytics.lastRequestBody\`. Per-module cleanup: \`deleteSessionAuth\`, \`deleteSessionCosts\`, \`deleteBillingPrefix\`, \`clearWarmupAuthDisabled\`, \`distillLimiter.evict()\`, \`curatorLimiter.evict()\`, \`evictQuotaCache(fingerprint)\`. Configurable via \`LORE\_SESSION\_EVICTION\_TIMEOUT\` (default 1800s). Sub-agents evict after \`SUBAGENT\_EVICTION\_MS=5min\`. GOTCHA: \`parsePositiveInt\` rejects \`0\` — \`LORE\_SESSION\_EVICTION\_TIMEOUT=0\` silently uses 1800s; use \`parseNonNegativeInt\`. quota.ts: \`fetchOAuthQuotaSnapshot()\` queries \`GET /api/oauth/usage\` with bearer + \`anthropic-beta: oauth-2025-04-20\`. \`utilization\` field may be 0.0–1.0 fraction OR 0–100 percent — normalize with \`>1 ? value : value\*100\`. \`resets\_at\` may be ISO-8601 string or Unix epoch number. 403 may indicate missing \`user:profile\` scope — re-mint token via \`claude /login\`. \`isAnthropicOAuthSession()\` required for quota checks.

<!-- lore:019e5544-4bc6-7ae6-ba2c-8d4493f6e1cf -->
* **gradient.ts: consecutiveBusts lifecycle — init, mutation, persistence, and DB-restore skip**: \`resolvedConversationTTL: '5m'|'1h'\` on \`SessionState\` (translate/types.ts:253). TTL resolution block in pipeline.ts lines 3444–3490: initialized from \`sessionState.resolvedConversationTTL ?? '5m'\`. Config \`'5m'\`/\`'1h'\`: pass through. Config \`'auto'\`: reads \`sessionState.coldCacheWindow\` (requires ≥5 samples). Auto-upgrade: \`coldFraction>0.4 && current==='5m'\` → immediate upgrade to \`'1h'\`, resets \`ttlDowngradeStreak=0\`. Auto-downgrade: \`coldFraction<0.2 && current==='1h'\` → hysteresis: requires 3 consecutive qualifying turns (\`streak≥3\`); rationale: TTL change modifies cached bytes AND drops idle threshold from 60min to 5min, causing compounding cache bust. \`consecutiveBusts\` in gradient.ts is separate from TTL streak. \`resolvedConversationTTL\` used in pipeline.ts, idle.ts, sentry.ts, cache-warmer.ts. Key \`SessionState\` fields (translate/types.ts:240–325): \`ttlDowngradeStreak\` (257), \`isSubagent\` (265), \`outputTokensEMA\` (285, α=0.3), \`consecutiveTextOnlyTurns\` (297), \`warmup: WarmupState\` (300), \`survivalModel: InterTurnHistogram\` (302), \`amnesia\` (318), \`\_dirty\` (324).

<!-- lore:019e5acc-87be-7373-a42e-16e8852dda40 -->
* **Hermes context markers: injection, extraction, sanitization, and stripping**: Hermes plugin injects \`\[lore:session-id=\<hex>]\` and \`\[lore:project=\<path>]\` into the last user message each turn (Tier 2.5 session identification). Gateway extracts markers in \`extractSessionMarker()\`/\`extractProjectMarker()\` before session identification, then must strip them via \`stripContextMarkers()\` before forwarding upstream — markers must never reach the LLM. \`extractProjectMarker()\` sanitizes: strips control chars, enforces 1024-char limit, rejects relative paths and \`..\` traversal components, strips trailing slashes. Session IDs must match \`\[a-f0-9]{8,64}\` — Hermes must always hash to hex via \`hashlib.sha256\` before injecting (Hermes-native session IDs may contain hyphens/uppercase that fail the regex). \`identifySession()\` returns \`tier: 2.5\` (type widened to \`1 | 2 | 2.5 | 3\`). \`stripContextMarkers()\` called in both \`handleConversationTurn\` and \`handleCompaction\`.

<!-- lore:019e7013-a9ea-72e6-8e8c-17ef1848e4e0 -->
* **llm-adapter.ts: retry logic, backoff schedule, and circuit breaker integration**: \`TRANSIENT\_CODES={429,500,502,503,529}\`; \`AUTH\_ERROR\_CODES={401,403}\`. \`MAX\_RETRIES\_RATE\_LIMIT=3\`; \`MAX\_RETRIES\_SERVER=3\`; \`MAX\_RETRIES\_URGENT=2\`. \`RETRY\_AFTER\_CAP\_URGENT\_MS=8\_000\`; \`RETRY\_AFTER\_CAP\_BACKGROUND\_MS=120\_000\`. \`backoffMs()\`: Retry-After present → \`min(retryAfterMs, cap)\`; urgent (no header) → \`min(1000×2^attempt, 4000)\`; 429 non-urgent → \`min(60\_000+attempt×60\_000, 180\_000)\`; 5xx → \`min(1000×2^attempt, 8000)\`. On 429 non-urgent → \`tripCircuitBreaker(pauseSec)\` before retry. Auth errors → \`markAuthStale\`, re-resolve, retry once if \`credentialChanged && attempt===0\`. \`buildAnthropicWorkerRequest()\`: injects billing block only when \`cred.scheme === 'bearer'\`; system sent as block array with \`cache\_control: { type: 'ephemeral', ttl: '1h' }\`; body signed via \`signBody()\`. \`opts.thinking\` intentionally NOT forwarded in bare API fetch. \`activeWorkerCalls=Set\<string>\` tracks worker session IDs.

<!-- lore:019e591d-acda-7f7d-b97b-85ec594a1445 -->
* **LLMClient.prompt() opts: urgent, thinking, workerID, maxTokens, temperature semantics**: \`LLMClient.prompt()\` opts: \`urgent:true\` = bypass batch queue — blocking callers (compaction, overflow recovery, query expansion) must set it; background workers must NOT. \`thinking:false\` = disable extended thinking — ALL background workers always set this. Only gateway's \`BatchLLMClient\` honors \`urgent\`; OpenCode/Pi adapters always process immediately. \`generateCompactionSummary()\`: \`compactMaxTokens = max(2048, min(ceil(inputLen/3 \* 0.5), 20\_000))\`; \`urgent:true, callType:'direct', temperature:0\`. Query expansion (\`search.ts\`): \`urgent:true\`. \`scheduleBackgroundWork()\`: urgent distillation bypasses \`isBackgroundPaused()\`; incremental distillation uses \`callType: batchQueueEnabled ? 'batch' : 'direct'\`; curation multiplier: \`inputCost >= 5 → 3x\`, \`>= 1 → 2x\`, else \`1x\`.

<!-- lore:019e5715-cccd-7be2-a979-407066c526d4 -->
* **log.ts: sink bridge, file rotation, and error visibility rules**: \`packages/core/src/log.ts\` (170 lines). Key rules: (1) \`log.error()\` \*\*always\*\* calls \`console.error()\` — 'Always visible — these indicate real failures.' \`log.info()\`/\`log.warn()\` only print if \`LORE\_DEBUG=1\`. (2) \`sink?.captureException(err)\` called only when an \`Error\` instance found in args via \`findError()\` — gateway bridges this to Sentry via \`registerSink()\`. (3) File logging: all levels written to \`~/.local/share/lore/lore.log\` regardless of \`LORE\_DEBUG\`; rotated at 5 MB (\`LOG\_MAX\_BYTES\`) with single \`.log.1\` backup; disabled in \`NODE\_ENV=test\`. (4) Only one \`LogSink\` supported at a time. Trap: any \`log.error(..., errorObj)\` auto-forwards to Sentry — including expected shutdown/auth errors. Use typed error subclasses (e.g. \`LocalProviderUnavailableError\`) to filter in \`beforeSend\`.

<!-- lore:019e54f5-04be-7a9f-9e62-177d65d5db9f -->
* **LTM forSession() budget, phases, and safety-net constants**: \`forSession()\` (ltm.ts): budgets: \`budget.ltm\`=0.05, \`budget.preferenceLtm\`=0.02. Phase 1: architecture entries up to ARCH\_BUDGET\_FRACTION=0.2. Phase 2: remaining entries by score descending. Phase 3: .lore.md sections. HEADER\_OVERHEAD\_TOKENS=15 subtracted upfront. Hard cap: \`if (used+cost>maxTokens) continue\`. PROJECT\_SAFETY\_NET=5: top-confidence project entries ALWAYS included even with zero relevance score — intentional design, guards against coarse term-overlap scoring excluding important project knowledge. NO\_CONTEXT\_FALLBACK\_CAP=10. Global entries (pid=null) must be cross\_project=1. \`vectorSearch()\` has no project\_id filter; dedup threshold for Nomic v1.5 is 0.935. GOTCHA: \`vectorSearch()\` queries \`knowledge WHERE embedding IS NOT NULL AND confidence>0.2\` — test suites must \`DELETE FROM knowledge WHERE embedding IS NOT NULL\` in beforeEach. Pool SQL: project pool uses \`cross\_project=0\`; cross pool uses \`project\_id IS NULL OR cross\_project=1\`. Promoted entries (cross\_project=1) lose PROJECT\_SAFETY\_NET guarantee in their home project — semantic regression to be aware of.

<!-- lore:1cdad2a4-5637-471b-a5cb-39c919fb1592 -->
* **Multilingual support: FTS tokenizer, Unicode tokenization, and non-English fallbacks**: Lore supports non-English (e.g. Turkish) conversations. Key design: (1) FTS5 uses \`tokenize='unicode61 remove\_diacritics 0'\` (NOT \`porter\` — English-only stemmer; NOT diacritic-folding — Turkish ç/ğ/ı/ö/ş/ü are distinct letters). Migration v32 rebuilds all 6 FTS tables. (2) \`filterTerms()\`/\`extractTopTerms()\` in \`search.ts\` strip via \`/[^\p{L}\p{N}\_\s]/gu\` (Unicode-aware) so Turkish words don't split at special letters. Underscore kept for snake\_case. (3) \`pattern-extract.ts\` regexes need NO translation — they run on the LLM observer's \*English-normalized\* output. (4) Two raw-user-text regex sites have a structural fallback: \`hasNonAsciiLetters()\` (exported from \`instruction-detect.ts\`) gates fallbacks in \`extractInstructionCandidates\` (emit whole message as candidate) and \`detectAssertions\` (pin first sentence) — requires ≥3 non-ASCII letters to avoid false positives on English loanwords (café, naïve). (5) \`ltm.rerankPreferences()\` no longer demotes entries lacking English directive words to 0.8. KNOWN LIMITATIONS (deferred): English \`STOPWORDS\` not filtered for other languages (vector+RRF compensates); ~3-4 chars/token estimate is English-tuned (fine for Turkish, off for CJK).

<!-- lore:019e4586-82db-796f-a3cd-d65f790276bf -->
* **OpenCode x-session-affinity is a per-process nanoid — not stable across restarts**: OpenCode generates \`x-session-affinity\` natively as a nanoid — random, per-process, NOT stable across restarts. The Lore plugin (\`packages/opencode/src/index.ts\`) never touches this header. \`input.sessionID\` in plugin hooks is OpenCode's persistent DB \`Session.id\` — stable across restarts. When OpenCode restarts, the new nanoid causes Tier 1 in \`identifySession()\` to create a brand-new Lore session, orphaning prior distillations/gradient state. Fix: inject \`input.sessionID\` as \`x-lore-session-id\` in the \`chat.headers\` hook. Tier 1 upgrade-path trap: when plugin starts sending \`x-lore-session-id\` alongside \`x-session-affinity\`, prior sessions indexed under \`x-session-affinity\` are invisible — Tier 1b must check both header names.

<!-- lore:019e1952-398c-753f-b92c-4a5fa5ecf15f -->
* **Pi plugin: which providers can be proxied through the gateway**: Pi plugin gateway proxy compatibility by wire protocol. \*\*Proxiable\*\*: \`anthropic\` → \`/v1/messages\`: \`anthropic\`, \`fireworks\`, \`github-copilot\`; \`openai-completions\` → \`/v1/chat/completions\`: \`deepseek\`, \`xai\`, \`groq\`, \`cerebras\`, \`openrouter\`, \`huggingface\`, \`zai\`, \`minimax\`, \`minimax-cn\`, \`kimi-coding\`, \`vercel-ai-gateway\`; \`openai-responses\` → \`/v1/responses\`: \`openai\`; native SDK via model-prefix routing: \`nvidia\`, \`mistral\`, \`google\`; local/self-hosted (OpenAI-compatible): \`vllm\`, \`llamacpp\`, \`ollama\`, \`lmstudio\`, \`jan\`, \`localai\`, \`tgi\`, \`tabbyml\`, \`litellm\`. \*\*Cannot proxy natively\*\*: \`google\`, \`google-vertex\`, \`amazon-bedrock\`, \`mistral\` (use native SDK path). \`registerProvider(name, { baseUrl })\` overrides base URL. Gateway routes by URL path only. OpenAI streaming clients receive true incremental SSE (\`stream/openai.ts\`).

<!-- lore:019e4c7d-bfae-7c96-a491-e57cef38d84d -->
* **pipeline.ts: per-turn auth binding, billing prefix capture, and session bootstrap sequence**: pipeline.ts key behaviors: (1) \`extractAuth()\` → \`setLastSeenAuth\` + \`setSessionAuth\` every request. (2) \`captureBillingPrefix(sessionID, req.system)\` at line 2945, then \`captureSessionHeaders\`. (3) New sessions: \`fingerprintMessages()\`, \`learnHeaders()\`, \`tryImportKnowledge(projectPath)\`. (4) \`stripContextWarnings()\` before forwarding. (5) 1h TTL → \`effectiveCacheWriteCost = cacheWriteCost \* 2\`. Daily budget throttle (lines 3500–3540): \`getDailyBudget()\` → \`estimateRequestCost\` → \`getDailyThrottleDelay(dailyBudget, estimatedCost, quotaPressure)\` — quota pressure flows through even when \`dailyBudget=0\`; sleep skipped if ≤0.5s; delay capped at 50% remaining cache TTL. Inline workers gated by \`isQuotaPaused(fingerprint)\` when quota pressure ≥ 0.95. \`handlePassthrough\` bypasses cache-TTL resolution block. \`getLLMClient()\` lazy singleton; wraps with \`createBatchLLMClient()\` unless \`LORE\_BATCH\_DISABLED=1\`. Total lines: 4377.

<!-- lore:019e5922-4280-76aa-9b79-5ebeae433d7a -->
* **Sub-agent session detection, isolation, and differential treatment**: Sub-agent sessions detected via \`x-parent-session-id\` header (pipeline.ts:2773-2809). Each sub-agent gets independent session: temporal storage, gradient state, distillation pipeline, LTM injection, cost tracking. Key differential treatment: (1) cache warming ALWAYS skipped — \`shouldWarm()\` returns false unconditionally (cache-warmer.ts:731-733); (2) \`findRotationPredecessor()\` (session.ts:505) skips sub-agents from Tier 1b rotation; (3) idle distillation/curation runs normally (no isSubagent check). \`isSubagent\`/\`parentSessionId\` persisted to DB (migration v26). Dashboard: collapsible cost-rollup tree via \`loadParentChildMap()\` + \`buildLiveSessionRows()\`; \`rollUp()\` propagates grandchildren costs bottom-up.

<!-- lore:019e56c3-c6af-78fd-a312-8ed73905680b -->
* **Team mode knowledge promotion: both explicit sharing and auto-suggest; approval workflow**: Team mode knowledge promotion: explicit sharing + auto-suggest with approval workflow. Architecture: database-per-user/team model (NOT shared DB with visibility column). Personal DB = local SQLite synced to Turso cloud; Team DB = Turso cloud only. Schema v29: \`knowledge\` table gets +11 cols (sensitivity, promotion\_status, source\_user\_id, source\_entry\_id, created\_by, updated\_by, promoted\_at, approval\_status, approved\_by, approved\_at, last\_accessed\_at); new \`team\_knowledge\` and \`team\_config\` tables. \`Sensitivity\`/\`PromotionStatus\`/\`ApprovalStatus\` types exported from ltm.ts. \`create()\` accepts \`createdBy?\` and \`sensitivity?\` params. \`update()\` accepts \`updatedBy?\`. Cross-project knowledge stays personal. \`recoverMissingObjects()\` must include \`team\_knowledge\` and \`team\_config\` tables. RFC documented in GitHub Issue #467. Implemented in feat/team-sync-schema branch (PR pending).

<!-- lore:019e6f25-51b7-7b3c-8461-dfec7c531902 -->
* **Worker credential routing: LORE\_WORKER\_API\_KEY, LORE\_WORKER\_UPSTREAM, LORE\_WORKER\_MODEL env vars**: Worker credential routing (PR #482): Three env vars routing background worker LLM calls independently from session credentials, wired through \`getLLMClient()\` in pipeline.ts: \`LORE\_WORKER\_API\_KEY\` → replaces \`resolveAuth\` for all worker calls (always returns \`{ scheme: 'api-key' }\`, bearer bypass in batch-queue never triggers); \`LORE\_WORKER\_UPSTREAM\` → overrides both anthropic+openai URLs; \`LORE\_WORKER\_MODEL\` → checked first in \`getWorkerModel()\` before config-file override (format: \`providerID/modelID\`). Zero changes needed in \`@loreai/core\`, \`auth.ts\`, \`llm-adapter.ts\`, or \`batch-queue.ts\`. LOREAI-GATEWAY-Z: OAuth tokens don't support Batch API; bearer bypass added in PR #502 — \`cred.scheme === 'bearer'\` at enqueue time skips queue, goes direct. Circuit breaker only trips on 429, not 401.

<!-- lore:019e4bbd-ade4-7355-8fb1-87df54c8ea69 -->
* **X-Lore-\* headers: project path injection and resolution priority**: X-Lore-\* headers and project path resolution: Priority in \`packages/gateway/src/config.ts\`: 1) \`X-Lore-Project\` header (absolute path, max 1024 chars, strips trailing slashes/control chars), 2) inferred from system prompt (\`cwd\` JSON key, \`Working directory:\` prefix, \`.lore.md\` path, generic \`/home/\`/\`/Users/\` paths), 3) \`process.cwd()\` fallback. Source tagged as \`ProjectPathSource\`: \`"header"|"inferred"|"cwd"\`. Pipeline warns on \`cwd\` source (dedup via \`cwdWarned\` Set). \`agents.ts\` \`envVars()\` injects \`X-Lore-Project\` + \`X-Lore-Git-Remote\` into \`ANTHROPIC\_CUSTOM\_HEADERS\`. Other headers: \`x-lore-session-id\`, \`x-lore-no-store\`, \`x-lore-upstream-url\`, \`x-lore-recall-invoked\` (response). \`lore run\` always runs locally.

### Gotcha

<!-- lore:019e5b19-138a-7e00-ab9e-720769924303 -->
* **bootstrapDailySpend() overcounts multi-day sessions and has double-count risk**: cost-tracker.ts design and gotchas: (1) v30 \`daily\_costs\` ledger (day, bucket, cost; PK=(day,bucket)) replaces session\_state cumulative sum — records cost at exact UTC day incurred. \`bootstrapDailySpend()\` reads ledger only (no session\_state fallback — that was the over-count bug). \`addDailyCost()\` guard: \`!Number.isFinite(cost) || cost <= 0\` (rejects negative+zero). (2) \`computeThrottleDelay\`: \`if (spendFraction>=1.0) return MAX\_THROTTLE\_DELAY\`; pressure²×tanh(overshoot/3) formula. (3) \`getDailyBudget()\` priority: LORE\_DAILY\_BUDGET env → KV \`'daily\_budget'\` → 0. (4) \`aggregateDistillationsBySession\` SQL includes \`SUM(CASE WHEN call\_type='batch' THEN token\_count ELSE 0 END) as batch\_tokens\` — proportional split by call count diverges >10% when token distributions differ. (5) Compaction formula \`1+floor((total-threshold)/stride)\` overestimates for large messages (overshoots not modeled). (6) \`HISTORICAL\_CACHE\_TTL\_MS\`=5min. \`DAILY\_BUDGET\_KV\_KEY='daily\_budget'\`.

<!-- lore:019e1c27-967c-7eb4-bd0e-afb195823970 -->
* **Bun NAPI crash on process.exit() — use safeExit() via libc \_exit()**: Bun NAPI crash on process.exit() with fastembed — use safeExit(): Loading fastembed (onnxruntime NAPI bindings) causes a C++ panic on \`process.exit()\` because Bun runs NAPI teardown destructors that throw. Fix: \`packages/gateway/src/cli/exit.ts\` exports \`safeExit(code)\` — uses \`\_exit()\` from libc via \`bun:ffi\` under Bun, falls back to \`process.exit()\` under Node.js. All gateway exit paths must use \`safeExit()\`. Do NOT call \`embedding.resetProvider()\` in test teardown \`resetPipelineState()\` — move \`resetProvider()\` to \`shutdown()\` in \`start.ts\` only. \`resetPipelineState()\` must preserve the 'fastembed unavailable' cached state.

<!-- lore:019e6a44-dbf1-7df6-ba60-94f75c21b81d -->
* **consolidationUser() batchTarget frame-of-reference mismatch: global target vs batch count produces negative excess**: Trap: \`batchTarget\` in batched consolidation was computed globally (\`Math.max(cfg.curator.maxEntries, entries.length - CONSOLIDATION\_BATCH\_SIZE/2)\` = 1118 for 1143 entries) but \`consolidationUser()\` computed \`excess = count - targetMax\` where \`count\` is the batch size (50). Result: \`excess = 50 - 1118 = -1068\` — prompt tells LLM 'must remove at least -1068 entries', LLM returns empty ops, idle scheduler enters 1h cooldown, system never converges. Fix: \`batchTarget\` must be batch-relative — \`Math.ceil(CONSOLIDATION\_BATCH\_SIZE / 2)\` so \`excess = 50 - 25 = 25\`. Fixed in e8a73f9. CONSOLIDATION\_BATCH\_SIZE=50 (curator.ts:643). Non-batched path (entries ≤ BATCH\_SIZE) correctly uses \`cfg.curator.maxEntries\` as target.

<!-- lore:019e54e4-fe50-71e7-ab69-49ac97cf32f3 -->
* **consumeSSE retries counter never resets after successful reconnection — permanent failure after 10 total disconnects**: Trap: \`retries\` in \`consumeSSE\` (src/commands/local/server.ts:469) increments on each disconnect but never resets on successful reconnection. After 10 total disconnections (even with successful reconnects between them), the client permanently stops. Looks correct because 10 retries sounds like a per-attempt limit. Fix: reset \`retries = 0\` on successful connection (first message or \`open\` event) so it tracks only consecutive failures. Also: initial SSE connection should fail fast on \`'error'\` result rather than retrying — fixed in commit d967edd51.

<!-- lore:019e6a1b-3408-7609-ba6b-d4ba9d552afd -->
* **curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields**: curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields: Trap: \`filterOps()\` (curator.ts:109–117) only checks \`op\` is a string — does NOT validate required fields (\`content\`, \`title\`, \`category\`) for \`create\` ops, casting result as \`CuratorOp\[]\` silencing TypeScript. When LLM emits a malformed \`create\` op missing \`content\`, \`applyOps()\` line 226 accesses \`op.content.length\` without null guard → \`TypeError: undefined is not an object\`. Fix: add null guard at line 226 AND field validation in \`filterOps()\` for \`create\` ops. The \`update\` branch at line 252 already has \`op.content !== undefined &&\` guard — \`create\` branch lacks equivalent. \`applyOps()\` calls \`ltm.syncRefs(id)\` and \`entities.syncEntityRefs(id, content)\` after each create/update; \`skipCreate\` flag skips creates (used by consolidation); cross-project guard prevents mutating foreign-project entries. \`cfg\` is in scope in \`runInner\` at line 414.

<!-- lore:019e6f25-51c3-738a-bdda-225933169d76 -->
* **ensureSelfEntity() is defined but never called — entity detail pages show no metadata/relationships until curator runs**: \`worker-model.ts\`: fetches pricing from \`https://models.dev/api.json\` (cached 1h). \`EXPENSIVE\_MODEL\_THRESHOLD=1.5\` ($/M input). \`WORKER\_DEFAULTS\`: anthropic→\`claude-sonnet-4-6\` (alreadyCheap: includes 'sonnet'/'haiku'), openai→\`gpt-5.4-mini\` (alreadyCheap: includes 'mini'/'nano'), github-copilot→\`gpt-5.4-mini\`. \`getWorkerModel()\` resolution: (1) \`LORE\_WORKER\_MODEL\` env (format: \`providerID/modelID\`), (2) explicit config override, (3) provider-aware cost default, (4) general fallback (GPT-5.4-mini), (5) session model. \`GENERAL\_FALLBACK\_WORKER={providerID:'github-copilot', modelID:'gpt-5.4-mini'}\`. Worker credential routing (PR #482): \`LORE\_WORKER\_API\_KEY\` replaces \`resolveAuth\` for worker calls; \`LORE\_WORKER\_UPSTREAM\` overrides anthropic+openai URLs. OAuth bearer sessions: \`cred.scheme === 'bearer'\` at enqueue time skips Batch API queue, goes direct (PR #502). Circuit breaker only trips on 429, not 401.

<!-- lore:019e69f0-a05a-7d48-8e35-76baf13533c5 -->
* **LOREAI-GATEWAY-1E: resp.usage undefined crash in postResponse() for vLLM/OpenAI path**: Trap: \`GatewayUsage\` typed as non-optional on \`GatewayResponse\`, so \`resp.usage.inputTokens\` looks safe. But \`resp.usage\` can be \`undefined\` at runtime (e.g., vLLM at \`http://localhost:8300\` returning partial response). All four accumulators construct \`usage\` with \`?? 0\` guards on individual fields — but \`resp.usage\` itself can still be undefined if an exception produces a partial response object. Crash sites: pipeline.ts lines 2069, 2101, 2137-2140, 2150-2153, 2204-2206, 2282-2285, 2341, 2363, 2402; also \`buildOpenAINonStreamResponse\` (openai.ts:296-300). Fix: add \`const usage = resp.usage ?? { inputTokens: 0, outputTokens: 0, cacheReadInputTokens: 0, cacheCreationInputTokens: 0 }\` guard at top of \`postResponse()\` and \`nonStreamHttpResponse()\`, then use \`usage.\*\` throughout.

<!-- lore:019e6b04-ae79-7df6-b34c-aad06d33c6c0 -->
* **LOREAI-GATEWAY-1G/1H: pickRemoteFallback() uses OPENAI\_API\_KEY=nokey placeholder — causes 401 Sentry noise**: Trap: \`pickRemoteFallback()\` (embedding.ts:633) treats any truthy \`OPENAI\_API\_KEY\` as valid. Users who set \`OPENAI\_API\_KEY=nokey\` as a placeholder (common when using custom \`OPENAI\_BASE\_URL\` with Codex/OpenCode) trigger the local→remote fallback path, which sends \`Authorization: Bearer nokey\` to \`api.openai.com/v1/embeddings\` → 401 → \`log.error()\` → Sentry. Full flow: LocalProvider WASM crash → \`embed()\` catches \`LocalProviderUnavailableError\` → \`pickRemoteFallback()\` finds truthy \`OPENAI\_API\_KEY\` → \`OpenAIProvider('nokey')\` → 401 → fire-and-forget \`.catch()\` → \`log.error()\` → \`sink.captureException()\` → Sentry. Two fixes: (1) add \`/Incorrect API key/i\` to \`TRANSIENT\_ERROR\_PATTERNS\` in \`instrument.ts:78\`; (2) validate key format in \`pickRemoteFallback()\` before constructing \`OpenAIProvider\` (e.g., \`key.startsWith('sk-') && key.length > 20\`). The literal string \`"nokey"\` does NOT exist in the Lore codebase — it comes from the user's shell environment.

<!-- lore:019e56fb-b446-759b-aff2-035a83700124 -->
* **LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm — resolveAuth returns same stale token**: LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm. \`resolveAuth(sessionID)\` marks session stale and falls back to \`getLastSeenAuth()\` — same expired token. \`credentialChanged=false\` so retry-once path never taken. Idle scheduler fires 4+ LLM calls/tick (distill, meta-distill, curation, consolidation), each 401ing independently. \`isAuthStale()\` exported but NEVER called in non-test source. Fix: (1) check \`isAuthStale(sessionID)\` in idle.ts before scheduling LLM work; (2) \`resolveAuth\` returns null when global fallback matches stale credential; (3) add \`/Worker upstream auth error/\` to \`TRANSIENT\_ERROR\_PATTERNS\` in \`instrument.ts\`. Circuit breaker (background-limiter.ts) only trips on 429, not 401. OAuth bearer sessions skip Batch API queue (PR #502).

<!-- lore:019e50e7-6250-779e-a842-b1b5baf87295 -->
* **ONNX OOM error code 284432024: single oversized text bypasses batch budget guard**: ONNX OOM fix chain (Nomic v1.5 pads all batch texts to longest sequence): (1) \`LOCAL\_MAX\_CHARS=16384\`; (2) Worker OOM-retry: attempt=0 full texts; on OOM, \`maxTokens=4096>>attempt\` → 4096→2048→1024; \`OOM\_MAX\_RETRIES=3\`; (3) \`truncateTexts(texts,maxTokens)\`; (4) \`nextBatch()\` adaptive batching caps \`batch\_size×max\_tokens\` at \`MAX\_BATCH\_TOKEN\_AREA=4096\`; (5) \`safeLocalTruncate()\` backs up 1 char on high surrogate boundary. \`isOomError()\` matches \`/^\d{6,}$/\` and \`/out.of.memory|alloc.\*fail|oom/i\`. WASM \`Aborted()\` NOT caught by \`isOomError()\` — use \`isWasmFatalError()\` matching \`/Aborted\\(\\)/i\`; post \`{type:'init-error'}\`, call \`process.exit(1)\`. \`shutdown()\` must reject with \`LocalProviderUnavailableError\` (not plain Error) to avoid Sentry noise. Bun NAPI crash on \`process.exit()\` with fastembed: use \`safeExit(code)\` from \`packages/gateway/src/cli/exit.ts\` (uses libc \`\_exit()\` via bun:ffi). Do NOT call \`embedding.resetProvider()\` in test teardown — only in \`shutdown()\` in \`start.ts\`.

<!-- lore:019e8d03-3fdf-76cf-b87d-eab142cad8b1 -->
* **OpenAI protocol: split tool messages + recall leak on streaming path**: Two OpenAI-protocol bugs. (1)  (translate/openai.ts) pushed separate user messages per  msg — breaks  Pass 2 which only checks . Fix: coalesce consecutive tool messages into one user message. (2) OpenAI/openai-responses streaming dispatch (pipeline.ts) accumulated upstream SSE then returned early, skipping the recall loop. Fix:  closure called from all three accumulated-response paths. Also:  (recall.ts) must set  — follow-ups parsed as JSON via ; without override, streaming clients forward  causing SSE→JSON parse crash.

<!-- lore:019e4eab-59b4-7fe3-be03-874f682cc340 -->
* **openai.ts: array-format content in system/developer messages silently dropped**: (gotcha) openai.ts / openai-responses.ts parsing traps: (1) \`parseOpenAIRequest\` (openai.ts:67–69) coerces non-string content to \`""\` — array-format content in \`system\`/\`developer\` messages silently dropped. Fix: check \`Array.isArray(content)\` and extract text parts. (2) \`buildOpenAIMessages\` (openai.ts:523–529): \`tool\_result\` blocks pushed mid-loop before \`textParts\`/\`toolUses\` flushed — tool\_result appears before text/tool\_use of same message. (3) \`buildOpenAIMessages\` always re-emits system as \`role:'system'\`, never \`'developer'\`. (4) \`parseOpenAIResponsesRequest\` reads system prompt from top-level \`instructions\` field — \`developer\`/\`system\` role messages in input array treated as regular user messages. (5) \`item\_reference\` and \`reasoning\` items in \`parseInputItems\` (line 196) are silently dropped — no else/default branch. \`store\` field not in extras allow-list (lines 72-81) — client-sent \`store\` silently dropped. LTM injection for Responses API: \`\[req.system, cache?.stableLtmSystem, cache?.ltmSystem].filter(Boolean).join('\n\n')\`.

<!-- lore:019e6bc9-17f3-780e-82e5-dfa7c11e329a -->
* **parsePositiveInt rejects 0 — 'set to 0 to disable' env var pattern silently falls back to default**: Trap: \`parsePositiveInt(env.LORE\_VAR, default)\` in \`packages/gateway/src/config.ts\` uses \`n <= 0\` guard — rejects \`0\` and falls back to the default with a warning. Any feature documented as 'set to 0 to disable' via env var silently ignores the user's intent. Confirmed: \`LORE\_SESSION\_EVICTION\_TIMEOUT=0\` silently uses 1800s. Fix: use a separate \`parseNonNegativeInt\` (guard: \`n < 0\`) for settings where 0 is a valid sentinel. Tests that bypass \`loadConfig()\` by setting the config field directly (e.g., \`sessionEvictionTimeoutSeconds: 0\`) will miss this bug — always test the env var path too. PR #478 had this exact bug: test at eviction.test.ts:158 set config directly, hiding the parsePositiveInt rejection.

<!-- lore:019e6ff7-3fcd-7cf6-84f3-632073dba9d6 -->
* **Pi plugin baseUrl mismatch causes 404 on /v1/responses — OpenAI SDK appends path to bare gateway root**: Trap: \`pi.registerProvider('openai', { baseUrl: 'http://127.0.0.1:3207' })\` looks correct because it points to the gateway. But OpenAI SDK's \`responses.create()\` concatenates \`baseURL + '/responses'\` → sends to \`http://127.0.0.1:3207/responses\`. Gateway routes only on \`pathname === '/v1/responses'\` (server.ts:316) → falls through to 404: 'No route for POST /responses'. Fix: Pi must set \`baseUrl: 'http://127.0.0.1:3207/v1'\` (with \`/v1\` suffix) so SDK produces the correct \`/v1/responses\` path. The \`/v1/responses\` route has existed since commit b8a4836 (pre-0.18.0) — this is a Pi plugin configuration bug, not a missing gateway route.

<!-- lore:019e6950-8200-7473-87a5-45a861bb84af -->
* **recall.ts: unescaped quotes/ellipsis in marker strings cause store miss and raw marker leak**: Trap: \`buildRecallMarker()\` embeds raw \`query\` between double-quotes and raw \`id\` before \`…\` — looks safe because queries are usually plain text. But if \`query\` contains \`"\`, \`MARKER\_REGEX\` (\`/📚 Searching (.+?) for "(.+?)"…/\`) truncates the captured group at the first inner quote; \`recallStoreKey()\` produces a mismatched key; \`store.get(key)\` returns undefined; the marker is left as raw text in the conversation visible to the upstream LLM. Same issue for \`id\` containing \`…\`. Fix: escape \`"\` → \`\\"\` in \`buildRecallMarker()\` and unescape in \`parseRecallMarker()\`; for IDs, percent-encode or strip \`…\` before embedding.

<!-- lore:019e2e20-95b3-7a9d-ab38-77d87eafecc4 -->
* **splitSegments() infinite recursion on oversized single messages**: \`splitSegments()\` infinite recursion on oversized single messages in \`packages/core/src/distillation.ts\`: recurses infinitely when a single message exceeds \`maxSegmentTokens\` (16384). \`findSplitIndex()\` returns \`messages.length\` (=1), so \`left = messages.slice(0, 1)\` produces an identical recursive call. Triggered on large tool outputs (~49KB+). Fix: add base case after the \`totalTokens <= maxTokens\` guard — \`if (messages.length <= 1) return \[messages]\`. The oversized message becomes an indivisible segment.

<!-- lore:019e49d5-15c4-78d1-8bbc-f43fde34d9f1 -->
* **startServer() returns plain object, not Promise — use startGateway() for programmatic async use**: Gateway startup traps: (1) \`startServer()\` returns \`{ stop, port, hosts }\` synchronously — use \`startGateway()\` from \`cli/start.ts\` for \`Promise\<GatewayHandle>\`. (2) \`loadConfig()\` in \`config.ts\` is SYNC — calling \`.then()\` throws TypeError. (3) EADDRINUSE fires as async \`'error'\` event — wrap \`server.listen()\` in a Promise. (4) Use port \`0\` (ephemeral) in tests — port 0 always succeeds or throws a non-EADDRINUSE error (unreachable EADDRINUSE path). (5) \`@loreai/core\` exports async \`load(directory)\` — distinct from gateway's sync \`loadConfig()\`. (6) In-process callers (OpenCode plugin, Pi extension) MUST pass \`local:true\` — hosted mode is process-wide and disables FS ops in \`@loreai/core\` (breaks \`getGitRemote()\`, \`.lore.md\` import, config loading). (7) \`lore run\` and \`lore import\` always run locally — agent/files are on the same machine; both set \`opts.local=true\`.

### Pattern

<!-- lore:019e56d6-9cca-7a77-aa1a-a5850daef395 -->
* **Gateway test patterns: unit vs integration vs subprocess isolation**: Gateway test patterns in \`packages/gateway/test/\`. Three patterns: (1) Unit: import functions directly, build fixtures inline — \`makeRequest()\`/\`makeUserMsg()\` factories. (2) Integration: \`createHarness()\` starts real server on random port with replay interceptor, provides \`chat()\` + \`queryDB()\`. (3) Subprocess isolation: \`mock.module()\` calls isolated in subprocess files to avoid global cache pollution. DB isolation via root \`bunfig.toml\` preloading \`packages/core/test/setup.ts\`. \`GatewayRequest\` minimal required fields: \`protocol, model, system, messages, tools, stream, maxTokens, metadata, rawHeaders\`. Use port \`0\` in tests. Session ID regex requires \`\[a-f0-9]\` hex chars only — alphanumeric IDs with non-hex chars (e.g. \`g-z\`) will fail marker extraction.

### Preference

<!-- lore:019e702e-3764-716c-ad7d-db1dfd8cd740 -->
* **Always catch and flag version/numbering discrepancies before merging**: When reviewing PRs or code changes involving schema migrations, always cross-check version numbers across ALL surfaces: PR title, PR description, commit messages, code comments, test assertions, and constants. Treat any inconsistency as a blocking issue. Example: PR #468 had PR title showing 'v28' while commit message said 'v29' and db.test.ts asserted \`toBe(29)\` — required correction before merge. Also flag commit message overcounts (e.g., claiming 9 new tests when actual count is 8 new + 1 modified). Always verify MIGRATIONS.length matches the claimed version number.

<!-- lore:019e8e34-b63d-734d-965b-60c3766703b5 -->
* **Always clean up stale/conflicted local bookmarks when syncing with main**: When syncing a repo with the latest main (e.g., before testing or creating a PR), the user expects stale and conflicted local bookmarks to be identified and cleaned up as part of the process. This includes deleting conflicted bookmarks (those marked with \`\*\` conflict indicators in \`jj bookmark list\`) and removing local-only branches that have already been merged or abandoned upstream. The cleanup should happen proactively when the user asks to 'sync' or 'clean up' branches, not just as an afterthought. Use \`jj bookmark list\` to audit, then delete stale entries with \`jj bookmark delete\`.

<!-- lore:019e8ded-cb0b-75c6-9db2-22b22f9dcf74 -->
* **Always conduct adversarial pre-merge reviews with severity-tiered findings**: When reviewing PRs before merging, the user expects a structured adversarial review that categorizes findings into CRITICAL, MEDIUM, and LOW severity tiers, with each finding including: a short code identifier (C1, M1, L1), the exact file path and line numbers, a description of the bug or issue, the consequence/impact, and a concrete fix proposal. The review must end with an explicit MERGE-READY or BLOCK verdict. CRITICAL findings block merging; MEDIUM and LOW findings are documented but may not block. The user also expects the review to verify specific contract invariants (e.g., pagination behavior, type guard correctness, documentation consistency).

<!-- lore:019e8e21-f1b1-726f-b96e-e35c03d7dd48 -->
* **Always confirm architectural facts by citing exact source code locations**: When discussing pipeline.ts or other core gateway files, the user consistently cross-references assertions against actual source code comments and line numbers. They treat code comments as authoritative documentation (e.g., 'never forwarded upstream', 'never watch client-controlled paths', 'never ran yet — session is new'). When reading files, they extract and record precise constants, function signatures, regex patterns, and architectural invariants with line numbers. The assistant should always cite specific line numbers and quote exact code comments when confirming or explaining architectural facts, and treat inline comments as the canonical source of truth for behavioral constraints.

<!-- lore:019e5975-9590-74b8-a983-35fa3fa00617 -->
* **Always constrain features to minimal viable scope and flag over-engineering risks**: Design philosophy: minimal viable scope, gradual throttling, no over-engineering. (1) Always prefer the simplest implementation that solves the core problem — reject slow/complex alternatives (e.g., \`computeDailyCosts()\` too slow for hot-path), defer non-MVP concerns, avoid extra config surfaces. (2) For rate-limiting/throttling: reject hard blocks or large fixed delays ('too annoying'); prefer EMA-style velocity detection with small incremental delays the user barely notices. Use rate/velocity signals (EMA of cost-per-hour, inter-turn gaps), apply gentle progressive friction, avoid binary allow/block walls. When presenting throttle curves, favor smooth ramps with small minimum delays over step functions with large penalties.

<!-- lore:019e8be5-0f0b-717b-9ac8-a68505bb4d9b -->
* **Always end planning turns with plan\_exit call**: When the user activates Plan mode, they consistently require the assistant to call \`plan\_exit\` at the end of every planning turn to signal completion. This is a mandatory workflow step — the assistant must never leave a planning turn without either asking a clarifying question or calling \`plan\_exit\`. The user has explicitly stated this directive multiple times across sessions and flags it as a hard requirement (🔴 priority). The plan file path is always provided at session start and should be referenced throughout planning. Plan files are stored at \`/home/byk/Code/opencode-lore/.opencode/plans/\<timestamp>-\<name>.md\`.

<!-- lore:019e6914-fa92-764d-bdfa-883f37ec2d46 -->
* **Always feed inner/base accumulator unconditionally before any routing or translation logic**: When implementing stream processing functions (accumulators, translators), always feed the inner/base accumulator unconditionally as the FIRST action in the event handler, before any filtering, routing, suppression, or translation logic. Enforced via code comment in \`stream/anthropic.ts\` line 712: 'Always feed the inner accumulator (it tracks full state)'. Inner accumulator created via \`createStreamAccumulator()\` with no options — never scales (\`scaleClientUsage\` omitted). \`scaleClientUsage\` only affects client-facing output via \`maybeScaleEvent()\`. Apply this pattern whenever wrapping or layering accumulators: call inner \`processEvent()\` first, then apply conditional logic afterward.

<!-- lore:019e5ac1-fea2-71ff-b900-b50443b120ca -->
* **Always include recoverMissingObjects() coverage for new tables in migration reviews**: When reviewing PRs that add new DB tables via migrations, always verify \`recoverMissingObjects()\` in \`db.ts\` covers every new table with \`CREATE TABLE IF NOT EXISTS\`. If omitted: partial migration leaves tables missing permanently — subsequent runs see the version already set and skip the migration. Flag as CRITICAL. Also: \`SCHEMA\_VERSION\` constant (stuck at 16) is dead code — \`migrate()\` uses \`MIGRATIONS.length\`. Always \`git pull --rebase\` before starting migration work to avoid version number collisions.

<!-- lore:019e7081-1377-7b08-8006-ec48d854c1a0 -->
* **Always investigate compaction/context issues with multi-area parallel exploration**: When investigating why compaction triggers unexpectedly, always investigate all 5 areas simultaneously: (1) compaction.ts math and thresholds, (2) pipeline.ts unsustainable detection and compaction hint logic, (3) server.ts endpoint handlers for streaming/non-streaming mismatches, (4) protocol translation files (translate/openai.ts) for usage field correctness, (5) codebase-wide search for compaction-related injected messages. Focus on how usage scaling interacts with non-Anthropic protocol models (OpenAI-protocol like MiniMax) where Anthropic-format internal responses are translated before being returned to the client. AUTOCOMPACT\_THRESHOLD=167,000 is hardcoded for 200K context models — if client uses a different model context window, scaling may be ineffective.

<!-- lore:019e5723-894e-7cbe-8dbb-e1e2da88a328 -->
* **Always investigate Sentry issues by exploring the full codebase context before proposing fixes**: Sentry investigation and triage rules: (1) Always explore full source files in \`packages/gateway/src/\` (not bundled dist), tracing call chains across auth/adapter/pipeline/idle/instrumentation before proposing fixes. (2) Enumerate ALL contributing root causes (numbered, with file:line) before proposing any fix. (3) Check user count and event count first; skip fixes for issues with 0 real users caused by caller-side misuse. (4) At session start, proactively fetch Sentry issues, triage by severity (user count, event count, regression, fatality), and present a prioritized action list. (5) \`log.error(..., errorObj)\` auto-forwards to Sentry via \`sink.captureException()\` — use typed error subclasses (e.g. \`LocalProviderUnavailableError\`) and \`beforeSend\` in \`instrument.ts\` to filter expected errors.

<!-- lore:019e8ddc-da3f-7af3-8076-af32b8095f02 -->
* **Always monitor CI results and report pass/fail status for all jobs before marking work complete**: After pushing a PR, the user consistently tracks CI run results at the job level — noting each job name, status (pass/skip/fail), and duration. Work is only considered done when all non-skipping CI checks pass. The user expects the assistant to report the full CI status (test, binary smoke darwin-arm64, binary smoke windows-x64) and explicitly confirm the PR is green and ready for review. Skipping jobs (nightly/release/delta patches) are noted as expected for PR branches. This pattern applies after every commit+push+PR cycle.

<!-- lore:019e8db2-87d0-77c2-9fae-9cf2806fdb88 -->
* **Always perform adversarial pre-merge code review before committing or merging**: Before merging any PR, the user expects a thorough adversarial review that categorizes findings by severity: CRITICAL, MEDIUM, LOW. Reviews cover edge cases, missing tests, security issues (e.g., injection via unsanitized input), correctness bugs (e.g., wrong variable used, off-by-one), and invariant violations. The review should read actual source files rather than relying on PR descriptions alone. Findings should be enumerated with specific file paths and line numbers. This applies to both the user's own PRs and others' PRs in the same repo.

<!-- lore:019e6994-3f8a-74cc-9c47-9f878dce7c4c -->
* **Always prefer semantically correct protocol structures over workarounds when fixing LLM behavior issues**: When the user encounters LLM misbehavior (e.g., echoing internal data, leaking formatting during streaming), they consistently prefer fixing the root cause by using the correct protocol semantics (e.g., tool\_use/tool\_result pairs) rather than brittle workarounds like XML wrapping, prompt instructions, or cache tricks. Explicitly rejected Option B (XML wrapping + prompt instructions) as 'relies on LLM following instructions, doesn't fix the root cause.' When proposing or implementing fixes, always lead with the semantically correct approach and explain why it prevents the issue structurally. Do not suggest prompt-engineering workarounds as primary solutions.

<!-- lore:019e6a9b-6376-73b1-93ec-da6801cced43 -->
* **Always request a pre-merge self-review with adversarial/critical eye before merging PRs**: Pre-merge and investigation workflow: (1) Always do read-only architecture analysis before implementation — trace call paths, file/line refs, document gaps; no edits during investigation phase. (2) Adversarial pre-merge review: read ALL changed files, check correctness/edge cases/type safety/test coverage/PR description accuracy vs. diff. Flag tests bypassing config parsing. Classify findings CRITICAL/MEDIUM/LOW. (3) Verify typecheck (\`bun --filter '\*' typecheck\`, all 4 packages) and tests pass before PR creation. (4) Confirm all CI checks green before merging. (5) Verify exact counts/details before stating in commit messages or PR descriptions — user catches discrepancies.

<!-- lore:019e8dfb-0869-7408-b8e2-daa747d5fec1 -->
* **Always verify file contents after implementation with exact line counts and key details**: After implementing or modifying files, the user consistently confirms the actual file contents with precise metadata: line count, whether the file is NEW or MODIFIED, exported symbols, key constants, notable logic details, and specific line numbers for important behaviors. This applies to both source files and test files. The user expects observations to capture not just 'what was done' but 'what the file actually contains' — including edge cases tested, interface shapes, default values, and implementation decisions. When writing tests, the user also notes test pollution prevention patterns (beforeEach/afterEach save/restore) and specific edge case descriptions.

<!-- lore:019e8e32-6d33-71fa-b410-e1841e729c32 -->
* **Always verify implementation details through direct code inspection before asserting behavior**: The user consistently performs or directs precise file-and-line-level code investigations before making assertions about system behavior. They cite exact file paths, line numbers, function names, and code logic when asserting facts (e.g., 'gateway never sets \`store\`', '\`item\_reference\` is explicitly dropped at openai-responses.ts:196'). When reviewing code, they trace edge cases, confirm invariants, and explicitly note when something is a pre-existing issue vs. in-scope. Assertions are always grounded in grep results, file reads, or test evidence — never assumed. When providing analysis or fixes, always reference specific line numbers and file paths, and distinguish confirmed findings from assumptions.

<!-- lore:019e2168-2fa4-77bd-a557-9d6dbcb40d81 -->
* **Prefer WASM backend over native onnxruntime-node for compiled binaries**: WASM backend for Bun \`--compile\` binaries with transformers.js: \`binaryExternalsPlugin\` in esbuild redirects \`onnxruntime-node\` → \`onnxruntime-web\` via \`onResolve\` (static imports only — does NOT redirect dynamic \`import()\` calls) and patches transformers.js CDN fallback via \`onLoad\` to read \`wasmPaths\` from \`globalThis.\_\_LORE\_VENDOR\_WASM\_PATHS\_\_\` (object form \`{ mjs, wasm }\` with exact hashed \`$bunfs\` filenames — directory strings fail). WASM files embedded as Bun \`{ type: 'file' }\` assets. For npm/CJS builds, \`onnxruntime-node\` stays external. WASM is ~2x faster on batches than native. Importing \`onnxruntime-web\` explicitly alongside the redirect creates two ort instances — 'cannot register backend cpu using priority 10' error.

<!-- lore:019e68cd-651a-7a1f-86b0-43d36249e915 -->
* **Prefers recall over assuming you don't have the information**: prefer recall over assuming you don't have the information.

<!-- lore:019e5b1a-0620-7016-b6e6-c5bdc392252e -->
* **Prefers update over create**: prefer update over create,

<!-- lore:019e5aa0-1e5f-7da9-a1f4-5b3b3f72b8e9 -->
* **Review code before committing**: Code review and workflow preferences: (1) Adversarial pre-merge review — severity-classified (CRITICAL/MEDIUM/LOW), file:line refs, concrete fixes; check PR description accuracy, test coverage, race conditions, type safety, SQL bugs. (2) Investigate Sentry issues by reading full source, tracing call chains, enumerating all root causes before fixes. (3) Run \`bun --filter '\*' typecheck\` (all 4 packages must pass) and full test suite before PR creation; note exact pass/fail/skip counts; distinguish pre-existing failures. (4) Verify all CI checks pass before merging. (5) Prefer update over create for knowledge entries. (6) Prefer minimal viable scope — gradual throttling over hard blocks, EMA signals over step functions. (7) Defer architectural changes when existing mechanisms already handle the case. (8) Always produce a structured implementation plan (file paths, line numbers, tasks) before making code changes — enumerate every affected file, functions/lines, backward-compatibility constraints, new module wiring. (9) Annotate architectural facts with source file, line numbers, and exact values. (10) Trace whether functions are actually called/wired up — flag dead code as a design con \[truncated — entry too long]
