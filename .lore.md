<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019ee004-037c-70e6-a910-eef98c0778aa -->
* **A1 quota trigger: counter-based enforce\_row\_quota — only INSERTs and revivals grow live-row count**: Post-A1 \`enforce\_row\_quota()\` (migration 0007): reads O(1) from \`user\_table\_usage(scope\_id, table\_name, row\_count, byte\_count)\`. Advisory lock keyed on \`hashtextextended(scope\_id::text || '|' || tg\_table\_name, 0)\` — MUST be taken before \`exists\_pk\` check (0005 ordering); 0007 moved \`exists\_pk\` outside lock (lines 226-239 before lock at 267) — SHOULD-FIX: restores TOCTOU hardening. Revival is an UPDATE with \`d\_bytes=0, d\_rows=0\` — guard returns early, never gated (row already counted at INSERT). Tombstone INSERTs now counted+gated (physical accounting). \`greatest(...,0)\` clamp never over-removes. \`user\_table\_usage\` has NO DML grant to \`authenticated\` — written ONLY by SECURITY DEFINER triggers. Raises errcode 23514 on violation. Self-DoS: user creating+deleting ~500 entries can exhaust free cap with zero live rows; no client-side reclaim path until A2 hard-DELETE reaper.

<!-- lore:019ee6bc-3ce2-7f81-a295-3271f5703cd8 -->
* **A2 sub-PR 2b-1: cross-references key on logical\_id, not per-version id**: PR #848 (commit \`01d035db\`): rebinds all knowledge cross-reference linkages (entity refs \`knowledge\_entity\_refs\`, wiki-links \`knowledge\_refs\`, transfer metrics \`knowledge\_transfers\`, \`.lore.md\` markers, tombstones) to key on stable \`logical\_id\` instead of per-version row \`id\`. Behavior-preserving today (\`id == logical\_id\` for all v1 rows). New helpers: \`getByLogical(logicalId)\` queries \`knowledge\_current WHERE logical\_id=?\`; \`logicalIdFor(id)\` queries base \`knowledge\` table (resolves superseded version ids). \`logical\_id\` intentionally excluded from \`syncColumns\` — remote mirror lacks that column; push values byte-identical to pre-PR. \`knowledge\_transfers\` and \`knowledge\_refs\` not in \`SYNCED\_TABLES\` — no egress impact.

<!-- lore:019ee74b-44ab-705f-bd98-9afb1301d8e5 -->
* **A2 sub-PR 2b-2b (#853): append-only flip — update/remove rewired to appendVersion**: PR #853 (commit \`0ec8949f\`, 15 files, 341+/116−): rewires \`update()\`/\`remove()\` onto \`appendVersion()\`. \`update()\` resolves id→logicalId via \`logicalIdOf\`, calls \`appendVersion\` if content changed, then \`UPDATE … WHERE logical\_id=? AND is\_current=1\` for mutable metadata. \`remove()\` calls \`appendVersion(logicalId, {isDeleted:true})\` — no physical DELETE; explicitly cleans \`knowledge\_entity\_refs\`, \`knowledge\_refs\`, \`knowledge\_transfers\` by \`logical\_id\` (FK CASCADE dormant since rows aren't deleted). \`appendVersion()\` now returns \`string|null\` (was void); current-row lookup inside \`withTransaction()\` (TOCTOU fix). \`clearTombstone()\` cannot lift death-cert tombstones — only clears legacy \`knowledge\_tombstones\` rows; test-only caller. \`isTombstoned()\` dual-path: (1) \`knowledge WHERE logical\_id=? AND is\_current=1 AND is\_deleted=1\`; (2) fallback \`knowledge\_tombstones WHERE id=?\`.

<!-- lore:019ee535-02c2-7e66-93a1-5fb8ba605c86 -->
* **A2/#823: append-only knowledge schema — FTS and FK CASCADE constraints**: A2 migration = Version 50 (index 49 in MIGRATIONS array). \`db.test.ts:61\` asserts version 50. Architecture: \*\*Option A\*\* (single append-only table). Key structures: \`knowledge(logical\_id TEXT, version INTEGER DEFAULT 1, is\_deleted INTEGER DEFAULT 0, is\_current INTEGER DEFAULT 1)\` immutable rows; \`knowledge\_current\` view = \`SELECT k.\* FROM knowledge k WHERE is\_current=1 AND is\_deleted=0\` (db.ts:1264–1265); indexes \`idx\_knowledge\_logical(logical\_id, version)\` and \`idx\_knowledge\_current(is\_current)\`; FTS triggers current-aware; \`knowledge\_meta(logical\_id, confidence, last\_reinforced\_at)\` deferred to sub-PR 3. Sub-PR status: 1 (#839) MERGED; 2a (#847) REVIEWED/SHIP-WITH-NITS; 2b-1 (#848, logical\_id rebind) SECURITY-REVIEWED/SHIP; 2b-2 (appendVersion writes + UNIQUE INDEX + transaction wrap); 3 (sync + knowledge\_meta); 4 (compaction). \`recoverMissingObjects()\` must include A2's new tables/view. \`knowledge\_entity\_refs.knowledge\_id\` has FK \`REFERENCES knowledge(id) ON DELETE CASCADE\`.

<!-- lore:019eec5f-e309-70b8-bc82-afb5af474ece -->
* **agents-file.ts \_importEntries: foreign-marker security guard — never overwrite another project's private knowledge**: Security invariant (PR #893, \`agents-file.ts:572–579\`): if \`existing.cross\_project === 0 && existing.project\_id !== null && existing.project\_id !== ensureProject(projectPath)\` → \`continue\` (skip). A \`.lore.md\` marker resolving to a DIFFERENT project's project-scoped entry is treated as foreign and silently skipped. Cross-project/global entries (\`cross\_project=1\` or \`project\_id=null\`) remain shared/updatable. Tombstoned entries also blocked (isTombstoned check at :587). Minor: \`ensureProject()\` called N times per loop for same \`projectPath\` (N redundant indexed SELECTs) — acceptable on cold import path, could be hoisted. 🔴 'Never overwrite another project's private knowledge' — explicit code comment directive.

<!-- lore:019eea38-ef3b-7a7a-ab73-ad7287731560 -->
* **agents.ts: AGENTS registry — 5 agents, env injection patterns, CLAUDE\_CODE\_FIRST\_PARTY\_ENV invariant**: 5 agents: claude-code (\`ANTHROPIC\_BASE\_URL\`, \`DISABLE\_AUTO\_COMPACT\`, \`CLAUDE\_CODE\_FIRST\_PARTY\_ENV\`), codex (CLI args \`-c openai\_base\_url\`, not env \`OPENAI\_BASE\_URL\`), pi (\`ANTHROPIC\_BASE\_URL\`+\`LORE\_GATEWAY\_URL\`), opencode (\`OPENCODE\_CONFIG\_CONTENT\` JSON plugin config — env bypass because \`resolveSDK()\` always passes \`options.baseURL\`), hermes (\`OPENAI\_BASE\_URL\`). 🔴 NEVER remove \`CLAUDE\_CODE\_FIRST\_PARTY\_ENV\` for claude-code: Claude Code ≥2.1.181 suppresses \`cch\` billing field unless host is exactly \`api.anthropic.com\`; without this flag, \`resignBody\` cannot re-sign the billing header it modifies.

<!-- lore:019ecd94-4df4-787b-89f7-31873df5ec0e -->
* **agents.ts: AGENTS registry shape and run.ts explicit-command spawn lookup**: \`AgentDef\` interface (agents.ts:16-33): \`name\`, \`displayName\`, \`binary: string\`, \`detect(): string | null\`, optional \`envVars()\`, optional \`cliArgs()\`. \`binary\` MUST be a constant placeholder string (NOT resolved at module load — avoids stale registry in long-running processes and platform-mutation test issues). \`detect()\` is called fresh each invocation. \`run.ts\` explicit-command branch: if matched by \`agent.name === cmdArgs\[0]\` → use \`detect()\`; if matched by \`agent.binary === cmdArgs\[0]\` → use literal \`cmdArgs\[0]\` (zero behavior change for existing PATH agents). \`main.ts\` gatekeeper MUST use \`AGENTS.some((a) => a.name === command || a.binary === command)\` — NOT \`AGENTS.map(a => a.binary).includes(command)\` (binary is a placeholder, not the resolved path). Current registry (6 agents): \`claude-code\`, \`claude-code-desktop\`, \`codex\`, \`pi\`, \`opencode\`, \`hermes\`.

<!-- lore:019ef0c0-02e8-701a-824e-329ffb264c14 -->
* **Append-only knowledge versioning invariants**: Two identities per entry: logical\_id (stable entry identity) + version (immutable snapshot keyed by UUIDv7). Invariant: version rows are immutable except is\_current — content, title, category, is\_deleted, version, logical\_id NEVER mutate after insert. Invariant: never resurrect a logical\_id whose latest version has is\_deleted=1 — no separate tombstones table needed. All non-FTS reads use knowledge\_current view (is\_current=1 AND is\_deleted=0). FTS5 indexes only current+live rows; superseded version's FTS row removed on append. Knowledge outbox keyed by logical\_id (not version id) so deletes always match their remote row.

<!-- lore:019eebaf-0f2c-7446-bd42-52f7f4366aca -->
* **background-limiter.ts: activeTasks Set, drainBackground, and boundedSettle — drain invariants**: \`runBackground()\` tracks STARTED (not queued) tasks in \`activeTasks = new Set\<Promise\<unknown>>()\` — task added inside \`limiter(async …)\` callback after \`fn()\` invoked, removed in \`.finally()\`. Queued-but-not-started tasks are never in the set. \`drainBackground(timeoutMs=5000)\`: calls \`limiter.clearQueue()\` (discards queued tasks whose promises never settle), then \`boundedSettle(\[...activeTasks], timeoutMs)\`. \`boundedSettle(promises, timeoutMs)\`: races \`Promise.allSettled\` against a timeout that only ever resolves — timer always cleared in \`finally\`, \`Promise.allSettled\` never rejects. 🔴 Never strand the drain on a task that will never run — only started tasks tracked. 🔴 Never hang a reset; timeout is a safety valve, not a correctness boundary. Livelock NIT: one-shot \`clearQueue\` + snapshot; tasks spawning \`runBackground\` mid-drain escape — not realized in current code.

<!-- lore:019ef3cd-af26-7cce-aa3a-090e43e6210e -->
* **Bedrock dual-endpoint architecture: bedrock-mantle vs bedrock-runtime**: AWS Bedrock has two inference endpoints: (1) \`bedrock-mantle.{region}.api.aws\` — recommended for new apps; supports Messages API, Chat Completions, Responses API, API key auth (\`x-api-key\` header), \`anthropic-version\` as HTTP header, no SigV4 required, no cross-region inference profiles needed; (2) \`bedrock-runtime.{region}.amazonaws.com\` — existing/native SDK path; SigV4 only; \`anthropic\_version\` in request body; supports InvokeModel, Converse, Guardrails, cross-region inference profiles. Critical: bedrock-mantle does NOT support structured outputs (\`output\_config.format\`) — returns 400. bedrock-runtime does NOT support Responses API or stateful conversation. Both support prompt caching and per-token pricing is identical.

<!-- lore:019ef47b-b8a8-7282-a8e1-538a0338b8ec -->
* **Bedrock Mantle: OpenAI-compat surface replaces bedrock-runtime — new routing model**: Bedrock Mantle (\`bedrock-mantle.us-east-1.api.aws\`) exposes an OpenAI-compatible API surface (\`GET /v1/models\`, \`/v1/chat/completions\`) authenticated via \`Authorization: Bearer \<ABSK\_KEY>\` — NOT SigV4. The existing \`bedrock-runtime.{region}.amazonaws.com\` + SigV4 path is being refactored/removed on branch \`feat/bedrock-mantle\`. Key difference: mantle uses long-term ABSK keys (base64, ~24h expiry), not AWS IAM credentials. Model IDs use dot-format (e.g. \`anthropic.claude-haiku-4-5\`). Anthropic Claude models still require separate per-model opt-in in Bedrock console (Model access page, us-east-1) — \`AmazonBedrockFullAccess\` IAM policy alone is insufficient.

<!-- lore:019eea78-60b3-7d6e-b90d-2ddab87e8031 -->
* **cache-analytics.ts: raw vs normalized body invariants — upstream never modified, analytics copy only**: Three load-bearing invariants (code comments, cache-analytics.ts): (1) 'The upstream request is NEVER modified — only the analytics copy.' (2) \`cache\_control\` marker 'always differs from the next turn's body' — produces false-positive mid-conversation divergence if stored normalized. (3) 'Anthropic always appends the marker as the last property of a content block.' \`normalizeBodyForComparison\` applies 4 regex replacements: CCH\_PATTERN→\`cch=\_\_\`, CC\_VERSION\_SUFFIX\_PATTERN→\`$1.\_\_\_;\`, CACHE\_CONTROL\_PATTERN→\`""\`, TOP\_LEVEL\_MAX\_TOKENS→\`$10\`. Function is idempotent. \`lastRequestBodyLength\` is set to \`normalizedBody.length\` (not raw) — used only as divergence-ratio denominator in log display, not in \`prefixMatchPercent\` calculation.

<!-- lore:019ee670-8bf7-7f04-8c19-2094f12cfe81 -->
* **cache-economics.ts: cost formulas and tie-break invariant**: Formulas at \`cache-economics.ts:190-205\`: \`holdWarm = k·F·r + p·(1+f)·F·r\`; \`coolBust = p·(C·w + f·C·r)\`; \`coolFullWrite = p·(F·w + f·F·r)\`. Keepalive term (\`k·F·r\`) is unconditional — warmups paid during idle regardless of return probability. Tie-break at lines 208-212 uses strict \`<\` — defaults to less-warming option on every tie. 🔴 Invariant: \*\*never pay to warm on a tie\*\* — strict \`<\` satisfies this. Units: warmer passes \`costPerMTok/1\_000\_000\` → $/token; gradient \`getCachePricing()\` returns per-token. \`expectedFutureTurns\` capped at \`SHADOW\_FUTURE\_TURNS\_CAP=50\`. Cool strategies conservatively assume a WRITE on return even within TTL — safe over-penalization, biases toward not-busting.

<!-- lore:019ee578-4eb7-7473-9ea3-0b15235d1c52 -->
* **cache-warmer: lastRequestBody invalidation gap — idle mutations not covered**: Root cause of warmup cache misses on large sessions: \`lastRequestBody\` (set at \`cache-analytics.ts:590\`) stores the normalized post-transform body that established the upstream cache. The 4 null-out sites in \`pipeline.ts\` (lines 4293, 4719, 4898, 4997) cover ONLY client-side \`/compact\` and model/provider switches — NOT lore's own idle-time prefix mutations. When meta-distillation rewrites \`messages\[1]\`, post-idle gradient compaction halves raw budget, or prompt-delta upserts shift message positions, \`lastRequestBody\` is NOT invalidated. Warmer replays pre-mutation body; next real turn discards it. Fix: null out \`lastRequestBody\` at each idle-time mutation site. Architectural tension: lore rewrites the PREFIX (append-only cache-friendliness vs aggressive compression are in conflict). PR #838 (diagnostic logging) merged 2026-06-20 to distinguish expiry vs body-mismatch misses.

<!-- lore:019ee201-4395-7551-83fd-54ff714be1dc -->
* **cache-warmer: per-bucket circuit breaker key and recovery paths**: Per-bucket circuit breaker (PR #836, Jun 20 2026): bucket key = \`sessionID \x1f model \x1f upstreamURL\`; \`unknown\` substituted for null fields but \`executeWarmup\` is gated behind \`resolveProfile\` returning non-null so \`unknown\` bucket never trips. \`BreakerEntry = { failures: number; tripped: boolean; trippedAt: number }\` — \`failures\` in-memory only; only tripped buckets persisted. KV v2 format: \`{ version: 2, buckets: { \[key]: { trippedAt } } }\`. Legacy v1 auto-cleared on first load. \`pruneExpiredCircuitBreakers()\`: GC for dead-session stale buckets — wired into idle tick before per-session loop. Three reset paths: (1) \`resetCircuitBreaker(bucketKey?)\` — production reset; (2) \`/lore:warm:reset\` slash command; (3) \`POST /ui/api/warming/:sessionId/reset\`. \`getCircuitBreakerSummary(now)\` filters display but does NOT delete. \`getCircuitBreakerStatus\` is a PURE READ — computes decay inline, no map mutation, no SQLite write. \`CIRCUIT\_BREAKER\_DECAY\_MS\` = 6h auto-decay constant.

<!-- lore:019edc02-dfde-7130-b70c-99ddf8734dcd -->
* **cache-warmer.ts: creditWarmupHit savings attribution — use lastWarmupRefreshTokens, not returning turn's cacheReadInputTokens**: 🔴 \`creditWarmupHit\` (cache-warmer.ts:1730-1785): savings MUST be credited against \`lastWarmupRefreshTokens\` (the prefix the warmup actually refreshed, recorded at line 1696), NEVER the returning turn's \`cacheReadInputTokens\`. Tool-use continuations / breakpoint shifts report a far smaller read on the returning turn, undercounting savings ~10×. Phantom-savings guard: hit only attributed when \`lastWarmupRefreshTokens > 0 && totalWarmups > 0\`. \`lastWarmupAt\` alone is insufficient (can ride along in restored/inherited blob). On confirmed hit: zeros both \`lastWarmupAt\` and \`lastWarmupRefreshTokens\` (warmup benefits only the first returning turn); increments \`warmupHits\`.

<!-- lore:019ee018-a829-706c-802d-bc63bea6da41 -->
* **cache-warmer.ts: global circuit breaker — persisted to KV, no production reset path**: Cache warmer circuit breaker (\`cache-warmer.ts\`): global across ALL sessions/providers (not per-session). Trips after \`CIRCUIT\_BREAKER\_MAX\_FAILURES=3\` consecutive uncached warmups (\`cacheCreationTokens>0 AND cacheReadTokens===0\`). State persisted to SQLite KV under \`CB\_KV\_KEY='warmup\_circuit\_breaker'\` — survives process restarts. Once tripped, ALL warming disabled for all sessions. \`ensureCircuitBreakerLoaded()\` restores state on first access. Counter resets ONLY on successful cache read, NOT on errors. No production reset mechanism — \`\_resetForTest()\` is the only reset path (writes \`{tripped:false,failures:0}\` to KV). No UI endpoint to reset. \`computeWarmingSnapshot()\` returns \`notWarmingReason: 'Circuit breaker tripped'\` when latched. Sentry metric: \`lore.cache\_warmup.circuit\_breaker\_tripped\`.

<!-- lore:019eefe8-ce95-7ef7-9edd-596c942c6b5b -->
* **compaction.ts: per-model usage cap formula and constants (PR fix/per-model-usage-scaling)**: \`maxReportedUsageForModel(contextWindow, maxOutputTokens) = Math.max(0, floor(0.9 \* ((contextWindow - min(maxOutputTokens, 20\_000)) - 13\_000)))\`. \`DEFAULT\_MAX\_REPORTED\_USAGE = 150\_300\` = result for 200K/20K. Exported constants: \`AUTOCOMPACT\_BUFFER\_TOKENS=13\_000\`, \`MAX\_OUTPUT\_RESERVE=20\_000\`, \`USAGE\_REPORT\_RATIO=0.9\`. \`AUTOCOMPACT\_THRESHOLD=167\_000\` retained for cost-tracker counterfactual only (TODO: make per-model). \`Math.max(0,...)\` floor prevents negative for tiny windows. Internal accumulator in \`createRecallAwareAccumulator\` NEVER scales — inner \`StreamAccumulator\` always created without \`scaleClientUsage\`, preserving real values for calibrate/bustRate. \`getModelEntrySync()\` used (not async) in pipeline; fallback default: \`{context:200\_000, output:8\_192}\` → yields ~160,927 cap (not 150,300 as documented).

<!-- lore:019edb00-69e2-7282-a2a3-edd5482dbd84 -->
* **consecutiveBusts is in-memory only — not restored across restarts, written to DB as diagnostic only**: \`consecutiveBusts\` in \`gradient.ts\` is intentionally NOT restored from DB across restarts. Written to \`dynamicContextCap\` DB column as write-only diagnostic. \`SUSTAINED\_BUST\_THRESHOLD=2\`; \`BUST\_PRESSURE\_THRESHOLD=3\`. \`unsustainable: true\` can ONLY surface on paths B/C/D — never on layer-0 passthrough (path A). Restart resets bust pressure intentionally. \`recordCacheUsage(isIdleResume=false)\`: if \`bustRatio > 0.5 && isIdleResume\`, logs but does NOT increment \`consecutiveBusts\`; if \`bustRatio <= 0.5\`, resets to 0 regardless of \`isIdleResume\`. Cache bust classification via \`categorizeBust(analysis, isPostIdle)\` in \`cache-analytics.ts\` — \`CacheBustCause\` union has 9 literals: \`"first-turn"\`, \`"system-host-change"\`, \`"system-ltm-change"\`, \`"tools-change"\`, \`"prefix-rewrite"\`, \`"window-shift"\`, \`"idle-resume"\`, \`"incremental"\`, \`"unknown"\`. \`"system-change"\` split (T4, PR #791) into \`"system-host-change"\` (system\[0]) and \`"system-ltm-change"\` (system\[1]/system\[2]).

<!-- lore:019ee6ae-7cc9-767a-b932-087221cf4e51 -->
* **embedding OOM adaptive cap: halve-and-respawn design with memory-aware initializer**: Branch \`fix/embedding-oom-adaptive-cap\`. Two-mechanism design: (1) Memory-aware initializer: \`cap₀ = clamp(√((freemem·0.5 − 400MB)/192), 256, 8192)\` — coarse first-run estimate only. (2) Halve-and-respawn backoff: on worker OOM-exit, main thread (embedding.ts) multiplies cap × 0.7, persists \`{cap, freememAtLearn}\` to kv\_meta, respawns worker with new cap, re-submits failed item. Fresh WASM heap per attempt — unlike in-process retry where WASM linear memory never shrinks after OOM. Floor=256 tokens (~3MB attention tensor); below floor → latch \`localProviderKnownBroken\` + surface 'switch to voyage/openai' hint. No additive increase — restart re-evaluates initializer from current freemem for upward adaptation. Persisted cap prevents re-convergence on restart (fixes Onur Temizkan's 'recurs on every restart' bug). In-process OOM retry ladder (embedding-worker.ts:504–539) deleted entirely.

<!-- lore:019ee6ae-7ce4-7699-aa77-9cb462214888 -->
* **embedding OOM instrumentation plan: 4 items across 3 PRs**: PR1 (embedding fix): instrumentation D (details TBD). PR2: A = periodic RSS + event-loop-lag gauge in idle tick (rss/heapUsed/external/arrayBuffers + monitorEventLoopDelay p99); B = startup-backfill span + RSS delta (runStartupBackfill returns stats, logs to stderr). PR3: C = client-abort-under-pressure capture at pipeline.ts:3182/7196 (fingerprinted). 🔴 All 4 instrumentation items (A/B/C/D) must be included — Burak Yigit Kaya directive: 'don't forget the instrumentation bits please'. Coverage stats in runStartupBackfill must always log to stderr so the problem is visible.

<!-- lore:019eeb20-310e-7c81-9af2-04409eb4157d -->
* **gradient.ts cacheSizeCompressed: authoritative exit-point write, scale caveat for PR2b**: PR #883 (\`fix/881-layer1-compressed-size\`): \`cacheSizeCompressed\` now set authoritatively at \`transform()\` exit using \`result.totalTokens\` for layer ≥ 1 paths. Previously, baseline set \`cacheSizeCompressed = cacheSizeFull\` and only the layer-0 tier-gate refined it — sessions held at layer ≥ 1 (sticky-layer, prefix-present floor, post-idle compact, first-sight-large) reported degenerate full==compressed. Scale caveat (issue #886, \`SCALE CAVEAT\` comment in gradient.ts): \`cacheSizeFull\` is INPUT-scale (overhead+LTM included, ×1.5 uncalibrated); \`result.totalTokens\` is BODY-scale (distilled+raw only). Convention pre-existed in old tier-gate. 🔴 PR2b MUST normalize both sides to same scale before any consumer acts on the delta.

<!-- lore:019eea52-a235-7cc4-985a-0a3dfbbffeb9 -->
* **gradient.ts: l0cap (maxLayer0Tokens) governs compression trigger, NOT layer-1 window size**: Hard design invariant: \`maxLayer0Tokens\` (l0cap) is the cost-cap threshold that determines WHEN to compress (layer-0 exit), not the size of the layer-1 window. Layer-1 \`rawBudget = usable\*0.4\` is intentionally larger than l0cap to give the stable window headroom for chunked eviction. Clamping the base rawBudget to l0cap breaks cache-stability. Only escalated-stage budgets (layers 2+) should be clamped via \`stageBudgetUsable = Math.min(usable, stageBudgetCeiling)\`. \`computeLayer0Cap(targetCostPerTurn, cacheReadCostPerToken)\` returns 0 if either arg ≤ 0; otherwise \`max(floor(target/readCost), MIN\_LAYER0\_FLOOR=40\_000)\`. \`layer0Bounds()\` is extracted so pipeline's turn-1 LTM decision (\`isLargeColdStart\`) shares ONE definition with layer choice. \`isEscalated = stageLayer > 1\`. 🔴 Any path where \`layer0Input\` uses a DIFFERENT effective factor (e.g. \`layer0Ceiling\` 0.7 cold-cache tightening at :2212-2213) that would make compressed's scale not match full's must be flagged.

<!-- lore:019ee58a-a5e5-7e44-86d9-85b37acd69f0 -->
* **gradient.ts: shouldCompress() economics — sustained busts mean compress, not stop**: 🔴 Two key invariants from code comments: (1) \`shouldCompress()\` deliberately does NOT say 'never compress' on high bust count — structural system\[2] busts eliminated by reorder-tolerant LTM pin; sustained bust now indicates raw-window growth, so compressing (one write) is cheaper than rewriting the whole grown context every turn. (2) Compression 'never busts an expensive cache' — \`shouldCompress()\` always returns true in that context (gradient.ts:408). Economics: \`bustCost = compressedTokens × cacheWriteCostPerToken\`; \`continueCost = currentTokens × (writePerToken if sustainedBust else readPerToken)\`; compress when \`bustCost < threshold × continueCost\`. \`cacheWriteCostPerToken\` reset to 0 at line 835 (free-write path). Call site at line 2431 passes \`freeWrite\` flag.

<!-- lore:019eefd7-e4b9-78ed-9dc5-aaca2a55fd9c -->
* **idle.ts shouldHoldPrefixWarm: pure helper for D6′ deferred prefix work gating**: \`shouldHoldPrefixWarm(econ: CacheStrategy | null | undefined, bustPressure: boolean): boolean\` — pure helper extracted for unit testing (following \`decideSkipCompact\` pattern). Returns \`true\` only when \`strategyWantsWarming(econ) && !bustPressure\`. Does NOT gate on \`isCacheWarm\` liveness (unlike \`decideSkipCompact\`) — intentional: strategy is always ≥1 tick stale (written only by warmer at cache-warmer.ts:1252, not by transform; idle handler runs before warmer loop in same tick). \`bustPressure = busts >= BUST\_PRESSURE\_THRESHOLD\` (exported constant, gradient.ts:1063) — NOT derived from \`metaThreshold < baseMetaThreshold\` (degenerate at schema minimum \`metaThreshold==3\`). When \`holdingWarm=true\`: \`force: !holdingWarm\` defers distillation; \`metaDistill\` also gated on \`!holdingWarm\`. Urgency always overrides hold-warm.

<!-- lore:019ef3a3-dd28-7199-8859-ab916b7dfa27 -->
* **Issue #627 Phase 0: reference-validity validator design — cannot-verify is strict no-op**: Phase 0 reference-validity validator (issue #627): scope = \`file:line\` + command refs only; symbol resolution deferred to #911. \`ReferenceResolver\` with 3 strategies: Direct-FS / Synthetic-probe / No-op (discriminated by whether gateway can \`stat\` client project root). Remote gateway mode (e.g. Onur Temizkan's setup) cannot run local git commands — must use Synthetic-probe or No-op. Remote path piggybacks session-start probe (shared plumbing with #627 Phase 1 \`gitHead\` capture). Invariant: 'cannot verify' ≠ 'broken' — unverifiable is strict no-op. Penalty path lowers confidence WITHOUT touching \`last\_reinforced\_at\` (see reinforce() gotcha). \`RESOLVE\_PROJECT\_SCRIPT\` at synthetic-tools.ts:365-370; gateway strips ALL synthetic tool\_use/tool\_result blocks from messages.

<!-- lore:019ef387-4ae0-7676-b7b5-140c85ad2627 -->
* **knowledge\_meta table: confidence/last\_reinforced\_at moved off knowledge table (v55)**: Migration v55 moves \`confidence\` and \`last\_reinforced\_at\` off the \`knowledge\` table into a new convergent register table: \`knowledge\_meta (logical\_id TEXT PRIMARY KEY, confidence REAL NOT NULL DEFAULT 1.0, last\_reinforced\_at INTEGER, updated\_at INTEGER NOT NULL DEFAULT 0)\`. \`knowledge\_current\` view exposes both via LEFT JOIN: \`SELECT k.\*, COALESCE(m.confidence, 1.0) AS confidence, m.last\_reinforced\_at FROM knowledge k LEFT JOIN knowledge\_meta m ON m.logical\_id = k.logical\_id WHERE k.is\_current=1 AND k.is\_deleted=0\`. Reading/writing confidence on the base \`knowledge\` table now errors with 'no such column'. For v1 rows: logical\_id == id. \`ltm.create()\` creates matching knowledge\_meta row automatically. Schema version bumped 54→55. \`markInjected()\` updates \`knowledge\_meta.last\_reinforced\_at\` but does NOT bump \`updated\_at\` — injection stays sync-silent. Content-only \`update()\` also must NOT bump \`meta.updated\_at\`.

<!-- lore:019eec9d-45f9-7513-98bc-abf932baab86 -->
* **llm-adapter.ts resolveTarget: cross-provider fail-closed — never silently land on api.anthropic.com**: \`resolveTarget()\` (llm-adapter.ts:379): session override endpoint honored ONLY when \`overrideProviderID === modelProviderID\`; otherwise routes by worker model's OWN provider via \`resolveProviderRoute\`. If no route URL, \`routeUnavailable\` set — caller must fail closed. 🔴 'Never silently land on api.anthropic.com' — foreign providers must use their own route or fail closed. Production incident: minimax model → api.anthropic.com with Anthropic credential caused 401 loop. \`TRANSIENT\_CODES = new Set(\[429,500,502,503,504,529])\` — 504 added (PR #900) for OpenRouter/slow free models that surface upstream timeouts as HTTP 200 + \`{error:{code:504}}\`. Body-level error envelopes handled via \`extractBodyErrorCode()\` (PR #900): accepts numeric or \`/^\d+$/\` string \`error.code\`; embedded-transient retry block inserted after body parse; embedded-429 trips circuit breaker same as HTTP-429; exhaustion records \`"no-response"\` (NEVER \`worker-incapable\`) + returns null. 🔴 Bare API call never forwards \`opts.thinking\` — thinking tokens intentionally suppressed. 🔴 Auth failures always recorded so worker-health ladder sees them.

<!-- lore:019edfbc-edd5-780d-ac81-07a35390121b -->
* **ltm.ts: confidence lifecycle — decay, reinforcement, eviction, and cross-project protection**: Knowledge confidence lifecycle (PR #814/#815/#816/#820 stack, v48): \`decayProject(path)\` decrements confidence on stale entries; never bumps \`updated\_at\`. \`markInjected(ids)\` writes only \`last\_reinforced\_at\`. \`evictLowestValue\` and \`pruneDeadEntries\`/\`pruneDeadEntriesAllProjects\` MUST skip \`cross\_project=1\` entries. \`DEAD\_CONFIDENCE\_FLOOR=0.2\`. \`enforceEntryCap\` counts promoted rows but eviction excludes them. Global sweep in \`idle.ts\` runs \`pruneDeadEntriesAllProjects()\` on first tick then hourly (\`GLOBAL\_DEAD\_SWEEP\_INTERVAL\_MS=3600000\`), gated on \`loreConfig().knowledge.enabled\`. \`lastGlobalDeadSweepAt\` starts at 0 (first tick always fires), set BEFORE try block (prevents retry storm on exception), resets on restart. Sweep wrapped in try/catch — errors logged, never propagated. \`pruneDeadEntriesAllProjects()\` queries \`WHERE cross\_project=0 AND confidence<=0.2 LIMIT ?\` — only project-owned rows; global/promoted entries never touched. Motivation: per-session \`pruneDeadEntries\` only fires for active project; idle projects accumulate zombie entries forever.

<!-- lore:019edfc4-d6b0-7691-9a79-10d86613ef14 -->
* **ltm.ts: cross\_project=0 always implies non-null project\_id — enforced at create()**: Hard invariant (🔴): \`cross\_project=0\` rows ALWAYS have a non-null \`project\_id\`. Enforced at \`ltm.ts:92-99\` in \`create()\`: \`pid = (scope==='project' && projectPath) ? ensureProject(projectPath) : null\`; then \`crossProject = pid === null ? true : (input.crossProject ?? false)\` — null pid forces \`cross\_project=1\` (comment: 'IF-2: Global entries (pid=null) must be cross-project to avoid a data hole'). Migration \`db.ts:1046-1051\` sets \`cross\_project=0\` only WHERE \`project\_id IS NOT NULL\`. Therefore \`WHERE cross\_project=0\` implicitly guarantees \`project\_id IS NOT NULL\` — no explicit null check needed in queries like \`pruneDeadEntriesAllProjects()\`.

<!-- lore:019ee013-8bbe-7e78-8da7-dec71009fa42 -->
* **migration 0007: scope\_id immutability invariant and author\_id forge risk**: Migration 0007 v1 invariants (post-re-review): \`scope\_id = author\_id = auth.uid()\` enforced. Both prior gaps resolved: (1) scope mutations blocked by BEFORE guard at \`0007:214-217\` (even for service\_role); (2) author\_id forge blocked by RLS \`WITH CHECK (scope\_id=auth.uid() AND author\_id=auth.uid())\`. SHOULD-FIX: \`exists\_pk\` check moved outside advisory lock (lines 226-239 before lock at 267) — weakens 0005 TOCTOU hardening; spurious reject is safe (self-heals on retry), ±1 overshoot unreachable until A2 hard-DELETE reaper. Stale comment at \`0007:191\` lists 'is\_deleted revival' as gated growth — incorrect under physical accounting (revival is UPDATE, d\_rows=0, guard returns early). Doc drift: \`owner\_user\_id\` still referenced in \`supabase/README.md:9,43\` and \`packages/core/src/sync-data.ts:53\`.

<!-- lore:019edc05-19bf-74c1-973c-0e0fff69ea36 -->
* **packages/core SQLite driver: #db/driver subpath import, never import node:sqlite directly**: All SQLite access in packages/core MUST go through \`#db/driver\` subpath import (package.json imports map: \`bun\` → \`driver.bun.ts\`, \`default\` → \`driver.node.ts\`). NEVER import \`node:sqlite\` directly outside \`driver.node.ts\` — it breaks the test runner which runs against src. \`Database extends DatabaseSync\` with \`query(sql)\` caching prepared statements in a \`WeakMap\`. \`traced.ts\` wraps only \`get\`/\`run\`/\`all\` via Proxy — NEVER alters arguments or return values; \`.exec()\` passes through untouched (traced.ts:18–21, :66–77). \`migrate()\` runs on the RAW connection before wrapping (no re-entrancy, no tracing). Statements via \`db().prepare(...)\` (used in distillation.ts/embedding) are NOT traced — observability gap.

<!-- lore:019eeb85-6440-7760-912a-30c1cdc31a9f -->
* **pipeline.ts inFlightBackground: tracks urgent distillation separately from limiter activeTasks**: \`inFlightBackground\` (\`Set\<Promise\<unknown>>\`, \`pipeline.ts\`) tracks urgent distillation promises that bypass the background limiter entirely. \`trackBackground(p)\` adds promise and removes on \`.finally()\`. Urgent distillation bypasses \`isBackgroundPaused()\` — rationale: degraded/overflowing context for up to 10 min is worse than one API call. \`resetPipelineState\` non-fast path drains BOTH: \`drainBackground()\` (limiter tasks via \`activeTasks\`) AND \`boundedSettle(\[...inFlightBackground], timeoutMs)\` (direct urgent distillation tasks). These are separate sets covering separate code paths — missing either leaves a drain gap. \`boundedSettle()\` is a shared exported helper in \`background-limiter.ts\`: snapshots promises, races \`Promise.allSettled\` against a timeout that only ever resolves (never rejects). Timer always cleared in \`finally\`.

<!-- lore:019ef12d-69d4-7d54-bf10-7eab46b87801 -->
* **pipeline.ts two-block LTM: system\[1] stable baseline vs system\[2] context-bound, cache invariants**: Two-block LTM architecture: system\[1] = stable LTM (preferences + entities + project-knowledge catalog, frozen 1h, \`STABLE\_LTM\_TTL\_MS = 3\_600\_000\`); system\[2] = context-bound LTM (relevance-ranked, re-pinned only on material change). 🔴 Invariants: (1) 'never bust the cache' — governs system\[2] re-pin logic. (2) 'ltmCacheText never diverges from ltmPinText' (pipeline.ts:6103). (3) system\[1] stable baseline 'never recomputed from the live knowledge table mid-session' (pipeline.ts:5948) — frozen durably (v45) including empty result. PR #930 adds \`buildKnowledgeCatalogText\` (project-knowledge recall-by-id catalog, \`STABLE\_KNOWLEDGE\_TOC\_MAX=15\`, \`includeCross=false\`, preferences excluded) folded into system\[1] baseline near entities block. Overflow (scored-but-unfit entries) surfaces in system\[2] delta via \`overflowSink\` → \`pendingKnowledgeDelta.overflow\` → \`buildKnowledgeDeltaMessage\`, sorted by id ASC for byte-stability, capped at \`OVERFLOW\_TOC\_MAX=12\`.

<!-- lore:019ebc96-83c7-7cd5-98d2-ca85724c2a85 -->
* **pipeline.ts: two curation paths — in-flight vs idle — must be gated separately**: Pipeline two curation paths: in-flight (mid-turn, gated by sessionState.curationScheduled) and idle (flush gen-0 then metaDistill) — must be gated separately. In-flight avoids system\[2] cache busts. System\[2] churn root causes: context-health note appended after diff-pinning, LTM entry selection changes per-turn. In-flight gen-0 distillation mutates messages\[1] in compression mode. LTM pin invariants: NEVER bust cache; ltmCacheText must never diverge from ltmPinText; coalesced seq-0 delta row describes full cumulative frozen→current delta.

<!-- lore:019eeea5-3425-7f1b-95ba-39aea2cd8749 -->
* **PR #897 sync model: remote is CURRENT-only mirror keyed by logical\_id — one row per logical entry**: PR #897 sync model: remote is CURRENT-only mirror keyed by logical\_id — one row per logical entry. Push: \`knowledgePushPlan\` resolves outbox version row → \`logical\_id\`; pushes CURRENT version's content under \`id=logical\_id\`; \`sync\_state\` keyed by \`logical\_id\` so version churn coalesces to one upsert. Pull: \`applyRemoteKnowledge\` / \`applyRemoteKnowledgeDelete\` apply logical\_id-keyed remote row into local append-only model. \`hasPendingKnowledgeChange\` resolves pending edits by \`logical\_id\` via JOIN. \`currentKnowledgeRow(logicalId)\` is the canonical read helper. Squash \`4c598643\` landed on main June 22 2026. Sub-PR 3b: knowledge\_meta convergent confidence register (delta-CRDT). Sub-PR 4: compaction/GC + remote hard-delete reaper — server-side system task; retention: keep current + max 2 most-recent superseded versions; hard-prune superseded versions older than ~1–2 months; must honor v1-anchor invariant; outbox converted to full \`logical\_id\` keying.

<!-- lore:019ef10f-5dfe-77b9-96fd-1f16768803e6 -->
* **PR #898 AWS Bedrock gateway: routing, URL-building, and header invariants**: Bedrock routing in \`pipeline.ts\`: \`selfUrlBuildingProtocol = providerRoute?.protocol === 'bedrock' || 'vertex'\`; \`providerRouteUsable\` accepts \`url: null\` when this is true. \`effectiveUpstreamBase\` priority: \`headerUpstream ?? selfBuiltUpstreamUrl ?? providerRoute?.url ?? modelRoute?.url ?? fallback\`. Bedrock branch returns early, builds URL via \`bedrockInvokeUrl(config.bedrockRegion, modelId)\` — \`effectiveUpstreamBase\` unused in fetch. \`PROVIDER\_ROUTES\` has both \`bedrock\` and \`amazon-bedrock\` entries (\`url: null, protocol: 'bedrock'\`); Vertex deliberately omitted (issue #870 part 2). \`npmToProtocol\` (worker-model.ts) can NEVER return \`'bedrock'\`/\`'vertex'\` — only static entries carry those protocols. \`bedrockRegion\` uses \`??\` chain with \`'us-east-1'\` default — empty string is NOT caught (pre-existing edge case, SigV4 fails regardless).

<!-- lore:019ef031-75fd-7023-9895-7abd4e7d9f1d -->
* **PR #913: knowledge outbox uniformly logical\_id-keyed — compaction prereq**: PR #913 \`feat/sync-knowledge-logical-outbox\`: \`installSyncCapture\` triggers in \`db.ts:1522-1525\` now use \`COALESCE(NEW/OLD.logical\_id, id)\` for all knowledge ops (INSERT/UPDATE/DELETE). Previously only DELETE used logical\_id; INSERT/UPDATE used version-id. Root cause: a delete keyed by version-id would never match the logical\_id-keyed remote row. \`knowledgePushPlan\` treats \`outboxRowId\` as logical\_id directly — no anchor JOIN needed. \`hasPendingKnowledgeChange\` and \`seedOutbox.latestForLogical\` also drop the JOIN. COALESCE fallback is pure defense — no production path leaves \`logical\_id\` NULL (v50 migration backfills, \`recoverMissingObjects\` re-backfills idempotently).

<!-- lore:019ef409-79a7-764c-b7f2-e5282a53a49b -->
* **PR #930 overflow-ToC: forSession overflowSink invariants and pipeline wiring**: \`forSession(options?.overflowSink)\` (ltm.ts step 9, lines 1655–1667): builds \`selectedIds = new Set(result.map(e=>e.id))\`, iterates \`allScored = \[...scoredProject, ...scoredCross]\` pushing non-selected entries into sink. lat.md synthetics packed separately in step 6 — NOT in \`allScored\`, so never leak into sink. 🔴 Invariants: (1) selected ∩ overflow = ∅ (disjoint); (2) lat.md synthetics never in sink; (3) ordering deterministic (matches \`allScored\` sort); (4) overflow alone never triggers delta — \`buildKnowledgeDeltaMessage\` returns null when no entries and no removedIds regardless of overflow. Two pipeline call sites (cold path + Layer-4 path) capture \`freshContextOverflow\` → \`pendingKnowledgeDelta.overflow\`. Overflow ToC sorted by id ASC (NOT relevance) for byte-stability.

<!-- lore:019ee535-02de-729a-a4f2-ecf36b96c70f -->
* **recall.ts: search architecture — RRF fusion, source weights, cross-session penalty**: recall.ts is the unified search entry point. \`CROSS\_SESSION\_RAW\_PENALTY=0.5\` applied to temporal/distillation from OTHER sessions only — knowledge/entities/lat-sections are intentionally cross-session and never penalized. \`searchRecall()\` short-circuits on \`EMPTY\_QUERY\`. Entity alias expansion always runs (fast, targeted); LLM expansion gated on \`queryTermCount <= expansionMaxTerms\` (default 8). Original query always first in RRF list. \`MAX\_RRF\_LISTS=14\` — expanded-query BM25 lists trimmed when exceeded; primary and supplemental lists always kept. \`formatFusedResults()\` backfills to 3 results only if items clear \`absoluteFloor\` — never force-inject weak content. 🔴 Invariant: never force-inject weak cross-session archives on off-topic queries.

<!-- lore:019ef451-f07d-750c-96cc-9d1098868ff3 -->
* **recordCacheTurnUsage seam: extracted from postResponse, ordering change is intentional**: PR #932 extracts cache-analytics + bust-tracking block from \`postResponse()\` into exported \`recordCacheTurnUsage(sessionState, usage, model, projectPath, requestBody?, genAiSpan?): CacheBustCause | undefined\` at \`pipeline.ts:~4169\`. One intentional behavioral change: \`recordCacheUsage\` now runs BEFORE \`genAiSpan.end()\` (safe — no span dependency, only arithmetic + map access). Exception-path gotcha: if \`recordCacheTurnUsage\` throws, catch block at \`pipeline.ts:4654\` only logs — \`genAiSpan.end()\` never called (pre-existing fragility, rated nit). \`getConsecutiveBusts\` uses \`Map.get()\` directly (not \`getSessionState()\`) to avoid creating phantom SessionState entries.

<!-- lore:019ef10f-c4fa-7bb8-a25e-f5e8eaa25e47 -->
* **Remote migration 0007 scope seam: immutable scope\_id, physical counting, author-is-writer**: Migration 0007 renames owner\_user\_id to scope\_id on 5 synced tables and adds author\_id. RLS: scope\_id = auth.uid() for SELECT; writes require scope\_id = auth.uid() AND author\_id = auth.uid() (author is always the writer). scope\_id immutable: UPDATE with different scope\_id raises check\_violation (RLS WITH CHECK + trigger backstop). user\_table\_usage trigger-written (SECURITY DEFINER, no DML grant). Physical footprint counts EVERY row regardless of is\_deleted (forward-compatible with append-only A2; closes tombstone-flood abuse). enforce\_row\_quota uses pg\_advisory\_xact\_lock per (scope\_id, table) for TOCTOU protection. maintain\_usage uses greatest(...,0) to prevent negative counts.

<!-- lore:019eea46-b8f1-7b46-8e53-52a9c21fe48c -->
* **reprobeEmbedCap: two hard behavioral invariants**: 🔴 Two invariants asserted by Burak Yigit Kaya for \`reprobeEmbedCap\` in \`embedding-cap.ts\`: (1) \*\*Never steps down\*\* — returns the cap unchanged if the memory model ceiling is lower than current cap; (2) \*\*Never re-probes up to or past a known-bad cap\*\* — \`lastOomCap\` guard prevents probing ≥ a cap that previously caused OOM unless freemem materially improves. These are load-bearing correctness invariants; violating either causes OOM regression or permanent cap suppression.

<!-- lore:019ee7af-7e10-7958-afb5-7fd54328ab93 -->
* **sentry.ts: resource monitor and abort-under-pressure instrumentation invariants**: PR #862 (\`feat/abort-under-pressure-capture\`): \`startResourceMonitor()\` idempotent, called once at idle.ts:225. \`emitResourceGauge()\` emits 5 Sentry metrics + calls \`hist.reset()\` (destructive). \`getEventLoopLagP99Ms()\` is peek-only (no reset) — returns 0 safely if histogram never started (Sentry off at scheduler start). \`isAbortUnderPressure(lagPercentileMs, freeMemoryBytes)\` is pure; OR logic: either \`>=1\_000ms\` lag OR \`<=512MB\` free memory triggers. \`captureClientAbortUnderPressure\` gated on \`Sentry.isInitialized()\` — guard must be INSIDE the try block (blast radius: if \`isInitialized()\` throws in a catch block, propagates as unhandled rejection). 🔴 Never throws — outer try/catch with empty catch enforced. 🔴 Telemetry must never break the request path or idle loop. Streaming abort at \`pipeline.ts:3509\` is never re-caught by \`handleRequest\` at \`:7682\` — \`handleRequest\` returns the streaming Response synchronously before \`start()\` runs.

<!-- lore:019ecb52-ff7c-7515-a435-be511f6fdde7 -->
* **session.ts: 3-tier session ID strategy, generateSessionID format, and marker functions**: Session ID strategy: Tier 1 priority: x-lore-session-id, x-claude-code-session-id, x-session-id, x-session-affinity. ROTATION\_ELIGIBLE\_HEADERS = ONLY x-session-affinity. x-claude-code-session-id is NOT rotation-eligible — new value = new session, never merge (cross-project contamination). isClaudeCodeClient checks x-claude-code-session-id header; callers should also check hasBillingHeader on system. isCC in pipeline.ts = isClaudeCodeClient OR hasBillingHeader. Tier 2: learned headers after 3 stable turns. Tier 3: SHA-256 of first user msg + auth suffix. generateSessionID: 12 bytes = 8 random + 4-byte BE unix ts, base62. Desktop uses same x-claude-code-session-id as CLI.

<!-- lore:019ef02c-f18a-71c8-88ec-4c33c6d71a14 -->
* **start.ts startGateway: pre-bind reuse probe + TOCTOU backstop pattern**: PR #908 fix: \`startGateway()\` now probes for an existing gateway BEFORE attempting each candidate port bind (skips port 0). Probe set: \`\[...new Set(\['127.0.0.1', ...config.hosts])]\` — loopback always included unconditionally. Returns \`{owned:false}\` immediately if any host answers \`/health\`. Post-bind EADDRINUSE catch probe retained as TOCTOU backstop only. Root cause of #908: probe was gated inside EADDRINUSE catch — non-overlapping interface bind never throws, so probe was unreachable. \`runDaemon()\` reuse-check similarly upgraded: probes \`\[...new Set(\[...opts.hosts, '127.0.0.1'])]\` (configured hosts first to preserve #875 reported address). \`firstReachableHost(hosts, port)\` helper drives both paths — parallel probes, returns first responding host by list order.

<!-- lore:019ee5b8-6fe3-77a7-84cc-ae658bd26268 -->
* **Stryker baseline: sync-data.ts mutation score 68.98% — 28 real logic gaps identified**: Stryker baseline (June 20 2026) for \`packages/core/src/sync-data.ts\`: \*\*68.98% mutation score\*\* (189 killed, 80 survived, 5 no-coverage). PR #849 closes 11 genuine survivors (see sync-data.ts mutation gaps entry). Three confirmed equivalent mutants: (1) \`:378\` \`pruneOutbox\` guard — seq starts at 1, early-return is no-op; (2) \`:326\` \`getRowById\` PAYLOAD\_EXCLUDE filter — those columns absent from all syncColumns; (3) \`:310\` \`contentHash\` \`.sort()\` — registry order is deterministic, no test pins a specific hex value. Earlier 'false survivor' diagnosis was a misdiagnosis — Stryker IS reliable. Baseline documented in \`quality/MUTATION\_TESTING.md\`.

<!-- lore:019ec7fc-3bab-7e84-b5b3-e8e9d802a806 -->
* **Supabase auth constraints: email templates blocked on free tier; no device flow**: Supabase auth constraints: Free tier blocks email template customization (HTTP 400); custom SMTP live: \`smtp\_host: mail.smtp2go.com\`, sender \`keeper@withlore.ai\`. Email templates pushed via Management API PATCH (4 fields only). \`supabase config push\` clobbers entire auth config — use Management API PATCH for targeted updates. GitHub OAuth must be enabled separately in Auth → Providers → GitHub. OTP length configurable (not fixed at 6 digits) — observed 8-digit tokens; use \`/^\d{4,12}$/\`. \`lore login --email\` active once custom SMTP configured. Supabase project ref: \`jlwxsrmvomocgngbxmcf\`, region \`us-east-2\`. \`config.toml\` auth settings only affect \`supabase start\` (local stack) — dashboard is authoritative for production. Email templates MUST include \`{{ .Token }}\` in body or users receive only a \`{{ .ConfirmationURL }}\` link. Redirect URLs: must include \`https://\` when not localhost; must include trailing \`/\`.

<!-- lore:019ef10f-c4d9-7073-8940-764d6bb63cc4 -->
* **Sync engine push/pull architecture: per-table cursors, quota isolation, local-first sync**: pushOnce()/pullOnce() operate per table with independent cursors: push cursor sync.push.\<table> (last seq), pull cursor sync.pull.\<table> (keyset format \`${updated\_at\_ms}|${row\_id}\`). Quota/transient failures never advance that table's cursor and never block other tables. pushOnce pushes local outbox before pulling remote (local SQLite is source of truth). Outbox pruning uses min cursor across tables with entries (empty tables excluded). Knowledge uses knowledgePushPlan() to coalesce append-only versions to one remote row keyed by logical\_id. Poison rows (over-size) recorded as rejected\_unsyncable (cursor advances); quota rows return stop (cursor unchanged for retry). PAGE=200.

<!-- lore:019ecb58-7fc8-70ad-85eb-5c27233ec4e3 -->
* **sync engine: Supabase mirror schema (migration 0002) — 5 tables, RLS, triggers**: Sync engine Supabase mirror schema — 7 migrations: \`0001\_accounts.sql\` (profiles table, SECURITY DEFINER trigger handle\_new\_user). \`0002\_sync\_basic.sql\` (5 tables: knowledge, entities, entity\_aliases, entity\_relations, knowledge\_entity\_refs; composite PK (owner\_user\_id, id); RLS owner-scoped). \`0003\_sync\_limits.sql\` (plan\_limits, quota trigger v1). \`0004\_sync\_hardening.sql\` (tier lock via column-level GRANTs + guard\_profile\_tier trigger, size CHECKs, DML GRANTs). \`0005\_sync\_quota\_hardening.sql\` (quota trigger v3 — revival gate, advisory lock). \`0006\_tier\_upgrade\_path.sql\` (GRANT SELECT, UPDATE ON profiles TO service\_role). \`0007\_scope\_seam.sql\` (renames owner\_user\_id→scope\_id, adds author\_id, replaces COUNT(\*) quota with trigger-maintained \`user\_table\_usage(scope\_id, table\_name, row\_count, byte\_count)\` counter table). Free tier caps: knowledge=500, entities=30, entity\_aliases=300, entity\_relations=300, knowledge\_entity\_refs=2000. NULL cap = unlimited.

<!-- lore:019eea5e-b325-720b-a9c1-1c526efc0096 -->
* **sync-data.ts seedOutbox: content-aware JS loop — 6 cases, cursor semantics, performance**: PR #868 rewrites \`seedOutbox()\` from single \`INSERT…SELECT\` to JS loop. Push cursor = \`Number(getKV('sync.push.'+table) ?? '0')\`. \`latestUnpushed\` = latest op with \`seq > cursor\`. Six cases: (a) no pending + hash==sync\_state → skip; (b) no pending + hash!=sync\_state → enqueue; (c) latest unpushed=upsert → skip; (d) latest unpushed=delete → enqueue trailing upsert; (e) stale upsert below cursor + content changed → enqueue (THE FIX); (f) stale upsert below cursor + content unchanged → skip. \`seq AUTOINCREMENT\` guarantees strict monotonicity. \`contentHash\` uses \`syncedColumns ∖ HASH\_EXCLUDE\`, sorted, SHA-256 truncated to 16 hex. 🔴 Wrap loop in \`withTransaction\` — N auto-commits cause ~23x slowdown. 🔴 \`seedOutbox\` is only ever reached via \`enableSync\`, never from within another transaction (code comment at line 498). PR #874 hoists \`columns(table)\` outside per-row loop; PR #878 memoizes globally via \`WeakMap\` keyed on \`db()\` instance — \`seedOutbox\` pre-warms cache for all tables, so subsequent \`pushOnce\` sees 0 PRAGMAs.

<!-- lore:019eeac5-97d6-7fff-bf6c-d675a7c3b77f -->
* **sync-data.ts syncedColumns: WeakMap memoization per DB connection (PR #878)**: PR #878 (\`perf/sync-push-columns-memo\`): \`syncedColumns(table)\` now memoizes via \`const syncedColumnsCache = new WeakMap\<object, Map\<string, readonly string\[]>>()\` keyed on \`db()\` connection instance. On miss: runs \`PRAGMA table\_info\`, intersects \`syncColumns ∩ local\`, stores \`Object.freeze(result)\`. Auto-invalidates when \`close()\` nulls \`instance\` — next \`db()\` returns a new object with a fresh WeakMap entry. Returns \`readonly string\[]\` (frozen); callers that need a mutable copy must call \`.filter()\`/\`.slice()\` first. \`columns()\` is a thin wrapper delegating to \`syncedColumns()\`. Rationale: \`PRAGMA table\_info\` was called 3× per row (via \`getRowById\` + \`contentHash\` + \`pickSyncColumns\`) — N+1 over every push/pull batch. Columns are stable for a connection's lifetime (migrations run on raw connection before \`db()\` is returned).

<!-- lore:019eea8f-aca1-7487-91cf-0d50a744b29c -->
* **sync-data.ts: hashRowColumns extraction and seedOutbox N+1 fix (PR #874)**: PR #874 (merged into origin/main): extracted \`hashRowColumns(cols, row)\` from inline body of \`contentHash\`; \`contentHash(table, row)\` now delegates to \`hashRowColumns(columns(table), row)\`. In \`seedOutbox\`, \`const cols = columns(m.table)\` hoisted above per-row loop — inner loop calls \`hashRowColumns(cols, row)\` instead of \`contentHash(m.table, row)\`, eliminating N×\`PRAGMA table\_info\` calls (proven: 50 rows → 50 PRACMAs before fix, 5 after). Safety: \`hashRowColumns\` does NOT mutate input \`cols\` (uses \`.filter().sort()\` producing new arrays); \`columns(table)\` is deterministic per table. \`hashRowColumns\` is NOT exported — private to file; 3 refs: line 312 (contentHash), line 320 (definition), line 499 (seedOutbox loop).

<!-- lore:019ee6f0-67e0-7148-be93-659c6953afa6 -->
* **sync-data.ts: PR #849 — 11 mutation gap tests, 3 confirmed equivalent mutants**: PR #849 appends 11 gap tests to \`sync-data.test.ts\` closing genuine survivors: \`:624\` \`remoteHash !== null\` (classifyRemoteRow); \`:296\` serializeValue \`||→&&\`; \`:316\` \`.slice(0,16)\` contentHash; \`:243\` \`if (!m) throw\` (meta guard); \`:270\` \`if (c in row)\` (pickSyncColumns); \`:392/399\` hasPendingChange both directions; \`:422\` \`idColumns.length===1\` (rowIdExpr composite); \`:467\` reconcile pullOnly guard; \`:533\` clearSyncState deletes row; \`:567\` \`nonPk.length>0\` DO NOTHING (applyRemoteUpsert); \`:670\` isSyncEnabled. Three equivalent mutants empirically confirmed surviving all 48 tests: \`:310\` sort removal, \`:326\` PAYLOAD\_EXCLUDE filter removal, \`:378\` pruneOutbox guard. \`beforeEach\` adds \`entities\`/\`knowledge\_entity\_refs\` cleanup; explicit refs deletion is redundant (CASCADE FKs) but harmless.

<!-- lore:019edff0-4729-73b3-a15c-55619ac3a620 -->
* **sync-data.ts: SYNCED\_TABLES.basic registry — syncColumns allowlist, PAYLOAD\_EXCLUDE, HASH\_EXCLUDE, versioned flag**: \`SYNCED\_TABLES.basic\` (sync-data.ts): 6 tables (added \`profiles\` in v49). \`profiles\`: \`pullOnly: true\`, idColumns \`\['id']\`, no syncColumns pushed, no outbox capture trigger — server-authoritative. \`knowledge\_entity\_refs\`: \`versioned: false\`, idColumns \`\['knowledge\_id','entity\_id']\`, only 2 syncColumns — no \`content\_hash\`/\`revision\` remotely. \`PAYLOAD\_EXCLUDE\`: \`embedding\`, \`last\_accessed\_at\`, \`last\_reinforced\_at\` — never sent over wire. \`HASH\_EXCLUDE\`: same + \`updated\_at\`. \`ROW\_SEP = '\x1f'\` joins composite row ids. Sync-management cols (\`scope\_id\`, \`author\_id\`, \`content\_hash\`, \`revision\`, \`is\_deleted\`) added by \`pushEntry\` separately — NOT in syncColumns registry. \`currentTier()\` queries local profiles mirror, defaults to \`'free'\` when no row exists. \`pro: \[]\` and \`max: \[]\` tiers empty.

<!-- lore:019ee773-d64a-7bb0-b5d4-665eb12e8ceb -->
* **sync.ts applyRemote: tombstone classification invariants and 4 outcome paths**: \`classifyRemoteRow\` contract (sync-data.ts): \`remoteHash\` MUST be null for any tombstone/delete row. Guard: \`if (remoteHash !== null && localHash === remoteHash) return 'skip'\`. Four tombstone outcomes: (a) local present unchanged → \`apply\`; (b) local present + \`pendingLocalChange\` → \`conflict\`, recordConflict(\`remote\_delete\_wins\`) then delete; (c) local absent → \`apply\`, no-op; (d) local diverged → \`conflict\` + delete. 🔴 \`applyRemote\` forces \`remoteHash=null\` when \`isDeleted\` — never fast-forward over unpushed local intent. Pull-only tables always take remote (\`cls='apply'\`, skips \`classifyRemoteRow\`). 🔴 \`getRowById\` with \`logical\_id\` fetches v1 (stale) for multi-version knowledge entries — classify and conflict-snapshot paths must use \`knowledge\_current WHERE COALESCE(logical\_id, id) = ?\` instead \[\[019eec5f-e309-70b8-bc82-afb5af474ece]].

<!-- lore:019edfc9-c361-72f2-ada3-fc735def69f0 -->
* **sync.ts: local prune vs genuine delete — tombstone suppression required**: Hard invariant (🔴): only a genuine user/curator-intent delete generates a tombstone and propagates to remote. Local TTL/size eviction (idle prune) must NOT enqueue a tombstone. Fix: run prune under a capture-suppressed context (analogous to \`withSyncApplying\`) that drops the DELETE-trigger tombstone and clears \`sync\_state\` only. \`reconcile\` (\`sync-data.ts:378-396\`) must never see evicted rows as deletes. Trap: conflating eviction with delete looks correct because both remove local rows — but eviction drops LOCAL RESIDENCY only, not the logical row. The logical dataset (shared memory) converges across all clients + remote; 'in sync' means logical set converges, not byte-identical full replicas. Evicted rows are re-pullable cache misses, not divergence. Remote is bounded by per-scope quota (\`max\_rows\`/\`max\_bytes\`), not a TTL reaper. Neither side is authoritative.

<!-- lore:019edff0-4711-73d1-9b16-7fba4282b220 -->
* **sync.ts: per-table cursor isolation — quota/transient failure on one table never blocks others**: Hard design invariant: cursors are per-table (\`sync.push.\<table>\` and \`sync.pull.\<table>\`). Quota rejection (HTTP 400, errcode 23514 + /quota exceeded/i) pauses ONLY the affected table — never an error to crash on, never blocks other tables. Outbox SQL-filtered per table so other tables' entries cannot starve it. \`classifyPushError\`: 'quota' → pause table, keep row pending; 'poison' (23514 without quota msg) → drop past cursor, record conflict, keep table syncing; 'transient' → keep pending, retry. \`drainTimestamp\` pages by id keyset when >PAGE rows share one millisecond. \`applyRemote\` strips remote-only cols (\`owner\_user\_id\`, \`content\_hash\`, \`revision\`, \`is\_deleted\`) and converts ISO timestamps to epoch ms for local storage.

<!-- lore:019edfff-dc0b-7552-89ba-9a71ebfea21c -->
* **sync.ts: pullOnly tables — server-authoritative, no outbox, no conflict classification**: \`SyncTableMeta.pullOnly: true\` marks server-authoritative tables (e.g. \`profiles\`). \`pushOnce\` skips them entirely (sync.ts:78). \`applyRemote\` bypasses \`classifyRemoteRow\` for pull-only rows — forces \`cls = 'apply'\` — because: (1) no outbox trigger means no pending local changes ever, (2) no \`content\_hash\` column means null remoteHash always mismatches non-null local hash → spurious conflict every tier-flip re-pull. \`currentTier()\` in \`sync-data.ts\` reads the local \`profiles\` mirror, defaulting to \`'free'\` when no row exists — \*\*advisory UX hint only\*\*, not a security gate; server RLS/quota is the real enforcement boundary. The 'outbox capture installed for every synced table' test must skip \`pullOnly\` tables. 🔴 \`seedOutbox\`/\`reconcile\` must also skip \`pullOnly\` tables — see prune-floor DoS gotcha.

<!-- lore:019edff0-4709-74cc-98eb-ad3bd46163c1 -->
* **sync.ts: push payload never includes owner\_user\_id — server default auth.uid() fills it; A1 changes onConflict target**: \`pushEntry()\` (sync.ts): payload = \`toRemoteRow(pickSyncColumns(table, row))\` + \`is\_deleted: false\` + \`content\_hash\`/\`revision\` (versioned tables only). \`scope\_id\` (was \`owner\_user\_id\`) NEVER included — server fills via \`default auth.uid()\`. \`onConflict\` target post-A1: \`\["scope\_id", ...idColumns(table)].join(",")\`. \`REMOTE\_ONLY\_COLS\` Set strips \`scope\_id\`, \`author\_id\`, \`content\_hash\`, \`revision\`, \`is\_deleted\` before local apply. Soft-delete push: \`.update({ is\_deleted: true }).match(decomposeId(table, rowId))\` — \`decomposeId\` keys only on \`idColumns\`; RLS scopes match automatically. \`pickSyncColumns\` strips local-only cols (e.g. \`knowledge.promoted\_at\`) to avoid PGRST204. Tombstone creation via INSERT is never a legitimate operation — only soft-delete UPDATE.

<!-- lore:019edfc9-c36b-7cd9-954e-44f27d8b356d -->
* **sync.ts: restore exemption — prune must key off local residency, not original created\_at**: Hard requirement (🔴): local prune must key off \*local residency timestamp\* (when the row was pulled/created locally), NOT the original \`created\_at\`. If prune uses \`created\_at\`, restored sessions older than local TTL (120 days) are deleted on the next idle tick — making cross-device backup useless for data older than local retention. Fresh device pulls full remote history (that is the restore path); pruning device is already past evicted rows' timestamps so evicted rows are never re-pulled by the same device (no ping-pong). Remote Supabase is a multi-master sync backend; many clients may read/write it; neither side is authoritative.

<!-- lore:019ef35a-0206-73ac-b15e-0ce6bdd349e6 -->
* **tool-trace.ts isVerifierCall: signal path to creditSessionOutcome — false positives corrupt knowledge confidence**: \`isVerifierCall(input)\` (tool-trace.ts): splits command on \`/&&|\\|\\||\[;\n|]/\`, tests each segment against \`VERIFIER\_LEADING\`. Match → \`verifier=1\` written by \`recordToolCalls\` (temporal.ts:215), called on hot path at pipeline.ts:4292/4330. \`sessionVerifierVerdict\` reads \`verifier=1\` rows: any error→fail, any completed→pass → drives \`creditSessionOutcome\` (PASS: +confidence capped; FAIL: −confidence). 🔴 Design invariant: 'Only verifier=1 calls count — incidental command failures never move knowledge confidence.' False positives on \`isVerifierCall\` directly corrupt long-term knowledge confidence. PR #927 broadened \`VERIFIER\_LEADING\`: added PM exec/dlx prefix, script keywords (\`ci\`, \`verify\`, \`validate\`, \`e2e\`, \`coverage\`, \`spec\`), task runners (\`make|just|task|rake\`), additional toolchains (\`deno test/check\`, \`swift test\`, \`tox\`, \`nox\`, \`pre-commit run\`). Validated against 51 test cases.

<!-- lore:019edfe6-3301-76b0-aea9-cba6052eeb6c -->
* **Upcoming: uniform append-only model for knowledge/entities — nothing should land on these tables before refactor**: 🔴 Major upcoming refactor: knowledge and entities tables move to immutable append-only model: \`logical\_id + version + is\_deleted\` marker, materialized current view, compaction. Engine reduced to append+cursor; usage counter maintained. Also adds \`author\_id\`/\`scope\_id\` seam on shareable tables (Folk Lore / team sync). Consequence: DO NOT land new migrations or features on \`knowledge\`/\`entities\` that the refactor would immediately rip out. Issue #495 (knowledge evolution trajectory / op-log) is subsumed — \`logical\_id+version\` history IS the evolution trajectory; building a standalone op-log on the current mutable table is throwaway work. Issue #467 RFC defines topology: Personal DB + Team DB, same schema (team columns nullable in personal), \`.lore.md\` = team entries, \`.lore.local.md\` = personal entries.

<!-- lore:019eec55-4bc1-7dbb-b360-f468fdcbe331 -->
* **worker-model.ts: same-provider invariant — workers MUST use session's provider, cross-provider always fails**: \`getWorkerModel()\` priority chain: (1) \`LORE\_WORKER\_MODEL\` env var; (2) \`cfg.model.providerID\`; (3) \`session.providerID\`. 🔴 Same-provider invariant: workers MUST use the same provider as the session — cross-provider calls always fail. Code comment at \`pipeline.ts:1722\`. \`getWorkerModel\` preserves provider for \`WORKER\_DEFAULTS\`, \`resolveGitHubCopilotWorker\` stays within \`github-copilot\`, \`findCheaperSameProviderModel\` passes \`effectiveProvider\` through. If no provider can be determined, returns \`undefined\` (skips background work). 🔴 \`config.workerApiKey\` path bypasses \`resolveAuth\` entirely — any provider-aware auth guard MUST add \`!config.workerApiKey &&\` exemption at both \`pipeline.ts\` and \`idle.ts\` guard sites. 🔴 Never fall back to \`config().model\` in distillation — cross-provider 404 trap (see distillation.ts:1041–1045).

### Decision

<!-- lore:019eead7-4d36-7eac-a6c0-ac700b10bb92 -->
* **Adopted prior session ${best.sid.slice(0,16)}**: adopted prior session ${best.sid.slice(0,16)} for resumed conversation (overlap=X/Y,

<!-- lore:019ef458-0046-747c-b0ff-e4c00a597e6b -->
* **Bedrock runtime code removed entirely — no dormant dead code**: Chose to remove Bedrock runtime code entirely (revert/clean up old PR) over leaving it dormant. Rationale: dead code adds maintenance burden and confusion; if Bedrock support is needed later it should be re-added cleanly. Rejected alternative: keep code dormant/disabled with feature flag.

<!-- lore:019ef143-9105-76ba-9178-124ed8195621 -->
* **Chose approach 2 (full 9-variant union in core) over minimal union**: Chose approach 2 (full 9-variant union in core) over minimal union.

<!-- lore:019ee751-adc9-7639-aee2-52c25c9ee6b8 -->
* **Decided to use an explore agent to extract sections most relevant to Lore from the saved Arbor paper file at /home/byk/**: decided to use an explore agent to extract sections most relevant to Lore from the saved Arbor paper file at /home/byk/.

<!-- lore:019ef0c0-0308-797a-ad49-7b0c08aedb80 -->
* **knowledge\_meta as separate synced convergent register table**: Chose separate knowledge\_meta table (Option A) over keeping confidence on per-version knowledge rows with backward sync compat (Option B). Rationale: Option B accrues sync complexity on a legacy column — confidence is mutable metadata, not per-version data, so a separate structure is semantically correct. Cost: requires remote migration 0009, RLS policies, and sync pipe plumbing. knowledge\_meta uses per-field merge: delta-CRDT for confidence/counts, max for timestamps. last\_reinforced\_at stays local (sync-excluded). cross\_project entries never adjusted by outcome-reward loop.

<!-- lore:019eec77-3f71-73bb-9884-1734fe116a2d -->
* **logical\_id retained across appendVersion — three load-bearing reasons**: Chose \`logical\_id\` (stable handle across versions) over 'delete+create new id' per edit. Three load-bearing reasons: (1) \*\*Inbound references\*\* — wiki-links \`\[\[id]]\`, transfer metrics, \`.lore.md\` \`\<!-- lore:UUID -->\` markers all point at stable id; new id per edit dangles refs or requires cascade repointing. (2) \*\*\`.lore.md\` marker contract\*\* — new UUID per edit = new marker line per edit → git churn + broken re-import identity. (3) \*\*Confidence/reinforcement/decay lifecycle\*\* — fresh id resets accrued signal (e.g. 'reinforced 10× over 3 months'). At storage layer, \`appendVersion\` already IS delete+create: inserts new immutable row, demotes old. \`logical\_id\` is the only thing carried across. Alternative 'new id + \`supersedes\` pointer' is isomorphic but strictly worse: 'find current entry' becomes chain-walk instead of O(1) indexed lookup.

<!-- lore:019ee6f2-a13d-7cab-8011-e54c77014231 -->
* **Switched from plan mode to build mode (system-reminder: no longer read-only**: switched from plan mode to build mode (system-reminder: no longer read-only,

### Gotcha

<!-- lore:019eec0c-e455-78ea-b101-0a07dd878704 -->
* **A2 append-only flip: sync push egresses superseded/deleted content — sub-PR 3 release gate**: Trap: after the append-only flip (PR #853), \`remove()\` no longer emits \`op='delete'\` — it appends a death-cert row. The outbox capture trigger fires \`op='upsert'\` for both the demote UPDATE and the death-cert INSERT. Fix (PR #853, commit \`d09048af\`): new \`knowledgeSyncOp(rowId)\` in \`sync-data.ts\` returns \`"skip"\` if row gone, \`"delete"\` if \`is\_current=0 OR is\_deleted=1\`, \`"upsert"\` otherwise. PR #897 replaces \`knowledgeSyncOp\` with \`knowledgePushPlan\` which resolves to \`logical\_id\` and coalesces all versions to one remote row. PR #913 extends further: outbox \`row\_id\` IS now the \`logical\_id\` for all knowledge ops (not just DELETE) — \`knowledgePushPlan\` treats \`rowId\` directly as \`logical\_id\`, calls \`currentKnowledgeRow(rowId)\` directly. Superseded/death-cert rows → \`op=delete\` by \`logical\_id\`. 🔴 Release gate: sub-PR 3 (PR #897) must land before shipping; PR #913 is prereq for PR #909 (A2 compaction).

<!-- lore:019ee74b-44d3-74bf-a935-5ca3e12d27ce -->
* **A2 CLI data commands: list displays current-version id prefix but resolveId matches logical\_id — list→show/delete/move broken for multi-version entries**: Trap: \`cli/data.ts:151\` printed \`e.id.slice(0,8)\` (current version id) in \`list\` output. \`resolveId\` matched against \`logical\_id\` only. For multi-version entries, current version id and logical\_id have different uuidv7 prefixes → prefix copied from \`list\` output fails \`resolveId\` → null → fallback fails. Fix (PR #853, commit \`d09048af\`): (1) \`cmdList()\` line 151 now displays \`e.logical\_id.slice(0,8)\`; (2) \`api.ts:167\` \`handleListKnowledge\` maps entries to \`{ ...e, id: e.logical\_id }\` before JSON response; (3) \`handleShowKnowledge\` (api.ts:191) resolves via \`get(id) ?? getByLogical(logicalIdOf(id))\`. \`KNOWLEDGE\_COLS\` (ltm.ts:68-69) confirmed to include \`logical\_id\` as final column — \`e.logical\_id\` is valid in list handler.

<!-- lore:019ee74b-44be-76be-a392-9ef97edfadba -->
* **A2 curator.ts: within-batch ops on same entry silently drop second op after appendVersion demotes id**: Trap: \`curator.ts:365/396\` uses bare \`ltm.get(op.id)\` — looks correct because \`op.id\` is the current version id at prompt-build time. After the first op calls \`appendVersion\`, the original id is demoted (is\_current=0); \`ltm.get(op.id)\` returns null for the second op → silently skipped. Fix (PR #853): \`const entry = ltm.get(op.id) ?? ltm.getByLogical(ltm.logicalIdOf(op.id))\` at both sites. Same fallback pattern required in \`recallById()\` \`k\`/\`xk\` case (PR #893, recall.ts:1248) and \`api.ts:189\`. Pattern: any site that holds a version \`id\` from before an \`appendVersion\` call must use the dual-lookup fallback — \`get()\` alone silently returns null for demoted rows excluded from \`knowledge\_current\` view.

<!-- lore:019ee5e4-69e2-7194-b76f-38036858bb1c -->
* **A2 knowledge schema: logical\_id column is permanently nullable — silent NULL on omitted inserts**: Trap: \`logical\_id\` was added as \`TEXT\` with NO DEFAULT and NO NOT NULL constraint (v50 migration). Backfill \`UPDATE knowledge SET logical\_id = id WHERE logical\_id IS NULL\` runs in the same transaction (safe for existing rows). Looks safe because migration is transactional. Fix: all future INSERT sites must explicitly set \`logical\_id\`. All existing test inserts omit it (integrity.test.ts, embedding.test.ts, sync-data.test.ts, etc.) — those rows have NULL \`logical\_id\`. Any query filtering on \`logical\_id\` silently misses test rows. Additionally, \`sync-data.ts:570\` \`applyRemoteUpsert\` and \`syncColumns\` (\`sync-data.ts:102-118\`) omit v50 columns → pulled rows get \`logical\_id=NULL\`. Backfill on sync-apply and consider \`NOT NULL\`/\`CHECK\` in sub-PR 2/3. Not a blocker while \`appendVersion()\` has no production callers, but will cause silent data holes once versioning queries go live.

<!-- lore:019ee671-1587-7063-bf4c-43369de6ce79 -->
* **A2 sub-PR 2a: knowledge\_meta in 2a looks right but is a premature coupled migration**: Trap: moving \`confidence\`/\`last\_reinforced\_at\` to \`knowledge\_meta\` in sub-PR 2a looks like the right time (you're already touching all read sites). Rejected because: (1) forces atomic coupled change — \`decayProject\`/\`pruneOversized\` need cross-table subqueries, all ~69 confidence reads must switch in lockstep; (2) zero local benefit — \`appendVersion()\` already copies confidence forward on the version row; (3) high risk for a 'pure read pass' PR. Fix: defer \`knowledge\_meta\` to sub-PR 3 (sync concern — register needed so remote append-only log doesn't carry a mutating value). Confidence stays as mutable column on \`is\_current\` version row until then. Never re-propose 2a as the right place for \`knowledge\_meta\`.

<!-- lore:019eeee5-07a8-7820-8313-cb6e89ff0db3 -->
* **Background Lore memory process competes with git staging during PR work**: Trap: the Lore memory system runs as a background process that continuously modifies \`.lore.md\`, \`AGENTS.md\`, \`cache-warmer.ts\`, \`pipeline.ts\`, and \`cache-warmer.test.ts\` during PR work. Symptom: \`git add\` picks up unrelated background edits; commits include 6+ files instead of 2; rebase/cherry-pick produces spurious conflicts in files the user never touched. Fix: before staging, \`git diff --stat\` to identify user-authored changes; soft-reset + re-add only the 1-2 files the PR actually modifies. After stash pop during rebase, if conflicts appear in files outside the PR scope, abandon the stash (changes are background process, not user work) and \`git checkout --theirs\` the unrelated files. Re-verify with \`git status\` before each commit.

<!-- lore:019ef10f-5e5a-7324-a26d-ee418af64780 -->
* **Bedrock headers: anthropic-beta and x-anthropic-billing-header skips are load-bearing**: Trap: \`buildBedrockHeaders\` explicit skips of \`anthropic-beta\` and \`x-anthropic-billing-header\` look redundant — they're not in \`GATEWAY\_MANAGED\_HEADERS\`. Fix: they ARE NOT in \`GATEWAY\_MANAGED\_HEADERS\`, so \`forwardClientHeaders\` would pass them through to Bedrock, which doesn't understand them. The explicit skips are load-bearing. \`anthropic-version\` skip IS redundant (it's in \`GATEWAY\_MANAGED\_HEADERS\`) but harmless. \`accept\` and \`content-type\` are set LAST to override any client values; \`accept\` for streaming = \`application/vnd.amazon.eventstream\` (required for \`InvokeModelWithResponseStream\`).

<!-- lore:019ef458-001b-74f3-8ec8-4d06a0622953 -->
* **Bedrock Mantle API: presigned bedrock-api-key- tokens rejected — requires long-term ABSK key**: Trap: \`bedrock-api-key-\<base64-presigned-STS-URL>\` format looks like a valid Bedrock API key and the mantle endpoint (\`https://bedrock-mantle.us-east-1.api.aws/anthropic/v1/messages\`) is real and speaks Anthropic protocol (proper error envelopes). Fix: mantle's \`x-api-key\` surface requires a long-term static Bedrock API key (\`ABSK…\` format) — presigned STS-embedded blobs are rejected with \`401: Invalid bearer token\`. Also: \`Authorization: Bearer \<key>\` returns \`Missing Authentication Token\`; key must go in \`x-api-key\` header. Short-term presigned keys (12h expiry, prefix \`bedrock-api-key-\`) are NOT accepted regardless of expiry status.

<!-- lore:019ef47b-b8ec-715f-baff-4ee25477fbc6 -->
* **Bedrock Mantle: model access 403 ≠ auth failure — requires per-model console opt-in**: Trap: \`403 permission\_error: 'model X is not available for this account'\` looks like a key/auth problem. Fix: the ABSK key is valid (auth accepted); the error means the specific Anthropic Claude model hasn't been enabled in the Bedrock console. \`AmazonBedrockFullAccess\` IAM policy does NOT grant model access — each model requires separate opt-in at AWS Console → Bedrock → Model access (us-east-1). Rows must show 'Access granted' (green), not 'Available to request'. Approval may take minutes after submitting the use-case form. Confirmed: \`anthropic.claude-opus-4-8\` and \`anthropic.claude-haiku-4-5\` both returned 403 despite valid ABSK key.

<!-- lore:019ef127-e5cd-7a78-8a69-d5194ac3761a -->
* **Bedrock postResponse lpRouteUsable: missing self-URL-building exception records wrong protocol in snapshot**: Trap: \`lpRouteUsable\` predicate in \`postResponse\` (pipeline.ts:4454-4455) looks symmetric to \`forwardToUpstream\`'s \`providerRouteUsable\`. Fix: without the \`selfUrlBuildingProtocol\` exception (added to \`forwardToUpstream\` for MUST-FIX #3), \`postResponse\` records protocol \`'anthropic'\` and \`url=''\` for bedrock sessions instead of \`'bedrock'\` + real URL — breaking cache-warmer's \`resolveProfile\` single-source-of-truth. Fix (PR #898): mirror the \`selfUrlBuildingProtocol\` exception in \`lpRouteUsable\`; set snapshot URL to \`bedrockInvokeUrl(region,…)\` for bedrock sessions.

<!-- lore:019ef127-e5c0-7e4f-99a2-5c6eaba58abe -->
* **Bedrock routing: providerRouteUsable nulls out null-url routes — X-Lore-Provider: bedrock falls through to Anthropic**: Trap: \`PROVIDER\_ROUTES.bedrock = { url: null, protocol: 'bedrock' }\` + \`X-Lore-Provider: bedrock\` looks like it should route to Bedrock. Fix: \`providerRouteUsable\` predicate nulls out any route with \`url == null\` unless \`X-Lore-Upstream-URL\` is also present → \`effectiveProtocol = 'anthropic'\` → fails with AWS-less creds. Fix (PR #898): \`selfUrlBuildingProtocol\` gated on \`providerRoute?.protocol === 'bedrock' || 'vertex'\` (pipeline.ts:2932-2934); bedrock branch returns early and builds its own URL via \`bedrockInvokeUrl(region,…)\` — never reaches \`effectiveUpstreamBase\`. \`npmToProtocol\` never returns \`bedrock\`/\`vertex\` so dynamic routes can't produce null-url bedrock routes.

<!-- lore:019ef3a3-6e7d-78bf-9907-dccbad8cc4f8 -->
* **Bedrock URL builders hardcode amazonaws.com — no local mock possible without code change**: Trap: setting override headers or env vars to redirect Bedrock traffic to a local mock looks plausible — gateway has override mechanisms for other providers. Fix: \`bedrockInvokeUrl()\`/\`bedrockInvokeNoStreamUrl()\` in \`translate/bedrock.ts\` hardcode \`https://bedrock-runtime.${region}.amazonaws.com/...\`; the Bedrock branch in pipeline.ts:3029 is classified as \`selfUrlBuildingProtocol\` so \`X-Lore-Upstream-URL\`/\`effectiveUpstreamBase\` never apply. No \`LORE\_BEDROCK\_ENDPOINT\` env exists. Options: (1) real AWS creds + \`LORE\_BEDROCK\_REGION\` for genuine E2E; (2) add \`LORE\_BEDROCK\_ENDPOINT\_URL\` override (~10-line change) to URL builders + bedrock branch + \`regionFromBedrockUrl\` to enable LocalStack Pro/moto. SigV4 region parsing also keys off the host, so endpoint override must update region extraction too.

<!-- lore:019ef346-6e54-7f76-bae5-a73af003074c -->
* **Bedrock worker 403: markAuthStale must NOT be called — no client credential to invalidate**: Trap: calling \`markAuthStale\`/\`markGlobalAuthStale\` on a Bedrock worker 403 looks correct — 403 usually means bad credentials. Fix: Bedrock workers authenticate via SigV4 (AWS IAM), not client API keys. There is no client credential to invalidate. Calling \`markAuthStale\` on a Bedrock 403 would incorrectly stale the Anthropic API key. Guard: skip \`markAuthStale\`/\`markGlobalAuthStale\` when \`effectiveProtocol === 'bedrock'\`. Same invariant applies to \`executeWarmup\` — Bedrock warmup never sends \`x-api-key\` or \`anthropic-version\`. Tests: llm-adapter.test.ts 'bedrock worker 403 does NOT mark client credential stale'; bedrock-warmup.test.ts 'a 403 does NOT mark the client credential stale (SigV4 has none)'. Both verified non-vacuous via swap-fail-restore.

<!-- lore:019ef3cd-af67-7de1-9cf8-f13b53c75cbc -->
* **bedrock-mantle: anthropic-version goes in HTTP header, NOT request body**: Trap: putting \`anthropic\_version\` in the request body looks correct — that's how bedrock-runtime works (\`anthropic\_version: 'bedrock-2023-05-31'\` in body). Fix: bedrock-mantle uses the standard Anthropic API convention — \`anthropic-version: 2023-06-01\` as an HTTP header, NOT in the body. Also: bedrock-mantle auth uses \`x-api-key: $BEDROCK\_API\_KEY\` header (same as Anthropic API), not SigV4. Mixing up the two endpoints' conventions causes silent auth or validation failures.

<!-- lore:019ef127-e5a3-7393-9383-3602221393c9 -->
* **Bedrock: accept header clobbered by forwarded client headers — set gateway headers LAST**: Trap: setting \`accept: application/vnd.amazon.eventstream\` first in \`buildBedrockHeaders\` then overlaying forwarded client headers looks correct (gateway sets defaults, client overrides). Fix: \`accept\` is NOT in \`GATEWAY\_MANAGED\_HEADERS\`, so \`forwardClientHeaders\` forwards it and the overlay loop overwrites the Bedrock-required value. A client sending \`accept: application/json\` silently breaks streaming. Fix (PR #898): run \`forwardClientHeaders\` loop FIRST, then set \`content-type\`/\`accept\` last so gateway values always win. \`forwardClientHeaders\` lowercases all keys; explicit skips for \`anthropic-version\`/\`anthropic-beta\`/\`x-anthropic-billing-header\` are load-bearing (not in \`GATEWAY\_MANAGED\_HEADERS\`).

<!-- lore:019ef10f-5e1c-70ee-97c2-cd2e0836bd28 -->
* **Bedrock: atob() mojibakes multi-byte UTF-8 — use Buffer.from().toString('utf8')**: Trap: \`atob(base64)\` looks correct for decoding Bedrock event-stream \`bytes\` field. Fix: \`atob()\` yields a Latin-1 binary string — multi-byte UTF-8 sequences (accents, CJK, emoji, smart quotes) are corrupted. Proven: \`"café ☕ 你好"\` → \`"cafÃ© â ä½ å¥½"\`. Use \`Buffer.from(bodyJson.bytes, 'base64').toString('utf8')\` — \`Buffer\` is a global in Node.js and Bun, no import needed. Tests missed it because all \`chunkFrame\` calls in \`bedrock-stream.test.ts\` used ASCII-only text (\`"hi"\`, \`"Hello"\`, \`"split"\`). Fixed in \`bedrock-stream.ts\` (PR #898).

<!-- lore:019ef10f-5e3c-741a-9a7a-2a3e119821ac -->
* **Bedrock: stream field in request body causes validation error — endpoint controls streaming**: Trap: adding \`stream: true|false\` to the Bedrock request body looks correct (mirrors Anthropic API). Fix: Bedrock does NOT accept a \`stream\` field in the body — streaming is controlled by the endpoint URL (\`/invoke-with-response-stream\` vs \`/invoke\`). Including \`stream\` causes Bedrock to reject with a validation error. \`buildBedrockRequestBody\` must never set this field. Confirmed by user directive and test assertion (\`'stream' in capturedBody === false\`).

<!-- lore:019ee719-946b-7dde-99f8-56c19833e6cc -->
* **bun:sqlite returns null (not undefined) for missing rows — existence checks must use count query**: Trap: \`db.prepare('SELECT \* FROM t WHERE id=?').get(id) !== undefined\` looks correct for existence checks (standard JS pattern). Fix: bun:sqlite driver returns \`null\` (not \`undefined\`) for no-row results — \`!== undefined\` always returns \`true\`. Use a count query: \`db.prepare('SELECT COUNT(\*) as n FROM t WHERE id=?').get(id).n > 0\`. Also: \`getKV()\` returns \`null\` for missing keys — use \`?? '0'\` not \`|| '0'\` when reading push cursors. Also: \`decideCacheStrategy\` returns \`confident:false\` when pricing or body tokens are non-finite/non-positive; \`confident:true\` otherwise. \`shouldHoldPrefixWarm(null,…)\` → \`undefined===true\` → false — null econ is safe fallback.

<!-- lore:019eefd7-e4cb-7679-97a1-538016b4a7a7 -->
* **bustPressure degeneracy: deriving from metaThreshold comparison fails at schema minimum**: Trap: \`bustPressure = metaThreshold < baseMetaThreshold\` looks correct (lowered threshold = bust pressure active). Fix: \`metaThreshold\` has \`z.number().min(3).default(20)\` — at schema minimum \`metaThreshold==3\`, \`effectiveMetaThreshold\` returns \`Math.max(3, Math.floor(3/4))=3\` which equals the base, so \`bustPressure\` is always \`false\` regardless of actual bust count. Fix: derive directly from \`busts >= BUST\_PRESSURE\_THRESHOLD\` (exported constant from gradient.ts:1063). Single source of truth; eliminates degeneracy. Caught independently by adversarial reviewer and Seer in PR #905.

<!-- lore:019ef451-f06b-7966-b97b-67de6e45e5de -->
* **cache-analytics normalized body must NEVER go upstream — non-hex placeholders break Anthropic cache**: Trap: storing \`normalizeBodyForComparison(body)\` as \`analytics.lastRequestBody\` looks correct — normalization is needed for divergence comparison. Fix: normalized form rewrites billing header in \`system\[0]\` into non-hex placeholders (\`CCH\_REPLACEMENT='cch=\_\_'\`, \`CC\_VERSION\_SUFFIX\_REPLACEMENT='.\_\_\_;'\`) which Anthropic does NOT strip → guaranteed \`cacheRead=0\` + full rewrite on warmup. Fix: store raw compressed body (\`compressBody(currentBody)\`) in \`analytics.lastRequestBody\`; normalize only on read for divergence comparison. \`classifyRelocatableSpan\` is pure/deterministic — operates only on normalized comparison strings, never on upstream request bytes. See comment at \`CACHE\_CONTROL\_PATTERN\` in \`cache-analytics.ts\`.

<!-- lore:019eea84-aff8-777f-82c9-1d641f527b53 -->
* **cache-analytics.ts: lastRequestBody must store raw body — normalized body breaks warmup cache hits**: cache-analytics.ts: \`lastRequestBody\` must store the RAW as-sent body, not the normalized body. Trap: storing \`normalizeBodyForComparison(currentBody)\` looks correct because normalization is used for divergence detection. Fix: store raw \`currentBody\` (or \`compressBody(currentBody)\`); apply \`normalizeBodyForComparison(decompressBody(...))\` on read at comparison time. Two root causes of warmup divergence from normalized storage: (1) multi-breakpoint layout lost — normalized body had single/no breakpoints, warmer replayed wrong layout → \`cacheRead=0\`; (2) billing header artifacts — \`cch=\_\_;\` (non-hex) and \`cc\_version=\_\_\_;\` not stripped by Anthropic's regex, causing content divergence. \`lastRequestBodyLength\` stays \`normalizedBody.length\` (divergence-ratio denominator only). 🔴 Invariant: normalized string is NEVER re-parsed or sent upstream. Tests asserting normalized storage must be updated. Additional invalidation sites: pipeline.ts:4309 (model/provider change), :4735 (compaction intercepted), :4914 (post-compaction diff), :5013 (summary non-null); idle.ts (PR #877): when idle distillation creates gen-0 AND \`getLastLayer >= 1\`.

<!-- lore:019eeb20-30fa-7d57-97a1-3f3e4d18597d -->
* **cache-analytics.ts: lastRequestBody stale after idle distillation — must null at idle mutation sites**: Trap: \`lastRequestBody\` is not invalidated when lore's own idle-time prefix mutations occur (meta-distill rewriting messages\[1], post-idle compaction) — looks safe because those are internal operations, not user turns. Fix (PR #877): null \`lastRequestBody\` in \`buildIdleWorkHandler\` when idle force-distillation creates gen-0 (\`distilled > 0\`) AND \`getLastLayer(sessionID) >= 1\`. Gate on \`distilled > 0\` (not unconditional) — mirrors \`if (meta)\` pattern; avoids disabling warming for layer-0 sessions. Existing invalidation sites: pipeline.ts:4309 (model/provider change), :4735 (compaction intercepted), :4914 (post-compaction diff), :5013 (summary non-null). \`lastRequestBodyLength\` intentionally NOT nulled — only read inside \`if (analytics.lastRequestBody !== null)\` guard.

<!-- lore:019ee5f0-5603-7df1-9bcd-57e7571b1b63 -->
* **cache-warmer bodyBytes metric: use Buffer.byteLength not .length for UTF-8 wire size**: Trap: \`signedBody.length\` returns UTF-16 code units (JS string length), not UTF-8 byte count. Looks correct because ASCII content gives identical results. Fix (PR #841, merged 2a63ad6d): use \`Buffer.byteLength(signedBody, 'utf8')\` — idiomatic Node.js form, not \`Buffer.from(signedBody, 'utf8').length\`. Matters for prose/code-heavy sessions with non-ASCII content; misleads cache divergence analysis when multi-byte Unicode is present. Flagged by Seer on PR #838 at \`packages/gateway/src/cache-warmer.ts:1855\`.

<!-- lore:019ee201-4386-7eb2-a958-c4150fec4705 -->
* **cache-warmer circuit breaker: global early-return in idle.ts blocks ALL sessions when any bucket trips**: Trap: \`idle.ts\` had \`if (isCircuitBreakerTripped()) return\` BEFORE the per-session loop — one tripped bucket silently disabled warming for every session. Looks correct because 'circuit breaker tripped → stop warming' is the right intent. Fix (per-bucket refactor, PR #836 Jun 20 2026): removed global early-return; replaced with per-bucket \`continue\` inside the loop after profile resolves (\`if (isCircuitBreakerTripped(warmupBucketKey(state), now)) continue\`). Each bucket \`(sessionID, model, upstreamURL)\` is now independent. \`isCircuitBreakerTripped\` signature changed to 2-arg \`(bucketKey, now)\` — all callers updated. \`pruneExpiredCircuitBreakers()\` called once per tick before the loop to GC dead-session stale buckets. CI \`test\` job was FAILING after PR #836 (run 27870535036) — one \`edit\` tool failure (\`could-not-find-oldstring\`) may have left a test file inconsistency; actual assertion not yet identified.

<!-- lore:019eea3b-3b87-72b9-a88e-ae206dcee9d6 -->
* **cache-warmer prepareAnthropicWarmupBody: single breakpoint vs multi-breakpoint real turns causes UNCACHED divergence**: Trap: \`prepareAnthropicWarmupBody\` (cache-warmer.ts:846) strips ALL \`cache\_control\` via \`normalizeBodyForComparison\`, then re-adds ONE breakpoint on the last block of the last message only. Real turns place MULTIPLE breakpoints (system\[1] stableLtm always, plus tools, etc.). Warmup body never matches real-turn body → \`cacheReadTokens=0\` on every warmup despite \`cacheLikelyAlive=true\` and age well within 300s TTL. Confirmed in production logs Jun 21 2026: 7 UNCACHED sessions (158K–814K tokens), all \`ageSinceCacheTouch\` 265–292s. Fix: \`ensureCacheBreakpoint\` must replicate the same breakpoint placement strategy as real turns — not just the last block.

<!-- lore:019ee670-8c0c-76b2-b0a1-0c411590477e -->
* **cache-warmer shadow mode: ROI/write-efficiency guards must come AFTER shadow eval block**: Trap: shadow eval block placed after ROI guard (\`hitRate < MIN\_SESSION\_HIT\_RATE\`) and write-efficiency guard — large/growing sessions that fail those guards are never measured. Looks correct because guards are 'early exits for unprofitable sessions.' Fix (branch \`fix/cache-economics-shadow-coverage\`): hoist survival computation (\`survivalFunction\`, \`breakFraction\`, \`pSessionFinished\`, \`pReturns\`, \`costThreshold\`, \`maxProfitableCycles\`) and shadow eval block ABOVE the ROI and write-efficiency guards. Correct placement: after messageCount/inputTokens/disabled checks, before ROI and writeEfficiency guards. Behavior-neutral reorder — returned boolean unchanged. Alternative (self-contained shadow block after cooldown check) rejected: measures too early, before messageCount/inputTokens thresholds.

<!-- lore:019ebc55-ba2e-7271-9906-905e4f5ed9bf -->
* **CCH seed extraction: seed may persist in binary but algorithm/preimage changed across versions**: CCH seed extraction: consult \`quality/CCH.md\`. Seed \`0x4d659218e32a3268\` (SEED\_2\_1\_138) unchanged in 2.1.175. Hash-input ≠ wire body — cch fn scans for \`cch=\` placeholder, splits/transforms before hashing. Binary is EXEC (not PIE), fixed base \`0x200000\`. CCH\_PLACEHOLDER = \`cch=00000\`; signBody replaces with hash; regex \`/cch=(\[0-9a-fA-F]{5});/\` (trailing semicolon required). Work files in stable dir (e.g. \`~/Code/cch-investigation/\`), not \`/tmp\`.

<!-- lore:019eeee5-07ca-7ed3-bcf6-a9b774098294 -->
* **CI 'Check generated docs' job requires pnpm run generate:docs (not generate:env-docs)**: Trap: adding new env vars (e.g. \`LORE\_BEDROCK\_REGION\`, \`LORE\_BEDROCK\_PROFILE\`, \`LORE\_VERTEX\_PROJECT\`, \`LORE\_VERTEX\_REGION\`) trips the \`Check generated docs\` CI job. Running \`pnpm run generate:env-docs\` fails — the script doesn't exist; the only docs-regenerator is \`pnpm run generate:docs\` which combines BOTH config and env docs. Fix: commit the regenerated docs file as a separate small commit (e.g. \`docs: regenerate env + config docs for ...\`) and push. The job runs in ~18s once the right script is invoked. Symptom: CI fails on docs check with stale env-var documentation.

<!-- lore:019eb4d0-6fd9-7ef5-b22f-0f156eeca4b0 -->
* **cli/main.ts OPTIONS map: undeclared flags silently become booleans under strict:false**: cli/main.ts OPTIONS map: undeclared flags silently become booleans under \`strict:false\`. \`parseArgs\` with \`strict: false\` treats any undeclared \`--flag value\` as \`{flag: true}\` with value falling into positionals — \`--flag=value\` (equals syntax) still works so it looks correct. Every flag used in ANY subcommand MUST be declared in the global \`OPTIONS\` map in \`packages/gateway/src/cli/main.ts\` with correct type. Discovered in PR #696: \`--to\`, \`--project\`, \`--dry-run\`, \`--no-children\`, \`--min-confidence\` were missing.

<!-- lore:019ecab6-f928-7927-a39e-d601fb850af6 -->
* **computeMaxTokens: EMA-based floor ignores thinking budget, causing silent agent loop exit**: \`computeMaxTokens\` EMA floor ignores thinking budget → silent agent loop exit. \`computeMaxTokens\` (pipeline.ts) floors \`max\_tokens\` at \`max(8192, 3×outputEMA)\`. Anthropic counts \`max\_tokens\` as combined thinking+visible budget. Model consumes entire 8192 on reasoning → \`finish:"length"\` → no text/tool part → agent loop exits silently. Fix: pass \`thinkingBudget\`; set \`baseFloor = thinkingBudget + THINKING\_OUTPUT\_HEADROOM (8192)\`, clamped by \`modelOutput\`. Gate: \`if (!isCC)\` — CC clients excluded; opencode does NOT send \`x-claude-code-session-id\` so fix applies. EMA not corrupted by thinking truncation (Anthropic includes thinking tokens in output\_tokens).

<!-- lore:019eefe8-ced5-7440-80fe-b71052314d87 -->
* **createRecallAwareAccumulator scaling has zero test coverage — recall path usage spoof untested**: Trap: adding \`scaleClientUsage\` to \`createRecallAwareAccumulator\` looks covered because \`createStreamAccumulator\` scaling is tested. Fix: all 22 instantiations of \`createRecallAwareAccumulator\` in \`recall-stream.test.ts\` pass no \`scaleClientUsage: true\` — \`maybeScaleEvent\` (both \`message\_start\` and \`message\_delta\` branches) has zero coverage. Mutant reverting recall-aware \`message\_delta\` scaling fix survived full targeted test run (35 tests). Completely disabling \`maybeScaleEvent\` (always returning null) also survives. A recall-heavy conversation leaks real token counts to the client. Fix: add tests to \`recall-stream.test.ts\` that pass \`scaleClientUsage: true\` and assert \`clientTotal ≤ maxReportedUsage\`.

<!-- lore:019ef48d-6dbf-7cb0-b4b9-6c02482ae19b -->
* **Dead migration code in a commit steals migration index — latent data bug even with zero runtime refs**: Trap: dead migration code (zero runtime references, untested) looks harmless — it won't execute. Fix: dead migration SQL in a commit steals the migration index for that version. Any future migration that legitimately needs that index must use the next one, creating a gap or collision. Worse: if the dead code includes schema changes (e.g. \`ADD COLUMN base\_confidence\`), \`create()\` paths that don't set the new column silently default it to 1.0 — a latent data bug that only manifests after the migration runs. In PR #929, v56 CRDT block absorbed into v55 commit caused exactly this: \`insertMeta\` inserts 4 columns, \`base\_confidence\` defaults to 1.0 for all post-PR entries regardless of actual confidence. Always excise dead migration code before merge.

<!-- lore:019edb00-69d6-7131-bac9-75cfdda4ecf3 -->
* **distillation.ts action-tag extraction produces garbage LTM entries from regex-matched content (e.g. 'a-z')**: Trap: \`distillation.ts:1181-1231\` extracts behavioral tags via \`extractActionTags(result.observations)\` and creates preference entries when a tag appears in ≥3 sessions. Regex can match spurious patterns like \`a-z\` from content (character ranges in code/prose), producing garbage entries titled 'A Z'. These pollute system\[1] and cause cache busts when later deleted by consolidation. Pre-check at lines 1204-1211 prevents duplicate creates but not initial creation of garbage. The cross-session behavioral patterns \`\[a-z]\`, \`\[a-z-]\`, \`\[tool-failure]\`, \`\[enforced-workflow]\`, \`\[requested-review]\`, \`\[rejected-approach]\`, \`\[requested-tests]\` are artifacts of this bug — regex-matched content, not real user behaviors.

<!-- lore:019eeaa9-8994-756d-ba31-bda7b45dbd26 -->
* **distillation.ts: never fall back to config().model — cross-provider 404 trap**: Trap: falling back to \`config().model\` when \`input.model\` is absent looks safe (it's the project/session model). Fix: \`config().model\` may be from a different provider than the worker's upstream URL — cross-provider model name causes 404 errors. 🔴 User directive (distillation.ts lines 1041–1045): always pass the explicit worker model (\`input.model\`) through; never substitute \`config().model\`. This applies in \`distillSegment\` and any other distillation call site that receives an explicit model parameter.

<!-- lore:019ef389-a501-7db4-a808-4e0c671bb2f0 -->
* **effectiveMetaThreshold: internal Date.now() makes tests flaky — inject nowMs parameter**: Trap: \`effectiveMetaThreshold(lastTurnAtMs)\` calling \`Date.now()\` internally looks correct — it needs the current time to compute \`gapMs\`. Fix: any test that captures \`const now = Date.now()\` then passes \`now - (DEEP\_IDLE\_MS - 1)\` as \`lastTurnAtMs\` is flaky — a ≥1ms scheduling delay pushes \`gapMs ≥ DEEP\_IDLE\_MS\`, flipping the return value. Fix (PR #926): add injectable \`nowMs\` parameter; tests pass a fixed \`NOW\` constant. Sentinel: \`lastTurnAtMs === 0\` means 'never set (first turn)' → treated as deep-idle (gapMs = Infinity). All 8 \`effectiveMetaThreshold\` tests rewritten to use fixed \`NOW\` — fully deterministic.

<!-- lore:019ee6ae-7cd7-7d7d-8f97-e71ecf048e83 -->
* **embedding OOM: in-process retry at lower token counts is futile — WASM heap never shrinks**: Trap: retrying at 2048/1024 tokens in the same worker process after OOM at 4096 looks like it should work (smaller input = less memory). Fix: WASM linear memory never shrinks after allocation — heap is exhausted/fragmented after attempt 0, so lower-cap retries in the same heap still can't allocate. Halve-and-respawn (exit → main thread halves cap → respawn) gives each lower cap a fresh WASM heap, which is why it works where in-process retry cannot. The existing in-process retry ladder (embedding-worker.ts:504–539) must be deleted, not patched.

<!-- lore:019ee69b-4c7c-70ed-889b-d470e69680a4 -->
* **embedding-worker.ts: LOCAL\_MAX\_CHARS too high for constrained RAM — OOM crash-loop on 8 GB boxes**: Trap: \`LOCAL\_MAX\_CHARS = 4096 \* 4 = 16384\` chars (~4096 tokens) looks safe because Nomic supports 8192 tokens. On 8 GB machines, 4096-token sequences OOM WASM heap (4096² × 12-head × 4-byte ≈ 800 MB attention tensor per layer). \`safeLocalTruncate\` trims to 16384 chars but that's still the OOM threshold. OOM retry ladder (full → 4096 → 2048 → 1024 tokens) is futile — WASM heap never shrinks between retries. Fix: replace \`LOCAL\_MAX\_CHARS\` with \`adaptiveLocalMaxTokens()\` derived from \`os.freemem()\` + O(L²) attention model: \`L\_max ≈ sqrt(freemem × FRACTION / (n\_heads × 4 × peakFactor))\`, clamped to \`\[floor, 8192]\`, \`n\_heads=12\`. Read per-batch in \`embed()\` at embedding.ts:497.

<!-- lore:019ee69b-4c8b-79d8-8758-00e29b2e3363 -->
* **embedding.ts: localProviderKnownBroken permanent latch — OOM causes process-lifetime embedding disable**: Trap: \`localProviderKnownBroken = true\` set at embedding.ts:449–454 on any non-zero worker exit looks like a correct fast-fail. It permanently disables local embeddings for the entire process lifetime — explains why Onur's errors stop after first crash and only recur on service restart. Without fix: recovery respawns worker → same ~4096-token text re-fed → same OOM → crash-loop → WASM heap balloons → 8 GB box swaps → main event loop stalls → \`ConnectTimeout\` to Anthropic. Fix: replace permanent latch with bounded respawn + per-text quarantine to prevent crash-loops.

<!-- lore:019ee562-c70c-7fa5-b9f3-7f60a3c4b578 -->
* **getCircuitBreakerStatus: status getter must be pure read — no side-effect delete on decay**: Trap: original \`getCircuitBreakerStatus\` called \`isCircuitBreakerTripped\` which deletes the entry on decay as a side effect, then re-read the same entry — just-decayed bucket returned \`trippedAt:0\`, indistinguishable from never-tripped. Also mutated the in-memory map and wrote to SQLite on the dashboard read path. Looks correct because 'decayed bucket IS recovered' is semantically true. Fix (PR fix/circuit-breaker-status-pure-read, Jun 20 2026): \`getCircuitBreakerStatus\` rewritten as pure read — computes decay inline without calling \`isCircuitBreakerTripped\`, reports decayed bucket as \`{tripped:false, failures:0, trippedAt:0}\`, no map mutation, no SQLite write. Pruning left exclusively to \`isCircuitBreakerTripped\` on decision path and idle sweep. Rule: status/summary getters must never have delete/write side effects.

<!-- lore:019eea52-a1f7-757a-b0ae-6b749bbcda18 -->
* **gradient.ts compression stages: rawFrac applied to full usable — escalating layers inflate window past l0cap**: Trap: COMPRESSION\_STAGES rawFrac (layer-2=0.5, layer-3=0.55) applied to full \`usable\` (~800K on 1M model) → layer-2 raw budget ~400K, larger than the ~200K l0cap that triggered compression AND larger than layer-1 window; escalating GREW the window until unrecoverable 400. Fix (PR #872, merged 2026-06-21T14:18:47Z, squash \`4d7910aa\`): introduce \`stageBudgetCeiling = maxLayer0Tokens > 0 ? Math.min(usable, maxLayer0Tokens) : usable\`; \`stageBudgetUsable = Math.min(usable, stageBudgetCeiling)\`; apply to escalated stages (layers 2+) only. Layer-1 base \`rawBudget = usable\*0.4\` must NOT be clamped. \`isEscalated = stageLayer > 1\`. Also clamp escalated-stage distilled budget. Confirmed in production logs: post-deploy layer-2 ratio dropped from ~0.499 (unclamped usable×0.5) to ~0.287–0.321 (clamped cap×0.5). M1–M4 mutant kills documented in test suite.

<!-- lore:019eea52-a26c-7447-b9cb-6afd47b6ef12 -->
* **gradient.ts: chars/3 token estimate undercounts by 1.9–3x on real sessions — safety factors must account for this**: Trap: \`chars/3\` token estimate looks like a reasonable approximation. In production, it undercounts by 1.9–3x. \`UNCALIBRATED\_SAFETY=1.5\` partially compensates but is insufficient for worst-case 3x undercount. \`CALIBRATED\_DELTA\_SAFETY=1.3\` applies only to delta estimates. \`HARD\_CEILING\_MARGIN=0.95\` blocks passthrough when rebuilt window exceeds 95% of maxInput. PR #887 (merged) normalizes \`cacheSizeCompressed\` to \`cacheSizeFull\`'s input scale: compressed size now includes the non-body floor (overhead + sessLtmTokens) and applies the same safety factor (1 if calibrated, else UNCALIBRATED\_SAFETY). Without this, the full-vs-compressed delta did not reflect actual compaction savings. Shadow/log-only change; no behavior change. 🔴 Any path where \`layer0Input\` uses a DIFFERENT effective factor than \`computeCompressedCacheSize\` must be flagged — scale mismatch fabricates savings.

<!-- lore:019edfbc-ede7-7351-a97a-ac6f6c0839ea -->
* **idle.ts global sweep: use loreConfig() not GatewayConfig for knowledge.enabled gate**: Trap: \`GatewayConfig\` param passed to the idle scheduler does NOT expose \`knowledge.enabled\` — it looks like the right place because it's the config object in scope. Fix: use \`loreConfig()\` (imported from core) to check \`knowledge.enabled\` before running \`pruneDeadEntriesAllProjects()\`. Same pattern used elsewhere in idle.ts for knowledge-gated operations. Note: if \`knowledge.enabled\` is false, \`lastGlobalDeadSweepAt\` is NOT updated — sweep runs on next tick after re-enable (intentional: catches entries that accumulated while disabled).

<!-- lore:019ef068-d952-7d04-8588-451fea6112fe -->
* **IPv6 host→URL interpolation: bracketHost must be applied at ALL CLI URL construction sites**: bracketHost must be applied at ALL CLI URL construction sites, not just server. Trap: fixing bracketHost only in server.ts handleNodeRequest looks complete but CLI code builds URLs from raw config.hosts/opts.hosts\[0] at start.ts:191 (daemon probe), start.ts:207–208 (runDaemon probe), and run.ts:195 (gatewayUrl). Without bracketing, http://::1:PORT is invalid, fetch throws, daemon never reports healthy. Apply bracketHost() at all http:// template-literal interpolation sites. PR #912 fixes all sites. Node's net.listen() requires unbracketed IPv6 — only URL-construction sites need bracketing.

<!-- lore:019ef35a-01ce-7c91-a77a-e81e9f1f537b -->
* **isVerifierCall: bare \`ci\` keyword matches \`npm ci\` (clean install) — false positive corrupts knowledge confidence**: Trap: \`(?:run\s+)?(?:test|...|ci|...)\` in \`VERIFIER\_LEADING\` looks correct — \`ci\` is a valid script name. Fix: \`npm ci\` (clean install, not a verifier) matches the optional-run group → false positive → confidence penalty. Fix (PR #927): move \`ci\` and other script keywords (\`verify\`, \`validate\`, \`e2e\`, \`coverage\`, \`spec\`) into a dedicated branch requiring explicit \`run\`/\`exec\`/\`dlx\` prefix: \`(?:npm|pnpm|yarn|bun)\s+(?:run|exec|dlx)\s+(?:test|ci|verify|...)\b\`. Bare \`npm ci\` no longer matches; \`pnpm run ci\` still does. \`ci\` retained for \`make\`/\`just\`/\`task\`/\`rake\` targets (always explicit). Confirmed via 51-case empirical test suite in PR #927 adversarial review.

<!-- lore:019ef48d-6d99-7044-88ce-b16763ca658c -->
* **jj absorb mis-routes adjacent lines into wrong commit — v56 contaminated v55 PR**: Trap: \`jj absorb\` during stacked-commit work looks safe — it routes hunks to the commit that last touched those lines. Fix: when new migration lines are adjacent to lines authored by a prior commit (e.g. v55 lines adjacent to v56 lines), \`jj absorb\` mis-routes the new block DOWN into the prior commit. In PR #929, v56 CRDT migration (\`base\_confidence\` ALTER, \`knowledge\_meta\_crdt\` CREATE TABLE, recovery block) was absorbed into the v55 commit (nnks/3b-1) because lines were adjacent. Result: dead untested code, stolen migration index, latent data bug (\`base\_confidence\` defaults to 1.0 for all post-PR entries). Fix: after \`jj absorb\`, always diff each commit individually to verify no cross-contamination before pushing.

<!-- lore:019ef387-4af0-7f82-944c-5270dfb2e537 -->
* **knowledge\_meta INNER JOIN in duplicate detection — raw INSERTs without meta rows return 0 duplicates**: Trap: \`ltm.check()\` duplicate detection (ltm.ts:2292–2305) uses \`INNER JOIN knowledge\_meta m ON m.logical\_id = k.logical\_id WHERE m.confidence > 0.2\` — looks like a LEFT JOIN would work. Fix: raw test INSERTs into \`knowledge\` without corresponding \`knowledge\_meta\` rows cause the INNER JOIN to return nothing → 0 duplicates found even when titles match. Any test using raw SQL to insert knowledge entries MUST also insert a \`knowledge\_meta\` row for those entries to appear in duplicate/integrity checks. \`ltm.create()\` handles this automatically; raw INSERTs do not. Note: \`vectorSearch\` uses \`knowledge\_current\` (LEFT JOIN, COALESCE→1.0) so entries without meta rows still pass confidence filter there — asymmetry between FTS INNER JOIN paths and vector search paths.

<!-- lore:019ee013-8bb2-734b-9994-9f1922bcf654 -->
* **maintain\_usage re-parent bug: scope\_id mutation drifts counter silently**: ✅ RESOLVED (migration 0007): \`authenticated\` re-parent blocked \`23514\` by scope-immutability guard at \`0007:214-217\`; \`service\_role\` re-parent also blocked \`23514\` (guard fires even bypassing RLS). \`maintain\_usage\` always writes to \`new/old.scope\_id = auth.uid()\` — users can only move their own counter, griefing other tenants' counters is impossible. SHOULD-FIX remaining: \`exists\_pk\` moved outside advisory lock in 0007 (before lock at line 267) — weakens TOCTOU hardening; fix by restoring 0005 ordering (lock before \`exists\_pk\`).

<!-- lore:019eea28-dcc7-768d-b1ac-2cc87ade0377 -->
* **measure-embed-cap harness: L values must be ascending — WASM heap never shrinks**: Trap: running token-length measurements in descending order (e.g. 4096→2048→1024) looks like it would isolate each footprint independently. Fix: WASM linear memory never shrinks after allocation — RSS at L=1024 after L=4096 reflects the 4096 high-water mark, not the 1024 footprint. Regression fit is ruined because all lower-L points share the same inflated RSS. Always run L values in strictly ascending order so each measurement captures the true incremental footprint at that length. This is why the harness uses ascending L sequence.

<!-- lore:019edb00-69ce-7fdb-873a-b0e7024a0323 -->
* **Mid-session LTM consolidation deletes entries, busting frozen system\[1] cache on next turn**: Mid-session LTM consolidation deletes entries, busting frozen system\[1] cache on next turn. Root cause: consolidation deletes shift byte offsets, busting downstream cache. Fix (PR #808, v45): \`stableLtmCache\` freezes system\[1] for the session's entire lifetime — NEVER recomputed from live knowledge table mid-session. The frozen pin is set once and persisted; consolidation deletes no longer affect the active session's system\[1] bytes. Newly-curated preferences are picked up only on the NEXT session start. PR #814 adds consolidation retry-storm fix: cooldown armed only on successful no-op (not on throw), in-flight guard set before await and cleared in \`finally\`.

<!-- lore:019ebafd-32b7-7468-bdb2-5c77ff6c4ccf -->
* **opencode-lore plugin: legacy export path causes getLegacyPlugins() to be invoked**: opencode-lore plugin: never export named functions alongside default — legacy loader invokes them as plugins. \`getLegacyPlugins()\` iterates \`Object.values(mod)\` and treats any function as a server plugin. So \`export default LorePlugin\` with separately-exported helpers (\`applyLoreProviderConfig\`, \`probeGateway\`) causes them to be invoked as plugins; returning undefined pushes them into \`hooks\[]\` → all dispatch loops crash. Fix: \`default export { server: LorePlugin }\` so \`readV1Plugin\` takes the v1 path. NEVER export helper functions from the plugin's top-level index — they leak into the legacy loader.

<!-- lore:019ef0c4-1c7f-7133-9958-57bc57571a44 -->
* **outcomeImpact integration gap: toConsolidationEntry DB flow untested — logical\_id vs id regression risk**: Trap: unit tests for \`consolidationValueTag\` rendering look complete (5 tests, all mutations killed). Fix: \`toConsolidationEntry\` calls \`ltm.outcomeImpact(e.logical\_id)\` — no test verifies this DB lookup flows through to the rendered prompt. Mutating \`toConsolidationEntry\` to always return zero impact kills 0 tests (confirmed in PR #921 review). Critical regression: if code mistakenly passes \`e.id\` instead of \`e.logical\_id\`, wrong counts returned for v2+ entries where \`id ≠ logical\_id\`. SHOULD-FIX: add integration test that records injections with verdicts and asserts rendered consolidation prompt shows correct verifier pass/fail counts keyed on \`logical\_id\`.

<!-- lore:019ef00b-bcc3-7a4a-9b4c-d524f62b9ffe -->
* **outcomeImpact: omitting verdict IN filter lets NULL rows clobber lastVerdict**: Trap: removing \`verdict IN ('pass','fail')\` filter from \`outcomeImpact\` SQL looks like an equivalent mutant — NULL-verdict rows aren't counted in pass/fail tallies because the JS loop only increments on explicit 'pass'/'fail'. Fix: \`lastVerdict\` uses \`MAX(created\_at)\` across verdict groups; a NULL-verdict row from a 'none' session with a later injection timestamp wins the MAX and overwrites \`lastVerdict\` with null. Confirmed: Mutation B survived all 25 tests; scratch test \`AssertionError: expected null to be 'pass'\` proved the bug. Coverage gap: no test mixes NULL-verdict rows with pass/fail rows under the same logical\_id. The filter is load-bearing for \`lastVerdict\` correctness even though it's invisible to pass/fail counts. Note: PR #906 dropped \`lastVerdict\` from \`OutcomeImpact\` entirely (misleading semantics); \`outcomeImpact\` now returns only \`{ passes, fails }\`. PR #921: \`outcomeImpact\` keyed by \`logical\_id\` in consolidation wiring.

<!-- lore:019ef3a3-dc76-7fc1-a291-3c5627e0bcf7 -->
* **Overflow ToC B: scoring cadence ≠ injection cadence — re-scores every turn but only re-freezes on material change**: Trap: assuming overflow ToC (B) requires a new per-turn injection mechanism because relevance is re-scored every turn. Fix: Lore re-scores every turn but only re-freezes the delta on material change (\`hasMaterialLtmDelta\`, pipeline.ts:1010). \`applySessionPromptDeltas\` (pipeline.ts:916) replays persisted deltas byte-identically to preserve conversation cache (#747 requirement). Conflating scoring cadence with injection cadence leads to incorrectly concluding B needs invasive new plumbing — it doesn't. B's overflow section rides the existing frozen-delta mechanism with zero new cache-bust events.

<!-- lore:019ef39c-72c4-7a00-bce7-4456e479867c -->
* **Partial v55 migration boot-loop: DROP COLUMN before backfill SELECT causes unrecoverable crash**: Trap: v55 migration runs via \`database.exec()\` (auto-commits each statement) — looks safe because migrations are idempotent. Fix: if interrupted after \`DROP COLUMN confidence\` but before completion, \`schema\_version\` stays <55. Next boot retries migration; backfill \`SELECT logical\_id, confidence FROM knowledge\` throws \`no such column: confidence\`. Catch block only matches \`/duplicate column name/i\` — this error re-throws → \`migrate()\` throws → boot loop. \`recoverMissingObjects\` unreachable (needs \`current>=55\`). \`stripAppliedAlters\` only handles ADD COLUMN. Fix: make backfill column-presence-aware (mirror \`recoverMissingObjects\`' \`hasConfidence\` check) OR wrap the DROP+backfill in a single transaction-safe block. Empirically confirmed with \`node:sqlite\`.

<!-- lore:019ee7c1-78ba-7300-9d61-eb87647879d1 -->
* **pipeline.ts buildStreamingResponse: sessionID must be threaded explicitly, not via recallContext**: Trap: accessing \`sessionID\` via \`recallContext\` inside \`buildStreamingResponse\` looks correct because recall context carries session state. Fix: \`recallContext\` is passed ONLY when \`hasRecallTool\` — for common non-recall streaming turns \`sessionID\` is \`undefined\` → \`"unknown"\`. The \`"request"\` route never passes a sessionID at all. Thread \`sessionID\` as an explicit parameter to \`buildStreamingResponse\` from \`sessionState.sessionID\` at the call site. Applied in PR #862 to ensure abort-under-pressure capture always has a valid session fingerprint.

<!-- lore:019eeea5-33d3-7fce-b5cd-9ee989d13881 -->
* **PR #897 property test gap: apply() uses raw SQL — never creates multi-version entries, vacuous ping-pong assertion**: Trap: \`sync.property.test.ts\` \`apply()\` helper used raw SQL (\`UPDATE knowledge SET content=? WHERE id=?\` / \`DELETE FROM knowledge WHERE id=?\`) — bypassed \`ltm.update()\`/\`ltm.remove()\`, so never created v2+ entries. The entire property suite never exercised the append-only versioning code path. The 'no ping-pong' / strict \`conflicts==0\` assertion was vacuous for multi-version entries. Fix (PR #897 commit \`3c251810\`): \`apply()\` \`update\` case now calls \`ltm.update(op.id, { content: op.content })\`; \`delete\` case calls \`ltm.remove(op.id)\`. \`insert\` case SQL now includes \`logical\_id\` column. Key insight: convergence oracle alone does NOT surface the BLOCKER — fixpoint drain eventually converges even with false conflicts. Only the strict \`conflicts==0\` property catches it. Property suite now stable across 5 consecutive runs.

<!-- lore:019eeea5-3386-71a4-8249-3a440154875e -->
* **PR #897: classifyRemoteRow uses getRowById (demoted base row) instead of knowledge\_current — spurious conflicts on multi-version entries**: classifyRemoteRow and localBefore snapshot must use knowledge\_current lookup, not getRowById with logical\_id. Trap: after any local edit, the logical\_id points to a demoted v1 row (is\_current=0), while the current version has a different id. getRowById returns the stale v1 row, producing false hash mismatches → spurious conflicts or wrong conflict snapshot. Fix: use currentKnowledgeRow(logicalId): query knowledge\_current WHERE COALESCE(logical\_id, id) = ?, re-key row.id = logicalId. Apply to classifyRemoteRow, localBefore, and any site holding a pre-versioning id. PR #897.

<!-- lore:019eeee4-fb23-717d-9e76-99443d234d8e -->
* **PR #902 outcome-reward: confidence < ceiling guard prevents demotion, not just over-boost**: Trap: \`confidence < OUTCOME\_BOOST\_CEILING\` guard in PASS block looks like it only prevents over-boosting entries already above ceiling. Fix: without the guard, \`MIN(0.8, 0.95+0.02)=0.8\` silently demotes a 0.95 entry to 0.8 on every passing session — flattening the entire upper confidence band. Existing test uses confidence exactly at ceiling (0.8), so \`MIN(0.8, 0.82)=0.8\` is identical with/without guard — Mutant B survived all 19 tests. 🔴 Fix shipped (PR #902): added test for high-confidence entry (0.95) in passing session — must NOT be demoted to ceiling. Guard's second role (demotion prevention) now tested. Also: \`is\_deleted = 0\` filter added to both UPDATEs in \`creditSessionOutcome\` to mirror \`knowledge\_current\` view.

<!-- lore:019eeee4-fb0f-77ad-91ae-a521fd7e09e6 -->
* **PR #902 outcome-reward: cross\_project=0 guard in UPDATE is non-redundant — promoted entries trap**: Trap: \`cross\_project=0\` guard in \`creditSessionOutcome\` UPDATEs looks redundant because \`project\_id=?\` already scopes to one project. Fix: promotion sets \`cross\_project=1\` WITHOUT clearing \`project\_id\` (ltm.ts:2588) — promoted entries have both \`project\_id=pid\` AND \`cross\_project=1\`. Without the backstop, a promoted entry passes the \`project\_id=?\` filter and gets incorrectly penalized. Existing test (line 307) uses a global entry (\`project\_id=NULL\`) — already blocked by \`project\_id=?\`, so the \`cross\_project=0\` guard was never exercised. Mutant A survived all 19 tests; killed only by a promoted-entry test. 🔴 Fix shipped (PR #902): test added — entry starts \`cross\_project=0\`, gets promoted (retains \`project\_id\`), receives FAIL credit — confidence must not change. PR #906 adds \`verdict\` column to the same UPDATE: \`SET credited=1, verdict=?\`. Verdict rows transition NULL→pass|fail exactly once; \`WHERE credited=0\` plus top-of-function early-return on \`verdict==='none'\` enforces this atomically inside \`withTransaction\`.

<!-- lore:019ef127-e5db-75e2-ae43-fc4359277234 -->
* **Property test headersArb never generates literal 'accept' — GATEWAY\_MANAGED\_HEADERS clobber bugs invisible to property tests**: Trap: property tests with \`headersArb\` generating random header names matching \`^\[a-z]\[a-z0-9-]\*$\` look like they cover header-forwarding edge cases. Fix: random generation will never produce the literal string \`'accept'\` (or other specific gateway-managed names). Example tests only inject \`anthropic-\*\` headers. Any bug where a specific header name is clobbered (e.g. Bedrock \`accept\` override, PR #898 MUST-FIX #2) is invisible to property tests — requires a targeted deterministic test injecting that exact header name.

<!-- lore:019eec9d-45ee-78ac-abc6-22fecf2e35da -->
* **provider-aware auth guard: must exempt config.workerApiKey or LORE\_WORKER\_API\_KEY users lose all background work**: Provider-aware auth guard must exempt \`config.workerApiKey\` or \`LORE\_WORKER\_API\_KEY\` users lose all background work. Trap: adding \`if (model && !resolveAuth(sessionID, model.providerID)) return\` to \`scheduleBackgroundWork\` (pipeline.ts) and idle loop (idle.ts) looks complete. When \`config.workerApiKey\` is set, \`getWorkerAuth\` bypasses \`resolveAuth\` entirely — the guard wrongly skips all background work for dedicated-key sessions. Fix: add \`!config.workerApiKey &&\` to both guard conditions (or check \`loreConfig().workerApiKey\` first and skip the \`resolveAuth\` check). \`config\` is in scope at both sites. Confirmed in PR #895 MUST-FIX #1 adversarial review.

<!-- lore:019ef451-f057-77dd-8681-56657edc5425 -->
* **recordCacheTurnUsage test: lastTurnWasIdle seed=false makes consumption assertion vacuous**: Trap: seeding \`lastTurnWasIdle: false\` in \`seedSession\` then asserting \`expect(state.lastTurnWasIdle).toBe(false)\` looks like it tests the consumption line \`sessionState.lastTurnWasIdle = false\`. Fix: the assertion checks the initial value — M3 mutation (removing the consumption line) survives all 5 tests. Fix: seed \`lastTurnWasIdle: true\` so the consumption line must actively flip it to \`false\`. Confirmed via: M3 applied + seed changed → test fails; M3 applied + seed unchanged → test passes. Location: \`packages/gateway/test/pipeline-cache-turn.test.ts\` line 72 (seed) and line 191 (assertion).

<!-- lore:019ef00b-bcae-78a6-9c93-11229164660c -->
* **recoverMissingObjects: index creation before ALTER makes recovery dead code**: Trap: placing \`CREATE INDEX idx\_ksi\_logical\_verdict ON knowledge\_session\_injections (logical\_id, verdict)\` in the large \`exec()\` block looks correct because it mirrors the migration. Fix: when \`knowledge\_session\_injections\` exists without the \`verdict\` column, the index creation throws \`no such column: verdict\` BEFORE the defensive ALTER block can add the column — making the ALTER dead/unreachable code. The wrong approach looks right because the big \`exec()\` block is where all schema objects live. Fix: move the index creation INSIDE the ALTER block, immediately after \`ALTER TABLE ... ADD COLUMN verdict TEXT\`. Confirmed empirically: faithful v54 repro DB (verdict + index both dropped) throws on reopen; fix resolves it. Applies to any \`recoverMissingObjects\` block that creates an index referencing a column added by a nearby ALTER.

<!-- lore:019ef3a3-dcea-747b-adff-131e114a77f3 -->
* **reinforce() always resets last\_reinforced\_at — negative delta resets decay clock protecting stale entries**: Trap: calling \`reinforce(id, delta)\` with a negative delta to penalize stale references looks correct — it adjusts confidence. Fix: \`reinforce()\` at ltm.ts:853 ALWAYS writes \`last\_reinforced\_at = now\`, even for negative deltas. A penalty call resets the decay clock, protecting a stale entry from decay for another 7 days (\`DECAY\_GRACE\_MS\`). Fix for Phase 0 reference-validity validator: implement \`penalizeStaleReferences()\` as a direct confidence UPDATE that does NOT touch \`last\_reinforced\_at\`. Penalty: \`−0.05\` per stale reference, soft-capped.

<!-- lore:019eebaf-0f40-7ab2-b74d-c2167c4e9e74 -->
* **resetPipelineState drain ordering: idle scheduler must stop BEFORE drain, not after**: Trap: stopping the idle scheduler after \`llmClient.shutdown\` (original order) looks correct because LLM drain is the 'big' shutdown step. Fix: idle scheduler must be stopped FIRST in the non-fast drain block — otherwise idle ticks fire during shutdown and schedule new \`runBackground\` work that isn't in the drain snapshot. Correct order: (1) \`stopIdleScheduler()\` + null it, (2) \`await drainBackground()\`, (3) \`await boundedSettle(\[...inFlightBackground])\`, (4) \`llmClient.shutdown\`, (5) swap/clear state. Double \`stopIdleScheduler\` call is safe: top block calls then nulls it; later block is \`if (stopIdleScheduler)\` → no-op in non-fast path; fast path skips top block so later block does the stop.

<!-- lore:019eeb34-c8ab-7321-98ae-f8cbd3953ad4 -->
* **resetPipelineState: fire-and-forget background promises write to new harness DB — test flake trap**: Trap: \`resetPipelineState\` swapped the \`db()\` singleton without draining in-flight background work. Urgent/incremental distillation promises are fire-and-forget; \`resetBackgroundLimiter()\` only \`clearQueue()\`s — in-flight tasks continue writing to the NEW harness's DB. Symptom: phantom header-NULL \`session\_state\` row via \`saveSessionTracking\` \`INSERT OR IGNORE\`, causing count=2 instead of 1 (~1/3639 flake, issue #885). Fix (PR #888): two-phase teardown in \`resetPipelineState\` — (1) stop idle scheduler FIRST, await \`drainBackground(5000)\` (limiter tasks), await \`boundedSettle(\[...inFlightBackground], 5000)\` (urgent distillation); (2) then swap/clear all state. Fast path (\`{ fast: true }\`) skips drain — test teardown uses non-fast; production shutdown uses fast to avoid Ctrl+C hang. \`boundedSettle\` is a shared helper in \`background-limiter.ts\`.

<!-- lore:019ee7c1-78c3-7016-bf43-155cda10ee6b -->
* **runStartupBackfill SQL predicates must match the backfill loops exactly**: Trap: SQL predicates in \`runStartupBackfill\` (embedding.ts:1614) that don't match the actual backfill loop conditions produce misleading coverage stats — the function reports different counts than what it actually processes. 🔴 User directive: 'always match what we're about to do.' Fix: ensure WHERE clauses in the pre-backfill COUNT queries are identical to the WHERE clauses in the backfill loops themselves. \`BackfillStats\` return type (fields: knowledgeEmbedded, distillationEmbedded, entityEmbedded, kTotal, kWithEmb, dTotal, dWithEmb) must reflect actual work done.

<!-- lore:019ece09-5df6-7392-b67c-043a729239a1 -->
* **safeDeltaInsertIndex: steady-layer-1 drift NOT fixed by delete/re-anchor predicate**: Trap: \`shouldResetDeltaOnCompression(prevLayer, curLayer)\` returns false when layer stays at 1 (curLayer===prevLayer), so the delete/re-anchor branch never fires for steady-layer-1 raw-window slides — the production bug case. It looks like the fix because it handles layer escalation. The docstring claimed 'steady-state-at-layer-1 raw-window slide is handled by the caller's additional raw-window-repin check' — but that check was NEVER implemented at the call site (pipeline.ts:6078). Fix: extended \`shouldResetDeltaOnCompression\` with third param \`outOfIdle: boolean\` (default false); returns \`true\` when \`outOfIdle===true\` regardless of layer change. Call site passes \`sessionState.lastTurnWasIdle ?? false\`. Replay-time re-validation (\`applySessionPromptDeltas\`) handles non-idle steady-layer-1 drift. One-shot post-idle re-anchor alone doesn't fix steady-layer-1 drift — window start marches forward every turn.

<!-- lore:019eeb06-075d-7495-9d44-4bab24508d1c -->
* **session-rotation-merge.test.ts: unscoped session\_state counts inflate via phantom rows from leaked background tasks**: Trap: \`SELECT session\_id, project\_path FROM session\_state\` with no WHERE clause looks correct for counting sessions in rotation tests. Fix: \`db()\` is a process-global singleton — leaked in-flight background tasks (urgent/incremental distillation, \`saveSessionTracking\`) at teardown→setup boundary call \`INSERT OR IGNORE\` into the next harness's DB, creating phantom rows. \`stopIdleScheduler\` only calls \`clearInterval\` (not async drain); \`resetBackgroundLimiter\` leaves in-flight tasks running. Phantom row inflates count from 1→2, flaking exact-count assertions. Fix (PR #884): scope all \`FROM session\_state\` selects to \`WHERE header\_name = '\<test header>'\`. Issue #885 tracks deeper fix: drain in-flight background/idle work before \`closeDB()\` swaps the singleton.

<!-- lore:019eead1-cc11-7665-8f64-02c4a46590fd -->
* **setup-backup.ts restoreTomlBackup: missing footer must bail, not truncate — data loss trap**: Trap: when the TOML backup footer (\`# end lore setup backup\`) is missing, the original \`restoreTomlBackup\` truncated file content from the header onwards — silently destroying user config. Looks correct because 'remove the backup block' is the intent. Fix (PR #876, commit \`65404cd7\`): if footer is not found, bail out entirely (refuse to touch the file). Also: \`buildTomlBackupBlock\` now accepts \`loreValues: Record\<string, string>\` (not \`keys: string\[]\`) and embeds the lore-set value alongside the prior value, enabling revert-only-if-unchanged comparison matching \`restoreJsonBackup\`'s safety check.

<!-- lore:019ef02c-f197-7282-8eb5-13a894b0cd15 -->
* **startGateway reuse probe: EADDRINUSE-gated probe misses non-overlapping interface binds**: Trap: placing existing-gateway reuse probe inside \`EADDRINUSE\` catch looks correct — port conflict is the only reason to check. Fix: when new instance binds non-overlapping interface (existing on \`127.0.0.1\`, new on \`127.0.0.2\`), bind succeeds, catch never fires, second redundant gateway starts. Fix (PR #908/#920): \`firstReachableHost(hosts, port, probe)\` probes all hosts concurrently via \`Promise.all\`. Pre-bind probe runs for each \`candidatePort !== 0\` BEFORE bind; post-bind EADDRINUSE catch retained as TOCTOU backstop. \`runDaemon()\` probes all configured hosts + \`127.0.0.1\` fallback. \`opts.local===true\` always disables remote mode. Port 0 skipped correctly. Both pre-bind and post-bind reuse blocks use \`firstReachableHost\` + pin \`config.hosts = \[reachableHost]\` so callers see the actual reachable host. All host→URL interpolations in CLI must use \`bracketHost\` for IPv6 (PR #912). Mutation A (disable pre-bind probe) killed by test 3 in start-gateway-reuse.test.ts; Mutation B (single-host daemon probe) killed by daemon-args.test.ts; Mutation C (disable hosts pin) killed by \`expect(handle.config.hosts).toEqual(\["127.0.0.1"])\` assertion.

<!-- lore:019eefe8-ceb8-700b-853c-586765b8ef89 -->
* **stream/anthropic.ts message\_delta carries full cumulative usage — must scale all fields, not just output\_tokens**: Trap: only rewriting \`output\_tokens\` in \`rewriteUsage()\` for \`message\_delta\` looks correct (delta = incremental output). Fix: Anthropic's terminal \`message\_delta\` carries FULL cumulative usage (\`input\_tokens + cache\_read + cache\_creation\`). Client's last-write-wins storage means unscaled fields from \`message\_delta\` overwrite the scaled \`message\_start\` values — defeating the meter spoof entirely. Fix: scale every field present in \`deltaUsage\` (guarded by \`typeof deltaUsage.X === 'number'\`), using real accumulated totals from internal \`usage\` state as proportional base. Fallback \`scaled.X ?? deltaUsage.X\` is a latent leak: if \`message\_start\` omitted a field but \`message\_delta\` includes it, raw unscaled value leaks to client. Safer: scale the delta's own value directly rather than fall back to raw.

<!-- lore:019ee5de-eb53-70a1-91b5-df122b02fd4b -->
* **Stryker vitest-runner@9.6.1 × Vitest 4: false survivors due to test attribution bug**: Stryker vitest-runner@9.6.1 × Vitest 4: compound-condition mutant footgun causes misdiagnosis: Trap: Stryker's ConditionalExpression mutator replaces left operand \`A\` of \`if (A && B)\` with \`true\` — visually identical in report to whole-condition mutant at same column. Hand-verifying the wrong mutant (\`if (true)\` instead of \`if (true && B)\`) produces a false 'confirmed false survivor'. Fix: when hand-verifying, apply the EXACT partial replacement. Earlier diagnosis of 'false survivors due to test attribution bug' was itself a misdiagnosis — Stryker IS reliable; 68.98% baseline is valid. Equivalent mutants (e.g. \`DELETE WHERE seq<=0\`) are a separate class — no test can kill them by design. Always apply the exact mutant from the report, not a visually similar simplification.

<!-- lore:019eeece-ef1d-7108-a182-be70680a232a -->
* **sync convergence oracle insufficient — strict conflicts==0 property required to surface false-conflict BLOCKER**: Trap: asserting only that local==remote after fixpoint drain looks sufficient for sync correctness. Fix: fixpoint drain eventually converges even when \`classifyRemoteRow\` produces false conflicts (remote-wins LWW still applies correct content). The BLOCKER (spurious conflicts on versioned entries) is invisible to convergence-only assertions. Only a strict \`conflicts==0\` property surfaces it. Confirmed: reverting the classify fix, convergence properties all pass (4/5); only the no-ping-pong \`conflicts==0\` property fails. Always include a strict zero-conflicts assertion in sync property tests, not just convergence.

<!-- lore:019ee719-9473-77e0-9ebc-77b2dffb2e73 -->
* **Sync convergence property: one-shot push+pull is too strict — self-conflicts require fixpoint drain**: Trap: asserting \`local == remote\` after a single \`pushOnce + pullOnce\` looks correct (sync should converge). Fix: self-conflict scenario \`insert→push→delete→pull\` causes remote-wins conflict resolution to resurrect the row locally; the pending delete in outbox gets pushed on next drain. Engine IS eventually consistent but requires multiple rounds. Use drain-to-fixpoint loop (up to 8 push+pull rounds until quiescent) for convergence assertions. Confirmed: engine settles within 2 rounds across 1000 random cases (5 seeds); maxRoundsToConverge=2, neverConvergedWithin32=0. Note: the \`content\_hash\` tombstone bug (see separate entry) causes STABLE divergence even with fixpoint drain — fix that bug first.

<!-- lore:019edff0-4721-7514-9ddf-f9eac54de7da -->
* **sync integration tests: hand-written mocks cannot exercise RLS/triggers/quotas — integration tests are the only valid gate**: Hand-written mocks cannot exercise RLS/triggers/quotas — integration tests are the only valid gate. \`sync-security.integration.test.ts\` runs against real Postgres+PostgREST via Docker; gated on \`LORE\_INTEGRATION=1\`. Test suites: (1) multi-tenant RLS isolation — cross-tenant read, scope\_id forge (42501), re-parent steal/donate, anon access; (2) tier server-only — column grant blocks tier UPDATE, service\_role can upgrade; (3) quota enforcement — row cap, byte cap, revival bypass, tombstone flood. \`FREE\_LIMITS\` constant mirrors 0003 caps. \`afterEach\` restores \`max\_rows\` but NOT \`max\_bytes\` — \`setByteCap()\` calls at lines 330, 346 leak into subsequent tests (benign due to test ordering; SHOULD-FIX). \`dockerReady()\` is synchronous. \`beforeAll\` timeout: 180,000ms. 30 concurrent INSERTs at \`max\_rows=10\`: exactly 10 committed, advisory lock serializes growth correctly.

<!-- lore:019ee014-04f3-7790-a7cd-931c174831fa -->
* **sync quota: tombstone-flood bypasses both row cap and byte budget**: ✅ RESOLVED (migration 0007 physical accounting): tombstone INSERTs now counted toward byte budget and gated. \`enforce\_row\_quota\` INSERT branch counts tombstone bytes; 50×8KB tombstones blocked at row 3 on byte cap (\`23514\`); \`counterRows == physicalRows\`. Soft-delete bytes never restored under physical accounting. Remaining LOW accepted tradeoff: user creating+deleting ~500 entries exhausts free row cap with zero live rows — self-inflicted, bounded by own cap, no cross-tenant harm. Documented at \`0007:31-32\`; depends on A2 hard-DELETE reaper for reclaim path.

<!-- lore:019ee014-0500-7aae-96f8-40ea917bc359 -->
* **sync security: author\_id forgery not blocked by RLS — attribution spoof possible**: ✅ RESOLVED (migration 0007): RLS \`WITH CHECK (scope\_id = auth.uid() AND author\_id = auth.uid())\` at \`0007:74\` blocks both INSERT and UPDATE with forged \`author\_id\` — rejected \`42501\`. Previously only \`scope\_id\` was constrained. All 5 synced table RLS policies updated. Empirically verified: forged author\_id on INSERT and UPDATE both rejected.

<!-- lore:019eea22-b8cc-76d1-b547-eba90b324c95 -->
* **sync-data.ts reconcile(): stale upsert below push cursor blocks delete tombstone — row never deleted on remote**: Trap: \`reconcile()\` old guard \`NOT EXISTS (any sync\_outbox entry for the row)\` blocks tombstone INSERT if ANY outbox entry exists — including a stale upsert that survived pruning because the prune floor was pinned by a lower-seq table (see #828). The stale upsert pushes as a no-op (row already gone), so the delete NEVER reaches the remote. Fix (PR #866): change guard to \`COALESCE((SELECT op ... ORDER BY seq DESC LIMIT 1), 'none') <> 'delete'\` — only blocks if latest op is already a delete (idempotent). Exhaustive harness confirmed 6 cases: no-entry→enqueue, stale-upsert→\[upsert,delete], latest=delete→unchanged, upsert+delete+upsert→+delete, delete+upsert+delete→unchanged, live-row+stale-upsert→no tombstone. \`seq\` is \`INTEGER PRIMARY KEY AUTOINCREMENT\` so \`ORDER BY seq DESC LIMIT 1\` reliably identifies latest op. Symmetric to seedOutbox fix (#861/#868).

<!-- lore:019ee7a3-1867-78c3-aaaa-46641cd927eb -->
* **sync-data.ts seedOutbox: trailing delete blocks re-sync of recreated rows — orphaned row bug**: Trap: old \`seedOutbox()\` WHERE clause \`NOT EXISTS (any pending outbox entry)\` skips rows with ANY pending entry, including a trailing \`delete\` or a STALE ALREADY-PUSHED upsert (seq ≤ push cursor). If a row is modified while sync is OFF, the stale upsert sits below the cursor; old latest-op guard sees it as 'already queued' and silently drops the edit — row never re-synced. Fix (PR #868): rewrite as JS loop; read \`latestUnpushed\` = latest op with \`seq > pushCursor\`; if upsert→skip; if delete→enqueue trailing upsert; if none→enqueue only when \`contentHash(row) != sync\_state.content\_hash\`. Confirmed discriminator: origin/main fails case (e) (stale upsert + content changed → no enqueue) and case (a) (unchanged row redundantly re-enqueued). Performance: wrap loop in \`withTransaction\` — N auto-commits cause ~23x slowdown. Hoist \`columns(table)\` outside per-row loop — \`syncedColumns\` runs PRAGMA on every call (no memoization).

<!-- lore:019ece19-d902-71c9-86d1-f51e74e8c6b9 -->
* **sync.ts drainTimestamp: keyset on idColumns\[0] only drops rows for composite-PK tables with >PAGE same-key entries**: Trap: \`drainTimestamp\` keysets by \`idc = idColumns\[0]\` (= \`knowledge\_id\`) for \`knowledge\_entity\_refs\`. If >200 refs share the same \`knowledge\_id\` + same \`updated\_at\`, rows beyond 200 are silently dropped — pagination terminates after 9 queries. Code comment at lines 322-324 claiming 're-scans the ms from the start … idempotent and loses nothing' is factually wrong. Empirically verified: single-group 250 refs (1 knowledge\_id, 1 ms) all applied correctly with composite keyset; multi-group 480 refs across 3 knowledge\_ids also correct. Fix: keyset on full composite \`(knowledge\_id, entity\_id)\` for composite-PK tables. \`\x1f\` (0x1f) sorts before any printable char — composite row\_id ordering is correct.

<!-- lore:019ef06b-99ca-7722-95a9-a165fc2f912f -->
* **sync.ts KnowledgePushPlan: \`skip\` op removed — tombstone is NEVER a content-match skip**: Trap: keeping a \`skip\` branch in \`pushEntry\` for knowledge looks safe (no-op). Fix: \`KnowledgePushPlan\` \`skip\` op removed (PR #913). Code comment invariant at \`sync.ts:468\`: 'a tombstone is NEVER a content-match skip.' A logical\_id with no live current row returns \`delete\` — \`.update(tombstone).match({id:logicalId})\` is an idempotent no-op if remote has no such row. \`RemoteClass\` 'skip' at \`sync.ts:499\` (\`classifyRemoteRow\`) is a DIFFERENT purpose and is retained. Only \`KnowledgePushPlan\` skip was removed.

<!-- lore:019ece19-d8e5-7e0c-9fbf-fb29368de83c -->
* **sync.ts pushEntry: epoch-ms integers rejected by Postgres timestamptz columns (22008)**: Trap: \`pushEntry\` sends epoch-ms integers into \`timestamptz\` columns. Real Postgres rejects with error code \`22008\`; \`classifyPushError\` maps this to 'transient' → cursor never advances → row retried forever. Fix: \`toRemoteRow()\` converts \`typeof out\[k] === 'number'\` → \`new Date(out\[k]).toISOString()\` for \`TS\_COLS = Set(\['created\_at','updated\_at'])\`. SECOND BUG: \`stripSyncCols\` uses \`Date.parse(v) || Date.now()\` — \`Date.parse\` of epoch-0 ISO string returns \`0\` (falsy) → replaced with \`Date.now()\`. Since \`created\_at\` is NOT in \`HASH\_EXCLUDE\`, this corrupts the content hash → row never settles as 'skip'. Fix: use \`Date.parse(v) !== 0 ? Date.parse(v) : 0\` (or explicit null check). Epoch-0 only occurs if caller explicitly inserts 0 (schema has \`NOT NULL\`, no default).

<!-- lore:019ed09f-854d-7b26-8002-40a13dc0b58b -->
* **sync.ts pushEntry: phantom columns on knowledge\_entity\_refs cause permanent push stall**: Trap: \`pushEntry\` (sync.ts) unconditionally adds \`content\_hash\`, \`revision\`, \`is\_deleted: false\` to ALL tables including \`knowledge\_entity\_refs\`. The join table has no \`content\_hash\`/\`revision\` columns on remote. PostgREST returns HTTP 400 PGRST204 for unknown columns. \`classifyPushError\` only special-cases errcode \`23514\`; PGRST204 falls through to 'transient' → returns 'stop' → push cursor for \`knowledge\_entity\_refs\` never advances → entire join table stuck pending forever. Fix: \`SyncTableMeta.versioned\` flag (defaults true); strip \`content\_hash\`/\`revision\` from upsert payload when \`versioned === false\`. \`knowledge\_entity\_refs\` is the only non-versioned table in \`SYNCED\_TABLES.basic\`.

<!-- lore:019ee762-dd6f-7a12-9dd0-1b365ed79e21 -->
* **sync.ts pushEntry: soft-delete tombstone retains content\_hash — cross-client deletes silently fail**: Trap: \`pushEntry\`'s soft-delete sends \`{is\_deleted: true}\` to remote but does NOT null out \`content\_hash\`. Tombstone retains original hash. \`classifyRemoteRow\` on the pulling client sees matching \`content\_hash\` → classifies as 'skip' instead of deletion → local row survives, delete never propagates. Fix (PR #856): in \`applyRemote()\` (sync.ts:411-414), force \`remoteHash = null\` whenever \`isDeleted\`, regardless of \`remote.content\_hash\`. Property test does NOT catch this bug in isolation at numRuns=60 — masked by #861 fix; deterministic regression at \`sync.test.ts:522\` is the reliable gate. 🔴 Invariant: a remote tombstone NEVER content-matches 'skip' — propagate the delete.

<!-- lore:019ece19-d8f2-7d7b-8b78-969756c830ae -->
* **sync.ts pushOnce: minCursor=0 from empty-table cursors blocks pruneOutbox forever**: Trap: \`pushOnce\` computes \`minCursor = Math.min(...per-table push cursors)\`. A synced table with zero outbox entries keeps cursor \`0\`. \`pruneOutbox(minCursor)\` early-returns when \`minCursor <= 0\` → outbox NEVER pruned whenever any one synced table has zero pending entries. Fix (implemented): compute \`minCursor\` only over tables where \`hasOutboxEntries()\` is true — empty tables excluded from floor calculation. Empirically verified: empty table does NOT pin floor; higher-cursor table with no peers fully pruned. Push-prune race is benign: rows inserted after cursors are read get \`seq > minCursor\` (AUTOINCREMENT monotonicity) → always survive prune.

<!-- lore:019ee0df-cba3-7fab-9beb-db13431cf1a4 -->
* **SYNCED\_TABLES fan-out: adding a table has silent ripple effects with no contract test**: Trap: adding a table to \`SYNCED\_TABLES\` (e.g. \`profiles\`) has ripple effects across \`seedOutbox\`, \`reconcile\`, \`pruneOutbox\`, \`applyRemote\` pull-classification, and outbox-capture triggers. No single contract test enforces all invariants for every registered table. \`profiles\` silently violated the prune-floor and tier-residue invariants on paths nobody re-checked after registration. Fix: add a per-table invariant test that asserts all registry-required properties (pullOnly guard in seed/reconcile, no outbox trigger, correct pull classification) whenever a new table is added to \`SYNCED\_TABLES\`.

<!-- lore:019eeac5-97f5-79de-ac27-16f1d9f2cd49 -->
* **syncedColumns must return synced set, never raw local column set — PGRST204 trap**: Trap: returning the raw local column set from \`syncedColumns()\` looks correct because it reflects the actual DB schema. Fix: local-only columns (e.g. \`knowledge.promoted\_at\`) are absent from the remote schema — pushing them causes PGRST204 (column not found). \`syncedColumns()\` MUST return \`syncColumns ∩ local\` (registry allowlist intersected with actual local columns), never the raw local set. Code comment at sync.ts:222 explicitly names \`promoted\_at\` as a local-only column that must NEVER be uploaded. The memoized result is \`Object.freeze()\`'d — direct \`.sort()\` or \`.push()\` throws \`TypeError\` in ESM strict mode. Safe caller patterns: \`for-of\`, \`.filter()\`, \`.filter().sort()\`. PR #878 memoizes via \`WeakMap\<object, Map\<string, readonly string\[]>>\` keyed on \`db()\` instance.

<!-- lore:019ef409-7953-7d95-b9b2-e0b3141af8de -->
* **ToC recall-by-id with shortId(8-char) prefix — recallById uses exact match, prefix always fails**: Trap: rendering \`\[${e.id.slice(0,8)}]\` in a ToC and instructing 'recall by id for detail' looks correct — shortId is the established display handle. Fix: \`recallById\` (recall.ts:1248) uses exact \`WHERE id = ?\` matching via \`ltm.get\`/\`getByLogical\`/\`logicalIdOf\` — zero prefix/LIKE fallback. \`runRecall\` short-circuits to \`recallById\` with no search fallback for \`k:\`/\`xk:\` paths. 8-char prefix \`k:019ef3fa\` → 'No entry found'; full UUID required. Prior truncated-ID surfaces (Superseded headers, distillation headers) were display-only — PR #930 is the first to pair a truncated ID with an explicit recall instruction, making it a real correctness bug. Fix: render full UUID in ToC, or add prefix resolution to \`recallById\`.

<!-- lore:019eea8f-acaa-76b0-8815-2bcec1be8af9 -->
* **traced.ts Proxy: assigning conn.query = spy after wrap has no effect — use registerSink/withDbSpan instead**: Trap: \`conn.query = spy\` after \`tracedDatabase()\` looks like it intercepts DB calls. Fix: \`traced.ts\` wraps DB in a Proxy with no \`set\` trap — assignment goes to underlying target, but Proxy \`get\` trap at line 70 always returns its own closure over \`originalQuery\` bound at wrap time, so spy is never invoked. Correct seam for SQL interception in tests: \`registerSink\` / \`withDbSpan\` from \`log.ts\` — \`PRAGMA table\_info\` executes via \`.all()\` which routes through \`traceDbQuery\`. Also: \`migrate()\` runs on the RAW connection before wrapping, so migration queries are never traced.

<!-- lore:019ef35a-01ee-7a3a-819a-4f98982c6a8d -->
* **VERIFIER\_LEADING regex: setup/config subcommands match verifier branches — pre-commit install, biome migrate, eslint --init are false positives**: Trap: broad tool-name branches in \`VERIFIER\_LEADING\` (e.g. \`pre-commit\`, \`biome\`, \`eslint\`, \`ruff\`, \`mypy\`, \`clippy-driver\`) look correct because those tools are used for verification. Fix: subcommands like \`install\`, \`autoupdate\`, \`uninstall\`, \`migrate\`, \`format --write\`, \`--init\`, \`--install-types\`, \`--version\` are setup/config operations, not verifiers. A false positive on the confidence-mutation path (\`sessionVerifierVerdict\`) incorrectly penalizes knowledge confidence. These are rated CONCERN (not BLOCKER) because they're uncommon in agent command sequences, but any expansion of tool-name branches must enumerate and exclude non-verifier subcommands explicitly.

<!-- lore:019ef389-a52f-7427-9099-f8dea38e8caf -->
* **vi.mock('@loreai/core') doesn't intercept pipeline imports — vitest alias resolution mismatch**: Trap: \`vi.mock('@loreai/core', ...)\` to spy on exports like \`recordCacheUsage\` looks correct — it's the package specifier pipeline uses. Fix: vitest config aliases \`@loreai/core\` → \`packages/core/src\` at \`resolve.alias\` (top-level, NOT \`test.resolve.alias\` — wrong placement silently resolves to stale dist). The mock targets the specifier but pipeline resolves to the source path, creating a module identity mismatch — spy is never invoked (call count stays 0). No clean workaround for aliased barrel mocks; use e2e harness tests or file as tracked follow-up instead.

### Pattern

<!-- lore:019ee6ae-7ceb-7aae-a9c1-5eb174493399 -->
* **embedding-worker.ts: key implementation invariants from code comments**: Invariants asserted in code comments: (1) CJS uses \`\_\_filename\` (always defined); ESM uses \`import.meta.url\` — \`import.meta.url\` branch is dead code in CJS at runtime. (2) \`purgeCachedModel()\` is npm-mode only — vendored binaries ship read-only model, must not be deleted. (3) \`truncateTexts()\` skips texts ≤ maxTokens chars as coarse pre-filter — texts shorter than maxTokens chars 'can never exceed the limit'. (4) \`isCorruptModelError()\` must exclude transient download/auth/network failures — false positive causes purge→redownload→fail→purge loop that never converges and hammers HuggingFace Hub.

<!-- lore:019ef39c-72e1-77c0-b39c-2c145ca5fd05 -->
* **knowledge\_meta invariants: sync-silence for markInjected, content-only update must not churn sync clock**: Three sync-silence invariants for \`knowledge\_meta\`: (1) \`markInjected()\` updates \`last\_reinforced\_at\` but NEVER bumps \`updated\_at\` — selection ≠ certainty, mirrors prior HASH\_EXCLUDE behavior. (2) Content-only \`update()\` (no \`input.confidence\`) must NOT bump \`meta.updated\_at\` — only confidence changes churn the sync clock. (3) \`create()\` calls \`insertMeta(id, confidence, now, now)\` with \`ON CONFLICT DO UPDATE\` — idempotent for cross-machine re-create but never destroys a converged confidence value (fresh UUIDv7 logical\_id guarantees no collision in practice). A \`knowledge\_meta\` row with no live \`knowledge\_current\` row is inert — never crashes a read.

<!-- lore:019eec48-01c5-78bd-b7f9-6cd8b7ddfada -->
* **Lore gateway deploy procedure on production host**: 🔴 Deploy procedure (Burak Yigit Kaya directive): \`git stash save && git pull && git stash pop && pnpm install && pnpm build && pnpm --filter @loreai/gateway run bundle && sudo service opencode restart && journalctl -u opencode -f\`. Plugin file at \`~/.config/opencode/plugins/lore.ts\` contains only \`export { LorePlugin as default } from "opencode-lore";\`. opencode-lore resolves from \`/home/byk/.config/opencode/node\_modules/opencode-lore\` (active built repo) — NOT the cached version at \`~/.cache/opencode/packages/opencode-lore@\*/\`.

<!-- lore:019ef3d7-3db5-7dbb-88f2-210e258d1334 -->
* **Lore workers never use structured outputs — output\_config never set in worker calls**: Lore's internal worker calls (distillation, curation, query-expansion) always use plain system+user→text prompts. \`llm-adapter.ts\` never sets \`output\_config\` or \`tools\` for worker requests. \`cache-warmer.ts:878\` and \`:983\` defensively strip \`output\_config\` from warmup request bodies (clients may send it in conversation passthrough, but workers never do). Structured-outputs gap on bedrock-mantle only affects conversation-passthrough clients using the Anthropic structured-outputs beta — identified as a niche edge case, not a blocker for lore's core use.

<!-- lore:019eec2f-3f3b-724a-bb98-d2dcede676d1 -->
* **ltm.ts appendVersion: DEMOTE-FIRST transaction ordering enforced by UNIQUE index**: 🔴 Invariant (code comment, ltm.ts:233): 'Never leave two is\_current=1 rows for one logical\_id.' Enforced by UNIQUE index \`idx\_knowledge\_one\_current\` on (logical\_id WHERE is\_current=1). \`appendVersion()\` transaction uses DEMOTE-FIRST ordering: UPDATE is\_current=0 on old row BEFORE INSERT of new row with is\_current=1. Rationale: UNIQUE index is checked per-statement — inserting second current row before demoting would violate it. \`update()\` idempotency guard: \`appended\` flag set only when \`cur.content !== input.content\`; \`appendVersion()\` skipped for byte-identical content; re-embed gated on \`appended\` flag — prevents unbounded version growth. Migration/sync setup queries are 'never traced / re-entrant' — Proxy wrapping of DB connection happens AFTER \`migrate()\` and \`installSyncCapture()\` (db.ts lines 1472–1480). \`migrate()\` always normalizes version to \`MIGRATIONS.length\` after loop.

<!-- lore:019eebaf-0f5d-7d7b-a0c9-c8b93775c4d0 -->
* **pipeline.ts urgent distillation: skipMeta invariant and bypass rationale**: 🔴 Code comment invariant (pipeline.ts:4457): 'Never run meta-distillation while the conversation cache is warm.' Urgent distillation always sets \`skipMeta: true\`. Idle-time meta (idle.ts) remains enabled because cache is already cold there. Urgent distillation bypasses \`isBackgroundPaused()\`, quota checks, and worker throttle checks — rationale: degraded/overflowing context for up to 10 min (max breaker duration) is worse than one API call. It is the ONLY direct (non-limiter) background DB writer; all other writers (incremental distillation, curation, idle work) go through \`runBackground()\` / background limiter.

<!-- lore:019eec5f-e2f9-74cc-ba57-ec86c0090206 -->
* **recallById k:/xk: prefix: dual-lookup for append-only logical\_id resolution**: \`recallById()\` \`k\`/\`xk\` case (recall.ts:1248): \`ltm.get(rawId) ?? ltm.getByLogical(ltm.logicalIdOf(rawId))\`. Root cause: \`knowledge\_current\` view only exposes \`is\_current=1\` rows — a recalled \`logical\_id\` whose current version has a different row \`id\` returns null from \`get()\`. \`logicalIdOf()\` queries base \`knowledge\` table (not view) so it resolves superseded version ids; falls back to input \`id\` if unknown (intentional — 'so ref writers always have a value'). Same fallback pattern used in curator.ts \[\[019eec2f-3f15-72f9-ae1c-59dc8aee24c6]] and api.ts. Regression test required: create entry, update it (logical\_id diverges from current version id), confirm \`recallById('k:'+logical\_id)\` returns entry.

<!-- lore:019ef387-4afe-7137-a151-88dff1dd0ab7 -->
* **Test fix rules for knowledge\_meta migration (RULE A and RULE B)**: Two patterns for fixing tests after the v55 knowledge\_meta migration: RULE A — remove hardcoded \`confidence\` column from raw \`INSERT INTO knowledge\` statements; add \`logical\_id\` column+value (= id for v1 rows). RULE B — split combined \`UPDATE knowledge SET cross\_project=1, confidence=X, last\_reinforced\_at=Y\` into two statements: one targeting \`knowledge\` (cross\_project), one targeting \`knowledge\_meta\` (confidence/last\_reinforced\_at + updated\_at). Also: any test asserting confidence must read from \`knowledge\_meta\` or \`knowledge\_current\` view, never from the base \`knowledge\` table. Helper pattern: \`confOf(id)\` queries \`knowledge\_meta WHERE logical\_id = ?\` (not \`knowledge WHERE id = ?\`). Test JOIN pattern: \`SELECT m.confidence FROM knowledge k JOIN knowledge\_meta m ON m.logical\_id = k.logical\_id WHERE k.id = ?\`.

<!-- lore:019ee5b8-6fed-7dcb-ab57-53e548838852 -->
* **vitest.config.ts: alias must be at top-level resolve.alias, NOT test.resolve.alias**: In \`vitest.config.ts\`, \`@loreai/core\` → \`packages/core/src\` and \`@loreai/gateway\` → \`packages/gateway/src\` aliases MUST be placed at top-level \`resolve.alias\`, NOT \`test.resolve.alias\`. Placing them under \`test.resolve.alias\` silently resolves to the stale dist build instead of src — tests pass against old compiled output, masking source changes. Comment in config notes this explicitly.

### Preference

<!-- lore:019eec5c-fa9c-7993-ad0a-f44f68400fde -->
* **Adversarial review heuristics include vacuity check — tests that don't fail on revert are vacuous**: When performing adversarial review \[\[019eec15-9385-79f3-b2a5-fc3928ccd1b7]], always include a vacuity check: temporarily revert the code fix the test claims to guard, run the test, confirm it FAILS. A test that passes against known-broken code is a non-test. Worktree technique: create detached-HEAD worktree at \`/tmp/pr-mut\`, symlink BOTH main repo's \`node\_modules\` AND per-package \`node\_modules\` (core, gateway, opencode, pi) — pnpm nested symlinks miss transitive deps (e.g. \`uuidv7\`) if only root is linked. Run vitest from worktree root. Apply mutations via \`sed\`/direct edit, revert via \`git checkout\`. Always take SHA-256 baseline checksums before mutations; restore byte-identical after all mutations. Always verify worktree is clean after all mutations reverted. PR #906: Mutation A (verdict param) killed; Mutation B (verdict IN filter) survived — genuine behavioral gap, not equivalent mutant.

<!-- lore:019ee4d4-bdd2-7641-b523-34aa917fdb48 -->
* **Always document directives with 🔴 markers and preserve them across sessions via written plans**: The user consistently marks critical invariants with 🔴 (red circle) to distinguish hard requirements from observations (🟡). These cover correctness invariants (e.g., per-bucket isolation, neutral non-2xx handling), role context, and repo paths. The user expects these captured in persistent written artifacts (e.g., \`.opencode/plans/\`) so they survive across sessions. When reviewing code, the user acts as a skeptical adversarial staff engineer and expects proactive surfacing of invariant violations. Always treat 🔴-marked statements as non-negotiable constraints. Key recurring 🔴 invariants: (1) non-2xx warmup result must be NEUTRAL — never increments/trips breaker; (2) per-bucket isolation — one bad bucket must never disable warming for other sessions; (3) legitimately cold re-warm (\`cacheLikelyAlive===false\`) must NOT count as failure.

<!-- lore:019ece05-3058-7c4b-afd7-f668085684cd -->
* **Always enforce tool\_use/tool\_result pair safety as a hard invariant with code comments**: tool\_use/tool\_result pair safety is a hard invariant. safeDeltaInsertIndex is the primary guard: walk back whenever messages\[idx-1] is assistant(tool\_use), regardless of what follows (covers completed pairs AND pending tail). Loop: \`while (idx > 0)\` (no upper bound). removeOrphanedToolResults is last-ditch backstop that 'should essentially never fire.' Always cover both completed pairs AND pending/in-flight dangling tool\_use at tail in tests. Never rely on destructive stripping as primary safety — insertion point must be computed correctly upfront. Invariant documented in code comments referencing bug #747.

<!-- lore:019edc8f-7bdd-73a0-a374-a700828d04be -->
* **Always exclude .lore.md from PR diffs by restoring origin/main version before branching**: When creating a new branch for a PR, always restore \`.lore.md\` to \`origin/main\` version before branching (\`git checkout origin/main -- .lore.md\`) to prevent \`.lore.md\` chore commits from polluting the PR diff. If \`.lore.md\` accidentally appears in a diff or commit, rebuild the branch clean (soft-reset, unstage \`.lore.md\`, recommit). Even if \`.lore.md\` was modified during the session (including by lint:fix auto-formatting), it must be reset before staging. Exception: when branch is created off \`origin/main\` directly and \`git diff origin/main -- .lore.md\` shows no changes — confirmed safe.

<!-- lore:019ef072-fae7-7a12-8c89-271e28dbca5d -->
* **Always include .lore.md in commits rather than excluding it from PRs**: 🔴 Directive (June 22 2026): \`.lore.md\` MUST be included in commits and PRs — never excluded. Workflow: commit accumulated \`.lore.md\` changes as a final \`chore(lore): update .lore.md knowledge\` commit on the branch. If the background lore process modifies \`.lore.md\` after the commit, amend the chore commit and force-push with lease (\`git push --force-with-lease\`). Force-push re-triggers CI, which is acceptable. Note: observed in PR #920 session that \`.lore.md\` was excluded from staging — this violates the directive.

<!-- lore:019ee535-02e6-7648-82b7-d6c68de1256a -->
* **Always provide file:line inventory when researching codebase impact**: When asked to research how a change would affect the codebase, the user expects an exhaustive file:line inventory covering every affected site. This is a recurring directive across 42+ sessions. Format: \`filename:line — description\`. Cover all SQL patterns, API surfaces, test assertions, and schema version checks. Research-only phases must not change code.

<!-- lore:019eec3f-d82f-7f0b-bb46-fd2a6dd28a4c -->
* **Always run progressively broader test suites before committing**: 🔴 Directive (stated June 21 2026): always run progressively broader test suites before committing. Start with the narrowest relevant test scope (single file/module), then widen to package-level, then full suite. Never commit after running only a single test in isolation. Also: run 3 consecutive test runs to check for flakiness before declaring a test suite clean. Mutation testing: create detached-HEAD worktree at \`/tmp/pr-mut\`, symlink main repo's \`node\_modules\`, run vitest from worktree root. Scope patch to specific files only (not broad \`git diff\`) when branch is ahead of origin/main — broad diff captures committed changes from ahead-commits, causing patch failures on unrelated files.

<!-- lore:019ef371-d1ab-791d-b04a-22f50f4e9013 -->
* **Always scope fix tasks strictly to test fixtures, never touching src files**: When the user assigns a task to fix failing tests after a schema/API migration, they explicitly constrain the work to \`packages/\*/test/\` directories only, with an explicit prohibition on modifying \`packages/\*/src/\` files. The user expects the assistant to identify all broken raw SQL or fixture code in test files and update only those, leaving production source code untouched. This pattern applies whenever a schema change has already been implemented in src and the remaining work is purely test-side adaptation.

<!-- lore:019edd39-903b-7f73-b186-daa7415b4dca -->
* **Always treat token budget as a hard cap, never a soft limit**: When implementing LTM/context packing logic, the token budget is an absolute hard cap that must never be exceeded. Applies to \`forSession()\` packing loops and any injection path. The user flags violations as HIGH-PRIORITY assertions. Comment \`// hard cap: never exceed total budget\` at ltm.ts:1070 is an intentional design assertion. Distinguish: token budget (hard cap protecting context window) vs entry count caps (control curator LLM input cost). Prefer eviction-based soft caps over hard count gates for entry management, but never relax the token budget constraint. \`forSession()\` enforces hard token budget (\`maxTokens\`) — count cap does NOT protect context window; count cap's real purpose is bounding curator LLM input cost and \`.lore.md\` size.

<!-- lore:019ee640-0ad2-7a98-87f8-111898a4a84d -->
* **ast-grep: always use stopBy: end for relational rules**: Always use \`stopBy: end\` for relational rules (\`inside\`, \`has\`) in ast-grep to ensure complete traversal. Stated as a firm rule unless there is a specific reason not to. ast-grep skill at \`/home/byk/.claude/skills/ast-grep\`. Key principles: start simple (pattern → kind → relational → composite); use \`--debug-query=cst\` to inspect AST; escape metavariables in inline rules with \`\\$VAR\`.

<!-- lore:019ee673-63cb-7a6d-9b3b-844fdab3eaff -->
* **Connection timeout: 10s is generous enough — do not increase; capture aborts instead of raw timeouts**: Burak Yigit Kaya considers 10-second connection timeout already generous and does not want it increased. Raw connect timeouts are not considered actionable for Sentry capture. Prefer capturing abort events with loop-lag/RSS context instead.

<!-- lore:019ef0e4-01d8-7f82-ad80-88a084d3ece8 -->
* **CONSOLIDATION\_MERGE\_SYSTEM invariants: no force-delete, no invented knowledge, opposing rules never merged**: 🔴 Directives from \`CONSOLIDATION\_MERGE\_SYSTEM\` (prompt.ts lines 619-631): (1) Opposing rules (e.g. 'always use tabs' vs 'always use spaces') are NEVER duplicates — never merge them. (2) Never force-deletes distinct entries — no numeric eviction target; returning empty array is acceptable. (3) Never invent new knowledge — only 'update' survivors and 'delete' true duplicates. (4) 'update' op: replace content with a concise rewrite under 150 words; use to merge survivors or trim verbose entries.

<!-- lore:019ee6ae-7cf6-7fdb-8959-36949bffa201 -->
* **Embedding OOM adaptive cap: decrease factor ≥ 0.7, no additive increase**: Burak Yigit Kaya chose decrease factor ≥ 0.7 for embedding OOM adaptive cap — gentler than 0.5 (which quarters attention memory per step). No additive increase — upward adaptation handled by memory-aware initializer re-evaluating freemem on each respawn. 0.8 noted as a tuning option. Pre-truncation must use free-memory-adjusted value (e.g., \`adaptiveLocalMaxTokens()\` derived from \`os.freemem()\` + O(L²) attention model) — hard-coded token limits like \`LOCAL\_MAX\_CHARS = 16384\` explicitly rejected. Coverage stats in \`runStartupBackfill()\` must always log to stderr so the problem is visible. These are locked decisions for \`fix/embedding-oom-adaptive-cap\`.

<!-- lore:019ef06b-99f1-7804-998a-fcb9d6a84d71 -->
* **File deferred sub-PR reasoning as GitHub issues — never let it get lost**: 🔴 Directive (June 22 2026): when deferring a sub-PR or architectural decision, always file a GitHub issue capturing the full reasoning so it is not forgotten. Exact quote: 'don't forget our reasoning.' Applied to sub-PR 4 → issue #909. This is distinct from the general 'store artifacts in version-controlled locations' rule — it specifically covers deferred design decisions that have no immediate code artifact.

<!-- lore:019eec5c-fa90-76ae-b698-8e00ce35790e -->
* **Mutation-test every test against its fix — red-phase must fail before green-phase passes**: Extension of TDD red-green discipline \[\[019eec55-4bd0-779b-b0f6-b3f4f9090790]]: for any test written to guard a specific fix, confirm the test FAILS when the fix is reverted (a manual mutation test). A test that passes with the fix applied AND with the fix reverted is vacuous — it does not guard the fix. This is especially important for refactoring tests that assert correctness of internal transformations (e.g., compression scale normalization, cache alignment). Verify by temporarily reverting the fix in a /tmp copy and running the test — it must fail.

<!-- lore:019ef4b8-cf17-700c-ab09-cc422f8505c3 -->
* **Never delete local branches without verifying merge status first**: Before deleting any local branch, verify it is fully merged into origin/main. Squash-merged branches won't be detected by \`git branch -d\` (requires \`-D\`), but that doesn't mean they're safe to delete — check if the branch has unmerged commits via \`git log origin/main..branch\` or \`git cherry -v origin/main branch\`. Worktree-held branches cannot be deleted at all. Report ambiguous local-only branches (no upstream tracking, not clearly merged) to the user rather than deleting them unilaterally.

<!-- lore:019edcd1-12e0-7981-aec5-0c11b3ba5154 -->
* **Never fall back to cfg.model for worker model in LLM calls**: When passing a model to LLM calls in curator/consolidation code, always use the explicit worker model from input (e.g., \`input.model\` or \`input.workerModel\`). Never fall back to \`cfg.model\` — it's the project/session model and may belong to a different provider than the worker's upstream URL, causing cross-provider model name mismatches (404 errors). Code comment at curator.ts:1301-1302 documents this as an invariant. \`runConsolidationPrompt\` enforces this: explicit worker model passed, never substituted. If model is undefined, let the gateway's cross-provider guard handle validation or skip — do not silently substitute \`cfg.model\`.

<!-- lore:019eec93-1fe9-7168-95fe-5b4c3e9747f8 -->
* **Never fund a second-or-later cycle for a session that has produced zero confirmed hits over its lifetime**: User asserted NEVER fund a second-or-later cycle for a session that has produced zero confirmed hits over its lifetime.

<!-- lore:019ef029-6ac3-7605-bc76-fc627dc3f50a -->
* **Never publish nightlies to npm — GHCR only**: 🔴 Directive (June 22 2026): nightlies are NEVER published to npm. Onur Temizkan uses \`lore upgrade nightly\` which pulls from GHCR (\`ghcr.io/byk/loreai:nightly\`). npm registry contains only stable releases (e.g. \`@loreai/gateway\`, \`@loreai/core\`, \`opencode-lore\`). Nightly version string is computed deterministically from commit timestamp. Publish pipeline: \`Publish Nightly to GHCR\` job in \`publish.yml\`; tagged as both \`nightly\` and \`nightly-\<version>\`.

<!-- lore:019eec27-f013-77ca-b75b-1a48ab751200 -->
* **Never trust a fix without seeing the test fail first**: 🔴 Directive: never trust a fix without seeing it fail first. When fixing a bug, always confirm the test catches the bug (red phase) before applying the fix (green phase). TDD red-green-refactor discipline: if you haven't seen the test fail, you don't know if the test actually guards against the regression. Related: 'If this test still passes with the guard deleted, it isn't testing the guard' \[\[019eec15-f0a4-769d-a1b1-bf87cac5c1e5]] and investigate test-coverage gaps \[\[019ee645-ff9e-77a1-ada6-7dc33c77f498]]. Also: always run progressively broader test suites before committing \[\[019eec55-4b73-7aa2-982e-5eecaab59339]]. Mutation test technique: create detached-HEAD worktree at \`/tmp/pr-mut\`, symlink main repo's \`node\_modules\`, run vitest from worktree root.

<!-- lore:019ed058-ae55-7a9f-b6ce-c79717e686af -->
* **Never use Supabase for temporal/high-volume data — treat it as row-level sync only for small structured data**: Hard rule: Supabase (PostgREST row-level) is only appropriate for small structured data AND linked temporal rows (distillation-referenced subset). NEVER use for arbitrary temporal bulk or GB-scale data. Blob snapshots rejected for multi-client: divergent WAL chains, no row-level merge path. Turso rejected for basic/pro tiers: whole-DB replication only, no table/row-level selective sync. Turso deferred to hosted-gateway decision only. Framing Supabase as a live query store for unreferenced temporal data is 'the GB-scale Option-C rewrite trap' and will be immediately rejected. SQLite WAL must NEVER run on network FS (EFS/NFS) — local volume only.

<!-- lore:019ef387-4b17-77d7-a76d-062269f0214b -->
* **Never weaken assertions or change expected values to make a test pass**: 🔴 Directive (seen 23+ sessions): never weaken/delete an assertion or change an expected value to make a test pass. When a test fails due to a schema change, fix the test setup (INSERT helpers, raw SQL, helper functions) to match the new schema — never adjust the assertion target. Example: integrity.test.ts duplicate detection fixed by adding knowledge\_meta rows to raw INSERTs, not by changing \`duplicates.length >= 1\` assertion.

<!-- lore:019ee565-89ae-7a43-925f-f341a156aa2d -->
* **Option A (single append-only table) preferred over Option B (CQRS/dual-table) for knowledge schema**: User explicitly rejected Option B (CQRS/dual-table with separate history table) in favor of Option A (single append-only \`knowledge\` table with \`is\_current\`/\`logical\_id+version\`). Rationale: both options have identical team-conflict capability; A is long-term simpler (one table, no materialization layer to drift). User is willing to pay the one-time ~60 read-site rewrite + FTS rebuild cost upfront due to small user base. Never re-propose Option B as a 'simpler migration path' — it was explicitly considered and rejected.

<!-- lore:019ef029-6ad7-7dd9-a310-cba40d1e468f -->
* **Ping Onur Temizkan over WhatsApp, not GitHub**: 🔴 Directive (June 22 2026): when notifying Onur Temizkan about releases or issues, ping him over WhatsApp via Beeper (chatID 556724, local-whatsapp\_ba\_aM0zYm0IF3-P1LLS6cjW6YsPQSA) — NOT via GitHub comments or issues. Always verify CI is green before pinging. Use Burak Yigit Kaya's actual WhatsApp tone: Turkish, lowercase, no punctuation, no markdown, terse, casual, minimal emoji (reactions only). Example: 'abi nightly cikti, lore upgrade nightly yap'. Never write formal English with markdown — that tone is wrong for WhatsApp.

<!-- lore:019ef48a-ac2c-7800-a9dd-a771194a323b -->
* **Prefers explicit over implicit**: prefer explicit over implicit.

<!-- lore:019ef10f-53c0-7494-9a6c-bef24e943dcb -->
* **Prefers UPDATES over CREATES (352-359)**: PREFER UPDATES OVER CREATES (352-359),

<!-- lore:019eec4d-5e2b-7f16-8521-3ef10cc17d2d -->
* **Protected content must never be stripped during context management**: 🔴 Directive (observed across 34+ sessions): content marked 'protected' must NEVER be stripped during compaction or context window management. Absolute invariant — no exceptions. Confirmed in architecture.md: 'The last 2 turns are always protected from stripping.' When implementing or modifying context compression, compaction, or eviction logic, always verify protected content survives all code paths. A stripping path that can remove protected content is a bug.

<!-- lore:019ebe56-3cfa-7b49-b786-3d603e608a37 -->
* **Test file conventions: Vitest, kebab-case, import style**: Always write new tests with \`import { describe, it, expect } from 'vitest'\`. Use kebab-case file naming for test files. Prefer writing more tests over lowering coverage thresholds or excluding files. Project migrated from Mocha+Chai+ts-node to Vitest (May 2026; 312ms vs 30s startup). Run tests from repo root, not package subdirectory — \`vitest\` with a filter from a subdirectory returns 'No test files found'.
