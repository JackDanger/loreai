<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019eaee5-6de2-7abb-ac6a-4bcd02ad862d -->
* **craft NpmTarget workspace expansion and OIDC auth strategy**: craft NpmTarget workspace expansion, OIDC auth, and publish flow: \`NpmTarget.expand()\` discovers workspaces via pnpm-workspace.yaml or package.json, topologically sorts (deps before dependents), validates public packages don't depend on private ones. Auth priority: \`oidc=true\` config → force OIDC (does NOT check \`isOidcEnvironment()\` — env vars only checked in auto-detect path); \`NPM\_TOKEN\` set → token auth; no token + OIDC env detected → auto OIDC; else error. OIDC env: \`ACTIONS\_ID\_TOKEN\_REQUEST\_URL\` + \`ACTIONS\_ID\_TOKEN\_REQUEST\_TOKEN\` (GitHub Actions) OR \`NPM\_ID\_TOKEN\` (GitLab). \`publishPackage()\`: path arg ALWAYS pushed last. \`getPublishTag()\`: pre-release → 'next'; new version < current latest → 'old'; else undefined. \`publishMain()\` sequence: artifact download → workspace expand → state resume → publish loop (state written after each target) → merge release branch (non-fatal) → post-release command. State file: \`getPublishStatePath(version)\` uses \`XDG\_STATE\_HOME\`. Legacy cwd state file no longer read (PR #797).

<!-- lore:019ea9c2-23e7-78a3-b559-416743674bc7 -->
* **Docs-site structure: Zod .describe() + generator + check-docs CI job**: Docs-site auto-generation: two pages in \`packages/website/src/content/docs/docs/\`. \`generate-config-docs.ts\` walks Zod \`.shape\`+\`.description\`+\`unwrap()\` directly (NOT zod-to-json-schema — fails on ZodEffects). \`generate-env-docs.ts\` walks gateway/core source for \`process.env.LORE\_\*\`/\`env.LORE\_\*\` refs, extracts JSDoc, parses defaults, groups by subsystem. Both wired via \`pnpm generate:docs\` + \`pnpm check:docs\` (--check exits 1 on drift). CI \`check-docs\` job gates on both. GOTCHA: backticks inside template literals MUST be \`\\\`\` — unescaped backticks cause esbuild parse errors. Zod 4 internals: \`s.description\` is top-level (NOT \`s.\_def.description\`). Default wrapper: \`\_def.type === 'default'\`, \`\_def.defaultValue\` is the value itself. Numeric checks: \`check.\_zod.def.check\`, \`.value\`, \`.inclusive\`. Object: \`\_def.shape\` is direct. Array: \`{ type: 'array', element }\`. Record: \`{ type: 'record', keyType, valueType }\`.

<!-- lore:019e7013-a9ea-72e6-8e8c-17ef1848e4e0 -->
* **llm-adapter.ts: retry logic, backoff schedule, and circuit breaker integration**: llm-adapter.ts retry/backoff: \`TRANSIENT\_CODES={429,500,502,503,529}\`; \`AUTH\_ERROR\_CODES={401,403}\`. \`MAX\_RETRIES\_RATE\_LIMIT=3\`; \`MAX\_RETRIES\_SERVER=3\`; \`MAX\_RETRIES\_URGENT=2\`. \`backoffMs()\`: Retry-After present → \`min(retryAfterMs, cap)\`; urgent → \`min(1000×2^attempt, 4000)\`; 429 non-urgent → \`min(60\_000+attempt×60\_000, 180\_000)\`; 5xx → \`min(1000×2^attempt, 8000)\`. On 429 non-urgent → \`tripCircuitBreaker(pauseSec)\`. Auth errors → \`markAuthStale\`, re-resolve, retry once if \`credentialChanged && attempt===0\`. \`buildAnthropicWorkerRequest()\`: injects billing block only when \`cred.scheme === 'bearer'\`; system sent as block array with \`cache\_control: { type: 'ephemeral', ttl: '1h' }\`; body signed via \`signBody()\`. \`opts.thinking\` intentionally NOT forwarded in bare API fetch. Sentry polyfill: \`util.getSystemErrorMap = () => new Map()\` BEFORE \`Sentry.init()\` in \`packages/gateway/instrument.ts\` (Bun's node:util lacks it).

<!-- lore:019e5715-cccd-7be2-a979-407066c526d4 -->
* **log.ts: sink bridge, file rotation, and error visibility rules**: \`packages/core/src/log.ts\`: (1) \`log.error()\` always calls \`console.error()\` — always visible, indicates real failures. \`log.info()\`/\`log.warn()\` only print if \`LORE\_DEBUG=1\`. (2) \`sink?.captureException(err)\` called when \`Error\` instance found via \`findError()\` — gateway bridges to Sentry via \`registerSink()\`. (3) File logging: all levels written to \`~/.local/share/lore/lore.log\` regardless of \`LORE\_DEBUG\`; rotated at 5 MB (\`LOG\_MAX\_BYTES\`), check every 1000 writes (\`ROTATION\_CHECK\_INTERVAL\`), single \`.log.1\` backup; disabled in \`NODE\_ENV=test\`. (4) Only one \`LogSink\` supported. TRAP: any \`log.error(..., errorObj)\` auto-forwards to Sentry — including expected shutdown/auth errors. Use typed error subclasses or \`TRANSIENT\_ERROR\_PATTERNS\` filter in instrument.ts \`beforeSend\`. \`log.warn()\` auth errors from batch-queue are never forwarded to Sentry (warn level not captured by sink).

<!-- lore:019e54f5-04be-7a9f-9e62-177d65d5db9f -->
* **LTM forSession() budget, phases, and safety-net constants**: LTM forSession() architecture: \`forSession()\` (ltm.ts) budgets: \`budget.ltm\`=0.05, \`budget.preferenceLtm\`=0.02. Phases: (1) architecture entries up to ARCH\_BUDGET\_FRACTION=0.2; (2) remaining by score desc; (3) .lore.md sections. HEADER\_OVERHEAD\_TOKENS=15 subtracted upfront. Hard cap: \`if (used+cost>maxTokens) continue\`. PROJECT\_SAFETY\_NET=5: top-confidence project entries ALWAYS included even with zero relevance score. NO\_CONTEXT\_FALLBACK\_CAP=10. Global entries (pid=null) must be cross\_project=1. Pool SQL: project pool uses \`cross\_project=0\`; cross pool uses \`project\_id IS NULL OR cross\_project=1\`. Promoted entries (cross\_project=1) lose PROJECT\_SAFETY\_NET guarantee in home project. GOTCHA: \`vectorSearch()\` queries \`knowledge WHERE embedding IS NOT NULL AND confidence>0.2\` — test suites must \`DELETE FROM knowledge WHERE embedding IS NOT NULL\` in beforeEach. Dedup threshold for Nomic v1.5 is 0.935.

<!-- lore:1cdad2a4-5637-471b-a5cb-39c919fb1592 -->
* **Multilingual support: FTS tokenizer, Unicode tokenization, and non-English fallbacks**: Lore supports non-English (e.g. Turkish) conversations. Key design: (1) FTS5 uses \`tokenize='unicode61 remove\_diacritics 0'\` (NOT \`porter\` — English-only stemmer; NOT diacritic-folding — Turkish ç/ğ/ı/ö/ş/ü are distinct letters). Migration v32 rebuilds all 6 FTS tables. (2) \`filterTerms()\`/\`extractTopTerms()\` in \`search.ts\` strip via \`/\[^\p{L}\p{N}\_\s]/gu\` (Unicode-aware) so Turkish words don't split at special letters. Underscore kept for snake\_case. (3) \`pattern-extract.ts\` regexes need NO translation — they run on the LLM observer's \*English-normalized\* output. (4) Two raw-user-text regex sites have a structural fallback: \`hasNonAsciiLetters()\` (exported from \`instruction-detect.ts\`) gates fallbacks in \`extractInstructionCandidates\` (emit whole message as candidate) and \`detectAssertions\` (pin first sentence) — requires ≥3 non-ASCII letters to avoid false positives on English loanwords (café, naïve). (5) \`ltm.rerankPreferences()\` no longer demotes entries lacking English directive words to 0.8. KNOWN LIMITATIONS (deferred): English \`STOPWORDS\` not filtered for other languages (vector+RRF compensates); ~3-4 chars/token estimate is English-tuned (fine for Turkish, off for CJK).

<!-- lore:019e16fc-4410-7424-8a8a-712db1934a44 -->
* **opencode cumulative branch tracks upstream dev with custom patches**: opencode cumulative branch rebases on \`origin/dev\`. Custom patches: (1) plan mode markdown display with collapsible modal in \`plan\_exit\` tool, (2) collapsible question modal/dock (\`origin/collapse-questions\`, not yet merged to dev), (3) unarchive stickiness fix, (4) plan\_exit/plan\_enter deny rules for subagents in task.ts tools map. Upstream renamed \`session/index.ts\` → \`session/session.ts\`. Upstream removed \`cleanupSessionCaches\` from \`session.updated\` (only \`session.deleted\` remains). During rebase, task.ts conflicts: keep upstream's \`deriveSubagentSessionPermission\`, add plan\_exit/plan\_enter inline permission checks in tools map region. Session-level deny alone is insufficient — must also deny in tools map passed to \`SessionPrompt.prompt()\`.

<!-- lore:019e5922-4280-76aa-9b79-5ebeae433d7a -->
* **Sub-agent session detection, isolation, and differential treatment**: Sub-agent session detection and isolation: detected via \`x-parent-session-id\` header (pipeline.ts:2773-2809). Each sub-agent gets independent session: temporal storage, gradient state, distillation pipeline, LTM injection, cost tracking. Key differential treatment: (1) cache warming ALWAYS skipped — \`shouldWarm()\` returns false unconditionally; (2) \`findRotationPredecessor()\` skips sub-agents from Tier 1b rotation; (3) idle distillation/curation runs normally. \`isSubagent\`/\`parentSessionId\` persisted to DB (migration v26). Dashboard: collapsible cost-rollup tree via \`loadParentChildMap()\` + \`buildLiveSessionRows()\`; \`rollUp()\` propagates grandchildren costs bottom-up. \*\*OpenCode plugin gap\*\*: \`chat.headers\` hook injects \`x-lore-session-id\` and \`x-lore-agent\` but does NOT set \`x-parent-session-id\` — gateway can't distinguish parent from sub-agent.

<!-- lore:019e4bbd-ade4-7355-8fb1-87df54c8ea69 -->
* **X-Lore-\* headers: project path injection and resolution priority**: X-Lore-\* headers and project path resolution: Priority in \`packages/gateway/src/config.ts\`: 1) \`X-Lore-Project\` header (absolute path, max 1024 chars, strips trailing slashes/control chars), 2) inferred from system prompt (\`cwd\` JSON key, \`Working directory:\` prefix, \`.lore.md\` path, generic \`/home/\`/\`/Users/\` paths), 3) \`process.cwd()\` fallback. Source tagged as \`ProjectPathSource\`: \`"header"|"inferred"|"cwd"\`. Pipeline warns on \`cwd\` source (dedup via \`cwdWarned\` Set). \`agents.ts\` \`envVars()\` injects \`X-Lore-Project\` + \`X-Lore-Git-Remote\` into \`ANTHROPIC\_CUSTOM\_HEADERS\`. \*\*Invariants (HIGH-PRIORITY, user directive)\*\*: (a) Confident path (source \`header\` or \`inferred\`) ALWAYS binds & clears provisional binding — overrides any prior state. (b) \`cwd\` NEVER overwrites a confident binding (pipeline.ts:1067-1070). (c) Side-channel sessions that never send the header NEVER self-recover — require bucketing/self-heal. (d) Gateway cwd must never appear as a project (test invariant: \`expect(projects.some(p=>p.path===process.cwd())).toBe(false)\`).

### Gotcha

<!-- lore:019e6a44-dbf1-7df6-ba60-94f75c21b81d -->
* **consolidationUser() batchTarget frame-of-reference mismatch: global target vs batch count produces negative excess**: Trap: \`batchTarget\` in batched consolidation was computed globally (\`Math.max(cfg.curator.maxEntries, entries.length - CONSOLIDATION\_BATCH\_SIZE/2)\` = 1118 for 1143 entries) but \`consolidationUser()\` computed \`excess = count - targetMax\` where \`count\` is the batch size (50). Result: \`excess = 50 - 1118 = -1068\` — prompt tells LLM 'must remove at least -1068 entries', LLM returns empty ops, idle scheduler enters 1h cooldown, system never converges. Fix: \`batchTarget\` must be batch-relative — \`Math.ceil(CONSOLIDATION\_BATCH\_SIZE / 2)\` so \`excess = 50 - 25 = 25\`. Fixed in e8a73f9. CONSOLIDATION\_BATCH\_SIZE=50 (curator.ts:643). Non-batched path (entries ≤ BATCH\_SIZE) correctly uses \`cfg.curator.maxEntries\` as target. Note: \`consolidate()\` NEVER creates entries — only 'update' and 'delete' ops applied; \`includeCross=false\` excludes cross-project entries.

<!-- lore:019e6a1b-3408-7609-ba6b-d4ba9d552afd -->
* **curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields**: curator.ts applyOps(): create op missing \`content\` field crashes at line 226 — \`filterOps()\` (lines 109–117) only checks \`op\` is a string, does NOT validate required fields (\`content\`, \`title\`, \`category\`). Fix: null guard at line 226 AND field validation in \`filterOps()\` for create ops. \`update\` branch at line 252 already has \`op.content !== undefined &&\` guard. \`applyOps()\` calls \`ltm.syncRefs(id)\` and \`entities.syncEntityRefs(id, content)\` after each create/update. \`skipCreate\` flag skips creates (consolidation). Cross-project guard prevents mutating foreign-project entries. Curator receives raw temporal message content or distillation observations as text — no file path extraction from tool outputs. \`buildToolFailureContext()\` reads tool\_calls table via \`toolTrace.toolFailureStats()\`.

<!-- lore:019e69f0-a05a-7d48-8e35-76baf13533c5 -->
* **LOREAI-GATEWAY-1E: resp.usage undefined crash in postResponse() for vLLM/OpenAI path**: Trap: \`GatewayUsage\` typed as non-optional on \`GatewayResponse\`, so \`resp.usage.inputTokens\` looks safe. But \`resp.usage\` can be \`undefined\` at runtime (e.g., vLLM returning partial response). Crash sites: pipeline.ts lines 2069, 2101, 2137-2140, 2150-2153, 2204-2206, 2282-2285, 2341, 2363, 2402; also \`buildOpenAINonStreamResponse\` (openai.ts:296-300). Fix: add \`const usage = resp.usage ?? { inputTokens: 0, outputTokens: 0, cacheReadInputTokens: 0, cacheCreationInputTokens: 0 }\` guard at top of \`postResponse()\` and \`nonStreamHttpResponse()\`, then use \`usage.\*\` throughout.

<!-- lore:019e56fb-b446-759b-aff2-035a83700124 -->
* **LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm — resolveAuth returns same stale token**: LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm. \`resolveAuth(sessionID)\` marks session stale and falls back to \`getLastSeenAuth()\` — same expired token. \`credentialChanged=false\` so retry-once path never taken. Idle scheduler fires 4+ LLM calls/tick (distill, meta-distill, curation, consolidation), each 401ing independently. Fix (PR #548): (1) \`isAuthStale(sessionID) && !resolveAuth(sessionID)\` guard in \`scheduleBackgroundWork()\` (pipeline.ts:2785) and \`idle.ts:144\`; (2) \`resolveAuth()\` returns null when global fallback matches stale credential; (3) add \`/Worker upstream auth error/\` to \`TRANSIENT\_ERROR\_PATTERNS\` in instrument.ts. \`resolveAuth()\` is pure read-only. Double-call pattern (guard + quota check) is intentional and race-free.

<!-- lore:019e4eab-59b4-7fe3-be03-874f682cc340 -->
* **openai.ts: array-format content in system/developer messages silently dropped**: openai.ts / openai-responses.ts parsing traps: (1) \`parseOpenAIRequest\` coerced non-string content to \`""\` — PR #526 fixed array-format system/developer content to extract text parts. (2) \`buildOpenAIMessages\` (openai.ts:523–529): \`tool\_result\` blocks pushed mid-loop before \`textParts\`/\`toolUses\` flushed — tool\_result appears before text/tool\_use of same message. (3) \`buildOpenAIMessages\` always re-emits system as \`role:'system'\`, never \`'developer'\`. (4) \`parseOpenAIResponsesRequest\` reads system prompt from top-level \`instructions\` field. (5) \`item\_reference\` and \`reasoning\` items silently dropped; \`store\` field not in extras allow-list — silently dropped. (6) LTM injection for Responses API: \`\[req.system, cache?.stableLtmSystem, cache?.ltmSystem].filter(Boolean).join(' ')\`. (7) Consecutive role:"tool" messages coalesced into one user message. (8) \`buildOpenAIMessages\` uses array content only when \`hasOpaque=true\`. (9) recall.ts must set content-type override — streaming clients forward SSE causing JSON parse crash on follow-ups.

<!-- lore:019e6950-8200-7473-87a5-45a861bb84af -->
* **recall.ts: unescaped quotes/ellipsis in marker strings cause store miss and raw marker leak**: Trap: \`buildRecallMarker()\` embeds raw \`query\` between double-quotes and raw \`id\` before \`…\` — looks safe because queries are usually plain text. But if \`query\` contains \`"\`, \`MARKER\_REGEX\` (\`/📚 Searching (.+?) for "(.+?)"…/\`) truncates the captured group at the first inner quote; \`recallStoreKey()\` produces a mismatched key; \`store.get(key)\` returns undefined; the marker is left as raw text in the conversation visible to the upstream LLM. Same issue for \`id\` containing \`…\`. Fix: escape \`"\` → \`\\"\` in \`buildRecallMarker()\` and unescape in \`parseRecallMarker()\`; for IDs, percent-encode or strip \`…\` before embedding.

<!-- lore:019e2e20-95b3-7a9d-ab38-77d87eafecc4 -->
* **splitSegments() infinite recursion on oversized single messages**: \`splitSegments()\` infinite recursion on oversized single messages in \`packages/core/src/distillation.ts\`: recurses infinitely when a single message exceeds \`maxSegmentTokens\` (16384). \`findSplitIndex()\` returns \`messages.length\` (=1), so \`left = messages.slice(0, 1)\` produces an identical recursive call. Triggered on large tool outputs (~49KB+). Fix: add base case after the \`totalTokens <= maxTokens\` guard — \`if (messages.length <= 1) return \[messages]\`. The oversized message becomes an indivisible segment. Also: \`distillTokenBudget()\` uses \`10×√N\` formula clamped to \[256, 4096]. \`storeDistillation()\` stores no file path column — paths only in \`temporal\_messages.content\` via \`toolStripAnnotation()\`.

<!-- lore:019e49d5-15c4-78d1-8bbc-f43fde34d9f1 -->
* **startServer() returns plain object, not Promise — use startGateway() for programmatic async use**: Gateway startup traps: (1) \`startServer()\` returns \`{ stop, port, hosts }\` synchronously — use \`startGateway()\` from \`cli/start.ts\` for \`Promise\<GatewayHandle>\`. (2) \`loadConfig()\` in \`config.ts\` is SYNC — calling \`.then()\` throws TypeError. (3) EADDRINUSE fires as async \`'error'\` event — wrap \`server.listen()\` in a Promise. (4) Use port \`0\` (ephemeral) in tests. (5) \`@loreai/core\` exports async \`load(directory)\` — distinct from gateway's sync \`loadConfig()\`. (6) In-process callers MUST pass \`local:true\` — hosted mode disables FS ops. (7) \`lore run\` and \`lore import\` always run locally — both set \`opts.local=true\`. Gateway discovery order: (0) \`LORE\_REMOTE\_URL\`; (1) \`LORE\_GATEWAY\_URL\`; (2) port file (ports 3207, 5673); (3) in-process \`startGateway({ quiet:true, local:true })\`. \`remoteGateway\` auto-detection: (1) \`LORE\_REMOTE\_GATEWAY\` env; (2) \`LORE\_HOSTED\_MODE\`; (3) non-loopback bind; (4) default=true. \`--local\` CLI flag ALWAYS wins.

<!-- lore:019ead5c-76e0-781b-9490-905af0472507 -->
* **Vitest resolves @loreai/core via symlink to source, but tsconfig/vite may read dist**: Vitest 4 + pnpm + Lore: \`@loreai/core\` resolves to stale dist despite alias config. ROOT CAUSE: \`vitest.config.ts\` had \`resolve.alias\` nested inside \`test: { resolve: { alias: {...} } }\` — Vite's \`resolve\` is a top-level option, NOT a \`test.\*\` option. Fix: move \`resolve.alias\` to top-level with \`path.resolve\` to \`packages/core/src\` and \`packages/gateway/src\`. Also delete \`packages/core/dist\` to force fresh. CI GOTCHA: dorny/paths-filter@v4 did NOT include root \`vitest.config.ts\` in \`code\` filter — test job was SKIPPED on config-only PR. Fix: add \`vitest.config.ts\` to paths-filter code list. \*\*tsc trap\*\*: stale \`.tsbuildinfo\` causes phantom type errors — delete tsbuildinfo files to clear. \*\*node:sqlite trap\*\*: \`pnpm test\` may spawn node 22.12 (via volta/pnpm engine resolution) which lacks \`node:sqlite\`; run \`npx vitest run\` directly with node 24+ to bypass. Core tsconfig includes only \`src/test/script\` — eval files dragged in by test imports (e.g. eval-judge.test.ts importing \`../eval/harness\`) surface Bun-ism errors (\`import.meta.dir\`); guard with \`(import.meta as {dir?:string}).dir ?? dirname(fileURLToPath(import.meta.url))\`.

### Pattern

<!-- lore:019e56d6-9cca-7a77-aa1a-a5850daef395 -->
* **Gateway test patterns: unit vs integration vs subprocess isolation**: Gateway/OpenCode test patterns — three isolation levels: (1) Unit: import functions directly, build fixtures via \`makeRequest()\`/\`makeUserMsg()\` factories. (2) Integration: \`createHarness()\` starts real server on random port with replay interceptor, provides \`chat()\` + \`queryDB()\`. (3) Subprocess isolation: \`mock.module()\` calls isolated in subprocess files to avoid global cache pollution. OpenCode plugin tests: \`LorePlugin\` + \`createMockClient()\` stubbing \`client.tui.showToast\` and \`client.session.\*\`. \`initPlugin()\` returns \`{hooks, tmpDir, cleanup}\`. DB isolation via root \`bunfig.toml\` preloading \`packages/core/test/setup.ts\`. \`GatewayRequest\` minimal required fields: \`protocol, model, system, messages, tools, stream\`. \`PluginInput\` type does NOT include \`chat\` — access via \`client.app.experimental.chat\`. All packages must be devDependencies — CI enforces with \`pnpm run check:deps\`.

<!-- lore:019ead47-995f-7339-955e-548f2b7625ae -->
* **lore setup command: per-agent config writers with idempotent JSON/TOML updaters**: \`packages/gateway/src/cli/setup.ts\` \`SUPPORTED\_APPS\` has 3 entries: codex (TOML, no plugin), opencode (JSON, plugin: opencodePluginSpec), claude-code (JSON, no plugin). \`opencodePluginSpec\` (exported const, declared BEFORE \`SUPPORTED\_APPS\`): \`npmPackage: "@loreai/opencode"\`. \`PluginSpec.registerConfigPath\` was DEAD CODE — removed in PR #660. \`installPlugin()\`: never throws; returns true on full success, false on failure — caller MUST check return value and set \`process.exitCode = 1\` on failure. \`isNpmPackageInstalled()\`: \`npm ls -g --json --depth=0\`, catches non-zero exit → false. \`AppSpec.run\` signature: \`(baseUrl: string, noPlugin: boolean) => void\` — codex/claude-code silently ignore \`noPlugin\`. Help text \`Supported:\` MUST list all 3 apps: \`codex, opencode, claude-code\`.

### Preference

<!-- lore:019e920f-0876-7aad-a306-8129434a3d6b -->
* **Always embed behavioral directives as code comments to assert invariants**: Behavioral directives: (1) Write invariant assertions as imperative code comments at enforcement points. (2) 🔴=durable/identity facts; 🟡=session-scoped — never promote 🟡→🔴 without confirmation. (3) After bulk type migrations, run typecheck and update ALL affected test files. (4) Review plan before execution — survey tsconfig, package.json, migration status; enumerate all consumers of shared state; prefer surgical minimal-churn. (5) Bug investigation: numbered hypotheses, exact line numbers, quote file comments verbatim. (6) Always update .lore.md alongside every PR. (7) Prefer CLI over raw API. (8) Address all code review comments completely — fix root cause, add missing tests, fix biome lint/format errors before pushing. (9) Before merging: run critical self-review checking logic flaws, incomplete allowlists, fragile logic, and CI status. (10) Always do thorough read-only codebase exploration before making changes (5-10 modules, structured findings with file:line refs).

<!-- lore:019ea574-982f-76b5-811b-6695654733cb -->
* **Always implement worker health monitoring and source attribution for background tasks**: When adding or modifying background worker infrastructure (distillation, curation, embedding, cache warming), consistently add: (1) graduated failure escalation with tiers (warn → Sentry → user-facing warnings → exceptions), (2) per-session sliding window failure tracking, (3) recovery detection on success, (4) source attribution columns (worker\_provider\_id, worker\_model\_id) to database tables, (5) wiring of health hooks at every call site in pipeline, idle, and adapter layers.

<!-- lore:019e9f06-597e-75c0-8f6a-2d2b4c144bc8 -->
* **Always remove all Bun-specific code and build targets**: Always remove all Bun-specific code: delete dist/bun directories, replace Bun APIs (Bun.mmap, Bun.file, Bun.zstdDecompressSync) with Node.js equivalents, remove Bun-specific mocks/test assertions/build targets. Migrate projects from Bun to Node.js and pnpm. Separate migration steps into distinct PRs — each CI-verified. Never add new Bun-specific code. \`node:worker\_threads\` works under both Bun and Node; load worker via \`new URL('./worker.ts', import.meta.url)\`; use \`Float32Array\` structured clone; \`worker.unref()\` for clean exit. Sentry: \`@sentry/bun\` imports remapped to \`@sentry/node\` via \`sentryBunToNodePlugin\`; Bun's \`node:util\` lacks \`getSystemErrorMap()\` — polyfill BEFORE \`Sentry.init()\` in \`packages/gateway/instrument.ts\` (also documented in llm-adapter.ts entry).

<!-- lore:019eae28-ce33-719c-a0fd-eb93c53c224a -->
* **Always run the full test suite to verify no regressions before committing or creating PRs**: Always run the full test suite before committing or creating PRs. Compare pass/fail counts; document pre-existing failures as non-regressions. Required CI checks before merging: \`test\`, \`actionlint\`, \`binary smoke (darwin-arm64)\`, \`binary smoke (windows-x64)\`, \`changes\`, \`preview\`, \`CI Status\`, \`Check generated docs\`, \`Check docs links\`. Nightly/release jobs SKIPPED is acceptable for non-release PRs. Never merge on partial CI or enable auto-merge before last required job passes. Run local \`pnpm -r typecheck\` and lint before pushing. Main/release pushes ALWAYS run full CI — enforced via \`Force full CI\` step (id: \`force\`, \`if: github.event\_name == 'push'\`) in \`changes\` job, overriding dorny/paths-filter. If CI skips a job due to path filters, treat that as a bug and fix the filter. Pre-merge reviews must be harsh and multi-pass.

<!-- lore:019e9f41-df33-7e3c-a1a5-843a62fcbd64 -->
* **Always use the same provider and never cross-provider**: Same-provider invariant (hard, PR #588): worker/warmup model ALWAYS uses same provider as session. Wire protocol resolved from \`resolveProviderRoute(providerID)?.protocol\`, NOT hardcoded \`providerID === 'openai'\` — NVIDIA/OpenRouter use OpenAI protocol but have non-'openai' providerIDs. \`isOpenAI\` flag in llm-adapter.ts:362 uses \`model.providerID === 'openai'\`, bypassing these providers — TRAP. \`createGatewayLLMClient()\` always injects \`state.lastUpstream.url\` since worker provider IS session provider. Provider mismatch guard: checks \`cred.scheme==='api-key' && !hasDedicatedKey && !upstreamOverride\`; guard BYPASSED when \`upstreamOverride\` active. Workers must NEVER fall back to \`config().model\`/\`cfg.model\` — \`const model = input.model\` with NO fallback is correct in curator.ts, distillation.ts, pattern-echo.ts. NEVER cross-provider batch.
