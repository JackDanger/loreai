<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019eaee5-6de2-7abb-ac6a-4bcd02ad862d -->
* **craft NpmTarget workspace expansion and OIDC auth strategy**: craft NpmTarget: \`expand()\` discovers workspaces via pnpm-workspace.yaml or package.json, topologically sorts, validates public packages don't depend on private ones. Auth priority: \`oidc=true\` config → force OIDC; \`NPM\_TOKEN\` set → token auth; no token + OIDC env detected → auto OIDC; else error. OIDC env: \`ACTIONS\_ID\_TOKEN\_REQUEST\_URL\`+\`ACTIONS\_ID\_TOKEN\_REQUEST\_TOKEN\` (GitHub) OR \`NPM\_ID\_TOKEN\` (GitLab). \`publishPackage()\`: path arg ALWAYS pushed last. \`getPublishTag()\`: pre-release → 'next'; new version < current latest → 'old'; else undefined. \`publishMain()\`: artifact download → workspace expand → state resume → publish loop (state written after each target) → merge release branch (non-fatal) → post-release command. State file uses \`XDG\_STATE\_HOME\`. Legacy cwd state file no longer read (PR #797).

<!-- lore:019e7013-a9ea-72e6-8e8c-17ef1848e4e0 -->
* **llm-adapter.ts: retry logic, backoff schedule, and circuit breaker integration**: llm-adapter.ts retry/backoff: \`TRANSIENT\_CODES={429,500,502,503,529}\`; \`AUTH\_ERROR\_CODES={401,403}\`. \`MAX\_RETRIES\_RATE\_LIMIT=3\`; \`MAX\_RETRIES\_SERVER=3\`; \`MAX\_RETRIES\_URGENT=2\`. \`backoffMs()\`: Retry-After present → \`min(retryAfterMs, cap)\`; urgent → \`min(1000×2^attempt, 4000)\`; 429 non-urgent → \`min(60\_000+attempt×60\_000, 180\_000)\`; 5xx → \`min(1000×2^attempt, 8000)\`. On 429 non-urgent → \`tripCircuitBreaker(pauseSec)\`. Auth errors → \`markAuthStale\`, re-resolve, retry once if \`credentialChanged && attempt===0\`. \`buildAnthropicWorkerRequest()\`: injects billing block only when \`cred.scheme === 'bearer'\`; system sent as block array with \`cache\_control: { type: 'ephemeral', ttl: '1h' }\`; body signed via \`signBody()\`. \`opts.thinking\` intentionally NOT forwarded in bare API fetch. Sentry init: \`packages/gateway/instrument.ts\`; polyfill \`util.getSystemErrorMap = () => new Map()\` BEFORE \`Sentry.init()\` (Bun's node:util lacks it).

<!-- lore:019e5715-cccd-7be2-a979-407066c526d4 -->
* **log.ts: sink bridge, file rotation, and error visibility rules**: \`packages/core/src/log.ts\`: (1) \`log.error()\` always calls \`console.error()\` — always visible. \`log.info()\`/\`log.warn()\` only print if \`LORE\_DEBUG=1\`. (2) \`sink?.captureException(err)\` called when \`Error\` instance found via \`findError()\` — gateway bridges to Sentry via \`registerSink()\`. (3) File logging: all levels written to \`~/.local/share/lore/lore.log\`; rotated at 5 MB, check every 1000 writes, single \`.log.1\` backup; disabled in \`NODE\_ENV=test\`. (4) Only one \`LogSink\` supported. TRAP: any \`log.error(..., errorObj)\` auto-forwards to Sentry — including expected shutdown/auth errors. Use typed error subclasses or \`TRANSIENT\_ERROR\_PATTERNS\` filter. \`AbortError\` (DOMException) in pipeline.ts and llm-adapter.ts downgraded to \`log.info\`.

<!-- lore:019e54f5-04be-7a9f-9e62-177d65d5db9f -->
* **LTM forSession() budget, phases, and safety-net constants**: LTM forSession() architecture: \`forSession()\` (ltm.ts) budgets: \`budget.ltm\`=0.05, \`budget.preferenceLtm\`=0.02. Phases: (1) architecture entries up to ARCH\_BUDGET\_FRACTION=0.2; (2) remaining by score desc; (3) .lore.md sections. HEADER\_OVERHEAD\_TOKENS=15 subtracted upfront. Hard cap: \`if (used+cost>maxTokens) continue\`. PROJECT\_SAFETY\_NET=5: top-confidence project entries ALWAYS included even with zero relevance score. NO\_CONTEXT\_FALLBACK\_CAP=10. Global entries (pid=null) must be cross\_project=1. Pool SQL: project pool uses \`cross\_project=0\`; cross pool uses \`project\_id IS NULL OR cross\_project=1\`. Promoted entries (cross\_project=1) lose PROJECT\_SAFETY\_NET guarantee in home project. GOTCHA: \`vectorSearch()\` queries \`knowledge WHERE embedding IS NOT NULL AND confidence>0.2\` — test suites must \`DELETE FROM knowledge WHERE embedding IS NOT NULL\` in beforeEach. Dedup threshold for Nomic v1.5 is 0.935.

<!-- lore:1cdad2a4-5637-471b-a5cb-39c919fb1592 -->
* **Multilingual support: FTS tokenizer, Unicode tokenization, and non-English fallbacks**: Lore supports non-English (e.g. Turkish) conversations. Key design: (1) FTS5 uses \`tokenize='unicode61 remove\_diacritics 0'\` (NOT \`porter\` — English-only stemmer; NOT diacritic-folding — Turkish ç/ğ/ı/ö/ş/ü are distinct letters). Migration v32 rebuilds all 6 FTS tables. (2) \`filterTerms()\`/\`extractTopTerms()\` in \`search.ts\` strip via \`/\[^\p{L}\p{N}\_\s]/gu\` (Unicode-aware) so Turkish words don't split at special letters. Underscore kept for snake\_case. (3) \`pattern-extract.ts\` regexes need NO translation — they run on the LLM observer's \*English-normalized\* output. (4) Two raw-user-text regex sites have a structural fallback: \`hasNonAsciiLetters()\` (exported from \`instruction-detect.ts\`) gates fallbacks in \`extractInstructionCandidates\` (emit whole message as candidate) and \`detectAssertions\` (pin first sentence) — requires ≥3 non-ASCII letters to avoid false positives on English loanwords (café, naïve). (5) \`ltm.rerankPreferences()\` no longer demotes entries lacking English directive words to 0.8. KNOWN LIMITATIONS (deferred): English \`STOPWORDS\` not filtered for other languages (vector+RRF compensates); ~3-4 chars/token estimate is English-tuned (fine for Turkish, off for CJK).

<!-- lore:019e16fc-4410-7424-8a8a-712db1934a44 -->
* **opencode cumulative branch tracks upstream dev with custom patches**: opencode cumulative branch rebases on \`origin/dev\`. Custom patches: (1) plan mode markdown display with collapsible modal in \`plan\_exit\` tool, (2) collapsible question modal/dock (\`origin/collapse-questions\`, not yet merged to dev), (3) unarchive stickiness fix, (4) plan\_exit/plan\_enter deny rules for subagents in task.ts tools map. Upstream renamed \`session/index.ts\` → \`session/session.ts\`. Upstream removed \`cleanupSessionCaches\` from \`session.updated\` (only \`session.deleted\` remains). During rebase, task.ts conflicts: keep upstream's \`deriveSubagentSessionPermission\`, add plan\_exit/plan\_enter inline permission checks in tools map region. Session-level deny alone is insufficient — must also deny in tools map passed to \`SessionPrompt.prompt()\`.

<!-- lore:019eb4d3-3184-7101-ad10-a711fdde9b13 -->
* **Remote sync backend: Supabase Postgres + RLS (chosen over Turso/Neon)**: Remote sync backend: Supabase Postgres + RLS (chosen over Turso/Neon). Bundles Auth (GitHub OAuth, SAML 2.0 SSO on Pro+), S3-compatible Storage, RLS. Single shared multi-tenant Postgres DB with \`owner\_user\_id = auth.uid()\` RLS on every synced table. CRITICAL: missing RLS policy = cross-user data leak. Phase 2 adds \`team\_id\` + team-sharing policies. RLS accepts external JWTs via \`auth.jwt()\`. Migration path: Supabase Auth + GitHub OAuth now → WorkOS for enterprise SAML/SCIM later. Phase 1 sync: outbox pattern with monotonic seq, push-then-pull, server-time authority. Dependency: \`@supabase/supabase-js\`.

<!-- lore:019e99ac-28eb-7a7f-a362-37e2a18eaec5 -->
* **Session auth cross-contamination: per-provider credential and upstream snapshot fix**: Session auth cross-contamination fix: \`SessionState.lastUpstream\` and \`sessionAuth\` are singular — overwritten when user switches providers mid-conversation. Fix: (1) \`upstreamByProvider: Map\<string, UpstreamSnapshot>\` added to \`SessionState\`; \`lastUpstream\` kept as convenience pointer. (2) \`sessionAuth\` → \`Map\<sessionID, Map\<providerID, AuthCredential>>\`; \`setSessionAuth\`/\`resolveAuth\` take optional \`providerID\`; \`\_default\` slot synced on every write. (3) Session-aware LLM wrapper looks up \`state.upstreamByProvider.get(workerProviderID)\`, falls back to \`state.lastUpstream\`. KNOWN LIMITATIONS: Provider ID namespace mismatch — \`upstreamByProvider\` keys from \`X-Lore-Provider\` header vs \`WORKER\_DEFAULTS\` providerID; MiniMax-only sessions fall back to \`lastUpstream\`. \`getWorkerAuth\` type annotation at pipeline.ts:898 is stale.

<!-- lore:019e5922-4280-76aa-9b79-5ebeae433d7a -->
* **Sub-agent session detection, isolation, and differential treatment**: Sub-agent session detection and isolation: detected via \`x-parent-session-id\` header (pipeline.ts:2773-2809). Each sub-agent gets independent session: temporal storage, gradient state, distillation pipeline, LTM injection, cost tracking. Key differential treatment: (1) cache warming ALWAYS skipped — \`shouldWarm()\` returns false unconditionally; (2) \`findRotationPredecessor()\` skips sub-agents from Tier 1b rotation; (3) idle distillation/curation runs normally. \`isSubagent\`/\`parentSessionId\` persisted to DB (migration v26). Dashboard: collapsible cost-rollup tree via \`loadParentChildMap()\` + \`buildLiveSessionRows()\`; \`rollUp()\` propagates grandchildren costs bottom-up. \*\*OpenCode plugin gap\*\*: \`chat.headers\` hook injects \`x-lore-session-id\` and \`x-lore-agent\` but does NOT set \`x-parent-session-id\` — gateway can't distinguish parent from sub-agent. \`KNOWN\_SESSION\_HEADERS\` (session.ts:252-257): x-lore-session-id, x-claude-code-session-id, x-session-id, x-session-affinity. Only \`x-session-affinity\` is rotation-eligible. \`x-claude-code-session-id\` is always a genuinely new session — never merge into existing. \`headerSessionIndex\` (pipeline.ts:467): Map keyed \`"headerName:sessionId"\` (Ti \[truncated — entry too long]

### Gotcha

<!-- lore:019eb4d0-6fd9-7ef5-b22f-0f156eeca4b0 -->
* **cli/main.ts OPTIONS map: undeclared flags silently become booleans under strict:false**: Trap: \`parseArgs\` with \`strict: false\` treats any undeclared \`--flag value\` as \`{flag: true}\` with \`value\` falling into positionals — looks correct because \`--flag=value\` (equals syntax) still works. Fix: every flag used in ANY subcommand MUST be declared in the global \`OPTIONS\` map in \`packages/gateway/src/cli/main.ts\` with its correct type (\`string\` or \`boolean\`). Discovered in PR #696: \`--to\`, \`--project\`, \`--dry-run\`, \`--no-children\`, \`--min-confidence\` were missing, causing \`--to /path\` to parse as \`{to: true}\` with \`/path\` as a positional.

<!-- lore:019e6a44-dbf1-7df6-ba60-94f75c21b81d -->
* **consolidationUser() batchTarget frame-of-reference mismatch: global target vs batch count produces negative excess**: Trap: \`batchTarget\` in batched consolidation was computed globally (\`Math.max(cfg.curator.maxEntries, entries.length - CONSOLIDATION\_BATCH\_SIZE/2)\` = 1118 for 1143 entries) but \`consolidationUser()\` computed \`excess = count - targetMax\` where \`count\` is the batch size (50). Result: \`excess = 50 - 1118 = -1068\` — prompt tells LLM 'must remove at least -1068 entries', LLM returns empty ops, idle scheduler enters 1h cooldown, system never converges. Fix: \`batchTarget\` must be batch-relative — \`Math.ceil(CONSOLIDATION\_BATCH\_SIZE / 2)\` so \`excess = 50 - 25 = 25\`. Fixed in e8a73f9. CONSOLIDATION\_BATCH\_SIZE=50 (curator.ts:643). Non-batched path (entries ≤ BATCH\_SIZE) correctly uses \`cfg.curator.maxEntries\` as target. Note: \`consolidate()\` NEVER creates entries — only 'update' and 'delete' ops applied; \`includeCross=false\` excludes cross-project entries.

<!-- lore:019e6a1b-3408-7609-ba6b-d4ba9d552afd -->
* **curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields**: curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields: Trap: \`filterOps()\` (curator.ts:109–117) only checks \`op\` is a string — does NOT validate required fields (\`content\`, \`title\`, \`category\`) for \`create\` ops, casting result as \`CuratorOp\[]\` silencing TypeScript. When LLM emits a malformed \`create\` op missing \`content\`, \`applyOps()\` line 226 accesses \`op.content.length\` without null guard → \`TypeError: undefined is not an object\`. Fix: add null guard at line 226 AND field validation in \`filterOps()\` for \`create\` ops. The \`update\` branch at line 252 already has \`op.content !== undefined &&\` guard — \`create\` branch lacks equivalent. \`applyOps()\` calls \`ltm.syncRefs(id)\` and \`entities.syncEntityRefs(id, content)\` after each create/update; \`skipCreate\` flag skips creates (used by consolidation); cross-project guard prevents mutating foreign-project entries. \`cfg\` is in scope in \`runInner\` at line 414.

<!-- lore:019eb4d0-6fe3-7dc1-b80b-fad31fa29748 -->
* **distillations table schema: NOT NULL columns narrative, facts, source\_ids (not content)**: Trap: \`distillations\` table has no \`content\` column — looks like it should because other tables use \`content\`. Actual NOT NULL columns: \`narrative\`, \`facts\`, \`source\_ids\`, \`generation\`, \`token\_count\`, \`created\_at\`. Also: \`tool\_calls\` table uses \`tool\` (not \`tool\_name\`) and requires \`message\_id\`. Test helpers that insert into these tables must include all NOT NULL columns or get constraint errors. Discovered while writing \`packages/core/test/data-move.test.ts\`.

<!-- lore:019e69f0-a05a-7d48-8e35-76baf13533c5 -->
* **LOREAI-GATEWAY-1E: resp.usage undefined crash in postResponse() for vLLM/OpenAI path**: Trap: \`GatewayUsage\` typed as non-optional on \`GatewayResponse\`, so \`resp.usage.inputTokens\` looks safe. But \`resp.usage\` can be \`undefined\` at runtime (e.g., vLLM returning partial response). All four accumulators construct \`usage\` with \`?? 0\` guards on individual fields — but \`resp.usage\` itself can still be undefined if an exception produces a partial response object. Crash sites: pipeline.ts lines 2069, 2101, 2137-2140, 2150-2153, 2204-2206, 2282-2285, 2341, 2363, 2402; also \`buildOpenAINonStreamResponse\` (openai.ts:296-300). Fix: add \`const usage = resp.usage ?? { inputTokens: 0, outputTokens: 0, cacheReadInputTokens: 0, cacheCreationInputTokens: 0 }\` guard at top of \`postResponse()\` and \`nonStreamHttpResponse()\`, then use \`usage.\*\` throughout.

<!-- lore:019e56fb-b446-759b-aff2-035a83700124 -->
* **LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm — resolveAuth returns same stale token**: LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm. \`resolveAuth(sessionID)\` marks session stale and falls back to \`getLastSeenAuth()\` — same expired token. \`credentialChanged=false\` so retry-once path never taken. Fix: (1) \`isAuthStale(sessionID) && !resolveAuth(sessionID)\` guard in \`scheduleBackgroundWork()\` and \`idle.ts:144\`; (2) \`resolveAuth()\` returns null when global fallback matches stale credential; (3) add \`/Worker upstream auth error/\` to \`TRANSIENT\_ERROR\_PATTERNS\`. \`setSessionAuth()\` no longer dual-writes to \`\_default\`; \`globalAuthStale\` boolean via \`markGlobalAuthStale()\`/\`isGlobalAuthStale()\`/\`setLastSeenAuth()\` clears staleness only when new credential differs.

<!-- lore:019e8d03-3fdf-76cf-b87d-eab142cad8b1 -->
* **OpenAI protocol: split tool messages + recall leak on streaming path**: Two OpenAI-protocol bugs fixed: (1) translate/openai.ts pushed separate user messages per tool\_result msg — breaks Pass 2 which only checks role. Fix: coalesce consecutive tool messages into one user message. (2) OpenAI/openai-responses streaming dispatch accumulated upstream SSE then returned early, skipping the recall loop. Fix: recall closure called from all three accumulated-response paths. Also: recall.ts must set content-type override — follow-ups parsed as JSON via streaming clients; without override, SSE→JSON parse crash occurs.

<!-- lore:019e4eab-59b4-7fe3-be03-874f682cc340 -->
* **openai.ts: array-format content in system/developer messages silently dropped**: openai.ts / openai-responses.ts parsing traps: (1) \`parseOpenAIRequest\` coerces non-string content to \`""\` — FIXED in PR #526: array-format system/developer content now extracts text parts. (2) \`buildOpenAIMessages\` (openai.ts:523–529): \`tool\_result\` blocks pushed mid-loop before \`textParts\`/\`toolUses\` flushed — tool\_result appears before text/tool\_use of same message. (3) \`buildOpenAIMessages\` always re-emits system as \`role:'system'\`, never \`'developer'\`. (4) \`parseOpenAIResponsesRequest\` reads system prompt from top-level \`instructions\` field. (5) \`item\_reference\` and \`reasoning\` items in \`parseInputItems\` silently dropped. \`store\` field not in extras allow-list — silently dropped. LTM injection for Responses API: \`\[req.system, cache?.stableLtmSystem, cache?.ltmSystem].filter(Boolean).join('\n\n')\`. \`buildOpenAIMessages\`: uses array content form only when \`hasOpaque=true\` (multimodal), plain string otherwise (cache stability). Consecutive role:"tool" messages coalesced into one user message.

<!-- lore:019e6950-8200-7473-87a5-45a861bb84af -->
* **recall.ts: unescaped quotes/ellipsis in marker strings cause store miss and raw marker leak**: Trap: \`buildRecallMarker()\` embeds raw \`query\` between double-quotes and raw \`id\` before \`…\` — looks safe because queries are usually plain text. But if \`query\` contains \`"\`, \`MARKER\_REGEX\` (\`/📚 Searching (.+?) for "(.+?)"…/\`) truncates the captured group at the first inner quote; \`recallStoreKey()\` produces a mismatched key; \`store.get(key)\` returns undefined; the marker is left as raw text in the conversation visible to the upstream LLM. Same issue for \`id\` containing \`…\`. Fix: escape \`"\` → \`\\"\` in \`buildRecallMarker()\` and unescape in \`parseRecallMarker()\`; for IDs, percent-encode or strip \`…\` before embedding.

<!-- lore:019eb897-5847-7625-8d58-b73e4ac1272d -->
* **Recurring edit failure: other:could-not-find-oldstring**: The \`edit\` tool repeatedly failed with 'other:could-not-find-oldstring' across 7 sessions (9 total failures). Root cause: oldString must match exactly including whitespace, indentation, and line endings. Common triggers: (1) reading a file with \`offset=\` and the displayed content has different indentation than the actual file, (2) the file was modified between read and edit, (3) trailing spaces or CRLF vs LF differences. Fix: always re-read the exact lines to edit immediately before editing; use the smallest possible unique oldString; verify indentation character by character.

<!-- lore:019eb897-5828-7756-b6c2-b501e9513ecf -->
* **Recurring plan\_exit failure: other:the-user-dismissed-this**: The \`question\` tool repeatedly failed with "other:the-user-dismissed-this" across 4 sessions (5 total failures). Sample error: The user dismissed this question. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019eb897-5832-7b03-bf69-19c0eca799f3 -->
* **Recurring read failure: not\_found**: The \`read\` tool repeatedly failed with 'not\_found' across 9 sessions (11 total failures). Root cause: file paths that look correct but don't exist — often because (1) the file is in a different worktree or branch, (2) the path uses a wrong package name, (3) the file was renamed/moved. Fix: use \`find\`/\`fd\` to locate the file before reading; verify the exact path with \`ls\` first. Also: \`read\` with \`offset=\` can fail with 'other:offset-is-out-of' if offset exceeds file length — always check file length before using large offsets.

<!-- lore:019eb897-583e-7454-bee9-680f3f2dfbb1 -->
* **Recurring webfetch failure: other:statuscode-non-xx-status**: The \`webfetch\` tool repeatedly failed with "other:statuscode-non-xx-status" across 5 sessions (18 total failures). Sample error: StatusCode: non 2xx status code (500 GET https://www.thefastcode.com/en-idr/article/how-to-customize-slack-s-appearance-with-themes). Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019eb897-5862-790e-8999-473c8fd8bf38 -->
* **Recurring write failure: aborted**: The \`write\` tool repeatedly failed with "aborted" across 4 sessions (4 total failures). Sample error: Tool execution aborted. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019eb897-5853-7567-bbda-1503a1aca4e4 -->
* **Recurring write failure: other:the-user-has-specified**: The \`write\` tool repeatedly failed with "other:the-user-has-specified" across 5 sessions (5 total failures). Sample error: The user has specified a rule which prevents you from using this specific tool call. Here are some of the relevant rules \[{"permission":"\*","action":"allow","pattern":"\*"},{"permission":"edit","patter. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019e2e20-95b3-7a9d-ab38-77d87eafecc4 -->
* **splitSegments() infinite recursion on oversized single messages**: \`splitSegments()\` infinite recursion on oversized single messages in \`packages/core/src/distillation.ts\`: recurses infinitely when a single message exceeds \`maxSegmentTokens\` (16384). \`findSplitIndex()\` returns \`messages.length\` (=1), so \`left = messages.slice(0, 1)\` produces an identical recursive call. Triggered on large tool outputs (~49KB+). Fix: add base case after the \`totalTokens <= maxTokens\` guard — \`if (messages.length <= 1) return \[messages]\`. The oversized message becomes an indivisible segment.

<!-- lore:019e49d5-15c4-78d1-8bbc-f43fde34d9f1 -->
* **startServer() returns plain object, not Promise — use startGateway() for programmatic async use**: Gateway startup traps: (1) \`startServer()\` returns \`{ stop, port, hosts }\` synchronously — use \`startGateway()\` from \`cli/start.ts\` for \`Promise\<GatewayHandle>\`. (2) \`loadConfig()\` in \`config.ts\` is SYNC — calling \`.then()\` throws TypeError. (3) EADDRINUSE fires as async \`'error'\` event — wrap \`server.listen()\` in a Promise. (4) Use port \`0\` (ephemeral) in tests. (5) \`@loreai/core\` exports async \`load(directory)\` — distinct from gateway's sync \`loadConfig()\`. (6) In-process callers MUST pass \`local:true\` — hosted mode disables FS ops in \`@loreai/core\` (breaks \`getGitRemote()\`, \`.lore.md\` import, config loading). (7) \`lore run\` and \`lore import\` always run locally — both set \`opts.local=true\`.

<!-- lore:019eb4d0-6fe8-7e3e-b9dc-46608c201399 -->
* **suggest.ts: RegExp flag deduplication required before appending 'g'**: Trap in \`packages/gateway/src/suggest.ts\`: constructing \`new RegExp(pattern.source, ${pattern.flags}g)\` produces \`'gg'\` if source pattern already has \`g\` flag, causing \`Invalid flags supplied to RegExp constructor\`. Fix: strip \`g\` from \`pattern.flags\` before appending: \`pattern.flags.replace(/g/g, '') + 'g'\`. Applies anywhere a RegExp is reconstructed with forced flags.

### Pattern

<!-- lore:019eb4d0-6fed-71ff-baca-7151d61bf315 -->
* **api.ts resolveProject() priority and parseBody zstd support**: \`packages/gateway/src/api.ts\`: \`resolveProject()\` priority: (1) \`:id\` route param, (2) \`?git\_remote=\`, (3) \`?path=\`. POST dispatch: literal routes first, then parameterized. TRAP: \`moveSessions()\`/\`reassignKnowledge()\` update \`knowledge SET project\_id=?\` but leave \`knowledge\_transfers\` rows pointing to old project — also update \`knowledge\_transfers\`. \`moveSessions()\` BFS-expands children before moving; \`sessions\_moved\` return overcounts if BFS includes children from other projects — use pre-counted WHERE queries. \`rebindActiveSession\` iterates only user-provided IDs, not BFS-expanded children. \`parseBody\<T>()\` supports optional zstd decompression via \`Content-Encoding: zstd\`.

<!-- lore:019e56d6-9cca-7a77-aa1a-a5850daef395 -->
* **Gateway test patterns: unit vs integration vs subprocess isolation**: Gateway/OpenCode test patterns — three isolation levels: (1) Unit: import functions directly, build fixtures via \`makeRequest()\`/\`makeUserMsg()\` factories. (2) Integration: \`createHarness()\` starts real server on random port with replay interceptor, provides \`chat()\` + \`queryDB()\`. (3) Subprocess isolation: \`mock.module()\` calls isolated in subprocess files to avoid global cache pollution. OpenCode plugin tests: \`LorePlugin\` + \`createMockClient()\` stubbing \`client.tui.showToast\` and \`client.session.\*\`. \`initPlugin()\` returns \`{hooks, tmpDir, cleanup}\`. DB isolation via root \`bunfig.toml\` preloading \`packages/core/test/setup.ts\`. \`GatewayRequest\` minimal required fields: \`protocol, model, system, messages, tools, stream\`. \`PluginInput\` type does NOT include \`chat\` — access via \`client.app.experimental.chat\`. All packages must be devDependencies — CI enforces with \`pnpm run check:deps\`.

<!-- lore:019ead47-995f-7339-955e-548f2b7625ae -->
* **lore setup command: per-agent config writers with idempotent JSON/TOML updaters**: \`packages/gateway/src/cli/setup.ts\` \`SUPPORTED\_APPS\` has a \`ConfigWriter\` per app. Codex: TOML (\`config.toml\`, \`openai\_base\_url\` + \`model\_auto\_compact\_token\_limit=999999999\`). JSON apps (opencode, claude-code): idempotent deep-merge — reads existing JSON, preserves unknown keys. Re-running \`lore setup\` is safe. Port defaults to 3207; override with \`-p\`/\`-r\`. \`opencode\` setup auto-installs \`@loreai/opencode\` npm plugin (skippable with \`--no-plugin\`) and registers it in the \`plugin\` array. \`installPlugin\` shells out via \`spawnSync('npm install -g')\`, gracefully surfaces failures. \*\*Type traps\*\*: JSON updater functions must return the agent's config type (e.g. \`OpencodeConfig\`), NOT \`Record\<string, unknown>\`. \`opencodePluginSpec\` must be declared BEFORE \`SUPPORTED\_APPS\` — forward reference causes TS2448/TS2454. Prefer bundling setup steps into \`lore setup \<agent>\` over new top-level commands.

### Preference

<!-- lore:019eb887-c1dd-7cbf-b643-7890a722fd9b -->
* **Always call plan\_exit to signal completion of planning phase**: The user consistently enforces a plan mode workflow rule: when in plan mode, the assistant must call \`plan\_exit\` to explicitly signal to the user that planning is complete. This appears as a system directive across every plan mode session. The assistant should never silently finish planning or transition without calling \`plan\_exit\`. This applies regardless of the project, task type, or complexity — it is a universal workflow requirement for all plan mode sessions.

<!-- lore:019eb88e-9fa4-7a33-b65d-ece2f29d1ed9 -->
* **Always document patch/script contents with precise structural summaries in session logs**: When discussing patch files, verification scripts, or bundle analysis: provide exact line counts, section-by-section breakdowns, specific marker names/counts, file paths, and function names. Write factual observations (🟡 bullets) capturing 'what and why'. PR reviews must cite specific file:line references, exact marker lists, and counts. Provide thorough enumerated structural descriptions rather than high-level summaries.

<!-- lore:019eb89b-e715-78fd-b2fe-ef1cf9b433f4 -->
* **Always enforce Node.js-only stack: replace Bun APIs, use vitest/pnpm/tsx, never add Bun-specific code**: Across all sessions, the user consistently enforces a strict Node.js >= 22.15 + pnpm + Vitest stack. When any Bun-specific code is found (Bun APIs, \`bun test\`, Bun-only Web APIs like \`DecompressionStream('zstd')\`, Bun native fetch, \`@sentry/bun\` at runtime), the user directs immediate replacement with Node.js equivalents: \`node:worker\_threads\`, \`node:fs\`, \`node:zlib\`, \`node:https\`, \`vitest run\`, \`npx tsx\`, \`pnpm add\`. The user treats this as a hard project rule, not a preference. When implementing fixes, always use Node.js APIs, never introduce Bun dependencies, and verify that test commands use \`vitest run\` (not \`bun test\`).

<!-- lore:019eb411-9558-7718-95fd-9ad111de16dc -->
* **Always enforce specific CI invariants as non-negotiable directives**: CI invariants — never violate: (1) Full CI always runs on main/release: \`code=true\` forced unconditionally; dorny/paths-filter only applies to PRs. (2) \`actionlint\` runs unconditionally, \`-shellcheck=""\` always set. (3) Coverage/artifacts upload even on failure (\`if: '!cancelled()'\`). (4) \`ci-status\` job exits 1 on \`failure\` or \`cancelled\`; new CI jobs MUST appear in its \`needs:\` array. (5) vitest.config.ts \`resolve.alias\` is top-level, NOT nested under \`test:\`. (6) Delete stale \`.tsbuildinfo\` files to clear phantom type errors. (7) Glob \`\*\*/\*.config.ts\` accidentally matches \`packages/core/src/config.ts\` — use \`\*.config.ts\` (root-only). (8) Biome \`noNonNullAssertion\` is CI-blocking — replace \`r!.property\` with \`r?.property\` after null checks. (9) \`check-docs\` CI job gates on config-docs and env-docs drift.

<!-- lore:019eb88c-55a2-7a6f-9579-748671ca86f0 -->
* **Always provide dense, structured context upfront before requesting code review or analysis**: When requesting PR review or technical analysis, front-load all relevant context in a single message: branch names, VCS/toolchain specifics (jj, pnpm, esbuild), architectural constraints, known gotchas, file-by-file change summaries with line counts, and invariants that must not be violated. Reviews must cite specific file:line references and cover logic flaws, invariant violations, missing coverage, and description accuracy. Never drip-feed context.

<!-- lore:019eb8a0-060f-7a55-a872-a54a7f9e50c9 -->
* **Always provide exact file:line references when reporting code findings**: When investigating or reporting on code behavior, the user expects every finding to include precise file paths and line numbers (e.g., \`packages/gateway/src/config.ts:629-634\`). This applies to function definitions, constants, bug sources, call sites, and any other code artifact. Vague references like 'in config.ts' or 'somewhere in pipeline.ts' are insufficient. When tracing a flow, each step in the chain must cite its exact location. This pattern holds for exploration reports, inline observations, and any summary of codebase findings.

<!-- lore:019eb557-7e5d-799d-a61a-ddbc1c59bfac -->
* **Always read and document db.ts schema migrations in full before making changes**: DB schema change discipline: Before any schema change, read the full MIGRATIONS array to catalog every migration (version, index, table, columns, purpose). Document current highest version and next available index. New migrations: NOT NULL DEFAULT for safe defaults, nullable for optional/backfilled columns, \`recoverMissingObjects()\` for idempotent recovery, \`schema\_version\` normalized to \`MIGRATIONS.length\`. Always verify \`MIGRATIONS.length\` before proposing a new migration. Reconnaissance first — no code until schema is fully understood.

<!-- lore:019e9f06-597e-75c0-8f6a-2d2b4c144bc8 -->
* **Always remove all Bun-specific code and build targets**: Always remove all Bun-specific code: delete dist/bun directories, replace Bun APIs (Bun.mmap, Bun.file, Bun.zstdDecompressSync) with Node.js equivalents, remove Bun-specific mocks/test assertions/build targets. Migrate projects from Bun to Node.js and pnpm. Separate migration steps into distinct PRs — each CI-verified. Never add new Bun-specific code. Bun bugs (#18145, #25677, #31158) cause model-init OOM in WASM paths — numThreads=1 avoids pthread paths. \`node:worker\_threads\` works under both Bun and Node; load worker via \`new URL('./worker.ts', import.meta.url)\`; use \`Float32Array\` structured clone; \`worker.unref()\` for clean exit.

<!-- lore:019eb19e-49a3-7756-9068-eaed09c05063 -->
* **Always request harsh multi-pass code review covering logic flaws, edge cases, and invariant violations**: Code review and commit discipline: (1) Harsh multi-pass self-review — logic flaws, edge cases, invariant violations. (2) Enumerate 5–10 targets with exact file:line refs before implementing. (3) Fix all failing tests; run full typecheck (zero errors) and full vitest suite before committing. (4) Use Biome (never ESLint/Prettier); run \`pnpm biome check --write\` before pushing. (5) CLI flags used in subcommands MUST be declared in global OPTIONS map in main.ts. (6) Always push branch and create PR as last step; watch CI to completion. (7) Exclude \`.lore.md\` from PR commits. (8) All packages must be devDependencies. (9) Real production bugs found in tests: fix in same PR with \`fix(scope):\`. (10) Never modify \`src/recorder.ts\` for test-only SSE needs. (11) New test files go on a dedicated \`test/\<description>\` branch. (12) Reconnaissance before any code — read full file structure first. (13) \`check-docs\` CI job gates on env-docs drift — run \`pnpm run generate:env-docs\` after touching env-var sites.

<!-- lore:019eb899-9cda-7346-97fd-d61d7b9b1aac -->
* **Always request thorough, multi-target investigation with explicit file paths and line numbers**: Always request thorough, multi-target investigation with explicit file paths and line numbers: When the user wants to understand a system or subsystem, they consistently ask for a 'thorough analysis' or 'thorough exploration' and enumerate specific investigation targets (typically 4–7 items). They expect results anchored to exact file paths and line numbers, not summaries. The scope always covers multiple packages in the monorepo simultaneously. When investigating, always: read the actual files, report findings with \`file:line\` references, cover every enumerated target, and surface implementation details (logic conditions, config values, edge cases) rather than high-level descriptions. Cross-session pattern: 'find what LLM providers opencode supports' style probes are recurring — trace from opencode binary source at /home/byk/Code/opencode/packages/opencode/src/, not from lore's monorepo (which depends only on @opencode-ai/plugin authoring SDK, not the binary or @ai-sdk/\* packages). Grep/ripgrep sometimes returns empty despite files existing — fall back to direct file reads.

<!-- lore:019e9f41-df33-7e3c-a1a5-843a62fcbd64 -->
* **Always use the same provider and never cross-provider**: Same-provider invariant: worker/warmup model ALWAYS uses same provider as session. \`getWorkerModel()\` (worker-model.ts): effectiveProvider = cfg.model?.providerID ?? sessionProviderID ?? 'anthropic'; fallback chain: (1) cfg.model, (2) sessionModelID only if hasKnownDefaults, (3) WORKER\_DEFAULTS\[effectiveProvider], (4) undefined. Unknown providers → returns undefined. Cross-provider guard in pipeline.ts: detects model/upstream provider mismatch at prompt time, calls \`recordWorkerFailure('cross-provider')\` + returns null. Wire protocol resolved from \`resolveProviderRoute(providerID)?.protocol\`, NOT hardcoded. Workers must NEVER fall back to \`config().model\`/\`cfg.model\` — \`const model = input.model\` with NO fallback is correct in curator.ts, distillation.ts, pattern-echo.ts. Any \`?? config().model\` in worker code is a critical bug.

<!-- lore:019e9d98-e22f-7f0d-87d3-91c4f8b9a5e3 -->
* **OpenCode plugin: per-project state map for project path header injection**: OpenCode plugin per-project state: \`projectState\` Map keyed by \`ctx.project.id\` → \`{ projectPath, gitRemote, lastSeenAt }\`; TTL \`SESSION\_STATE\_TTL\_MS = 24h\`. \`reapStaleProjectState()\` called on every new entry. \`chat.headers\` hook uses per-project Map entry to avoid cross-project clobbering. Module-level \`currentProjectPath\`/\`currentGitRemote\` updated on every call — fallback only when hook bypassed. \`loreInitPromise\` memoization prevents concurrent plugin calls racing on probe→spawn. Gateway discovery order: (0) \`LORE\_REMOTE\_URL\`; (1) \`LORE\_GATEWAY\_URL\`; (2) port file (ports 3207, 5673); (3) in-process \`startGateway({ quiet:true, local:true })\`. \`remoteGateway\` auto-detection 4-layer precedence in \`start.ts\`: (1) \`LORE\_REMOTE\_GATEWAY\` env; (2) \`LORE\_HOSTED\_MODE\`; (3) bind address (\`0.0.0.0\`/\`::\` = non-loopback); (4) default=true. \`--local\` CLI flag ALWAYS wins. In-process callers MUST pass \`local:true\`.

<!-- lore:019eb885-9bfe-7c6a-987c-6edda2862119 -->
* **Prefers recall over assuming you don't have the information**: Prefers recall over assuming you don't have the information: Encoded in RECALL\_TOOL\_DESCRIPTION (packages/core/src/recall.ts:1311-1314): 'Always prefer recall over recall assuming you don't have the information.' Applies to entities (people, services, tools, projects), file paths, past decisions, user preferences, and prior approaches. Known behavioral gap: passive directive — agents explore filesystem instead of calling recall first when a known entity is referenced by name. Fix: strengthen to be proactive (check memory BEFORE exploring). Cross-project key paths to recall first: opencode binary source at /home/byk/Code/opencode/packages/opencode/src/ (not lore's packages/opencode/ — that's a monorepo, depends only on @opencode-ai/plugin authoring SDK, NOT the binary or @ai-sdk/\* packages). @ai-sdk packages live at /home/byk/Code/opencode/packages/opencode/node\_modules/@ai-sdk/.

<!-- lore:019eb89a-a3e9-7f87-a7be-a772a0c762fc -->
* **Tool Failure**: Behavioral pattern detected across 15 sessions (action: tool-failure). The user consistently demonstrates this behavior.
