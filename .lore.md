<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019f14f5-5f81-7109-8535-bc1be9e5704a -->
* **@loreai/pi extension: all logging via core log module — never console — TUI-safety invariant**: 🔴 Invariant: \`@loreai/pi\` extension must NEVER call \`console.\*\` — Pi runs a full-screen TUI and any raw terminal write corrupts it. All diagnostics go through \`log\` from \`@loreai/core\` (file-based, terminal-suppressed unless \`LORE\_DEBUG=1\`). Embedded mode hard-silences stderr ALWAYS — 'the TUI is sacrosanct; debug via \`lore logs\` or run the gateway standalone.' No conditional silencing. PR #1050 adds \`log.silenceStderr()\` called at plugin init (line 77) when \`!inTestEnv\`. \`afterEach\` reset in \`core/test/setup.ts\` is REQUIRED — silence state lives on \`globalThis\` and leaks across tests if not reset. \`log.error\` still routes to \`sink.error\` + \`captureException\` when silenced — only stderr suppressed. Known gaps: (1) \`gateway/src/sync.ts\` 9x \`console.error\` → migrated to \`log.\*\` in PR #1050 follow-up; (2) \`embedding-worker.ts\` \`console.warn\` runs in a separate \`worker\_threads.Worker\` globalThis — silence flag cannot reach it; fix requires propagating via \`workerData\` or \`stderr:true\`+drain.

<!-- lore:019f13ee-ab1a-7161-b71f-afa426e54b93 -->
* **@loreai/sqlite-vec-vendored: SEA stub, ESM-only, and no-top-level-await invariants**: PR #1038 introduces \`@loreai/sqlite-vec-vendored\` (workspace package, \`type: module\`, ESM-only, no CJS export). Three invariants: 1. \*\*SEA builds\*\*: \`stubSqliteVecPlugin()\` in \`build-binary-sea.ts\` stubs the package entirely — \`getLoadablePath()\` returns \`undefined\`, falling back to \`globalThis.\_\_LORE\_VEC\_EXTENSION\_PATH\_\_\` set by \`native-loader.cjs\`. 2. \*\*Gateway CJS bundle\*\*: marks package external; \`require(ESM)\` is safe because gateway engines floor \`>=22.15\` exceeds Node 22.12 (unflagged \`require(ESM)\` threshold) and package is synchronous. 3. \*\*🔴 Invariant\*\*: vendored package must NEVER introduce top-level await — \`require(ESM)\` of a TLA module throws, breaking the gateway CJS bundle. darwin-x64 is intentionally absent (Apple Silicon only).

<!-- lore:019eff67-79c5-792c-a43e-07e8f24f4d8d -->
* **#627 Phase 2: appendVersion COALESCE pattern for metadata forward-copy**: \`appendVersion()\` uses \`COALESCE(?, metadata)\` for the metadata column — passing \`stringifyMetadata(overrides.metadata)\` (NULL when \`{}\`) forward-copies prior metadata when no override is supplied. \`update()\` passes \`metadata: input.metadata\` to \`appendVersion\` ONLY in the content-changed branch (A2 immutability: metric/sensitivity-only updates leave gitHead untouched). \`remove(id, metadata?)\` passes metadata to the death-cert version. \`curator.applyOps\` threads \`input.metadata\` into both update and delete paths. Merged as PR #986 (\`b8ccf562\`).

<!-- lore:019ee004-037c-70e6-a910-eef98c0778aa -->
* **A1 quota trigger: counter-based enforce\_row\_quota — only INSERTs and revivals grow live-row count**: Post-A1 \`enforce\_row\_quota()\` (migration 0007): reads O(1) from \`user\_table\_usage(scope\_id, table\_name, row\_count, byte\_count)\`. Advisory lock keyed on \`hashtextextended(scope\_id::text || '|' || tg\_table\_name, 0)\` — MUST be taken before \`exists\_pk\` check (0005 ordering); 0007 moved \`exists\_pk\` outside lock — SHOULD-FIX. Revival is UPDATE with \`d\_bytes=0, d\_rows=0\` — guard returns early, never gated. Tombstone INSERTs counted+gated (physical accounting). \`user\_table\_usage\` has NO DML grant to \`authenticated\` — written ONLY by SECURITY DEFINER triggers. Raises errcode 23514 on violation. Self-DoS: user creating+deleting ~500 entries can exhaust free cap with zero live rows; no client-side reclaim path until A2 hard-DELETE reaper.

<!-- lore:019eec5f-e309-70b8-bc82-afb5af474ece -->
* **agents-file.ts \_importEntries: foreign-marker security guard — never overwrite another project's private knowledge**: Security invariant (PR #893, \`agents-file.ts:572–579\`): if \`existing.cross\_project === 0 && existing.project\_id !== null && existing.project\_id !== ensureProject(projectPath)\` → \`continue\` (skip). A \`.lore.md\` marker resolving to a DIFFERENT project's project-scoped entry is treated as foreign and silently skipped. Cross-project/global entries (\`cross\_project=1\` or \`project\_id=null\`) remain shared/updatable. Tombstoned entries also blocked (isTombstoned check at :587). Minor: \`ensureProject()\` called N times per loop for same \`projectPath\` (N redundant indexed SELECTs) — acceptable on cold import path, could be hoisted. 🔴 'Never overwrite another project's private knowledge' — explicit code comment directive.

<!-- lore:019ef0c0-02e8-701a-824e-329ffb264c14 -->
* **Append-only knowledge versioning invariants**: Two identities per entry: logical\_id (stable entry identity) + version (immutable snapshot keyed by UUIDv7). Invariant: version rows are immutable except is\_current — content, title, category, is\_deleted, version, logical\_id NEVER mutate after insert. Invariant: never resurrect a logical\_id whose latest version has is\_deleted=1 — no separate tombstones table needed. All non-FTS reads use knowledge\_current view (is\_current=1 AND is\_deleted=0). FTS5 indexes only current+live rows; superseded version's FTS row removed on append. Knowledge outbox keyed by logical\_id (not version id) so deletes always match their remote row.

<!-- lore:019f00ae-c725-7360-8b18-fbc59c9fcdd9 -->
* **cache-warmer: dynamic warmup margin — 5m TTL scales by token count, 1h flat at 300,000ms**: \`computeWarmupMargin(ttlMs, prefixTokens)\` pure function replaces hardcoded \`warmupMarginMs\` in \`buildAnthropicProfile\`. 5m TTL (300,000ms) scales dynamically by token count — root cause: 164K prefix = 3 cache blocks with different effective TTLs; flat 45s margin insufficient for large multi-block prefixes. Size-dependence is boundary-exposure probability (larger multi-block prefixes have higher P(at least one block's TTL boundary falls inside headroom window)). 1h TTL stays flat at 300,000ms — 0.4×TTL ceiling rejected as too aggressive (24-minute margin = thrash). All ~8 consumers read via \`profile.warmupMarginMs\` unchanged. \`cooldownMs = Math.max(ttlMs - warmupMarginMs, 0)\` drives all downstream timing. Branch: \`fix/warmup-margin-size-scaled\`.

<!-- lore:019ee201-4395-7551-83fd-54ff714be1dc -->
* **cache-warmer: per-bucket circuit breaker key and recovery paths**: cache-warmer: per-bucket circuit breaker (PR #836). Bucket key = \`sessionID \x1f model \x1f upstreamURL\`. \`BreakerEntry = { failures, tripped, trippedAt }\` — failures in-memory only; only tripped buckets persisted. KV v2: \`{ version:2, buckets:{ \[key]:{ trippedAt } } }\`. Legacy v1 auto-cleared on load. \`CIRCUIT\_BREAKER\_DECAY\_MS\`=6h. Three reset paths: (1) \`resetCircuitBreaker(bucketKey?)\` — production reset; (2) \`/lore:warm:reset\` slash command; (3) \`POST /ui/api/warming/:sessionId/reset\`. \`getCircuitBreakerStatus\` is PURE READ — no map mutation, no SQLite write. \`pruneExpiredCircuitBreakers()\` GCs dead-session buckets in idle tick.

<!-- lore:019eefe8-ce95-7ef7-9edd-596c942c6b5b -->
* **compaction.ts: per-model usage cap formula and constants (PR fix/per-model-usage-scaling)**: \`maxReportedUsageForModel(contextWindow, maxOutputTokens) = Math.max(0, floor(0.9 \* ((contextWindow - min(maxOutputTokens, 20\_000)) - 13\_000)))\`. \`DEFAULT\_MAX\_REPORTED\_USAGE = 150\_300\` = result for 200K/20K. Exported constants: \`AUTOCOMPACT\_BUFFER\_TOKENS=13\_000\`, \`MAX\_OUTPUT\_RESERVE=20\_000\`, \`USAGE\_REPORT\_RATIO=0.9\`. \`AUTOCOMPACT\_THRESHOLD=167\_000\` retained for cost-tracker counterfactual only (TODO: make per-model). \`Math.max(0,...)\` floor prevents negative for tiny windows. Internal accumulator in \`createRecallAwareAccumulator\` NEVER scales — inner \`StreamAccumulator\` always created without \`scaleClientUsage\`, preserving real values for calibrate/bustRate. \`getModelEntrySync()\` used (not async) in pipeline; fallback default: \`{context:200\_000, output:8\_192}\` → yields ~160,927 cap (not 150,300 as documented).

<!-- lore:019ef676-61a4-7221-bcb5-9af320d0fc41 -->
* **confidence is a materialized cache, never written directly by lifecycle ops**: confidence column in knowledge\_meta is a materialized cache derived from CRDT deltas (base\_confidence + Σpos − Σneg). It is NEVER written directly by lifecycle operations (create, update delete update delete). Only the outcome-reward loop (creditSessionOutcome) and pruneOversized write deltas. Reads always materialize via view or explicit SUM. User assertion: 'never written directly by' — this invariant prevents drift between base and deltas.

<!-- lore:019edb00-69e2-7282-a2a3-edd5482dbd84 -->
* **consecutiveBusts is in-memory only — not restored across restarts, written to DB as diagnostic only**: \`consecutiveBusts\` in \`gradient.ts\` is intentionally NOT restored from DB across restarts. Written to \`dynamicContextCap\` DB column as write-only diagnostic. \`SUSTAINED\_BUST\_THRESHOLD=2\`; \`BUST\_PRESSURE\_THRESHOLD=3\`. \`unsustainable: true\` can ONLY surface on paths B/C/D — never on layer-0 passthrough (path A). Restart resets bust pressure intentionally. \`recordCacheUsage(isIdleResume=false)\`: if \`bustRatio > 0.5 && isIdleResume\`, logs but does NOT increment \`consecutiveBusts\`; if \`bustRatio <= 0.5\`, resets to 0 regardless of \`isIdleResume\`. Cache bust classification via \`categorizeBust(analysis, isPostIdle)\` in \`cache-analytics.ts\` — \`CacheBustCause\` union: \`"first-turn"\`, \`"system-host-change"\`, \`"system-ltm-change"\`, \`"tools-change"\`, \`"prefix-rewrite"\`, \`"window-shift"\`, \`"idle-resume"\`, \`"incremental"\`, \`"unknown"\`. EMA update fires on every actual cache event (inside \`total > 0\` check); \`bustCause\` threaded from pipeline.ts:4501 to \`recordCacheUsage\` at 4562.

<!-- lore:019f13e8-41ae-7a5d-842a-13930c4a3ae8 -->
* **docs-deploy.yml: isolated from docs-preview.yml to prevent nondeterministic cancellation on merge**: docs-deploy.yml is isolated from docs-preview.yml to prevent nondeterministic cancellation: merging to main fires both \`push: main\` and \`pull\_request: closed\` triggers; combined, GitHub nondeterministically cancels one run. \`docs-deploy.yml\` uses \`environment: production\` and \`concurrency: docs-deploy-production, cancel-in-progress: true\`. 🔴 Environment secrets (e.g., \`BSKY\_APP\_PASSWORD\`) are only injected when the job declares \`environment: production\` — without it, the secret evaluates to empty string and publish silently skips. \`env:\` must be set at job level (not step level) so \`if: ${{ env.BSKY\_APP\_PASSWORD != '' }}\` can evaluate before the step runs.

<!-- lore:019f17c1-9eb3-704c-8838-0316a3bc3279 -->
* **embedding-cap.ts: reconcileEmbedCap freeMemBytes=0 → ratio=1 (always trust stored cap)**: \`reconcileEmbedCap(freeBytes, stored, modelCap)\` in \`embedding-cap.ts\`: guard \`const ratio = stored.freeMemBytes > 0 ? freeBytes / stored.freeMemBytes : 1\`. When \`freeMemBytes=0\` (non-positive learn-time baseline), ratio short-circuits to 1 — no division, no NaN. \`ratio=1 ∈ \[0.75, 1.25]\` → returns \`clampEmbedCap(stored.cap)\`, fully independent of live \`freemem()\`. \`clampEmbedCap(8192)=8192\`. 🔴 Invariant: adaptive cap never exceeds \`MODEL\_MAX\_TOKENS=8192\`. 🔴 Invariant: free memory never improves → never re-probe (\`shouldReprobeEmbedCap\` fires only when \`free ≥ 1.3×capFreememAtLearn\`). \`\_persistEmbedCap\` gains optional \`freeMemBytes\` param (default = live \`freemem()\`) — production callers at embedding.ts lines 729, 802 pass explicit \`free\` arg, unaffected. Test seam: seed with \`\_persistEmbedCap(8192, 0)\` to make start cap env-independent.

<!-- lore:019f0134-e7f0-7ce4-a3b0-38138fbd88bb -->
* **embedding.ts poolOrInProcess: sync fallback removed — pool timeout returns empty/partial**: embedding.ts poolOrInProcess: sync fallback removed — pool timeout returns empty/partial: \`poolOrInProcess()\`: on pool timeout, sync fallback must NOT run — triggers 320MB scan. Return empty/partial on timeout + cancel via \`retireTimedOutWorker(pw)\`. \`VECTOR\_SEARCH\_TIMED\_OUT\` sentinel (≠\`null\`) = timeout → return empty; \`null\` = pool disabled/broken → run in-process fallback. 🔴 Callers MUST distinguish the two. \`tryPoolVectorSearch()\` contract: NEVER throws/rejects — all failures resolve to \`null\` or \`VECTOR\_SEARCH\_TIMED\_OUT\`. PR #1005: \`tryPoolRead()\` returns \`READ\_JOB\_TIMED\_OUT|{rows}|null\`; offload variants degrade to \`\[]/null\` on timeout. PR #966: recall FTS fan-out offloaded — \`offloadAllOrTimeout\` returns \`READ\_JOB\_TIMED\_OUT\`; callers return \`null\` to abort cascade.

<!-- lore:019f1473-7738-74e4-ba74-a3352a1aac42 -->
* **Entity graph schema: 5 tables, types, and cross-project scoping rules**: Entity graph lives in 5 tables: \`entities\` (nodes, migration v27), \`entity\_aliases\` (surface forms, UNIQUE on alias\_type+alias\_value), \`knowledge\_entity\_refs\` (mention join — stores stable \`logical\_id\`, not version id), \`entity\_relations\` (edges, migration v28), \`knowledge\_symbol\_presence\` (symbol drift detection, unrelated to people/org graph). Entity types: \`self|person|org|service|tool|repo|infra\`. \`CROSS\_PROJECT\_TYPES = {self,person,org,service,tool}\` — repo/infra are project-scoped. Alias types: \`name|email|github|slack|phone|nickname|url|domain\`. \`IDENTITY\_ALIAS\_TYPES\` excludes \`url\`/\`domain\` (co-occur on unrelated entities). Relation types (interpersonal only): \`friend|colleague|manager|report|collaborator|client|mentor|partner\`. Edges are undirected — queried symmetrically via \`entity\_a=? OR entity\_b=?\`.

<!-- lore:019f1473-7776-78dd-b4cd-4b06af89089c -->
* **Entity injection: frozen system\[1] baseline, conservative visibility, recall-tool overflow**: Entities are injected into the frozen \`system\[1]\` block (pipeline.ts:6567-6592) — present from turn 1, never churns cache. \`entitiesForSession()\` returns only current project's + \`cross\_project=1\` entities; other projects' repos intentionally excluded (cross-project context leak repaired in migration 38). Appends caveat: \`"(Partial list — use the recall tool to resolve any name not shown here…)"\`. Entity vector search (recall.ts:1000-1044) uses \`e:\` RRF key — same key as FTS entity list, so entities matching both lexically and semantically get boosted. \`infra\` stays project-scoped even in \`scope="all"\`.

<!-- lore:019f1473-7757-76f5-8f91-419a6d97d594 -->
* **Entity recall: alias expansion runs always; knowledge\_entity\_refs does NOT expand retrieval**: \`expandQueryWithEntities()\` (entities.ts:783) runs on every recall query regardless of term count — entity alias lookups are fast and targeted (contrast: LLM expansion is term-count-gated at ≤8 terms). Two-pass: per-term \`resolve()\` + full-query \`resolve()\`. Adds up to 4 alias variants to \`queries\[]\`. \`knowledge\_entity\_refs\` join table is NOT used to expand retrieval. Matching an entity does NOT pull in linked knowledge entries, and matching knowledge does NOT pull in linked entities. The join table serves only: (1) relevance ranking in \`entitiesForSession()\`, (2) \`formatRelationsForPrompt()\` display, (3) dashboard, (4) sync. No recursive CTEs or multi-hop graph traversal exist — only single-hop \`relationsFor()\` queries.

<!-- lore:019f05dc-b466-78f7-ade8-f42ea6e8cd8e -->
* **gateway bundle.ts: undici external + ONNX numThreads=1 invariants**: \`undici\` is external in the Bun ESM bundle and lazily imported only on the Node path in \`fetch.ts\` — never bundled or evaluated under Bun. Real undici@7 hangs on streaming response reads under Bun; the Bun path uses native fetch instead. ONNX WASM runtime ships UNPATCHED \`ort-wasm-simd-threaded.mjs\` because the embedding worker forces \`numThreads=1\` → ort-web takes the single-thread path and never calls its pthread \`new Worker(...)\` (which would crash under Node where the worker has no global \`Worker\`). 🔴 If \`numThreads\` ever changes, \`ort-wasm-simd-threaded.mjs\` must be patched. \`@loreai/core\` is marked external in the Bun ESM bundle (gateway \`bundle.ts\` line 171) — required so Bun shares a single module instance of \`@loreai/core\` (specifically \`\_originalFetch\`). \`@loreai/sqlite-vec-vendored\` is marked external in vector-worker.cjs and vector-worker.js; stubbed entirely in SEA builds via \`stubSqliteVecPlugin()\`.

<!-- lore:019f1811-5a57-71aa-b53f-71f65a4670e1 -->
* **gateway http-body.ts: decodeRequestBody + encodeUpstreamBody — zstd/gzip/br/deflate codec invariants**: PR #1049 adds \`packages/gateway/src/http-body.ts\` to fix issue #1032 (Codex zstd-compressed bodies → 400). Key invariants: - \`normalizeRequestEncoding\`: null for absent/empty/\`identity\`; splits on comma, takes first token, lowercases. \`identity\` and empty never reach codec switch. - \`decodeRequestBody(req)\`: if null encoding → \`req.text()\`; if unsupported → throws; else \`req.arrayBuffer()\` → \`decompressBody\` → UTF-8 string. - \`encodeUpstreamBody(serializedBody, rawEncoding)\`: re-compresses with client's original encoding; on failure falls back to uncompressed string. Returns \`{body, contentEncoding}\`. - \`deflate\` decompression: tries \`inflateSync\` first, falls back to \`inflateRawSync\` (raw DEFLATE clients omit zlib header). - \`zstd\*Sync\` requires Node ≥22.15 — matches \`@loreai/gateway\` engines floor. - \`content-encoding\` is in \`GATEWAY\_MANAGED\_HEADERS\` — never forwarded from client; gateway owns re-serialization.

<!-- lore:019f0043-c936-7899-bc68-9d1f0b724418 -->
* **gradient.ts circuit-breaker: all-or-nothing warmup — partial hits count as failures**: PR #995 changes warmup circuit-breaker semantics: a warmup is a success ONLY when \`cacheCreationTokens === 0\` (100% cache read — no new write). Partial hits (\`cacheReadTokens > 0 && cacheCreationTokens > 0\`) count as failures, same as full misses. Failure condition: \`cacheCreationTokens > 0 && cacheLikelyAlive\` (read>0 no longer exempts). Reset requires \`read>0 && creation===0\`. \`writeEfficiencySamples\` field and \`warmupWriteEfficiencyTooLow()\` removed entirely. Write while \`cacheLikelyAlive=false\` (expired) is neutral — never increments failure counter. Breaker sensitivity: trips after 3 partial warmups while \`cacheLikelyAlive\`, disables warming for bucket for 6h — watch metric \`lore.cache\_warmup.circuit\_breaker\_tripped\` post-deploy. Rationale: warmups are 100% cache read by construction — any creation means the prefix diverged.

<!-- lore:019eea52-a235-7cc4-985a-0a3dfbbffeb9 -->
* **gradient.ts: l0cap (maxLayer0Tokens) governs compression trigger, NOT layer-1 window size**: Hard design invariant: \`maxLayer0Tokens\` (l0cap) is the cost-cap threshold that determines WHEN to compress (layer-0 exit), not the size of the layer-1 window. Layer-1 \`rawBudget = usable\*0.4\` is intentionally larger than l0cap to give the stable window headroom for chunked eviction. Clamping the base rawBudget to l0cap breaks cache-stability. Only escalated-stage budgets (layers 2+) should be clamped via \`stageBudgetUsable = Math.min(usable, stageBudgetCeiling)\`. \`computeLayer0Cap(targetCostPerTurn, cacheReadCostPerToken)\` returns 0 if either arg ≤ 0; otherwise \`max(floor(target/readCost), MIN\_LAYER0\_FLOOR=40\_000)\`. \`layer0Bounds()\` is extracted so pipeline's turn-1 LTM decision (\`isLargeColdStart\`) shares ONE definition with layer choice. \`isEscalated = stageLayer > 1\`. 🔴 Any path where \`layer0Input\` uses a DIFFERENT effective factor (e.g. \`layer0Ceiling\` 0.7 cold-cache tightening at :2212-2213) that would make compressed's scale not match full's must be flagged.

<!-- lore:019ef387-4ae0-7676-b7b5-140c85ad2627 -->
* **knowledge\_meta table: confidence/last\_reinforced\_at moved off knowledge table (v55)**: Migration v55 moves \`confidence\` and \`last\_reinforced\_at\` off the \`knowledge\` table into a new convergent register table: \`knowledge\_meta (logical\_id TEXT PRIMARY KEY, confidence REAL NOT NULL DEFAULT 1.0, last\_reinforced\_at INTEGER, updated\_at INTEGER NOT NULL DEFAULT 0)\`. \`knowledge\_current\` view exposes both via LEFT JOIN: \`SELECT k.\*, COALESCE(m.confidence, 1.0) AS confidence, m.last\_reinforced\_at FROM knowledge k LEFT JOIN knowledge\_meta m ON m.logical\_id = k.logical\_id WHERE k.is\_current=1 AND k.is\_deleted=0\`. Reading/writing confidence on the base \`knowledge\` table now errors with 'no such column'. For v1 rows: logical\_id == id. \`ltm.create()\` creates matching knowledge\_meta row automatically. Schema version bumped 54→55. \`markInjected()\` updates \`knowledge\_meta.last\_reinforced\_at\` but does NOT bump \`updated\_at\` — injection stays sync-silent. Content-only \`update()\` also must NOT bump \`meta.updated\_at\`.

<!-- lore:019eff67-7951-7434-a6c0-d9dd0dfc81e3 -->
* **knowledge\_symbol\_presence table: presence-history drift signal for symbol validation (#911)**: knowledge\_symbol\_presence table (migration v59): \`(logical\_id TEXT, symbol TEXT, last\_present\_at INTEGER, PRIMARY KEY(logical\_id, symbol))\`. Symbol refs resolve to \`"ok"\` or \`"unknown"\` — NEVER \`"missing"\`. Penalty fires only when \`wasSymbolPresent()\` returns true (previously confirmed present, now absent). \`recordSymbolPresent()\` fires only on \`st === "ok"\`. External/historical/rejected mentions never get a presence record. \`RepoView.presentSymbols: Set\<string> | null\` — null = grep unavailable (neutral). Cleanup: \`remove()\` in \`ltm.ts\` must delete from \`knowledge\_symbol\_presence\` and \`knowledge\_ref\_validity\` via \`LOGICAL\_ID\_BOOKKEEPING\_TABLES\` loop BEFORE the knowledge DELETE. \`knowledge\_session\_injections\` and \`knowledge\_meta\` are intentionally excluded from that loop (sync-destined / tracked follow-up).

<!-- lore:019eec9d-45f9-7513-98bc-abf932baab86 -->
* **llm-adapter.ts resolveTarget: cross-provider fail-closed — never silently land on api.anthropic.com**: \`resolveTarget()\` (llm-adapter.ts:379): session override endpoint honored ONLY when \`overrideProviderID === modelProviderID\`; otherwise routes by worker model's OWN provider via \`resolveProviderRoute\`. If no route URL, \`routeUnavailable\` set — caller must fail closed. 🔴 'Never silently land on api.anthropic.com' — foreign providers must use their own route or fail closed. Production incident: minimax model → api.anthropic.com with Anthropic credential caused 401 loop. \`TRANSIENT\_CODES = new Set(\[429,500,502,503,504,529])\` — 504 added (PR #900) for OpenRouter/slow free models that surface upstream timeouts as HTTP 200 + \`{error:{code:504}}\`. Body-level error envelopes handled via \`extractBodyErrorCode()\` (PR #900): accepts numeric or \`/^\d+$/\` string \`error.code\`; embedded-transient retry block inserted after body parse; embedded-429 trips circuit breaker same as HTTP-429; exhaustion records \`"no-response"\` (NEVER \`worker-incapable\`) + returns null. 🔴 Bare API call never forwards \`opts.thinking\` — thinking tokens intentionally suppressed. 🔴 Auth failures always recorded so worker-health ladder sees them.

<!-- lore:019f17ff-e5b8-7b70-9e5d-4479be36e7d3 -->
* **log API visibility rules: info/warn are debug-gated; notice/error always fire**: \`@loreai/core/src/log.ts\` has two visibility tiers: - \`log.info()\` / \`log.warn()\`: debug-gated (\`isDebug && !readStderrSilenced()\`) — silent in production unless \`LORE\_DEBUG=1\`. - \`log.notice()\`: NOT debug-gated (\`!readStderrSilenced()\`) — always visible in CLI; routes to \`sink?.warn\`; use for operational warnings (invalid config, quota hits, transient errors). - \`log.error()\`: NOT debug-gated — routes to \`sink?.error\` + \`captureException\` if Error object found; use only for genuine bugs warranting Sentry capture. In \`sync.ts\`: quota/transient/poison-row sites → \`log.notice\`; unexpected catch-all → \`log.error\`. Never use \`console.error\` in gateway — any byte corrupts TUI.

<!-- lore:019f17c7-1fcf-743a-b61f-f402b3ce4601 -->
* **log.ts silenceStderr: globalThis key for cross-bundle silence propagation**: PR #1050 adds \`silenceStderr()\`/\`isStderrSilenced()\` in \`packages/core/src/log.ts\`. Flag lives on \`globalThis\["\_\_loreStderrSilenced"]\` (STDERR\_SILENCED\_KEY), NOT a module-level \`let\` — gateway's CJS bundle (\`dist/index.cjs\`) inlines a SEPARATE copy of \`@loreai/core\`; a module-level flag set by the plugin's core would NOT be seen by the gateway's bundled core. \`globalThis\` is shared across all core instances in the main thread. All 4 log functions (\`info\`, \`warn\`, \`error\`, \`notice\`) route through \`readStderrSilenced()\`. \`notice()\` always writes to stderr unless silenced; routes to \`sink?.warn\` (warning severity, not error). \`error()\` still routes to \`sink.error\` + \`captureException\` even when silenced — only stderr suppressed. \`isDebug\` uses strict string comparison to avoid \`LORE\_DEBUG=0\` being treated as enabled.

<!-- lore:019edfbc-edd5-780d-ac81-07a35390121b -->
* **ltm.ts: confidence lifecycle — decay, reinforcement, eviction, and cross-project protection**: ltm.ts: confidence lifecycle — decay, reinforcement, eviction, cross-project protection. \`decayProject(path)\` decrements confidence, never bumps \`updated\_at\`. \`markInjected(ids)\` writes only \`last\_reinforced\_at\`. \`evictLowestValue\`/\`pruneDeadEntries\`/\`pruneDeadEntriesAllProjects\` MUST skip \`cross\_project=1\` entries. \`DEAD\_CONFIDENCE\_FLOOR=0.2\`. \`enforceEntryCap\` counts promoted rows but eviction excludes them. Global sweep in \`idle.ts\`: \`pruneDeadEntriesAllProjects()\` on first tick then hourly (\`GLOBAL\_DEAD\_SWEEP\_INTERVAL\_MS=3600000\`), gated on \`loreConfig().knowledge.enabled\`. \`lastGlobalDeadSweepAt\` starts at 0, set BEFORE try block (prevents retry storm). Sweep wrapped in try/catch. Queries \`WHERE cross\_project=0 AND confidence<=0.2 LIMIT ?\` — \`cross\_project=0\` implicitly guarantees non-null \`project\_id\` (enforced at \`create()\`: null pid forces \`cross\_project=1\`).

<!-- lore:019efa21-0e50-7290-ae68-9231f88f690b -->
* **openai-codex ingress: codex flag, store:false enforcement, max\_output\_tokens suppression**: \`POST /v1/codex/responses\` → \`parseOpenAICodexRequest\` sets \`req.codex = true\`, captures Codex control fields (\`include\`, \`prompt\_cache\_key\`, \`text\`, \`tool\_choice\`, \`parallel\_tool\_calls\`, \`service\_tier\`) into \`req.extras\`. \`store\` is NOT captured — \`applyCodexResponsesDelta\` always forces \`store: false\` (ChatGPT rejects \`store: true\`). \`max\_output\_tokens\` is deleted from upstream body (ChatGPT rejects it). Upstream URL: \`https://chatgpt.com/backend-api/codex/responses\`. Gateway is stateless full-history proxy — never persists upstream item IDs. \`stream\` flag passed directly from \`GatewayRequest\` into upstream body via \`buildOpenAIResponsesUpstreamRequest\`.

<!-- lore:019efa21-0e29-732d-a4a9-55ca43b41947 -->
* **openai-codex recall follow-up: ChatGPT backend mandates stream:true — JSON path causes 400**: \`openai-codex\` provider maps to protocol \`openai-responses\` + \`codex: true\` flag (config.ts:461-463). ChatGPT \`/backend-api/codex/responses\` rejects \`stream: false\` with \`400 {"detail":"Stream must be set to true"}\`. Fix (PR #968, merged 2026-06-24): \`runRecallFollowUpStreamAccumulated()\` in \`recall.ts\` forces \`stream: true\`, accumulates SSE via injected \`ctx.parseSSE\` (wired to \`accumulateResponsesSSEStream\`), returns same \`GatewayResponse\` shape. Branch gate in \`pipeline.ts\`: \`const followUpRequiresStream = currentModifiedReq.codex === true\`. Non-codex \`openai-responses\` keeps JSON path unchanged. \`RecallFollowUpCtx\` extended with optional \`parseSSE?\` field. Design invariant: follow-up stream flag can NEVER diverge from how the continuation is consumed — coupling build(stream:X) + forward + parse in one call enforces this. \`codex\` flag preserved through \`buildRecallFollowUpRequest\` via \`...originalReq\`.

<!-- lore:019f17ff-e5f2-75a0-a009-a1e62559b6c9 -->
* **OpenCode plugin ctx.client.tui.showToast: TUI-safe error surface for init failures**: \`ctx.client.tui.showToast({ body: { message, variant: 'error', title?, duration? } })\` is the TUI-safe channel for surfacing gateway init failures to the user — it does NOT write to stderr. Use fire-and-forget (non-blocking, optional chaining) after \`log.error()\` when the gateway fails to start in embedded mode, since the dropped \`process.stderr.write\` leaves zero terminal signal otherwise. All 4 opencode test files already stub \`client.tui.showToast: () => Promise.resolve()\`. SDK type: \`TuiShowToastData\` body = \`{ message: string, variant: 'info'|'success'|'warning'|'error', title?: string, duration?: number }\`.

<!-- lore:019f14c0-7d16-764d-935c-ab6e59edf186 -->
* **openReaderConnection: never runs migrations or installs triggers — query\_only only**: \`openReaderConnection\` (reader.ts): opens \`new Database(path)\`, sets \`PRAGMA busy\_timeout = 5000\`, sets \`PRAGMA query\_only = TRUE\`, calls \`loadVecForConnection\`, returns \`{ db, vecAvailable }\`. It NEVER runs migrations or installs triggers — reader connection is strictly read-only. \`vecAvailable\` is never flipped back to \`false\` after being set to \`true\` (comment at vec.ts line 72 documents this guarantee). \`resetVecState()\` resets both \`vecAvailable\` and \`attempted\` to false only on DB close/swap.

<!-- lore:019eeb85-6440-7760-912a-30c1cdc31a9f -->
* **pipeline.ts inFlightBackground: tracks urgent distillation separately from limiter activeTasks**: pipeline.ts inFlightBackground + urgent distillation drain invariants: \`inFlightBackground\` (Set\<Promise>) tracks urgent distillation promises that bypass the background limiter. \`trackBackground(p)\` adds/removes on \`.finally()\`. Urgent distillation bypasses \`isBackgroundPaused()\` — degraded context for 10 min is worse than one API call. \`resetPipelineState\` non-fast path drains BOTH: \`drainBackground()\` (limiter's \`activeTasks\`) AND \`boundedSettle(\[...inFlightBackground], timeoutMs)\`. Missing either leaves a drain gap. \`boundedSettle()\` in background-limiter.ts: snapshots promises, races \`Promise.allSettled\` against a timeout that only resolves (never rejects); timer always cleared in \`finally\`. \`drainBackground\`: calls \`limiter.clearQueue()\` then \`boundedSettle(\[...activeTasks])\`. Queued-but-not-started tasks are never in \`activeTasks\`.

<!-- lore:019f04dc-f7ec-7397-8b7c-b4549c870c03 -->
* **pipeline.ts system block structure (v45): system\[0/1/2] and distillations placement**: pipeline.ts system block structure (v45): \`system\[0]\`: harness rules/identity. \`system\[1]\`: stable LTM baseline, 1h TTL, frozen at first turn, NEVER recomputed mid-session. \`system\[2]\`: context-bound LTM, diff-pinned, 5m cache, deferred to turn 2+. Distillations are NOT a system block — synthetic messages at \`messages\[0/1]\` via \`distilledPrefix()\`. Trap: docs often say system\[1]=distillations, system\[2]=LTM pin — both wrong. Agents: 5 in AGENTS registry. \`ANTHROPIC\_BASE\_URL\`: claude-code (:105), pi (:219). \`OPENAI\_BASE\_URL\`: Hermes (:243), NOT Codex. Codex: Rust binary, CLI flag. Ingress: \`/v1/messages\` (:406), \`/v1/chat/completions\` (:411), \`/v1/codex/responses\` (:421), \`/v1/responses\` (:426).

<!-- lore:019ef12d-69d4-7d54-bf10-7eab46b87801 -->
* **pipeline.ts two-block LTM: system\[1] stable baseline vs system\[2] context-bound, cache invariants**: pipeline.ts two-block LTM: system\[1] = stable LTM (preferences + entities + project-knowledge catalog, frozen 1h, \`STABLE\_LTM\_TTL\_MS=3\_600\_000\`); system\[2] = context-bound LTM (relevance-ranked, re-pinned on material change). 🔴 Invariants: (1) never bust the cache; (2) \`ltmCacheText\` never diverges from \`ltmPinText\`; (3) system\[1] never recomputed mid-session (frozen durably including empty result). PR #930: \`buildKnowledgeCatalogText\` folded into system\[1] (\`STABLE\_KNOWLEDGE\_TOC\_MAX=15\`, \`includeCross=false\`, preferences excluded). Overflow surfaces in system\[2] via \`overflowSink\`→\`pendingKnowledgeDelta.overflow\`→\`buildKnowledgeDeltaMessage\`, sorted id ASC for byte-stability, capped at \`OVERFLOW\_TOC\_MAX=12\`. Two curation paths: in-flight (gated by \`sessionState.curationScheduled\`) and idle (flush gen-0 then metaDistill) — gated separately to avoid system\[2] cache busts.

<!-- lore:019f04c9-26e4-794e-8d94-dd107b7bf454 -->
* **pipeline.ts: prompt-delta system — surfaced-set, reanchor-not-delete, and cache-stability invariants**: Three-layer delta system in \`pipeline.ts\`: 1. \*\*\`detectSurfacedMutations\`\*\* (line 1185): compares already-surfaced \`id:hash\` keys against current DB state — fires only on genuine content change or tombstone, never on per-turn ranking churn (churn-immunity property). 2. \*\*\`reanchorDeltaOnCompression\`\*\* (line 945): on a compressing turn, ALWAYS re-anchor (preserve \`content\` + \`mut\` signatures, rewrite only \`insertAt\`) — NEVER delete. Deleting wipes surfaced-set history, causing next append to re-derive full cumulative pin→DB wall (the regrowth churn #1013 trimmed but #1017 root-caused). 3. \*\*\`applySessionPromptDeltas\`\*\*: splices back-to-front so equal-position blocks replay in ascending-seq order; lower-index blocks never shifted by later splices. 🔴 Delta block must go BEFORE any assistant \`tool\_use\` — never between \`tool\_use\` and its \`tool\_result\`.

<!-- lore:019f1530-ffed-709b-8064-f407a1239c09 -->
* **recall.ts: entity-graph fan-in — graphExpand traverses knowledge\_entity\_refs + entity\_relations**: PR #1047 adds entity-graph fan-in to \`searchRecall()\`. When query-matched entities are found, \`graphExpand()\` in \`entities.ts\` does a bounded BFS (depth 0 = seed entities, depth 1 = 1-hop relation neighbors) collecting linked knowledge entries via \`knowledgeForEntity\` / \`knowledgeRefsForEntities\`. Scoring formula: \`(1 / (1 + depth)) \* (1 + Math.log(reach))\` — depth-decay × centrality. Results merged into RRF as a new list weighted by \`graphBoostWeight\` (default 1.0). Feature gated by \`search.graphExpansion\` (default \`true\`) and \`search.graphBoostWeight\` config fields. Visibility enforces project boundaries — no cross-project data leakage. Previously the entity graph was dashboard-only and never traversed during search.

<!-- lore:019ef10f-c4fa-7bb8-a25e-f5e8eaa25e47 -->
* **Remote migration 0007 scope seam: immutable scope\_id, physical counting, author-is-writer**: Migration 0007 renames owner\_user\_id to scope\_id on 5 synced tables and adds author\_id. RLS: scope\_id = auth.uid() for SELECT; writes require scope\_id = auth.uid() AND author\_id = auth.uid() (author is always the writer). scope\_id immutable: UPDATE with different scope\_id raises check\_violation (RLS WITH CHECK + trigger backstop). user\_table\_usage trigger-written (SECURITY DEFINER, no DML grant). Physical footprint counts EVERY row regardless of is\_deleted (forward-compatible with append-only A2; closes tombstone-flood abuse). enforce\_row\_quota uses pg\_advisory\_xact\_lock per (scope\_id, table) for TOCTOU protection. maintain\_usage uses greatest(...,0) to prevent negative counts.

<!-- lore:019f0134-e80b-740d-a6b0-3631d4e3f98b -->
* **runTemporal: native sqlite-vec path has LIMIT; O(n) scan only on JS fallback**: runTemporal vector search: native sqlite-vec path (\`vector-query.ts:326-327\`) uses bounded SQL with LIMIT and C-level cosine distance. JS brute-force fallback (\`vector-query.ts:336-339\`) loads ALL rows for project (no LIMIT) — O(n) scan. \`vec.ts:23\` contract: \`loadVecExtension()\` never throws — failures silently route to JS fallback. sqlite-vec 0.1.9 has no true ANN (no HNSW/IVF) — brute-force KNN with SIMD only.

<!-- lore:019f14c2-c258-7d55-ba0e-b9a3bf5b2c8b -->
* **standard.site protocol: rkey safety invariants and AT Proto constraints**: AT Protocol rkeys must pass a charset guard: reject slugs containing \`/\`, unicode, \`.\`, \`..\`, spaces, or >512 chars. Implemented in \`standard-site.ts\`; fails build loudly. \`PUBLICATION\_RKEY = "self"\`. \`continue-on-error: true\` on publish step — transient PDS outage must never fail deploy. ESM direct-run guard: \`main()\` only auto-executes when \`import.meta.url\` matches \`process.argv\[1]\` — safe to import in tests without triggering side effects. \`computeOrphanRkeys\` is a pure exported function for unit testing. \`capTextContent()\` caps \`textContent\` to 64 KiB: byte-accurate UTF-8 check, backs off to word boundary, appends \`\[truncated]\` within budget, never splits multi-byte code points. Enforced at \`buildDocumentRecord()\` chokepoint.

<!-- lore:019ef02c-f18a-71c8-88ec-4c33c6d71a14 -->
* **start.ts startGateway: pre-bind reuse probe + TOCTOU backstop pattern**: PR #908 fix: \`startGateway()\` now probes for an existing gateway BEFORE attempting each candidate port bind (skips port 0). Probe set: \`\[...new Set(\['127.0.0.1', ...config.hosts])]\` — loopback always included unconditionally. Returns \`{owned:false}\` immediately if any host answers \`/health\`. Post-bind EADDRINUSE catch probe retained as TOCTOU backstop only. Root cause of #908: probe was gated inside EADDRINUSE catch — non-overlapping interface bind never throws, so probe was unreachable. \`runDaemon()\` reuse-check similarly upgraded: probes \`\[...new Set(\[...opts.hosts, '127.0.0.1'])]\` (configured hosts first to preserve #875 reported address). \`firstReachableHost(hosts, port)\` helper drives both paths — parallel probes, returns first responding host by list order.

<!-- lore:019ef10f-c4d9-7073-8940-764d6bb63cc4 -->
* **Sync engine push/pull architecture: per-table cursors, quota isolation, local-first sync**: pushOnce()/pullOnce() operate per table with independent cursors: push cursor sync.push.\<table> (last seq), pull cursor sync.pull.\<table> (keyset format \`${updated\_at\_ms}|${row\_id}\`). Quota/transient failures never advance that table's cursor and never block other tables. pushOnce pushes local outbox before pulling remote (local SQLite is source of truth). Outbox pruning uses min cursor across tables with entries (empty tables excluded). Knowledge uses knowledgePushPlan() to coalesce append-only versions to one remote row keyed by logical\_id. Poison rows (over-size) recorded as rejected\_unsyncable (cursor advances); quota rows return stop (cursor unchanged for retry). PAGE=200.

<!-- lore:019eea5e-b325-720b-a9c1-1c526efc0096 -->
* **sync-data.ts seedOutbox: content-aware JS loop — 6 cases, cursor semantics, performance**: PR #868 rewrites \`seedOutbox()\` from single \`INSERT…SELECT\` to JS loop. Push cursor = \`Number(getKV('sync.push.'+table) ?? '0')\`. \`latestUnpushed\` = latest op with \`seq > cursor\`. Six cases: (a) no pending + hash==sync\_state → skip; (b) no pending + hash!=sync\_state → enqueue; (c) latest unpushed=upsert → skip; (d) latest unpushed=delete → enqueue trailing upsert; (e) stale upsert below cursor + content changed → enqueue (THE FIX); (f) stale upsert below cursor + content unchanged → skip. \`seq AUTOINCREMENT\` guarantees strict monotonicity. \`contentHash\` uses \`syncedColumns ∖ HASH\_EXCLUDE\`, sorted, SHA-256 truncated to 16 hex. 🔴 Wrap loop in \`withTransaction\` — N auto-commits cause ~23x slowdown. 🔴 \`seedOutbox\` is only ever reached via \`enableSync\`, never from within another transaction (code comment at line 498). PR #874 hoists \`columns(table)\` outside per-row loop; PR #878 memoizes globally via \`WeakMap\` keyed on \`db()\` instance — \`seedOutbox\` pre-warms cache for all tables, so subsequent \`pushOnce\` sees 0 PRAGMAs.

<!-- lore:019edfc9-c361-72f2-ada3-fc735def69f0 -->
* **sync.ts: local prune vs genuine delete — tombstone suppression required**: sync.ts: local prune vs genuine delete — tombstone suppression required. Hard invariant (🔴): only genuine user/curator-intent deletes generate tombstones and propagate to remote. Local TTL/size eviction (idle prune) must NOT enqueue a tombstone — run prune under a capture-suppressed context (analogous to \`withSyncApplying\`) that drops the DELETE-trigger tombstone and clears \`sync\_state\` only. \`reconcile\` must never see evicted rows as deletes. Evicted rows are re-pullable cache misses, not divergence. Remote is bounded by per-scope quota, not a TTL reaper.

<!-- lore:019edff0-4711-73d1-9b16-7fba4282b220 -->
* **sync.ts: per-table cursor isolation — quota/transient failure on one table never blocks others**: sync.ts: per-table cursor isolation — quota/transient failure on one table never blocks others. Cursors are per-table (\`sync.push.\<table>\`, \`sync.pull.\<table>\`). Quota rejection (HTTP 400, errcode 23514 + /quota exceeded/i) pauses ONLY the affected table. \`classifyPushError\`: 'quota'→pause table keep pending; 'poison' (23514 without quota msg)→drop past cursor record conflict; 'transient'→keep pending retry. \`drainTimestamp\` pages by id keyset when >PAGE rows share one millisecond. \`applyRemote\` strips remote-only cols and converts ISO timestamps to epoch ms. TRAP: epoch-ms integers rejected by Postgres timestamptz (22008) → 'transient' → cursor stuck. Fix: \`toRemoteRow()\` converts numeric TS\_COLS to ISO. TRAP: \`Date.parse(v)||Date.now()\` — epoch-0 is falsy; use explicit \`!== 0\` check.

<!-- lore:019edfff-dc0b-7552-89ba-9a71ebfea21c -->
* **sync.ts: pullOnly tables — server-authoritative, no outbox, no conflict classification**: sync.ts: pullOnly tables — server-authoritative, no outbox, no conflict classification. \`SyncTableMeta.pullOnly: true\` (e.g. \`profiles\`): \`pushOnce\` skips them; \`applyRemote\` forces \`cls='apply'\` bypassing \`classifyRemoteRow\` (no outbox trigger, no \`content\_hash\` column → null hash always mismatches). \`currentTier()\` in \`sync-data.ts\` reads local \`profiles\` mirror, defaults \`'free'\` — advisory UX hint only, not a security gate. \`seedOutbox\`/\`reconcile\` MUST skip pullOnly tables — prune-floor DoS gotcha. The 'outbox capture installed for every synced table' test must skip pullOnly tables.

<!-- lore:019edff0-4709-74cc-98eb-ad3bd46163c1 -->
* **sync.ts: push payload never includes owner\_user\_id — server default auth.uid() fills it; A1 changes onConflict target**: sync.ts push payload: \`pushEntry()\` sends \`toRemoteRow(pickSyncColumns(table,row))\` + \`is\_deleted:false\` + \`content\_hash\`/\`revision\` (versioned only). \`scope\_id\` NEVER included — server fills via \`default auth.uid()\`. \`onConflict\` target post-A1: \`\["scope\_id",...idColumns(table)].join(",")\`. \`REMOTE\_ONLY\_COLS\` strips \`scope\_id\`,\`author\_id\`,\`content\_hash\`,\`revision\`,\`is\_deleted\` before local apply. Soft-delete push: \`.update({is\_deleted:true}).match(decomposeId(table,rowId))\`. \`pickSyncColumns\` strips local-only cols (e.g. \`knowledge.promoted\_at\`) to avoid PGRST204. \`SyncTableMeta.versioned\` flag (default true): strip \`content\_hash\`/\`revision\` when false — \`knowledge\_entity\_refs\` is the only non-versioned table; without this fix PGRST204 → 'transient' → push cursor stuck forever.

<!-- lore:019edfe6-3301-76b0-aea9-cba6052eeb6c -->
* **Upcoming: uniform append-only model for knowledge/entities — nothing should land on these tables before refactor**: 🔴 Major upcoming refactor: knowledge/entities tables move to immutable append-only model: \`logical\_id + version + is\_deleted\`, materialized current view, compaction. DO NOT land new migrations/features on these tables before refactor. Issue #495 (op-log) subsumed — \`logical\_id+version\` history IS the evolution trajectory. Issue #467 RFC topology: Personal DB + Team DB, same schema (team columns nullable in personal), \`.lore.md\` = team entries, \`.lore.local.md\` = personal entries. Option A (single append-only table with \`is\_current\`) chosen over Option B (CQRS/dual-table) — B was explicitly rejected as more complex long-term despite similar migration cost.

<!-- lore:019f14c0-7ccd-7b4c-a55c-a0714f181fda -->
* **vec-store.ts: VecStorageMode/VecReadMode seam — 2×2 matrix collapsed by resolveReadMode**: \`vec-store.ts\` is the capability seam for PR3 of the DiskANN/vec0 cutover series. \`resolveReadMode(mode, vecAvailable)\` collapses the 2×2 matrix so callers never reason about it inline: (vec0,true)→"vec0"; (vec0,false)→"degraded"; (blob,true)→"blob-native"; (blob,false)→"blob-js". After \`degraded\` early-return and \`assertBlobReadMode\` guard, readMode is always \`blob-native\` or \`blob-js\`. \`readStorageMode\` defaults to "blob" on absent key, unrecognized value, or thrown error. \`storeEmbedding\` uses \`conn.query()\` for driver-cached prepared statements. \`VEC\_STORAGE\_MODE\_KEY="vec.storage\_mode"\` — no production writer exists yet; vec0/degraded modes are unreachable in production until the cutover PR. \`BASE\_TABLE\` maps logical→physical (temporal→temporal\_messages).

<!-- lore:019f1717-d2a1-7016-84b8-d116d09c1bfe -->
* **vec0 vector-query.ts: overfetch pattern and read-mode branches**: \`runKnowledge()\` and \`runDistillations()\` use \`overfetchK(limit) = max(4\*limit, limit+50)\` in the vec0 CTE to compensate for post-filter joins (confidence > 0.2 for knowledge; \`archived=0\` for distillations). \`runEntities()\` uses \`k = limit\` (no post-filter). \`runTemporal()\` uses \`k = limit\` — partition keys (\`project\_id\`, \`session\_id\`) enable sub-ms shard scans so no overfetch needed. All functions return \`\[]\` immediately if \`readMode === "degraded"\`. Blob-native path for temporal is a STOPGAP: scores only most-recent \`MAX\_TEMPORAL\_VECTOR\_ROWS\` rows (recency cap). Vec0 path removes this cap entirely — the recency-cap removal test seeds 4010 rows and verifies oldest row is returned. Overfetch can under-return if many low-confidence/archived entries cluster near query — known should-fix for adversarial data.

<!-- lore:019f1717-d262-761b-b10e-4afa7d4b71b7 -->
* **vec0-cutover (PR #1051): blob→vec0 migration architecture and invariants**: PR #1051 moves embedding storage from per-row \`embedding\` BLOB columns to sqlite-vec FLAT \`vec0\` virtual tables. Per-DB KV flag \`vec.storage\_mode ∈ {blob,vec0}\` (+ \`vec.dimension\`) drives behavior. Four vec0 tables: \`knowledge\_vec\`, \`entity\_vec\`, \`distillation\_vec\`, \`temporal\_vec\`. DiskANN rejected: no partition/metadata filtering, ~400× slower inserts, approximate only. Cutover is one-way via \`maybeCutoverToVec0\`. \`ensureVec0Store(conn, dim)\` is idempotent — drops+recreates all 4 tables on dimension mismatch, no-op on same dim. \`deleteEmbeddings\` chunked at 900 ids (SQLite bound-variable ceiling). \`temporal\_vec\` keyed by \`chunk\_id\` (\`${id}#0\`), aux col \`message\_id\` used for hydration. \`distillation\_vec\` has \`project\_id PARTITION KEY\` + \`+session\_id\` aux col. Orphan vec0 rows (base row deleted) reclaimed by \`gcVec0DanglingRows\`.

<!-- lore:019f0308-9ed7-7260-93e2-43389f8ec26f -->
* **vector-pool.ts: timeout retirement vs structural failure — slowness ≠ structural failure invariant**: \`retireTimedOutWorker(pw)\` handles search timeouts WITHOUT incrementing \`structuralFailures\` or setting \`poolBroken\`. Timeout order: (1) \`pw.inflight.delete(id)\`, (2) \`resolve(VECTOR\_SEARCH\_TIMED\_OUT)\`, (3) \`retireTimedOutWorker(pw)\`. \`retireTimedOutWorker\`: guard \`if (pw.dead) return\`, sets \`pw.dead=true\` BEFORE \`terminate()\` so synchronous \`exit\` event hits \`markDead\` as no-op; calls \`timeoutAll(pw)\` (sentinel, never rejection) for collateral in-flight. \`failAll\` rejects remaining inflight — used only for structural deaths. \`markDead\` (structural path) never called on timeout. \`VECTOR\_SEARCH\_TIMED\_OUT\` sentinel (≠ \`null\`) signals timeout; \`null\` signals pool disabled/broken. \`dispatchToPool\` timer uses \`.unref()\` — prevents in-flight request from delaying process exit on shutdown. 🔴 Invariant: repeated timeouts must NEVER latch \`poolBroken\`. Mutation-verified: M2 (revert \`timeoutAll\`→\`failAll\`) kills 2 collateral tests.

### Decision

<!-- lore:019ef143-9105-76ba-9178-124ed8195621 -->
* **Chose approach 2 (full 9-variant union in core) over minimal union**: Chose approach 2 (full 9-variant union in core) over minimal union.

<!-- lore:019ef0c0-0308-797a-ad49-7b0c08aedb80 -->
* **knowledge\_meta as separate synced convergent register table**: Chose separate knowledge\_meta table (Option A) over keeping confidence on per-version knowledge rows with backward sync compat (Option B). Rationale: Option B accrues sync complexity on a legacy column — confidence is mutable metadata, not per-version data, so a separate structure is semantically correct. Cost: requires remote migration 0009, RLS policies, and sync pipe plumbing. knowledge\_meta uses per-field merge: delta-CRDT for confidence/counts, max for timestamps. last\_reinforced\_at stays local (sync-excluded). cross\_project entries never adjusted by outcome-reward loop.

<!-- lore:019efe86-ad54-7ad3-868b-c3a76af9d306 -->
* **Lore license: FSL-1.1-Apache-2.0 (Fair Source), NOT open source**: Lore/loreai is Fair Source, specifically FSL-1.1-Apache-2.0 (Functional Source License v1.1, Apache 2.0 Future License). Competing Use (using code to compete against Lore) is prohibited. Converts to Apache 2.0 later. Copyright 2026 Burak Yigit Kaya. Implication: competitors legally cannot lift Lore's context-management code. This kills any 'more open than ByteRover's Elastic License' positioning angle — both are source-available, not OSS. Licensing framing belongs on positioning pages, not in vendor-neutral blog posts.

<!-- lore:019ef58f-9ff2-7edc-a84c-e7b3ba2622a4 -->
* **Migrated to register (no assertion values changed)**: migrated to register (no assertion values changed).

<!-- lore:019f0103-15cb-708f-9cff-f5c8f060d37a -->
* **Migrated to Vitest in May 2026 — 312ms vs 30s startup time cited as reason**: migrated from Mocha + Chai + ts-node to Vitest in May 2026 — 312ms vs 30s startup time cited as reason.

<!-- lore:019efa1b-96f8-7702-8ddb-ccbc2fece37e -->
* **sqlite-vec: native extension chosen over WASM; vec0 virtual table deferred**: Chose native extension + JS fallback over WASM-first for sqlite-vec. WASM build targets browser sqlite-wasm engine only — using it in node:sqlite/bun:sqlite would require replacing the entire SQLite engine, slowing ALL queries. Drop-in \`vec\_distance\_cosine()\` over existing BLOB columns chosen for PR #967 scope; \`vec0\` virtual table migration rejected as first step. Portability strategy: keep embeddings as F32 BLOB columns (byte-compatible with libSQL's \`F32\_BLOB\`); when hosted gateway becomes real, add \`driver.libsql.ts\` behind existing \`#db/driver\` map backed by \`vector\_top\_k\` — no data migration, no caller changes.

<!-- lore:019f05e3-1abc-7c77-944b-bc9e7ae25bb1 -->
* **Switched from plan mode to build mode (read-write**: switched from plan mode to build mode (read-write,

### Gotcha

<!-- lore:019f14f5-5f31-70ea-a24b-fe61689d222e -->
* **@loreai/gateway dynamic import: must use const gwPkg indirection to avoid TS2307**: @loreai/gateway dynamic import TS2307 fix: Use \`const gwPkg = "@loreai/gateway"; const gw = await import(gwPkg)\` — non-literal string bypasses compile-time resolution. TypeScript resolves string literals at compile time and emits TS2307 because \`@loreai/gateway\` has no type declarations resolvable from \`packages/pi/\`. Pattern established in \`extension.e2e.test.ts\` lines 65–66; must be replicated in any new test file that dynamically imports gateway.

<!-- lore:019f1806-4ee6-720a-8c0e-8fe0359ef1c2 -->
* **@loreai/sqlite-vec-vendored: AVX hazard is self-inflicted — upstream 0.1.9 linux-x64 is pure scalar**: Trap: the vendored sqlite-vec fork's AVX/SIGILL guard (patch 0002) looks like a safety fix for upstream binaries. Fix: upstream sqlite-vec 0.1.9 linux-x64 prebuilt (159,816 bytes) contains ZERO AVX instructions — no ymm registers, no fmadd, no cpuid dispatch. The SIGILL hazard exists ONLY because the vendored fork is compiled with \`-DSQLITE\_VEC\_ENABLE\_AVX\`, introducing 41+ ymm0 occurrences. Patch 0002 guards a problem the fork itself creates. Implication: if the vendored fork is ever replaced with upstream 0.1.9, the AVX guard patch is unnecessary on linux-x64. 🔴 Never assume upstream prebuilts carry AVX — verify with \`objdump -d | grep ymm\` before adding SIGILL mitigations.

<!-- lore:019eec0c-e455-78ea-b101-0a07dd878704 -->
* **A2 append-only flip: sync push egresses superseded/deleted content — sub-PR 3 release gate**: A2 append-only sync: after PR #853, \`remove()\` appends a death-cert row; outbox trigger fires \`op='upsert'\` for both demote UPDATE and death-cert INSERT — egresses superseded/deleted content. Fix: \`knowledgePushPlan\` (PR #897) coalesces all versions to one remote row; PR #913 makes outbox \`row\_id\` = \`logical\_id\` for all knowledge ops; superseded/death-cert rows → \`op=delete\` by logical\_id. 🔴 Release gate: PR #897 must land before shipping A2; PR #913 is prereq for PR #909 (A2 compaction).

<!-- lore:019ef645-24ae-7562-ace6-3b0d343cdb3f -->
* **applyRemoteMetaCrdt uses Date.now() for updated\_at — corrupts remote timestamp semantics**: Trap: \`applyRemoteMetaCrdt\` passes \`Date.now()\` as \`updated\_at\` when merging remote CRDT row — looks fine because sync advances cursors anyway. Fix: remote \`row\` already contains correct \`updated\_at\` (epoch ms); using local \`Date.now()\` loses the actual change timestamp, corrupting \`updated\_at\` semantic meaning (though not breaking CRDT merge or cursor advancement). Fix: pass \`Number(row.updated\_at ?? 0)\` instead. Confirmed in PR #1126 fix commit \`36173a7\`.

<!-- lore:019ef346-6e54-7f76-bae5-a73af003074c -->
* **Bedrock worker 403: markAuthStale must NOT be called — no client credential to invalidate**: Trap: calling \`markAuthStale\`/\`markGlobalAuthStale\` on a Bedrock worker 403 looks correct — 403 usually means bad credentials. Fix: Bedrock workers authenticate via SigV4 (AWS IAM), not client API keys. There is no client credential to invalidate. Calling \`markAuthStale\` on a Bedrock 403 would incorrectly stale the Anthropic API key. Guard: skip \`markAuthStale\`/\`markGlobalAuthStale\` when \`effectiveProtocol === 'bedrock'\`. Same invariant applies to \`executeWarmup\` — Bedrock warmup never sends \`x-api-key\` or \`anthropic-version\`. Tests: llm-adapter.test.ts 'bedrock worker 403 does NOT mark client credential stale'; bedrock-warmup.test.ts 'a 403 does NOT mark the client credential stale (SigV4 has none)'. Both verified non-vacuous via swap-fail-restore.

<!-- lore:019ef10f-5e1c-70ee-97c2-cd2e0836bd28 -->
* **Bedrock: atob() mojibakes multi-byte UTF-8 — use Buffer.from().toString('utf8')**: Trap: \`atob(base64)\` looks correct for decoding Bedrock event-stream \`bytes\` field. Fix: \`atob()\` yields a Latin-1 binary string — multi-byte UTF-8 sequences (accents, CJK, emoji, smart quotes) are corrupted. Proven: \`"café ☕ 你好"\` → \`"cafÃ© â ä½ å¥½"\`. Use \`Buffer.from(bodyJson.bytes, 'base64').toString('utf8')\` — \`Buffer\` is a global in Node.js and Bun, no import needed. Tests missed it because all \`chunkFrame\` calls in \`bedrock-stream.test.ts\` used ASCII-only text (\`"hi"\`, \`"Hello"\`, \`"split"\`). Fixed in \`bedrock-stream.ts\` (PR #898).

<!-- lore:019ef10f-5e3c-741a-9a7a-2a3e119821ac -->
* **Bedrock: stream field in request body causes validation error — endpoint controls streaming**: Trap: adding \`stream: true|false\` to the Bedrock request body looks correct (mirrors Anthropic API). Fix: Bedrock does NOT accept a \`stream\` field in the body — streaming is controlled by the endpoint URL (\`/invoke-with-response-stream\` vs \`/invoke\`). Including \`stream\` causes Bedrock to reject with a validation error. \`buildBedrockRequestBody\` must never set this field. Confirmed by user directive and test assertion (\`'stream' in capturedBody === false\`).

<!-- lore:019ee719-946b-7dde-99f8-56c19833e6cc -->
* **bun:sqlite returns null (not undefined) for missing rows — existence checks must use count query**: Trap: \`db.prepare('SELECT \* FROM t WHERE id=?').get(id) !== undefined\` looks correct for existence checks (standard JS pattern). Fix: bun:sqlite driver returns \`null\` (not \`undefined\`) for no-row results — \`!== undefined\` always returns \`true\`. Use a count query: \`db.prepare('SELECT COUNT(\*) as n FROM t WHERE id=?').get(id).n > 0\`. Also: \`getKV()\` returns \`null\` for missing keys — use \`?? '0'\` not \`|| '0'\` when reading push cursors. Also: \`decideCacheStrategy\` returns \`confident:false\` when pricing or body tokens are non-finite/non-positive; \`confident:true\` otherwise. \`shouldHoldPrefixWarm(null,…)\` → \`undefined===true\` → false — null econ is safe fallback.

<!-- lore:019f1523-f7fc-7e11-a586-361aadcd0114 -->
* **byk.github.io: cover images not emitted to dist unless passed through getImage — field name mismatch trap**: Trap: \`src/pages/posts/\[slug].astro\` rendered \`entry.data.banner\` but the schema field is \`image\` — the \`\<Image>\` component never fired, so cover images were never emitted to \`dist/\_astro/\`. Looks correct because the conditional \`{entry.data.banner && \<Image ...>}\` silently evaluates to false. Fix: use \`entry.data.image\` (matching \`postsCollection\` schema in \`content.config.ts\`). Corollary: Astro only emits optimized assets to \`dist/\_astro/\` for images actually passed to \`\<Image>\` or \`getImage()\` — unused \`image()\` frontmatter fields produce no output files. \`SeoHead.astro\` correctly reads \`postData?.data?.image.src\` for og:image, so the meta tag worked even when the rendered \`\<Image>\` didn't.

<!-- lore:019ee5f0-5603-7df1-9bcd-57e7571b1b63 -->
* **cache-warmer bodyBytes metric: use Buffer.byteLength not .length for UTF-8 wire size**: cache-warmer bodyBytes metric: use \`Buffer.byteLength(signedBody, 'utf8')\` not \`signedBody.length\`. \`.length\` returns UTF-16 code units, not UTF-8 byte count — misleads cache divergence analysis for non-ASCII content. Fixed PR #841.

<!-- lore:019ee201-4386-7eb2-a958-c4150fec4705 -->
* **cache-warmer circuit breaker: global early-return in idle.ts blocks ALL sessions when any bucket trips**: cache-warmer circuit breaker: global early-return in idle.ts blocks ALL sessions when any bucket trips. Fix (PR #836): removed global early-return; replaced with per-bucket \`continue\` inside the loop after profile resolves. Each bucket \`(sessionID, model, upstreamURL)\` is now independent. \`isCircuitBreakerTripped\` signature changed to 2-arg \`(bucketKey, now)\`. \`pruneExpiredCircuitBreakers()\` called once per tick before the loop to GC dead-session stale buckets.

<!-- lore:019ee670-8c0c-76b2-b0a1-0c411590477e -->
* **cache-warmer shadow mode: ROI/write-efficiency guards must come AFTER shadow eval block**: cache-warmer shadow mode: ROI/write-efficiency guards must come AFTER shadow eval block. Trap: shadow eval block placed after ROI guard (\`hitRate < MIN\_SESSION\_HIT\_RATE\`) — large/growing sessions that fail those guards are never measured. Fix: hoist survival computation (\`survivalFunction\`, \`breakFraction\`, \`pSessionFinished\`, \`pReturns\`, \`costThreshold\`, \`maxProfitableCycles\`) and shadow eval block ABOVE the ROI and write-efficiency guards. Correct placement: after messageCount/inputTokens/disabled checks, before ROI and writeEfficiency guards. Behavior-neutral reorder.

<!-- lore:019efa3e-34e4-7fdf-a0f7-a2633fec1306 -->
* **CI not starting on a PR usually means merge conflicts, not a CI issue**: Trap: when CI doesn't start on a PR, it looks like a CI/GitHub Actions configuration problem. Fix: GitHub never queues CI for a PR in CONFLICTING/DIRTY mergeable state — merge conflicts silently block CI from starting. Always check PR mergeable state first (\`gh pr view --json mergeable,mergeStateStatus\`) before investigating CI config. Confirmed in PR #967 (main advanced 3 PRs) and PR #968 (main advanced via PR #971 after branch push). User directive: 'if CI does not start on a PR, it is probably due to merge conflicts on the PR.' Also: a top-level \`paths:\` filter in the workflow would block the trigger entirely — but BYK/loreai ci.yml has NO top-level paths filter; \`dorny/paths-filter\` only gates individual jobs, not the workflow trigger.

<!-- lore:019ecab6-f928-7927-a39e-d601fb850af6 -->
* **computeMaxTokens: EMA-based floor ignores thinking budget, causing silent agent loop exit**: \`computeMaxTokens\` EMA floor ignores thinking budget → silent agent loop exit. \`computeMaxTokens\` (pipeline.ts) floors \`max\_tokens\` at \`max(8192, 3×outputEMA)\`. Anthropic counts \`max\_tokens\` as combined thinking+visible budget. Model consumes entire 8192 on reasoning → \`finish:"length"\` → no text/tool part → agent loop exits silently. Fix: pass \`thinkingBudget\`; set \`baseFloor = thinkingBudget + THINKING\_OUTPUT\_HEADROOM (8192)\`, clamped by \`modelOutput\`. Gate: \`if (!isCC)\` — CC clients excluded; opencode does NOT send \`x-claude-code-session-id\` so fix applies. EMA not corrupted by thinking truncation (Anthropic includes thinking tokens in output\_tokens).

<!-- lore:019f1531-003a-7904-9346-4debc5a1e767 -->
* **Concurrent sessions switching branches mid-mutation: stash captured at wrong state — always verify stash contents before recovery**: Trap: a stash taken during mutation testing looks safe to pop later — it was just WIP. Fix: if a concurrent session switches branches between the mutation and the stash, the stash captures the mutated (broken) state, not the restored state. Before recovering any stash, diff stash contents against HEAD (\`git diff stash@{0} HEAD -- \<file>\`) to confirm which version is correct. Recovery pattern: selectively restore only the files that are correct in the stash (\`git checkout stash@{0} -- \<file>\`), skipping files where the working tree has the correct version. Record stash SHA before dropping (\`git stash drop\`) — recoverable via \`git stash store \<sha>\`.

<!-- lore:019efb79-a067-7947-b900-e8fd56134b27 -->
* **cost-bulk-queries.test.ts: \`not.toContain('SCAN t')\` is fragile — misses covering scans**: 🔴 Directive (June 24 2026): use \`not.toMatch(/SCAN t(?! USING)/)\` to reject heap scans in SQLite EQP tests — never bare \`not.toContain("SCAN t")\`. Rationale: SQLite reports index-only walks as \`SCAN t USING COVERING INDEX …\` — bare contains false-fails on valid covering plans. Negative lookahead passes covering scans and kills true heap scans. Also: deliberate decision NOT to add \`(created\_at, session\_id)\` index — SQLite planner always prefers session-ordered \`idx\_temporal\_session\` / \`idx\_distillation\_session\` scan for token-sum, distillation, and recent-session aggregates; \`(created\_at, session\_id)\` index is pure write amplification.

<!-- lore:019efb40-0000-7000-9000-000000000003 -->
* **creditWarmupHit three guards: paid (Bug A) + within-TTL + actually-read (Bug C); credit is binary not pro-rata**: \`creditWarmupHit(warmup, sinceWarmupMs, ttlMs, cacheReadTokens)\` attributes a warmup hit ONLY when ALL hold: (A) the session paid (\`totalWarmups>0 && lastWarmupRefreshTokens>0\` — \`lastWarmupAt\` alone rides along in restored blobs → phantom); within TTL (\`sinceWarmupMs < ttlMs\`); (C) the returning turn ACTUALLY READ the cache (\`cacheReadTokens > 0\`). Bug C fix: a turn that pivoted context / used a different tool chain / found the cache evicted reports \`cache\_read=0\` → no savings; without the gate, warmupHits/creditedTokens inflate and warp the ROI analysis gating future warming. Call site passes \`usage.cacheReadInputTokens ?? 0\` (same signal the adjacent 1h-TTL-savings branch gates on). The warmup is consumed (markers zeroed) BEFORE all three guards, so a no-hit still can't re-credit later. \`creditedTokens\` is the warmed prefix (\`refreshTokens\`, Bug B), NOT the returning read. Known residual: the read gate is BINARY — a partial read (0 < read < refreshTokens, e.g. breakpoint shift) still credits the full refreshTokens. Call-site threading (pipeline postResponse warmup-hit branch) is NOT seam-tested — hardcoding the 4th arg survives all warmup pipeline tests (tracked follow-up).

<!-- lore:019f04c9-271d-7e72-a91d-295518f5c7f6 -->
* **deleteSessionPromptDelta on compression wipes surfaced-set history — causes full cumulative re-render**: Trap: calling \`deleteSessionPromptDelta\` on a compressing turn looks correct — the stored \`insertAt\` is stale after gradient reshuffles indices. Fix: deleting wipes the \`mut\` surfaced-set history from all blocks; the next \`appendKnowledgePromptDelta\` call re-derives the entire cumulative pin→DB wall from the frozen baseline, growing larger each compression+change turn as consolidation tombstones/edits pinned entries — busting ~250K tokens of cache per turn. Fix (PR #1017): \`reanchorDeltaOnCompression()\` re-anchors (preserves \`content\` + \`mut\`, rewrites only \`insertAt\`) so \`advanceSurfacedKeys\` reconstructs the advancing surfaced set correctly and subsequent appends contribute only genuinely-new increments. PR #1013 trimmed the symptom (additive-only); PR #1017 fixed the root cause.

<!-- lore:019ef389-a501-7db4-a808-4e0c671bb2f0 -->
* **effectiveMetaThreshold: internal Date.now() makes tests flaky — inject nowMs parameter**: Trap: \`effectiveMetaThreshold(lastTurnAtMs)\` calling \`Date.now()\` internally looks correct — it needs the current time to compute \`gapMs\`. Fix: any test that captures \`const now = Date.now()\` then passes \`now - (DEEP\_IDLE\_MS - 1)\` as \`lastTurnAtMs\` is flaky — a ≥1ms scheduling delay pushes \`gapMs ≥ DEEP\_IDLE\_MS\`, flipping the return value. Fix (PR #926): add injectable \`nowMs\` parameter; tests pass a fixed \`NOW\` constant. Sentinel: \`lastTurnAtMs === 0\` means 'never set (first turn)' → treated as deep-idle (gapMs = Infinity). All 8 \`effectiveMetaThreshold\` tests rewritten to use fixed \`NOW\` — fully deterministic.

<!-- lore:019f17c1-9ed1-7a80-9420-dcaa4afb4426 -->
* **embedding OOM test flake: trust-band rejects seeded cap under parallel CI load**: Trap: seeding \`\_persistEmbedCap(8192)\` (no \`freeMemBytes\` arg) in OOM recovery tests looks correct — it sets the stored cap to model max. Fix: \`reconcileEmbedCap\` only trusts stored cap when \`freemem()@construct / freemem()@persist\` is within \`\[0.75, 1.25]\`. Under heavy parallel CI load, free memory swings >25% between the two reads → provider starts at freemem-derived memory-model cap (~2.3K) instead of 8192 → \`AssertionError: expected 2532 to be 8192\`. Fix (PR #1054): seed with \`\_persistEmbedCap(8192, 0)\` — \`freeMemBytes=0\` forces \`ratio=1\`, making start cap independent of host free memory. Production unchanged: both production \`persistEmbedCap\` call sites pass explicit \`free\` arg.

<!-- lore:019f17ff-e574-7d90-a822-12d2f36cf4f2 -->
* **embedding-worker.ts console.warn bypasses log.silenceStderr — separate globalThis in worker\_threads**: Trap: \`log.silenceStderr()\` silences all stderr output — looks like it covers the entire process. Fix: \`embedding-worker.ts\` runs inside a \`worker\_threads.Worker\` with its own separate \`globalThis\`. The silence flag set in the host process cannot reach the worker's globalThis. Any \`console.warn\` in the worker auto-pipes to host stderr (default behavior when \`stderr:true\` not set). Fix requires either propagating the flag via \`workerData\` at spawn time, or spawning with \`stderr:true\` and draining the stream. Known gap as of PR #1050.

<!-- lore:019f0504-8ff3-7c36-ab81-6deb230b5de7 -->
* **entities.ts unbounded IN clauses: batchLoadAliases and knowledgeRefCounts must chunk at 900 — SQLite param limit**: entities.ts unbounded IN clauses: \`batchLoadAliases\` and \`knowledgeRefCounts\` must chunk \`entityIds\` at 900 per query and union results — SQLite's bind-parameter limit is ~32766 but large projects easily exceed it. PRs #1018, #1020. Offloaded variants \`batchLoadAliasesOffloaded\` and \`getManyWithAliasesOffloaded\` share the same unbounded-IN shape — tracked in issue #1022. If a BLOB column is ever added to \`entity\_aliases\`, \`SELECT \*\` must switch to explicit non-BLOB column list to avoid copying BLOBs across worker boundary.

<!-- lore:019f1811-5b05-7283-b3b6-f82ae4324373 -->
* **gateway re-compression: mirrors client encoding regardless of upstream support — latent protocol-translation risk**: Trap: \`encodeUpstreamBody\` re-compresses with whatever encoding the client sent (e.g., zstd) before forwarding upstream — looks correct because the client proved the encoding works. Fix: upstream may differ from client target on protocol-translation/provider-routing paths and may reject zstd. Sending uncompressed upstream is strictly safer — no correctness need to re-compress. Practically narrow today (only Codex compresses; stays \`openai-responses\`→chatgpt backend which accepts zstd), but the invariant is unverified. Additionally, the re-compression wiring in \`pipeline.ts\` has no integration test — \`harness.ts\` observes upstream body via parsed-object arg, never real \`BodyInit\`, so nothing asserts upstream receives compressed bytes + correct \`content-encoding\` header while \`serializedBody\` stays uncompressed. (PR #1049 SHOULD-FIX findings.)

<!-- lore:019ee562-c70c-7fa5-b9f3-7f60a3c4b578 -->
* **getCircuitBreakerStatus: status getter must be pure read — no side-effect delete on decay**: getCircuitBreakerStatus must be pure read — no side-effect delete on decay. Trap: original called \`isCircuitBreakerTripped\` which deletes entry on decay, then re-read — just-decayed bucket returned \`trippedAt:0\`, indistinguishable from never-tripped. Also mutated in-memory map and wrote to SQLite on dashboard read path. Fix (PR fix/circuit-breaker-status-pure-read): rewritten as pure read — computes decay inline, reports decayed bucket as \`{tripped:false, failures:0, trippedAt:0}\`, no map mutation, no SQLite write. Rule: status/summary getters must never have delete/write side effects.

<!-- lore:019eea52-a1f7-757a-b0ae-6b749bbcda18 -->
* **gradient.ts compression stages: rawFrac applied to full usable — escalating layers inflate window past l0cap**: Trap: COMPRESSION\_STAGES rawFrac (layer-2=0.5, layer-3=0.55) applied to full \`usable\` (~800K on 1M model) → layer-2 raw budget ~400K, larger than the ~200K l0cap that triggered compression AND larger than layer-1 window; escalating GREW the window until unrecoverable 400. Fix (PR #872, merged 2026-06-21T14:18:47Z, squash \`4d7910aa\`): introduce \`stageBudgetCeiling = maxLayer0Tokens > 0 ? Math.min(usable, maxLayer0Tokens) : usable\`; \`stageBudgetUsable = Math.min(usable, stageBudgetCeiling)\`; apply to escalated stages (layers 2+) only. Layer-1 base \`rawBudget = usable\*0.4\` must NOT be clamped. \`isEscalated = stageLayer > 1\`. Also clamp escalated-stage distilled budget. Confirmed in production logs: post-deploy layer-2 ratio dropped from ~0.499 (unclamped usable×0.5) to ~0.287–0.321 (clamped cap×0.5). M1–M4 mutant kills documented in test suite.

<!-- lore:019ef645-2484-71dd-bf92-eccd8e962083 -->
* **insertMeta ON CONFLICT must not update base\_confidence — immutable create-time anchor**: Trap: \`insertMeta\` uses \`ON CONFLICT(logical\_id) DO UPDATE SET base\_confidence = excluded.base\_confidence\` — looks correct for idempotent re-create. Fix: \`base\_confidence\` is the immutable create-time anchor for PN-counter deltas; overwriting it on conflict corrupts all future confidence calculations. The conflict path only occurs when \`create()\` is called with an explicit \`input.id\` that already exists (deduplication check skipped). Fix: remove \`base\_confidence = excluded.base\_confidence\` from the DO UPDATE clause. Confirmed in PR #1126 fix commit \`36173a7\`.

<!-- lore:019ef35a-01ce-7c91-a77a-e81e9f1f537b -->
* **isVerifierCall: bare \`ci\` keyword matches \`npm ci\` (clean install) — false positive corrupts knowledge confidence**: Trap: \`(?:run\s+)?(?:test|...|ci|...)\` in \`VERIFIER\_LEADING\` looks correct — \`ci\` is a valid script name. Fix: \`npm ci\` (clean install, not a verifier) matches the optional-run group → false positive → confidence penalty. Fix (PR #927): move \`ci\` and other script keywords (\`verify\`, \`validate\`, \`e2e\`, \`coverage\`, \`spec\`) into a dedicated branch requiring explicit \`run\`/\`exec\`/\`dlx\` prefix: \`(?:npm|pnpm|yarn|bun)\s+(?:run|exec|dlx)\s+(?:test|ci|verify|...)\b\`. Bare \`npm ci\` no longer matches; \`pnpm run ci\` still does. \`ci\` retained for \`make\`/\`just\`/\`task\`/\`rake\` targets (always explicit). Confirmed via 51-case empirical test suite in PR #927 adversarial review.

<!-- lore:019efa1b-973b-7f5a-a92e-3be491e5b46f -->
* **libSQL DiskANN embedded npm build is impractical — 113s build for 5k vectors**: Trap: libSQL's \`vector\_top\_k\` ANN index looks like a drop-in replacement for sqlite-vec. Fix: the embedded npm addon builds DiskANN graph per-row on insert — 113s build for 5k vectors (~38 min for 100k), only 2× query speedup, 62% recall@10 at N=5k. Creating index before insert forces per-row graph update on disk (~273ms/insert). Genuine test requires libsql-server (Rust), not npm addon. Random uniform 768-dim vectors are worst case for ANN (real embeddings cluster better). Deferred to issue #955.

<!-- lore:019f17ff-e513-719e-95f9-98b66bda7d46 -->
* **log.silenceStderr globalThis: never leaks across tests — afterEach reset required**: Trap: \`log.silenceStderr()\` sets a flag on \`globalThis\` — looks like it would be isolated per-module or per-test. Fix: \`globalThis\` persists across tests in the same worker even after \`resetModules()\`. Without an \`afterEach\` reset in \`core/test/setup.ts\`, silence state leaks into subsequent tests, causing false-pass on TUI-safety assertions. The reset survives \`resetModules\` precisely because it targets \`globalThis\`, not the module cache. 🔴 Directive: \`afterEach\` reset is mandatory in any test suite that calls \`log.silenceStderr()\`.

<!-- lore:019ef947-54d5-73d6-8a1b-06575dddcf2f -->
* **Lore website: Vite transformIndexHtml is dead code for Astro static generation**: Trap: adding a Vite \`transformIndexHtml\` plugin to \`astro.config.mjs\` looks like the right way to rewrite HTML links at build time — Vite is Astro's bundler. Fix: Astro's static generation pipeline does NOT route through Vite's HTML transform hook. The plugin is silently ignored; links are never rewritten. Confirmed by grep of built HTML showing unmodified hrefs despite plugin being active. Use \`astro:build:done\` integration hook instead — it receives the final output directory and can rewrite HTML files directly.

<!-- lore:019f140e-5c70-7df9-8119-ea233b197eff -->
* **lore-hermes pytest suite: conftest.py stub is load-bearing — tests silently never ran before PR #1040**: Trap: the \`packages/hermes/tests/\` pytest suite looks like it was always wired into CI — test files existed in the repo. Fix: \`lore\_hermes.engine.LoreContextEngine\` subclasses \`ContextEngine\` from external \`agent.context\_engine\` (not vendored). Without \`conftest.py\` injecting a stub \`agent\` module into \`sys.modules\`, ALL 18 tests fail at collection with \`ModuleNotFoundError: No module named 'agent'\`. The suite genuinely never ran before PR #1040. Verified by removing \`conftest.py\` from a \`/tmp\` copy — all 18 tests fail immediately. The stub is not test scaffolding; it is a hard prerequisite for any test execution.

<!-- lore:019efc12-5bea-700b-87dd-615866d86741 -->
* **meta-distillation rewrites messages\[1] each turn — busts warmup prefix, turns partial warmups into $2–3 writes**: Trap: meta-distillation running while cache is warm looks safe — it's background work. Fix: meta archives gen-0 rows and creates a gen-1 row, rewriting the synthetic distilled prefix at \`messages\[0/1]\` on the next turn. That early-message rewrite is a real prompt-cache bust. Confirmed in production: session \`1EQ2VGLS1de9zcIH\` turn=76 showed divergence at \`messages\[1].content\[0].text\`, reason='distilled conversation prefix changed (meta-distillation rewrite)', forcing 100K+ token recreation per turn. Code comment invariant: 'Never run meta-distillation while the conversation cache is warm.' Idle-time meta in idle.ts remains enabled because the cache is already cold there. All distillation call sites in pipeline.ts pass \`skipMeta: true\` when cache is warm.

<!-- lore:019edb00-69ce-7fdb-873a-b0e7024a0323 -->
* **Mid-session LTM consolidation deletes entries, busting frozen system\[1] cache on next turn**: Mid-session LTM consolidation deletes entries, busting frozen system\[1] cache on next turn. Fix (PR #808, v45): \`stableLtmCache\` freezes system\[1] for the session's entire lifetime — NEVER recomputed from live knowledge table mid-session. Newly-curated preferences picked up only on NEXT session start. PR #814 adds consolidation retry-storm fix: cooldown armed only on successful no-op (not on throw), in-flight guard set before await and cleared in \`finally\`.

<!-- lore:019efa7b-7094-7654-87f4-0d5b7066beaa -->
* **node:sqlite must never be imported outside driver.node.ts — breaks Bun test runner**: Trap: importing \`node:sqlite\` directly in any \`@loreai/core\` source file looks correct — it's the standard Node SQLite API. Fix: Bun does not implement \`node:sqlite\`; the test runner runs against \`src/\` directly under Bun. Any direct import outside \`driver.node.ts\` breaks the entire test suite. All SQLite access must go through the \`#db/driver\` subpath import map, which resolves to \`driver.node.ts\` on Node and a Bun-compatible shim on Bun. \`allowExtension\` option (Node ≥23.5) and \`DatabaseSync\` are both gated inside \`driver.node.ts\` only. Corollary: \`logical\_id\` is the stable entry identity across versions (A2, #823); cross-reference linkages key on \`logical\_id\`, never the per-version \`id\`. For v1/unversioned rows, \`logical\_id\` equals \`id\`.

<!-- lore:019ef00b-bcc3-7a4a-9b4c-d524f62b9ffe -->
* **outcomeImpact: omitting verdict IN filter lets NULL rows clobber lastVerdict**: Trap: removing \`verdict IN ('pass','fail')\` filter from \`outcomeImpact\` SQL looks like an equivalent mutant — NULL-verdict rows aren't counted in pass/fail tallies because the JS loop only increments on explicit 'pass'/'fail'. Fix: \`lastVerdict\` uses \`MAX(created\_at)\` across verdict groups; a NULL-verdict row from a 'none' session with a later injection timestamp wins the MAX and overwrites \`lastVerdict\` with null. Confirmed: Mutation B survived all 25 tests; scratch test \`AssertionError: expected null to be 'pass'\` proved the bug. Coverage gap: no test mixes NULL-verdict rows with pass/fail rows under the same logical\_id. The filter is load-bearing for \`lastVerdict\` correctness even though it's invisible to pass/fail counts. Note: PR #906 dropped \`lastVerdict\` from \`OutcomeImpact\` entirely (misleading semantics); \`outcomeImpact\` now returns only \`{ passes, fails }\`. PR #921: \`outcomeImpact\` keyed by \`logical\_id\` in consolidation wiring.

<!-- lore:019ef39c-72c4-7a00-bce7-4456e479867c -->
* **Partial v55 migration boot-loop: DROP COLUMN before backfill SELECT causes unrecoverable crash**: Trap: v55 migration runs via \`database.exec()\` (auto-commits each statement) — looks safe because migrations are idempotent. Fix: if interrupted after \`DROP COLUMN confidence\` but before completion, \`schema\_version\` stays <55. Next boot retries migration; backfill \`SELECT logical\_id, confidence FROM knowledge\` throws \`no such column: confidence\`. Catch block only matches \`/duplicate column name/i\` — this error re-throws → \`migrate()\` throws → boot loop. \`recoverMissingObjects\` unreachable (needs \`current>=55\`). \`stripAppliedAlters\` only handles ADD COLUMN. Fix: make backfill column-presence-aware (mirror \`recoverMissingObjects\`' \`hasConfidence\` check) OR wrap the DROP+backfill in a single transaction-safe block. Empirically confirmed with \`node:sqlite\`.

<!-- lore:019f0134-e838-7cb6-ab45-8d517460b17b -->
* **pattern-echo.ts throttle: arm cooldown unconditionally, not only after successful ltm.create**: Trap: arming the 10-minute cooldown (\`PATTERN\_COOLDOWN\_MS\`) only after a successful \`ltm.create()\` call in \`pattern-echo.ts\` looks correct — why throttle if nothing was created? Fix: if embed+search runs but \`ltm.create()\` fails or returns no-op, the cooldown is never armed → embed+search runs again on every subsequent distillation, burning compute. Additionally, rate-limited segments were skipping the embed entirely, creating a latent recall gap (segments never became vector-searchable). Fix (PR #1009): reorder \`\_detect()\` so embed+store (job 1) runs unconditionally BEFORE the rate-limit check; arm cooldown unconditionally immediately after the check passes (line 128), not inside the \`ltm.create()\` try block. Two bugs fixed: (a) no-pattern outcome never armed cooldown; (b) rate-limited segments skipped embedding.

<!-- lore:019eeee4-fb23-717d-9e76-99443d234d8e -->
* **PR #902 outcome-reward: confidence < ceiling guard prevents demotion, not just over-boost**: Trap: \`confidence < OUTCOME\_BOOST\_CEILING\` guard in PASS block looks like it only prevents over-boosting entries already above ceiling. Fix: without the guard, \`MIN(0.8, 0.95+0.02)=0.8\` silently demotes a 0.95 entry to 0.8 on every passing session — flattening the entire upper confidence band. Existing test uses confidence exactly at ceiling (0.8), so \`MIN(0.8, 0.82)=0.8\` is identical with/without guard — Mutant B survived all 19 tests. 🔴 Fix shipped (PR #902): added test for high-confidence entry (0.95) in passing session — must NOT be demoted to ceiling. Guard's second role (demotion prevention) now tested. Also: \`is\_deleted = 0\` filter added to both UPDATEs in \`creditSessionOutcome\` to mirror \`knowledge\_current\` view.

<!-- lore:019eff67-7994-761e-87db-410f16953690 -->
* **Probe exit-status trap: last \`git grep -q\` returns 1 on miss → marks whole probe as error**: Trap: ending the symbol probe section with a \`git grep -q\` call looks correct — it's the last check. Fix: \`git grep -q\` exits 1 when the symbol is absent; if it's the last command in the probe subshell, the subshell exits 1 → \`isError=true\` → entire probe discarded → \`presentSymbols=null\` (neutral, no penalty). Fix: append \`true\` as the final command in the probe subshell to force exit 0 regardless of last grep result. Confirmed by regression test in \`references.test.ts\`.

<!-- lore:019ef127-e5db-75e2-ae43-fc4359277234 -->
* **Property test headersArb never generates literal 'accept' — GATEWAY\_MANAGED\_HEADERS clobber bugs invisible to property tests**: Trap: property tests with \`headersArb\` generating random header names matching \`^\[a-z]\[a-z0-9-]\*$\` look like they cover header-forwarding edge cases. Fix: random generation will never produce the literal string \`'accept'\` (or other specific gateway-managed names). Example tests only inject \`anthropic-\*\` headers. Any bug where a specific header name is clobbered (e.g. Bedrock \`accept\` override, PR #898 MUST-FIX #2) is invisible to property tests — requires a targeted deterministic test injecting that exact header name.

<!-- lore:019eec9d-45ee-78ac-abc6-22fecf2e35da -->
* **provider-aware auth guard: must exempt config.workerApiKey or LORE\_WORKER\_API\_KEY users lose all background work**: Provider-aware auth guard must exempt \`config.workerApiKey\` or \`LORE\_WORKER\_API\_KEY\` users lose all background work. Trap: adding \`if (model && !resolveAuth(sessionID, model.providerID)) return\` to \`scheduleBackgroundWork\` (pipeline.ts) and idle loop (idle.ts) looks complete. When \`config.workerApiKey\` is set, \`getWorkerAuth\` bypasses \`resolveAuth\` entirely — the guard wrongly skips all background work for dedicated-key sessions. Fix: add \`!config.workerApiKey &&\` to both guard conditions (or check \`loreConfig().workerApiKey\` first and skip the \`resolveAuth\` check). \`config\` is in scope at both sites. Confirmed in PR #895 MUST-FIX #1 adversarial review.

<!-- lore:019ef645-249a-7099-9b79-a286babbb764 -->
* **pruneOversized uses clamped confidence — CRDT hysteresis leaves raw value > 1.0 unzeroed**: Trap: \`pruneOversized\` reads \`confidence\` from \`knowledge\_current\` (materialized, clamped to \[0,1]) and applies \`-confidence\` delta — looks correct for zeroing. Fix: CRDT deltas accumulate on \`base\_confidence\`; raw value can exceed 1.0 while materialized stays clamped at 1.0. Applying \`-1.0\` delta leaves raw > 0 → entry not actually pruned, GC fails silently. Fix: fetch raw unclamped value (\`base\_confidence + SUM(deltas)\`) and apply negative delta equal to that raw sum. Confirmed in PR #1126 fix commit \`36173a7\`.

<!-- lore:019f157d-82b8-7e7f-b602-16c36e379c36 -->
* **recall.ts graph expansion: entity visibility filter is untested — removal leaves all tests green**: Trap: the neighbor-entity visibility filter at \`recall.ts:1236-1240\` (\`ent.project\_id === gpid || ent.project\_id === null || ent.cross\_project === 1\`) looks symmetric with the knowledge visibility filter and appears covered by the same test suite. Fix: Mutation 4 (PR #1047 review) confirmed removing the entity filter leaves all 10 graph tests green — zero failures. The knowledge filter IS guarded (Mutation 3 kills 1 test). A neighbor entity from another project could leak if the filter were accidentally removed. A dedicated test seeding a cross-project entity neighbor and asserting it does NOT appear in results is required to close this gap.

<!-- lore:019f157d-82f8-77b3-825a-c5ea7af045e7 -->
* **recall.ts graphExpand: synchronous ltm.getByLogical() on hot path reverses #966 off-thread pattern**: Trap: \`ltm.getByLogical(gk.logicalId)\` called synchronously up to \`limit\*2\` (=20) times inside the graph-knowledge hydration loop in \`recall.ts\` looks correct — it's a simple lookup. Fix: PR #966 moved all DB reads off the main event loop to read-worker pool for latency reasons. \`graphExpand\` re-introduces synchronous main-thread DB reads on the recall hot path. No batched offloaded logical-id hydration exists yet. This is a perf risk (not a correctness bug) — under load, 20 synchronous SQLite reads block the event loop. A batched \`getManyByLogical\` offloaded variant is the correct fix (SHOULD-FIX, PR #1047 Finding F2).

<!-- lore:019f04b8-26f5-71fd-b8e2-e8dcca6e1c40 -->
* **recall.ts: getManyOffloaded must query knowledge\_current view, never base knowledge table**: Trap: querying \`SELECT ${KNOWLEDGE\_COLS} FROM knowledge WHERE id IN (...)\` looks correct — it's the base table and avoids view overhead. Fix: \`knowledge\_current\` is a view that filters superseded versions AND exposes \`confidence\` and other meta columns not present on the base \`knowledge\` table. Querying base \`knowledge\` causes \`Error: no such column: confidence\` at runtime. Mutation M1 (PR #1019 review) confirmed: reverting to base table kills 4 tests. Always use \`knowledge\_current\` in \`getManyOffloaded\` and any batch-hydration path.

<!-- lore:019efa17-e5b6-75a7-940b-7367c9de371e -->
* **references.ts codeSpanText: newline join fuses adjacent spans — cross-span phantom commands**: Trap: \`codeSpanText()\` joining all backtick code-span inner text with \`"\n"\` looks safe — newline is a natural separator. Fix: \`\n\` matches \`\s\` in \`PM\_CMD\_RE\`/\`MAKE\_CMD\_RE\`, so adjacent spans like \`\` \`make\` \`\` and \`\` \`Makefile\` \`\` fuse into \`make Makefile\` → phantom command → confidence penalty. Examples: \`\` \`npm\` or \`yarn\` \`\` → \`npm yarn\`; \`\` \`make sense\` \`\` → \`make sense\`. Fix: split on lines first, run regexes per-line (renamed \`codeLines\`). Invariant: unverifiable refs must be strict no-op — false negatives (silent skips) are acceptable; false positives that trigger penalties are NEVER acceptable. Mutation-verified: reverting per-line split causes 2 fusion guard tests to fail. Fixed in PR #939 Round-2.

<!-- lore:019efa17-e60f-7a38-9bec-b1770ecb3aa6 -->
* **references.ts: make/yarn unresolved refs must resolve to 'unknown', never 'missing' — no penalty on unverifiable commands**: Trap: resolving an unrecognized \`make \<target>\` or \`yarn \<script>\` as 'missing' looks correct — the target doesn't exist. Fix: commands extracted from code spans may reference targets in repos the gateway can't inspect. 'missing' triggers a confidence penalty; 'unknown' is a neutral no-op. Invariant: unverifiable refs must NEVER penalize — silent skip is always preferable to a false penalty. Same rule applies to symbol refs: absent symbol → \`"unknown"\` (NEVER \`"missing"\`); penalty only fires when \`wasSymbolPresent()\` confirms prior presence. \`DirectFsResolver\` and \`SyntheticProbe\` both return 'unknown' for unresolved make/yarn/symbol refs. PR #939 (file/command refs), PR #988 (symbol refs). PR #973: file-reference resolution case-insensitive; case-collision → \`"unknown"\` (neutral).

<!-- lore:019eeb34-c8ab-7321-98ae-f8cbd3953ad4 -->
* **resetPipelineState: fire-and-forget background promises write to new harness DB — test flake trap**: resetPipelineState: two-phase teardown required — stop idle scheduler and drain background work BEFORE swapping db() singleton. (1) Correct order: stopIdleScheduler() + null it → await drainBackground(5000) → await boundedSettle(\[...inFlightBackground], 5000) → llmClient.shutdown → swap/clear state. Double stopIdleScheduler is safe (fast path skips top block; later block is no-op if already nulled). Trap A: stopping after llmClient.shutdown lets idle ticks schedule new runBackground during drain. (2) Trap B: fire-and-forget background promises write to new harness DB — urgent/incremental distillation, saveSessionTracking INSERT OR IGNORE create phantom rows in next harness (~1/3639 flake, issue #885). Fast path skips drain; production shutdown uses fast to avoid Ctrl+C hang. \`boundedSettle\` is a shared helper in \`background-limiter.ts\`.

<!-- lore:019efa7b-70b5-7f11-9a79-9d8d3d47c348 -->
* **sqlite-vec excludeCategories on vec path: untested coverage gap — mutation survives 142 tests**: sqlite-vec excludeCategories on vec path: NOW TESTED (PR #967 Round-2, commit b58a7d93) — mutant killed. \`excludeCategories\` filtering on knowledge vec SQL path (\`embedding.ts:1282-1285\`) was previously untested because \`ltm.test.ts\` excludeCategories tests don't populate embedding columns → code fell back to FTS path. Fix: new tests in \`vec-search.test.ts\` insert knowledge rows with explicit \`embedding\` BLOBs across multiple categories, call \`vectorSearch(query, limit, excludeCategories)\`, assert excluded categories absent and included categories present. Tests must be run via full-file vitest — filtered \`-t\` runs produce false-pass because parity describe's afterAll cleanup doesn't run. Mutation-verified load-bearing: removing vec-branch excludeCategories clause kills "excludes the named category on the vec path"; removing JS-branch excludeCategories clause kills "excludes the named category on the JS fallback path".

<!-- lore:019efa1b-971e-7ae9-805d-396a35489300 -->
* **sqlite-vec vec0 rowid must be bound as BigInt — node:sqlite passes numbers as float64**: Trap: binding loop variable \`i\` as a JS number for \`vec0\` rowid looks correct — SQLite integers accept numbers. Fix: \`node:sqlite\` passes JS numbers as float64; \`vec0\` requires integer primary key values as BigInt. Binding a float64 causes \`ERR\_SQLITE\_ERROR: Only integers are allowed for primary key values on v\`. Fix: bind as \`BigInt(i)\`. Discovered during ANN benchmark at N=10,000.

<!-- lore:019ef02c-f197-7282-8eb5-13a894b0cd15 -->
* **startGateway reuse probe: EADDRINUSE-gated probe misses non-overlapping interface binds**: Trap: placing existing-gateway reuse probe inside \`EADDRINUSE\` catch looks correct — port conflict is the only reason to check. Fix: when new instance binds non-overlapping interface (existing on \`127.0.0.1\`, new on \`127.0.0.2\`), bind succeeds, catch never fires, second redundant gateway starts. Fix (PR #908/#920): \`firstReachableHost(hosts, port, probe)\` probes all hosts concurrently via \`Promise.all\`. Pre-bind probe runs for each \`candidatePort !== 0\` BEFORE bind; post-bind EADDRINUSE catch retained as TOCTOU backstop. \`runDaemon()\` probes all configured hosts + \`127.0.0.1\` fallback. \`opts.local===true\` always disables remote mode. Port 0 skipped correctly. Both pre-bind and post-bind reuse blocks use \`firstReachableHost\` + pin \`config.hosts = \[reachableHost]\` so callers see the actual reachable host. All host→URL interpolations in CLI must use \`bracketHost\` for IPv6 (PR #912). Mutation A (disable pre-bind probe) killed by test 3 in start-gateway-reuse.test.ts; Mutation B (single-host daemon probe) killed by daemon-args.test.ts; Mutation C (disable hosts pin) killed by \`expect(handle.config.hosts).toEqual(\["127.0.0.1"])\` assertion.

<!-- lore:019eff67-7977-7ec5-bfc2-e55be71237ee -->
* **Symbol grep must run from git toplevel with scrubbedGitEnv — subdir and GIT\_DIR env both corrupt results**: Trap: running \`git grep\` from \`this.root\` (project root) looks correct — it's where the code lives. Fix: in a monorepo, \`this.root\` may be a subdirectory; grep from a subdir misses symbols defined elsewhere. Always \`cd $(git rev-parse --show-toplevel)\` first. Second trap: \`GIT\_DIR\`/\`GIT\_WORK\_TREE\` env vars inherited from the agent's shell override git's working-tree detection, causing grep to search the wrong tree. \`scrubbedGitEnv()\` strips both vars before every git subprocess. Both traps look safe because grep succeeds (exit 0) — it just silently searches the wrong scope.

<!-- lore:019eeece-ef1d-7108-a182-be70680a232a -->
* **sync convergence oracle insufficient — strict conflicts==0 property required to surface false-conflict BLOCKER**: Trap: asserting only that local==remote after fixpoint drain looks sufficient for sync correctness. Fix: fixpoint drain eventually converges even when \`classifyRemoteRow\` produces false conflicts (remote-wins LWW still applies correct content). The BLOCKER (spurious conflicts on versioned entries) is invisible to convergence-only assertions. Only a strict \`conflicts==0\` property surfaces it. Confirmed: reverting the classify fix, convergence properties all pass (4/5); only the no-ping-pong \`conflicts==0\` property fails. Always include a strict zero-conflicts assertion in sync property tests, not just convergence.

<!-- lore:019edff0-4721-7514-9ddf-f9eac54de7da -->
* **sync integration tests: hand-written mocks cannot exercise RLS/triggers/quotas — integration tests are the only valid gate**: sync integration tests: hand-written mocks cannot exercise RLS/triggers/quotas — integration tests are the only valid gate. \`sync-security.integration.test.ts\` runs against real Postgres+PostgREST via Docker; gated on \`LORE\_INTEGRATION=1\`. Test suites: (1) multi-tenant RLS isolation — cross-tenant read, scope\_id forge (42501), re-parent steal/donate, anon access; (2) tier server-only — column grant blocks tier UPDATE, service\_role can upgrade; (3) quota enforcement — row cap, byte cap, revival bypass, tombstone flood. \`FREE\_LIMITS\` constant mirrors 0003 caps. \`afterEach\` restores \`max\_rows\` but NOT \`max\_bytes\` — \`setByteCap()\` calls at lines 330, 346 leak into subsequent tests (benign due to test ordering; SHOULD-FIX). \`dockerReady()\` is synchronous. \`beforeAll\` timeout: 180,000ms. 30 concurrent INSERTs at \`max\_rows=10\`: exactly 10 committed, advisory lock serializes growth correctly.

<!-- lore:019eea22-b8cc-76d1-b547-eba90b324c95 -->
* **sync-data.ts reconcile(): stale upsert below push cursor blocks delete tombstone — row never deleted on remote**: sync-data.ts seedOutbox + reconcile(): stale upsert below push cursor blocks re-sync/delete — orphaned row bug. (a) seedOutbox: old \`NOT EXISTS (any pending outbox entry)\` skips rows with any pending entry including a trailing delete or stale already-pushed upsert (seq ≤ push cursor). Modified-while-sync-off rows never re-sync. Fix (PR #868): rewrite as JS loop; read \`latestUnpushed\` = latest op with \`seq > pushCursor\`; if upsert→skip; if delete→enqueue trailing upsert; if none→enqueue only when \`contentHash(row) != sync\_state.content\_hash\`. Wrap in \`withTransaction\`; hoist \`columns(table)\` outside per-row loop (\`syncedColumns\` runs PRAGMA on every call). (b) reconcile(): old guard blocks tombstone INSERT if ANY outbox entry exists including stale upsert that survived pruning (prune floor pinned by lower-seq table per #828). Stale upsert pushes as no-op → delete NEVER reaches remote. Fix (PR #866): change guard to \`COALESCE((SELECT op ... ORDER BY seq DESC LIMIT 1), 'none') <> 'delete'\` — only blocks if latest op is already a delete (idempotent). \`seq\` is \`INTEGER PRIMARY KEY AUTOINCREMENT\` so \`ORDER BY seq DESC LIMIT 1\` reliably identifies latest op.

<!-- lore:019ece19-d902-71c9-86d1-f51e74e8c6b9 -->
* **sync.ts drainTimestamp: keyset on idColumns\[0] only drops rows for composite-PK tables with >PAGE same-key entries**: Trap: \`drainTimestamp\` keysets by \`idc = idColumns\[0]\` (= \`knowledge\_id\`) for \`knowledge\_entity\_refs\`. If >200 refs share the same \`knowledge\_id\` + same \`updated\_at\`, rows beyond 200 are silently dropped — pagination terminates after 9 queries. Code comment at lines 322-324 claiming 're-scans the ms from the start … idempotent and loses nothing' is factually wrong. Empirically verified: single-group 250 refs (1 knowledge\_id, 1 ms) all applied correctly with composite keyset; multi-group 480 refs across 3 knowledge\_ids also correct. Fix: keyset on full composite \`(knowledge\_id, entity\_id)\` for composite-PK tables. \`\x1f\` (0x1f) sorts before any printable char — composite row\_id ordering is correct.

<!-- lore:019ece19-d8f2-7d7b-8b78-969756c830ae -->
* **sync.ts pushOnce: minCursor=0 from empty-table cursors blocks pruneOutbox forever**: Trap: \`pushOnce\` computes \`minCursor = Math.min(...per-table push cursors)\`. A synced table with zero outbox entries keeps cursor \`0\`. \`pruneOutbox(minCursor)\` early-returns when \`minCursor <= 0\` → outbox NEVER pruned whenever any one synced table has zero pending entries. Fix (implemented): compute \`minCursor\` only over tables where \`hasOutboxEntries()\` is true — empty tables excluded from floor calculation. Empirically verified: empty table does NOT pin floor; higher-cursor table with no peers fully pruned. Push-prune race is benign: rows inserted after cursors are read get \`seq > minCursor\` (AUTOINCREMENT monotonicity) → always survive prune.

<!-- lore:019f17c7-2006-7e3e-88b6-9220f2e4d7b9 -->
* **sync.ts: 9 console.error calls bypass silenceStderr() — unreachable by silence flag when sync enabled**: Trap: PR #1050 migrates ~18 gateway console.error sites to \`log.\*\`, appearing to cover all in-process stderr leaks. Fix: \`sync.ts\` has 9 \`console.error\` calls (lines 217, 266, 273, 285, 349, 422, 441, 590, 596) that were NOT migrated. \`sync.ts\` is reachable in-process: \`pipeline.ts:2061–2062\` dynamically imports \`./sync\` and calls \`startSyncScheduler()\`. Gated only by \`isSyncEnabled()\` (user opt-in), NOT by the silence flag. Lines 590 and 596 (quota reached + background cycle failed) fire in the scheduler tick — confirmed unmitigated stderr leak when sync is enabled in embedded mode. All 9 sites need migration to \`log.\*\` with \`isStderrSilenced()\` guards.

<!-- lore:019ee0df-cba3-7fab-9beb-db13431cf1a4 -->
* **SYNCED\_TABLES fan-out: adding a table has silent ripple effects with no contract test**: SYNCED\_TABLES fan-out: adding a table has silent ripple effects with no contract test. Adding a table to \`SYNCED\_TABLES\` (e.g. \`profiles\`) has ripple effects across \`seedOutbox\`, \`reconcile\`, \`pruneOutbox\`, \`applyRemote\` pull-classification, and outbox-capture triggers. No single contract test enforces all invariants for every registered table. \`profiles\` silently violated the prune-floor and tier-residue invariants on paths nobody re-checked after registration. Fix: add a per-table invariant test that asserts all registry-required properties (pullOnly guard in seed/reconcile, no outbox trigger, correct pull classification) whenever a new table is added to \`SYNCED\_TABLES\`.

<!-- lore:019f03c2-0d94-77a6-bbe8-f7fae06c639e -->
* **temporal.searchScored: must use lean column list, never SELECT m.\* — embedding BLOB forbidden at worker boundary**: Trap: \`SELECT m.\*\` in \`temporal.searchScored\` looks correct — it's the standard pattern used in sync \`temporal.search()\`. Fix: \`embedding BLOB\` is added by migration 449 and is NOT in \`TemporalMessage\` type, but \`SELECT m.\*\` would include it. BLOBs must never cross the read-worker boundary (read-job contract in \`read-job.ts\`). Lean column list: \`m.id, m.project\_id, m.session\_id, m.role, m.content, m.tokens, m.distilled, m.created\_at, m.metadata, rank\`. Same applies to \`ltm.searchScored\`/\`ltm.searchScoredOtherProjects\` — use \`KNOWLEDGE\_COLS\_K\` which already excludes \`embedding\`. In vec0 mode, \`embedding\` column is dropped from base tables entirely — \`SELECT \*\` would throw \`no such column: embedding\` if column was re-added. Always use explicit column lists in read-worker queries. Mutation M3 confirmed: reverting to \`SELECT m.\*\` kills the test.

<!-- lore:019efa1b-9771-70f1-ba6a-f6eccead4374 -->
* **undici is external in Bun ESM bundle — real undici@7 hangs on streaming reads under Bun**: Trap: bundling \`undici\` into the Bun ESM gateway bundle looks correct — it's a Node.js HTTP client. Fix: real \`undici@7\` hangs on streaming response reads under Bun. \`undici\` is lazily imported only on the Node path in \`fetch.ts\` — never bundled or evaluated under Bun. Bun path uses native fetch instead. \`undici\` must remain external in the Bun ESM bundle.

<!-- lore:019f14c0-7cf1-7dd2-be90-a34480c59da6 -->
* **vec-store.ts: embedding NEVER transferred across worker boundary — structured-clone only**: Trap: transferring the \`Float32Array\` embedding across the worker boundary looks like a performance win (avoids copy). Fix: transferring detaches the caller's buffer and corrupts the in-process fallback that reuses it. The embedding (~3 KB for 768 dims) is always structured-cloned, never transferred. This is an explicit design invariant documented in \`vector-worker.ts\`. Do not add \`Transferable\` to the embedding field in \`VectorQuerySpec\`.

<!-- lore:019f1717-d2be-7121-8a47-9c925728001e -->
* **vec0 storeEmbeddingVec0: distillations and temporal require base-row lookup before INSERT**: Trap: \`storeEmbeddingVec0\` for \`distillations\` and \`temporal\` tables looks like a simple INSERT — but both vec0 tables have PARTITION KEY columns (\`project\_id\`, \`session\_id\`) that must be populated. Fix: for \`distillations\`, reads \`project\_id, session\_id\` from the base \`distillations\` row first; skips if row is gone (race with delete). For \`temporal\`, reads from \`temporal\_messages\`. \`knowledge\` and \`entities\` use simple \`INSERT OR REPLACE INTO \<t>\_vec(id, embedding)\` — no base-row lookup needed. \`temporal\` chunk\_id = \`${id}#0\` (single-chunk era). Missing the base-row lookup would insert NULL partition keys, breaking partition-scoped queries.

<!-- lore:019f1717-d283-7ea1-9a56-c2acb82924dc -->
* **vec0-cutover hybrid-state boot-loop: checkConfigChange throws on partial cutover + config change**: Trap: \`maybeCutoverToVec0\` is non-transactional — if an exception hits after the first \`dropEmbeddingColumn\` but before \`setStorageMode("vec0")\`, the DB is left in hybrid state (mode=blob, columns already dropped). On next startup WITH a pending config change, \`checkConfigChange\` runs first and calls \`countBlobEmbeddings\`, which reads the dropped \`embedding\` column → throws \`no such column: embedding\`. Stored fingerprint never updates → every subsequent restart re-throws → permanent boot-loop degrading vector recall to empty. Self-heals only if user reverts embedding config to match stored fingerprint. Fix: make \`countBlobEmbeddings\` presence-aware (check column existence before reading), OR run cutover before \`checkConfigChange\`, OR wrap copy-drop-setMode in a transaction. Empirically reproduced in \`/tmp/opencode/pr1051-mut\` repro test.

<!-- lore:019f0052-45d6-7e13-a065-0a0b051f01c7 -->
* **Vertex global endpoint: bare aiplatform.googleapis.com, not global-aiplatform.googleapis.com**: Trap: \`vertexRawPredictUrl(region, ...)\` building \`${region}-aiplatform.googleapis.com\` looks correct — all regional endpoints follow that pattern. Fix: \`region="global"\` must use bare \`aiplatform.googleapis.com\` (path still uses \`locations/global\`). \`global-aiplatform.googleapis.com\` resolves via DNS but returns HTTP 404. Since \`global\` is the default \`LORE\_VERTEX\_REGION\`, the default config would 404 every call without this fix. \`VERTEX\_HOST\_RE = /^(?:(\[a-z0-9-]+)-)?aiplatform\\.googleapis\\.com$/\` handles legacy \`global-aiplatform\` form (self-heals). \`vertexHost()\` helper centralizes host construction. Design limitation: X-Lore-Upstream-URL with a non-Vertex host → \`vertexRegionFromUrl\` returns null → region falls back to configRegion → proxy host silently discarded; Vertex cannot be proxied via X-Lore-Upstream-URL (intentional, documented by test). 🔴 NEVER produce the dead \`global-aiplatform\` host.

<!-- lore:019eff03-2426-75f6-9539-c9db86f8600a -->
* **Vertex: GCP bearer token must NEVER ride along on non-Vertex requests**: Trap: the Vertex auth path mints a GCP OAuth2 bearer token and sets \`Authorization: Bearer \<token>\` — it looks like a normal auth header that could be forwarded. Fix: \`executeWarmup()\` and \`buildVertexWorkerRequest()\` both skip \`resolveAuth()\` entirely and mint the GCP token only for Vertex branches. \`forwardClientHeaders()\` strips \`authorization\` before snapshot storage, so the token is never persisted. \`effectiveProtocol === 'anthropic'\` gate ensures billing re-signing (\`cch\`) never fires for Vertex. The \`isVertexHost()\` check in \`resolveProfile()\` and \`vertexRegionFromUrl()\` regex (\`\[a-z0-9-]+\`) constrain the token destination to \`\*.aiplatform.googleapis.com\` — it cannot be sent to an arbitrary domain even if config is misconfigured.

<!-- lore:019ef389-a52f-7427-9099-f8dea38e8caf -->
* **vi.mock('@loreai/core') doesn't intercept pipeline imports — vitest alias resolution mismatch**: Trap: \`vi.mock('@loreai/core', ...)\` to spy on exports like \`recordCacheUsage\` looks correct — it's the package specifier pipeline uses. Fix: vitest config aliases \`@loreai/core\` → \`packages/core/src\` at \`resolve.alias\` (top-level, NOT \`test.resolve.alias\` — wrong placement silently resolves to stale dist). The mock targets the specifier but pipeline resolves to the source path, creating a module identity mismatch — spy is never invoked (call count stays 0). No clean workaround for aliased barrel mocks; use e2e harness tests or file as tracked follow-up instead.

### Pattern

<!-- lore:019f00ae-c762-7067-8197-83d9b70d8dd9 -->
* **cache-warmer evidence gate (Bug C): always fund first probe, never fund second+ cycle with zero lifetime hits**: 🔴 Directive: ALWAYS fund the first warmup of a break (\`cyclesSpent === 0\` — the probe that learns if this slow tool returns within TTL), but NEVER fund a second-or-later cycle for a session that has produced zero confirmed hits over its lifetime. Implementation: \`if (cyclesSpent >= 1 && (state.warmup?.warmupHits ?? 0) < TOOL\_CALL\_MIN\_HITS\_FOR\_CONTINUATION) return false\`. \`warmupHits\` is lifetime/durable across breaks; \`warmupCount\` (cyclesSpent) is per-break (reset on return in pipeline.ts). Rationale: 66 of 74 zero-hit warmups occurred at ≤5 lifetime warmups, below \`MIN\_WARMUPS\_FOR\_ROI\_CHECK\`, so hit-rate guard never engaged.

<!-- lore:019efc12-5c34-7395-8ed7-b70ab32481a8 -->
* **cache-warmer invariants: probe always funded, zero-hit sessions blocked after MIN\_WARMUPS\_FOR\_ROI\_CHECK**: Three invariants enforced in \`shouldWarm()\` (cache-warmer.ts): 1. \*\*Prefix-churn gate\*\* (\`prefixChurnEma >= PREFIX\_CHURN\_WARM\_BLOCK=0.40\`) blocks ALL warming paths including first-probe and tool-call fast path — no carve-out. Gate sits before \`forceKeepWarm\` check. \`getPrefixChurnRate\` uses \`sessionStates.get(...)?.\` (non-creating). EMA rises on \`bustCause==='prefix-rewrite'\` turns, decays on clean turns (\`PREFIX\_CHURN\_ALPHA=0.35\`). NIT: rewrite/clean/rewrite/clean steady state settles EMA ≈0.394 < 0.40 — warmup can occasionally slip through on the dip (caught by breaker as defense-in-depth). 2. \*\*Zero-hit sessions blocked after \`MIN\_WARMUPS\_FOR\_ROI\_CHECK=5\`\*\* — never fund second-or-later cycle for zero confirmed lifetime hits (evidence gate). 3. \*\*Sub-agent sessions always exempt\*\* — too short-lived for warming to be profitable, even with /lore:warm:keep force. 4. \*\*Beyond \`maxCycles\` always unprofitable\*\* — even with /lore:warm:keep. \`writeEfficiencySamples\`/\`warmupWriteEfficiencyTooLow()\` removed in PR #995.

<!-- lore:019ef39c-72e1-77c0-b39c-2c145ca5fd05 -->
* **knowledge\_meta invariants: sync-silence for markInjected, content-only update must not churn sync clock**: Three sync-silence invariants for \`knowledge\_meta\`: (1) \`markInjected()\` updates \`last\_reinforced\_at\` but NEVER bumps \`updated\_at\` — selection ≠ certainty, mirrors prior HASH\_EXCLUDE behavior. (2) Content-only \`update()\` (no \`input.confidence\`) must NOT bump \`meta.updated\_at\` — only confidence changes churn the sync clock. (3) \`create()\` calls \`insertMeta(id, confidence, now, now)\` with \`ON CONFLICT DO UPDATE\` — idempotent for cross-machine re-create but never destroys a converged confidence value (fresh UUIDv7 logical\_id guarantees no collision in practice). A \`knowledge\_meta\` row with no live \`knowledge\_current\` row is inert — never crashes a read.

<!-- lore:019eec48-01c5-78bd-b7f9-6cd8b7ddfada -->
* **Lore gateway deploy procedure on production host**: 🔴 Deploy procedure (Burak Yigit Kaya directive): \`git stash save && git pull && git stash pop && pnpm install && pnpm build && pnpm --filter @loreai/gateway run bundle && sudo service opencode restart && journalctl -u opencode -f\`. Plugin file at \`~/.config/opencode/plugins/lore.ts\` contains only \`export { LorePlugin as default } from "opencode-lore";\`. opencode-lore resolves from \`/home/byk/.config/opencode/node\_modules/opencode-lore\` (active built repo) — NOT the cached version at \`~/.cache/opencode/packages/opencode-lore@\*/\`.

<!-- lore:019eec2f-3f3b-724a-bb98-d2dcede676d1 -->
* **ltm.ts appendVersion: DEMOTE-FIRST transaction ordering enforced by UNIQUE index**: 🔴 Invariant (code comment, ltm.ts:233): 'Never leave two is\_current=1 rows for one logical\_id.' Enforced by UNIQUE index \`idx\_knowledge\_one\_current\` on (logical\_id WHERE is\_current=1). \`appendVersion()\` transaction uses DEMOTE-FIRST ordering: UPDATE is\_current=0 on old row BEFORE INSERT of new row with is\_current=1. Rationale: UNIQUE index is checked per-statement — inserting second current row before demoting would violate it. \`update()\` idempotency guard: \`appended\` flag set only when \`cur.content !== input.content\`; \`appendVersion()\` skipped for byte-identical content; re-embed gated on \`appended\` flag — prevents unbounded version growth. Migration/sync setup queries are 'never traced / re-entrant' — Proxy wrapping of DB connection happens AFTER \`migrate()\` and \`installSyncCapture()\` (db.ts lines 1472–1480). \`migrate()\` always normalizes version to \`MIGRATIONS.length\` after loop.

<!-- lore:019f14f5-5edb-7bd1-bc31-9b10062ce1a3 -->
* **Pi real-runtime e2e: console spy must be installed BEFORE resourceLoader.reload()**: Trap: installing \`console.\*\` spies after \`resourceLoader.reload()\` looks correct — the test body runs after setup. Fix: Pi's extension factory executes during \`reload()\`, so any \`console.\*\` calls in the factory are missed by late-installed spies. Additionally, Vitest intercepts \`console.\*\` away from \`process.stdout.write\`, so \`process.stdout/stderr.write\` spies also miss console output. Both traps look safe because the test passes — it just silently misses the mutation. Fix: install direct \`console.\*\` method spies BEFORE \`resourceLoader.reload()\` so the entire reload + \`createAgentSession\` + prompt window is captured. Mutation A (inject \`console.info("pi: ...")\` at line 99 of index.ts) confirmed this kills the markers test when spies are correctly placed.

<!-- lore:019f14f5-5f02-72d0-b8dd-975c07a43a17 -->
* **Pi SDK real-runtime e2e: Tier 1 test pattern with DefaultResourceLoader + createAgentSession**: Pi SDK real-runtime e2e pattern: \`DefaultResourceLoader({ extensionFactories: \[lorePiExtension] })\` → \`loader.reload()\` → \`createAgentSession({ resourceLoader, sessionManager: SessionManager.inMemory(), settingsManager: SettingsManager.inMemory({compaction:{enabled:false}}) })\`. Set \`LORE\_PI\_FORCE\_ACTIVE=1\` and \`LORE\_GATEWAY\_URL=baseURL\`. Assert \`extensionsResult.errors\` is empty (API-drift guard). Use \`const gwPkg = "@loreai/gateway"\` indirection for dynamic import (avoids TS2307). \`@earendil-works/pi-coding-agent\` is a real devDependency (^0.79.1). \`inMemory()\` variants avoid file I/O.

<!-- lore:019eebaf-0f5d-7d7b-a0c9-c8b93775c4d0 -->
* **pipeline.ts urgent distillation: skipMeta invariant and bypass rationale**: pipeline.ts urgent distillation: skipMeta invariant and bypass rationale: 🔴 Invariant (pipeline.ts:4457): 'Never run meta-distillation while the conversation cache is warm.' Urgent distillation always sets \`skipMeta: true\`. Idle-time meta (idle.ts) remains enabled (cache already cold). Urgent distillation bypasses \`isBackgroundPaused()\`, quota checks, and worker throttle — degraded/overflowing context for up to 10 min is worse than one API call. It is the ONLY direct (non-limiter) background DB writer; all other writers go through \`runBackground()\` / background limiter.

<!-- lore:019f147e-7b0d-7ed0-aef5-bb2b8c0bc065 -->
* **standard.site publish script: safety invariants for orphan cleanup**: Three invariants in \`publish-standard-site.mjs\` orphan cleanup (PR #1043, also ported to byk.github.io PR #58): 1. \*\*Upserts-first ordering\*\*: all \`putRecord\` calls complete before any \`deleteRecord\` — a mid-run failure never strands the blog without live records. 2. \*\*Empty-manifest guard\*\*: if \`manifest.documents.length === 0\`, skip orphan cleanup entirely with \`console.warn\` — a broken/empty build cannot wipe the whole collection. 3. \*\*Pagination termination\*\*: \`listAllRecords\` uses dual-condition stop — (a) stop when batch length is 0 even if PDS returns a cursor (PDS returns trailing cursor on last page — confirmed live), (b) stop when cursor is null/undefined. 4. \*\*DID cross-check\*\*: if \`session.did !== recordDid\`, script refuses to publish. 5. \*\*\`validate: false\`\*\* on \`putRecord\` — PDS doesn't host \`site.standard.\*\` lexicons. 6. \*\*Blob uploads\*\*: blobs are content-addressed (CID) → idempotent re-runs; best-effort — record published without image rather than failing; size cap \`MAX\_BLOB\_BYTES = 1\_000\_000\` (lexicon \`maxSize\`); raw source images may exceed cap but Astro-optimized webp variants are always under 1MB.

<!-- lore:019ef387-4afe-7137-a151-88dff1dd0ab7 -->
* **Test fix rules for knowledge\_meta migration (RULE A and RULE B)**: Two patterns for fixing tests after the v55 knowledge\_meta migration: RULE A — remove hardcoded \`confidence\` column from raw \`INSERT INTO knowledge\` statements; add \`logical\_id\` column+value (= id for v1 rows). RULE B — split combined \`UPDATE knowledge SET cross\_project=1, confidence=X, last\_reinforced\_at=Y\` into two statements: one targeting \`knowledge\` (cross\_project), one targeting \`knowledge\_meta\` (confidence/last\_reinforced\_at + updated\_at). Also: any test asserting confidence must read from \`knowledge\_meta\` or \`knowledge\_current\` view, never from the base \`knowledge\` table. Helper pattern: \`confOf(id)\` queries \`knowledge\_meta WHERE logical\_id = ?\` (not \`knowledge WHERE id = ?\`). Test JOIN pattern: \`SELECT m.confidence FROM knowledge k JOIN knowledge\_meta m ON m.logical\_id = k.logical\_id WHERE k.id = ?\`.

### Preference

<!-- lore:019eec5c-fa9c-7993-ad0a-f44f68400fde -->
* **Adversarial review heuristics include vacuity check — tests that don't fail on revert are vacuous**: When performing adversarial review \[\[019eec15-9385-79f3-b2a5-fc3928ccd1b7]], always include a vacuity check: temporarily revert the code fix the test claims to guard, run the test, confirm it FAILS. A test that passes against known-broken code is a non-test. Worktree technique: create detached-HEAD worktree at \`/tmp/pr-mut\`, symlink BOTH main repo's \`node\_modules\` AND per-package \`node\_modules\` (core, gateway, opencode, pi) — pnpm nested symlinks miss transitive deps (e.g. \`uuidv7\`) if only root is linked. Run vitest from worktree root. Apply mutations via \`sed\`/direct edit, revert via \`git checkout\`. Always take SHA-256 baseline checksums before mutations; restore byte-identical after all mutations. Always verify worktree is clean after all mutations reverted. PR #906: Mutation A (verdict param) killed; Mutation B (verdict IN filter) survived — genuine behavioral gap, not equivalent mutant.

<!-- lore:019f16b3-30fd-780e-9bc2-eb374c2a69e4 -->
* **Always add clarifying comments when fixing test setup calls that have non-obvious parameter semantics**: When the user applies fixes to test files—especially calls to setup/helper functions like \`\_persistEmbedCap\`—they consistently add inline comments explaining the intent or behavior of each call site (e.g., whether a fresh provider starts at model max or minimum token floor). This applies whenever a parameter value or function call could be misread without context (e.g., magic numbers like 8192 or 0 for freeMemBytes). Follow this pattern by annotating each modified or added test call with a brief comment clarifying what state is being simulated or why that specific value was chosen.

<!-- lore:019f180c-49e7-7e45-ac51-aabc9e6fa0ae -->
* **Always conduct adversarial code reviews with mutation testing and structured deliverables**: When reviewing PRs, the user takes an adversarial staff engineer perspective: they read all changed files in full, cross-reference test coverage against implementation, probe for edge cases (e.g., fallback paths, unsupported encodings, header stripping), and verify mutation-resistance of tests. They scan for unmitigated side effects across the codebase (e.g., worker threads, sync schedulers bypassing silence flags). Reviews are written to a specific file (e.g., \`/tmp/opencode/review-1049.md\`). The working repo is treated as read-only; experiments go to \`/tmp\`. The user expects the review to cover: correctness, security, Bun/Node compatibility, test quality, and invariant preservation across all call sites — not just the directly modified files.

<!-- lore:019f0072-5f9a-7997-863b-62d17d2d8cba -->
* **Always create PR without waiting to be asked; check CI and address bot comments automatically**: 🔴 Directive: always create the PR without waiting to be asked. After pushing a branch, automatically check CI status. When bots (Seer, Cursor Bugbot, Sentry Bot) leave inline comments, address them automatically without waiting for explicit instruction — assess findings, implement fixes if warranted, reply to comments with resolution details. Merge immediately when user instructs — no need to wait for CI if PR is MERGEABLE/CLEAN and user explicitly says to merge. When merging sequential PRs that touch different files, squash-merge in order — no conflict risk. Local branch/worktree cleanup errors after merge are non-blocking. PR branch naming: \`fix/\<scope>-\<issue>\` format (e.g. \`fix/ci-status-preview-links-wiring\`); test branches: \`test/\<scope>\`.

<!-- lore:019ee4d4-bdd2-7641-b523-34aa917fdb48 -->
* **Always document directives with 🔴 markers and preserve them across sessions via written plans**: 🔴 markers = hard requirements (non-negotiable constraints). 🟡 = observations. Capture 🔴 invariants in persistent artifacts (\`.opencode/plans/\`). Act as skeptical adversarial staff engineer; proactively surface invariant violations. Key recurring 🔴 invariants: (1) non-2xx warmup result must be NEUTRAL — never increments/trips breaker; (2) per-bucket isolation — one bad bucket must never disable warming for other sessions; (3) legitimately cold re-warm (\`cacheLikelyAlive===false\`) must NOT count as failure. Code reviews: adversarial, per-finding verdicts (BLOCKER/SHOULD-FIX/NIT), overall verdict (MERGE/SHIP-WITH-FOLLOWUPS/NEEDS-WORK), written reports to /tmp/opencode/, READ-ONLY on working repo. 🔴 Fix known issues immediately rather than defer — 'I'd just try to fix it instead of defer since we \_know\_ there's an issue.'

<!-- lore:019f1716-9d04-7f52-9b72-c745e0a97001 -->
* **Always document PR review findings with severity ratings and root cause analysis in structured reports**: When reviewing PRs, the user expects a thorough adversarial review that: (1) classifies findings by severity (BLOCKER, SHOULD-FIX, etc.), (2) identifies root causes with specific file/line references, (3) evaluates multiple fix options with explicit rejection reasoning, (4) confirms findings empirically via mutation testing or reproduction steps, and (5) writes the full report to a file (e.g., /tmp/opencode/pr1051-review.md). The user expects the chosen fix approach to be justified over alternatives. Reviews should cover correctness bugs, edge cases like partial-failure hybrid states, and silent failure modes — not just surface-level issues.

<!-- lore:019f1801-bc21-76b6-a3fa-a86e2ec1757c -->
* **Always enforce read-only working repo with mutations isolated to /tmp**: When conducting PR reviews in the loreai monorepo, the user consistently establishes two hard constraints at session start: (1) the working repo/worktree is READ-ONLY — no edits, commits, staging, or branch switches allowed there; (2) any mutation experiments must be done by copying files to /tmp first. The review report deliverable is always written to /tmp/opencode/review-NNNN.md. Follow these constraints without being reminded: never touch tracked files in the working repo, always copy to /tmp before mutating, and write the final report to the designated /tmp/opencode/ path.

<!-- lore:019ece05-3058-7c4b-afd7-f668085684cd -->
* **Always enforce tool\_use/tool\_result pair safety as a hard invariant with code comments**: tool\_use/tool\_result pair safety is a hard invariant. safeDeltaInsertIndex is the primary guard: walk back whenever messages\[idx-1] is assistant(tool\_use), regardless of what follows (covers completed pairs AND pending tail). Loop: \`while (idx > 0)\` (no upper bound). removeOrphanedToolResults is last-ditch backstop that 'should essentially never fire.' Always cover both completed pairs AND pending/in-flight dangling tool\_use at tail in tests. Never rely on destructive stripping as primary safety — insertion point must be computed correctly upfront. Invariant documented in code comments referencing bug #747.

<!-- lore:019f155d-cd5a-7107-a8c6-18a67c6f376a -->
* **Always enforce TUI-safe logging: never use console/stdout/stderr in Pi extension**: TUI-safe logging invariant: Pi extension (\`packages/pi/\`) must NEVER write to console/stdout/stderr — Pi runs a full-screen TUI and raw terminal writes corrupt it. All logging must use the core \`log\` module (file-based, terminal-suppressed). Flag any \`console.log\`, \`console.error\`, \`process.stdout.write\`, or \`process.stderr.write\` calls in \`packages/pi/\` as violations.

<!-- lore:019f16ef-6f09-74f9-8676-577630c22ec3 -->
* **Always evaluate crash-safety and idempotency of multi-phase database operations**: When reviewing or designing multi-step database migration or cutover sequences, the user consistently identifies scenarios where a process crash between phases leaves the database in a permanent hybrid/inconsistent state. They expect each phase transition to be crash-safe and resumable: the system should detect partial completion on next boot and recover correctly rather than entering a degradation loop. They treat unrecoverable hybrid states (e.g., columns dropped but mode flag not updated) as blocker-level bugs. Solutions must be idempotent — re-running after a crash should detect what was already done (via schema inspection like PRAGMA table\_info) and skip completed steps rather than failing or duplicating work.

<!-- lore:019edc8f-7bdd-73a0-a374-a700828d04be -->
* **Always exclude .lore.md from PR diffs by restoring origin/main version before branching**: Always restore \`.lore.md\` to \`origin/main\` before branching for a PR (\`git checkout origin/main -- .lore.md\`) to prevent chore commits polluting the PR diff. If it accidentally appears, rebuild the branch clean (soft-reset, unstage, recommit). Exception: branch created directly off \`origin/main\` with no diff — confirmed safe. Standing directive (June 24): include a \`.lore.md\` chore commit as the final commit in each coding session. When conflicts arise on a PR branch, the file may be dropped from that specific branch only.

<!-- lore:019f16aa-70fa-7bd6-88ad-1d927fd449b7 -->
* **Always handle LocalProviderUnavailableError silently to prevent Sentry noise**: When writing embedding code that catches errors, the user consistently distinguishes between LocalProviderUnavailableError and other errors: LocalProviderUnavailableError must be caught silently (log.info at most, never log.error) to avoid triggering captureException/Sentry noise (referenced as LOREAI-GATEWAY-Q). All other errors may use log.error. This pattern applies universally across fire-and-forget embed functions, backfill loops, and any other embedding call site. Never use log.error or allow Sentry capture for LocalProviderUnavailableError — it is an expected, non-exceptional condition indicating the local provider is simply unavailable.

<!-- lore:019efb91-ecc4-76e1-ae17-85a2f250331e -->
* **Always normalize schema\_version to MIGRATIONS.length, never a hardcoded number**: 🔴 Directive (seen 34+ prior sessions): always normalize \`schema\_version\` to \`MIGRATIONS.length\` (not a hardcoded integer) when updating after the migration loop. Comment at db.ts line 1822 enforces this. Hardcoded numbers drift when new migrations are added — \`MIGRATIONS.length\` is always correct by construction. Current state: MIGRATIONS.length == 60 (v60 = session\_rollup at MIGRATIONS\[59]; v59 = knowledge\_symbol\_presence at MIGRATIONS\[58]). Seer caught stale 'v59' comment refs at db.ts lines ~1612, 1617, 1890, 1976 in PR #992 — fixed in d758f376.

<!-- lore:019f1708-ad3f-79c1-9e49-697fb580d4b1 -->
* **Always open a PR immediately after pushing a branch and track it alongside CI**: After completing a fix or feature and pushing a branch, the user consistently opens a PR right away and then monitors CI in parallel — often while starting the next task. The user maintains awareness of multiple PRs in flight simultaneously, tracking their CI status, bot comments, and review state. When one PR is opened, the user immediately identifies and queues the next pending work item (e.g., a flaky test fix as a separate PR). The assistant should proactively open PRs after pushes, report the PR URL, and maintain a clear status summary of all in-flight PRs with their CI/review state.

<!-- lore:019ee535-02e6-7648-82b7-d6c68de1256a -->
* **Always provide file:line inventory when researching codebase impact**: Always provide file:line inventory when researching codebase impact. Expected format: \`filename:line — description\`. Cover all SQL patterns, API surfaces, test assertions, and schema version checks. Research-only phases must not change code. This is a recurring directive across 42+ sessions.

<!-- lore:019f17bb-36f2-7595-b9cd-62facd3f6c12 -->
* **Always request adversarial, READ-ONLY pre-merge code reviews with written output to file AND inline in response**: When reviewing PRs, the user expects a rigorous adversarial review (not a rubber-stamp), explicitly READ-ONLY (no edits to source), covering specific scrutiny points they enumerate upfront. The review must always be written to a file (e.g., /tmp/opencode/review-NNNN.md) AND returned in full in the final message — never just one or the other. The user also insists on never returning an empty report: if the full review cannot be completed, return PARTIAL findings. Reviews must verify root-cause accuracy, production-safety, test seam purity, whether the fix masks a real bug, test vacuity, and determinism claims.

<!-- lore:019f1729-9cde-7010-ae29-4b1fb25a87ba -->
* **Always require explicit merge confirmation before merging a PR**: Even when a PR is fully mergeable (MERGEABLE/CLEAN, all checks passing), the assistant must wait for explicit user go-ahead before executing the merge. The user gives direct merge instructions (e.g., 'merge PR #1051 now via squash merge'). The assistant should never auto-merge based solely on a clean/mergeable state. Once the user confirms, proceed immediately with a squash merge. This pattern applies across all repositories and PR types.

<!-- lore:019ef947-5487-71bc-bbb9-7b03b4f768b0 -->
* **Always run full CI on main/release push — dorny/paths-filter only for PRs**: 🔴 Directive (June 24 2026, seen 34+ prior sessions): always run full CI on main/release push events. \`dorny/paths-filter\` skip logic only applies to PRs. Workflow bugs 'should never slip through regardless of which files changed.' Enforced in \`ci.yml\` via a 'Force full CI on main/release push' step (id: \`force\`) that sets \`code=true\` unconditionally on \`push\` events via \`echo "code=true" >> "$GITHUB\_OUTPUT"\`. The \`actionlint\` job runs unconditionally (no \`needs\`/\`if\`) for the same reason.

<!-- lore:019eec3f-d82f-7f0b-bb46-fd2a6dd28a4c -->
* **Always run progressively broader test suites before committing**: 🔴 Directive (stated June 21 2026): always run progressively broader test suites before committing. Start with the narrowest relevant test scope (single file/module), then widen to package-level, then full suite. Never commit after running only a single test in isolation. Also: run 3 consecutive test runs to check for flakiness before declaring a test suite clean. Mutation testing: create detached-HEAD worktree at \`/tmp/pr-mut\`, symlink main repo's \`node\_modules\`, run vitest from worktree root. Scope patch to specific files only (not broad \`git diff\`) when branch is ahead of origin/main — broad diff captures committed changes from ahead-commits, causing patch failures on unrelated files. NEVER use \`-t\` filter — parentheses in test names are regex metacharacters causing all tests to skip (false pass). Always run full test file. Mutation kill confirms tests are non-vacuous — 4/4 mutation classes killed is the standard for a clean review.

<!-- lore:019ef371-d1ab-791d-b04a-22f50f4e9013 -->
* **Always scope fix tasks strictly to test fixtures, never touching src files**: When the user assigns a task to fix failing tests after a schema/API migration, they explicitly constrain the work to \`packages/\*/test/\` directories only, with an explicit prohibition on modifying \`packages/\*/src/\` files. The user expects the assistant to identify all broken raw SQL or fixture code in test files and update only those, leaving production source code untouched. This pattern applies whenever a schema change has already been implemented in src and the remaining work is purely test-side adaptation.

<!-- lore:019f1712-b54c-7f77-a023-0623abacc0e2 -->
* **Always track PR review work via explicit numbered task lists with priority levels and status updates**: When conducting PR reviews, the user consistently creates a numbered checklist of specific scrutiny tasks, each tagged with a priority level (high/medium) and a status (pending/in\_progress/completed). Tasks are checked off incrementally as work proceeds, and a final summary/report is written to /tmp at the end. The user expects the assistant to maintain and update this task list throughout the session, marking items complete as they are verified, and to conclude with a written report. This pattern applies to all PR review sessions regardless of PR complexity.

<!-- lore:019f1523-f81b-7844-b9de-56db41d8ed3c -->
* **Always use ast-grep instead of rg for structural searches**: 🔴 Directive (June 29 2026): always use \`ast-grep\` instead of \`rg\` for structural/syntactic searches going forward.

<!-- lore:019f1694-acfb-7274-aa84-108dba6cdafd -->
* **Always use deterministic test seams to eliminate flakiness from environment-dependent values**: Always use deterministic test seams to eliminate flakiness from environment-dependent values: Identify the non-deterministic input (e.g., os.freemem(), timestamps), add an optional defaulted parameter to the production function (e.g., \`freeMemBytes = freemem()\`), expose via a \`\_\`-prefixed test-only wrapper, and pass a fixed value from tests. Verify by first passing an extreme value to force failure, then applying the real fix. Production callers unchanged; only tests pass controlled values. Never use retry-based or timing-adjusted fixes.

<!-- lore:019f1806-7b52-79b1-8823-f46e39fdf71a -->
* **Always verify assumptions with empirical tool results before accepting architectural justifications**: When evaluating whether a vendored/forked dependency, patch, or custom build pipeline is still necessary, the user consistently demands concrete evidence rather than accepting theoretical reasoning. They run actual binary inspection tools (grep for instruction sets, objdump, file size checks) to confirm or refute claims. If a justification turns out to be self-inflicted (e.g., the fork introduces the very problem its patch fixes), the user expects that to be surfaced explicitly. Always run verification tools to ground architectural decisions in measured facts, and flag circular justifications where a custom modification creates the problem it claims to solve.

<!-- lore:019f1708-2fe6-7b0e-9798-c3fefd1f5ebc -->
* **Always verify findings empirically with a repro test before proceeding**: When the assistant identifies a bug or correctness issue through code analysis, the user expects a concrete reproduction test to be written and executed in an isolated/disposable worktree (e.g., /tmp/opencode/...) to empirically confirm the finding before moving forward. The user also expects full output capture — if console output is suppressed (e.g., by Vitest defaults), the user wants a workaround (e.g., writing results to a temp file) to get the exact error details. Don't just assert a bug exists from static analysis; prove it with a passing repro test that demonstrates the failure.

<!-- lore:019f16ed-86ea-7525-adae-90d8d7017b32 -->
* **Always verify fixes with both a normal run and a stress/repro test to confirm flakiness is genuinely resolved**: When fixing flaky tests, the user follows a two-phase validation approach: (1) run the test normally to confirm it passes with the fix applied, and (2) reintroduce the failure condition (e.g., adversarial parameter values like \`freeMemBytes=1\`) to prove the repro path still triggers the bug without the fix, confirming the fix is causally effective rather than coincidentally passing. This pattern applies whenever a non-deterministic or environment-sensitive test is being stabilized. The assistant should proactively suggest both steps after applying a fix, not just the happy-path run.

<!-- lore:019f16f6-3400-700b-9ea6-f10631fe8f8b -->
* **Always verify lint passes with zero errors before considering a PR ready**: The user consistently checks that lint exits with code 0 (no errors) before a PR is considered complete. When lint errors are found, the user investigates whether they are pre-existing or introduced by the current changes. Pre-existing errors in unrelated files are noted but not necessarily fixed in the current PR — however, if a pre-existing lint error would cause CI to fail for the current branch, the user prefers to include the fix directly in the PR rather than waiting or stacking branches. The user also distinguishes between errors (blocking) and warnings (non-blocking) in lint output.

<!-- lore:019f17c4-61d9-7215-b598-cdf698e0305e -->
* **Always verify test suite validity through mutation testing before approving PRs**: When reviewing PRs that add or modify tests, the user consistently runs mutation tests to confirm the test suite is non-vacuous — that it actually catches real bugs rather than passing trivially. This involves flipping silence guards, reverting migrations, or removing key logic, then confirming tests fail as expected. The user expects all mutation classes to be killed, documents which mutations are caught by which tests, and flags any gaps where mutations would go undetected. This applies especially to behavioral guards (silence flags, error suppression, cross-instance state) where tests could pass vacuously.

<!-- lore:019f1711-ca18-75f9-ab3a-499aceb33fbf -->
* **Always verify tests with mutation testing before considering implementation complete**: After writing tests, the user consistently requires a mutation verification step: deliberately injecting a regression (reverting or breaking the exact logic the test guards), confirming the test fails, then restoring the original code. This proves the test actually catches the regression it claims to catch — not just passing vacuously. The pattern applies to any new or modified test, especially regression tests. The assistant should proactively plan and execute this red/green mutation cycle: (1) establish baseline all-passing, (2) mutate the guarded logic, (3) confirm the specific test fails while others pass, (4) revert cleanly. Never consider a test suite complete without this verification.

<!-- lore:019ee640-0ad2-7a98-87f8-111898a4a84d -->
* **ast-grep: always use stopBy: end for relational rules**: Always use \`stopBy: end\` for relational rules (\`inside\`, \`has\`) in ast-grep to ensure complete traversal. Stated as a firm rule unless there is a specific reason not to. ast-grep skill at \`/home/byk/.claude/skills/ast-grep\`. Key principles: start simple (pattern → kind → relational → composite); use \`--debug-query=cst\` to inspect AST; escape metavariables in inline rules with \`\\$VAR\`. Prefer \`ast-grep\` over \`rg\` for all structural/syntactic searches (🔴 directive, June 29 2026).

<!-- lore:019ef63e-ffa5-740c-b8ab-ace77eae9232 -->
* **Avoids execution yet**: user does not want execution yet,

<!-- lore:019effdc-765b-7dc0-912f-69bb716352f4 -->
* **Blog #663 voice: human but serious — no slang, emoji, or self-deprecating footnotes**: 🔴 Directive (June 25 2026): Lore.AI blog voice is human but serious, not cheeky. Contractions OK; rhetorical beats OK; direct address OK. No slang, emoji, self-deprecating footnotes, or cheeky headers. Use 'we' for Lore voice. Em-dashes are an AI tell — remove all (0 remaining in rev 9). Applies to blog post #663 and any future Lore positioning copy.

<!-- lore:019eff04-9cff-74c1-a8d2-45d12e194d39 -->
* **Blog copy: Fair Source named proudly with fair.io link**: 🔴 Directive: if licensing is mentioned in Lore blog/positioning copy, name Fair Source proudly by name and link to fair.io. Use it as a trust signal in the local-first paragraph, not a disclaimer. Consistent with \[\[019efe86-ad54-7ad3-868b-c3a76af9d306]] (FSL-1.1-Apache-2.0 license).

<!-- lore:019eff04-9d31-7864-811d-39dbb8803da3 -->
* **Blog copy: feature-dump bullets compressed to one consequences-framed sentence**: 🔴 Directive: compress platform feature-dump bullets (cache/extract/patterns/track-spend) to one sentence framed as consequences, not the argument. User confirmed: 'Like this.' — explicit agreement with reviewer suggestion to avoid the 'ah, the pitch' reaction from all three reader personas.

<!-- lore:019eff04-9d5d-7934-b32b-fa411111b211 -->
* **Blog copy: preferred vivid image is 'genius a minute ago' / amnesia — not chopping-block**: 🔴 Directive: the single keeper vivid image for Lore positioning copy is 'genius a minute ago, amnesic now' (or 'lobotomy' variant). The chopping-block scene is cut. Exact confirmed phrases: 'genius a minute ago,' / 'never decides what leaves it.' / 'never lift a finger.' / 'never the hard part.' / 'never gets a vote'.

<!-- lore:019f014d-b0ea-752b-9279-fb271edf3119 -->
* **Blog posts: publish to loreai monorepo website, use dedicated worktree, no per-post banner**: 🔴 Directives for blog publishing: (1) publish to Lore's website (loreai monorepo at \`packages/website/src/content/blog/\`), NOT byk.im; (2) always use a separate dedicated worktree off \`origin/main\` — never reuse an in-progress worktree; (3) global OG image is sufficient — never prompt for or add a per-post banner image; (4) leave platform feature bullets as-is in the post body.

<!-- lore:019eff04-9cca-712f-bdb7-535292cfc96b -->
* **Blog tone: category-not-competitor thesis — gentle, not dunking**: 🔴 Directive: convey the 'new category being invented' thesis gently — acknowledge the existing category isn't enough and that something new is being built, without dunking on existing tools. Generous close required: 'a good store is genuinely worth having.'

<!-- lore:019eff04-9c94-773c-b2ed-dca9ee730f6f -->
* **Blog tone: manual note-taking critique — respect skilled practitioners, highlight focus cost**: 🔴 Directive: when critiquing manual note-taking in Lore positioning copy, never imply incompetence ('you're bad at it anyway'). Acknowledge skilled practitioners exist; frame the cost as time and focus/context-switch overhead: 'The cost was never that you're bad at it. It's that the filing keeps pulling your focus off the thing you were actually trying to do.'

<!-- lore:019ee673-63cb-7a6d-9b3b-844fdab3eaff -->
* **Connection timeout: 10s is generous enough — do not increase; capture aborts instead of raw timeouts**: Connection timeout: 10s is generous — do not increase. Raw connect timeouts are not actionable for Sentry capture. Prefer capturing abort events with loop-lag/RSS context instead.

<!-- lore:019ef0e4-01d8-7f82-ad80-88a084d3ece8 -->
* **CONSOLIDATION\_MERGE\_SYSTEM invariants: no force-delete, no invented knowledge, opposing rules never merged**: 🔴 Directives from \`CONSOLIDATION\_MERGE\_SYSTEM\` (prompt.ts lines 619-631): (1) Opposing rules (e.g. 'always use tabs' vs 'always use spaces') are NEVER duplicates — never merge them. (2) Never force-deletes distinct entries — no numeric eviction target; returning empty array is acceptable. (3) Never invent new knowledge — only 'update' survivors and 'delete' true duplicates. (4) 'update' op: replace content with a concise rewrite under 150 words; use to merge survivors or trim verbose entries.

<!-- lore:019efae0-89c8-7ac9-a9ec-aba6ed3c2d33 -->
* **Convention: X') + patterns in pattern-extract**: our convention is X') + patterns in pattern-extract.

<!-- lore:019ee6ae-7cf6-7fdb-8959-36949bffa201 -->
* **Embedding OOM adaptive cap: decrease factor ≥ 0.7, no additive increase**: Burak Yigit Kaya chose decrease factor ≥ 0.7 for embedding OOM adaptive cap — gentler than 0.5 (which quarters attention memory per step). No additive increase — upward adaptation handled by memory-aware initializer re-evaluating freemem on each respawn. 0.8 noted as a tuning option. Pre-truncation must use free-memory-adjusted value (e.g., \`adaptiveLocalMaxTokens()\` derived from \`os.freemem()\` + O(L²) attention model) — hard-coded token limits like \`LOCAL\_MAX\_CHARS = 16384\` explicitly rejected. Coverage stats in \`runStartupBackfill()\` must always log to stderr so the problem is visible. These are locked decisions for \`fix/embedding-oom-adaptive-cap\`.

<!-- lore:019ef06b-99f1-7804-998a-fcb9d6a84d71 -->
* **File deferred sub-PR reasoning as GitHub issues — never let it get lost**: 🔴 Directive (June 22 2026): when deferring a sub-PR or architectural decision, always file a GitHub issue capturing the full reasoning so it is not forgotten. Exact quote: 'don't forget our reasoning.' Applied to sub-PR 4 → issue #909. This is distinct from the general 'store artifacts in version-controlled locations' rule — it specifically covers deferred design decisions that have no immediate code artifact.

<!-- lore:019ef69c-9430-7808-a25f-a20edeaf14f5 -->
* **Make sure to address the seer comment there (re-emphasized via system-reminder directive — overrides prior state**: User stated make sure to address the seer comment there (re-emphasized via system-reminder directive — overrides prior state,

<!-- lore:019eec5c-fa90-76ae-b698-8e00ce35790e -->
* **Mutation-test every test against its fix — red-phase must fail before green-phase passes**: Extension of TDD red-green discipline \[\[019eec55-4bd0-779b-b0f6-b3f4f9090790]]: for any test written to guard a specific fix, confirm the test FAILS when the fix is reverted (a manual mutation test). A test that passes with the fix applied AND with the fix reverted is vacuous — it does not guard the fix. This is especially important for refactoring tests that assert correctness of internal transformations (e.g., compression scale normalization, cache alignment). Verify by temporarily reverting the fix in a /tmp copy and running the test — it must fail.

<!-- lore:019eff03-77c0-7db3-ac9c-4cc105952816 -->
* **Never evaluate alternatives in this project context**: 🔴 Directive (June 25 2026): never evaluate alternatives. Applies to assistant behavior when working on the loreai/opencode-lore project.

<!-- lore:019ef029-6ac3-7605-bc76-fc627dc3f50a -->
* **Never publish nightlies to npm — GHCR only**: 🔴 Directive (June 22 2026): nightlies are NEVER published to npm. Onur Temizkan uses \`lore upgrade nightly\` which pulls from GHCR (\`ghcr.io/byk/loreai:nightly\`). npm registry contains only stable releases (e.g. \`@loreai/gateway\`, \`@loreai/core\`, \`opencode-lore\`). Nightly version string is computed deterministically from commit timestamp. Publish pipeline: \`Publish Nightly to GHCR\` job in \`publish.yml\`; tagged as both \`nightly\` and \`nightly-\<version>\`.

<!-- lore:019eec27-f013-77ca-b75b-1a48ab751200 -->
* **Never trust a fix without seeing the test fail first**: 🔴 Directive: never trust a fix without seeing it fail first. When fixing a bug, always confirm the test catches the bug (red phase) before applying the fix (green phase). TDD red-green-refactor discipline: if you haven't seen the test fail, you don't know if the test actually guards against the regression. Mutation test technique: create detached-HEAD worktree at \`/tmp/pr-mut-\<issue>\`, symlink main repo's \`node\_modules\`, run vitest from worktree root. NEVER use \`-t\` filter — parentheses in test names are regex metacharacters causing all tests to skip (false pass). Always run full test file. Related: 'If this test still passes with the guard deleted, it isn't testing the guard' \[\[019eec15-f0a4-769d-a1b1-bf87cac5c1e5]] and investigate test-coverage gaps \[\[019ee645-ff9e-77a1-ada6-7dc33c77f498]]. Also: always run progressively broader test suites before committing \[\[019eec55-4b73-7aa2-982e-5eecaab59339]].

<!-- lore:019ef387-4b17-77d7-a76d-062269f0214b -->
* **Never weaken assertions or change expected values to make a test pass**: 🔴 Directive (seen 23+ sessions): never weaken/delete an assertion or change an expected value to make a test pass. When a test fails due to a schema change, fix the test setup (INSERT helpers, raw SQL, helper functions) to match the new schema — never adjust the assertion target. Example: integrity.test.ts duplicate detection fixed by adding knowledge\_meta rows to raw INSERTs, not by changing \`duplicates.length >= 1\` assertion.

<!-- lore:019ef029-6ad7-7dd9-a310-cba40d1e468f -->
* **Ping Onur Temizkan over WhatsApp, not GitHub**: 🔴 Directive (June 22 2026): when notifying Onur Temizkan about releases or issues, ping him over WhatsApp via Beeper (chatID 556724, local-whatsapp\_ba\_aM0zYm0IF3-P1LLS6cjW6YsPQSA) — NOT via GitHub comments or issues. Always verify CI is green before pinging. Use Burak Yigit Kaya's actual WhatsApp tone: Turkish, lowercase, no punctuation, no markdown, terse, casual, minimal emoji (reactions only). Example: 'abi nightly cikti, lore upgrade nightly yap'. Never write formal English with markdown — that tone is wrong for WhatsApp.

<!-- lore:019ef655-bc07-7aba-80c5-bb5ca9216d47 -->
* **Prefers cool-bust only when compaction is STRICTLY cheaper — a tie means compaction buys nothing (e.g. compressed === full), so default over cool-full-write and never claim a pointless bust**: prefer cool-bust only when compaction is STRICTLY cheaper — a tie means compaction buys nothing (e.g. compressed === full), so default to cool-full-write and never claim a pointless bust.

<!-- lore:019f03bd-7047-7c42-b440-2a59b788e769 -->
* **Prefers durable pins over mid-session recomputation**: Prefer durable pins over mid-session recomputation.

<!-- lore:019ef48a-ac2c-7800-a9dd-a771194a323b -->
* **Prefers explicit over implicit**: prefer explicit over implicit.

<!-- lore:019f13ff-bcfb-74d6-b29f-6dcdc2e39bc4 -->
* **Prefers LORE\_REMOTE\_URL, prefers LORE\_GATEWAY\_URL over default ports**: prefers LORE\_REMOTE\_URL, prefers LORE\_GATEWAY\_URL over default ports,

<!-- lore:019f12fe-cac2-7c76-825b-77f117fd0e64 -->
* **Prefers patch files over long-lived fork (auditable**: prefer patch files over long-lived fork (auditable,

<!-- lore:019efad5-e983-7270-a61a-f0fca14983c6 -->
* **Prefers pin over cache**: prefers pin over cache,

<!-- lore:019f145e-ee7e-7832-86b1-36d87298bdbe -->
* **Prefers recall over assuming you don't have the information**: prefer recall over assuming you don't have the information.

<!-- lore:019ef10f-53c0-7494-9a6c-bef24e943dcb -->
* **Prefers UPDATES over CREATES (352-359)**: PREFER UPDATES OVER CREATES (352-359),

<!-- lore:019ef4be-ac81-7964-bf08-f24d7c8e2ad5 -->
* **Prefers UPDATES over CREATES section at line 390 of prompt**: PREFER UPDATES OVER CREATES section at line 390 of prompt.

<!-- lore:019f0109-c33d-7f41-829d-1c8e860ce4af -->
* **Prefers vector score × confidence, fall back over FTS5 score × confidence**: prefer vector score × confidence, fall back to FTS5 score × confidence.

<!-- lore:019f0103-160f-77b8-8e3a-f84034642067 -->
* **Prefers writing more tests over lowering the threshold or excluding files**: prefer writing more tests over lowering the threshold or excluding files.

<!-- lore:019eec4d-5e2b-7f16-8521-3ef10cc17d2d -->
* **Protected content must never be stripped during context management**: 🔴 Directive (observed across 34+ sessions): content marked 'protected' must NEVER be stripped during compaction or context window management. Absolute invariant — no exceptions. Confirmed in architecture.md: 'The last 2 turns are always protected from stripping.' When implementing or modifying context compression, compaction, or eviction logic, always verify protected content survives all code paths. A stripping path that can remove protected content is a bug.

<!-- lore:019ef676-61ff-7594-83e1-e3e1a1ff7a26 -->
* **replicaId() must always read from live DB (KV) each call**: replicaId() reads from KV on every invocation — never caches the value. Test suite swaps DB between cases so cached replicaId would return stale device identity. User assertion: 'always read it from the live DB' and 'always reads from live DB'. This ensures each test case gets a fresh replica ID matching its isolated database.

<!-- lore:019efb32-23de-7867-8e02-e5e57d2a734b -->
* **Ship bug fixes as separate sequential PRs per bug**: User directive (June 24–25 2026): when fixing multiple bugs or shipping a feature series, ship them as separate sequential PRs — one logical change per PR. Do not combine fixes into a single PR even if related. Enforced across: Bug 1 (PR #974), Bug 2 (PR #976), Bug 3 (PR #979), regressions (PR #982), #627 Phase 0 (PR #939), Phase 1 (PR #977), Phase 2 (PR #986), #911 (PR #988). Follow-up PRs for regressions introduced by a fix are also shipped as separate PRs, not folded back into the original.

<!-- lore:019f17c2-11df-731b-b7b9-747b0e03eeb0 -->
* **Uses local ONNX embeddings (default for recall vector search via sqlite-vec) — not only on explicit local embeddings provider selection**: user uses local ONNX embeddings (default for recall vector search via sqlite-vec) — not only on explicit local embeddings provider selection.
