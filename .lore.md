<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019eb931-7257-7ef6-87db-b2dd034c3ea7 -->
* **embedding-worker.ts: self-heal retry for corrupt model — skipped for vendored binaries**: embedding-worker self-heal retry: if !vendorModel && isCorruptModelError, purgeCachedModel then retry once. Never post init-error before retry (embedding.ts sets localProviderKnownBroken=true permanently). /load model.\* failed/i misclassifies 401/403 causing redownload loop. embedding-worker.ts not importable in tests.

<!-- lore:019ebdd7-b5a4-7b44-840d-2bd981d368ff -->
* **gradient.ts: selectDistillations() always preserves meta (gen>=1) entries — never evicted by recency scoring**: distillation.ts: selectDistillations() always preserves meta (gen>=1) entries — never evicted by recency scoring. distLimit slots = limit - meta.length; gen-0 scored by recency (70%) and content signal (30%). Meta guarantee retains session history (fixes #417). Dead parameter metaThresholdOverride still plumbed through run() but never passed — removed after PR #751. distilledPrefixCached three-branch: (1) no new rows → cache hit, (2) new gen-0 → append delta (prepends headers, causes duplication in messages\[1]), (3) full re-render on meta rewrite. Cache validity: same sessionID, rowCount grew, last cached row unchanged. Layer 0 (warm-cache) does not inject prefix — only Layer 1+ (compression mode).

<!-- lore:019ebc36-830a-7307-8a10-9ff12dfa5740 -->
* **openai-responses.ts: Codex delta must live in a dedicated wrapper, not inline conditionals**: Codex delta: parseOpenAICodexRequest captures codex=true. buildOpenAIResponsesUpstreamRequest is agnostic; applyCodexResponsesDelta mutates: removes max\_output\_tokens, forces store:false, re-emits control fields, swaps URL to /codex/responses. previous\_response\_id not forwarded.

<!-- lore:019ebc96-83c7-7cd5-98d2-ca85724c2a85 -->
* **pipeline.ts: two curation paths — in-flight vs idle — must be gated separately**: pipeline.ts LTM caching: Two curation paths — in-flight (mid-turn, gated by sessionState.curationScheduled) and idle (on idle, flush gen-0 then metaDistill). In-flight avoids system\[2] cache busts during active conversations. System\[2] churn root causes: context-health note appended after diff-pinning fluctuates by layer (1→2→1), LTM entry selection changes per-turn (~7 of 22 entries dropped). In-flight gen-0 distillation mutates messages\[1] in compression mode (gradient.ts:1553-1569) — append-only but deep divergence point causes cache misses. Warm-cache DB divergence: when pin reused, ltmCacheText=NULL while ltmPinText populated causes spurious re-pin; fix: persist pinned text. Volatile delta channel for mid-session preference changes: assign stable IDs, reference by ID in volatile tail — zero cache cost.

### Gotcha

<!-- lore:019ebc9e-c290-71a4-bf63-c1cfde288bda -->
* **cache-analytics: system\[1] bust at turn 2 is transient — actually system\[2] insertion**: Trap: post-restart cache-analytics logs show \`divergence='system\[1]'\` at turn 2 with reason 'stable LTM changed'. Looks like a real LTM churn bug. Fix: this is a one-time transient — system\[2] (context-bound LTM) is first injected at turn 2, shifting the block boundary so the analytics sees it as a system\[1] change. After turn 2 it stabilizes. Do not treat this as evidence of ongoing cache busting; verify by checking whether the same session shows the bust only at turn=2.

<!-- lore:019ebc55-ba2e-7271-9906-905e4f5ed9bf -->
* **CCH seed extraction: seed may persist in binary but algorithm/preimage changed across versions**: CCH seed extraction: consult \`quality/CCH.md\`. Seed \`0x4d659218e32a3268\` (SEED\_2\_1\_138) unchanged in 2.1.175. Hash-input ≠ wire body — cch fn scans for \`cch=\` placeholder, splits/transforms before hashing. Binary is EXEC (not PIE), fixed base \`0x200000\`. CCH\_PLACEHOLDER = \`cch=00000\`; signBody replaces with hash; regex \`/cch=(\[0-9a-fA-F]{5});/\` (trailing semicolon required). Work files in stable dir (e.g. \`~/Code/cch-investigation/\`), not \`/tmp\`.

<!-- lore:019eb4d0-6fd9-7ef5-b22f-0f156eeca4b0 -->
* **cli/main.ts OPTIONS map: undeclared flags silently become booleans under strict:false**: Trap: \`parseArgs\` with \`strict: false\` treats any undeclared \`--flag value\` as \`{flag: true}\` with \`value\` falling into positionals — looks correct because \`--flag=value\` (equals syntax) still works. Fix: every flag used in ANY subcommand MUST be declared in the global \`OPTIONS\` map in \`packages/gateway/src/cli/main.ts\` with its correct type (\`string\` or \`boolean\`). Discovered in PR #696: \`--to\`, \`--project\`, \`--dry-run\`, \`--no-children\`, \`--min-confidence\` were missing, causing \`--to /path\` to parse as \`{to: true}\` with \`/path\` as a positional.

<!-- lore:019eb4d0-6fe3-7dc1-b80b-fad31fa29748 -->
* **distillations table schema: NOT NULL columns narrative, facts, source\_ids (not content)**: Trap: \`distillations\` table has no \`content\` column — looks like it should because other tables use \`content\`. Actual NOT NULL columns: \`narrative\`, \`facts\`, \`source\_ids\`, \`generation\`, \`token\_count\`, \`created\_at\`. Also: \`tool\_calls\` table uses \`tool\` (not \`tool\_name\`) and requires \`message\_id\`. Test helpers that insert into these tables must include all NOT NULL columns or get constraint errors. Discovered while writing \`packages/core/test/data-move.test.ts\`.

<!-- lore:019ebb4a-397c-76aa-89c8-93110713f3d4 -->
* **formatKnowledge: title-collision within a category produces non-deterministic render order**: Trap: \`formatKnowledge\` sorts entries within each category by title for byte-stable rendering. If two entries share an identical title, the sort returns 0 and their relative order is non-deterministic — text can swap between turns, busting the prompt cache. This is an edge case (duplicate titles are rare) but worth guarding. Fix: add a secondary sort key (e.g. entry id) as a tiebreak after title comparison, mirroring the id-tiebreak already used in the preference fast-path packing loop.

<!-- lore:019ebafd-32b7-7468-bdb2-5c77ff6c4ccf -->
* **opencode-lore plugin: legacy export path causes getLegacyPlugins() to be invoked**: OpenCode plugin loader iterates Object.values(mod) — treats every function export as server plugin and throws on non-functions. lore plugin must export default { server: LorePlugin }. OpenCode package must only export LorePlugin + same-reference default. applyLoreProviderConfig returning undefined causes TypeError. Fix: helpers in internal.ts, guarded by plugin shape test.

<!-- lore:019ebdff-9119-775b-b51b-c6356df3baf0 -->
* **Recurring bash failure: aborted**: The \`bash\` tool repeatedly failed with "aborted" across 8 sessions (9 total failures). Sample error: \[Tool execution was interrupted]. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebdff-9155-72a9-8b9b-0e39211968fe -->
* **Recurring edit failure: aborted**: The \`edit\` tool repeatedly failed with "aborted" across 3 sessions (4 total failures). Sample error: Tool execution aborted. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebdff-9122-7359-b088-0ff918bfdc15 -->
* **Recurring edit failure: other:could-not-find-oldstring**: The \`edit\` tool repeatedly failed with "other:could-not-find-oldstring" across 5 sessions (7 total failures). Sample error: Could not find oldString in the file. It must match exactly, including whitespace, indentation, and line endings.. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebdff-9113-7e3f-a20c-c8eb3636f16f -->
* **Recurring plan\_exit failure: other:the-user-dismissed-this**: The \`question\` tool repeatedly failed with "other:the-user-dismissed-this" across 4 sessions (5 total failures). Sample error: The user dismissed this question. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019eb94d-db25-72d2-ba71-f2b1ae570c51 -->
* **Recurring read failure: not\_found**: Recurring tool failures (transient; expect and retry): \`edit\` fails 'aborted' or 'could-not-find-oldstring' (re-read file before edit); \`write\` fails 'aborted' or 'other:the-user-has-specified' (check permissions); \`bash\`/\`task\` fails 'aborted' (interrupted); \`webfetch\` returns 404 for raw GitHub URLs (check branch/path); \`read\` fails 'not\_found' (verify path with ls/find); \`question\`/\`plan\_exit\` fails 'user-dismissed-this' when dismissed. Treat as expected transient errors; retry or adjust approach.

<!-- lore:019ebdff-914f-7251-bbce-2efc9211300a -->
* **Recurring task failure: aborted**: The \`task\` tool repeatedly failed with "aborted" across 3 sessions (4 total failures). Sample error: Tool execution aborted. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebdff-912a-7502-afcd-ac7c3872492f -->
* **Recurring webfetch failure: other:statuscode-non-xx-status**: The \`webfetch\` tool repeatedly failed with "other:statuscode-non-xx-status" across 4 sessions (10 total failures). Sample error: StatusCode: non 2xx status code (404 GET https://raw.githubusercontent.com/oven-sh/bun/main/src/http/http\_client.zig). Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebdff-9145-7f18-9c90-b29030f90f1e -->
* **Recurring write failure: aborted**: The \`write\` tool repeatedly failed with "aborted" across 4 sessions (4 total failures). Sample error: Tool execution aborted. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebdff-913e-7d3a-948f-d48cfbc853ef -->
* **Recurring write failure: other:the-user-has-specified**: The \`write\` tool repeatedly failed with "other:the-user-has-specified" across 4 sessions (4 total failures). Sample error: The user has specified a rule which prevents you from using this specific tool call. Here are some of the relevant rules \[{"permission":"\*","action":"allow","pattern":"\*"},{"permission":"edit","patter. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebc92-64c5-72d2-ab28-a0d51595d713 -->
* **Self-referential log analysis: debugging session content appears in its own cache-bust diagnostics**: Trap: When running cache-bust diagnostics (e.g. sampling \`messages\[N]\` diffs) inside a long debugging session, the diagnostic output itself — cch hashes, commit IDs, log snippets pasted into the conversation — becomes part of the session content. The analyzer then flags its own growing conversation as a cache-bust source. Looks like a real bug because the diffs are large and change every turn. Fix: always verify that sampled divergence snippets are NOT from the current session's own output before treating them as independent bug signals. Cross-check session IDs and content origin before acting.

<!-- lore:019ebcab-947e-76d8-8dcd-294c8fd5b164 -->
* **sessionState.curationScheduled typed boolean | undefined — coerce with !! before passing to strict boolean param**: Trap: \`sessionState.curationScheduled\` is typed \`boolean | undefined\` in pipeline.ts session state. Helper functions like \`shouldRunInFlightCuration\` declare their params as \`boolean\` (not \`boolean | undefined\`), so passing the field directly causes TS2322. Looks safe because the value is only ever set to \`true\` explicitly. Fix: coerce with \`!!sessionState.curationScheduled\` at the call site. Applies to any \`sessionState\` field that starts undefined and is only assigned truthy values.

<!-- lore:019eb4d0-6fe8-7e3e-b9dc-46608c201399 -->
* **suggest.ts: RegExp flag deduplication required before appending 'g'**: Trap in \`packages/gateway/src/suggest.ts\`: constructing \`new RegExp(pattern.source, ${pattern.flags}g)\` produces \`'gg'\` if source pattern already has \`g\` flag, causing \`Invalid flags supplied to RegExp constructor\`. Fix: strip \`g\` from \`pattern.flags\` before appending: \`pattern.flags.replace(/g/g, '') + 'g'\`. Applies anywhere a RegExp is reconstructed with forced flags.

### Pattern

<!-- lore:019eb4d0-6fed-71ff-baca-7151d61bf315 -->
* **api.ts resolveProject() priority and parseBody zstd support**: resolveProject() priority: id route param, git\_remote, path. moveSessions/reassignKnowledge do not update knowledge\_transfers. moveSessions BFS overcounts children from other projects. parseBody supports zstd via Content-Encoding.

### Preference

<!-- lore:019ebc36-8316-7816-9936-29c3d1613cd0 -->
* **Always add regression tests when fixing protocol/builder bugs**: When fixing protocol bugs (e.g., field suppression), add regression tests: (1) field absent when client sends it (budget still records), (2) non-codex path still emits field, (3) worker-side field absent. Missing: tests that control fields are still emitted after refactor.

<!-- lore:019ebafe-90fd-772e-aa97-fc7a70383558 -->
* **Always call plan\_exit to signal completion of the planning phase**: The user consistently enforces the directive to call \`plan\_exit\` at the end of the planning phase. This appears as a repeated red-flag observation across every session, indicating the user treats it as a critical, non-negotiable instruction. When in plan mode, after completing exploration and writing the plan file, the assistant must explicitly call \`plan\_exit\` to signal to the user that planning is done. Failing to do so is treated as a violation. This applies universally across all plan-mode sessions regardless of the task being planned.

<!-- lore:019ebdcc-f2a4-79f1-b4ec-325fb01aab95 -->
* **Always investigate before modifying — trace code paths read-only first**: Always investigate before modifying: trace code paths read-only first. When encountering a bug, frame initial requests as 'trace', 'investigate', or 'review' — not 'fix'. Read and report what the code does, confirm root causes, understand the full call chain. Document findings with exact file paths, function names, and line numbers. Mark inline code comments with 🔴 (user directives/invariants) vs 🟡 (observations). Only after investigation is complete does the user authorize changes. Always perform adversarial code review before AND after merging PRs: look for reasons NOT to merge — logic correctness, edge cases, missing tests, API contract issues. Use a subagent for objectivity. If already merged, conduct post-hoc review and file follow-up fixes.

<!-- lore:019ebc55-ba42-7f31-9d4f-447fbe237bef -->
* **Always monitor CI runs after pushing changes and investigate failures immediately**: Always monitor CI runs after pushing changes, investigate failures immediately. Create clean branch off origin/main before opening PR: stash uncommitted changes with \`git stash -u\` (covers untracked files). Store work files in \`~/Code/\` not \`/tmp\`. CI \`gh\` check may fail from non-git dirs — ignore.

<!-- lore:019ebde0-db17-7a14-81ed-b02e968300f1 -->
* **Always prefer proper, maintainable solutions over hacks or workarounds**: When presented with multiple implementation options, the user consistently rejects short-term hacks, workarounds, or solutions that gut functionality (e.g., skipping background work entirely). They explicitly prefer the 'long-term, proper, maintainable, better solution' — such as adding a first-class protocol, abstracting shared code into reusable functions/classes, or fixing the root cause rather than papering over it. When the assistant proposes a shortcut or makes an unsupported assertion, the user challenges it and redirects toward the correct architectural approach. Always present and implement the proper solution; only mention workarounds if explicitly asked.

<!-- lore:019ebc68-e704-760e-b022-ed1aaf6457b2 -->
* **Always preserve raw bytes when handling HTTP request bodies**: Handle HTTP bodies as Buffer\[] with Buffer.concat, never string concatenation. Use latin1 encoding for text search in binary bodies. Copy bytes before mutation; guard offset writes. Pass as bytes not strings.

<!-- lore:019ebe0b-fb25-7f19-b7d2-e3d1380e0ced -->
* **Always request adversarial code review focused on real bugs, not style issues**: When submitting or reviewing PRs, the user explicitly wants adversarial, deep code reviews that find REAL problems — logic bugs, race conditions, incorrect assumptions, missing guards, and false guarantees in comments. The user explicitly rejects superficial or stylistic findings. Reviews should cover: atomicity/concurrency issues, incorrect fallback behavior, overly broad pattern matching, missing TTLs or retry thresholds, and factually wrong comments (e.g., claiming UUIDv4 is monotonic). The user often uses a subagent for objectivity. Findings should be categorized by severity (BLOCKER, SHOULD-FIX, MINOR) and be actionable with specific fix suggestions.

<!-- lore:019ebe1a-92c3-7a8d-86ff-8d780160719a -->
* **Always request an independent adversarial subagent review before merging a PR**: Before merging any PR, the user consistently requests a thorough, adversarial self-review using an independent subagent for objectivity. The subagent is expected to find REAL problems (not superficial/stylistic issues), categorize findings by severity (BLOCKER, SHOULD-FIX, NIT), verify correctness of non-issues, and identify test coverage gaps. The user then triages findings, deciding which to fix before merge vs. defer. This pattern applies to every PR regardless of size or CI status — even after all CI checks pass, the adversarial review is a mandatory pre-merge step.

<!-- lore:019ebdf0-79f5-7208-a60a-2c1965e486ab -->
* **Always track remaining work items explicitly after merging PRs**: After merging a PR or completing a fix, immediately surface residual issues, incomplete tasks, or next steps — do not treat a merge as 'done'. Also: always use \`MIGRATIONS.length\` for schema version instead of hardcoded constants in \`UPDATE schema\_version SET version = ...\` statements, so version stays in sync with the migration array automatically.

<!-- lore:019ebdef-0a6c-7084-a019-52924c1bb07a -->
* **Always treat codebase directive comments as authoritative design invariants**: When the user highlights comments in source files (marked with 🔴), they are asserting these as non-negotiable design invariants — not just documentation. These include policies like 'never silently disable retries', 'raw data is never lost on fall-back', 'Retry-After is always honored', 'hosted mode never shares filesystem with clients'. The user expects the assistant to treat these directive comments as ground truth when reasoning about the system, proposing changes, or identifying bugs. Proposals that would violate these invariants are rejected. When the assistant correctly identifies and respects these invariants in its analysis or RC proposals, the user confirms and proceeds.

<!-- lore:019ebdff-9f49-73aa-b179-6cb251a41d63 -->
* **Always verify exact line numbers, type signatures, and export status before implementing changes**: Before making any code changes, the user consistently reads and confirms the exact current state of the codebase: specific line numbers, type union members, function signatures, what is/isn't exported, and which sites would be affected by a change. This applies to types, constants, dispatch functions, and test files. The user expects the assistant to enumerate all affected call sites when proposing changes to shared types or interfaces, and to confirm export status of functions before writing tests that import them. Changes are only applied after this verification step, not speculatively.

<!-- lore:019ebc46-148e-75b8-a591-c04300540d92 -->
* **Always verify implementation correctness by running typecheck and tests after code changes**: After completing any implementation or refactor, always run typechecks (tsc --noEmit across all packages) and test suites (vitest). Zero typecheck errors and all relevant tests must pass. PRs with 0% test coverage are blockers — not acceptable even if CI passes. Extract decision logic into small pure helper functions to make them unit-testable rather than burying logic in large pipeline functions. Inspect test helper definitions and existing test structure before writing new tests. Also run lint (biome) before pushing. Run full verification suite in sequence and confirm all pass before proceeding to push/PR creation.

<!-- lore:019ebdc7-57dd-7842-9236-b9f9dc3a8736 -->
* **Always verify that detected/computed values are actually used in routing/forwarding logic, not just computed**: When reviewing or implementing code that detects a protocol, parses a body, or computes a routing decision, always verify the detected value is actually wired into the downstream routing/forwarding path — not just computed and discarded. Critical flaw pattern: \`detectProtocolFromBody()\` in fetch-interceptor.ts was computed but never transmitted to the gateway (which routes by URL path + \`X-Lore-Upstream-URL\` header only), making body detection dead code. When this gap is found, fix the wiring (make it functional) rather than remove the dead code. Always trace the full data flow: detection → header/parameter propagation → gateway endpoint selection.

<!-- lore:019ebde2-37a4-78b8-a749-edcd3c40d1da -->
* **Never add provider-specific worker defaults for providers with frequently changing model catalogs**: When discussing worker model defaults, the user explicitly rejects adding WORKER\_DEFAULTS entries for providers like 'opencode' whose model catalogs are large and frequently changing. The rationale is that a hardcoded default becomes stale quickly and doesn't make sense given the variety. Only add WORKER\_DEFAULTS for stable, well-known providers (e.g., anthropic, openai, openai-codex, github-copilot) where a reasonable cheap default model can be identified and is unlikely to disappear. For dynamic providers, leave the defaults map empty and handle fallback differently.

<!-- lore:019ebe0b-fa0e-7d0c-8b60-c75943b20ca1 -->
* **Prefers text block over thinking block**: prefers text block over thinking block,

<!-- lore:019ebcb3-5d82-7591-91dc-c9e587602f24 -->
* **Stop chasing live-log rabbit holes — focus on planned work**: When redirected away from an investigation (e.g. 'stop chasing X; focus on the planned work'), drop it immediately and execute planned tasks in order. Do not theorize about live session data (specific session IDs, cch= content) without first verifying. Directive: verify before theorizing, return to task list when redirected.
