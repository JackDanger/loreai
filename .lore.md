<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019eb931-7257-7ef6-87db-b2dd034c3ea7 -->
* **embedding-worker.ts: self-heal retry for corrupt model — skipped for vendored binaries**: embedding-worker.ts self-heal retry: \`initPromise\` wraps \`loadPipeline()\`; on catch, if \`!vendorModel && isCorruptModelError(msg)\`, calls \`purgeCachedModel()\` then retries once. NEVER post \`{type:'init-error'}\` before the retry — \`embedding.ts\` sets \`localProviderKnownBroken=true\` permanently on init-error. Only the final \`.catch\` may post \`init-error\`. Error classifiers are INLINED — Node ESM resolver cannot map \`./foo.js\` → \`./foo.ts\` for spawned workers. TRAP: \`/load model .\* failed/i\` is too broad — misclassifies 401/403 → purge→redownload→401 loop. \`embedding-worker.ts\` cannot be imported in tests (throws at module top-level if \`!parentPort\`). \`vendorModel=true\`: skip all purge/heal paths (model is read-only inside binary).

<!-- lore:019ebdd7-b5a4-7b44-840d-2bd981d368ff -->
* **gradient.ts: selectDistillations() always preserves meta (gen>=1) entries — never evicted by recency scoring**: gradient.ts selectDistillations() always preserves meta (gen>=1) entries — never evicted by recency scoring. distLimit slots = limit - meta.length; gen-0 scored by recency (70%) and content signal (30%). Meta guarantee retains session history (fixes #417). distilledPrefixCached three-branch: (1) no new rows → cache hit, (2) new gen-0 → append delta (prepends headers, causes duplication in messages\[1]), (3) full re-render on meta rewrite. Cache validity: same sessionID, rowCount grew, last cached row unchanged. Layer 0 (warm-cache) does not inject prefix — only Layer 1+ (compression mode).

<!-- lore:019ecab6-f934-757f-a73e-2a14234d8c4f -->
* **lore gateway: thinking parameter handling — never forwarded in bare API calls, lives in req.metadata**: (architecture) lore gateway: thinking parameter handling — metadata-roundtrip, bare-API exclusion: \`thinking\` is NOT in \`KNOWN\_BODY\_FIELDS\` (anthropic.ts:25-36; only: model, system, messages, tools, max\_tokens, stream). \`parseAnthropicRequest\` puts unknown fields into \`req.metadata\` via loop (lines 226-229) — \`thinking\` arrives as \`req.metadata.thinking\` with shape \`{type:"enabled", budget\_tokens:N}\`. \`buildAnthropicRequest\` egress (lines 486-488) restores metadata fields back to body — both rewritten \`max\_tokens\` AND original \`thinking\` flow to Anthropic. Bare API calls NEVER include \`thinking\` (llm-adapter.ts:897-899). \`cache-warmer.ts\` detects thinking via \`"thinking" in body\`; uses \`max\_tokens:1\` for thinking sessions — LATENT BUG: \`max\_tokens=1\` violates Anthropic's \`max\_tokens > budget\_tokens\` when \`budget\_tokens>1\`, causing 400 on every thinking-session warmup (non-fatal, circuit-breaker protected). Meta request passthrough MUST NEVER enable caching (anthropic.ts:262-263). See also computeMaxTokens gotcha for the thinking-budget interaction bug.

<!-- lore:019ebc36-830a-7307-8a10-9ff12dfa5740 -->
* **openai-responses.ts: Codex delta must live in a dedicated wrapper, not inline conditionals**: openai-responses.ts: Use \`buildOpenAICodexUpstreamRequest\` wrapper and \`applyCodexResponsesDelta\` helper over inline \`req.codex\` guards. Codex has 4+ divergences: URL \`/codex/responses\`, \`store:false\`, control fields passthrough, \`max\_output\_tokens\` suppression. \`applyCodexResponsesDelta\` mutates body post-build. Guard \`if (!extras) return\` runs AFTER setting \`store=false\` — so \`store:false\` always applies even when \`req.extras\` is absent. TRAP: no test that control fields (\`include\`, \`prompt\_cache\_key\`, etc.) are still emitted after refactor — regression could silently drop them. Always add regression tests when fixing Codex builder bugs: (1) suppressed field absent even when client sends it, (2) non-codex path still emits the field, (3) worker-side asserts field absent.

<!-- lore:019ec858-3d58-7239-8458-34089e467a91 -->
* **opencode session abort/cancel mechanism: call sites, log lines, and non-abort paths**: opencode session abort/cancel: HTTP \`POST /session/:id/abort\` → \`promptSvc.cancel(sessionID)\` → \`SessionPrompt.cancel\` → \`SessionRunState.cancel\` → \`Runner.cancel\` → \`Fiber.interrupt\` + \`Deferred.fail(Cancelled)\`. \`processor.ts\` \`Effect.onInterrupt\` sets \`aborted=true\`, calls \`halt(AbortError)\`. Exactly 3 cancel() call sites: (1) HTTP abort endpoint, (2) ACP \`closeSession\`/\`cancel\`, (3) \`control-plane/workspace.ts:630\`. TUI ESC: 3rd qualifying ESC within 5s (\`store.interrupt >= 2\`) triggers abort. Instance eviction calls \`runner.cancel\` DIRECTLY (bypassing \`SessionPrompt.cancel\`) — does NOT log \`message=cancel\`. SSE drop does NOT abort. SIGHUP: only \`destroyRenderer()\`, server session keeps running. SDK sets \`timeout:false\` globally; no inter-chunk watchdog for Anthropic. \`MessageAbortedError\` (zero output tokens) = killed during prefill; \`finish:"length"\` with only reasoning parts = \`max\_tokens\` exhausted by thinking budget.

<!-- lore:019ebc96-83c7-7cd5-98d2-ca85724c2a85 -->
* **pipeline.ts: two curation paths — in-flight vs idle — must be gated separately**: pipeline.ts: Two curation paths — in-flight (mid-turn, gated by \`sessionState.curationScheduled\`) and idle (on idle, flush gen-0 then metaDistill) — must be gated separately. In-flight avoids system\[2] cache busts during active conversations. System\[2] churn root causes: context-health note appended after diff-pinning fluctuates by layer, LTM entry selection changes per-turn. In-flight gen-0 distillation mutates messages\[1] in compression mode. Warm-cache DB divergence: when pin reused, \`ltmCacheText=NULL\` while \`ltmPinText\` populated causes spurious re-pin; fix: persist pinned text. Volatile delta channel (PR #747): keep system\[1]/system\[2] pinned, surface changed/new entries as ID-referenced delta in always-rewritten conversation tail.

<!-- lore:019ec7fc-3bab-7e84-b5b3-e8e9d802a806 -->
* **Supabase auth constraints: email templates blocked on free tier; no device flow**: Supabase auth constraints: Free tier blocks email template customization (HTTP 400); custom SMTP live: \`smtp\_host: mail.smtp2go.com\`, sender \`keeper@withlore.ai\`. Email templates pushed via Management API PATCH (4 fields only). \`supabase config push\` clobbers entire auth config — use Management API PATCH for targeted updates. GitHub OAuth must be enabled separately in Auth → Providers → GitHub. OTP length configurable (not fixed at 6 digits) — observed 8-digit tokens; use \`/^\d{4,12}$/\`. \`lore login --email\` active once custom SMTP configured. Supabase project ref: \`jlwxsrmvomocgngbxmcf\`, region \`us-east-2\`. \`config.toml\` auth settings only affect \`supabase start\` (local stack) — dashboard is authoritative for production. Email templates MUST include \`{{ .Token }}\` in body or users receive only a \`{{ .ConfirmationURL }}\` link. Redirect URLs: must include \`https://\` when not localhost; must include trailing \`/\`.

<!-- lore:019ecaa6-0dc0-76ac-9bc9-be4ef41f6c1e -->
* **sync-data.ts: apply-suppression must be connection-scoped, never a shared persisted row**: sync-data.ts gotchas: (1) \`withApplying\` suppression flag MUST be connection/session-scoped (in-memory variable), never a shared \`team\_config\` row. Shared row causes cross-process bleed: gateway + CLI both open the same DB; connection A sets \`sync.applying='1'\` mid-pull, connection B's legitimate INSERT is silently dropped from \`sync\_outbox\` → permanent local/remote divergence. Additionally, \`withApplying\` is not re-entrant: \`finally\` clears flag unconditionally, so nested calls re-enable capture mid-batch. Fix: save/restore prior value or use a depth counter. (2) \`applyRemoteUpsert\`: \`INSERT OR REPLACE\` does DELETE+INSERT, causing rowid churn on \`knowledge\`/\`entities\`/\`entity\_aliases\`. These tables use external-content FTS5 (\`content=…, content\_rowid=rowid\`). Rowid churn accumulates stale FTS postings; SQLite reuses rowids after deletes (no AUTOINCREMENT), so a future row can inherit a stale rowid and match wrong FTS results. Index bloats unboundedly per sync cycle. \`applyRemoteUpsert\` never calls \`rebuildFts\`. Fix: use \`INSERT … ON CONFLICT … DO UPDATE SET …\` to keep rowid stable.

### Decision

<!-- lore:019ecaa6-0dda-770a-b12c-2b61138af774 -->
* **Sync engine pieces 1+2+3 must land together — piece 1 alone is dead code with migration cost**: Chose to land sync pieces 1+2+3 together over merging piece 1 (commit f42ad40a, v43 migration) alone. Piece 1 has zero syncing capability: \`sync\_outbox\` triggers are gated behind \`sync.enabled='1'\` (nothing sets it yet), \`sync-data.ts\` has no callers, no Supabase client or network code. Merging alone runs the v43 migration for every user on upgrade with no offsetting benefit. Rejected: incremental merge of piece 1 as a foundation PR.

### Gotcha

<!-- lore:019ebe4e-e788-76c6-818d-faad0018b8eb -->
* **cache-analytics.ts mapOffsetToJsonPath: system\[2] insertion yields path 'system\[1]' not 'system\[2]'**: \`inferDivergenceReason(path, prevLength, currLength)\` in cache-analytics.ts: When \`system\[2]\` is inserted on turn 2, offset lands at array boundary — path stays at \`system\[1]\` (bare, no \`.text\`). Turn-2 returns 'stable LTM pinned — system\[2] first injected'; fallback 'stable LTM block diverged'. TRAP: \`grew = currLength - prevLength\` measures full body delta, so >200 byte threshold mislabels re-curations as insertions when system\[1] diverges. Bare vs \`.text\` path suffix is the only reliable discriminator.

<!-- lore:019ebc55-ba2e-7271-9906-905e4f5ed9bf -->
* **CCH seed extraction: seed may persist in binary but algorithm/preimage changed across versions**: CCH seed extraction: consult \`quality/CCH.md\`. Seed \`0x4d659218e32a3268\` (SEED\_2\_1\_138) unchanged in 2.1.175. Hash-input ≠ wire body — cch fn scans for \`cch=\` placeholder, splits/transforms before hashing. Binary is EXEC (not PIE), fixed base \`0x200000\`. CCH\_PLACEHOLDER = \`cch=00000\`; signBody replaces with hash; regex \`/cch=(\[0-9a-fA-F]{5});/\` (trailing semicolon required). Work files in stable dir (e.g. \`~/Code/cch-investigation/\`), not \`/tmp\`.

<!-- lore:019ebe5d-132f-7674-a47a-dd41c820fc07 -->
* **cch-seed-check.yml: @loreai/core must be built before scripts run**: Trap: \`scripts/check-cc-version.ts\` and \`scripts/extract-cch-seed.ts\` both import \`packages/gateway/src/cch.ts\`, which imports \`@loreai/core\` — resolving to \`packages/core/dist/node/index.js\`. The root \`postinstall\` only builds \`@loreai/gateway\`, never \`@loreai/core\`. So after \`pnpm install --frozen-lockfile\`, \`dist/\` is missing and the scripts crash with \`ERR\_MODULE\_NOT\_FOUND\`. Looks safe because \`pnpm install\` succeeds. Fix: add \`pnpm --filter @loreai/core run build\` after install in BOTH \`check-version\` and \`extract-seed\` jobs. Precedent: \`ci.yml\` line 501 uses the same pattern before \`check:docs\`.

<!-- lore:019eb4d0-6fd9-7ef5-b22f-0f156eeca4b0 -->
* **cli/main.ts OPTIONS map: undeclared flags silently become booleans under strict:false**: Trap: \`parseArgs\` with \`strict: false\` treats any undeclared \`--flag value\` as \`{flag: true}\` with \`value\` falling into positionals — looks correct because \`--flag=value\` (equals syntax) still works. Fix: every flag used in ANY subcommand MUST be declared in the global \`OPTIONS\` map in \`packages/gateway/src/cli/main.ts\` with its correct type (\`string\` or \`boolean\`). Discovered in PR #696: \`--to\`, \`--project\`, \`--dry-run\`, \`--no-children\`, \`--min-confidence\` were missing, causing \`--to /path\` to parse as \`{to: true}\` with \`/path\` as a positional.

<!-- lore:019ecab6-f928-7927-a39e-d601fb850af6 -->
* **computeMaxTokens: EMA-based floor ignores thinking budget, causing silent agent loop exit**: computeMaxTokens: EMA-based floor ignores thinking budget, causing silent agent loop exit. Trap: \`computeMaxTokens\` (pipeline.ts) floors \`max\_tokens\` at \`max(8192, 3×outputEMA)\`. Anthropic counts \`max\_tokens\` as combined thinking+visible budget. Model consumes entire 8192 on reasoning → \`finish:"length"\` → no text/tool part → agent loop exits silently. Looks like a normal completion. Fix: pass \`thinkingBudget\` to \`computeMaxTokens\`; set \`baseFloor = thinkingBudget + THINKING\_OUTPUT\_HEADROOM (8192)\`, clamped by \`modelOutput\`. \`THINKING\_OUTPUT\_HEADROOM = 8192\` constant; \`MAX\_TOKENS\_FLOOR = 8192\`. Only ONE \`req.maxTokens =\` write in conversation path (pipeline.ts:5056). \`thinking\` lives in \`req.metadata.thinking\` (not KNOWN\_BODY\_FIELD). Gate: \`if (!isCC)\` — CC clients excluded; opencode does NOT send \`x-claude-code-session-id\` so fix applies. EMA not corrupted by thinking truncation (Anthropic includes thinking tokens in output\_tokens).

<!-- lore:019eb4d0-6fe3-7dc1-b80b-fad31fa29748 -->
* **distillations table schema: NOT NULL columns narrative, facts, source\_ids (not content)**: Trap: \`distillations\` table has no \`content\` column — looks like it should because other tables use \`content\`. Actual NOT NULL columns: \`narrative\`, \`facts\`, \`source\_ids\`, \`generation\`, \`token\_count\`, \`created\_at\`. Also: \`tool\_calls\` table uses \`tool\` (not \`tool\_name\`) and requires \`message\_id\`. Test helpers that insert into these tables must include all NOT NULL columns or get constraint errors. Discovered while writing \`packages/core/test/data-move.test.ts\`.

<!-- lore:019ebb4a-397c-76aa-89c8-93110713f3d4 -->
* **formatKnowledge: title-collision within a category produces non-deterministic render order**: Trap: \`formatKnowledge\` sorts entries within each category by title for byte-stable rendering. If two entries share an identical title, the sort returns 0 and their relative order is non-deterministic — text can swap between turns, busting the prompt cache. This is an edge case (duplicate titles are rare) but worth guarding. Fix: add a secondary sort key (e.g. entry id) as a tiebreak after title comparison, mirroring the id-tiebreak already used in the preference fast-path packing loop.

<!-- lore:019ebafd-32b7-7468-bdb2-5c77ff6c4ccf -->
* **opencode-lore plugin: legacy export path causes getLegacyPlugins() to be invoked**: Trap: lore's plugin module exports named functions (\`applyLoreProviderConfig\`, \`probeGateway\`, \`LorePlugin\`) plus \`export default LorePlugin\`. OpenCode's \`getLegacyPlugins()\` iterates \`Object.values(mod)\` and treats ANY function as a server plugin (\`isServerPlugin\` = \`typeof === 'function'\`). So \`applyLoreProviderConfig\` and \`probeGateway\` are invoked as plugins; \`applyLoreProviderConfig\` returns \`undefined\` → pushed into \`hooks\[]\` → all dispatch loops (\`config\`, \`event\`, \`dispose\`) crash: \`j.config\` / \`A.event\` / \`j.dispose\` on undefined. Fix: lore's plugin must \`default export { server: LorePlugin }\` (plain object with \`server\` key) so \`readV1Plugin\` takes the v1 path and \`getLegacyPlugins\` is never called. Additionally, do NOT export helper functions (\`applyLoreProviderConfig\`, \`probeGateway\`) from the plugin's top-level index — they leak into the legacy loader.

<!-- lore:019ecac3-5c26-764b-a5b9-0475ec38f541 -->
* **Recurring bash failure: aborted**: The \`bash\` tool repeatedly failed with "aborted" across 9 sessions (10 total failures). Sample error: \[Tool execution was interrupted]. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ecac3-5c41-7d91-90a9-81d56716c153 -->
* **Recurring edit failure: aborted**: The \`edit\` tool repeatedly failed with "aborted" across 4 sessions (5 total failures). Sample error: Tool execution aborted. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ecac3-5c2e-7777-97f8-6630dcff6920 -->
* **Recurring edit failure: other:could-not-find-oldstring**: The \`edit\` tool repeatedly failed with "other:could-not-find-oldstring" across 6 sessions (8 total failures). Sample error: Could not find oldString in the file. It must match exactly, including whitespace, indentation, and line endings.. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ecac3-5c1c-770d-8bfc-ee8c3e701fda -->
* **Recurring plan\_exit failure: other:the-user-dismissed-this**: The \`question\` tool repeatedly failed with "other:the-user-dismissed-this" across 4 sessions (5 total failures). Sample error: The user dismissed this question. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019eb94d-db25-72d2-ba71-f2b1ae570c51 -->
* **Recurring read failure: not\_found**: Recurring transient tool failures — expect and retry: \`edit\` fails 'aborted' or 'could-not-find-oldstring' (re-read full file first, match whitespace/indentation/line-endings exactly). \`write\` fails 'aborted' or 'other:the-user-has-specified' (check permissions/rules). \`bash\`/\`task\` fails 'aborted' (interrupted — retry). \`webfetch\` returns 404 for stale docs URLs (check branch/path, try alternate URLs). \`read\` fails 'not\_found' (verify path) or 'offset-is-out-of' (check line count first). \`question\`/\`plan\_exit\` fails 'user-dismissed-this' when dismissed. All are expected transient errors — retry or adjust approach.

<!-- lore:019ecac3-5c5c-7e2b-ab50-22d33c263053 -->
* **Recurring read failure: other:offset-is-out-of**: The \`read\` tool repeatedly failed with "other:offset-is-out-of" across 3 sessions (4 total failures). Sample error: Offset 64 is out of range for this file (57 lines). Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ecac3-5c56-7a2f-8ec1-9e5d3f0a8701 -->
* **Recurring task failure: aborted**: The \`task\` tool repeatedly failed with "aborted" across 3 sessions (4 total failures). Sample error: Tool execution aborted. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ecac3-5c35-7a8f-90c0-0edb1287fcc7 -->
* **Recurring webfetch failure: other:statuscode-non-xx-status**: The \`webfetch\` tool repeatedly failed with "other:statuscode-non-xx-status" across 5 sessions (11 total failures). Sample error: StatusCode: non 2xx status code (404 GET https://supabase.com/docs/guides/local-development/cli/config). Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ecac3-5c4f-71eb-b78d-ae9f1776e21e -->
* **Recurring write failure: aborted**: The \`write\` tool repeatedly failed with "aborted" across 4 sessions (4 total failures). Sample error: Tool execution aborted. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ecac3-5c48-7790-91e1-4db74b954118 -->
* **Recurring write failure: other:the-user-has-specified**: The \`write\` tool repeatedly failed with "other:the-user-has-specified" across 4 sessions (4 total failures). Sample error: The user has specified a rule which prevents you from using this specific tool call. Here are some of the relevant rules \[{"permission":"\*","action":"allow","pattern":"\*"},{"permission":"edit","patter. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebcab-947e-76d8-8dcd-294c8fd5b164 -->
* **sessionState.curationScheduled typed boolean | undefined — coerce with !! before passing to strict boolean param**: Trap: \`sessionState.curationScheduled\` is typed \`boolean | undefined\` in pipeline.ts session state. Helper functions like \`shouldRunInFlightCuration\` declare their params as \`boolean\` (not \`boolean | undefined\`), so passing the field directly causes TS2322. Looks safe because the value is only ever set to \`true\` explicitly. Fix: coerce with \`!!sessionState.curationScheduled\` at the call site. Applies to any \`sessionState\` field that starts undefined and is only assigned truthy values.

<!-- lore:019ebe57-26dd-7356-b5e9-feae1dbb9cd6 -->
* **ui.ts esc() + JS confirm(): HTML-decoded entities break single-quoted JS strings**: Trap: \`esc()\` converts \`'\` → \`\&#39;\`, safe for HTML attributes. But inline JS event handlers (\`onsubmit="return confirm('...')"\`) are HTML-parsed first — browser decodes \`\&#39;\` → \`'\` before JS engine sees it, so a title like \`O'Brien\` breaks out of the single-quoted \`confirm()\` string. Latent XSS/JS-injection risk exists in both knowledge banner (ui.ts:1843) and entity banner (ui.ts:3118). Fix: double-escape or move to \`data-\*\` attributes + JS event listeners. Also: \`ltm.getDedupFeedback(projectId)\` has NO \`kind\`/\`source\` filter — returns ALL \`dedup\_feedback\` rows, so entity rows could pollute knowledge threshold calibration in \`calibrateDedupThreshold\`. Pre-existing latent bug.

### Pattern

<!-- lore:019eb4d0-6fed-71ff-baca-7151d61bf315 -->
* **api.ts resolveProject() priority and parseBody zstd support**: \`packages/gateway/src/api.ts\`: \`resolveProject()\` priority: (1) \`:id\` route param, (2) \`?git\_remote=\`, (3) \`?path=\`. POST dispatch: literal routes first, then parameterized. TRAP: \`moveSessions()\`/\`reassignKnowledge()\` update \`knowledge SET project\_id=?\` but leave \`knowledge\_transfers\` rows pointing to old project. \`moveSessions()\` BFS-expands children before moving; \`sessions\_moved\` return overcounts if BFS includes children from other projects. \`rebindActiveSession\` iterates only user-provided IDs, not BFS-expanded children. \`parseBody\<T>()\` supports optional zstd decompression via \`Content-Encoding: zstd\`.

<!-- lore:019ebe57-26d4-78b4-885e-53acd79fb38f -->
* **db.ts ensureProject: /test/ path guard uses LORE\_DB\_PATH-unset as production proxy**: \`ensureProject\` (db.ts:1584–1592) rejects paths matching \`/^\\/test\\/\` when \`LORE\_DB\_PATH\` is unset, treating unset env var as a proxy for production DB. Acknowledged limitation: won't catch case where \`LORE\_DB\_PATH\` is explicitly set to the prod path. \`cmdDedup\` in data.ts mirrors this by filtering \`/^/test//\` project paths before calling \`ltm.deduplicate()\` — belt-and-suspenders guard so test-bucket projects are never deduped in production runs. Synthetic unattributed buckets get a clearly-marked provisional name so the dashboard never shows a bare session ID as a real repo.

<!-- lore:019ec81f-7bc5-7cd7-bd64-16d343c83bd1 -->
* **login.ts canOpenBrowser(): auto-detect browser capability for OAuth flows**: \`canOpenBrowser()\` in \`packages/gateway/src/cli/login.ts\` checks: \`LORE\_NO\_BROWSER\` env var (force-disable), \`CI\` env var, \`SSH\_CONNECTION\`/\`SSH\_TTY\` (headless SSH). On Linux: requires \`DISPLAY\` or \`WAYLAND\_DISPLAY\`. WSL (\`WSL\_DISTRO\_NAME\`) treated as browser-capable. \`--no-browser\` flag forces manual/device flow. Two GitHub login flows: (1) loopback callback — opens browser automatically; (2) device/manual flow — prints terminal QR code + URL via \`qrcode-terminal\` (\`{ small: true }\`). QR error-correction level \*\*L\*\* (minimum terminal height ~27 lines). Loopback server created BEFORE \`signInWithOAuth\` (correct — port bound before redirect). No \`state\`/CSRF check on loopback; mitigated by loopback-only bind, ephemeral port, single-shot, and PKCE — local racer can DoS but not steal credentials. 10-min timeout \`timer\` is \`unref()\`'d but never cleared on success (harmless NIT).

<!-- lore:019ebe57-26bc-7048-8c84-a8cbad611781 -->
* **ui.ts knowledge dedup banner: Merge deletes duplicates, no content absorption**: Knowledge dedup (both CLI \`--apply\` and dashboard Merge via \`/ui/api/merge/knowledge/:survivingId/:sourceId\`) is delete-by-design: survivor kept, duplicates removed via \`ltm.remove()\`, no content absorption. Contrast: entity merge absorbs aliases into target via \`entities.merge()\`. \`ltm.\_dedup\` star-clustering picks survivor by: highest confidence → most recent → shortest title. Dashboard routes record \`recordDedupFeedback\` with \`source='dashboard'\` (new \`DedupFeedbackSource\` value), which feeds \`calibrateDedupThreshold\` — consistent with existing \`cli\_yes\`/\`cli\_interactive\` behavior.

### Preference

<!-- lore:019ebc68-e704-760e-b022-ed1aaf6457b2 -->
* **Always preserve raw bytes when handling HTTP request bodies**: Handle HTTP bodies as Buffer\[] with Buffer.concat, never string concatenation. Use latin1 encoding for text search in binary bodies. Copy bytes before mutation; guard offset writes. Pass as bytes not strings.

<!-- lore:019ecabb-1740-7009-8e3f-47e0ba9d6f5d -->
* **Always request adversarial code review with file:line evidence and severity-categorized findings**: (preference) Always use file:line evidence for both reading code and adversarial review: When reading or reviewing code, every claim about behavior must cite specific file:line from actual code reads — never assume from memory. For reviews, request adversarial focus on real bugs (logic errors, races, missing guards), not style. Findings must cite file:line, be categorized by severity (BLOCKER, SHOULD-FIX, NICE-TO-HAVE, NON-ISSUE), and include actionable fixes. Verify all bypass paths, not just happy path. Run adversarial/self-review before merging non-trivial changes — passing CI is necessary but not sufficient. Record blockers as hard assertions (🔴) and defer/restruct merge plan. Treat inline code comments as authoritative design directives (NEVER/Always/MUST are binding); when tests violate directives, tests are wrong.

<!-- lore:019ec105-fe7d-7c64-9b29-712e0343951c -->
* **Always require backups before any destructive or write operation on live databases**: Backup before any write on live DBs: snapshot → dry-run → apply. \`lore data split\` uses \`backupDatabase\` via \`VACUUM INTO ?\` (bound param) on live connection — consistent snapshot, no WAL/SHM risk. \`--no-backup\` skips for non-live DBs. NEVER delete WAL/SHM files. \`backupDatabase\` pre-clears \`dest\`, \`dest-wal\`, \`dest-shm\` via \`rmSync({force:true})\` (VACUUM INTO refuses overwrite). Auto-generates \`${src}.backup-\<ISO-timestamp>\`. TRAP: post-apply validation message 'A backup was taken' is false when \`--no-backup\` passed — gate on \`!noBackup\`; when skipped, say 'No backup was taken (--no-backup) — manual recovery required.' Guard: \`if (resolve(dest) === resolve(src)) throw\` — callers must NEVER pass live DB path as dest. \`--interactive\`/\`-i\` flag added to \`lore data split\` for interactive plan review.

<!-- lore:019ecac1-14f6-7ee6-87d5-615a547275ad -->
* **Always run adversarial subagent code review before shipping a fix**: Before merging or finalizing any bug fix or feature, the user consistently commissions a dedicated adversarial/critical review — either a subagent task or an independent reviewer — to verify correctness with file:line evidence, identify gaps (correctness, edge cases, test coverage), and produce a ranked list of issues (blocker / should-fix / minor / nit). The review must confirm specific invariants the user cares about (e.g., 'never below floor', 'never exceeds model limit') and trace all relevant code paths. Only after the review verdict (e.g., 'SHIP WITH MINOR FIXES') does the user proceed. This applies to both new features and targeted bug fixes.

<!-- lore:019ebe56-3cd7-7530-aa15-80b82b7786e3 -->
* **Always use MIGRATIONS.length for schema version in UPDATE statements**: Always use \`MIGRATIONS.length\` for schema version instead of hardcoded constants in \`UPDATE schema\_version SET version = ...\` statements. Hardcoded version numbers become stale when migrations are added and cause schema version mismatches. Using \`MIGRATIONS.length\` ensures the version is always derived from the actual migration array length.

<!-- lore:019ecabd-6cea-774e-8fdd-0183d3cc4df0 -->
* **Always verify code assertions with file:line evidence before accepting them as true**: When reviewing or implementing fixes, every claim about code behavior must be backed by specific file:line citations from actual code reads. Always read the relevant source lines and cite them explicitly rather than assuming behavior from memory or inference. Embed invariants as runtime guards + inline MUST/NEVER/Always comments co-located with the code they protect. Treat inline code comments as authoritative design directives: comments like 'NEVER overwrites a confident binding', 'Never throws', 'always binds the session' are binding design rules. When tests encode behavior violating a directive, flag the test as wrong and rewrite it. 🔴-marked directives in .lore.md are hard behavioral invariants — verify and acknowledge before proceeding.

<!-- lore:019ebde2-37a4-78b8-a749-edcd3c40d1da -->
* **Never add provider-specific worker defaults for providers with frequently changing model catalogs**: When discussing worker model defaults, the user explicitly rejects adding WORKER\_DEFAULTS entries for providers like 'opencode' whose model catalogs are large and frequently changing. The rationale is that a hardcoded default becomes stale quickly and doesn't make sense given the variety. Only add WORKER\_DEFAULTS for stable, well-known providers (e.g., anthropic, openai, openai-codex, github-copilot) where a reasonable cheap default model can be identified and is unlikely to disappear. For dynamic providers, leave the defaults map empty and handle fallback differently.

<!-- lore:019ebe56-3cf3-7441-9125-9e5fe3a5c8bb -->
* **Prefers AGENTS.md over CLAUDE.md for agent instructions**: Prefers AGENTS.md over CLAUDE.md for agent instructions. When creating or referencing agent instruction files, use AGENTS.md.

<!-- lore:019ecabf-b0a6-76c1-a6d7-6dd8e39e831c -->
* **Prefers authoritative inference over stale X-Lore-Project header (#762)**: prefer authoritative inference over stale X-Lore-Project header (#762).

<!-- lore:019ec11e-29e5-7e21-aa2e-346e9be1dc31 -->
* **Reject premature optimization that risks harming the common case**: Reject premature optimization that risks harming the common case. Evaluate suggestions against impact on the 80% common case; explicitly reject changes that may harm it. Prefer trusting existing systems over adding complexity. When proposing changes: (1) clearly justify why needed, (2) assess common-case impact, (3) accept user's verdict when they decide current behavior is acceptable. Do not push further optimization once user has decided.

<!-- lore:019ebe56-3cfa-7b49-b786-3d603e608a37 -->
* **Test file conventions: Vitest, kebab-case, import style**: Always write new tests with \`import { describe, it, expect } from 'vitest'\`. Use kebab-case file naming for test files. Prefer writing more tests over lowering coverage thresholds or excluding files. Project migrated from Mocha+Chai+ts-node to Vitest (May 2026; 312ms vs 30s startup). Run tests from repo root, not package subdirectory — \`vitest\` with a filter from a subdirectory returns 'No test files found'.
