<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019ee004-037c-70e6-a910-eef98c0778aa -->
* **A1 quota trigger: counter-based enforce\_row\_quota — only INSERTs and revivals grow live-row count**: Post-A1 \`enforce\_row\_quota()\` (migration 0007): reads O(1) from \`user\_table\_usage(scope\_id, table\_name, row\_count, byte\_count)\`. Advisory lock keyed on \`hashtextextended(scope\_id::text || '|' || tg\_table\_name, 0)\` — MUST be taken before \`exists\_pk\` check (0005 ordering); 0007 moved \`exists\_pk\` outside lock (lines 226-239 before lock at 267) — SHOULD-FIX: restores TOCTOU hardening. Revival is an UPDATE with \`d\_bytes=0, d\_rows=0\` — guard returns early, never gated (row already counted at INSERT). Tombstone INSERTs now counted+gated (physical accounting). \`greatest(...,0)\` clamp never over-removes. \`user\_table\_usage\` has NO DML grant to \`authenticated\` — written ONLY by SECURITY DEFINER triggers. Raises errcode 23514 on violation. Self-DoS: user creating+deleting ~500 entries can exhaust free cap with zero live rows; no client-side reclaim path until A2 hard-DELETE reaper.

<!-- lore:019ee565-89a2-7cb9-a0fe-6516ab295d34 -->
* **A2 knowledge\_meta: mutable convergent register for confidence — separate from immutable version rows**: \`knowledge\_meta(logical\_id, confidence, last\_reinforced\_at)\` holds the only mutable state per logical entry. Version rows in \`knowledge\` are truly immutable (title/content/category/is\_deleted never change after insert). \`knowledge\_current\` view joins \`knowledge\` + \`knowledge\_meta\` so callers see \`confidence\` as a normal column. Confidence sync strategy: last-writer-wins now; real merge (\`max\` or recency-weighted) planned for team divergence. \`last\_reinforced\_at\` is per-device relevance clock — excluded from sync. Converging confidence across devices/team members is a core collaboration goal; future work needed for team divergence merge. \`logical\_id\` is the stable identity surviving all edits; entity-refs, \`.lore.md\` marker, embedding, sync all reference \`logical\_id\` not per-version id.

<!-- lore:019ee640-0aff-7be7-8dea-2d85823c8a4b -->
* **A2 sub-PR 2a: two read-rewrite patterns for knowledge\_current migration**: Sub-PR 2a routes all 69 \`knowledge\` reads through the new view/register. Two distinct patterns: 1. \*\*Non-FTS reads\*\* (\`FROM knowledge\`): rewrite to \`FROM knowledge\_current\` — straightforward table swap. 2. \*\*FTS-join reads\*\*: stay on \`knowledge\` base table (need \`k.rowid\` for external-content FTS5 join) but add \`LEFT JOIN knowledge\_meta\` for \`confidence\`/\`last\_reinforced\_at\`. Some \`data.ts\`/\`db.ts\` count/merge reads need per-site judgment — not all should be current-only (e.g., integrity counts, admin views). \`knowledge.confidence\` becomes vestigial after 2a; \`update()\`/\`remove()\` still mutate in-place so \`knowledge\_current ≡ knowledge\` — behavior preserved.

<!-- lore:019ee535-02c2-7e66-93a1-5fb8ba605c86 -->
* **A2/#823: append-only knowledge schema — FTS and FK CASCADE constraints**: A2 migration = Version 50 (index 49 in MIGRATIONS array) — NOT 49, because PR #828 (profiles pull-only mirror) consumed v49. \`db.test.ts:61\` must assert version 50. Architecture: \*\*Option A\*\* (single append-only table). Key structures: \`knowledge(logical\_id, version, is\_deleted, ...)\` immutable rows; \`knowledge\_meta(logical\_id, confidence, last\_reinforced\_at)\` mutable convergent register; \`knowledge\_current\` view joins both (non-FTS reads swap \`FROM knowledge\` → \`FROM knowledge\_current\`). FTS triggers index only current version; drop superseded version's FTS row on append. FK refs (\`knowledge\_entity\_refs\`, \`knowledge\_refs\`) rebind to \`logical\_id\`. \`knowledge\_tombstones\` table eliminated — delete = \`is\_deleted=true\` version row. \`recoverMissingObjects()\` must include A2's new tables. Supabase migrations 0001–0007 all applied; Sub-PR 3 needs remote migration 0008+ for \`logical\_id\`/\`version\`. PR #830 merged 2026-06-20; PR #831 (sync registry-contract + identity-lifecycle invariant batteries) merged; PR #838 (warmup diagnostics) merged 2026-06-20. Main is now at v49.

<!-- lore:019ecd94-4df4-787b-89f7-31873df5ec0e -->
* **agents.ts: AGENTS registry shape and run.ts explicit-command spawn lookup**: \`AgentDef\` interface (agents.ts:16-33): \`name\`, \`displayName\`, \`binary: string\`, \`detect(): string | null\`, optional \`envVars()\`, optional \`cliArgs()\`. \`binary\` MUST be a constant placeholder string (NOT resolved at module load — avoids stale registry in long-running processes and platform-mutation test issues). \`detect()\` is called fresh each invocation. \`run.ts\` explicit-command branch: if matched by \`agent.name === cmdArgs\[0]\` → use \`detect()\`; if matched by \`agent.binary === cmdArgs\[0]\` → use literal \`cmdArgs\[0]\` (zero behavior change for existing PATH agents). \`main.ts\` gatekeeper MUST use \`AGENTS.some((a) => a.name === command || a.binary === command)\` — NOT \`AGENTS.map(a => a.binary).includes(command)\` (binary is a placeholder, not the resolved path). Current registry (6 agents): \`claude-code\`, \`claude-code-desktop\`, \`codex\`, \`pi\`, \`opencode\`, \`hermes\`.

<!-- lore:019edc02-dfc2-710a-8997-09f649d496dd -->
* **anthropic.ts: 3-block system\[] assembly in buildAnthropicRequest**: In \`buildAnthropicRequest\` (anthropic.ts:374-433), system is assembled as a variable-length block array when \`systemTTL\` is set: - \`system\[0]\`: \`req.system\` (host/agent prompt) — no \`cache\_control\` unless no stableLtm (fallback: line 416) - \`system\[1]\`: \`stableLtmSystem\` — ALWAYS carries \`cache\_control\` breakpoint (1h or bare ephemeral) - \`system\[2]\`: \`ltmSystem\` (contextLtm) — NO \`cache\_control\`; rides conversation-cache breakpoint Array collapses if stableLtm/contextLtm absent. No-cache fallback (line 430-431): concatenates all LTM into single string. Natural attach point for a future \`system\[3]\` dynamic-tail block: after line 424, before \`body.system = blocks\` at line 427 — must carry NO \`cache\_control\` (second system breakpoint would corrupt prefix coverage). \`normalizeSystem()\` (lines 125-137) flattens incoming system to plain string, discarding client-side \`cache\_control\`.

<!-- lore:019ee581-7943-7be0-a5c0-455b7cec4877 -->
* **cache-warmer + gradient.ts: shared economic model gap — warmer and bust calculator are decoupled heuristics**: 🔴 Design goals (user-stated): (1) Stay on same gradient level as long as economically sensible; (2) Layer change = expected cache bust; (3) Warmups orthogonal to compaction decisions. Core gap: warmer's economic calculations and gradient.ts \`shouldCompress\` (bust calculator) are separate heuristics with no shared inputs. Warmer assumes full-body write on return; \`shouldCompress\` treats \`continueCost\` as cheap read (repriced to write only under \`consecutiveBusts\`). Only coupling: heuristic boolean \`isCacheWarm()\` → \`onIdleResume(skipCompact)\` at gradient.ts:558. Oscillation risk: warmer's \`writeEfficiency\` guard kills warming → flips \`isCacheWarm\` false → re-enables compaction → busts the cache warming was preventing. Task #2 (shared cache economics refactor) in progress: 7-item plan — pure core cache-economics module, wire shouldWarm (warmer), wire shouldCompress/onIdleResume (gradient), gate deferred LTM/distillation/prompt-delta flush on COOL+BUST. PR #838 merged (diagnostics); refactor is next.

<!-- lore:019ee578-4eb7-7473-9ea3-0b15235d1c52 -->
* **cache-warmer: lastRequestBody invalidation gap — idle mutations not covered**: Root cause of warmup cache misses on large sessions: \`lastRequestBody\` (set at \`cache-analytics.ts:590\`) stores the normalized post-transform body that established the upstream cache. The 4 null-out sites in \`pipeline.ts\` (lines 4293, 4719, 4898, 4997) cover ONLY client-side \`/compact\` and model/provider switches — NOT lore's own idle-time prefix mutations. When meta-distillation rewrites \`messages\[1]\`, post-idle gradient compaction halves raw budget, or prompt-delta upserts shift message positions, \`lastRequestBody\` is NOT invalidated. Warmer replays pre-mutation body; next real turn discards it. Fix: null out \`lastRequestBody\` at each idle-time mutation site. Architectural tension: lore rewrites the PREFIX (append-only cache-friendliness vs aggressive compression are in conflict). PR #838 (diagnostic logging) merged 2026-06-20 to distinguish expiry vs body-mismatch misses.

<!-- lore:019ee201-4395-7551-83fd-54ff714be1dc -->
* **cache-warmer: per-bucket circuit breaker key and recovery paths**: Per-bucket circuit breaker (PR #836, Jun 20 2026): bucket key = \`sessionID \x1f model \x1f upstreamURL\`; \`unknown\` substituted for null fields but \`executeWarmup\` is gated behind \`resolveProfile\` returning non-null so \`unknown\` bucket never trips. \`BreakerEntry = { failures: number; tripped: boolean; trippedAt: number }\` — \`failures\` in-memory only; only tripped buckets persisted. KV v2 format: \`{ version: 2, buckets: { \[key]: { trippedAt } } }\`. Legacy v1 auto-cleared on first load. \`pruneExpiredCircuitBreakers()\`: GC for dead-session stale buckets — wired into idle tick before per-session loop. Three reset paths: (1) \`resetCircuitBreaker(bucketKey?)\` — production reset; (2) \`/lore:warm:reset\` slash command; (3) \`POST /ui/api/warming/:sessionId/reset\`. \`getCircuitBreakerSummary(now)\` filters display but does NOT delete. \`getCircuitBreakerStatus\` is a PURE READ — computes decay inline, no map mutation, no SQLite write. \`CIRCUIT\_BREAKER\_DECAY\_MS\` = 6h auto-decay constant.

<!-- lore:019ee581-794e-7b61-8032-23b8a00a4c30 -->
* **cache-warmer: UNCACHED warmup diagnostic — cacheLikelyAlive divergence logging (PR #838)**: PR #838 (\`fix/warmup-uncached-diagnostic\`): enhanced UNCACHED branch (cache-warmer.ts lines 1836–1840) to log \`cacheLikelyAlive\`, \`ageSinceCacheTouch\`, \`ttl\`, \`bodyBytes\`. When \`cacheLikelyAlive=true\` → logs 'DIVERGENCE: cache should have been live but warmup missed' (body mismatch path). When \`cacheLikelyAlive=false\` → normal expiry log. \`cacheLikelyAlive\` computed at line 1709: \`lastCacheTouch > 0 && Date.now() - lastCacheTouch < profile.ttlMs\`. \`storedBody = decompressBody(lastRequestBody)\` at line 1712; \`warmupBody = profile.prepareWarmupBody(storedBody)\` at line 1715. Commit b67073e7. This is diagnostic-only; the shared economic model fix is a separate follow-on.

<!-- lore:019edc02-dfde-7130-b70c-99ddf8734dcd -->
* **cache-warmer.ts: creditWarmupHit savings attribution — use lastWarmupRefreshTokens, not returning turn's cacheReadInputTokens**: 🔴 \`creditWarmupHit\` (cache-warmer.ts:1730-1785): savings MUST be credited against \`lastWarmupRefreshTokens\` (the prefix the warmup actually refreshed, recorded at line 1696), NEVER the returning turn's \`cacheReadInputTokens\`. Tool-use continuations / breakpoint shifts report a far smaller read on the returning turn, undercounting savings ~10×. Phantom-savings guard: hit only attributed when \`lastWarmupRefreshTokens > 0 && totalWarmups > 0\`. \`lastWarmupAt\` alone is insufficient (can ride along in restored/inherited blob). On confirmed hit: zeros both \`lastWarmupAt\` and \`lastWarmupRefreshTokens\` (warmup benefits only the first returning turn); increments \`warmupHits\`.

<!-- lore:019ee018-a829-706c-802d-bc63bea6da41 -->
* **cache-warmer.ts: global circuit breaker — persisted to KV, no production reset path**: Cache warmer circuit breaker (\`cache-warmer.ts\`): global across ALL sessions/providers (not per-session). Trips after \`CIRCUIT\_BREAKER\_MAX\_FAILURES=3\` consecutive uncached warmups (\`cacheCreationTokens>0 AND cacheReadTokens===0\`). State persisted to SQLite KV under \`CB\_KV\_KEY='warmup\_circuit\_breaker'\` — survives process restarts. Once tripped, ALL warming disabled for all sessions. \`ensureCircuitBreakerLoaded()\` restores state on first access. Counter resets ONLY on successful cache read, NOT on errors. No production reset mechanism — \`\_resetForTest()\` is the only reset path (writes \`{tripped:false,failures:0}\` to KV). No UI endpoint to reset. \`computeWarmingSnapshot()\` returns \`notWarmingReason: 'Circuit breaker tripped'\` when latched. Sentry metric: \`lore.cache\_warmup.circuit\_breaker\_tripped\`.

<!-- lore:019edb00-69e2-7282-a2a3-edd5482dbd84 -->
* **consecutiveBusts is in-memory only — not restored across restarts, written to DB as diagnostic only**: \`consecutiveBusts\` in \`gradient.ts\` is intentionally NOT restored from DB across restarts. Written to \`dynamicContextCap\` DB column as write-only diagnostic. \`SUSTAINED\_BUST\_THRESHOLD=2\`; \`BUST\_PRESSURE\_THRESHOLD=3\`. \`unsustainable: true\` can ONLY surface on paths B/C/D — never on layer-0 passthrough (path A). Restart resets bust pressure intentionally. \`recordCacheUsage(isIdleResume=false)\`: if \`bustRatio > 0.5 && isIdleResume\`, logs but does NOT increment \`consecutiveBusts\`; if \`bustRatio <= 0.5\`, resets to 0 regardless of \`isIdleResume\`. Cache bust classification via \`categorizeBust(analysis, isPostIdle)\` in \`cache-analytics.ts\` — \`CacheBustCause\` union has 9 literals: \`"first-turn"\`, \`"system-host-change"\`, \`"system-ltm-change"\`, \`"tools-change"\`, \`"prefix-rewrite"\`, \`"window-shift"\`, \`"idle-resume"\`, \`"incremental"\`, \`"unknown"\`. \`"system-change"\` split (T4, PR #791) into \`"system-host-change"\` (system\[0]) and \`"system-ltm-change"\` (system\[1]/system\[2]).

<!-- lore:019ee535-02ce-78d7-b274-cde8d8f03e6b -->
* **db.ts: MIGRATIONS array structure, migrate() invariants, and schema\_version normalization**: MIGRATIONS array: \`db.ts:42-1220\`, 49 entries (versions 1–49) after PR #828 merged (v49 = profiles mirror). A2 (#823) will be v50 (index 49 in MIGRATIONS array). \`migrate()\` at \`db.ts:1409-1466\`. \`VACUUM\_MIGRATION\_INDEX=2\` (db.ts:1407) — version-3 migration runs outside transaction. After loop: \`UPDATE schema\_version SET version = MIGRATIONS.length\` (db.ts:1461) — NEVER hardcode version number, always normalize to array length. \`stripAppliedAlters()\` swallows duplicate-column errors for idempotency. \`recoverMissingObjects()\` (db.ts:1490-1692) idempotently re-creates fragile tables — new tables/columns from any migration must also be added there. Schema version asserted in \`db.test.ts:61\`. Note: \`rg -c '-- Version'\` returns 47 (not 48) because v37 at db.ts:1033 is a bare one-liner with no \`-- Version 37\` comment.

<!-- lore:019eb931-7257-7ef6-87db-b2dd034c3ea7 -->
* **embedding-worker.ts: self-heal retry for corrupt model — skipped for vendored binaries**: embedding-worker.ts self-heal retry for corrupt model — skipped for vendored binaries. \`initPromise\` wraps \`loadPipeline()\`; on catch, if \`!vendorModel && isCorruptModelError(msg)\`, calls \`purgeCachedModel()\` then retries once. NEVER post \`{type:'init-error'}\` before the retry — \`embedding.ts\` sets \`localProviderKnownBroken=true\` permanently on init-error. Only the final \`.catch\` may post \`init-error\`. Error classifiers are INLINED — Node ESM resolver cannot map \`./foo.js\` → \`./foo.ts\` for spawned workers. TRAP: \`/load model .\* failed/i\` is too broad — misclassifies 401/403 → purge→redownload→401 loop. \`vendorModel=true\`: skip all purge/heal paths (model is read-only inside binary).

<!-- lore:019ee64d-c30c-7cfe-b5ac-5b8a75ab7102 -->
* **gradient.ts evaluateCacheStrategy: pricing override required for background warmer callers**: \`evaluateCacheStrategy(sessionID, survival, pricing?, now?)\` reads global pricing via \`getCachePricing()\` as fallback. Module-global pricing reflects the LAST transform's model — may be a different session/model when called from the background cache warmer. Callers in \`cache-warmer.ts\` MUST pass session-own model pricing explicitly. Comment in gradient.ts warns callers of this. Guard: returns null if no state or \`cacheSizeFull <= 0\` (session never transformed). NaN/degenerate-cost guard in \`decideCacheStrategy\` (lines 163–177) returns \`confident: false\` — shadow log gated on \`shadow?.confident\`, so garbage data cannot produce a misleading confident log entry.

<!-- lore:019ee58a-a5e5-7e44-86d9-85b37acd69f0 -->
* **gradient.ts: shouldCompress() economics — sustained busts mean compress, not stop**: 🔴 Two key invariants from code comments: (1) \`shouldCompress()\` deliberately does NOT say 'never compress' on high bust count — structural system\[2] busts eliminated by reorder-tolerant LTM pin; sustained bust now indicates raw-window growth, so compressing (one write) is cheaper than rewriting the whole grown context every turn. (2) Compression 'never busts an expensive cache' — \`shouldCompress()\` always returns true in that context (gradient.ts:408). Economics: \`bustCost = compressedTokens × cacheWriteCostPerToken\`; \`continueCost = currentTokens × (writePerToken if sustainedBust else readPerToken)\`; compress when \`bustCost < threshold × continueCost\`. \`cacheWriteCostPerToken\` reset to 0 at line 835 (free-write path). Call site at line 2431 passes \`freeWrite\` flag.

<!-- lore:019ecab6-f934-757f-a73e-2a14234d8c4f -->
* **lore gateway: thinking parameter handling — never forwarded in bare API calls, lives in req.metadata**: Lore gateway thinking parameter: NOT in KNOWN\_BODY\_FIELDS → parseAnthropicRequest puts it in req.metadata.thinking. buildAnthropicRequest restores metadata. Bare calls NEVER include thinking. cache-warmer detects via \`'thinking' in body\`; uses max\_tokens:1 for thinking sessions. LATENT BUG: max\_tokens=1 violates Anthropic's max\_tokens > budget\_tokens when budget\_tokens>1, causing 400 on every thinking-session warmup (non-fatal, circuit-breaker protected — but each 400 counts as an UNCACHED failure and increments the global circuit breaker failure counter). Meta request passthrough MUST NEVER enable caching. resignBody gate uses hasBillingHeader(req.system).

<!-- lore:019edfbc-edd5-780d-ac81-07a35390121b -->
* **ltm.ts: confidence lifecycle — decay, reinforcement, eviction, and cross-project protection**: Knowledge confidence lifecycle (PR #814/#815/#816/#820 stack, v48): \`decayProject(path)\` decrements confidence on stale entries; never bumps \`updated\_at\`. \`markInjected(ids)\` writes only \`last\_reinforced\_at\`. \`evictLowestValue\` and \`pruneDeadEntries\`/\`pruneDeadEntriesAllProjects\` MUST skip \`cross\_project=1\` entries. \`DEAD\_CONFIDENCE\_FLOOR=0.2\`. \`enforceEntryCap\` counts promoted rows but eviction excludes them. Global sweep in \`idle.ts\` runs \`pruneDeadEntriesAllProjects()\` on first tick then hourly (\`GLOBAL\_DEAD\_SWEEP\_INTERVAL\_MS=3600000\`), gated on \`loreConfig().knowledge.enabled\`. \`lastGlobalDeadSweepAt\` starts at 0 (first tick always fires), set BEFORE try block (prevents retry storm on exception), resets on restart. Sweep wrapped in try/catch — errors logged, never propagated. \`pruneDeadEntriesAllProjects()\` queries \`WHERE cross\_project=0 AND confidence<=0.2 LIMIT ?\` — only project-owned rows; global/promoted entries never touched. Motivation: per-session \`pruneDeadEntries\` only fires for active project; idle projects accumulate zombie entries forever.

<!-- lore:019edfc4-d6b0-7691-9a79-10d86613ef14 -->
* **ltm.ts: cross\_project=0 always implies non-null project\_id — enforced at create()**: Hard invariant (🔴): \`cross\_project=0\` rows ALWAYS have a non-null \`project\_id\`. Enforced at \`ltm.ts:92-99\` in \`create()\`: \`pid = (scope==='project' && projectPath) ? ensureProject(projectPath) : null\`; then \`crossProject = pid === null ? true : (input.crossProject ?? false)\` — null pid forces \`cross\_project=1\` (comment: 'IF-2: Global entries (pid=null) must be cross-project to avoid a data hole'). Migration \`db.ts:1046-1051\` sets \`cross\_project=0\` only WHERE \`project\_id IS NOT NULL\`. Therefore \`WHERE cross\_project=0\` implicitly guarantees \`project\_id IS NOT NULL\` — no explicit null check needed in queries like \`pruneDeadEntriesAllProjects()\`.

<!-- lore:019ee535-02d6-740f-8194-e73f9618892c -->
* **ltm.ts: full read-path SQL surface — all knowledge table queries**: All knowledge SQL in ltm.ts: title-match lookups at lines 114/119/140 (project/null/cross\_project variants, \`confidence > 0\`). INSERT at line 169. UPDATE at line 308. \`SELECT project\_id\` at 326. DELETE at 339. FTS queries at 450-458: \`SELECT k.id, k.title FROM knowledge\_fts f CROSS JOIN knowledge k ON k.rowid = f.rowid\`. Bulk SELECTs at 542/559/568/599/623. \`markInjected\` UPDATE at 717. \`vectorSearch()\` SQL: \`SELECT id, embedding FROM knowledge WHERE embedding IS NOT NULL AND confidence > 0.2\`. All read paths filter \`confidence > 0.2\` or \`confidence > 0\`. A2 must update ALL these sites to read from the current-version view.

<!-- lore:019ee013-8bbe-7e78-8da7-dec71009fa42 -->
* **migration 0007: scope\_id immutability invariant and author\_id forge risk**: Migration 0007 v1 invariants (post-re-review): \`scope\_id = author\_id = auth.uid()\` enforced. Both prior gaps resolved: (1) scope mutations blocked by BEFORE guard at \`0007:214-217\` (even for service\_role); (2) author\_id forge blocked by RLS \`WITH CHECK (scope\_id=auth.uid() AND author\_id=auth.uid())\`. SHOULD-FIX: \`exists\_pk\` check moved outside advisory lock (lines 226-239 before lock at 267) — weakens 0005 TOCTOU hardening; spurious reject is safe (self-heals on retry), ±1 overshoot unreachable until A2 hard-DELETE reaper. Stale comment at \`0007:191\` lists 'is\_deleted revival' as gated growth — incorrect under physical accounting (revival is UPDATE, d\_rows=0, guard returns early). Doc drift: \`owner\_user\_id\` still referenced in \`supabase/README.md:9,43\` and \`packages/core/src/sync-data.ts:53\`.

<!-- lore:019ebc36-830a-7307-8a10-9ff12dfa5740 -->
* **openai-responses.ts: Codex delta must live in a dedicated wrapper, not inline conditionals**: openai-responses.ts: Codex delta must live in a dedicated wrapper, not inline conditionals. Use \`buildOpenAICodexUpstreamRequest\` and \`applyCodexResponsesDelta\` helper. Codex divergences: URL \`/codex/responses\`, \`store:false\`, control fields passthrough, \`max\_output\_tokens\` suppression. \`applyCodexResponsesDelta\` mutates body post-build. Guard \`if (!extras) return\` runs AFTER setting \`store=false\` — so \`store:false\` always applies even when \`req.extras\` is absent. TRAP: no test that control fields (\`include\`, \`prompt\_cache\_key\`, etc.) are still emitted after refactor — regression could silently drop them. Always add regression tests for Codex builder bugs: (1) suppressed field absent even when client sends it, (2) non-codex path still emits the field, (3) worker-side asserts field absent.

<!-- lore:019edc05-19bf-74c1-973c-0e0fff69ea36 -->
* **packages/core SQLite driver: #db/driver subpath import, never import node:sqlite directly**: All SQLite access in packages/core MUST go through \`#db/driver\` subpath import (package.json imports map: \`bun\` → \`driver.bun.ts\`, \`default\` → \`driver.node.ts\`). NEVER import \`node:sqlite\` directly outside \`driver.node.ts\` — it breaks the test runner which runs against src. \`Database extends DatabaseSync\` with \`query(sql)\` caching prepared statements in a \`WeakMap\`. \`traced.ts\` wraps only \`get\`/\`run\`/\`all\` via Proxy — NEVER alters arguments or return values; \`.exec()\` passes through untouched (traced.ts:18–21, :66–77). \`migrate()\` runs on the RAW connection before wrapping (no re-entrancy, no tracing). Statements via \`db().prepare(...)\` (used in distillation.ts/embedding) are NOT traced — observability gap.

<!-- lore:019edc02-dfcf-7bda-8f23-b92426e422ef -->
* **pipeline.ts: cacheOptions construction and protocol-specific TTL downgrade**: \`cacheOptions\` (pipeline.ts:6477-6484): \`{ systemTTL: "1h", stableLtmSystem, ltmSystem, cacheTools: true, cacheConversation: true, conversationTTL: resolvedConversationTTL }\`. Protocol dispatch (lines 2891-2944): OpenAI/OpenAI-Responses flatten all LTM into single system string (no blocks). Anthropic non-native upstreams (not \`api.anthropic.com\`) have \`systemTTL\`/\`conversationTTL\` downgraded to \`"5m"\` (1h TTL is Anthropic beta extension). Billing re-sign gate (lines 2960-2983): \`effectiveProtocol === "anthropic" && hasBillingHeader(req.system)\` — NOT the \`isCC\` OR form, because re-signing requires the header to actually be in \`system\[0]\`. Conversation TTL auto-mode (lines 6430-6474): upgrades to 1h immediately when \`coldFraction > 0.4\`; downgrades to 5m only after 3 consecutive qualifying turns (hysteresis prevents compounding bust).

<!-- lore:019ecdf2-310b-77fc-a51f-189ecb397a38 -->
* **pipeline.ts: safeDeltaInsertIndex and durable prompt delta system**: pipeline.ts durable prompt delta (content-anchored): \`appendKnowledgePromptDeltaAt\` persists delta with \`MessageInsertSelector.afterHash\` = fnv1a(\`${role}\x1f${blocksToText(content)}\`) of the message immediately before insert point. \`resolveDeltaInsertIndex\`: tail-first scan for \`afterHash\` match → \`safeDeltaInsertIndex(messages, i+1)\`. Legacy rows (no \`afterHash\`) fall back to clamped absolute \`insertAt\`. TRAP: tail-first scan rebinds to NEWEST hash match — if same content repeats (e.g. duplicate assistant \`"ok"\` or \`"DONE"\` messages, or \`\[tool results provided]\` placeholder injected by \`removeOrphanedToolResults\`), append-only turns shift delta to wrong index (invariant 2 violation). \`blocksToText\` for \`tool\_use\` uses only \`block.name\` (not id/input) → same-name tool calls are indistinguishable. Fix direction: prefer occurrence nearest creation-time \`insertAt\` (exact hit at persisted \`insertAt-1\` before falling back to scan). \`shouldResetDeltaOnCompression\`: true when curLayer>=1 && curLayer!==prevLayer. \`applySessionPromptDeltas\` re-runs \`safeDeltaInsertIndex\` at replay for steady-layer-1 drift.

<!-- lore:019ebc96-83c7-7cd5-98d2-ca85724c2a85 -->
* **pipeline.ts: two curation paths — in-flight vs idle — must be gated separately**: Pipeline two curation paths: in-flight (mid-turn, gated by sessionState.curationScheduled) and idle (flush gen-0 then metaDistill) — must be gated separately. In-flight avoids system\[2] cache busts. System\[2] churn root causes: context-health note appended after diff-pinning, LTM entry selection changes per-turn. In-flight gen-0 distillation mutates messages\[1] in compression mode. LTM pin invariants: NEVER bust cache; ltmCacheText must never diverge from ltmPinText; coalesced seq-0 delta row describes full cumulative frozen→current delta.

<!-- lore:019ee64d-c2fe-7cb1-a4ba-3ab13da17e85 -->
* **PR #844 (cache-economics-evaluator): shadow-mode only, 3 new SessionState fields, no behavior change**: PR #844 is sub-PR 2a of the cache-economics unification refactor. Shadow-mode ONLY — stores size snapshot on session state, computes strategy, logs what unified model would do vs legacy decision. Zero runtime behavior change. 3 new \`SessionState\` fields (in-memory only, NOT persisted by \`saveGradientState\`): \`cacheSizeFull: number\` (init 0), \`cacheSizeCompressed: number\` (init 0), \`cacheStrategy: { result: CacheEconomicsResult; decidedAt: number } | null\` (init null). Cleared by \`evictSession\`. \`cache-economics.ts\` is a pure leaf module (no imports) — no circular dependency risk with \`gradient.ts\`. Shadow eval block in \`shouldWarm\` is self-contained \`{}\` scope (lines 1248–1280). \`evaluateCacheStrategy\` uses non-creating \`sessionStates.get\` — cannot leak session-state entries.

<!-- lore:019ee535-02de-729a-a4f2-ecf36b96c70f -->
* **recall.ts: search architecture — RRF fusion, source weights, cross-session penalty**: recall.ts is the unified search entry point. \`CROSS\_SESSION\_RAW\_PENALTY=0.5\` applied to temporal/distillation from OTHER sessions only — knowledge/entities/lat-sections are intentionally cross-session and never penalized. \`searchRecall()\` short-circuits on \`EMPTY\_QUERY\`. Entity alias expansion always runs (fast, targeted); LLM expansion gated on \`queryTermCount <= expansionMaxTerms\` (default 8). Original query always first in RRF list. \`MAX\_RRF\_LISTS=14\` — expanded-query BM25 lists trimmed when exceeded; primary and supplemental lists always kept. \`formatFusedResults()\` backfills to 3 results only if items clear \`absoluteFloor\` — never force-inject weak content. 🔴 Invariant: never force-inject weak cross-session archives on off-topic queries.

<!-- lore:019ecb52-ff7c-7515-a435-be511f6fdde7 -->
* **session.ts: 3-tier session ID strategy, generateSessionID format, and marker functions**: Session ID strategy: Tier 1 priority: x-lore-session-id, x-claude-code-session-id, x-session-id, x-session-affinity. ROTATION\_ELIGIBLE\_HEADERS = ONLY x-session-affinity. x-claude-code-session-id is NOT rotation-eligible — new value = new session, never merge (cross-project contamination). isClaudeCodeClient checks x-claude-code-session-id header; callers should also check hasBillingHeader on system. isCC in pipeline.ts = isClaudeCodeClient OR hasBillingHeader. Tier 2: learned headers after 3 stable turns. Tier 3: SHA-256 of first user msg + auth suffix. generateSessionID: 12 bytes = 8 random + 4-byte BE unix ts, base62. Desktop uses same x-claude-code-session-id as CLI.

<!-- lore:019ee64d-c325-7eb1-885a-0f3b96bac299 -->
* **shouldWarm invariants: sub-agent exempt, maxCycles cap absolute, first probe always funded**: Three hard invariants in \`shouldWarm\` (cache-warmer.ts): 1. Sub-agent sessions always exempt — \`if (state.isSubagent) return false\` at line 1065. Too short-lived for warming to be profitable, even with \`/lore:warm:keep\` force. 2. Warming beyond \`maxCycles\` is always unprofitable — break-even cap enforced even in forced mode (lines 1098–1103). 3. First warmup of every break (\`cyclesSpent === 0\`) MUST always be funded — it is the irreducible probe that refreshes \`writeEfficiencySamples\`. Write-efficiency check gated on \`cyclesSpent >= 1\` (lines 1139–1142) to prevent blocking the probe on stale samples.

<!-- lore:019ee5b8-6fe3-77a7-84cc-ae658bd26268 -->
* **Stryker baseline: sync-data.ts mutation score 68.98% — 28 real logic gaps identified**: Stryker baseline (June 20 2026) for \`packages/core/src/sync-data.ts\`: \*\*68.98% mutation score\*\* is an UNDERESTIMATE due to confirmed false survivors. L624 \`if (remoteHash !== null && localHash === remoteHash)\` is a false survivor — forced mutation kills 4 tests, but all 4 Stryker runner modes (vitest-runner perTest, coverageAnalysis:all, coverageAnalysis:off, command runner) report it as survived. Root cause: Stryker vitest-runner@9.6.1 × Vitest 4 attribution bug (avg 2.3 tests/mutant, suspiciously low vs ~67 expected). L378 \`pruneOutbox\` guard is a confirmed \*\*equivalent mutant\*\* (\`DELETE WHERE seq<=0\` deletes nothing; seq starts at 1). Gap list from baseline contains false positives — do NOT use it to drive survivor-killing work until Stryker × Vitest 4 bug is resolved.

<!-- lore:019ee5b8-6fc6-7395-920e-3ed300eb5f31 -->
* **Stryker mutation testing: config, scope, and delivery vehicle**: Stryker mutation testing (#832): installed \`@stryker-mutator/core\` + \`@stryker-mutator/vitest-runner@9.6.1\` as root devDependencies. Config file: \`stryker.config.mjs\` (must be \`.mjs\` — project is \`type: module\` ESM). Scoped mutation test config: \`vitest.mutation.config.ts\`. Allowlisted files: \`sync-data.ts\`, \`sync/gateway-sync\`, \`gradient.ts\`, \`ltm.ts\`. No hard build gate — thresholds ratchet over time. Delivery vehicle: GitHub Actions nightly workflow (NOT per-PR, NOT local foreground run — Stryker takes 13+ minutes even scoped to one file). \`premutation\` script mirrors \`pretest\` gateway bundle step. Stryker parallel workers are safely isolated via \`packages/core/test/setup.ts\` (unique temp DB per process via \`LORE\_DB\_PATH\`).

<!-- lore:019ec7fc-3bab-7e84-b5b3-e8e9d802a806 -->
* **Supabase auth constraints: email templates blocked on free tier; no device flow**: Supabase auth constraints: Free tier blocks email template customization (HTTP 400); custom SMTP live: \`smtp\_host: mail.smtp2go.com\`, sender \`keeper@withlore.ai\`. Email templates pushed via Management API PATCH (4 fields only). \`supabase config push\` clobbers entire auth config — use Management API PATCH for targeted updates. GitHub OAuth must be enabled separately in Auth → Providers → GitHub. OTP length configurable (not fixed at 6 digits) — observed 8-digit tokens; use \`/^\d{4,12}$/\`. \`lore login --email\` active once custom SMTP configured. Supabase project ref: \`jlwxsrmvomocgngbxmcf\`, region \`us-east-2\`. \`config.toml\` auth settings only affect \`supabase start\` (local stack) — dashboard is authoritative for production. Email templates MUST include \`{{ .Token }}\` in body or users receive only a \`{{ .ConfirmationURL }}\` link. Redirect URLs: must include \`https://\` when not localhost; must include trailing \`/\`.

<!-- lore:019ecb58-7fc8-70ad-85eb-5c27233ec4e3 -->
* **sync engine: Supabase mirror schema (migration 0002) — 5 tables, RLS, triggers**: Sync engine Supabase mirror schema — 7 migrations: \`0001\_accounts.sql\` (profiles table, SECURITY DEFINER trigger handle\_new\_user). \`0002\_sync\_basic.sql\` (5 tables: knowledge, entities, entity\_aliases, entity\_relations, knowledge\_entity\_refs; composite PK (owner\_user\_id, id); RLS owner-scoped). \`0003\_sync\_limits.sql\` (plan\_limits, quota trigger v1). \`0004\_sync\_hardening.sql\` (tier lock via column-level GRANTs + guard\_profile\_tier trigger, size CHECKs, DML GRANTs). \`0005\_sync\_quota\_hardening.sql\` (quota trigger v3 — revival gate, advisory lock). \`0006\_tier\_upgrade\_path.sql\` (GRANT SELECT, UPDATE ON profiles TO service\_role). \`0007\_scope\_seam.sql\` (renames owner\_user\_id→scope\_id, adds author\_id, replaces COUNT(\*) quota with trigger-maintained \`user\_table\_usage(scope\_id, table\_name, row\_count, byte\_count)\` counter table). Free tier caps: knowledge=500, entities=30, entity\_aliases=300, entity\_relations=300, knowledge\_entity\_refs=2000. NULL cap = unlimited.

<!-- lore:019edff0-4719-79dc-82ff-37b649e49e5c -->
* **sync integration test harness: pg-harness.ts spins real Postgres+PostgREST via Docker; supabase-shim.sql replicates auth.uid()/roles**: sync integration test harness: pg-harness.ts spins real Postgres+PostgREST via Docker; supabase-shim.sql replicates auth.uid()/roles. \`runAs(role, claims, fn)\`: wraps in transaction with \`SET LOCAL ROLE\` + \`set\_config('request.jwt.claims', ...)\`. \`asService\<T>(fn)\` at line 65; \`createUser(email?)\` at line 68; \`userJwt(uid)\` at line 72. \`handle\_new\_user\` trigger inserts into \`profiles\` without specifying tier — DB default \`'free'\` applies. JWT\_SECRET: \`lore-integration-test-jwt-secret-min-32-bytes-long\`. Tests gated on \`LORE\_INTEGRATION=1\`. Cap mutations restored via \`afterEach\` admin connection. Key pattern: advance pull-only table cursors in \`beforeEach\` to prevent ambient profile pulls from inflating \`r.pulled\` counts in unrelated tests; profile-specific tests reset cursor to \`'0|'\` to opt in to fresh profile data. A1 migration (0007) adds \`scope\_id\`/\`author\_id\`/\`user\_table\_usage\`.

<!-- lore:019ee0df-cbae-76a7-8b33-41c2c11d5f6a -->
* **sync test coverage gap: point-wise tests miss path-wise bugs — prioritized fixes**: Sync test suite is structurally sound (3-tier: unit + faithful PostgREST mock + real Postgres/PostgREST integration) but systematically under-covers lifecycle sequences and invariants-over-paths. Both prune-floor (Bug 1) and tier-residue (Bug 2) bugs lived in state transitions, not single operations. Prioritized fixes: (1) Mutation testing (Stryker) on \`sync-data.ts\`/\`sync.ts\`/\`gradient.ts\`/\`ltm.ts\` to measure behavioral constraint; (2) Property-based tests (fast-check) for sync state machine with random op sequences asserting invariants; (3) \`assertSyncInvariants()\` helper in \`afterEach\` across all sync tests; (4) Lifecycle sequence tests (enable/disable/re-enable, login/logout/switch-account). Integration \`beforeEach\` parking profiles cursor far ahead optimizes for clean suite over realistic state — masks bugs.

<!-- lore:019edff0-4729-73b3-a15c-55619ac3a620 -->
* **sync-data.ts: SYNCED\_TABLES.basic registry — syncColumns allowlist, PAYLOAD\_EXCLUDE, HASH\_EXCLUDE, versioned flag**: \`SYNCED\_TABLES.basic\` (sync-data.ts): 6 tables (added \`profiles\` in v49). \`profiles\`: \`pullOnly: true\`, idColumns \`\['id']\`, no syncColumns pushed, no outbox capture trigger — server-authoritative. \`knowledge\_entity\_refs\`: \`versioned: false\`, idColumns \`\['knowledge\_id','entity\_id']\`, only 2 syncColumns — no \`content\_hash\`/\`revision\` remotely. \`PAYLOAD\_EXCLUDE\`: \`embedding\`, \`last\_accessed\_at\`, \`last\_reinforced\_at\` — never sent over wire. \`HASH\_EXCLUDE\`: same + \`updated\_at\`. \`ROW\_SEP = '\x1f'\` joins composite row ids. Sync-management cols (\`scope\_id\`, \`author\_id\`, \`content\_hash\`, \`revision\`, \`is\_deleted\`) added by \`pushEntry\` separately — NOT in syncColumns registry. \`currentTier()\` queries local profiles mirror, defaults to \`'free'\` when no row exists. \`pro: \[]\` and \`max: \[]\` tiers empty.

<!-- lore:019edfc9-c361-72f2-ada3-fc735def69f0 -->
* **sync.ts: local prune vs genuine delete — tombstone suppression required**: Hard invariant (🔴): only a genuine user/curator-intent delete generates a tombstone and propagates to remote. Local TTL/size eviction (idle prune) must NOT enqueue a tombstone. Fix: run prune under a capture-suppressed context (analogous to \`withSyncApplying\`) that drops the DELETE-trigger tombstone and clears \`sync\_state\` only. \`reconcile\` (\`sync-data.ts:378-396\`) must never see evicted rows as deletes. Trap: conflating eviction with delete looks correct because both remove local rows — but eviction drops LOCAL RESIDENCY only, not the logical row. The logical dataset (shared memory) converges across all clients + remote; 'in sync' means logical set converges, not byte-identical full replicas. Evicted rows are re-pullable cache misses, not divergence. Remote is bounded by per-scope quota (\`max\_rows\`/\`max\_bytes\`), not a TTL reaper. Neither side is authoritative.

<!-- lore:019edff0-4711-73d1-9b16-7fba4282b220 -->
* **sync.ts: per-table cursor isolation — quota/transient failure on one table never blocks others**: Hard design invariant: cursors are per-table (\`sync.push.\<table>\` and \`sync.pull.\<table>\`). Quota rejection (HTTP 400, errcode 23514 + /quota exceeded/i) pauses ONLY the affected table — never an error to crash on, never blocks other tables. Outbox SQL-filtered per table so other tables' entries cannot starve it. \`classifyPushError\`: 'quota' → pause table, keep row pending; 'poison' (23514 without quota msg) → drop past cursor, record conflict, keep table syncing; 'transient' → keep pending, retry. \`drainTimestamp\` pages by id keyset when >PAGE rows share one millisecond. \`applyRemote\` strips remote-only cols (\`owner\_user\_id\`, \`content\_hash\`, \`revision\`, \`is\_deleted\`) and converts ISO timestamps to epoch ms for local storage.

<!-- lore:019edfff-dc0b-7552-89ba-9a71ebfea21c -->
* **sync.ts: pullOnly tables — server-authoritative, no outbox, no conflict classification**: \`SyncTableMeta.pullOnly: true\` marks server-authoritative tables (e.g. \`profiles\`). \`pushOnce\` skips them entirely (sync.ts:78). \`applyRemote\` bypasses \`classifyRemoteRow\` for pull-only rows — forces \`cls = 'apply'\` — because: (1) no outbox trigger means no pending local changes ever, (2) no \`content\_hash\` column means null remoteHash always mismatches non-null local hash → spurious conflict every tier-flip re-pull. \`currentTier()\` in \`sync-data.ts\` reads the local \`profiles\` mirror, defaulting to \`'free'\` when no row exists — \*\*advisory UX hint only\*\*, not a security gate; server RLS/quota is the real enforcement boundary. The 'outbox capture installed for every synced table' test must skip \`pullOnly\` tables. 🔴 \`seedOutbox\`/\`reconcile\` must also skip \`pullOnly\` tables — see prune-floor DoS gotcha.

<!-- lore:019edff0-4709-74cc-98eb-ad3bd46163c1 -->
* **sync.ts: push payload never includes owner\_user\_id — server default auth.uid() fills it; A1 changes onConflict target**: \`pushEntry()\` (sync.ts): payload = \`toRemoteRow(pickSyncColumns(table, row))\` + \`is\_deleted: false\` + \`content\_hash\`/\`revision\` (versioned tables only). \`scope\_id\` (was \`owner\_user\_id\`) NEVER included — server fills via \`default auth.uid()\`. \`onConflict\` target post-A1: \`\["scope\_id", ...idColumns(table)].join(",")\`. \`REMOTE\_ONLY\_COLS\` Set strips \`scope\_id\`, \`author\_id\`, \`content\_hash\`, \`revision\`, \`is\_deleted\` before local apply. Soft-delete push: \`.update({ is\_deleted: true }).match(decomposeId(table, rowId))\` — \`decomposeId\` keys only on \`idColumns\`; RLS scopes match automatically. \`pickSyncColumns\` strips local-only cols (e.g. \`knowledge.promoted\_at\`) to avoid PGRST204. Tombstone creation via INSERT is never a legitimate operation — only soft-delete UPDATE.

<!-- lore:019edfc9-c36b-7cd9-954e-44f27d8b356d -->
* **sync.ts: restore exemption — prune must key off local residency, not original created\_at**: Hard requirement (🔴): local prune must key off \*local residency timestamp\* (when the row was pulled/created locally), NOT the original \`created\_at\`. If prune uses \`created\_at\`, restored sessions older than local TTL (120 days) are deleted on the next idle tick — making cross-device backup useless for data older than local retention. Fresh device pulls full remote history (that is the restore path); pruning device is already past evicted rows' timestamps so evicted rows are never re-pulled by the same device (no ping-pong). Remote Supabase is a multi-master sync backend; many clients may read/write it; neither side is authoritative.

<!-- lore:019edfd3-8f0f-77f7-8e9d-54f7be68bdbb -->
* **sync.ts: tombstones required only on mutable tables — append-only tables are tombstone-free**: Hard invariant: tombstones CANNOT be eliminated for mutable tables (knowledge, entities). Deletion is positive information; absence is indistinguishable from eviction or never-pulled — fundamental distributed-systems result: any eventually-consistent multi-master set supporting removal needs a tombstone or moral equivalent (CRDT remove-tag/version-vector). Tombstone-free path exists ONLY for append-only tables (\`temporal\_messages\`, \`distillations\`) — no genuine deletes, so cursor/version is fully sufficient. Tombstones on mutable tables are GC-able once every known device's pull cursor has passed them (per-scope low-water mark tracking). Trap: eliminating tombstones looks safe because 'missing = deleted' seems equivalent — but it conflates eviction (local cache miss, re-pullable) with genuine deletion (logical row gone).

<!-- lore:019edfe6-3301-76b0-aea9-cba6052eeb6c -->
* **Upcoming: uniform append-only model for knowledge/entities — nothing should land on these tables before refactor**: 🔴 Major upcoming refactor: knowledge and entities tables move to immutable append-only model: \`logical\_id + version + is\_deleted\` marker, materialized current view, compaction. Engine reduced to append+cursor; usage counter maintained. Also adds \`author\_id\`/\`scope\_id\` seam on shareable tables (Folk Lore / team sync). Consequence: DO NOT land new migrations or features on \`knowledge\`/\`entities\` that the refactor would immediately rip out. Issue #495 (knowledge evolution trajectory / op-log) is subsumed — \`logical\_id+version\` history IS the evolution trajectory; building a standalone op-log on the current mutable table is throwaway work. Issue #467 RFC defines topology: Personal DB + Team DB, same schema (team columns nullable in personal), \`.lore.md\` = team entries, \`.lore.local.md\` = personal entries.

<!-- lore:019ee645-ffac-7236-ab99-a9a8d6160483 -->
* **Worker health critical errors: sustained failure on POST /v1/responses — active Sentry issue LOREAI-GATEWAY-2C**: Active Sentry issue LOREAI-GATEWAY-2C: 'Worker health critical: sustained worker failure' on POST /v1/responses. 304 events, 4 users, first seen 2026-06-10, still firing hourly as of 2026-06-20. All recent events from Seylan Çinar Kaya's MacBook (Seylans-MacBook-Pro.local), release 0.27.1-dev.1781176263, port 3207. Companion issue LOREAI-GATEWAY-2B ('Worker health degraded') fires on same host minutes before critical. Onur Temizkan's gateway issues (onur-ThinkPad-E470, Tailscale IP 100.107.38.38) are a separate cluster — LOREAI-GATEWAY-2J (release 0.31.0-dev) and LOREAI-GATEWAY-2P (TypeError: fetch failed on POST /v1/messages).

### Decision

<!-- lore:019ee5f0-55f7-7c8e-a63a-90d965e92f22 -->
* **D6′ design: COOL+BUST as opportunistic trigger for deferred prefix work, not full coupling**: Deferred prefix work (meta-distill, post-idle compaction, prompt-delta reanchor) keeps its own independent schedule (existing idle/gen-0-count/urgency thresholds). COOL+BUST becomes an additional opportunistic 'flush now' trigger — free because prefix is busting anyway. HOLD-WARM adds 'prefer to defer' bias for non-urgent rewrites. Urgency thresholds (temporal backlog, gen-0 over cap, forced flags) always override — sessions staying warm forever never starve distillation/LTM. Rejected: full coupling (D6 original) where prefix work schedule was driven entirely by cache state — too aggressive, starves distillation on warm sessions. For \`fullBodyTokens\` sourcing in \`decideCacheStrategy\`: Option D (gradient's own \`layer0Input\`/\`compressedEstimate\` snapshot on session state) preferred over Option A (last real API token count) — both sides read identical values so estimation error cancels; Option A is ground truth but unavailable on warmer side without extra plumbing.

<!-- lore:019edfc9-c374-7336-87d5-c8dbfad99bdb -->
* **Encryption: client-side for conversation blobs; team mode uses group key (not Signal Double Ratchet)**: Chosen approach: client-side encrypt conversation blobs. In team mode, distillations AND associated temporal messages must be decryptable by selected team members — ALL synced tables (knowledge, entities, distillations, temporal) get \`author\_id\` + \`scope\_id\`. RLS gates who can fetch the row; encryption gates who can read its contents. Sensitive tables get a \`shareability\`/\`encrypted\` flag. T2 'personal-only/no scope\_id' premise is WRONG — structural exclusion is not the access control; encryption is. Signal Double Ratchet / Sender Keys rejected — designed for real-time chat forward secrecy, not async document sharing. Group key approach preferred for team mode. Key management: dual model — server-side escrow (weaker security, less data loss risk) AND fully client-side (stronger security, loss risk), per Bitwarden/BitLocker/Firefox Sync pattern. QR code + temporary short code for new device onboarding / short-term sharing. Migration \`0007\` adds \`author\_id\` + \`scope\_id\`.

### Gotcha

<!-- lore:019ee004-036c-73ab-8b87-6938d079ca7e -->
* **A1 quota counter: revival bypass test must size rows so two fit under cap before revival triggers overflow**: Trap: writing a revival-bypass test with two ~1500-byte rows under a 2000-byte cap looks correct because the revival is the intended overflow trigger. Fix: the second INSERT is correctly rejected BEFORE revival logic is reached — the cap is already exceeded. Size rows so two fit (e.g. ~700 bytes each ≈ 1400 bytes < 2000) and the revival of a third is the overflow event. Verified: 23/23 integration tests pass after resize. Rule: always verify the pre-revival state is under cap before asserting revival is blocked.

<!-- lore:019ee5e4-69e2-7194-b76f-38036858bb1c -->
* **A2 knowledge schema: logical\_id column is permanently nullable — silent NULL on omitted inserts**: Trap: \`logical\_id\` was added as \`TEXT\` with NO DEFAULT and NO NOT NULL constraint (v50 migration). Backfill \`UPDATE knowledge SET logical\_id = id WHERE logical\_id IS NULL\` runs in the same transaction (safe for existing rows). Looks safe because migration is transactional. Fix: all future INSERT sites must explicitly set \`logical\_id\`. All existing test inserts omit it (integrity.test.ts, embedding.test.ts, sync-data.test.ts, etc.) — those rows have NULL \`logical\_id\`. Any query filtering on \`logical\_id\` silently misses test rows. Additionally, \`sync-data.ts:570\` \`applyRemoteUpsert\` and \`syncColumns\` (\`sync-data.ts:102-118\`) omit v50 columns → pulled rows get \`logical\_id=NULL\`. Backfill on sync-apply and consider \`NOT NULL\`/\`CHECK\` in sub-PR 2/3. Not a blocker while \`appendVersion()\` has no production callers, but will cause silent data holes once versioning queries go live.

<!-- lore:019ee5e4-69d5-7af8-b4e6-5671cabaef29 -->
* **A2 versioning: 3 full-mirror FTS assumptions detonate once appendVersion() goes live**: Three code sites assume \`COUNT(knowledge) == COUNT(knowledge\_fts)\` — safe in PR #839 (appendVersion has zero production callers), but will detonate in sub-PR 2: 1. \*\*\`data.ts:937\` \`validateDatabaseIntegrity()\`\*\*: \`ok = integrity==='ok' && COUNT(knowledge)===COUNT(knowledge\_fts)\` — fires false integrity alarms; consumed at \`gateway/src/cli/data.ts:2597\`, does \`process.exit(1)\` on mismatch. 2. \*\*\`sync-data.ts:591\` + \`sync.ts:323\` \`rebuildFts()\`\*\*: FTS5 \`'rebuild'\` re-indexes ENTIRE content table without \`is\_current\` filter — re-indexes superseded/deleted rows, destroying partial-mirror invariant. 3. \*\*FTS5 \`rank=1\` external-content integrity check\*\*: returns \`'database disk image is malformed'\` (false positive) after any \`appendVersion\` call — plain \`integrity()\` and \`PRAGMA integrity\_check\` both return \`'ok'\`; search works correctly. All 3 must be fixed before \`appendVersion()\` gets a production caller.

<!-- lore:019ee5e9-eaca-7703-a2e2-4712e3b5368b -->
* **appendVersion() 30-column copy-forward INSERT…SELECT silently drops future columns**: Trap: \`appendVersion()\` INSERT…SELECT in \`ltm.ts:235-260\` explicitly lists all 30 columns. Future \`ALTER TABLE ADD COLUMN\` will silently omit new columns from copied versions — the new column gets its DEFAULT value instead of the source row's value. Looks safe because the explicit list was verified complete at PR #839 merge. Fix: add a comment warning, and in sub-PR 2 consider a dynamic column-list approach or a test that asserts the copy-forward list matches \`PRAGMA table\_info(knowledge)\`.

<!-- lore:019ee5e9-eac3-731d-8bc5-866fb1e661eb -->
* **appendVersion() insert+demote is non-atomic — crash leaves two is\_current=1 rows**: Trap: \`appendVersion()\` in \`ltm.ts:235-266\` performs two separate autocommit statements (INSERT new version + UPDATE old to \`is\_current=0\`). A crash between them leaves two \`is\_current=1\` rows for the same \`logical\_id\`. Looks safe because both ops are in the same function. No \`UNIQUE(logical\_id)\` partial index on \`is\_current=1\` nor \`UNIQUE(logical\_id, version)\` to catch this. Fix deferred to sub-PR 2 when wiring \`update()\`/\`remove()\` — wrap both statements in an explicit transaction. Dormant in PR #839 since \`appendVersion()\` has zero production callers.

<!-- lore:019eccdc-2a14-7401-9661-20cd5c574a47 -->
* **buildKnowledgeDeltaMessage: skipped-overflow slice is NOT order-stable — pre-existing cache bust risk**: buildKnowledgeDeltaMessage: \`skipped\` overflow slice is NOT order-stable. \`formatKnowledge\` sorts \`rendered\` body alphabetically (byte-stable), but the \`skipped\` overflow array (entries >400-token \`KNOWLEDGE\_DELTA\_TOKEN\_BUDGET\`) preserves \`forSession\` ranking order, driven by per-turn \`contextHint\`. Re-ranking same set between turns → \`skipped\` membership/order changes → different bytes in 'Additional Changed Knowledge (truncated)' \`.slice(0,3)\` → cache bust with no real content change. Trigger: curator burst updating many pinned entries simultaneously. Fix: sort \`skipped\` deterministically (e.g. by entry id) before slicing. Non-blocking but amplified by frozen-baseline fix (material-delta path now fires every turn while diverged).

<!-- lore:019ee5f0-5603-7df1-9bcd-57e7571b1b63 -->
* **cache-warmer bodyBytes metric: use Buffer.byteLength not .length for UTF-8 wire size**: Trap: \`signedBody.length\` returns UTF-16 code units (JS string length), not UTF-8 byte count. Looks correct because ASCII content gives identical results. Fix (PR #841, merged 2a63ad6d): use \`Buffer.byteLength(signedBody, 'utf8')\` — idiomatic Node.js form, not \`Buffer.from(signedBody, 'utf8').length\`. Matters for prose/code-heavy sessions with non-ASCII content; misleads cache divergence analysis when multi-byte Unicode is present. Flagged by Seer on PR #838 at \`packages/gateway/src/cache-warmer.ts:1855\`.

<!-- lore:019ee4dc-1197-7aa9-ad55-dc8ab8a5617a -->
* **cache-warmer circuit breaker: 'does not count failed requests' test is tautological — ok===false guard is untested**: Trap: the test 'does not count failed requests' uses \`ok:false, cacheCreationTokens:0\` — the \`> 0\` token guard short-circuits before the \`result.ok === false\` guard is reached, so the test passes regardless of whether the \`ok\` guard exists. Looks like a real coverage test because it asserts the breaker doesn't trip on failure. Root cause: \`executeWarmup\` always sets \`cacheCreationTokens=0\` on HTTP errors (line 1734), making \`ok:false\` with nonzero creation tokens structurally impossible in production. The \`result.ok\` guard is therefore redundant but harmless. A proper test would need \`ok:false\` + positive creation tokens + \`cacheLikelyAlive:true\` to isolate the guard — but this state is unreachable in production.

<!-- lore:019ee201-4386-7eb2-a958-c4150fec4705 -->
* **cache-warmer circuit breaker: global early-return in idle.ts blocks ALL sessions when any bucket trips**: Trap: \`idle.ts\` had \`if (isCircuitBreakerTripped()) return\` BEFORE the per-session loop — one tripped bucket silently disabled warming for every session. Looks correct because 'circuit breaker tripped → stop warming' is the right intent. Fix (per-bucket refactor, PR #836 Jun 20 2026): removed global early-return; replaced with per-bucket \`continue\` inside the loop after profile resolves (\`if (isCircuitBreakerTripped(warmupBucketKey(state), now)) continue\`). Each bucket \`(sessionID, model, upstreamURL)\` is now independent. \`isCircuitBreakerTripped\` signature changed to 2-arg \`(bucketKey, now)\` — all callers updated. \`pruneExpiredCircuitBreakers()\` called once per tick before the loop to GC dead-session stale buckets. CI \`test\` job was FAILING after PR #836 (run 27870535036) — one \`edit\` tool failure (\`could-not-find-oldstring\`) may have left a test file inconsistency; actual assertion not yet identified.

<!-- lore:019ee018-a837-7598-ad39-02a95e20d9ea -->
* **cache-warmer circuit breaker: prefix mismatch (not cache expiry) causes UNCACHED failures that trip breaker**: Trap: UNCACHED warmup failures (\`cacheRead=0, cacheWrite>0\`) look like cache TTL expiry — but the real cause is prefix mismatch (warmup body diverges from what Anthropic has cached, e.g. due to compaction, layer changes, or LTM churn). Three such failures in 27 minutes (Jun 16 2026) tripped the global breaker. Session \`0rG8v7uBA0BOoV8y\` grew 398K→584K tokens yet had cacheRead=0 on both warmups — concurrent healthy session was getting 100% refreshes simultaneously, ruling out Anthropic-side expiry. Fix direction: detect prefix mismatch vs. expiry before incrementing failure counter; don't penalize warmups where the session itself changed substantially between warmup and return.

<!-- lore:019ebc55-ba2e-7271-9906-905e4f5ed9bf -->
* **CCH seed extraction: seed may persist in binary but algorithm/preimage changed across versions**: CCH seed extraction: consult \`quality/CCH.md\`. Seed \`0x4d659218e32a3268\` (SEED\_2\_1\_138) unchanged in 2.1.175. Hash-input ≠ wire body — cch fn scans for \`cch=\` placeholder, splits/transforms before hashing. Binary is EXEC (not PIE), fixed base \`0x200000\`. CCH\_PLACEHOLDER = \`cch=00000\`; signBody replaces with hash; regex \`/cch=(\[0-9a-fA-F]{5});/\` (trailing semicolon required). Work files in stable dir (e.g. \`~/Code/cch-investigation/\`), not \`/tmp\`.

<!-- lore:019eb4d0-6fd9-7ef5-b22f-0f156eeca4b0 -->
* **cli/main.ts OPTIONS map: undeclared flags silently become booleans under strict:false**: cli/main.ts OPTIONS map: undeclared flags silently become booleans under \`strict:false\`. \`parseArgs\` with \`strict: false\` treats any undeclared \`--flag value\` as \`{flag: true}\` with value falling into positionals — \`--flag=value\` (equals syntax) still works so it looks correct. Every flag used in ANY subcommand MUST be declared in the global \`OPTIONS\` map in \`packages/gateway/src/cli/main.ts\` with correct type. Discovered in PR #696: \`--to\`, \`--project\`, \`--dry-run\`, \`--no-children\`, \`--min-confidence\` were missing.

<!-- lore:019ecab6-f928-7927-a39e-d601fb850af6 -->
* **computeMaxTokens: EMA-based floor ignores thinking budget, causing silent agent loop exit**: \`computeMaxTokens\` EMA floor ignores thinking budget → silent agent loop exit. \`computeMaxTokens\` (pipeline.ts) floors \`max\_tokens\` at \`max(8192, 3×outputEMA)\`. Anthropic counts \`max\_tokens\` as combined thinking+visible budget. Model consumes entire 8192 on reasoning → \`finish:"length"\` → no text/tool part → agent loop exits silently. Fix: pass \`thinkingBudget\`; set \`baseFloor = thinkingBudget + THINKING\_OUTPUT\_HEADROOM (8192)\`, clamped by \`modelOutput\`. Gate: \`if (!isCC)\` — CC clients excluded; opencode does NOT send \`x-claude-code-session-id\` so fix applies. EMA not corrupted by thinking truncation (Anthropic includes thinking tokens in output\_tokens).

<!-- lore:019ee021-c2da-7f87-b0a2-6f8a8a6c1da5 -->
* **currentTier(): LIMIT 1 with no WHERE returns wrong-account tier after account switch**: Trap: \`currentTier()\` (sync-data.ts:214) uses \`LIMIT 1\` with no \`WHERE\` and no \`ORDER BY\`. Local \`profiles\` mirror has no single-row constraint; \`applyRemoteUpsert\` deduplicates only by \`id\` (\`ON CONFLICT(id)\`). On account switch (logout → login as B), pull inserts \`{id=B}\` alongside stale \`{id=A}\`. SQLite returns lowest rowid → previous account A's tier. Free user can read prior account's \`pro\` tier or vice-versa. Fix: add \`WHERE id = (SELECT value FROM kv WHERE key='auth.user\_id')\` or equivalent user-scoped filter. Also: \`commandLogout\` must clear \`profiles\` mirror, \`sync.pull.profiles\` cursor, and \`sync\_state\` on logout — currently only \`clearSession()\` is called.

<!-- lore:019edb00-69d6-7131-bac9-75cfdda4ecf3 -->
* **distillation.ts action-tag extraction produces garbage LTM entries from regex-matched content (e.g. 'a-z')**: Trap: \`distillation.ts:1181-1231\` extracts behavioral tags via \`extractActionTags(result.observations)\` and creates preference entries when a tag appears in ≥3 sessions. Regex can match spurious patterns like \`a-z\` from content (character ranges in code/prose), producing garbage entries titled 'A Z'. These pollute system\[1] and cause cache busts when later deleted by consolidation. Pre-check at lines 1204-1211 prevents duplicate creates but not initial creation of garbage. The cross-session behavioral patterns \`\[a-z]\`, \`\[a-z-]\`, \`\[tool-failure]\`, \`\[enforced-workflow]\`, \`\[requested-review]\`, \`\[rejected-approach]\`, \`\[requested-tests]\` are artifacts of this bug — regex-matched content, not real user behaviors.

<!-- lore:019ee562-c70c-7fa5-b9f3-7f60a3c4b578 -->
* **getCircuitBreakerStatus: status getter must be pure read — no side-effect delete on decay**: Trap: original \`getCircuitBreakerStatus\` called \`isCircuitBreakerTripped\` which deletes the entry on decay as a side effect, then re-read the same entry — just-decayed bucket returned \`trippedAt:0\`, indistinguishable from never-tripped. Also mutated the in-memory map and wrote to SQLite on the dashboard read path. Looks correct because 'decayed bucket IS recovered' is semantically true. Fix (PR fix/circuit-breaker-status-pure-read, Jun 20 2026): \`getCircuitBreakerStatus\` rewritten as pure read — computes decay inline without calling \`isCircuitBreakerTripped\`, reports decayed bucket as \`{tripped:false, failures:0, trippedAt:0}\`, no map mutation, no SQLite write. Pruning left exclusively to \`isCircuitBreakerTripped\` on decision path and idle sweep. Rule: status/summary getters must never have delete/write side effects.

<!-- lore:019ee64d-c31c-72ef-b4ff-e9ac9efd407a -->
* **gradient.ts snapshot bias: sessions skipping tier gate keep compressedSize === fullSize**: Trap: sessions that skip the tier gate (sticky guards, prefix-present floor, or other conditions) never reach the compressed-estimate refinement at lines 2550–2554, leaving \`cacheSizeCompressed === cacheSizeFull\`. Looks correct because the snapshot write at 2468–2471 initializes both to the same value. Fix: this is a known shadow-mode bias — the unified model underrepresents compression benefit for sessions that would have been compressed but were held at a higher layer. Acceptable for shadow logging; must be addressed before the unified model drives real decisions. Layer-0 passthrough sessions correctly leave both equal (no compaction available on that path).

<!-- lore:019edfbc-ede7-7351-a97a-ac6f6c0839ea -->
* **idle.ts global sweep: use loreConfig() not GatewayConfig for knowledge.enabled gate**: Trap: \`GatewayConfig\` param passed to the idle scheduler does NOT expose \`knowledge.enabled\` — it looks like the right place because it's the config object in scope. Fix: use \`loreConfig()\` (imported from core) to check \`knowledge.enabled\` before running \`pruneDeadEntriesAllProjects()\`. Same pattern used elsewhere in idle.ts for knowledge-gated operations. Note: if \`knowledge.enabled\` is false, \`lastGlobalDeadSweepAt\` is NOT updated — sweep runs on next tick after re-enable (intentional: catches entries that accumulated while disabled).

<!-- lore:019ee013-8bb2-734b-9994-9f1922bcf654 -->
* **maintain\_usage re-parent bug: scope\_id mutation drifts counter silently**: ✅ RESOLVED (migration 0007): \`authenticated\` re-parent blocked \`23514\` by scope-immutability guard at \`0007:214-217\`; \`service\_role\` re-parent also blocked \`23514\` (guard fires even bypassing RLS). \`maintain\_usage\` always writes to \`new/old.scope\_id = auth.uid()\` — users can only move their own counter, griefing other tenants' counters is impossible. SHOULD-FIX remaining: \`exists\_pk\` moved outside advisory lock in 0007 (before lock at line 267) — weakens TOCTOU hardening; fix by restoring 0005 ordering (lock before \`exists\_pk\`).

<!-- lore:019edb00-69ce-7fdb-873a-b0e7024a0323 -->
* **Mid-session LTM consolidation deletes entries, busting frozen system\[1] cache on next turn**: Mid-session LTM consolidation deletes entries, busting frozen system\[1] cache on next turn. Root cause: consolidation deletes shift byte offsets, busting downstream cache. Fix (PR #808, v45): \`stableLtmCache\` freezes system\[1] for the session's entire lifetime — NEVER recomputed from live knowledge table mid-session. The frozen pin is set once and persisted; consolidation deletes no longer affect the active session's system\[1] bytes. Newly-curated preferences are picked up only on the NEXT session start. PR #814 adds consolidation retry-storm fix: cooldown armed only on successful no-op (not on throw), in-flight guard set before await and cleared in \`finally\`.

<!-- lore:019ee0f3-8292-73b9-a58d-8f6923c8a236 -->
* **migration 0007: exists\_pk outside advisory lock weakens TOCTOU hardening from 0005**: Trap: \`exists\_pk\` check (0007 lines 226-239) was moved BEFORE the advisory lock (line 267), reversing 0005's TOCTOU hardening. Looks safe because spurious reject self-heals on retry — but the ±1 overshoot race (two concurrent INSERTs both see exists\_pk=false, both proceed past guard) is dangerous. Currently unreachable only because A2 hard-DELETE reaper doesn't exist yet. Fix: restore 0005 ordering — take \`pg\_advisory\_xact\_lock(hashtextextended(new.scope\_id::text || '|' || tg\_table\_name, 0))\` immediately after scope-immutability check, before \`exists\_pk\`.

<!-- lore:019ebafd-32b7-7468-bdb2-5c77ff6c4ccf -->
* **opencode-lore plugin: legacy export path causes getLegacyPlugins() to be invoked**: opencode-lore plugin: never export named functions alongside default — legacy loader invokes them as plugins. \`getLegacyPlugins()\` iterates \`Object.values(mod)\` and treats any function as a server plugin. So \`export default LorePlugin\` with separately-exported helpers (\`applyLoreProviderConfig\`, \`probeGateway\`) causes them to be invoked as plugins; returning undefined pushes them into \`hooks\[]\` → all dispatch loops crash. Fix: \`default export { server: LorePlugin }\` so \`readV1Plugin\` takes the v1 path. NEVER export helper functions from the plugin's top-level index — they leak into the legacy loader.

<!-- lore:019ece09-5df6-7392-b67c-043a729239a1 -->
* **safeDeltaInsertIndex: steady-layer-1 drift NOT fixed by delete/re-anchor predicate**: Trap: \`shouldResetDeltaOnCompression(prevLayer, curLayer)\` returns false when layer stays at 1 (curLayer===prevLayer), so the delete/re-anchor branch never fires for steady-layer-1 raw-window slides — the production bug case. It looks like the fix because it handles layer escalation. The docstring claimed 'steady-state-at-layer-1 raw-window slide is handled by the caller's additional raw-window-repin check' — but that check was NEVER implemented at the call site (pipeline.ts:6078). Fix: extended \`shouldResetDeltaOnCompression\` with third param \`outOfIdle: boolean\` (default false); returns \`true\` when \`outOfIdle===true\` regardless of layer change. Call site passes \`sessionState.lastTurnWasIdle ?? false\`. Replay-time re-validation (\`applySessionPromptDeltas\`) handles non-idle steady-layer-1 drift. One-shot post-idle re-anchor alone doesn't fix steady-layer-1 drift — window start marches forward every turn.

<!-- lore:019ee635-7679-7fb3-baeb-6b350151d174 -->
* **Stray backtick in migration template literal: caught at parse/compile time, not by SQL tests**: Trap: a stray backtick inside a SQL string in a migration template literal (db.ts) closes the JS template literal early, making \`data\` parse as JavaScript — the file fails to parse entirely. Looks like it could slip through because it's inside a SQL comment. Fix: it can't slip through — any one of three local gates catches it before commit: (1) vitest/esbuild transform throws \`Expected "]"\`; (2) \`tsc --noEmit\` exits 2; (3) Biome lint exits 1. Additionally, every core test opens a fresh DB via \`test/setup.ts\` which runs \`migrate()\` and \`database.exec()\`s every migration string — malformed SQL throws at exec, failing the whole suite.

<!-- lore:019ee5de-eb53-70a1-91b5-df122b02fd4b -->
* **Stryker vitest-runner@9.6.1 × Vitest 4: false survivors due to test attribution bug**: Trap: Stryker reports a mutant as 'survived' even when existing tests demonstrably kill it (forced manual mutation kills 4 tests). Looks correct because Stryker ran the tests and reported results. Root cause: vitest-runner@9.6.1 × Vitest 4 attribution bug — avg ~2.3 tests/mutant selected (should be ~67), meaning most tests are never run against each mutant. Confirmed across all 4 modes: perTest, coverageAnalysis:all, coverageAnalysis:off, command runner. Fix: do not trust survivor lists from this combination; file tracking issue; treat baseline scores as underestimates. Equivalent mutants (e.g. \`DELETE WHERE seq<=0\`) are a separate class — no test can kill them by design.

<!-- lore:019ee5b8-6fd7-7915-ae0b-642db286f8d4 -->
* **Stryker/Vitest: never use & background jobs in shell — orphaned processes wedge the shell**: Trap: launching Stryker with \`setsid ... &\` looks like a way to run it in the background and check progress. Fix: the shell tool waits on background jobs anyway, and orphaned Stryker master + Vitest fork children peg CPU and block subsequent \`pkill\` calls (which also time out). Use foreground execution with a \`timeout\` safety wrapper instead — prior successful 127s full-suite run used foreground with 320s tool timeout. Rule: no \`&\` background jobs in shell tool for long-running processes.

<!-- lore:019edff0-4721-7514-9ddf-f9eac54de7da -->
* **sync integration tests: hand-written mocks cannot exercise RLS/triggers/quotas — integration tests are the only valid gate**: Hand-written mocks cannot exercise RLS/triggers/quotas — integration tests are the only valid gate. \`sync-security.integration.test.ts\` runs against real Postgres+PostgREST via Docker; gated on \`LORE\_INTEGRATION=1\`. Test suites: (1) multi-tenant RLS isolation — cross-tenant read, scope\_id forge (42501), re-parent steal/donate, anon access; (2) tier server-only — column grant blocks tier UPDATE, service\_role can upgrade; (3) quota enforcement — row cap, byte cap, revival bypass, tombstone flood. \`FREE\_LIMITS\` constant mirrors 0003 caps. \`afterEach\` restores \`max\_rows\` but NOT \`max\_bytes\` — \`setByteCap()\` calls at lines 330, 346 leak into subsequent tests (benign due to test ordering; SHOULD-FIX). \`dockerReady()\` is synchronous. \`beforeAll\` timeout: 180,000ms. 30 concurrent INSERTs at \`max\_rows=10\`: exactly 10 committed, advisory lock serializes growth correctly.

<!-- lore:019ee014-04f3-7790-a7cd-931c174831fa -->
* **sync quota: tombstone-flood bypasses both row cap and byte budget**: ✅ RESOLVED (migration 0007 physical accounting): tombstone INSERTs now counted toward byte budget and gated. \`enforce\_row\_quota\` INSERT branch counts tombstone bytes; 50×8KB tombstones blocked at row 3 on byte cap (\`23514\`); \`counterRows == physicalRows\`. Soft-delete bytes never restored under physical accounting. Remaining LOW accepted tradeoff: user creating+deleting ~500 entries exhausts free row cap with zero live rows — self-inflicted, bounded by own cap, no cross-tenant harm. Documented at \`0007:31-32\`; depends on A2 hard-DELETE reaper for reclaim path.

<!-- lore:019ee014-0500-7aae-96f8-40ea917bc359 -->
* **sync security: author\_id forgery not blocked by RLS — attribution spoof possible**: ✅ RESOLVED (migration 0007): RLS \`WITH CHECK (scope\_id = auth.uid() AND author\_id = auth.uid())\` at \`0007:74\` blocks both INSERT and UPDATE with forged \`author\_id\` — rejected \`42501\`. Previously only \`scope\_id\` was constrained. All 5 synced table RLS policies updated. Empirically verified: forged author\_id on INSERT and UPDATE both rejected.

<!-- lore:019ee0df-cb91-7385-87e6-b3bcf6898a44 -->
* **sync tests: setup ordering masks pullOnly/lifecycle bugs — vacuous pass anti-pattern**: Trap: sync tests call \`enableSync()\` BEFORE inserting profile rows, so \`reconcile\`/\`seedOutbox\` run on empty table — the dangerous path (write→disable→enable with mirror populated) is never exercised. Tests pass vacuously by avoiding the buggy ordering. Similarly, no test exercises multi-step lifecycle sequences (logout→read, login-A→login-B) — single-account happy-path only. Fix: (1) insert mirror data BEFORE enabling sync in setup; (2) add lifecycle sequence tests for account-switch and enable/disable/re-enable; (3) add \`assertSyncInvariants()\` in \`afterEach\` (outbox prunable, no orphaned cursors). Root cause: example-based tests encode author-chosen orderings; bugs live in orderings the author didn't think to write.

<!-- lore:019ee021-c2c9-7a52-ad4b-c2a98d4d2567 -->
* **sync-data.ts: seedOutbox/reconcile include pull-only tables — prune-floor DoS**: Trap: \`seedOutbox\` (sync-data.ts:412-426) and \`reconcile\` (sync-data.ts:436-455) iterate \`syncedTables(tier)\` with NO \`pullOnly\` guard — they enqueue outbox rows for \`profiles\`. \`pushOnce\` skips pull-only tables (sync.ts:78) so \`sync.push.profiles\` never advances from 0. \`profiles\` passes \`hasOutboxEntries()\` filter → contributes cursor 0 → \`minCursor=0\` → \`pruneOutbox(0)\` returns immediately → \*\*outbox pruning permanently disabled for ALL tables\*\* → unbounded SQLite growth + ever-growing per-cycle replay. The 'filter empty tables' fix (019ece19) does NOT help because \`profiles\` is non-empty yet un-advanceable. Fix: add \`pullOnly\` guard in \`seedOutbox\` and \`reconcile\` before enqueuing. Comment at sync-data.ts:52-54 claiming pull-only tables 'are never enqueued' is factually false.

<!-- lore:019ece19-d902-71c9-86d1-f51e74e8c6b9 -->
* **sync.ts drainTimestamp: keyset on idColumns\[0] only drops rows for composite-PK tables with >PAGE same-key entries**: Trap: \`drainTimestamp\` keysets by \`idc = idColumns\[0]\` (= \`knowledge\_id\`) for \`knowledge\_entity\_refs\`. If >200 refs share the same \`knowledge\_id\` + same \`updated\_at\`, rows beyond 200 are silently dropped — pagination terminates after 9 queries. Code comment at lines 322-324 claiming 're-scans the ms from the start … idempotent and loses nothing' is factually wrong. Empirically verified: single-group 250 refs (1 knowledge\_id, 1 ms) all applied correctly with composite keyset; multi-group 480 refs across 3 knowledge\_ids also correct. Fix: keyset on full composite \`(knowledge\_id, entity\_id)\` for composite-PK tables. \`\x1f\` (0x1f) sorts before any printable char — composite row\_id ordering is correct.

<!-- lore:019ece19-d8e5-7e0c-9fbf-fb29368de83c -->
* **sync.ts pushEntry: epoch-ms integers rejected by Postgres timestamptz columns (22008)**: Trap: \`pushEntry\` sends epoch-ms integers into \`timestamptz\` columns. Real Postgres rejects with error code \`22008\`; \`classifyPushError\` maps this to 'transient' → cursor never advances → row retried forever. Fix: \`toRemoteRow()\` converts \`typeof out\[k] === 'number'\` → \`new Date(out\[k]).toISOString()\` for \`TS\_COLS = Set(\['created\_at','updated\_at'])\`. SECOND BUG: \`stripSyncCols\` uses \`Date.parse(v) || Date.now()\` — \`Date.parse\` of epoch-0 ISO string returns \`0\` (falsy) → replaced with \`Date.now()\`. Since \`created\_at\` is NOT in \`HASH\_EXCLUDE\`, this corrupts the content hash → row never settles as 'skip'. Fix: use \`Date.parse(v) !== 0 ? Date.parse(v) : 0\` (or explicit null check). Epoch-0 only occurs if caller explicitly inserts 0 (schema has \`NOT NULL\`, no default).

<!-- lore:019ed09f-854d-7b26-8002-40a13dc0b58b -->
* **sync.ts pushEntry: phantom columns on knowledge\_entity\_refs cause permanent push stall**: Trap: \`pushEntry\` (sync.ts) unconditionally adds \`content\_hash\`, \`revision\`, \`is\_deleted: false\` to ALL tables including \`knowledge\_entity\_refs\`. The join table has no \`content\_hash\`/\`revision\` columns on remote. PostgREST returns HTTP 400 PGRST204 for unknown columns. \`classifyPushError\` only special-cases errcode \`23514\`; PGRST204 falls through to 'transient' → returns 'stop' → push cursor for \`knowledge\_entity\_refs\` never advances → entire join table stuck pending forever. Fix: \`SyncTableMeta.versioned\` flag (defaults true); strip \`content\_hash\`/\`revision\` from upsert payload when \`versioned === false\`. \`knowledge\_entity\_refs\` is the only non-versioned table in \`SYNCED\_TABLES.basic\`.

<!-- lore:019ece19-d8f2-7d7b-8b78-969756c830ae -->
* **sync.ts pushOnce: minCursor=0 from empty-table cursors blocks pruneOutbox forever**: Trap: \`pushOnce\` computes \`minCursor = Math.min(...per-table push cursors)\`. A synced table with zero outbox entries keeps cursor \`0\`. \`pruneOutbox(minCursor)\` early-returns when \`minCursor <= 0\` → outbox NEVER pruned whenever any one synced table has zero pending entries. Fix (implemented): compute \`minCursor\` only over tables where \`hasOutboxEntries()\` is true — empty tables excluded from floor calculation. Empirically verified: empty table does NOT pin floor; higher-cursor table with no peers fully pruned. Push-prune race is benign: rows inserted after cursors are read get \`seq > minCursor\` (AUTOINCREMENT monotonicity) → always survive prune.

<!-- lore:019ee0df-cba3-7fab-9beb-db13431cf1a4 -->
* **SYNCED\_TABLES fan-out: adding a table has silent ripple effects with no contract test**: Trap: adding a table to \`SYNCED\_TABLES\` (e.g. \`profiles\`) has ripple effects across \`seedOutbox\`, \`reconcile\`, \`pruneOutbox\`, \`applyRemote\` pull-classification, and outbox-capture triggers. No single contract test enforces all invariants for every registered table. \`profiles\` silently violated the prune-floor and tier-residue invariants on paths nobody re-checked after registration. Fix: add a per-table invariant test that asserts all registry-required properties (pullOnly guard in seed/reconcile, no outbox trigger, correct pull classification) whenever a new table is added to \`SYNCED\_TABLES\`.

### Pattern

<!-- lore:019ee535-02ec-7d0b-958a-1af2797b36db -->
* **db.ts knowledge table: columns added across migrations**: Base \`knowledge\` table (db.ts:108-120, v1): id, project\_id, category, title, content, source\_session, cross\_project, confidence, created\_at, updated\_at, metadata. Added: \`embedding BLOB\` (v8, line 250); \`created\_by, updated\_by, sensitivity, promotion\_status, promoted\_at, approval\_status, approved\_by, approved\_at, source\_user\_id, source\_entry\_id, last\_accessed\_at\` (v29, lines 701-721); \`worker\_provider\_id, worker\_model\_id\` (v35, lines 1007-1008); \`last\_reinforced\_at INTEGER\` backfilled=updated\_at (v48, lines 1216-1217). Indexes: \`idx\_knowledge\_project/category/cross\` (lines 148-150), \`idx\_knowledge\_worker\` (line 1011, recovered line 1657). No UNIQUE on \`(project\_id, title)\` — dedup enforced in application code.

<!-- lore:019ee578-4d12-7a19-8462-ba315a379a83 -->
* **pnpm-lock.yaml merge conflict resolution: take main as base, regenerate via pnpm install**: When rebasing a branch with \`pnpm-lock.yaml\` conflicts, the correct strategy is: (1) take \`origin/main\`'s lockfile as the base (accept theirs), (2) run \`pnpm install\` to regenerate the lockfile incorporating the branch's package.json changes. Never manually merge lockfile content — the regenerated lockfile is canonical. After regeneration, verify new package scopes are present and old ones are absent before committing.

<!-- lore:019ed0b5-e3c2-77b1-a8b1-94d56f85a9da -->
* **pnpm.overrides used for transitive vulnerability fixes in root package.json**: Transitive dependency vulnerabilities are fixed via \`pnpm.overrides\` in root \`package.json\`. Current overrides: \`basic-ftp: 5.3.1\`, \`ip-address: 10.2.0\`, \`fast-xml-parser: 5.7.3\`, \`@huggingface/hub: 2.11.0\`, \`yaml: >=2.8.3\`, \`esbuild: >=0.28.1\`, \`@opentelemetry/core: >=2.8.0\`, \`protobufjs: >=7.6.3\`. CI uses \`pnpm-lock.yaml\` as canonical lockfile (all jobs use \`pnpm install --frozen-lockfile\`). \`bun.lock\` was deleted — it was stale and only used by the now-removed \`bun pm pack\` path in \`bump-version.sh\`.

<!-- lore:019ee571-e330-7de7-ac46-3e33555f368d -->
* **PR merge order for stacked branches: retarget to main after base merges**: When a PR is stacked on a feature branch (e.g. #831 base = \`feat/824-profiles-tier\`), after the base PR squash-merges into main the stacked PR must be retargeted to \`main\` before merging. GitHub's 'CLEAN' mergeable status only checks textual conflicts — after retargeting, verify CI passes and the diff is correct (no duplicate commits from the base). Pattern observed: #828 merged first (consumed migration v49), then #831 retargeted to main.

<!-- lore:019ee1f9-fe94-7852-afed-4d15d606b3da -->
* **sync test stacking: invariant batteries stacked on feature branch, not main**: When adding invariant/contract test batteries that depend on a feature branch's new machinery (e.g. \`pullOnly\` flag, \`clearProfileMirror\`), stack the test branch on the feature branch rather than main. Pattern used in PR #828 (\`test/sync-invariant-batteries\` stacked on \`feat/824-profiles-tier\`): avoids duplicating machinery, keeps separation of concerns, acknowledges dependency explicitly. Do NOT add test batteries directly to a feature branch already under review — create a stacked branch instead.

<!-- lore:019ee5b8-6fed-7dcb-ab57-53e548838852 -->
* **vitest.config.ts: alias must be at top-level resolve.alias, NOT test.resolve.alias**: In \`vitest.config.ts\`, \`@loreai/core\` → \`packages/core/src\` and \`@loreai/gateway\` → \`packages/gateway/src\` aliases MUST be placed at top-level \`resolve.alias\`, NOT \`test.resolve.alias\`. Placing them under \`test.resolve.alias\` silently resolves to the stale dist build instead of src — tests pass against old compiled output, masking source changes. Comment in config notes this explicitly.

### Preference

<!-- lore:019ee562-ccf0-7e2f-b798-c79d2e8c9799 -->
* **Always address Seer (sentry\[bot]) review comments by fixing or explicitly rejecting with reasoning**: When Seer leaves review comments on PRs, the user expects each comment to be handled explicitly: either fix the flagged issue (with a targeted code change, regression test, and clean build gates) or reject it with documented reasoning (e.g., reply explaining why the suggested change is incorrect). The user does not ignore Seer comments. After addressing them, the user expects: (1) the fix isolated to a dedicated branch, (2) a regression test added near the relevant test block, (3) typecheck + lint + full test suite passing, and (4) only the intended files changed. Seer comments are treated as actionable code review items regardless of severity level.

<!-- lore:019ee003-8a07-77cc-a945-d2935117fa6b -->
* **Always annotate critical invariants with directive comments in source code**: The user consistently embeds authoritative behavioral directives as inline comments directly in source code (e.g., 'never fast-forward over it', 'Any other UPDATE can never increase it', 'never an error to crash on, and never blocks other tables'). These comments serve as canonical rules that govern implementation decisions. When reviewing or writing code, treat these inline directive comments as hard constraints — not suggestions. When implementing related logic, check for existing directive comments and ensure the implementation strictly honors them. When adding new invariants, express them as imperative directive comments at the relevant code site.

<!-- lore:019ee635-7c57-71ac-896b-b12d71720009 -->
* **Always ask why a bug wasn't caught by tests when discovering a defect**: When the user finds a bug or error (especially a recurring one documented as a known footgun), they immediately ask why the existing test suite didn't catch it. They expect the assistant to explain which gate(s) caught it, why it was or wasn't caught earlier, and what the coverage gap is. If tests did catch it, the assistant should clarify the exact mechanism. If tests didn't catch it, the assistant should identify the gap and implicitly or explicitly suggest a fix. This applies to parse errors, migration SQL issues, lint failures, and logic bugs alike.

<!-- lore:019edfc9-c73c-7376-83fc-a1c95deaf2e8 -->
* **Always assert precise semantic invariants when architectural framing is incorrect**: When the assistant uses imprecise or incorrect framing (e.g., 'server-side reaper,' 'authoritative remote,' 'never deleted'), the user immediately corrects it with a precise invariant statement (e.g., 'remote is a multi-master sync backend,' 'prune is sync-invisible cache eviction only,' 'undistilled messages are NEVER enqueued'). These corrections are declarative and absolute — the user states what MUST or MUST NEVER happen, often tied to specific code locations or design goals. The assistant should treat these corrections as hard requirements, update all downstream reasoning accordingly, and never reintroduce the rejected framing.

<!-- lore:019ee5d4-d872-7778-800e-07b72f6b3b4d -->
* **Always check for Seer review comments on PRs before proceeding with next tasks**: Before moving on to a new issue or task, the user consistently checks whether Seer (sentry\[bot]) has posted a review comment on the current or relevant PR. This applies even when CI is fully green. The user expects the assistant to fetch and report the Seer comment status explicitly — noting if no Seer comment exists yet, or summarizing any Seer-flagged issues if present. Only after confirming Seer's review status does the user proceed to the next work item.

<!-- lore:019edf93-0d53-7c8d-990f-36f9fe322bfe -->
* **Always conduct adversarial code reviews with explicit PASS/FAIL/CONCERN verdicts and file:line evidence**: When reviewing code, the user expects an adversarial subagent review that evaluates each scrutiny point with an explicit verdict (PASS, FAIL, CONCERN, or MUST-FIX) backed by specific file:line citations. Reviews must verify build gates (typecheck, lint, tests) before proceeding. Each finding should state the claim being tested, the code path that satisfies or violates it, and any residual risks. User also embeds authoritative assertions (marked 🔴) about intended invariants that reviewers must treat as ground truth rather than re-litigate. Minor issues are labeled NITs or CONCERNs; blocking issues are MUST-FIX.

<!-- lore:019ee5df-60fe-71ed-be0f-8d376a26c591 -->
* **Always conduct adversarial pre-merge reviews with empirical verification before shipping**: Before merging any PR, the user expects a thorough adversarial review that includes: (1) empirically verifying behavioral claims using actual tool results (grep, test runs, typechecks), (2) identifying dormant vs. live issues and distinguishing blockers from nits, (3) confirming invariants hold across the full codebase (not just the diff), and (4) producing a clear verdict (e.g., SHIP-WITH-NITS, MERGE-AFTER-FIXES). Reviews must be READ-ONLY — no file modifications. The user expects findings to be categorized by severity and cross-referenced with specific file/line locations.

<!-- lore:019ee5d2-1d6e-7607-b465-5bbba13b4984 -->
* **Always conduct adversarial pre-merge reviews with explicit READ-ONLY constraints and structured scrutiny points**: When reviewing PRs, the user assigns the assistant a staff engineer adversarial reviewer role with explicit READ-ONLY mode (no file modifications). Reviews are scoped to specific numbered scrutiny points (correctness, logic, migration-safety, behavior-neutral claim verification). The user pre-loads critical context: migration version numbers, behavior-neutral claims to verify, files changed, and what is explicitly out of scope (e.g., security). The user expects the assistant to verify each scrutiny point systematically against actual code, not just accept PR claims at face value. This pattern applies whenever a sub-PR lands in a multi-PR epic sequence.

<!-- lore:019ee663-d668-7388-a357-1184d10236ce -->
* **Always conduct adversarial READ-ONLY audits before shipping, with explicit PASS/FAIL verdicts per section**: When reviewing code (especially merged PRs or new modules), the user expects a structured adversarial audit that is strictly READ-ONLY, covers each logical section with an explicit verdict (PASS, CONCERN, MUST-FIX), includes hand-computed spot-checks of formulas with concrete numbers, verifies invariants the user has previously asserted (e.g., 'never pay to warm on a tie', 'never return empty'), and concludes with an overall ship verdict (e.g., SHIP-WITH-FOLLOWUPS, NEEDS-REWORK). The audit must always produce a written report — never an empty response. Concerns that don't block shipping are noted as follow-ups, not blockers.

<!-- lore:019ee645-fc75-77da-b86a-343928a967a4 -->
* **Always conduct adversarial read-only code reviews with a mandatory written report**: When the user requests a code review, they consistently specify two hard constraints: (1) READ-ONLY — the assistant must never modify any files, and (2) never return empty — a full written report must always be produced. Reviews are adversarial and skeptical in tone. The user typically scopes the review explicitly (e.g., specific files, specific PRs, specific concerns like shadow-mode correctness or logic-only), and may exclude certain already-reviewed modules from re-review. The assistant should read all specified files carefully, produce a structured written findings report, flag bugs/logic issues, and never skip the report even if no issues are found.

<!-- lore:019ee582-de70-757d-ab75-4f917ab0928c -->
* **Always coordinate multi-session work with explicit handoff briefs and sync-back protocols**: When multiple parallel sessions or contributors are working on related branches, the user expects the assistant to: (1) produce a structured handoff brief listing ownership boundaries, migration version numbers, specific write-sites to touch, deferred items, and CI gotchas; (2) annotate shared design docs (e.g., GitHub issues) with pre-implementation corrections before the other session starts; (3) define explicit sync-back triggers (e.g., 'ping when Sub-PR 1 is merged'). This applies whenever work is split across sessions, PRs, or contributors touching overlapping files (db.ts migrations, sync-data.ts, test files). The assistant should proactively identify cross-session conflicts, version numbering collisions, and dependency ordering without being asked.

<!-- lore:019ee595-351e-792d-8651-d83e6e8f825b -->
* **Always create a dedicated branch before starting issue work**: When beginning work on a GitHub issue, the user consistently creates a new branch named with the pattern \`\<type>/\<issue-number>-\<short-description>\` (e.g., \`chore/832-stryker-mutation-testing\`, \`feat/db-query-tracing-seam\`) branched off main before making any changes. The user confirms the branch is tracking origin/main and notes the commit it branched from. This happens at the very start of issue work, before any file edits, installs, or configuration changes. Always branch first, using the conventional naming pattern, before proceeding with implementation steps.

<!-- lore:019ee00a-fcd9-7640-837f-cd5d38627647 -->
* **Always demand a written report; never return empty**: The user explicitly and repeatedly states 'never return empty' — the assistant must always produce a full written report as output. This applies to all review sessions (code review, design review, logic review) regardless of scope. Even if findings are minor or the change set is small, the assistant must deliver a structured written report covering the requested review lens. Returning nothing, a one-liner, or a deferral is never acceptable. The report should be thorough, organized, and directly address the stated review focus (e.g., correctness, logic, architecture) without drifting into out-of-scope concerns (e.g., security when not asked, backward compatibility when greenfield context is asserted).

<!-- lore:019ee0e0-baff-7864-a54f-01e948422fb8 -->
* **Always document design decisions and code locations as structured session observations before implementing**: Before writing any code, the user systematically catalogs: (1) exact file locations and line numbers of relevant functions, (2) confirmed design decisions with specific constants/values, (3) current state of affected types and exports, (4) existing test coverage and what must change, and (5) UI/API surface areas impacted. These are recorded as timestamped 🟡/🔴 observations. Red observations flag user-asserted constraints or enforced workflow rules; yellow observations are confirmed facts. The assistant should mirror this pattern by surfacing all relevant code locations and design constraints explicitly before proposing implementation steps.

<!-- lore:019ee57e-b737-7a19-8116-89a19175c180 -->
* **Always document design invariants as explicit named rules with verbatim rationale**: When the user identifies a design constraint, economic rule, or behavioral invariant in the codebase, they state it as an explicit, named rule with verbatim wording (e.g., 'always compress at tier boundaries,' 'never require > 100% probability'). These rules are treated as inviolable constraints that proposed fixes must not violate. When reviewing code changes, the user checks whether proposals respect all stated invariants and rejects those that interfere — even if the fix is technically correct. The assistant should surface these rules explicitly when proposing changes, confirm each invariant is preserved, and never silently trade off one invariant against another.

<!-- lore:019ee4d4-bdd2-7641-b523-34aa917fdb48 -->
* **Always document directives with 🔴 markers and preserve them across sessions via written plans**: The user consistently marks critical invariants with 🔴 (red circle) to distinguish hard requirements from observations (🟡). These cover correctness invariants (e.g., per-bucket isolation, neutral non-2xx handling), role context, and repo paths. The user expects these captured in persistent written artifacts (e.g., \`.opencode/plans/\`) so they survive across sessions. When reviewing code, the user acts as a skeptical adversarial staff engineer and expects proactive surfacing of invariant violations. Always treat 🔴-marked statements as non-negotiable constraints. Key recurring 🔴 invariants: (1) non-2xx warmup result must be NEUTRAL — never increments/trips breaker; (2) per-bucket isolation — one bad bucket must never disable warming for other sessions; (3) legitimately cold re-warm (\`cacheLikelyAlive===false\`) must NOT count as failure.

<!-- lore:019ee0df-5a7b-7e54-b3c1-87114892f258 -->
* **Always document exact file locations, line numbers, and export relationships for all touched symbols**: When exploring or modifying code, the user consistently records precise file paths, line numbers, export names, import sites, and call sites for every relevant symbol — including constants, functions, types, and test helpers. This applies to both the code being changed and all downstream consumers. The user expects observations to capture not just what a symbol does, but where it lives, where it's imported, and what other code depends on it. When planning refactors, the user maps the full dependency graph (e.g., exported from X, imported in Y at line N, called at Z) before writing any code. Follow this pattern by always anchoring implementation notes to specific line numbers and tracing all import/export relationships explicitly.

<!-- lore:019edc56-256a-7564-8e86-b60e066a89d3 -->
* **Always document invariants and design decisions as code comments at the exact location they apply**: The user consistently embeds precise behavioral invariants, design rationale, and non-obvious constraints as code comments directly at the relevant lines (e.g., 'never below the floor', 'never between an assistant(tool\_use) and its user(tool\_result)', 'always recover LTM on the post-idle turn'). These comments are treated as authoritative assertions about system behavior. When investigating code, the user expects the assistant to read and cite these comments as ground truth. When writing or modifying code, the assistant should add similar inline comments that capture the 'why' and any invariants that must hold, placed at the exact line where the constraint is enforced.

<!-- lore:019edff4-de44-7cdb-b6bf-bd90f2c46718 -->
* **Always enforce security constraints at the database layer, never in application code**: When designing multi-tenant data systems, the user insists that all security invariants (quota enforcement, ownership isolation, immutability of scope\_id, author\_id integrity) live exclusively in Postgres via RLS policies, SECURITY DEFINER triggers, GRANTs/REVOKEs, and CHECK constraints — never in application code or client logic. Clients write directly to PostgREST as \`authenticated\` role, so the database must be the sole enforcement layer. When reviewing or writing migrations, always ensure: counters are trigger-maintained (never client-writable), immutability guards raise exceptions before any mutation, and no DML privilege is granted to \`authenticated\` on sensitive tables like \`user\_table\_usage\`.

<!-- lore:019ee643-4a4c-7ca4-b507-eab72442fdfd -->
* **Always enforce single-entry-point architecture to prevent divergence between parallel subsystems**: When two subsystems (e.g., cache warmer and gradient compaction) model the same event with separate heuristics, the user insists on a single centralized evaluation function that reads all inputs once, decides once, and stores the result. Consumers must be read-only branches off that stored result — they never recompute independently. This pattern applies whenever divergence between parallel decision paths is structurally possible. The user will block implementation until this architecture is locked in, and expects a two-phase rollout: shadow mode first (log would-be vs current), then flip consumers to read the stored result.

<!-- lore:019edc41-8ace-7f96-9a53-fd86dd3a2a29 -->
* **Always enforce strict branch/PR scope discipline by keeping unrelated changes on separate branches**: Strict branch/PR scope discipline: each branch and PR must contain exactly one logical fix/feature. Uncommitted working-tree changes from a different concern are a structural violation — stash/patch to a clean branch or flag explicitly. When working-tree changes span multiple concerns, identify as a scope problem and propose separation before proceeding. Never bundle unrelated changes into an existing PR without explicit user approval. Never delete local-only branches or branches checked out in other worktrees without confirming scope. When stranded changes are found on a merged/wrong branch: (1) identify files, (2) create clean branch off origin/main, (3) apply only relevant changes, (4) open new PR.

<!-- lore:019ee5e8-8abe-79e6-9cbc-c96303bd3fa0 -->
* **Always enforce strict inequality for tie-breaking toward less-warming options**: When implementing decision logic that chooses between warming and cooling strategies, the user requires that ties always resolve toward the cooling/less-warming option. Hold-warm must only be selected with strict \`<\` (never \`<=\`) compared to the best cool cost. Similarly, among cool options, cool-bust wins only on strict \`<\` vs cool-full-write. The user explicitly corrects any tie-breaking that would allow a warming strategy to win on equality. The rule: 'never pay to warm on a tie' — cooling is always the default when costs are equal.

<!-- lore:019ece05-3058-7c4b-afd7-f668085684cd -->
* **Always enforce tool\_use/tool\_result pair safety as a hard invariant with code comments**: tool\_use/tool\_result pair safety is a hard invariant. safeDeltaInsertIndex is the primary guard: walk back whenever messages\[idx-1] is assistant(tool\_use), regardless of what follows (covers completed pairs AND pending tail). Loop: \`while (idx > 0)\` (no upper bound). removeOrphanedToolResults is last-ditch backstop that 'should essentially never fire.' Always cover both completed pairs AND pending/in-flight dangling tool\_use at tail in tests. Never rely on destructive stripping as primary safety — insertion point must be computed correctly upfront. Invariant documented in code comments referencing bug #747.

<!-- lore:019edc8f-7bdd-73a0-a374-a700828d04be -->
* **Always exclude .lore.md from PR diffs by restoring origin/main version before branching**: When creating a new branch for a PR, always restore \`.lore.md\` to \`origin/main\` version before branching (\`git checkout origin/main -- .lore.md\`) to prevent \`.lore.md\` chore commits from polluting the PR diff. If \`.lore.md\` accidentally appears in a diff or commit, rebuild the branch clean (soft-reset, unstage \`.lore.md\`, recommit). Even if \`.lore.md\` was modified during the session (including by lint:fix auto-formatting), it must be reset before staging. Exception: when branch is created off \`origin/main\` directly and \`git diff origin/main -- .lore.md\` shows no changes — confirmed safe.

<!-- lore:019ee01d-335d-7cb1-a691-85e8b7c18f2c -->
* **Always expect a written report with severity-tagged findings after code review**: When the user requests a code or security review, they expect a structured written report as output — never an empty or conversational response. Findings must be tagged by severity/disposition (e.g., SHIP-WITH-NITS, SHOULD-FIX, NIT, blocker vs. non-blocker) and include specific file/line references. The report should distinguish between functional blockers and acceptable-for-now issues, explain the empirical or logical basis for each finding, and propose concrete fixes. The user explicitly stated 'never return empty' and frames reviews as offensive security/correctness audits with a pre-merge gate mindset.

<!-- lore:019ee657-e62f-7a4c-af62-1f8673e0fcd2 -->
* **Always explicitly state 'never return empty' as a session directive before requesting code reviews**: At the start of adversarial or holistic code review sessions, the user consistently issues the directive 'never return empty,' meaning the assistant must always produce a complete written report regardless of findings. This applies to READ-ONLY audit sessions, pre-merge reviews, and multi-PR holistic reviews. When this directive is given, the assistant must always deliver a structured written report — never a blank, minimal, or deferred response. The pattern appears in every review session and is treated as a hard constraint, not a preference.

<!-- lore:019ee020-6399-73c1-a2e1-46a66b82a267 -->
* **Always flag factually incorrect code comments as bugs requiring correction**: When reviewing code, the user treats misleading or false inline comments as bugs on par with logic errors — not just style issues. They explicitly call out when a comment claims behavior that the code does not implement (e.g., 'pull-only tables are never enqueued' when no such guard exists). When providing reviews or analysis, always verify that comments accurately describe the actual code behavior. If a comment contradicts the implementation, flag it as a factual error requiring a fix, not merely a documentation improvement. This applies especially to security/correctness-critical comments about data flow, guards, and invariants.

<!-- lore:019ee00b-4073-7c80-b869-4d0ed72dd040 -->
* **Always frame security/code reviews with explicit role, repo path, and 'never return empty' directive**: At the start of each review session, the user consistently establishes: (1) their role (offensive security engineer, staff engineer, etc.), (2) the exact repo path and branch, (3) the specific issue/PR being reviewed, (4) a 'never return empty' directive requiring the assistant to always produce a written report. They also front-load critical architectural constraints (e.g., direct PostgREST writes, no server gateway, all isolation in Postgres) and greenfield context (no production data, backward compat is non-issue). The assistant should always acknowledge these directives explicitly and produce a substantive written report — never a refusal or empty response — structured around the stated review scope.

<!-- lore:019ee595-31f4-737e-8120-91c5d9bb1129 -->
* **Always front-load critical constraints and invariants as explicit 🔴 assertions before implementation begins**: At the start of each session or sub-task, the user explicitly states non-negotiable constraints, invariants, and coordination requirements as 🔴 items before any implementation work begins. These cover: version numbers, migration sequencing, which files are off-limits, which functions have no callers yet, which guards must be preserved, and what CI tests will break if violated. The user expects the assistant to treat these as hard requirements — not suggestions — and to flag immediately if any proposed change would violate them. When handing off to another agent/session, the user also expects these constraints to be compiled into a structured brief so nothing is lost across context boundaries.

<!-- lore:019edfeb-45b4-7fd0-8c6d-d366b336dae8 -->
* **Always front-load session context with explicit red-line directives before exploration begins**: At the start of each session, the user declares a structured set of 🔴 (hard constraints/directives) and 🟡 (informational context) observations before any work begins. Red-line directives include: mode restrictions (read-only vs. write), scope boundaries (which files/tables are in play), architectural invariants (e.g., never WAL on NFS, never push profiles), and explicit behavioral rules for the assistant (e.g., 'never return empty', 'do NOT modify files', 'do NOT touch live Supabase'). The assistant should treat 🔴 items as inviolable constraints throughout the session and 🟡 items as reference context. Always acknowledge and respect these upfront directives without requiring re-statement mid-session.

<!-- lore:019ee640-03aa-76b7-ab9d-bf5cb31885a3 -->
* **Always hand-verify surviving mutants by manually applying the mutant and running tests directly**: The user consistently insists on manual verification of Stryker surviving mutants rather than trusting automated reports alone. When a mutant is reported as surviving, the user (or team policy) requires manually applying the mutation to the source code and running the relevant test file directly to confirm whether it truly survives or is a false positive. This is especially important given the known Stryker × Vitest 4.1 false-survivor bug. When documenting mutation testing results or config files, always include a prominent note reminding contributors to hand-verify survivors before acting on them. Never treat a Stryker 'Survived' result as ground truth without this manual confirmation step.

<!-- lore:019ee571-e91c-7311-b5f1-271336d0e907 -->
* **Always investigate root causes through systematic code tracing before proposing fixes**: When debugging a complex issue, the user consistently follows a structured investigation pattern: (1) locate all relevant code sites using tool reads, (2) document exact line numbers and logic at each site, (3) identify gaps between what existing code covers and what it misses, (4) articulate the root cause precisely before proposing any fix. The user does not jump to solutions — they insist on confirming the full picture (e.g., all null-out sites, all bust paths, all assembly points) and explicitly naming the architectural tension before moving to 'next step: locate exact code sites for invalidation.' Fixes are proposed only after the gap is fully mapped.

<!-- lore:019ee1fa-0665-7619-b4c3-23ea7c45d4f8 -->
* **Always isolate in-review PRs by stacking dependent work on feature branches rather than adding to them**: When a PR is already under review, the user never adds new commits to it directly. Instead, they create a new branch stacked on the in-review feature branch (not main) to keep separation of concerns while acknowledging the dependency. The user explicitly names the dependency in the branch name or commit message, enables auto-merge where appropriate, and files unrelated items as follow-up GitHub issues rather than bundling them into the current PR. This pattern applies whenever new work depends on unreleased machinery from a branch already in review.

<!-- lore:019edffd-9a2d-756e-ac21-ed02f83056df -->
* **Always lock in explicit decisions before implementation begins**: Before writing any code or migrations, the user explicitly enumerates and locks named decisions (D1, D2, D3, etc.) covering schema changes, behavioral choices, and constraints. These decisions are stated as directives that govern the entire implementation scope. The user also issues explicit session-mode directives (e.g., 'read-only exploration', 'do NOT modify files', 'very thorough') before work starts. When implementing, the user expects the assistant to reference these locked decisions rather than re-derive them. Any deviation or ambiguity in a decision must be surfaced before coding, not discovered mid-implementation.

<!-- lore:019edfec-f6d6-7bdb-ab15-7399db9961c6 -->
* **Always mirror local plan files to GitHub issues to establish shared source of truth**: Local \`.opencode/plans/\*.md\` files are gitignored and not accessible to others. Always dump plan content into their respective GitHub issues so collaborators can access them. GitHub issues are the authoritative source of truth for plans; local plan files should not be relied upon. When creating issues from plans, include a banner linking the parent epic and dependencies at the top of each sub-issue body.

<!-- lore:019edba4-107c-7eba-9435-3ec40c81de31 -->
* **Always normalize schema version to MIGRATIONS.length after migration loop**: When adding or reviewing database migrations, the user consistently enforces that the schema version is set to \`MIGRATIONS.length\` dynamically — never a hardcoded integer. This applies in db.ts where \`UPDATE schema\_version SET version = ${MIGRATIONS.length}\` is the authoritative pattern. Correspondingly, the test assertion in db.test.ts (e.g., \`expect(row.version).toBe(N)\`) must be bumped to match the new array length whenever a migration is added. Never introduce a manual version constant; always derive the version from the array length.

<!-- lore:019ee1fb-083b-756f-9bb3-0537364e2057 -->
* **Always open a PR that closes the issue and references the parent epic**: After pushing a feature branch, the user consistently opens a PR that explicitly closes the implementing issue (e.g., 'Closes #822') and references the parent epic (e.g., 'part of epic #821'). Always include both references in the PR description. The user also expects CI to pass cleanly (all checks green) before considering the PR ready, and treats a clean CI run as the final validation step before merge.

<!-- lore:019ee5e4-7124-73a8-ba61-144a7886e645 -->
* **Always perform adversarial READ-ONLY code reviews with structured verdict reports**: When reviewing code, the user expects a formal adversarial review that is strictly read-only (no file modifications), produces a written verdict report (never empty), and follows a structured format: per-section PASS/FAIL/CONCERN verdicts, hand-verified arithmetic spot-checks, monotonicity/invariant proofs, and explicitly categorized findings as MUST-FIX, SHOULD-FIX, CONCERN, or NIT. Reviews must confirm build gates (typecheck, lint, tests) pass. MUST-FIX items require a concrete wrong-decision example demonstrating real harm. User directives and design invariants are flagged with 🔴 and verified against implementation line numbers.

<!-- lore:019ee0de-6eca-7993-990f-4a659bba8fa6 -->
* **Always perform adversarial review to find bugs that tests miss**: The user consistently applies adversarial/critical review to PRs and testing strategies, specifically hunting for bugs that pass through existing tests due to structural blind spots. They focus on: (1) vacuous test passes caused by setup ordering or unrealistic state, (2) untested skip/early-return branches, (3) missing lifecycle/state-transition sequences, and (4) invariants that exist only in comments rather than being continuously checked. When reviewing code or tests, proactively identify these failure modes, flag zero-coverage guard branches, and recommend concrete fixes (mutation testing, property-based tests, invariant helpers in afterEach). Don't just confirm tests pass — assess whether they actually constrain the behavior they claim to test.

<!-- lore:019edff3-834e-77ad-85fd-e79a3db836d0 -->
* **Always prefix observations with colored emoji indicators (🔴/🟡) to signal importance level**: When recording session observations, the user consistently uses 🔴 (red circle) to mark critical directives, invariants, and design constraints that must never be violated, and 🟡 (yellow circle) to mark factual findings, code details, and contextual information. Each observation is timestamped. Red observations represent user-authored rules or hard behavioral requirements; yellow observations represent discovered facts about the codebase. When summarizing or presenting findings back to the user, mirror this convention: use 🔴 for constraints/rules and 🟡 for factual/contextual details.

<!-- lore:019ee1fa-63b0-72f9-8735-da197b8fc3ba -->
* **Always preserve design rules and invariants as code comments at the exact assertion site**: When the user asserts a design rule, invariant, or behavioral constraint (e.g., 'savings MUST be credited against warmup's cacheReadTokens, NEVER the returning turn's', 'sub-agent sessions are always exempt', 'always fund first warmup of a break'), they expect it to be captured as a comment directly in the source code at the relevant line — not just acknowledged in conversation. These comments serve as authoritative documentation for future maintainers. The pattern appears consistently: user states a rule, the rule is embedded as a comment in the implementation file at the precise location where the behavior is enforced. Always write these assertions as inline comments when implementing or modifying the related code.

<!-- lore:019ee641-7645-75ff-8137-ec174943a983 -->
* **Always promote implicit invariants to explicit, continuously-checked runtime assertions in test teardown**: When the user identifies invariants that are currently only documented in comments or implicitly assumed, they create dedicated helper functions (e.g., \`assertSyncInvariants()\`) and wire them into \`afterEach\` hooks across all relevant test files. This pattern applies specifically to load-bearing correctness properties (e.g., 'never wedge the prune floor', 'mirror holds ≤1 row') that bugs have slipped past. The helpers must be fast (pure DB queries), exported for reuse, and documented. The user files GitHub issues to track this work and implements it on dedicated branches. When suggesting or implementing invariant checks, always wire them into \`afterEach\` across all relevant test suites, not just the test that motivated the change.

<!-- lore:019ee535-02e6-7648-82b7-d6c68de1256a -->
* **Always provide file:line inventory when researching codebase impact**: When asked to research how a change would affect the codebase, the user expects an exhaustive file:line inventory covering every affected site. This is a recurring directive across 42+ sessions. Format: \`filename:line — description\`. Cover all SQL patterns, API surfaces, test assertions, and schema version checks. Research-only phases must not change code.

<!-- lore:019ee52e-aaab-7576-98f5-8d246e7180b7 -->
* **Always pull from latest main before making architectural or sequencing decisions on active LTM work**: When assessing what work remains, what to tackle next, or how to sequence issues/PRs, the user expects the repo to be pulled to latest main first. LTM/knowledge work is actively merged and the user has explicitly stated 'pull from latest main before making final decisions on anything — LTM work is active on main.' The assistant should check current PR/branch status and pull latest before advising on priorities, dependencies, or what is blocked/unblocked. Decisions made on stale state are considered invalid.

<!-- lore:019edfb0-94bd-700f-b6fa-2853572cd489 -->
* **Always rebase dependent PR branches onto new main after each PR merges**: After a PR is squash-merged into main, the user immediately rebases all downstream/dependent branches onto the new main tip, dropping the now-redundant commits that were squashed. The pattern is: (1) confirm the merge commit SHA on main, (2) use \`git rebase --onto origin/main \<old-tip>\` to rebase each dependent branch, dropping already-applied commits, (3) force-push the rebased branch, (4) verify typecheck and targeted tests pass, (5) check CI status on the retargeted PR. This applies to stacked PR workflows where PR2 and PR3 depend on PR1 — once PR1 merges, PR2 is rebased onto main, then PR3 onto the updated PR2, maintaining a clean linear stack.

<!-- lore:019ee578-58d1-707c-9221-58a5592534f5 -->
* **Always rebase/merge onto origin/main and regenerate pnpm-lock.yaml when conflicts arise**: When a branch has merge conflicts (especially in pnpm-lock.yaml), the user consistently resolves them by rebasing or merging onto origin/main, then regenerating the lockfile via \`pnpm install\` rather than manually resolving lockfile conflicts. After conflict resolution, the user always runs typecheck (\`tsc --noEmit\`) to verify correctness, then force-pushes (with lease) and waits for CI to pass before merging. The user never includes \`.lore.md\` in commits and never stashes it — it is restored to HEAD before rebase operations.

<!-- lore:019ee0d8-86e5-78d0-8129-464e0cea22a0 -->
* **Always record user-authored comments and directives from source files as 🔴 observations**: When reading source files (SQL migrations, TypeScript, config), the user expects user-authored comments, design invariants, and explicit directives embedded in the code to be extracted and logged as 🔴 (high-priority) observations — distinct from assistant reasoning (🟡). This applies to inline SQL comments, file header design assertions, and any imperative statements the user wrote directly into the code. These are treated as authoritative constraints, not just documentation, and must be surfaced verbatim or near-verbatim rather than paraphrased into assistant analysis.

<!-- lore:019ee4ee-630d-77f4-8e19-67fc463ac1fc -->
* **Always request adversarial pre-merge code review before committing changes**: Before merging or finalizing code changes, the user consistently requests an adversarial, skeptical review — explicitly framed as a staff engineer doing a read-only audit. This review should: (1) identify specific risk areas upfront, (2) scrutinize them in parallel using subagents when possible, (3) produce a written report (never return empty), and (4) focus on correctness invariants the user has stated (e.g., per-bucket isolation, neutral non-2xx handling, cold-rewarm exemption). The review should be proactive — flag memory leaks, write overhead, edge cases, and circular imports — not just confirm the happy path. Always produce findings even if no critical bugs are found.

<!-- lore:019edf91-b5ca-78f3-987d-042124af662e -->
* **Always request adversarial self-review of all PRs before merging, suggesting subagent use for objectivity**: Before merging any PR, the user consistently launches a dedicated adversarial review subagent that evaluates the change across multiple named axes (e.g., safety, correctness, idempotency, test quality). The subagent must produce an explicit verdict (APPROVE/MERGE/MERGE-AFTER-FIXES/NEEDS-REWORK) with per-axis PASS/FAIL/CONCERN ratings. Reviews must include: running typecheck, linter (biome), and targeted tests; auditing all code paths relevant to the change; and flagging blockers vs. non-blocking concerns. The assistant should proactively structure reviews this way and never recommend merging without this gate.

<!-- lore:019ee01f-0f21-785a-9e1e-5aefd225505b -->
* **Always request adversarial/security review before merging sync-related PRs**: Before merging any database migration or security-sensitive PR, the user consistently requests a dedicated adversarial or security-focused review (often via a subagent). These reviews follow a structured format: enumerate attack axes, run empirical probes, assign severity levels (HIGH/MEDIUM/LOW/NIT), and return a verdict (SHIP-WITH-NITS, MERGE-AFTER-FIXES, SOUND, etc.). The user expects: (1) concrete SHOULD-FIX vs NIT distinctions, (2) empirical confirmation of fixes (not just static analysis), (3) explicit tracking of prior findings as RESOLVED/OPEN, and (4) documented accepted tradeoffs. Always structure security reviews with these elements rather than giving informal feedback.

<!-- lore:019ee00c-3862-7b11-9190-b5eb03017dfe -->
* **Always request adversarial/skeptical READ-ONLY security reviews of PRs or design plans before implementation or merge**: The user consistently requests security-focused, read-only reviews (no file modifications) of either design plans or PRs before they are implemented or merged. Reviews are framed from a specific adversarial role (senior security engineer, database-security engineer, skeptical principal engineer). The user provides precise context upfront: repo path, branch, changed files, remote schema details, and architectural constraints. The assistant must produce a written report — never return empty. Focus areas include: RLS bypass risks, tier escalation vectors, push/pull boundary enforcement, schema invariants, and test coverage gaps.

<!-- lore:019ee0df-d168-7b4f-b726-35431954fba1 -->
* **Always request thorough diagnosis of why tests failed to catch bugs, not just what the bugs were**: When bugs are discovered (especially in PR review), the user consistently asks for a deep analysis of the testing strategy itself — why the existing tests missed the bugs, what structural blind spots exist, and what systemic improvements would prevent recurrence. The user wants: (1) root cause of each test failure mode, (2) identification of the common pattern across missed bugs, (3) honest assessment of testing strategy strengths and weaknesses, and (4) a prioritized list of concrete fixes. Responses should go beyond 'the test didn't cover this case' to explain the fundamental reason (e.g., author-chosen orderings, vacuous passes, missing lifecycle sequences).

<!-- lore:019edfdd-e8bb-777d-b2ae-1d69ef1448e3 -->
* **Always require architecture decisions to be documented in both code comments and architecture docs**: When a significant architectural decision is made (e.g., choosing libsodium now with MLS later, encryption ordering constraints, immutable data models), the user explicitly directs that the decision, its rationale, thresholds, and upgrade/migration path MUST be recorded in two places: (1) inline code comments at the relevant implementation site, and (2) architecture documentation files. This applies to any non-obvious design choice that affects future evolution, especially decisions involving tradeoffs, sequencing constraints, or planned migration paths. Do not treat verbal agreement as sufficient — proactively note these decisions in both locations without waiting to be reminded.

<!-- lore:019edfe6-376a-7e44-86bd-a05c08575575 -->
* **Always review architectural impact before allowing work to land on tables slated for refactor**: When a major architectural refactor is planned (e.g., append-only model, schema changes), the user consistently identifies which existing tables/issues are affected and explicitly blocks or flags any work that would land on those tables before the refactor. The user expects the assistant to proactively surface subsumption relationships (e.g., 'issue X is made redundant by the refactor'), ordering constraints (e.g., 'encryption must land before pro tables'), and throwaway-work risks. When reviewing plans or open issues, always check whether proposed work touches tables or abstractions that an upcoming refactor will rip out, and call this out explicitly before proceeding.

<!-- lore:019ee01b-d6ad-7189-a78b-3321d2accb15 -->
* **Always run adversarial review before creating branch/PR or applying changes**: Before merging, branching, or applying any significant change, the user consistently requests an adversarial review first — typically split into two parallel agents: one for code-correctness/logic and one for security. The user explicitly confirms this ordering (review → branch+PR → live apply) and treats the review as a hard gate. Reviews are scoped with explicit context: repo path, branch, greenfield/backward-compat status, and a 'never return empty' directive requiring a written report. The user acts as a staff engineer or offensive security engineer during these reviews, expecting ruthless, opinionated findings rather than polite summaries.

<!-- lore:019edfb1-f331-7aea-893d-c5c9fe7a6be2 -->
* **Always run full test suite after implementing or merging changes to verify no regressions**: After implementing features, merging PRs, or restarting builds, the user consistently runs the full test suite and checks results before proceeding. All tests must pass before moving forward; test failures are blockers. Tests are written alongside implementation (not after). Test results gate correctness of DB state, schema migrations, and code logic. When reporting status, always include test counts, duration, and pass/fail summary. Current baseline: ~3480+ total tests, 145+ test files. PR #824 targeted suite: 18 gateway sync tests + 136 core sync-data tests, all passing. Always pull from latest main before making final decisions — LTM work is active on main.

<!-- lore:019ee662-6454-7d5a-9a80-3de7db31c65f -->
* **Always run full test suite, lint, and build before committing and opening a PR**: Before committing and opening a PR, the user consistently runs: (1) the full vitest test suite to confirm all tests pass, (2) a lint check (biome) and applies autofixes if needed, (3) a build step to verify all packages compile cleanly. Only after all three gates pass does the user commit, push the branch, and open the PR. If lint fails post-commit, the user fixes and re-runs targeted tests before proceeding. This sequence is non-negotiable — always perform all three checks in order before any PR creation.

<!-- lore:019ee580-1adb-79ee-92c4-18dee2e024be -->
* **Always ship diagnostic/fix work as small, focused PRs before moving to the next task**: When the user has multiple pending changes or a multi-part plan, they consistently direct the agent to isolate and ship each piece as a separate, minimal PR before starting the next one. This includes: creating a dedicated branch off origin/main (not off dirty main), excluding unrelated files (e.g., .lore.md) from the PR, and using patch-based workflows to cherry-pick only the relevant changes. The user explicitly sequences work as 'ship this first, then start the next one' rather than batching everything into one large PR.

<!-- lore:019ee52e-ab4c-7c77-bdba-a13027b093f6 -->
* **Always start with research-only inventory before any code changes**: When beginning work on a significant architectural change or new feature, the user explicitly directs a research-only phase first — no code modifications allowed. During this phase, the user requests an exhaustive, file:line-level inventory of all affected areas (schema definitions, API surfaces, read/write paths, triggers, migrations, etc.). Only after this inventory is complete does implementation begin. When the user says 'research only' or 'DO NOT change code,' treat it as a hard constraint and produce a comprehensive audit report instead of making any edits.

<!-- lore:019edfff-da14-70f2-8c7e-4db831689c8a -->
* **Always track migration file details with exact line numbers and logic summaries**: When working with database migrations, the user consistently records and references exact line numbers, function logic, constraint names, and security rationale for each migration file. They expect the assistant to read migration files fully, cite specific line ranges for key logic blocks (e.g., 'advisory lock at lines 41-42'), and maintain precise cross-references between migrations (e.g., what 0005 fixes from 0003/0004). When writing or reviewing migrations, always document: the exact lines of critical logic, the security classification of each fix (HIGH/MEDIUM/LOW), the directive/invariant the code enforces (quoted verbatim if from a comment), and downstream impacts on application code and tests.

<!-- lore:019edd39-903b-7f73-b186-daa7415b4dca -->
* **Always treat token budget as a hard cap, never a soft limit**: When implementing LTM/context packing logic, the token budget is an absolute hard cap that must never be exceeded. Applies to \`forSession()\` packing loops and any injection path. The user flags violations as HIGH-PRIORITY assertions. Comment \`// hard cap: never exceed total budget\` at ltm.ts:1070 is an intentional design assertion. Distinguish: token budget (hard cap protecting context window) vs entry count caps (control curator LLM input cost). Prefer eviction-based soft caps over hard count gates for entry management, but never relax the token budget constraint. \`forSession()\` enforces hard token budget (\`maxTokens\`) — count cap does NOT protect context window; count cap's real purpose is bounding curator LLM input cost and \`.lore.md\` size.

<!-- lore:019ee5b6-48d1-7c07-aa4b-e757fed68dfa -->
* **Always use foreground execution with timeout wrappers instead of background jobs for long-running processes**: When running long-running shell processes (e.g., Stryker, Vitest), the user consistently prefers foreground execution with an explicit \`timeout\` safety wrapper rather than backgrounding with \`&\`. Background jobs (\`&\`, \`setsid &\`) cause orphaned processes that block the shell and require painful cleanup. The adopted standard is: foreground + \`timeout \<seconds> \<command>\`, with output piped to a log file and monitored via \`tail\`. Never use \`&\` background jobs in shell tool invocations. If a process may exceed the tool timeout, increase the tool timeout rather than backgrounding the process.

<!-- lore:019ee5e7-2dfc-7631-b596-2a7afda561d8 -->
* **Always verify arithmetic and invariants with concrete hand-computed examples**: When reviewing or validating code (especially economic/cost calculations, thresholds, or guard conditions), the user expects explicit hand-computed spot-checks with real numbers, not just structural/logical assertions. They confirm formulas by substituting concrete values, trace edge cases (NaN, undefined, tie-breaking), and verify monotonicity properties. They also assert specific invariants verbatim (e.g., 'never pay to warm on a tie', 'never be reported as cheaper') and expect the assistant to confirm each one explicitly against the actual implementation. Reviews must flag gaps between documented behavior and actual code, not just describe what the code does.

<!-- lore:019ee584-b0ea-794c-8996-e6e16f6136f4 -->
* **Always verify CI is fully green before merging PRs**: Before merging any PR, the user consistently checks that all required CI checks have passed (green). This includes: test suite, integration tests (real Postgres), binary smoke tests (darwin-arm64, windows-x64), lint/typecheck, and auxiliary checks (docs links, generated docs, social meta tags, actionlint, Sentry patches, preview). The user waits for CI to complete after any branch update (e.g., rebasing onto main) before proceeding with the merge. Skipped optional jobs (nightly builds, release patches) are acceptable. Only after confirming all required checks pass does the user proceed with squash-merge.

<!-- lore:019ee577-dae1-74d8-a16a-b4394d2d18ae -->
* **Always verify existing GitHub issues before drafting new designs or recommendations**: Before drafting architecture designs, recommendations, or implementation plans, the user expects the assistant to check whether a relevant GitHub issue already exists and contains a finalized decision. If an issue exists (e.g., #823), the assistant must read its full content to confirm what was already decided, then proceed from that locked design rather than re-deriving or re-recommending. The user explicitly flags when they suspect prior work exists ('may already be implemented in #823 — asked assistant to verify before drafting'). Skipping this check and drafting redundant or conflicting designs wastes effort and risks overwriting settled decisions.

<!-- lore:019edd2b-46b0-7f6d-becf-65cf1f738d76 -->
* **Always verify implementation with targeted test runs and typecheck/lint before marking PRs complete**: Always verify implementation before marking PRs complete: (1) targeted vitest run on affected files, (2) full test suite across all packages, (3) \`tsc --noEmit\` across affected packages, (4) lint with auto-fix then clean check — zero new warnings in touched files, (5) full monorepo build. All five must pass. Check patch coverage ≥80%; add targeted tests to close gaps. Prefer real implementations with no-op LLM mocks (returning null → early-return path) over fragile ESM namespace spy patterns (\`export \* as curator\` creates non-configurable properties). For consolidation/curator branches: seed enough entries to exceed per-category threshold, mock LLM to return null, verify cooldown is set, verify second call skips.

<!-- lore:019edfbf-3888-75cd-ad59-78422c4f2d09 -->
* **Always verify invariants by pointing to specific source code locations**: When asserting a data or logic invariant, the user always backs it up by citing the exact function and file where the invariant is enforced (e.g., 'check \`create()\` in ltm.ts — does \`project\_id IS NULL\` force \`cross\_project = 1\`?'). When reviewing code, proactively trace invariants back to their enforcement point in source. If an invariant is claimed but no enforcement site is cited or found, flag it as unverified. Always reference the specific function, file, and mechanism (e.g., a guard clause, INSERT logic, or migration) that upholds the invariant.

<!-- lore:019ee583-5a76-7aef-bbf2-78cdef203cfb -->
* **Always verify PR dependencies and merge order before proceeding with new work**: Before starting new implementation work or merging PRs, the user consistently checks the full PR landscape to identify stacked/dependent PRs, migration version collisions, and correct merge sequencing. They expect the assistant to: (1) identify which PRs must merge first and why (e.g., migration number conflicts), (2) flag semantic risks that GitHub's 'CLEAN' status misses, (3) update branches onto main and re-run CI before merging when overlap exists, and (4) note retargeting steps for stacked PRs after their base merges. This applies whenever multiple open PRs touch related files or share epic scope.

<!-- lore:019ee536-633e-7338-99cb-2c68ba550ff3 -->
* **Always verify PR/branch status before merging or proceeding to next task**: Before merging a PR or moving to the next work item, the user expects an explicit check of the current PR landscape — including open PRs, recently merged PRs, and branch states. This applies especially when resuming work across sessions or when multiple PRs are in flight. The check should confirm: which PRs are open vs merged, whether prerequisite PRs have landed, and whether the target branch is clean. Report findings before taking action, and flag any blockers (e.g., a dependency PR still open) rather than proceeding blindly.

<!-- lore:019ee640-0ad2-7a98-87f8-111898a4a84d -->
* **ast-grep: always use stopBy: end for relational rules**: Always use \`stopBy: end\` for relational rules (\`inside\`, \`has\`) in ast-grep to ensure complete traversal. Stated as a firm rule unless there is a specific reason not to. ast-grep skill at \`/home/byk/.claude/skills/ast-grep\`. Key principles: start simple (pattern → kind → relational → composite); use \`--debug-query=cst\` to inspect AST; escape metavariables in inline rules with \`\\$VAR\`.

<!-- lore:019edfdb-a052-7181-adb5-295136020fdb -->
* **Follow the established git workflow (branch, PR, review)**: Behavioral pattern detected across 22 sessions (action: enforced-workflow). The user consistently demonstrates this behavior.

<!-- lore:019edcd1-12e0-7981-aec5-0c11b3ba5154 -->
* **Never fall back to cfg.model for worker model in LLM calls**: When passing a model to LLM calls in curator/consolidation code, always use the explicit worker model from input (e.g., \`input.model\` or \`input.workerModel\`). Never fall back to \`cfg.model\` — it's the project/session model and may belong to a different provider than the worker's upstream URL, causing cross-provider model name mismatches (404 errors). Code comment at curator.ts:1301-1302 documents this as an invariant. \`runConsolidationPrompt\` enforces this: explicit worker model passed, never substituted. If model is undefined, let the gateway's cross-provider guard handle validation or skip — do not silently substitute \`cfg.model\`.

<!-- lore:019ed058-ae55-7a9f-b6ce-c79717e686af -->
* **Never use Supabase for temporal/high-volume data — treat it as row-level sync only for small structured data**: Hard rule: Supabase (PostgREST row-level) is only appropriate for small structured data AND linked temporal rows (distillation-referenced subset). NEVER use for arbitrary temporal bulk or GB-scale data. Blob snapshots rejected for multi-client: divergent WAL chains, no row-level merge path. Turso rejected for basic/pro tiers: whole-DB replication only, no table/row-level selective sync. Turso deferred to hosted-gateway decision only. Framing Supabase as a live query store for unreferenced temporal data is 'the GB-scale Option-C rewrite trap' and will be immediately rejected. SQLite WAL must NEVER run on network FS (EFS/NFS) — local volume only.

<!-- lore:019ee565-89ae-7a43-925f-f341a156aa2d -->
* **Option A (single append-only table) preferred over Option B (CQRS/dual-table) for knowledge schema**: User explicitly rejected Option B (CQRS/dual-table with separate history table) in favor of Option A (single append-only \`knowledge\` table with \`is\_current\`/\`logical\_id+version\`). Rationale: both options have identical team-conflict capability; A is long-term simpler (one table, no materialization layer to drift). User is willing to pay the one-time ~60 read-site rewrite + FTS rebuild cost upfront due to small user base. Never re-propose Option B as a 'simpler migration path' — it was explicitly considered and rejected.

<!-- lore:019ee565-88e7-7710-ac28-d06201206e25 -->
* **Prefer architectural simplicity over short-term implementation convenience**: When presented with two architectural options, the user consistently chooses the simpler long-term design even when it requires more upfront rewrite cost. The user explicitly rejects approaches that add layers (dual tables, separate tombstone tables, CQRS materialization) in favor of unified models (single append-only table, is\_deleted as data, logical\_id as stable identity). The user accepts one-time migration pain and is willing to rewrite ~60 read sites rather than carry permanent architectural complexity. When the assistant recommends a more complex option for short-term convenience, the user pushes back and the assistant should reverse to the simpler design. Always lead with the architecturally cleanest solution.

<!-- lore:019ee635-ed31-7d27-8e27-d112f7b5a5f9 -->
* **Prefer shared/unified models over duplicated heuristics for economic decisions**: When two subsystems (e.g., cache warmer and compaction logic) independently estimate the same quantity or make related economic decisions, the user consistently pushes toward a single shared model/snapshot rather than letting each side use its own heuristic. This applies to token counts, cost estimates, and strategy signals. The user expects: (1) both sides read from the same stored snapshot, (2) estimation errors cancel rather than compound, (3) the shared model is the authoritative source. When proposing designs, always identify whether two components are modeling the same thing with divergent inputs, and consolidate into one canonical source stored on session state.

<!-- lore:019edcd2-7310-7e4a-bf36-16c5485f3c9d -->
* **Reject arbitrary magic numbers in favor of principled, adaptive configuration**: When reviewing configuration constants or thresholds, the user consistently questions values chosen arbitrarily and pushes for principled alternatives. They prefer: (1) values derived from actual system constraints (e.g., token budgets, LLM input costs), (2) proportional/relative thresholds (e.g., per-category limit as a fraction of \`maxEntries\`), or (3) quality-based adaptive mechanisms (e.g., eviction by confidence/recency) over hard caps. When a hard cap is proposed, the user asks what it actually protects and whether a different mechanism better addresses the real concern. \`maxEntries=40\` was explicitly called 'a number I pulled out of thin air' — raised to 100 with proportional per-category threshold. Always justify numeric constants with a concrete rationale tied to system behavior.

<!-- lore:019ee563-3e05-7f28-b157-2018dbee8bad -->
* **Respect explicitly rejected approaches**: Behavioral pattern detected across 13 sessions (action: rejected-approach). The user consistently demonstrates this behavior.

<!-- lore:019ee01c-baf6-7094-809a-3fdd4148e4b9 -->
* **Review code before committing**: Behavioral pattern detected across 18 sessions (action: requested-review). The user consistently demonstrates this behavior.

<!-- lore:019ebe56-3cfa-7b49-b786-3d603e608a37 -->
* **Test file conventions: Vitest, kebab-case, import style**: Always write new tests with \`import { describe, it, expect } from 'vitest'\`. Use kebab-case file naming for test files. Prefer writing more tests over lowering coverage thresholds or excluding files. Project migrated from Mocha+Chai+ts-node to Vitest (May 2026; 312ms vs 30s startup). Run tests from repo root, not package subdirectory — \`vitest\` with a filter from a subdirectory returns 'No test files found'.
