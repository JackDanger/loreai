<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019e313c-367e-7e2d-9d21-670fac772f45 -->
* **Claude Code skills system: structure and auto-discovery**: Skills live under \`~/.claude/skills/\<skill-name>/\` (Claude Code) or \`~/.config/opencode/skills/\` (OpenCode global), or \`.opencode/skills/\`/\`.claude/skills/\`/\`.agents/skills/\` project-locally. Each skill is a directory with a \`SKILL.md\` (YAML frontmatter + markdown body) and optional \`references/\` subdirectory. Frontmatter fields: \`name\` (required), \`description\` (required), \`version\` (optional), \`requires.bins\`, \`requires.auth\`. Auto-discovered by convention — no registration needed. Community jj skills: \[HotThoughts/jj-skills]\(https://github.com/HotThoughts/jj-skills) (9★, 3 skills: jj-workflow/jj-create-pr/jj-update-pr), \[mtaran/jj-guide]\(https://github.com/mtaran/jj-guide) (most comprehensive, progressive disclosure via references/), \[antstanley/jj-workspace-skill]\(https://github.com/antstanley/jj-workspace-skill) (workspace/worktree interception), \[RealAdarsh/jj-skill]\(https://github.com/RealAdarsh/jj-skill) (cross-agent: Claude/Codex/Gemini).

<!-- lore:019e6904-e0e1-7689-bb52-7e8a0cb0edda -->
* **Gateway recall: Marker-and-Expand strategy — tool injection, interception, and round-trip**: Recall tool injected by gateway pipeline (pipeline.ts), NOT OpenCode plugin. Injection condition: \`modifiedReq.tools.length > 0 && !clientHasRecallTool(tools)\`. Strategy: (1) Response: recall \`tool\_use\` replaced with marker text via \`replaceRecallWithMarker()\`; result stored in \`sessionState.recallStore\`. (2) Next request: \`expandRecallMarkers()\` restores \`tool\_use\`+\`tool\_result\` pairs before forwarding. \`buildRecallFollowUp()\`: assistant message = ORIGINAL \`tool\_use\` block; user message = \`tool\_result\` block with recall results (NOT plain text) — prevents LLM echoing raw recall formatting during streaming. \`hasContinuationAfter\` logic: if blocks follow marker, truncates/splices; else prepends tool\_result to next user msg. \`RecallAwareAccumulator\` suppresses recall SSE blocks. MAX\_RECALL\_DEPTH=10. Follow-up requests: \`cacheConversation: false\`.

<!-- lore:019e7013-a9ea-72e6-8e8c-17ef1848e4e0 -->
* **llm-adapter.ts: retry logic, backoff schedule, and circuit breaker integration**: llm-adapter.ts retry/backoff: \`TRANSIENT\_CODES={429,500,502,503,529}\`; \`AUTH\_ERROR\_CODES={401,403}\`. \`MAX\_RETRIES\_RATE\_LIMIT=3\`; \`MAX\_RETRIES\_SERVER=3\`; \`MAX\_RETRIES\_URGENT=2\`. \`backoffMs()\`: Retry-After present → \`min(retryAfterMs, cap)\`; urgent (no header) → \`min(1000×2^attempt, 4000)\`; 429 non-urgent → \`min(60\_000+attempt×60\_000, 180\_000)\`; 5xx → \`min(1000×2^attempt, 8000)\`. On 429 non-urgent → \`tripCircuitBreaker(pauseSec)\` before retry. Auth errors → \`markAuthStale\`, re-resolve, retry once if \`credentialChanged && attempt===0\`. \`buildAnthropicWorkerRequest()\`: injects billing block only when \`cred.scheme === 'bearer'\`; system sent as block array with \`cache\_control: { type: 'ephemeral', ttl: '1h' }\`; body signed via \`signBody()\`. \`opts.thinking\` intentionally NOT forwarded in bare API fetch. \`activeWorkerCalls=Set\<string>\` tracks worker session IDs.

<!-- lore:019e54f5-04be-7a9f-9e62-177d65d5db9f -->
* **LTM forSession() budget, phases, and safety-net constants**: LTM forSession() architecture: \`forSession()\` (ltm.ts) budgets: \`budget.ltm\`=0.05, \`budget.preferenceLtm\`=0.02. Phases: (1) architecture entries up to ARCH\_BUDGET\_FRACTION=0.2; (2) remaining by score desc; (3) .lore.md sections. HEADER\_OVERHEAD\_TOKENS=15 subtracted upfront. Hard cap: \`if (used+cost>maxTokens) continue\`. PROJECT\_SAFETY\_NET=5: top-confidence project entries ALWAYS included even with zero relevance score. NO\_CONTEXT\_FALLBACK\_CAP=10. Global entries (pid=null) must be cross\_project=1. Pool SQL: project pool uses \`cross\_project=0\`; cross pool uses \`project\_id IS NULL OR cross\_project=1\`. Promoted entries (cross\_project=1) lose PROJECT\_SAFETY\_NET guarantee in home project. GOTCHA: \`vectorSearch()\` queries \`knowledge WHERE embedding IS NOT NULL AND confidence>0.2\` — test suites must \`DELETE FROM knowledge WHERE embedding IS NOT NULL\` in beforeEach. Dedup threshold for Nomic v1.5 is 0.935.

<!-- lore:1cdad2a4-5637-471b-a5cb-39c919fb1592 -->
* **Multilingual support: FTS tokenizer, Unicode tokenization, and non-English fallbacks**: Lore supports non-English (e.g. Turkish) conversations. Key design: (1) FTS5 uses \`tokenize='unicode61 remove\_diacritics 0'\` (NOT \`porter\` — English-only stemmer; NOT diacritic-folding — Turkish ç/ğ/ı/ö/ş/ü are distinct letters). Migration v32 rebuilds all 6 FTS tables. (2) \`filterTerms()\`/\`extractTopTerms()\` in \`search.ts\` strip via \`/\[^\p{L}\p{N}\_\s]/gu\` (Unicode-aware) so Turkish words don't split at special letters. Underscore kept for snake\_case. (3) \`pattern-extract.ts\` regexes need NO translation — they run on the LLM observer's \*English-normalized\* output. (4) Two raw-user-text regex sites have a structural fallback: \`hasNonAsciiLetters()\` (exported from \`instruction-detect.ts\`) gates fallbacks in \`extractInstructionCandidates\` (emit whole message as candidate) and \`detectAssertions\` (pin first sentence) — requires ≥3 non-ASCII letters to avoid false positives on English loanwords (café, naïve). (5) \`ltm.rerankPreferences()\` no longer demotes entries lacking English directive words to 0.8. KNOWN LIMITATIONS (deferred): English \`STOPWORDS\` not filtered for other languages (vector+RRF compensates); ~3-4 chars/token estimate is English-tuned (fine for Turkish, off for CJK).

<!-- lore:019e16fc-4422-7087-881e-0a690c28482a -->
* **opencode collapsible question dock lives on origin/collapse-questions, not yet merged to dev**: The collapsible question modal/dock feature is on \`origin/collapse-questions\` branch and has NOT been merged to \`origin/dev\`. The minimized-prompts feature is separate. When rebasing the cumulative branch, these features must be re-applied manually — they won't come in automatically from upstream dev.

<!-- lore:019e16fc-4410-7424-8a8a-712db1934a44 -->
* **opencode cumulative branch tracks upstream dev with custom patches**: Maintains a cumulative branch rebasing on \`origin/dev\`. Key custom patches: (1) plan mode markdown display with collapsible modal in \`plan\_exit\` tool, (2) collapsible question modal/dock (origin/collapse-questions, not yet merged), (3) unarchive stickiness fix, (4) plan\_exit/plan\_enter deny rules for subagents in task.ts tools map. Upstream renamed \`session/index.ts\` → \`session/session.ts\` — apply touch fix to \`session/session.ts\` instead. Upstream also removed \`cleanupSessionCaches\` call from \`session.updated\` archived branch (only \`session.deleted\` usage remains). During rebase, task.ts will conflict — resolve by keeping upstream's \`deriveSubagentSessionPermission\` and adding plan\_exit/plan\_enter inline permission checks in the tools map region. Track patches by commit hash.

<!-- lore:019e313c-3687-7429-9c86-858af28899db -->
* **OpenCode vs Claude Code configuration separation**: OpenCode config lives at \`~/.config/opencode/opencode.json\` and configures MCP servers and plugins — it does NOT reference the skills directory. Claude Code config lives at \`~/.claude/settings.json\` and controls permissions, theme, model. The skills system (\`~/.claude/skills/\`) is a Claude Code concept; OpenCode uses MCP servers instead. These two tooling layers are independent.

<!-- lore:019e6966-0250-70dd-9626-c07d04bd353f -->
* **Sentry CLI: Bun → Node.js migration in progress**: MIGRATION-PLAN.md tracks ongoing Bun→Node.js migration (status: In Progress). Completed: pnpm replaces bun as package manager; SQLite adapter is runtime-detecting (bun:sqlite under Bun, node:sqlite under Node.js); Vitest replaces bun:test. Remaining: Phase 2 Groups A (Bun.file/Bun.write → fs promises), B (Bun.spawn → child\_process, Bun.which, Bun.sleep), C (Bun.Glob → tinyglobby, Bun.randomUUIDv7 → uuidv7, Bun.semver → semver, Bun.zstd → zlib), E (bspatch.ts/upgrade.ts). Phase 4: update GitHub Actions to pnpm+Node.js, replace \`bunx\` with \`pnpm exec\`, \`bun run\` → \`pnpm run\`.

<!-- lore:019e6966-0241-79aa-b212-07e8e3ffe075 -->
* **Sentry CLI: execution flow and command structure**: Entry: bin.ts → cli.ts \`startCli()\`. Fast-path: \`\_\_complete\` arg → \`runCompletion()\` (skips heavy imports, never crashes). Normal path: \`preloadProjectContext()\` → \`hoistGlobalFlags()\` (moves --verbose/--json to end) → middleware chain (outermost: \`autoAuthMiddleware\` → \`rcImportMiddleware\` → \`seerTrialMiddleware\`) → \`runCommand()\` with \`withTelemetry()\` → Stricli \`run(app, argv, context)\`. Commands are async generators: \`yield new CommandOutput(data)\`. \`buildCommand\` wrapper auto-injects --json/--fields, --verbose, telemetry spans, dual output modes. 22 command domains including issue/, trace/, release/, sourcemap/, dashboard/.

<!-- lore:019e6966-023c-7a8d-a655-60aac13a36f1 -->
* **Sentry CLI: project overview and tech stack**: Sentry CLI (\`sentry\` npm package, v0.35.0-dev.0) at /home/byk/Code/getsentry/cli. TypeScript CLI replacing the older Rust-based sentry-cli. Runtime: Node.js ≥22.15 (uses experimental \`node:sqlite\`). Stack: Stricli (Bloomberg CLI framework), esbuild bundler, Node SEA binaries via fossilize (5 targets), SQLite via node:sqlite, Vitest+fast-check testing, Zod, React+Ink TUI, @clack/prompts, OAuth RFC 8628, tsx dev runner, Astro+Starlight docs, pnpm 10.11.0. License: FSL-1.1-Apache-2.0. Critical rule: ALL packages in devDependencies — nothing in dependencies; everything bundled at build time; CI enforces via \`pnpm run check:deps\`.

<!-- lore:019e99ac-28eb-7a7f-a362-37e2a18eaec5 -->
* **Session auth cross-contamination: per-provider credential and upstream snapshot fix**: Session auth cross-contamination fix: \`SessionState.lastUpstream\` and \`sessionAuth\` are singular — overwritten when user switches providers mid-conversation. Fix: (1) \`upstreamByProvider: Map\<string, UpstreamSnapshot>\` added to \`SessionState\`; \`lastUpstream\` kept as convenience pointer. (2) \`sessionAuth\` → \`Map\<sessionID, Map\<providerID, AuthCredential>>\`; \`setSessionAuth\`/\`resolveAuth\` take optional \`providerID\`; \`\_default\` slot synced on every write. (3) Session-aware LLM wrapper looks up \`state.upstreamByProvider.get(workerProviderID)\`, falls back to \`state.lastUpstream\`. KNOWN LIMITATIONS: Provider ID namespace mismatch — \`upstreamByProvider\` keys from \`X-Lore-Provider\` header vs \`WORKER\_DEFAULTS\` providerID; MiniMax-only sessions fall back to \`lastUpstream\`. \`getWorkerAuth\` type annotation at pipeline.ts:898 is stale — typed as \`(sessionID?)\` but callers pass \`(sessionID?, providerID?)\`.

<!-- lore:019e698a-a227-768b-9a29-b545f598725d -->
* **Spotlight: architecture overview and tech stack**: Spotlight ('Sentry for Development') at getsentry/spotlight — local dev observability tool v4.11.3. Sentry SDKs send telemetry to local Sidecar HTTP proxy (default port 8969); Sidecar parses envelopes, buffers in circular \`MessageBuffer\` (500 items), streams via SSE/MCP/formatters. Delivery: CLI (\`npx @spotlightjs/spotlight\`), Electron desktop (macOS), Docker. Stack: TypeScript strict, Node≥20, React 18, Hono+@hono/node-server, React Router v6, Zustand v5 (9 slices), Tailwind v4, Vite (5 configs: base/node/ui/dev/electron), Biome 1.9 (lint+format), pnpm 9.15.9, Turborepo v2, Vitest+Playwright, Electron v39, MCP via @modelcontextprotocol/sdk+@hono/mcp, Zod v4 (server)/v3 (MCP schemas), Fossilize (SEA builds). Monorepo: packages/spotlight + packages/website.

<!-- lore:019e5922-4280-76aa-9b79-5ebeae433d7a -->
* **Sub-agent session detection, isolation, and differential treatment**: Sub-agent session detection, isolation, and differential treatment: Sub-agent sessions detected via \`x-parent-session-id\` header (pipeline.ts:2773-2809). Each sub-agent gets independent session: temporal storage, gradient state, distillation pipeline, LTM injection, cost tracking. Key differential treatment: (1) cache warming ALWAYS skipped — \`shouldWarm()\` returns false unconditionally (cache-warmer.ts:731-733); (2) \`findRotationPredecessor()\` (session.ts:505) skips sub-agents from Tier 1b rotation; (3) idle distillation/curation runs normally; (4) requests with \`x-parent-session-id\` carry their own \`x-session-affinity\` nanoid — full Lore pipeline, no parent corruption. \`isSubagent\`/\`parentSessionId\` persisted to DB (migration v26). Dashboard: collapsible cost-rollup tree via \`loadParentChildMap()\` + \`buildLiveSessionRows()\`; \`rollUp()\` propagates grandchildren costs bottom-up. \`isCacheWarm()\` (pipeline.ts:3231–3253): returns false if no \`warmup.lastWarmupAt\`; \`forceKeepWarm\` uses 2× TTL safety margin. \*\*OpenCode plugin gap\*\*: \`chat.headers\` hook injects \`x-lore-session-id\` and \`x-lore-agent\` but does NOT set \`x-parent-session-id\` — gateway can't distinguish parent from sub-agent.

<!-- lore:019e4bbd-ade4-7355-8fb1-87df54c8ea69 -->
* **X-Lore-\* headers: project path injection and resolution priority**: X-Lore-\* headers and project path resolution: Priority in \`packages/gateway/src/config.ts\`: 1) \`X-Lore-Project\` header (absolute path, max 1024 chars, strips trailing slashes/control chars), 2) inferred from system prompt (\`cwd\` JSON key, \`Working directory:\` prefix, \`.lore.md\` path, generic \`/home/\`/\`/Users/\` paths), 3) \`process.cwd()\` fallback. Source tagged as \`ProjectPathSource\`: \`"header"|"inferred"|"cwd"\`. Pipeline warns on \`cwd\` source (dedup via \`cwdWarned\` Set). \`agents.ts\` \`envVars()\` injects \`X-Lore-Project\` + \`X-Lore-Git-Remote\` into \`ANTHROPIC\_CUSTOM\_HEADERS\`. \*\*Invariants (HIGH-PRIORITY, user directive)\*\*: (a) Confident path (source \`header\` or \`inferred\`) ALWAYS binds & clears provisional binding — overrides any prior state. (b) \`cwd\` NEVER overwrites a confident binding (pipeline.ts:1067-1070). (c) Side-channel sessions that never send the header NEVER self-recover — require bucketing/self-heal. (d) Gateway cwd must never appear as a project (test invariant: \`expect(projects.some(p=>p.path===process.cwd())).toBe(false)\`).

### Decision

<!-- lore:019e1740-43b4-793a-a0d8-c08aa8b0394e -->
* **Chose \`node:worker\_threads\` over Bun-native \`Worker\` API**: \`@loreai/core\` runs under both Bun and Node. All required APIs confirmed working in Bun 1.3.13: \`Worker\`, \`parentPort\`, \`workerData\`, \`Float32Array\` structured clone, \`worker.terminate()\`, \`worker.unref()\`. Key implementation notes: load worker via \`new URL('./worker.ts', import.meta.url)\` (Bun runs TS natively); use \`Float32Array\` structured clone instead of manual \`ArrayBuffer\` transfer (simpler, works identically); no \`'ready'\` handshake needed — Bun auto-enqueues messages before worker starts; \`worker.unref()\` for clean process exit.

<!-- lore:019ea8ae-cdec-73ed-81f2-60e260a125ed -->
* **Decided to use margin on individual elements for frame borders**: Use margin on individual sidebar elements rather than padding on the container for Beeper CSS frame borders.

### Gotcha

<!-- lore:019e9938-e95a-7c60-934d-811f4a80ac50 -->
* **Beeper CSS: \`.left \*\` color rule makes unread badges invisible — exclude badge elements**: Beeper CSS gotchas: (1) \`color: #1d1c1d !important\` on \`.left, .left \*\` makes unread badges invisible — scope with \`:not()\` exclusions for badge/settings elements. (2) \`overflow: hidden\` on \`.panes\` swallows vertical scroll on the conversation list — never set it on \`.panes\`. (3) Adding \`padding\` to \`.home\` shrinks children and clips sidebar controls — compensate with \`flex-shrink: 0; min-width: 280px\` on \`.left\` and use \`box-sizing: border-box\`. (4) \`overflow: hidden\` on \`.left\` clips conversation list items on the right (timestamps truncated) — use \`overflow: clip\` with matching border-radius. (5) \`margin\` on \`.left\` for frame border gets absorbed by flex layout — prefer \`padding\` on \`.home\` outer container instead. (6) \`.left\` flush against window edge has no left border showing — add padding to \`.home\` to create the dark blue frame. (7) Avatar cropping in \`.left-pane-header\` when padding pushes content — adjust header height or use \`overflow: visible\` carefully.

<!-- lore:019e98db-f36d-7469-960e-aa97432f6fbb -->
* **Beeper custom CSS: syntax error causes 0 rules parsed — always lint before pasting**: Trap: a CSS syntax error (e.g. a rule block accidentally nested inside a \`@media\` block) causes Beeper to silently parse 0 rules from the custom-css \`\<style>\` element — no error shown, marker loads fine, but nothing applies. Looks like a specificity issue. Fix: run \`npx stylelint beeper-orchid.css\` from \`/home/byk/Code/\` before pasting (config at \`/home/byk/Code/.stylelintrc.json\`). Verify with \`Array.from(document.styleSheets).map(s => s.ownerNode?.id + '/' + s.cssRules.length)\` — custom-css sheet should show ~51 rules, not 0. Common cause: inserting a top-level rule block inside an unclosed \`@media (prefers-color-scheme: dark)\` block. Use \`npx\` — do not install stylelint globally.

<!-- lore:019e9821-4426-7423-a568-231456b4b3a1 -->
* **Beeper v4 custom CSS: real class names differ from nocturne theme examples**: Beeper v4 DOM class names and overflow traps: \`.left\` (sidebar body), \`.left-pane-header\` (52px, has \`-webkit-app-region: drag\` by default, needs \`no-drag !important\`), \`.filters-pane-list\`, \`.threads\`, \`.threads-list-container\`, \`.tab-bar-container\`/\`.tab-bar\` (lives INSIDE \`.left-pane-header\`), \`.sidebar-thread.isSelected\`, \`.bp\_MainPanel\`/\`.right-pane\`, \`.panes\` (wraps sidebar+main). NEVER apply sidebar styles to \`.panes\`. \`body.theme-monochrome\` overrides — scope with \`body.theme-monochrome .left\`. \`.left-pane-header\` may be a SIBLING of \`.left\` in DOM, not a child. \`contain: paint\` on \`.sidebar-thread\` BREAKS virtualized scroll — never add it. \`.panes\` \`overflow: hidden\` swallows vertical scroll. \`overflow: hidden\` on \`.left\` clips conversation list right side (timestamps). \*\*VIRTUAL LIST SCROLL FIX (v57)\*\*: removing \`overflow: hidden\` from \`.virtual-list\`, \`.threads\`, and \`.threads-list-container\` is REQUIRED — the virtualized list MUST be scrollable; setting it kills scroll entirely. \*\*Frame border approach\*\*: \`padding\` on \`.home\` (with \`box-sizing: border-box\`) is MORE reliable than \`margin\` on \`.left\` — margin gets absorbed by flex layout. Pair with \`flex-shrink: 0; min-wi \[truncated — entry too long]

<!-- lore:019e6a44-dbf1-7df6-ba60-94f75c21b81d -->
* **consolidationUser() batchTarget frame-of-reference mismatch: global target vs batch count produces negative excess**: Trap: \`batchTarget\` in batched consolidation was computed globally (\`Math.max(cfg.curator.maxEntries, entries.length - CONSOLIDATION\_BATCH\_SIZE/2)\` = 1118 for 1143 entries) but \`consolidationUser()\` computed \`excess = count - targetMax\` where \`count\` is the batch size (50). Result: \`excess = 50 - 1118 = -1068\` — prompt tells LLM 'must remove at least -1068 entries', LLM returns empty ops, idle scheduler enters 1h cooldown, system never converges. Fix: \`batchTarget\` must be batch-relative — \`Math.ceil(CONSOLIDATION\_BATCH\_SIZE / 2)\` so \`excess = 50 - 25 = 25\`. Fixed in e8a73f9. CONSOLIDATION\_BATCH\_SIZE=50 (curator.ts:643). Non-batched path (entries ≤ BATCH\_SIZE) correctly uses \`cfg.curator.maxEntries\` as target. Note: \`consolidate()\` NEVER creates entries — only 'update' and 'delete' ops applied; \`includeCross=false\` excludes cross-project entries.

<!-- lore:019e6a1b-3408-7609-ba6b-d4ba9d552afd -->
* **curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields**: curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields: Trap: \`filterOps()\` (curator.ts:109–117) only checks \`op\` is a string — does NOT validate required fields (\`content\`, \`title\`, \`category\`) for \`create\` ops, casting result as \`CuratorOp\[]\` silencing TypeScript. When LLM emits a malformed \`create\` op missing \`content\`, \`applyOps()\` line 226 accesses \`op.content.length\` without null guard → \`TypeError: undefined is not an object\`. Fix: add null guard at line 226 AND field validation in \`filterOps()\` for \`create\` ops. The \`update\` branch at line 252 already has \`op.content !== undefined &&\` guard — \`create\` branch lacks equivalent. \`applyOps()\` calls \`ltm.syncRefs(id)\` and \`entities.syncEntityRefs(id, content)\` after each create/update; \`skipCreate\` flag skips creates (used by consolidation); cross-project guard prevents mutating foreign-project entries. \`cfg\` is in scope in \`runInner\` at line 414.

<!-- lore:019e69f0-a05a-7d48-8e35-76baf13533c5 -->
* **LOREAI-GATEWAY-1E: resp.usage undefined crash in postResponse() for vLLM/OpenAI path**: Trap: \`GatewayUsage\` typed as non-optional on \`GatewayResponse\`, so \`resp.usage.inputTokens\` looks safe. But \`resp.usage\` can be \`undefined\` at runtime (e.g., vLLM returning partial response). All four accumulators construct \`usage\` with \`?? 0\` guards on individual fields — but \`resp.usage\` itself can still be undefined if an exception produces a partial response object. Crash sites: pipeline.ts lines 2069, 2101, 2137-2140, 2150-2153, 2204-2206, 2282-2285, 2341, 2363, 2402; also \`buildOpenAINonStreamResponse\` (openai.ts:296-300). Fix: add \`const usage = resp.usage ?? { inputTokens: 0, outputTokens: 0, cacheReadInputTokens: 0, cacheCreationInputTokens: 0 }\` guard at top of \`postResponse()\` and \`nonStreamHttpResponse()\`, then use \`usage.\*\` throughout.

<!-- lore:019e56fb-b446-759b-aff2-035a83700124 -->
* **LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm — resolveAuth returns same stale token**: LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm. \`resolveAuth(sessionID)\` marks session stale and falls back to \`getLastSeenAuth()\` — same expired token. \`credentialChanged=false\` so retry-once path never taken. Idle scheduler fires 4+ LLM calls/tick (distill, meta-distill, curation, consolidation), each 401ing independently. \`isAuthStale()\` was exported but never called in non-test source. Fix (PR #548): (1) \`isAuthStale(sessionID) && !resolveAuth(sessionID)\` guard in \`scheduleBackgroundWork()\` (pipeline.ts:2785) and \`idle.ts:144\`; (2) \`resolveAuth()\` returns null when global fallback matches stale credential; (3) add \`/Worker upstream auth error/\` to \`TRANSIENT\_ERROR\_PATTERNS\` in instrument.ts. \`resolveAuth()\` is pure read-only (no side effects). Double-call pattern (guard + quota check) is intentional and race-free (JS single-threaded, no awaits between calls).

<!-- lore:019e8d03-3fdf-76cf-b87d-eab142cad8b1 -->
* **OpenAI protocol: split tool messages + recall leak on streaming path**: Two OpenAI-protocol bugs. (1)  (translate/openai.ts) pushed separate user messages per  msg — breaks  Pass 2 which only checks . Fix: coalesce consecutive tool messages into one user message. (2) OpenAI/openai-responses streaming dispatch (pipeline.ts) accumulated upstream SSE then returned early, skipping the recall loop. Fix:  closure called from all three accumulated-response paths. Also:  (recall.ts) must set  — follow-ups parsed as JSON via ; without override, streaming clients forward  causing SSE→JSON parse crash.

<!-- lore:019e4eab-59b4-7fe3-be03-874f682cc340 -->
* **openai.ts: array-format content in system/developer messages silently dropped**: openai.ts / openai-responses.ts parsing traps: (1) \`parseOpenAIRequest\` coerces non-string content to \`""\` — FIXED in PR #526: array-format system/developer content now extracts text parts. (2) \`buildOpenAIMessages\` (openai.ts:523–529): \`tool\_result\` blocks pushed mid-loop before \`textParts\`/\`toolUses\` flushed — tool\_result appears before text/tool\_use of same message. (3) \`buildOpenAIMessages\` always re-emits system as \`role:'system'\`, never \`'developer'\`. (4) \`parseOpenAIResponsesRequest\` reads system prompt from top-level \`instructions\` field. (5) \`item\_reference\` and \`reasoning\` items in \`parseInputItems\` silently dropped. \`store\` field not in extras allow-list — silently dropped. LTM injection for Responses API: \`\[req.system, cache?.stableLtmSystem, cache?.ltmSystem].filter(Boolean).join('\n\n')\`. \`buildOpenAIMessages\`: uses array content form only when \`hasOpaque=true\` (multimodal), plain string otherwise (cache stability). Consecutive role:"tool" messages coalesced into one user message.

<!-- lore:019e170b-d4bb-7c55-9b59-afe5c1003961 -->
* **opencode build: first run installs @opentui packages mid-build, second run succeeds**: The opencode build (\`bun run build\`) may fail on first run with a resolver error for \`@opentui/solid\` exports (e.g. \`./runtime-plugin-support/configure\`). This happens because \`bun add\` for \`@opentui/\*\` packages runs mid-build. Re-running the build immediately after the first attempt succeeds — no code change needed. The files exist on disk after the first run installs them.

<!-- lore:019e6950-8200-7473-87a5-45a861bb84af -->
* **recall.ts: unescaped quotes/ellipsis in marker strings cause store miss and raw marker leak**: Trap: \`buildRecallMarker()\` embeds raw \`query\` between double-quotes and raw \`id\` before \`…\` — looks safe because queries are usually plain text. But if \`query\` contains \`"\`, \`MARKER\_REGEX\` (\`/📚 Searching (.+?) for "(.+?)"…/\`) truncates the captured group at the first inner quote; \`recallStoreKey()\` produces a mismatched key; \`store.get(key)\` returns undefined; the marker is left as raw text in the conversation visible to the upstream LLM. Same issue for \`id\` containing \`…\`. Fix: escape \`"\` → \`\\"\` in \`buildRecallMarker()\` and unescape in \`parseRecallMarker()\`; for IDs, percent-encode or strip \`…\` before embedding.

<!-- lore:019ea87a-0194-7bf4-b797-6c463373c8f2 -->
* **Recurring plan\_exit failure: other:the-user-dismissed-this**: The \`plan\_exit\` tool repeatedly failed with "other:the-user-dismissed-this" across 6 sessions (10 total failures). Sample error: The user dismissed this question. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ea87a-019c-7cc8-b717-b83c967992a3 -->
* **Recurring read failure: not\_found**: The \`read\` tool repeatedly failed with "not\_found" across 5 sessions (5 total failures). Sample error: File not found: /tmp/brand-mark.svg. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ea8ac-324f-7d55-838c-a31940d83ceb -->
* **Recurring webfetch failure: other:statuscode-non-xx-status**: The \`webfetch\` tool repeatedly failed with "other:statuscode-non-xx-status" across 5 sessions (18 total failures). Sample error: StatusCode: non 2xx status code (500 GET https://www.thefastcode.com/en-idr/article/how-to-customize-slack-s-appearance-with-themes). Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019e6966-024c-7dc7-81b0-c6a21b9a44ae -->
* **Sentry CLI: never merge PR with failing CI — build jobs catch require resolution bugs unit tests miss**: Trap: unit tests pass but the SEA binary or CJS bundle fails at runtime due to unresolved \`require()\` paths — these are only caught by the build CI jobs, not by \`vitest\`. Fix: always wait for ALL CI jobs to pass (including build jobs) before merging. Never merge a PR with failing CI. Build jobs are the safety net for esbuild bundling issues that unit tests cannot detect.

<!-- lore:019e2e20-95b3-7a9d-ab38-77d87eafecc4 -->
* **splitSegments() infinite recursion on oversized single messages**: \`splitSegments()\` infinite recursion on oversized single messages in \`packages/core/src/distillation.ts\`: recurses infinitely when a single message exceeds \`maxSegmentTokens\` (16384). \`findSplitIndex()\` returns \`messages.length\` (=1), so \`left = messages.slice(0, 1)\` produces an identical recursive call. Triggered on large tool outputs (~49KB+). Fix: add base case after the \`totalTokens <= maxTokens\` guard — \`if (messages.length <= 1) return \[messages]\`. The oversized message becomes an indivisible segment.

<!-- lore:019e698a-a22c-7f90-8990-83dcb2ec28be -->
* **Spotlight Electron: always quits on window close on Linux (no tray support)**: Trap: assuming Electron tray minimize-to-tray behavior works on Linux looks correct since it works on macOS. Fix: Spotlight explicitly detects Linux (\`isLinux\`) and always quits when the window is closed — comment says 'some desktop environments don't support tray apps like gnome'. Tray is not created on Linux (\`if (!isLinux) createTray()\`). The \`win.on('close')\` and \`app.on('window-all-closed')\` handlers both check \`!isQuitting && !isLinux\` before preventing quit. Never add tray-dependent logic without guarding against Linux.

<!-- lore:019e698a-a231-76c0-b0de-628fd77dd123 -->
* **Spotlight Vite: @sentry plugin uses inject:false to avoid circular dep in Node 24.x**: Trap: enabling \`inject: true\` in Sentry's Vite plugin looks like the right default for source map upload. Fix: Spotlight sets \`inject: false\` in \`sentryPluginOptions\` in \`vite.config.base.ts\` — comment says this prevents a circular dependency in Node 24.x when \`preserveModules\` is enabled. Never re-enable \`inject: true\` without verifying the circular dep is resolved upstream.

<!-- lore:019e49d5-15c4-78d1-8bbc-f43fde34d9f1 -->
* **startServer() returns plain object, not Promise — use startGateway() for programmatic async use**: Gateway startup traps: (1) \`startServer()\` returns \`{ stop, port, hosts }\` synchronously — use \`startGateway()\` from \`cli/start.ts\` for \`Promise\<GatewayHandle>\`. (2) \`loadConfig()\` in \`config.ts\` is SYNC — calling \`.then()\` throws TypeError. (3) EADDRINUSE fires as async \`'error'\` event — wrap \`server.listen()\` in a Promise. (4) Use port \`0\` (ephemeral) in tests. (5) \`@loreai/core\` exports async \`load(directory)\` — distinct from gateway's sync \`loadConfig()\`. (6) In-process callers MUST pass \`local:true\` — hosted mode disables FS ops in \`@loreai/core\` (breaks \`getGitRemote()\`, \`.lore.md\` import, config loading). (7) \`lore run\` and \`lore import\` always run locally — both set \`opts.local=true\`.

<!-- lore:019e16fc-441e-7a45-ada6-7285a3e0fb5b -->
* **Upstream task.ts subagent permission refactor: deriveSubagentSessionPermission**: Upstream refactored plan tool permission checks in task.ts into \`deriveSubagentSessionPermission\`. During rebase conflicts on task.ts: (1) Drop old variable declarations for plan\_exit/plan\_enter deny flags, (2) Keep upstream's \`deriveSubagentSessionPermission\` call in Region 2, (3) In Region 3 (inline tools map permission checks), add plan\_exit/plan\_enter deny logic following the same inline pattern as other tools. The session-level deny alone is insufficient — must also deny in the tools map passed to \`SessionPrompt.prompt()\`. See \[\[019e16fc-441b-7f98-bfef-b14e7c366ac1]] for why session-level permissions are insufficient.

### Pattern

<!-- lore:019e98db-f377-70ad-b95b-ac131ef71521 -->
* **Beeper CSS debugging workflow: use console snippets to find real selectors and computed styles**: Beeper CSS debugging workflow: use console snippets to find real selectors and computed styles. When a rule doesn't apply: (1) verify custom-css sheet rule count > 0 via \`Array.from(document.styleSheets).map(s => s.ownerNode?.id + '/' + s.cssRules.length)\`; (2) check \`getComputedStyle(el).backgroundColor\`; (3) enumerate matching rules across all sheets; (4) check \`el.getAttribute('style')\` for injected inline styles; (5) walk ancestor chain for background leakthrough; (6) check \`-webkit-app-region\` — if \`drag\`, Electron suppresses CSS background rendering entirely regardless of \`!important\`; (7) verify parent/child relationship in DOM — siblings in DOM tree won't inherit container styles; (8) check if \`.left\` is a flex item — \`margin\` on flex children can be absorbed by layout. Key findings: \`.left-pane-header\` has \`-webkit-app-region: drag\` by default — must add \`-webkit-app-region: no-drag !important\` to make background colors render. For frame borders, prefer \`padding\` on the outer container (\`.home\`) over \`margin\` on inner children — margin gets absorbed. Burak runs console snippets directly without being asked first.

<!-- lore:019e9938-e963-75dd-8f04-474cda1d0ecb -->
* **Beeper CSS: Slack Orchid sidebar color reference**: Slack Orchid sidebar color reference (for matching in Beeper custom CSS): sidebar body bg \`#e8eaf0\` (light blue-grey), left rail (icon strip) bg \`#1164a3\` (dark blue), header text \`#1d1c1d\` (dark, NOT white — header is light blue not dark), selected item bg \`#1164a3\` with white text, unread badge bg \`#007a5a\` (green) with white text, normal item text \`#1d1c1d\`. The dark blue is ONLY the narrow icon rail — the sidebar header and body use light blue with dark text.

<!-- lore:019e9821-442b-7ef1-96b4-ab292d9beff9 -->
* **Beeper custom CSS: version markers for cross-device copy-paste workflow**: Burak Yigit Kaya edits CSS on one device and copy-pastes to Beeper on another. Beeper doesn't read from a file path — CSS is pasted directly into Beeper's Custom CSS field. Use version markers at the top (e.g. \`v25-2026-06-05-r26 / 3a7f-orchid-conic-v25\`) to verify loaded version. When debugging, always ask which marker is showing before assuming the file isn't loading. File lives at \`/home/byk/Code/beeper-orchid.css\`. Stylelint config at \`/home/byk/Code/.stylelintrc.json\` — run \`npx stylelint beeper-orchid.css\` (from \`/home/byk/Code/\`) before pasting. A CSS syntax error causes Beeper to parse 0 rules. Use \`npx\` — do not install stylelint globally.

<!-- lore:019e56d6-9cca-7a77-aa1a-a5850daef395 -->
* **Gateway test patterns: unit vs integration vs subprocess isolation**: Gateway/OpenCode test patterns — three isolation levels: (1) Unit: import functions directly, build fixtures inline via \`makeRequest()\`/\`makeUserMsg()\` factories. (2) Integration: \`createHarness()\` starts real server on random port with replay interceptor, provides \`chat()\` + \`queryDB()\`. (3) Subprocess isolation: \`mock.module()\` calls isolated in subprocess files to avoid global cache pollution. OpenCode plugin tests: use \`LorePlugin\` + \`createMockClient()\` stubbing \`client.tui.showToast\` and \`client.session.\*\`. \`initPlugin()\` returns \`{hooks, tmpDir, cleanup}\`. DB isolation via root \`bunfig.toml\` preloading \`packages/core/test/setup.ts\`. \`GatewayRequest\` minimal required fields: \`protocol, model, system, messages, tools, stream\`. Hosted-config tests save/restore \`LORE\_HOSTED\_MODE\` in \`beforeEach\`/\`afterEach\`. \`PluginInput\` type does NOT include \`chat\` — access via \`client.app.experimental.chat\`. \`pattern-echo.ts:160\` model resolution: \`const model = input.model ?? config().model;\` — input.model from \`getWorkerModel(state.lastUpstream)\` takes priority. \`idle.ts:466\` uses \`getWorkerModel(state.lastUpstream)\` for force-distillation on idle. \`pipeline.ts\` getWorkerModel call sites: 3 \[truncated — entry too long]

<!-- lore:019e6966-0246-73f1-8e61-e7f49d0f9391 -->
* **Sentry CLI: all packages must be devDependencies — never dependencies**: All npm packages must be in \`devDependencies\`, never \`dependencies\`. Everything is bundled at build time via esbuild. CI enforces this with \`pnpm run check:deps\`. Always use \`pnpm add -D \<package>\` (the \`-D\` flag) when adding any package. This is an absolute project rule — violating it will fail CI.

### Preference

<!-- lore:019ea836-419a-7e83-9583-35d4e8bf1bd8 -->
* **Always address all code review comments (Copilot or human) completely before moving on**: Always address all code review comments completely before moving on: Every comment (Copilot, GitHub reviewers) must be addressed in full — fix root cause, add missing tests, apply consistent fixes across all affected files. Do not skip or defer. After each discrete fix: (1) run build/typecheck/lint, (2) create conventional-commit message, (3) push to working branch. Auto-fix lint issues in a separate 'style:' commit. Batch related bug fixes into a single PR with tests and updated .lore.md. Always fix biome lint/format errors before or immediately after committing — never leave biome errors unresolved when pushing.

<!-- lore:019ea8f5-191a-73fb-8aa4-435d71b69633 -->
* **Always document findings in \`.lore.md\` with structured observations**: Across sessions, the user (Burak) consistently captures and updates structured notes in \`.lore.md\` during/after working on gateway code. Observations use a typed format with severity emojis (🔴 stated-fact/directive/trap, 🟡 neutral observations) plus timestamps. Each entry cites specific file:line references and explains the why. The user also removes entries when they become obsolete (e.g., deleting \`createGatewayLLMClient\` and \`LLMClient.prompt() opts\` entries after refactor). When working on \`llm-adapter.ts\` and \`pipeline.ts\`, the user expects new architectural decisions, traps, and directives to be distilled into \`.lore.md\` entries — not just left in chat.

<!-- lore:019e920f-0876-7aad-a306-8129434a3d6b -->
* **Always embed behavioral directives as code comments to assert invariants**: Behavioral directives & investigation style: (1) Write invariant assertions as imperative code comments at enforcement points. (2) 🔴 = durable/identity-level facts; 🟡 = session-scoped — never promote 🟡 to 🔴 without explicit user confirmation. (3) After bulk type migrations, run typecheck and update ALL affected test files. Fix ALL occurrences before marking complete. (4) Review plan before execution — survey tsconfig, package.json, migration status, export patterns. Enumerate all consumers of shared state before modifying. Prefer surgical minimal-churn over full structural replacement. (5) Bug investigation: present numbered hypotheses, cite exact line numbers, quote file comments verbatim as design intent evidence, label primary root cause vs secondary/contributing factors. (6) Always update .lore.md alongside every PR — required, not optional. (7) Prefer CLI over raw API.

<!-- lore:019ea574-982f-76b5-811b-6695654733cb -->
* **Always implement worker health monitoring and source attribution for background tasks**: When adding or modifying background worker infrastructure (distillation, curation, embedding, cache warming), the user consistently adds: (1) graduated failure escalation with tiers (warn → Sentry → user-facing warnings → exceptions), (2) per-session sliding window failure tracking, (3) recovery detection on success, (4) source attribution columns (worker\_provider\_id, worker\_model\_id) to database tables, (5) wiring of health hooks at every call site in pipeline, idle, and adapter layers. The pattern includes both the monitoring infrastructure and the data plumbing to trace which worker model produced each output.

<!-- lore:019ea277-caba-7f13-8b95-a86f8c6f7b88 -->
* **Always investigate Bun/Node.js runtime compatibility issues by examining Sentry setup, polyfills, and SDK choices**: Sentry/runtime gotchas in gateway package: (1) Sentry init file is \`packages/gateway/instrument.ts\` (NOT \`src/instrument.ts\`). (2) \`@sentry/bun\` imports remapped to \`@sentry/node\` via \`sentryBunToNodePlugin\`. (3) \`NODE\_ENV==='test'\` is the hard-off gate for Sentry. (4) Bun's \`node:util\` doesn't implement \`getSystemErrorMap()\`, called by \`@sentry/node-core\` during \`processEvent()\` — crashes before \`beforeSend\` runs. Fix: polyfill \`util.getSystemErrorMap = () => new Map()\` in \`instrument.ts\` BEFORE \`Sentry.init()\`. (5) \`log.error()\` always calls \`console.error()\` — any \`log.error(..., errorObj)\` auto-forwards to Sentry including expected shutdown/auth errors; use typed error subclasses or \`TRANSIENT\_ERROR\_PATTERNS\` filter.

<!-- lore:019ea911-931e-7e62-ab53-0e5b92263930 -->
* **Always load jj-guide skill at session start when working in jj repos**: At the beginning of coding sessions involving version control operations, the user loads the \`jj-guide\` skill from \`/home/byk/.claude/skills/jj-guide\`. This skill establishes critical jj-specific rules: never use interactive flags (\`-i\`, \`--interactive\`), always pass \`-m "msg"\` for commit messages, prefer change IDs over commit IDs, never rebase immutable commits, use \`jj undo\` for recovery, and understand that jj never fails on conflict (conflicts are recorded in the commit). When this skill is loaded, follow all its rules strictly for any VCS operations in the session.

<!-- lore:019e9f06-597e-75c0-8f6a-2d2b4c144bc8 -->
* **Always remove all Bun-specific code and build targets**: Always remove all Bun-specific code: delete dist/bun directories, replace Bun APIs (Bun.mmap, Bun.file, Bun.zstdDecompressSync) with Node.js equivalents, remove Bun-specific mocks/test assertions/build targets. Migrate projects from Bun to Node.js and pnpm. Root package.json bun scripts may remain as migration artifacts — convert to pnpm. Separate migration steps into distinct PRs (package manager, runtime APIs, test runner, CI, cleanup) — each CI-verified and self-contained. Never add new Bun-specific code. Bun bugs (#18145, #25677, #31158) cause model-init OOM in WASM paths — numThreads=1 avoids pthread paths. Bun 1.3.14 does NOT fix these.

<!-- lore:019e6966-0223-71a5-90a4-c10314841290 -->
* **Always start sessions with comprehensive codebase exploration before any task**: Always start sessions with comprehensive codebase exploration before any task: provide a structured analysis covering (1) what the project is/does, (2) tech stack and tooling, (3) architecture overview (entry points, directory structure, key modules), (4) key challenges/complexity areas, (5) notable rules/constraints (AGENTS.md, package.json scripts, CI requirements). Explore full repo structure, read key config files, and produce a detailed summary proactively — not piecemeal on request.

<!-- lore:019e9f41-df33-7e3c-a1a5-843a62fcbd64 -->
* **Always use the same provider and never cross-provider**: Same-provider invariant: worker/warmup model ALWAYS uses same provider as session. Wire protocol resolved from \`resolveProviderRoute(providerID)?.protocol\`, NOT hardcoded \`providerID === 'openai'\`. \`getWorkerModel()\` (gateway/src/worker-model.ts): effectiveProvider = cfg.model?.providerID ?? sessionProviderID ?? 'anthropic'; fallback = cfg.model ?? sessionModel ?? WORKER\_DEFAULTS\[effectiveProvider]. For unknown providers (MiniMax, NVIDIA, xAI, etc.) with no WORKER\_DEFAULTS entry, fallback = session model itself — correct behavior. \`createGatewayLLMClient\` defense-in-depth mismatch check (llm-adapter.ts:428-452): skipped when \`hasDedicatedKey\`, \`upstreamOverride\` active, or cred.scheme !== 'api-key'. TRAP: \`upstreamOverride\` is always truthy for workers routed through proxy (OpenCode Zen) — mismatch check never fires for proxy sessions. \`resolveWorkerProtocol()\` determines actual wire protocol from providerID.

<!-- lore:019ea83f-b035-7b13-9be0-41965e86ca37 -->
* **Always verify favicon rendering visually and iterate until size/appearance meets expectations**: Favicon rendering rules (lucky-cactus/packages/website/): (1) Use a SINGLE adaptive \`favicon.svg\` with embedded \`\<style>\` + \`@media (prefers-color-scheme:dark)\` CSS — NOT dual \`\<link media>\` tags; Firefox ignores \`media\` on \`\<link>\` favicon tags. (2) High-contrast fills at 32px: light mode \`#0a1a10\` + \`#2d5e38\` dots; dark mode \`#ffffff\` + \`#7debb0\` mint. Brand palette only for logo SVGs (40–64px). (3) ViewBox tight (~50×50) so glyph fills ~92%+ of 32px space; remove decorative bottom dots. (4) PNG fallbacks via sharp: \`.png({ palette: true, compressionLevel: 9, effort: 10 })\` — 8-bit indexed. Prevents 16-bit RGBA regression (favicon-32.png 289B vs 4.2KB). (5) Extract inline \`data:\` URIs to static files in \`public/\` — inline URIs look broken in DevTools. (6) Build-time auto-generation via custom Astro integration (\`faviconAssets\`) — no manual step. (7) Always verify at actual browser tab size on both light/dark backgrounds and in Firefox.

<!-- lore:019ea87a-018b-7f19-a6b7-499ccb159e97 -->
* **Audit all call sites before touching shared state or project-wide renames**: User expects the assistant to systematically enumerate every read/write/type reference across the codebase with file:line precision BEFORE proposing or implementing changes that touch shared structures, project naming, or cross-cutting refactors. When a refactor has 50+ call sites, prefer surgical minimal-churn approaches (compound-key existing maps, add parallel field) over full structural replacement. This applies to: (1) renaming project identifiers (e.g., \`nuum\`/\`opencode-lore\` → \`Lore\`) — every category must be classified (self-reference vs third-party credit vs historical narrative vs test fixture vs CHANGELOG/legacy alias); (2) modifying shared session/state structures — list all consumers, classify each as read/write/type, flag any needing updates. Always distinguish Craft-managed files (CHANGELOG, version) that must NOT be manually edited.

<!-- lore:019ea8bf-faaa-7a83-b877-8d5641c46cf7 -->
* **Demand last critical code review of own PR before declaring it done**: After every PR is created, CI passes, and merge appears imminent, the user insists on a final critical self-review of the code/PR/diff with fresh eyes. The user phrases this as 'review one last time with critical eye' or 'if still good, merge. If not improve.' They want the assistant to re-read the actual code changes and catch anything missed before treating work as finished. This applies after CI is green but before user-acknowledged completion. When asked post-merge, acknowledge it's a post-merge review and treat the request as a chance to catch regressions or improvements for the next version.

<!-- lore:019ea8ae-ce2d-73a1-a1df-b21df4e288d4 -->
* **Follow the established git workflow (branch, PR, review)**: Behavioral pattern detected across 26 sessions (action: enforced-workflow). The user consistently demonstrates this behavior.

<!-- lore:019e9d98-e22f-7f0d-87d3-91c4f8b9a5e3 -->
* **OpenCode plugin: per-project state map for project path header injection**: OpenCode plugin per-project state: \`projectState\` Map keyed by \`ctx.project.id\` → \`{ projectPath, gitRemote, lastSeenAt }\`; TTL \`SESSION\_STATE\_TTL\_MS = 24h\`. \`reapStaleProjectState()\` called on every new entry. \`chat.headers\` hook uses per-project Map entry to avoid cross-project clobbering. Module-level \`currentProjectPath\`/\`currentGitRemote\` updated on every call — fallback only when hook bypassed. \`loreInitPromise\` memoization prevents concurrent plugin calls racing on probe→spawn. Gateway discovery order: (0) \`LORE\_REMOTE\_URL\`; (1) \`LORE\_GATEWAY\_URL\`; (2) port file (ports 3207, 5673); (3) in-process \`startGateway({ quiet:true, local:true })\`. \`remoteGateway\` auto-detection 4-layer precedence in \`start.ts\`: (1) \`LORE\_REMOTE\_GATEWAY\` env; (2) \`LORE\_HOSTED\_MODE\`; (3) bind address (\`0.0.0.0\`/\`::\` = non-loopback); (4) default=true. \`--local\` CLI flag ALWAYS wins. In-process callers MUST pass \`local:true\`.

<!-- lore:019e6966-0236-78e3-a522-d10ac763cdba -->
* **Prefers SF relocation); offered over help draft reply to Bill or prepare interview questions**: Bill Yang (billyang.io, GitHub: goldflag, X: yang\_frog) is a candidate for the #team-spec-eng (Special Projects Engineering) team at Sentry. David Cramer identified him as top candidate. Background: heavy frontend (React at Anduril ~5 years, bored), located in SoCal. Cramer open to remote but prefers SF relocation. Cultural fit (quiet/introverted) is #1 open question; technical depth less of a concern. Burak Yigit Kaya is conducting the technical/fit interview on Cramer's behalf.

<!-- lore:019e98dd-daea-7541-8a69-11d82fbbadca -->
* **Run Beeper console snippets directly without asking first**: Burak Yigit Kaya runs Beeper console snippets directly and explicitly said: 'I will run your snippets so no need to ask before. Just share the snippets.' Never ask for permission before sharing a console snippet for Beeper debugging — just provide it directly.

<!-- lore:019ea8ae-f8a5-7025-87dd-97ccb57b7da0 -->
* **Use dedicated branches for fixes and open PRs against the canonical repo**: Across sessions, the user consistently creates focused, single-purpose fix branches (e.g., fix/esm-core-external, fix/worker-provider-match) for specific bugs, commits narrowly-scoped changes with conventional commit prefixes like fix():, and opens PRs against the canonical repo (BYK/loreai). When diagnosing issues, they articulate the root cause precisely (e.g., 'bundled copy returns globalThis.fetch creating infinite loop') and ensure the fix is minimal and targeted. Before merging, they request thorough critical review via subagents. Follow this pattern: branch per issue, conventional commit message, narrow diff, PR with clear description.
