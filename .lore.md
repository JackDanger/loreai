<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019ee004-037c-70e6-a910-eef98c0778aa -->
* **A1 quota trigger: counter-based enforce\_row\_quota — only INSERTs and revivals grow live-row count**: Post-A1 \`enforce\_row\_quota()\` (migration 0007): reads O(1) from \`user\_table\_usage(scope\_id, table\_name, row\_count, byte\_count)\`. Advisory lock keyed on \`hashtextextended(scope\_id::text || '|' || tg\_table\_name, 0)\` — MUST be taken before \`exists\_pk\` check (0005 ordering); 0007 moved \`exists\_pk\` outside lock — SHOULD-FIX. Revival is UPDATE with \`d\_bytes=0, d\_rows=0\` — guard returns early, never gated. Tombstone INSERTs counted+gated (physical accounting). \`user\_table\_usage\` has NO DML grant to \`authenticated\` — written ONLY by SECURITY DEFINER triggers. Raises errcode 23514 on violation. Self-DoS: user creating+deleting ~500 entries can exhaust free cap with zero live rows; no client-side reclaim path until A2 hard-DELETE reaper.

<!-- lore:019ef0c0-02e8-701a-824e-329ffb264c14 -->
* **Append-only knowledge versioning: invariants + metadata forward-copy**: Two identities per entry: logical\_id (stable entry identity) + version (immutable snapshot keyed by UUIDv7). Invariant: version rows are immutable except is\_current — content, title, category, is\_deleted, version, logical\_id NEVER mutate after insert. Invariant: never resurrect a logical\_id whose latest version has is\_deleted=1 — no separate tombstones table needed. All non-FTS reads use knowledge\_current view (is\_current=1 AND is\_deleted=0). FTS5 indexes only current+live rows; superseded version's FTS row removed on append. Knowledge outbox keyed by logical\_id (not version id) so deletes always match their remote row.

<!-- lore:019f1fde-f17e-7835-a964-dfb1b4c697f5 -->
* **backfillTemporalEmbeddings: pool-gate on recall embeds in flight (PR #1111)**: PR #1111 (\`fix/temporal-backfill-pool-gate\`) replaces the session-activity idle-gate (PR #1108) with an embed-pool capacity gate. Root cause of old gate: 5 active sessions kept backfill parked continuously; cursor never advanced during 8+ min window. New signal: module-level \`\_recallEmbedsInFlight\` counter in \`embedding.ts\`. Incremented in \`embed()\` when \`inputType === "query" && texts.length === 1\`; decremented in \`finally\`. Exported as \`recallEmbedsInFlight()\` + \`\_setRecallEmbedsInFlightForTest()\` seam. Gate factory \`makeTemporalBackfillGate(deps)\` parks when \`isPaused() || isEmbedBusy()\`. Wired in \`pipeline.ts\` as \`isEmbedBusy: () => embedding.recallEmbedsInFlight() > 0\`. Backfill always uses \`"document"\` inputType → \`isRecall\` always false → no self-gating. Throwing gate predicate caught with \`break\` in \`awaitBackfillIdle\` — never wedges walk.

<!-- lore:019ee201-4395-7551-83fd-54ff714be1dc -->
* **cache-warmer: per-bucket circuit breaker (bucket key, warmup semantics, protocol gating, pure-read status)**: cache-warmer: per-bucket circuit breaker (PR #836). Bucket key = \`sessionID \x1f model \x1f upstreamURL\`. \`BreakerEntry = { failures, tripped, trippedAt }\` — failures in-memory only; only tripped buckets persisted. KV v2: \`{ version:2, buckets:{ \[key]:{ trippedAt } } }\`. Legacy v1 auto-cleared on load. \`CIRCUIT\_BREAKER\_DECAY\_MS\`=6h. Three reset paths: (1) \`resetCircuitBreaker(bucketKey?)\` — production reset; (2) \`/lore:warm:reset\` slash command; (3) \`POST /ui/api/warming/:sessionId/reset\`. \`getCircuitBreakerStatus\` is PURE READ — no map mutation, no SQLite write. \`pruneExpiredCircuitBreakers()\` GCs dead-session buckets in idle tick.

<!-- lore:019f85e0-6e5d-7bdb-8122-021a0218cf50 -->
* **config.ts load(): hosted mode never reads client-controlled .lore.json**: load(directory) in packages/core/src/config.ts only reads directory/.lore.json when !isHostedMode(); in hosted mode it always falls back to LoreConfig.parse({}) (defaults), never touching a client-supplied path. Rationale: a crafted .lore.json could alter gateway behavior (budget, model, thresholds) if the hosted gateway trusted client-controlled paths — this is a security boundary, not an oversight. Parse failures on .lore.json call warn() and fall back to defaults rather than throwing. config() simply returns the module-level \`current\` set by the last load() call, so tests that need modelExplicit=true must call load()/config() with a real .lore.json on disk first.

<!-- lore:019f85d2-bd80-7bea-b79b-e86c9b62157f -->
* **Cross-provider/worker routing: never send provider A's model to provider B's endpoint**: Invariant: never send provider A's model to provider B's endpoint. pipeline.ts's cross-provider guard only fires when modelProvider!==upstreamProvider — blind when providerID matches but the endpoint is single-model-only (ChatGPT-backend and real api.openai.com both report providerID='openai'). Concrete case: bare Codex clients send providerID='openai' while upstream-url points at chatgpt.com/backend-api, so getWorkerModel substitutes a WORKER\_DEFAULTS\['openai'] model that chatgpt.com rejects with 404 (it only serves the session's own model). Fix direction: detect single-model-only hosts and force worker-model reuse, not substitution. General rule: if the worker model's provider differs from the session/override provider, fall back to the model's own route table and fail closed (recordWorkerFailure+markWorkerPaused) rather than defaulting to the session's endpoint — a real incident routed a minimax worker model to api.anthropic.com, causing a 401 retry loop.

<!-- lore:019f1a70-b05d-7756-aa08-d9d57fb9cbb0 -->
* **embedding.ts: embedInTokenBatches + nextBatch — sub-batching invariants for temporal chunk fan-out**: \`embedInTokenBatches(texts, type)\` loops \`nextBatch\` (generic over \`{text:string}\`, \`MAX\_BACKFILL\_CHUNK=8\`, \`MAX\_BATCH\_TOKEN\_AREA=4096\`, \`CHARS\_PER\_TOKEN=4\`). Key invariant: area guard fires only when \`batch.length > 0\` → first item always added unconditionally → loop always terminates. Vectors accumulate in input order; \`storeTemporalChunks\` called exactly once (all-or-nothing). \`MAX\_TEMPORAL\_CHUNKS\_PER\_MESSAGE=64\`: keeps first 63 verbatim, folds \`slice(63)\` into one merged chunk via \`join("\n")\` → exactly 64 texts. Oversized merged tail goes alone in its own batch; worker \`truncateTexts()\` + \`truncation:true\` handle downstream. PR #1075 (\`a2d71daa\`). Root cause of #1072: \`embed()\` forwarded entire \`texts\` array in ONE call — ONNX pads all texts to longest sequence, creating oversized tensor (OOM). Fix: \`embedInTokenBatches\` was already used by knowledge/distillation/entity backfill — gap was \`embedTemporalMessage\` did not use it. Mutation A (revert sub-batch) and Mutation B (revert cap fold) each independently kill their respective tests.

<!-- lore:019f43a4-4edb-7066-b799-932b9ee9b165 -->
* **forSession packing loop uses continue (not break) on over-budget entries**: In \`ltm.ts\` packing loop (line ~2358): \`if (used + cost > maxTokens) continue;\` — NOT \`break\`. This means over-budget entries are skipped but smaller subsequent entries can still be packed. The loop never exceeds total budget but does not stop at first overflow. This is intentional: allows smaller high-value entries to fill remaining budget after a large entry is skipped.

<!-- lore:019f85ac-d2bc-7a6b-8d9f-57626d38f6de -->
* **gateway upgrade binary: Node 22 SEA via build-binary-sea.ts, not Bun build --compile**: The distributed Lore gateway CLI binary is built via script/build-binary-sea.ts + fossilize targeting Node 22 SEA — NOT \`bun build --compile\`. Chose Node SEA over Bun's native compile because of Bun packaging bugs (oven-sh/bun#18145, #25677). esbuild externals: node:\*, onnxruntime-node, sharp, sqlite-vec; @sentry/bun is remapped to @sentry/node in the CJS bundle. This is distinct from the deployed gateway SERVER process, which runs under Bun at runtime \[\[019f8577-13a9-7bcd-a134-5bc7fd98910d]] — don't conflate 'the gateway runs on Bun' with 'the distributed self-upgrade binary is a Bun executable.'

<!-- lore:019f8615-1221-709a-8ad6-491c4b5f5f8e -->
* **getWorkerModel resolution order & ChatGPT-backend detection (worker-model.ts)**: getWorkerModel(session) order: 1) LORE\_WORKER\_MODEL env override; 2) effectiveProvider = cfg.model?.providerID ?? sessionProviderID (undefined → skip, never fabricate an anthropic worker); 3) if input cost ≥ EXPENSIVE\_MODEL\_THRESHOLD ($1.5/M): WORKER\_DEFAULTS\[provider] family pin (gated by alreadyCheap) or findCheaperSameProviderModel otherwise; 4) fallback cfg.model > session model > WORKER\_DEFAULTS > undefined. Gotcha: real api.openai.com and ChatGPT-backend sessions both report providerID='openai' — indistinguishable by that field. Fix (PR #1437): detect via upstream URL (isChatGPTBackend: host 'chatgpt.com' or path '/backend-api') and short-circuit to reuse the session's own model exactly (except providerID==='openai-codex', which legitimately downgrades to codex-mini). That short-circuit must return the LIVE \`sessionProviderID\`/\`sessionModelID\`, not \`effectiveModelID\` — the latter prefers \`cfg.model\` overrides, so a stale \`.lore.json\` override caused real 404s despite claiming to 'reuse the session's model'.

<!-- lore:019f8510-6946-73db-bed9-89d1c2abd54f -->
* **gradient.ts quality/compression model: fill-fraction knee, cost guards, module-global races, Wilson-gated updates**: qualityCostMultiplier(tokens): fill=tokens/contextLimit; below the model's qualityKnee multiplier=1, above it a convex ramp. Knee resolution: config override → literature family table (matched on LAST '/'-segment) → 0.4 fallback. computeLayer0Cap is clamped to contextLimit (unclamped caused l0cap≈35M for cheap cache-read models, disabling layer-0 compression). GOTCHA: shouldCompress treats cacheWriteCost/cacheReadCost<=0 as 'free', but DeepSeek/OpenRouter often report 0 for UNKNOWN pricing — treat 0 as unknown, not free. GOTCHA: module globals (contextLimit, qualityKnee, cache pricing) set per-request can be overwritten by a concurrent different-model request across awaits — snapshot+apply via applyModelBudget(). Literature-seeded knees are being replaced per-model with empirical values derived from MATRIX-V5 behavioral-rot data, gated on Wilson significance + adequate sample size per model/cap cell — where a cell lacks power, keep the literature prior rather than overwrite with noisy data.

<!-- lore:019eea52-a235-7cc4-985a-0a3dfbbffeb9 -->
* **gradient.ts: l0cap (maxLayer0Tokens) governs compression trigger, NOT layer-1 window size**: Hard design invariant: \`maxLayer0Tokens\` (l0cap) is the cost-cap threshold that determines WHEN to compress (layer-0 exit), not the size of the layer-1 window. Layer-1 \`rawBudget = usable\*0.4\` is intentionally larger than l0cap to give the stable window headroom for chunked eviction. Clamping the base rawBudget to l0cap breaks cache-stability. Only escalated-stage budgets (layers 2+) should be clamped via \`stageBudgetUsable = Math.min(usable, stageBudgetCeiling)\`. \`computeLayer0Cap(targetCostPerTurn, cacheReadCostPerToken)\` returns 0 if either arg ≤ 0; otherwise \`max(floor(target/readCost), MIN\_LAYER0\_FLOOR=40\_000)\`. \`layer0Bounds()\` is extracted so pipeline's turn-1 LTM decision (\`isLargeColdStart\`) shares ONE definition with layer choice. \`isEscalated = stageLayer > 1\`. 🔴 Any path where \`layer0Input\` uses a DIFFERENT effective factor (e.g. \`layer0Ceiling\` 0.7 cold-cache tightening at :2212-2213) that would make compressed's scale not match full's must be flagged.

<!-- lore:019f1890-9144-77d2-98f3-57e2fa2dfc68 -->
* **http-body.ts: encodeUpstreamBodyForRoute — single chokepoint for re-compression scoping**: PR #1059 adds \`encodeUpstreamBodyForRoute(serializedBody, rawEncoding, route: UpstreamRouteContext)\` as the single chokepoint for upstream re-compression decisions. PR #1064 (\`de7bbd79\`, 2026-06-30) extends with origin-aware distrust: \`mayReencodeUpstream(route)\` returns false when \`autoCrossRouted = !hasUpstreamUrlOverride && !hasProviderOverride && (effectiveProtocol !== ingressProtocol || effectiveOrigin !== ingressOrigin)\`. F3 root cause: protocol-only predicate trusted same-protocol re-routes to different hosts. \`buildUpstreamRouteContext\` accepts options object with \`ingressUpstreamBase\`/\`effectiveUpstreamBase\`. Vertex/Bedrock never reach origin comparison — both require explicit \`X-Lore-Provider\` → short-circuit to trusted. Codex always carries \`x-lore-provider\` → trusted. \`fetch.ts\` gets \`setUpstreamDispatcherForTest\` seam. Mutation-verified: removing origin clause → 4 tests RED.

<!-- lore:019ef387-4ae0-7676-b7b5-140c85ad2627 -->
* **knowledge\_meta table (v55): architecture, rationale, invariants, migration gotcha**: Migration v55 moves \`confidence\` and \`last\_reinforced\_at\` off the \`knowledge\` table into a new convergent register table: \`knowledge\_meta (logical\_id TEXT PRIMARY KEY, confidence REAL NOT NULL DEFAULT 1.0, last\_reinforced\_at INTEGER, updated\_at INTEGER NOT NULL DEFAULT 0)\`. \`knowledge\_current\` view exposes both via LEFT JOIN: \`SELECT k.\*, COALESCE(m.confidence, 1.0) AS confidence, m.last\_reinforced\_at FROM knowledge k LEFT JOIN knowledge\_meta m ON m.logical\_id = k.logical\_id WHERE k.is\_current=1 AND k.is\_deleted=0\`. Reading/writing confidence on the base \`knowledge\` table now errors with 'no such column'. For v1 rows: logical\_id == id. \`ltm.create()\` creates matching knowledge\_meta row automatically. Schema version bumped 54→55. \`markInjected()\` updates \`knowledge\_meta.last\_reinforced\_at\` but does NOT bump \`updated\_at\` — injection stays sync-silent. Content-only \`update()\` also must NOT bump \`meta.updated\_at\`.

<!-- lore:019f84f0-bea9-7040-90c2-d51c9e717505 -->
* **llm-adapter.ts worker reasoning-budget floor: effort-typed models, centralized application, restart required**: Worker calls (distillation/curation) pass tiny raw token budgets (~1-8K) with no explicit reasoning effort. Aggregators (e.g. OpenRouter) routing reasoning models reason regardless, burning the whole budget on hidden reasoning → finish\_reason:'length', empty output — looks broken but is starved reasoning budget. Fix: floorsReasoningBudget applies for protocol==='openai'||'gemini' (Anthropic/Vertex builders disable thinking explicitly). reasoningFloor: explicit effort→thinkingBudget+8192; else if workerModelReasons(model)→DEFAULT\_REASONING\_MODEL\_BUDGET+8192; else 0. maxTokens = max(rawMaxTokens, reasoningFloor), clamped to model's output limit, applied ONCE at retry-loop entry. PR #1430 raised DEFAULT\_REASONING\_MODEL\_BUDGET 8192→16384, no-effort floor to 24576. Constants read at startup — requires gateway restart, not hot-reloaded.

<!-- lore:019f8532-af3d-77b0-b08d-9a8f3491fe75 -->
* **ltm.ts recall scoring: minRelevance floor + sessionContext construction window**: isRelevant() requires vecScore>0 && vecScore>=config().knowledge.minRelevance (default 0.35) for a vector-only match to count — guards against near-orthogonal/negative-cosine entries. An FTS keyword hit bypasses this floor entirely. scoreOf() ranks by max(normalized vecScore, ftsScore)\*entry.confidence so high-FTS/low-cosine entries aren't underranked. Vector search failure falls back to scoreEntriesFTS with a log.warn. Separately, forSession()'s sessionContext (used for both vector embed and FTS) is built from only the SINGLE most-recent distillation plus the 10 most-recent temporal\_messages — NOT all distillations. A later off-topic distillation can crowd out an earlier fact-bearing one, and old facts can fall outside the 10-message window, silently degrading recall even though still directly FTS-matchable elsewhere.

<!-- lore:019edfbc-edd5-780d-ac81-07a35390121b -->
* **ltm.ts: confidence lifecycle — decay, reinforcement, eviction, and cross-project protection**: ltm.ts: confidence lifecycle — decay, reinforcement, eviction, cross-project protection. \`decayProject(path)\` decrements confidence, never bumps \`updated\_at\`. \`markInjected(ids)\` writes only \`last\_reinforced\_at\`. \`evictLowestValue\`/\`pruneDeadEntries\`/\`pruneDeadEntriesAllProjects\` MUST skip \`cross\_project=1\` entries. \`DEAD\_CONFIDENCE\_FLOOR=0.2\`. \`enforceEntryCap\` counts promoted rows but eviction excludes them. Global sweep in \`idle.ts\`: \`pruneDeadEntriesAllProjects()\` on first tick then hourly (\`GLOBAL\_DEAD\_SWEEP\_INTERVAL\_MS=3600000\`), gated on \`loreConfig().knowledge.enabled\`. \`lastGlobalDeadSweepAt\` starts at 0, set BEFORE try block (prevents retry storm). Sweep wrapped in try/catch. Queries \`WHERE cross\_project=0 AND confidence<=0.2 LIMIT ?\` — \`cross\_project=0\` implicitly guarantees non-null \`project\_id\` (enforced at \`create()\`: null pid forces \`cross\_project=1\`).

<!-- lore:019f1e66-a084-7822-bfe1-51bfb5cfd6a4 -->
* **maybeCutoverToVec0: resetTemporalRechunkProgress must be called after setStorageMode**: \`maybeCutoverToVec0\` calls \`resetTemporalRechunkProgress()\` immediately after \`setStorageMode('vec0')\` (added in PR #1101). Rationale: blob-mode runs can never latch the done flag (vec0-only gate returns before done-check), so the flag is always un-latched when cutover fires — the call is a no-op today but self-documents the load-bearing ordering invariant: cutover always precedes an armed walk in the same pass. A later dimension change never touches \`copyBlobsToVec0\` (columns are gone) — it goes through \`checkConfigChange\` → \`resetTemporalRechunkProgress()\` instead. Mutation confirmed non-vacuous: removing the call inside \`maybeCutoverToVec0\` kills the 'arm-on-cutover' test. KV keys: \`lore:temporal\_rechunk.done\`, \`lore:temporal\_rechunk.cursor\`, \`lore:temporal\_rechunk.attempts\`.

<!-- lore:019f8684-bede-74f7-993d-72fcb6de2c70 -->
* **Nightly release pipeline: ci.yml computes version, publishes to GHCR not npm**: Nightlies build/publish from \`ci.yml\` on every push to \`main\` — not from PRs, not from \`publish.yml\`/\`release.yml\`. Version format: \`${NEXT}-dev.${TS}\` where NEXT = next semver bump computed from \`packages/gateway/package.json\`'s current version, TS = merge commit's Unix timestamp — ensures nightlies always sort ahead of the current release (e.g. 0.19.0-dev.X > 0.18.0). \`publish-nightly\` job depends on \`\[changes, test, build-nightly-binaries, build-nightly-darwin, generate-patches]\`. Publishes via ORAS CLI to GHCR (\`ghcr.io/byk/loreai\`) as OCI artifacts, NOT npm packages: \`oras push ghcr.io/byk/loreai:nightly\` (rolling tag) then \`oras tag ... "nightly-${VERSION}"\` (immutable, enables patch-chain resolution to specific versions). \`lore upgrade --channel nightly\` tracks the rolling tag.

<!-- lore:019efa21-0e50-7290-ae68-9231f88f690b -->
* **openai-codex ingress: codex flag, store:false enforcement, max\_output\_tokens suppression**: \`POST /v1/codex/responses\` → \`parseOpenAICodexRequest\` sets \`req.codex = true\`, captures Codex control fields (\`include\`, \`prompt\_cache\_key\`, \`text\`, \`tool\_choice\`, \`parallel\_tool\_calls\`, \`service\_tier\`) into \`req.extras\`. \`store\` is NOT captured — \`applyCodexResponsesDelta\` always forces \`store: false\` (ChatGPT rejects \`store: true\`). \`max\_output\_tokens\` is deleted from upstream body (ChatGPT rejects it). Upstream URL: \`https://chatgpt.com/backend-api/codex/responses\`. Gateway is stateless full-history proxy — never persists upstream item IDs. \`stream\` flag passed directly from \`GatewayRequest\` into upstream body via \`buildOpenAIResponsesUpstreamRequest\`.

<!-- lore:019f8534-73ee-7176-99ed-d7b439e9d3cd -->
* **pipeline.ts effectiveUpstreamBase four-tier resolution order**: pipeline.ts resolves the per-request upstream base URL via a strict priority chain: (1) X-Lore-Upstream-URL header (explicit override); (2) self-built URL for bedrock-mantle/vertex protocols (region from env at request time); (3) providerRoute.url from X-Lore-Provider header lookup; (4) modelRoute.url from model-id-prefix fallback; (5) protocol-based config default. The same chain feeds both the live request and UpstreamSnapshot, so they can't diverge. GitHub Copilot CLI is special-cased: since it can't set X-Lore-Provider, its Copilot-Integration-Id header triggers resolveProviderRoute('github-copilot') only if no providerRoute/headerUpstream is already set — explicit headers always win.

<!-- lore:019eeb85-6440-7760-912a-30c1cdc31a9f -->
* **pipeline.ts inFlightBackground: tracks urgent distillation separately from limiter activeTasks**: pipeline.ts inFlightBackground + urgent distillation drain invariants: \`inFlightBackground\` (Set\<Promise>) tracks urgent distillation promises that bypass the background limiter. \`trackBackground(p)\` adds/removes on \`.finally()\`. Urgent distillation bypasses \`isBackgroundPaused()\` — degraded context for 10 min is worse than one API call. \`resetPipelineState\` non-fast path drains BOTH: \`drainBackground()\` (limiter's \`activeTasks\`) AND \`boundedSettle(\[...inFlightBackground], timeoutMs)\`. Missing either leaves a drain gap. \`boundedSettle()\` in background-limiter.ts: snapshots promises, races \`Promise.allSettled\` against a timeout that only resolves (never rejects); timer always cleared in \`finally\`. \`drainBackground\`: calls \`limiter.clearQueue()\` then \`boundedSettle(\[...activeTasks])\`. Queued-but-not-started tasks are never in \`activeTasks\`.

<!-- lore:019f04c9-26e4-794e-8d94-dd107b7bf454 -->
* **pipeline.ts: prompt-delta system — surfaced-set, reanchor-not-delete, and cache-stability invariants**: Three-layer delta system in \`pipeline.ts\`: 1. \*\*\`detectSurfacedMutations\`\*\* (line 1185): compares already-surfaced \`id:hash\` keys against current DB state — fires only on genuine content change or tombstone, never on per-turn ranking churn (churn-immunity property). 2. \*\*\`reanchorDeltaOnCompression\`\*\* (line 945): on a compressing turn, ALWAYS re-anchor (preserve \`content\` + \`mut\` signatures, rewrite only \`insertAt\`) — NEVER delete. Deleting wipes surfaced-set history, causing next append to re-derive full cumulative pin→DB wall (the regrowth churn #1013 trimmed but #1017 root-caused). 3. \*\*\`applySessionPromptDeltas\`\*\*: splices back-to-front so equal-position blocks replay in ascending-seq order; lower-index blocks never shifted by later splices. 🔴 Delta block must go BEFORE any assistant \`tool\_use\` — never between \`tool\_use\` and its \`tool\_result\`.

<!-- lore:019f324d-a433-76ef-a7d3-c9acc21c6335 -->
* **scope\_keys.wrapped\_dek: first-write-wins immutability via key\_epoch guard trigger**: PR #1179 (migration 0012\_scope\_key\_immutable.sql) adds trigger \`guard\_scope\_key\_immutable\` on \`scope\_keys\` (BEFORE UPDATE): raises \`check\_violation\` (SQLSTATE 23514) when \`new.wrapped\_dek IS DISTINCT FROM old.wrapped\_dek AND new.key\_epoch <= old.key\_epoch\`. Metadata-only changes and higher-epoch DEK rotations are allowed; same-epoch DEK clobber is blocked. Rationale: DEK clobber is the catastrophic failure mode — it orphans already-encrypted ciphertext across devices. \`account\_escrow\` is NOT made immutable (must change on passphrase rotation) — only \`scope\_keys.wrapped\_dek\` gets the guard, since escrow clobber is recoverable (device-1 retains local identity) but DEK clobber is not. Trigger ordering matters: Postgres fires BEFORE UPDATE triggers alphabetically by name — \`scope\_keys\_guard\_immutable\` (0012) fires before \`scope\_keys\_row\_quota\`/\`scope\_keys\_set\_updated\_at\` (0010) since 'g' < 'r' < 's'.

<!-- lore:019ef10f-c4d9-7073-8940-764d6bb63cc4 -->
* **Sync engine: push/pull architecture, cursors, and known gotchas**: pushOnce()/pullOnce() operate per table with independent cursors: push cursor sync.push.\<table> (last seq), pull cursor sync.pull.\<table> (keyset format \`${updated\_at\_ms}|${row\_id}\`). Quota/transient failures never advance that table's cursor and never block other tables. pushOnce pushes local outbox before pulling remote (local SQLite is source of truth). Outbox pruning uses min cursor across tables with entries (empty tables excluded). Knowledge uses knowledgePushPlan() to coalesce append-only versions to one remote row keyed by logical\_id. Poison rows (over-size) recorded as rejected\_unsyncable (cursor advances); quota rows return stop (cursor unchanged for retry). PAGE=200.

<!-- lore:019eea5e-b325-720b-a9c1-1c526efc0096 -->
* **sync-data.ts seedOutbox: content-aware JS loop — 6 cases, cursor semantics, performance**: PR #868 rewrites \`seedOutbox()\` from single \`INSERT…SELECT\` to JS loop. Push cursor = \`Number(getKV('sync.push.'+table) ?? '0')\`. \`latestUnpushed\` = latest op with \`seq > cursor\`. Six cases: (a) no pending + hash==sync\_state → skip; (b) no pending + hash!=sync\_state → enqueue; (c) latest unpushed=upsert → skip; (d) latest unpushed=delete → enqueue trailing upsert; (e) stale upsert below cursor + content changed → enqueue (THE FIX); (f) stale upsert below cursor + content unchanged → skip. \`seq AUTOINCREMENT\` guarantees strict monotonicity. \`contentHash\` uses \`syncedColumns ∖ HASH\_EXCLUDE\`, sorted, SHA-256 truncated to 16 hex. 🔴 Wrap loop in \`withTransaction\` — N auto-commits cause ~23x slowdown. 🔴 \`seedOutbox\` is only ever reached via \`enableSync\`, never from within another transaction (code comment at line 498). PR #874 hoists \`columns(table)\` outside per-row loop; PR #878 memoizes globally via \`WeakMap\` keyed on \`db()\` instance — \`seedOutbox\` pre-warms cache for all tables, so subsequent \`pushOnce\` sees 0 PRAGMAs.

<!-- lore:019edfc9-c361-72f2-ada3-fc735def69f0 -->
* **sync.ts: local prune vs genuine delete — tombstone suppression required**: sync.ts: local prune vs genuine delete — tombstone suppression required. Hard invariant (🔴): only genuine user/curator-intent deletes generate tombstones and propagate to remote. Local TTL/size eviction (idle prune) must NOT enqueue a tombstone — run prune under a capture-suppressed context (analogous to \`withSyncApplying\`) that drops the DELETE-trigger tombstone and clears \`sync\_state\` only. \`reconcile\` must never see evicted rows as deletes. Evicted rows are re-pullable cache misses, not divergence. Remote is bounded by per-scope quota, not a TTL reaper.

<!-- lore:019edff0-4709-74cc-98eb-ad3bd46163c1 -->
* **sync.ts: push payload, pullOnly tables, and per-table cursor isolation**: sync.ts push payload: \`pushEntry()\` sends \`toRemoteRow(pickSyncColumns(table,row))\` + \`is\_deleted:false\` + \`content\_hash\`/\`revision\` (versioned only). \`scope\_id\` NEVER included — server fills via \`default auth.uid()\`. \`onConflict\` target post-A1: \`\["scope\_id",...idColumns(table)].join(",")\`. \`REMOTE\_ONLY\_COLS\` strips \`scope\_id\`,\`author\_id\`,\`content\_hash\`,\`revision\`,\`is\_deleted\` before local apply. Soft-delete push: \`.update({is\_deleted:true}).match(decomposeId(table,rowId))\`. \`pickSyncColumns\` strips local-only cols (e.g. \`knowledge.promoted\_at\`) to avoid PGRST204. \`SyncTableMeta.versioned\` flag (default true): strip \`content\_hash\`/\`revision\` when false — \`knowledge\_entity\_refs\` is the only non-versioned table; without this fix PGRST204 → 'transient' → push cursor stuck forever.

<!-- lore:019f869d-b722-77f0-8391-a2eb4a732802 -->
* **worker-health.ts succeededSessions Map suppresses first-run degradation false positives**: worker-health.ts tracks a bounded \`succeededSessions: Map\<sessionID, lastSuccessAt>\`, populated inside \`recordWorkerSuccess\` (which otherwise deletes the failure-ladder state entry entirely, so no persistent success flag existed before). \`getDegradationWarning\` suppresses the WARN banner only when a session has NO entry in \`succeededSessions\` AND all recorded failure reasons are credential-class (\`no-auth\`, \`cross-provider\`) — real 'was working, now broken' warnings still fire for mixed-reason or post-success failures. Chose a bounded Map over an unbounded Set to avoid memory growth; entries are evicted by the existing TTL sweep (SESSION\_TTL\_MS=1h), same as failure-ladder state. Fixes the pre-first-authenticated-turn WARN spam (~50+ min) that read as 'Lore is broken' during onboarding (issue #1398, Erica's Slack thread).

### Decision

<!-- lore:019f43a4-4ab3-72a3-a2b6-6d1a1290548c -->
* **Adopted adversarial mutation-testing methodology:**: adopted adversarial mutation-testing methodology: for each of 4 identified risk areas (M1-M4),

<!-- lore:019f4c06-ef9f-7603-bf46-5f1957887cf1 -->
* **Adopted checkout under different path, NULL git\_remote); 4) convergeProjectsByRemote determinism + FK-safety (min-id winner, runs after pullOnce, outside any transaction); 5) git\_remote gate consistency between capture trigger and seedSelect (FK poison risk**: adopted checkout under different path, NULL git\_remote); 4) convergeProjectsByRemote determinism + FK-safety (min-id winner, runs after pullOnce, outside any transaction); 5) git\_remote gate consistency between capture trigger and seedSelect (FK poison risk for remote-backed vs remote-less projects); 6) pull order — projects registered before knowledge/entities/temporal/distillations but after account\_escrow/scope\_keys (needs DEK first); 7) migration 0022 idempotency vs 0020 structure,

<!-- lore:019f5692-6771-7577-965f-da25f1e11f6d -->
* **Adopted three-commit strategy: commit 1 = tooling/config, commit 2 = mechanical reformat + safe autofixes, commit 3 = manual semantic (async-safety) fixes. (PR #1277 final commits: 0993b690 tooling, f11f2e4b reformat+autofix, a3486c5f**: Adopted three-commit strategy: commit 1 = tooling/config, commit 2 = mechanical reformat + safe autofixes, commit 3 = manual semantic (async-safety) fixes.

<!-- lore:019f3974-7c6a-7091-8602-b4ec0bb763a4 -->
* **Chose next task: knowledge\_meta seed-order alignment (self-contained, low-risk, completes value-ranked seed coherence work started with knowledge #1189 and entities #1194) — over #1191b (continuous top-N eviction)**: Completed value-ranked seed coherence work in sequence: #1189 (knowledge seed value-ranking) → PR #1194 (entity-graph seed: entities ranked by knowledge\_entity\_refs count DESC then recency; entity\_relations by recency; entity\_aliases by created\_at DESC) → PR #1196 (knowledge\_meta seed aligned to knowledge's ORDER BY: confidence DESC, COALESCE(last\_reinforced\_at, updated\_at) DESC, logical\_id tiebreak). Deferred #1191b (continuous top-N eviction for remote) as a separate larger/riskier task needing a design doc first (remote eviction, cap discovery, thrash guards, concurrency) — not suitable for autonomous same-session implementation. Follow-up issue #1197 filed for a known non-blocking edge case: a deleted high-confidence entry's orphaned knowledge\_meta row can outrank a live entry's meta row under the cap (self-healing, not data loss).

<!-- lore:019f821b-dbd6-74c8-b138-a378a7d3c1e3 -->
* **Chose PostgreSQL over MySQL for JSONB support**: Chose PostgreSQL over MySQL for JSONB support.

<!-- lore:019f4629-d9d0-7c59-924c-dc7341e57c77 -->
* **Closed #1231 (distillation value-retention): reuse knowledge\_refs + file reads instead of memorizing repo-resident values**: Chose to close PR #1231 (feat: retain exact literal/style values in observations) over building a value-extractor. Rationale: a stored value is a cache of the file — the moment the file changes, every distillation that memorized the old value is confidently wrong. A pointer + file Read is always correct and costs the same tool call. \`knowledge\_refs\` + \`knowledge\_ref\_validity\` already store and validate file:line pointers. The value-extractor would duplicate this and add staleness. Boundary: ephemeral facts (test run counts, stated numbers, decisions) have no file to read → those belong in memory via distillation/knowledge. Code-resident specifics → pointer + read.

<!-- lore:019f821b-dc33-73a0-b54a-ebe01390e550 -->
* **Going with Bun**: Going with Bun because it ships a native SQLite driver; zero deps needed.

<!-- lore:019f869d-b751-7934-863a-2bd644fb1541 -->
* **lore import fails loudly on zero credentials instead of silent '0 created' success**: Design decision (BYK): standalone \`lore import\` deliberately fails loudly (pre-flight guard) rather than silently completing with '0 entries created' when no usable credential exists — silent success previously trapped users (Kjaer's GitHub issue #1398, Erica's Slack onboarding thread) into thinking import worked when it didn't. Complementary rule: accepted import offers must NOT pre-record the agent as 'already imported' until a real extraction actually completes (\`chunksAnswered > 0\`), to avoid falsely suppressing future auto-import retries. Implemented in commit e4216a34 (PR #1406).

<!-- lore:019f5b56-d717-7acf-82cd-ecfc75ca4871 -->
* **Migrated to TG): test failed at sync**: migrating to TG): test failed at sync.

<!-- lore:019f0103-15cb-708f-9cff-f5c8f060d37a -->
* **Migrated to Vitest in May 2026 — 312ms vs 30s startup time cited as reason**: migrated from Mocha + Chai + ts-node to Vitest in May 2026 — 312ms vs 30s startup time cited as reason.

<!-- lore:019f869d-b6ed-7367-97e2-b28439596ccf -->
* **Onboarding error messages gate provider-naming on cfg.model != null**: Decision: in import.ts/import-auto.ts no-credential and provider-mismatch messages, only name a specific provider (e.g. 'anthropic') when \`modelExplicit = cfg.model != null\` is true; otherwise use generic provider-neutral phrasing leading with \`lore run\`. Rejected alternative: always naming \`defaultModel.providerID\` — looks correct since it's the configured default, but \`cfg.model\` is normally unset and silently defaults to hardcoded "anthropic", so this was actively misleading Copilot/OpenRouter users who have no Anthropic key to export (root cause of GitHub issue #1398 and Erica's onboarding friction).

<!-- lore:019f18a1-0f46-7d96-9aa5-151ac33868ec -->
* **Reverted to upstream sqlite-vec@0.1.9 from vendored 0.1.10-alpha.4 — AVX2 buys nothing on FLAT vec0**: Chose upstream \`sqlite-vec@0.1.9\` over vendored \`@loreai/sqlite-vec-vendored\` (0.1.10-alpha.4 with AVX2 + DiskANN + rescore flags) because FLAT vec0 cosine scan is memory-bandwidth-bound — 7-run pinned benchmark (106K vectors × 768-dim, single-core) showed 0.1ms p50 difference (dead heat). DiskANN rejected: partition keys unsupported, metadata filter columns unsupported, insert cost ~48ms/vec (106K backfill ≈ 85 min), recall@10 ~0.82 (approximate), cannot scope search to project/session. Vendored fork also introduced \`INSERT OR REPLACE\` vec0 support that 0.1.9 lacks, requiring a portable DELETE+INSERT upsert workaround. PR #1058 (\`25c9ea7e\`) reverts PR #1038. Upstream 0.1.9 linux-x64 prebuilt is pure scalar SSE2 baseline — zero AVX, zero cpuid dispatch — so AVX2 patch buys nothing. FLAT vec0 confirmed: inserts ~0.122ms/vec; exact recall (recall=1.0); supports partition keys + metadata columns.

<!-- lore:019efa1b-96f8-7702-8ddb-ccbc2fece37e -->
* **sqlite-vec: native extension decision + DiskANN impracticality + rowid BigInt gotcha**: Chose native extension + JS fallback over WASM-first for sqlite-vec. WASM build targets browser sqlite-wasm engine only — using it in node:sqlite/bun:sqlite would require replacing the entire SQLite engine, slowing ALL queries. Drop-in \`vec\_distance\_cosine()\` over existing BLOB columns chosen for PR #967 scope; \`vec0\` virtual table migration rejected as first step. Portability strategy: keep embeddings as F32 BLOB columns (byte-compatible with libSQL's \`F32\_BLOB\`); when hosted gateway becomes real, add \`driver.libsql.ts\` behind existing \`#db/driver\` map backed by \`vector\_top\_k\` — no data migration, no caller changes.

<!-- lore:019f821b-dc02-78a3-ad92-ad6c671702b6 -->
* **Switched from create-react-app to Next**: Switched from create-react-app to Next.

<!-- lore:019f869b-5a17-7ae0-a933-1993f8584217 -->
* **Switched from plan to build**: switched from plan to build.

<!-- lore:019f820f-3d9c-778a-b648-d23ebce968f6 -->
* **Switched from plan to build (file changes/shell commands now permitted)**: switched from plan to build (file changes/shell commands now permitted).

<!-- lore:019f8228-4e34-708b-9b09-4867fef00937 -->
* **Switched from plan to build (no longer read-only) via system reminder**: switched from plan to build (no longer read-only) via system reminder.

<!-- lore:019f8615-13c6-78f9-90f8-fff465d4b991 -->
* **Worker model for real openai: pin gpt-5.6-luna, not lineage 'closest-cheaper-tier'**: Chose to hardcode WORKER\_DEFAULTS\['openai'] to family 'gpt-luna' (gpt-5.6-luna) over reusing findCheaperSameProviderModel's lineage-aware 'closest cheaper tier' logic (#1407). Rejected because gpt-5.6-sol ($5/M), gpt-5.6-luna ($1/M), gpt-5.4-mini ($0.75/M), and gpt-5.6-terra ($2.5/M) all share lineage 'gpt' — 'closest cheaper tier still under session cost' would pick gpt-5.6-terra (most expensive tier still cheaper than $5 sol), not luna. User explicitly chose 'pin luna explicitly' when presented this conflict. Implemented via resolveNewestInFamily (family='gpt-luna'), same mechanism as other WORKER\_DEFAULTS entries, not findCheaperSameProviderModel.

### Gotcha

<!-- lore:019f869d-b781-754c-bae0-5ca0a697fe17 -->
* **\_loreBackup sidecar migration lives in setup.ts + setup-backup.ts, not setup-io.ts**: Trap: looking for a file literally named \`setup-io.ts\` for the \`\_loreBackup\` unrecognized-key crash fix (PR #1297/#1299) — the name is implied by test file \`setup-io.test.ts\` but no such source file exists. Fix: logic is split — \`setup-backup.ts\` holds pure helpers (\`LORE\_BACKUP\_KEY="\_loreBackup"\`, \`readLegacyJsonBackup()\`), while \`setup.ts\` holds IO/handlers (\`jsonBackupPath()\`, \`loadJsonSetupBackup()\` — never throws, \`persistJsonSetupBackup()\` — never overwrites the true original). This fix landed on main/nightly but is NOT in stable 0.37.0, so stable users still hit the opencode-refuses-to-start crash until a new stable release ships (deferred item).

<!-- lore:019f1fde-f198-76a6-a8e1-1ba44ab8cf1d -->
* **backfillTemporalEmbeddings idle-gate: session-activity window starves backfill under sustained load**: Trap: gating temporal backfill on session activity (any session active within \`idleTimeoutSeconds \* 1000\` ms) looks correct — avoids competing with live traffic. Fix: on a busy machine with 5 active sessions and 142 turns in ~18 min, the gate never opens; cursor never advances during 8+ min monitoring window. The session-activity window is too coarse — it parks backfill even when the embed worker is idle between requests. Fix (PR #1111): gate on \`recallEmbedsInFlight() > 0\` instead. This is selective — backfill progresses during recall's sub-second gaps between requests. Under active multi-session load post-deploy: park/resume cycles seconds apart, ~16 msgs/min throughput confirmed in logs. Inherent limitation: sustained back-to-back recall with no gaps still starves backfill, but this is accurate (shared single worker) rather than a false signal.

<!-- lore:019f1c21-e737-76f1-94ad-d8e2e6db16b2 -->
* **backfillTemporalEmbeddings: distilled messages retain embeddings — full corpus scope, not just undistilled tail**: Trap: assuming distilled messages have their embeddings NULLed (stale JSDoc on \`embedTemporalMessage\` claims this) looks correct — distillation compresses old messages. Fix: \`markDistilled()\` only sets \`distilled=1\`; it never clears the embedding column. \`deleteEmbeddings\` is never called during distillation. Consequence: the temporal rechunk backfill must walk the ENTIRE embeddable corpus (220K+ messages on a real install), not just the ~34K undistilled tail — 6.5× larger scope. The stale JSDoc comment was corrected in PR #1087. Always verify by checking \`markDistilled()\` in temporal.ts — it is a simple UPDATE with no embedding side-effects.

<!-- lore:019ef10f-5e1c-70ee-97c2-cd2e0836bd28 -->
* **Bedrock: atob() mojibakes multi-byte UTF-8 — use Buffer.from().toString('utf8')**: Trap: \`atob(base64)\` looks correct for decoding Bedrock event-stream \`bytes\` field. Fix: \`atob()\` yields a Latin-1 binary string — multi-byte UTF-8 sequences (accents, CJK, emoji, smart quotes) are corrupted. Proven: \`"café ☕ 你好"\` → \`"cafÃ© â ä½ å¥½"\`. Use \`Buffer.from(bodyJson.bytes, 'base64').toString('utf8')\` — \`Buffer\` is a global in Node.js and Bun, no import needed. Tests missed it because all \`chunkFrame\` calls in \`bedrock-stream.test.ts\` used ASCII-only text (\`"hi"\`, \`"Hello"\`, \`"split"\`). Fixed in \`bedrock-stream.ts\` (PR #898).

<!-- lore:019ef10f-5e3c-741a-9a7a-2a3e119821ac -->
* **Bedrock: stream field in request body causes validation error — endpoint controls streaming**: Trap: adding \`stream: true|false\` to the Bedrock request body looks correct (mirrors Anthropic API). Fix: Bedrock does NOT accept a \`stream\` field in the body — streaming is controlled by the endpoint URL (\`/invoke-with-response-stream\` vs \`/invoke\`). Including \`stream\` causes Bedrock to reject with a validation error. \`buildBedrockRequestBody\` must never set this field. Confirmed by user directive and test assertion (\`'stream' in capturedBody === false\`).

<!-- lore:019f858d-4008-7fcd-8554-015db3988776 -->
* **bspatch.ts (delta-upgrade): format, gotchas, and untested branches**: TRDIFF10 format: 32B header (magic + controlLen/diffLen/newSize as offtin-encoded i64) then zstd(control)|zstd(diff)|zstd(extra). OldReader abstracts fd-backed (pread+1MiB cache) vs in-memory sources; multi-patch chains keep intermediates in JS heap, hashing/persisting only the final binary. SWAR Uint32Array diff-add is 4x faster than naive — never port to BigUint64Array, it has a carry bug diverging on ~52% of byte values. Gotchas (PR #1436): parsePatchHeader's \`newSize\` was attacker-controlled with no bound, risking OOM before SHA verification — fixed with a 2GiB MAX\_OUTPUT\_SIZE cap. The committed test file is only 4KB (one cache block), so FileOldReader's cache-bypass, block-boundary refill, and negative-oldpos zero-fill paths are load-bearing but untested — mutation testing broke all three silently. Add a >1MiB boundary-straddling + backward-seek + negative-oldpos test. Final binary is always SHA-256-verified; failure falls back to full download.

<!-- lore:019ee719-946b-7dde-99f8-56c19833e6cc -->
* **bun:sqlite returns null (not undefined) for missing rows — existence checks must use count query**: Trap: \`db.prepare('SELECT \* FROM t WHERE id=?').get(id) !== undefined\` looks correct for existence checks (standard JS pattern). Fix: bun:sqlite driver returns \`null\` (not \`undefined\`) for no-row results — \`!== undefined\` always returns \`true\`. Use a count query: \`db.prepare('SELECT COUNT(\*) as n FROM t WHERE id=?').get(id).n > 0\`. Also: \`getKV()\` returns \`null\` for missing keys — use \`?? '0'\` not \`|| '0'\` when reading push cursors. Also: \`decideCacheStrategy\` returns \`confident:false\` when pricing or body tokens are non-finite/non-positive; \`confident:true\` otherwise. \`shouldHoldPrefixWarm(null,…)\` → \`undefined===true\` → false — null econ is safe fallback.

<!-- lore:019ee670-8c0c-76b2-b0a1-0c411590477e -->
* **cache-warmer shadow mode: ROI/write-efficiency guards must come AFTER shadow eval block**: cache-warmer shadow mode: ROI/write-efficiency guards must come AFTER shadow eval block. Trap: shadow eval block placed after ROI guard (\`hitRate < MIN\_SESSION\_HIT\_RATE\`) — large/growing sessions that fail those guards are never measured. Fix: hoist survival computation (\`survivalFunction\`, \`breakFraction\`, \`pSessionFinished\`, \`pReturns\`, \`costThreshold\`, \`maxProfitableCycles\`) and shadow eval block ABOVE the ROI and write-efficiency guards. Correct placement: after messageCount/inputTokens/disabled checks, before ROI and writeEfficiency guards. Behavior-neutral reorder.

<!-- lore:019f85b3-0baa-7cf0-8f66-75f0bdc6fe71 -->
* **codex/openai-responses SSE streaming: true-streaming path, wire-form gotchas**: streamResponsesPassthrough true-streams Codex/openai-responses events live with keepalive pings when: no recall tool active (checked via the FINAL post-injection tools list, not the original request), client uses Responses API, no warning state. Otherwise falls back to accumulateResponsesSSEStream+finalizeWithRecall, draining the ENTIRE upstream before responding — codex clients got zero bytes until generation completed under the old buffer-then-send default, causing real hangs (PR #1435); buffering is now conditional. Forwarding must skip untyped/\[DONE] SSE frames — forwarding them corrupts the wire form for downstream parsers; reserializeSSE synthesizes \`event: message\` for untyped \`data:\` lines. Test gotcha: asserting the event-type string merely \`toContain\`s in output is insufficient (it also appears in the JSON payload's \`type\` field) — assert the literal wire form \`'event: \<type>\ndata:'\`. Mutation-verified: checking original pre-injection tools instead of final silently breaks recall interception undetected.

<!-- lore:019f869d-b6ba-7604-a094-c4a33ab43149 -->
* **compact-endpoint.test.ts 'bad port' failure is a known flake**: Trap: a full-suite failure in compact-endpoint.test.ts with a 'bad port' fetch error looks like a real regression when it appears right after unrelated changes. Fix: it's a pre-existing flake unrelated to code changes — confirmed twice by isolated re-run passing cleanly while the full suite intermittently fails on it. When this specific test fails in a full run, isolate and re-run it alone before treating it as a regression; don't chase it as caused by your diff.

<!-- lore:019ecab6-f928-7927-a39e-d601fb850af6 -->
* **computeMaxTokens: EMA-based floor ignores thinking budget, causing silent agent loop exit**: \`computeMaxTokens\` EMA floor ignores thinking budget → silent agent loop exit. \`computeMaxTokens\` (pipeline.ts) floors \`max\_tokens\` at \`max(8192, 3×outputEMA)\`. Anthropic counts \`max\_tokens\` as combined thinking+visible budget. Model consumes entire 8192 on reasoning → \`finish:"length"\` → no text/tool part → agent loop exits silently. Fix: pass \`thinkingBudget\`; set \`baseFloor = thinkingBudget + THINKING\_OUTPUT\_HEADROOM (8192)\`, clamped by \`modelOutput\`. Gate: \`if (!isCC)\` — CC clients excluded; opencode does NOT send \`x-claude-code-session-id\` so fix applies. EMA not corrupted by thinking truncation (Anthropic includes thinking tokens in output\_tokens).

<!-- lore:019f1e66-a002-7d12-872b-04e7900bc8c1 -->
* **copyBlobsToVec0: stale-dimension blobs abort cutover — dimension guard required**: Trap: copyBlobsToVec0: stale-dimension blobs abort cutover — dimension guard required. Fix: add length(embedding) = dim\*4 guard to all 4 INSERT queries; thread dim: number param. Skip-in-copy ≡ remove+recreate: base embedding column is dropped after copy; next backfill regenerates from source text via missingEmbeddingSql.

<!-- lore:019efb79-a067-7947-b900-e8fd56134b27 -->
* **cost-bulk-queries.test.ts: \`not.toContain('SCAN t')\` is fragile — misses covering scans**: 🔴 Directive (June 24 2026): use \`not.toMatch(/SCAN t(?! USING)/)\` to reject heap scans in SQLite EQP tests — never bare \`not.toContain("SCAN t")\`. Rationale: SQLite reports index-only walks as \`SCAN t USING COVERING INDEX …\` — bare contains false-fails on valid covering plans. Negative lookahead passes covering scans and kills true heap scans. Also: deliberate decision NOT to add \`(created\_at, session\_id)\` index — SQLite planner always prefers session-ordered \`idx\_temporal\_session\` / \`idx\_distillation\_session\` scan for token-sum, distillation, and recent-session aggregates; \`(created\_at, session\_id)\` index is pure write amplification.

<!-- lore:019efb40-0000-7000-9000-000000000003 -->
* **creditWarmupHit three guards: paid (Bug A) + within-TTL + actually-read (Bug C); credit is binary not pro-rata**: \`creditWarmupHit(warmup, sinceWarmupMs, ttlMs, cacheReadTokens)\` attributes a warmup hit ONLY when ALL hold: (A) the session paid (\`totalWarmups>0 && lastWarmupRefreshTokens>0\` — \`lastWarmupAt\` alone rides along in restored blobs → phantom); within TTL (\`sinceWarmupMs < ttlMs\`); (C) the returning turn ACTUALLY READ the cache (\`cacheReadTokens > 0\`). Bug C fix: a turn that pivoted context / used a different tool chain / found the cache evicted reports \`cache\_read=0\` → no savings; without the gate, warmupHits/creditedTokens inflate and warp the ROI analysis gating future warming. Call site passes \`usage.cacheReadInputTokens ?? 0\` (same signal the adjacent 1h-TTL-savings branch gates on). The warmup is consumed (markers zeroed) BEFORE all three guards, so a no-hit still can't re-credit later. \`creditedTokens\` is the warmed prefix (\`refreshTokens\`, Bug B), NOT the returning read. Known residual: the read gate is BINARY — a partial read (0 < read < refreshTokens, e.g. breakpoint shift) still credits the full refreshTokens. Call-site threading (pipeline postResponse warmup-hit branch) is NOT seam-tested — hardcoding the 4th arg survives all warmup pipeline tests (tracked follow-up).

<!-- lore:019f86b6-b9f7-7c4f-b096-3a59ab5f59d7 -->
* **edit tool 'could not find oldString' from swapped oldString/newString args**: Trap: when \`edit\` fails with 'Could not find oldString in the file... must match exactly', the instinct is to re-verify whitespace/exact match in the same oldString text. Observed root cause: oldString and newString had been swapped in the call (the replacement text was passed as the search text). Fix: before retrying, re-check which argument is the 'find' text vs the 'replace' text — don't just re-copy the same (wrong) strings. This failure mode ('could-not-find-oldstring') recurs frequently (28 sessions, 60 occurrences) — check argument order first, then whitespace/exact-match.

<!-- lore:019f8684-beb1-70ca-8686-0203ff413c3f -->
* **engram export: read from file, not stdout, in lore import**: Trap: reading Engram's export via stdout looks fine for other import sources (mem0 storage.sqlite reads work this way). Fix: Engram's export can be binary/non-JSON, which gets corrupted when piped through stdout — issue #1398 (fixed in PR #1438, commit 210a911a). \`packages/core/src/import/structured-sources.ts\` now reads the Engram export from a file path instead. Any new Engram import/export handling must read from a file, never stdout.

<!-- lore:019f853b-4ce9-767c-9025-7e6078b8bef5 -->
* **eval/ excluded from tsconfig typecheck — invalid enum literals compile clean**: packages/core/tsconfig.json include=\["src","test","script"] excludes eval/; CI's typecheck is \`pnpm -r run typecheck\` (per-package \`tsc --noEmit\`), so eval/ files are NEVER typechecked by CI even though root tsconfig.json includes packages/\*/eval. Trap: an invalid literal like \`"no-memory"\` (not a member of the \`BaselineMode\` union) compiles clean in eval/scenarios/decision-recall.ts and is silently dropped by harness.ts's \`.includes(b)\` filter — looks type-safe but was never checked. Compounded by a vacuous test asserting mere array-membership of the string instead of validity against \`ALL\_BASELINES\`. Fix applied: literal → "raw"; test hardened to check every listed baseline is a valid BaselineMode. Adding eval/ to core tsconfig surfaces 60 unrelated pre-existing errors (ImportMeta.dir, bun:sqlite) — deferred to issue #1434 rather than fixed inline.

<!-- lore:019f19db-f73c-74d1-865d-116cf01c0100 -->
* **gateway re-compression: protocol-only predicate trusts same-protocol cross-host routes — origin check required**: Trap: comparing only \`effectiveProtocol !== ingressProtocol\` to detect auto-cross-routing looks correct — protocol translation is the obvious signal. Fix: same-protocol re-routes to different hosts (e.g. bare openai client → \`deepseek-chat\` → \`api.deepseek.com\` ≠ \`api.openai.com\`) are also distrusted. The merged PR #1059 predicate was protocol-only; PR #1064 adds \`|| effectiveOrigin !== ingressOrigin\`. Combined predicate: \`autoCrossRouted = !hasUpstreamUrlOverride && !hasProviderOverride && (effectiveProtocol !== ingressProtocol || effectiveOrigin !== ingressOrigin)\`. Mutation A (remove origin clause) → 4 tests RED: 3 unit + 1 MockAgent e2e showing zstd replayed to foreign host.

<!-- lore:019f8684-bf0e-7b4e-9cdf-462c402f4b89 -->
* **gh pr merge warns 'could not determine current branch' under jj — cosmetic**: When merging a PR from a Jujutsu (jj)-managed workspace, \`gh pr merge\` may print "could not determine current branch: failed to run git: not on any branch". This looks like a merge failure but is a cosmetic side-effect of jj's git backend (no named branch/detached-HEAD-like state) — the merge itself succeeds. Don't treat this warning as a failure signal; confirm actual merge success via \`gh pr view --json mergedAt\` instead of the merge command's stderr.

<!-- lore:019eea52-a1f7-757a-b0ae-6b749bbcda18 -->
* **gradient.ts compression stages: rawFrac applied to full usable — escalating layers inflate window past l0cap**: Trap: COMPRESSION\_STAGES rawFrac (layer-2=0.5, layer-3=0.55) applied to full \`usable\` (~800K on 1M model) → layer-2 raw budget ~400K, larger than the ~200K l0cap that triggered compression AND larger than layer-1 window; escalating GREW the window until unrecoverable 400. Fix (PR #872, merged 2026-06-21T14:18:47Z, squash \`4d7910aa\`): introduce \`stageBudgetCeiling = maxLayer0Tokens > 0 ? Math.min(usable, maxLayer0Tokens) : usable\`; \`stageBudgetUsable = Math.min(usable, stageBudgetCeiling)\`; apply to escalated stages (layers 2+) only. Layer-1 base \`rawBudget = usable\*0.4\` must NOT be clamped. \`isEscalated = stageLayer > 1\`. Also clamp escalated-stage distilled budget. Confirmed in production logs: post-deploy layer-2 ratio dropped from ~0.499 (unclamped usable×0.5) to ~0.287–0.321 (clamped cap×0.5). M1–M4 mutant kills documented in test suite.

<!-- lore:019f86b6-b9ca-7731-93d0-1841509d9660 -->
* **import-auto.ts deferred import permanently lost for non-default provider**: Trap: \`job()\`'s usability check \`hasWorkerKey || resolveAuth(undefined, defaultModel.providerID) != null\` looks correct — it checks whether some credential exists. Fix: \`defaultModel.providerID\` defaults to hardcoded 'anthropic' when \`cfg.model\` is unset; a user authenticated via a non-default provider (e.g. Copilot) fails this check despite having usable auth, falling into the generic skip message. \`flushPendingImport()\` (pending-import.ts) is one-shot — clears \`pending\` before running — so the import was permanently lost, breaking the promised 'send one message and it will import automatically'. Fix: on skip, call \`registerPendingImport(job)\` to re-register for a later usable-credential turn. Rejected alternative: reroute extraction to \`authedProviderID\`'s model — rejected because the llm client is already pinned to \`defaultModel\` at creation and picking a correct modelID for an arbitrary provider is non-trivial.

<!-- lore:019f8651-ed76-76b7-a054-d5a9de02591e -->
* **import.ts/import-auto.ts: error handling & provider-neutral messaging**: import.ts's CLI import-source error handler only prints to console — never calls Sentry's captureException; don't check Sentry for CLI import failures, trace the code path directly. \`cfg.model\` is normally unset (Lore infers the model from the first client request); track \`modelExplicit = cfg.model != null\` and only name a specific provider in error/skip messages when explicitly set — otherwise use generic wording ('\<key for your provider>'). The old \`defaultModel ?? {providerID:'anthropic'}\` fallback wrongly told non-Anthropic users (Copilot/OpenRouter/MiniMax) to export an anthropic key; fixed via the modelExplicit check in import.ts and import-auto.ts. Tests need a real \`.lore.json\` on disk to exercise the explicit-provider branch. Separately: accepting the 'Import knowledge?' prompt isn't recorded immediately — deferred until the first authenticated turn fires flushPendingImport and may never run; only completion records it (declines record immediately).

<!-- lore:019ef35a-01ce-7c91-a77a-e81e9f1f537b -->
* **isVerifierCall: bare \`ci\` keyword matches \`npm ci\` (clean install) — false positive corrupts knowledge confidence**: Trap: \`(?:run\s+)?(?:test|...|ci|...)\` in \`VERIFIER\_LEADING\` looks correct — \`ci\` is a valid script name. Fix: \`npm ci\` (clean install, not a verifier) matches the optional-run group → false positive → confidence penalty. Fix (PR #927): move \`ci\` and other script keywords (\`verify\`, \`validate\`, \`e2e\`, \`coverage\`, \`spec\`) into a dedicated branch requiring explicit \`run\`/\`exec\`/\`dlx\` prefix: \`(?:npm|pnpm|yarn|bun)\s+(?:run|exec|dlx)\s+(?:test|ci|verify|...)\b\`. Bare \`npm ci\` no longer matches; \`pnpm run ci\` still does. \`ci\` retained for \`make\`/\`just\`/\`task\`/\`rake\` targets (always explicit). Confirmed via 51-case empirical test suite in PR #927 adversarial review.

<!-- lore:019f81bf-c8d1-7c66-93d4-b19abc62e7b2 -->
* **jj (Jujutsu) colocated repo gotchas: snapshotting, rebase, PR creation**: jj only snapshots the working tree into \`@\` when a \`jj\` command runs — Edit/Write changes sit un-snapshotted indefinitely otherwise, and raw git ops (checkout, reset --hard, clean -fd) in the same tree can destroy them with zero recovery (no commit/blob/reflog ever existed). Always run \`jj describe -m "..."\` or \`jj commit\` early/often. Also check \`jj diff --stat\` in the default workspace \`@\` before editing — it may hold unrelated pre-existing WIP; if so, use an isolated workspace/bookmark instead (recover via \`jj op log\`/\`jj op restore\` if mixed in accidentally). During \`git rebase --continue\` after a conflict, set \`GIT\_EDITOR=true\` to avoid 'EDITOR unset' failures; rebase leaves HEAD detached afterward (normal) — force-push the branch ref explicitly. \`gh pr create\` fails with 'could not determine current branch' here (HEAD detached) — pass \`--head \<branch-name>\` explicitly.

<!-- lore:019f867a-c2d5-71e4-a7f3-c45c91b857ca -->
* **jj review worktree goes stale mid mutation-test — run \`jj workspace update-stale\`**: Trap: during mutation-kill verification in an isolated jj worktree, a hash check or grep for the mutant marker can look wrong (mutant appears to still be present after restore, or a new commit's changes seem missing) — looks like the restore failed. Fix: the review worktree's working copy can go stale relative to the actual commit tree (jj doesn't always auto-sync a second worktree when the source branch advances). Run \`jj workspace update-stale\` first, then re-check hash/grep — status should read 'clean'/'no changes' and the file hash should match the pre-mutation baseline exactly. Always update-stale before trusting any verification result in a worktree you aren't actively committing in.

<!-- lore:019f8502-ab74-7875-8613-0949dab186f9 -->
* **loreai format:check runs oxfmt, not Biome**: Trap: assuming Biome is the formatter/linter for a package.json 'Format check' CI failure looks right — Biome is a known dependency in the broader loreai/opencode ecosystem. Fix: check package.json scripts first — \`lint\` = \`oxlint --type-aware\`, \`lint:fix\` = \`oxlint --type-aware --fix\`, \`format\` = \`oxfmt\`, \`format:check\` = \`oxfmt --check\`. The biome binary may not even be present at node\_modules/.bin/biome for a given package. To reproduce/fix a CI 'Format check' failure, run \`oxfmt\` locally (not \`biome check --write\`). Confirmed on PR #1431: oxfmt reformatted 3 files in ~0.6s across 634 files; format:check then passed. Also: pnpm install WARN lines about missing bin ENOENT (e.g. lore-gateway/lore dist/bin.cjs) are noise, not the actual CI failure — check the actual failing step name first.

<!-- lore:019f8532-aee7-7fd9-89cc-f3f3db0bb654 -->
* **ltm.ts forSession: empty-knowledge fast path + isPreferenceOnly both skip context-source fold**: Two related bugs in forSession() (PR #1432, fixes #961): (1) early return at ~line 2488 fired before the context-source fold at ~line 2731, silently dropping contextSources injection whenever the \`knowledge\` table is empty — fixed to only return early when \`includeContextSources\` is unset. (2) \`isPreferenceOnly\` fast path also bypassed the fold. Both mutation-verified in ltm-context-sources.test.ts. MATRIX-V5 validation (N=5, single-long cap200) confirms the fix generalizes across all 3 tested model families: DeepSeek and MiniMax-M3 both show identical clean contrast (lore 4/4 probes/0 compactions ×5, nolore 0/4 probes/5 compactions ×5). Sonnet is noisier on the nolore arm (probes 0,0,4,3,0 — avg ~1.4/4, 7 compactions every run) because the stronger model sometimes self-recovers facts from its own compaction pass, but lore stays deterministic 4/4 with 0 compactions — still a clean reliability+efficiency win. Not an isolated smoke test or model-specific ceiling.

<!-- lore:019f8662-dc90-7f83-92d8-1e28f87aecd3 -->
* **mem0.ts defaultEmbeddedDirs used process.env.HOME — undefined on Windows**: Trap: using \`process.env.HOME\` for mem0's embedded-storage default directory looks correct — it matches Unix convention, and mem0Source.detect() elsewhere already used os.homedir(), so both seemed consistent. Fix: process.env.HOME is undefined on Windows, so detect() succeeded (uses homedir()) but resolveMem0Doc failed with 'No mem0 data found' (used HOME) — a silent split-brain between two functions that should agree. Fixed in packages/core/src/import/sources/mem0.ts by importing \`homedir\` from node:os and using it in defaultEmbeddedDirs(). Flagged post-merge by Seer/sentry\[bot] on PR #1417 (HIGH severity) — missed by human + adversarial review pre-merge. Fix included exporting defaultEmbeddedDirs as a testable seam and adding a regression test that unsets HOME and confirms ~/.mem0 still resolves via os.homedir().

<!-- lore:019efc12-5bea-700b-87dd-615866d86741 -->
* **meta-distillation rewrites messages\[1] each turn — busts warmup prefix, turns partial warmups into $2–3 writes**: Trap: meta-distillation running while cache is warm looks safe — it's background work. Fix: meta archives gen-0 rows and creates a gen-1 row, rewriting the synthetic distilled prefix at \`messages\[0/1]\` on the next turn. That early-message rewrite is a real prompt-cache bust. Confirmed in production: session \`1EQ2VGLS1de9zcIH\` turn=76 showed divergence at \`messages\[1].content\[0].text\`, reason='distilled conversation prefix changed (meta-distillation rewrite)', forcing 100K+ token recreation per turn. Code comment invariant: 'Never run meta-distillation while the conversation cache is warm.' Idle-time meta in idle.ts remains enabled because the cache is already cold there. All distillation call sites in pipeline.ts pass \`skipMeta: true\` when cache is warm.

<!-- lore:019f0134-e838-7cb6-ab45-8d517460b17b -->
* **pattern-echo.ts throttle: arm cooldown unconditionally, not only after successful ltm.create**: Trap: arming the 10-minute cooldown (\`PATTERN\_COOLDOWN\_MS\`) only after a successful \`ltm.create()\` call in \`pattern-echo.ts\` looks correct — why throttle if nothing was created? Fix: if embed+search runs but \`ltm.create()\` fails or returns no-op, the cooldown is never armed → embed+search runs again on every subsequent distillation, burning compute. Additionally, rate-limited segments were skipping the embed entirely, creating a latent recall gap (segments never became vector-searchable). Fix (PR #1009): reorder \`\_detect()\` so embed+store (job 1) runs unconditionally BEFORE the rate-limit check; arm cooldown unconditionally immediately after the check passes (line 128), not inside the \`ltm.create()\` try block. Two bugs fixed: (a) no-pattern outcome never armed cooldown; (b) rate-limited segments skipped embedding.

<!-- lore:019f8666-84e9-77aa-81a9-c9f8ecef6a8a -->
* **pnpm --filter @loreai/gateway test silently no-ops (no test script)**: Trap: \`pnpm --filter @loreai/gateway test -- \<file> --run\` looks like the right way to scope a test run to one package. Fix: packages/gateway/package.json has no \`test\` script (only typecheck/build/bundle/build:binary/start) — pnpm --filter silently no-ops on a missing script, exiting 0 with zero output, faking a pass. Root package.json defines \`"test": "vitest run"\`. Always run \`node\_modules/.bin/vitest run \<file>\` from repo root instead, and confirm the output shows real pass/fail counts (e.g. 'X passed') before trusting any gate result. Discovered while gating PR #1439 review.

<!-- lore:019ef645-249a-7099-9b79-a286babbb764 -->
* **pruneOversized uses clamped confidence — CRDT hysteresis leaves raw value > 1.0 unzeroed**: Trap: \`pruneOversized\` reads \`confidence\` from \`knowledge\_current\` (materialized, clamped to \[0,1]) and applies \`-confidence\` delta — looks correct for zeroing. Fix: CRDT deltas accumulate on \`base\_confidence\`; raw value can exceed 1.0 while materialized stays clamped at 1.0. Applying \`-1.0\` delta leaves raw > 0 → entry not actually pruned, GC fails silently. Fix: fetch raw unclamped value (\`base\_confidence + SUM(deltas)\`) and apply negative delta equal to that raw sum. Confirmed in PR #1126 fix commit \`36173a7\`.

<!-- lore:019efa17-e60f-7a38-9bec-b1770ecb3aa6 -->
* **references.ts: unresolved refs resolve to 'unknown' never 'missing'; code-span fusion trap**: Trap: resolving an unrecognized \`make \<target>\` or \`yarn \<script>\` as 'missing' looks correct — the target doesn't exist. Fix: commands extracted from code spans may reference targets in repos the gateway can't inspect. 'missing' triggers a confidence penalty; 'unknown' is a neutral no-op. Invariant: unverifiable refs must NEVER penalize — silent skip is always preferable to a false penalty. Same rule applies to symbol refs: absent symbol → \`"unknown"\` (NEVER \`"missing"\`); penalty only fires when \`wasSymbolPresent()\` confirms prior presence. \`DirectFsResolver\` and \`SyntheticProbe\` both return 'unknown' for unresolved make/yarn/symbol refs. PR #939 (file/command refs), PR #988 (symbol refs). PR #973: file-reference resolution case-insensitive; case-collision → \`"unknown"\` (neutral).

<!-- lore:019eeb34-c8ab-7321-98ae-f8cbd3953ad4 -->
* **resetPipelineState: fire-and-forget background promises write to new harness DB — test flake trap**: resetPipelineState: two-phase teardown required — stop idle scheduler and drain background work BEFORE swapping db() singleton. (1) Correct order: stopIdleScheduler() + null it → await drainBackground(5000) → await boundedSettle(\[...inFlightBackground], 5000) → llmClient.shutdown → swap/clear state. Double stopIdleScheduler is safe (fast path skips top block; later block is no-op if already nulled). Trap A: stopping after llmClient.shutdown lets idle ticks schedule new runBackground during drain. (2) Trap B: fire-and-forget background promises write to new harness DB — urgent/incremental distillation, saveSessionTracking INSERT OR IGNORE create phantom rows in next harness (~1/3639 flake, issue #885). Fast path skips drain; production shutdown uses fast to avoid Ctrl+C hang. \`boundedSettle\` is a shared helper in \`background-limiter.ts\`.

<!-- lore:019f1e66-a04d-722c-822f-0cb794b9ebe5 -->
* **runStartupBackfill 'scheduled' log absent when only temporal backfill is pending**: Trap: absence of 'embedding backfill scheduled: N knowledge + M distillations pending' log looks like \`runStartupBackfill\` didn't run. Fix: the scheduled log fires ONLY if \`pendingKnowledge + pendingDistillations > 0\` — temporal backfill pending count is NOT included. Similarly, ~2,410 vec-missing distillations that are archived gen-0 are excluded by \`archived=0\` predicate, so they don't trigger the log. The final coverage log prefix is \`'embedding startup:'\` NOT \`'coverage:'\`. \`STARTUP\_BACKFILL\_DELAY\_MS = 2\_000\`. To confirm temporal backfill is running, check for \`lore:temporal\_rechunk.cursor\` KV key or the \`'temporal re-chunk: N messages to scan'\` log line (added in PR #1104).

<!-- lore:019f61b7-d498-7883-8c36-b1f4ab9d3d2e -->
* **SessionCostSnapshot inputTokens and outputTokens are required**: Trap: forgetting to initialize inputTokens and outputTokens in SessionCostSnapshot looks correct. Fix: always initialize with 0. User made required inputTokens and outputTokens fields in SessionCostSnapshot type and added test verifying round-trip.

<!-- lore:019ef02c-f197-7282-8eb5-13a894b0cd15 -->
* **startGateway reuse probe: EADDRINUSE-gated probe misses non-overlapping interface binds**: Trap: placing existing-gateway reuse probe inside \`EADDRINUSE\` catch looks correct — port conflict is the only reason to check. Fix: when new instance binds non-overlapping interface (existing on \`127.0.0.1\`, new on \`127.0.0.2\`), bind succeeds, catch never fires, second redundant gateway starts. Fix (PR #908/#920): \`firstReachableHost(hosts, port, probe)\` probes all hosts concurrently via \`Promise.all\`. Pre-bind probe runs for each \`candidatePort !== 0\` BEFORE bind; post-bind EADDRINUSE catch retained as TOCTOU backstop. \`runDaemon()\` probes all configured hosts + \`127.0.0.1\` fallback. \`opts.local===true\` always disables remote mode. Port 0 skipped correctly. Both pre-bind and post-bind reuse blocks use \`firstReachableHost\` + pin \`config.hosts = \[reachableHost]\` so callers see the actual reachable host. All host→URL interpolations in CLI must use \`bracketHost\` for IPv6 (PR #912). Mutation A (disable pre-bind probe) killed by test 3 in start-gateway-reuse.test.ts; Mutation B (single-host daemon probe) killed by daemon-args.test.ts; Mutation C (disable hosts pin) killed by \`expect(handle.config.hosts).toEqual(\["127.0.0.1"])\` assertion.

<!-- lore:019eff67-7977-7ec5-bfc2-e55be71237ee -->
* **Symbol grep probe gotchas: toplevel+env scrubbing, exit-status trap**: Trap: running \`git grep\` from \`this.root\` (project root) looks correct — it's where the code lives. Fix: in a monorepo, \`this.root\` may be a subdirectory; grep from a subdir misses symbols defined elsewhere. Always \`cd $(git rev-parse --show-toplevel)\` first. Second trap: \`GIT\_DIR\`/\`GIT\_WORK\_TREE\` env vars inherited from the agent's shell override git's working-tree detection, causing grep to search the wrong tree. \`scrubbedGitEnv()\` strips both vars before every git subprocess. Both traps look safe because grep succeeds (exit 0) — it just silently searches the wrong scope.

<!-- lore:019ef389-a52f-7427-9099-f8dea38e8caf -->
* **Testing gotchas: vitest alias mock mismatch + Date.now() flakiness**: Trap: \`vi.mock('@loreai/core', ...)\` to spy on exports like \`recordCacheUsage\` looks correct — it's the package specifier pipeline uses. Fix: vitest config aliases \`@loreai/core\` → \`packages/core/src\` at \`resolve.alias\` (top-level, NOT \`test.resolve.alias\` — wrong placement silently resolves to stale dist). The mock targets the specifier but pipeline resolves to the source path, creating a module identity mismatch — spy is never invoked (call count stays 0). No clean workaround for aliased barrel mocks; use e2e harness tests or file as tracked follow-up instead.

<!-- lore:019f8468-759c-7969-9344-d6be41f6a9b4 -->
* **tool-trace.ts/gradient.ts extractFilePath ReDoS: token-split before regex, not just a length cap**: tool-trace.ts's extractFilePath plaintext fallback regex catastrophically backtracks on long slash-separated runs (10K segments=7.6s; a real 114KB input stalled >40s, blocking the event loop and holding the SQLite write lock). Trap: capping the scanned slice at 64KB looks like the fix, but a pathological slash-run fits entirely inside 64KB and still backtracks — even a flatter single-char-class regex without nested quantifiers still backtracks. Actual fix: split input on whitespace first (linear), then regex-match only tokens ≤260 chars — a giant no-space token is skipped entirely, never reaching the regex. gradient.ts's sibling implementation has the same latent vulnerability and was left unfixed. Mutation-verified: reverting to naive match makes the guard test fail (19s vs <1s expected).

<!-- lore:019efa1b-9771-70f1-ba6a-f6eccead4374 -->
* **undici must stay external in Bun bundle — Bun uses node:https instead**: Trap: bundling \`undici\` into the Bun ESM gateway bundle looks correct — it's a Node.js HTTP client. Fix: real \`undici@7\` hangs on streaming response reads under Bun. \`undici\` is lazily imported only on the Node path in \`fetch.ts\` — never bundled or evaluated under Bun. Bun path uses native fetch instead. \`undici\` must remain external in the Bun ESM bundle.

<!-- lore:019f1cc8-b6eb-7d3f-9be2-2cf81848d519 -->
* **Vacuous seam test: single-message transform input never reaches loadDistillationsCached (Layer 0 early return)**: Trap: writing an end-to-end seam test for \`prewarmDistillationSnapshot\` with a single small message looks correct — it exercises the prewarm→transform path. Fix: \`transformInner\` early-returns at Layer 0 when all messages fit, so \`loadDistillationsCached\` is never called. Single-message inputs with empty \`parts\` (0 tokens) always take the Layer 0 path. To force the compaction path where \`loadDistillationsCached\` is actually called, build a large input (~60K chars across ~20 user+assistant messages) that exceeds the Layer 0 ceiling. Verify non-vacuousness by mutating \`loadDistillationsCached\` to always force cache-miss — seam test must fail.

<!-- lore:019f43a4-4ea3-752b-bd99-f2ef59d93db6 -->
* **vectorSearchDistillations is not project-scoped — hydration query must filter by project\_id**: Trap: calling \`vectorSearchDistillations(queryVec, limit)\` and hydrating results looks correct — it mirrors \`vectorSearchTemporal\`. Fix: \`vectorSearchDistillations\` takes no project param; results include distillations from ALL projects. \`vectorSearchTemporal(queryVec, pid, limit)\` is correctly project-scoped. Any code path that calls \`vectorSearchDistillations\` must filter the hydration query with \`WHERE archived = 0 AND project\_id = ?\`, over-fetching (\`limit \* 4\`) and capping at \`limit\` after filtering to avoid multi-project starvation. Seer caught this as a CRITICAL cross-project data leak in PR #1228 review. Follow-up: add a natively project-scoped distillation vector search function.

<!-- lore:019f0052-45d6-7e13-a065-0a0b051f01c7 -->
* **Vertex protocol gotchas: global endpoint host + bearer token isolation**: Trap: \`region-aiplatform.googleapis.com\` pattern breaks for \`region="global"\` (the default) — must use bare \`aiplatform.googleapis.com\` (path still \`locations/global\`); \`global-aiplatform.googleapis.com\` DNS-resolves but 404s. \`VERTEX\_HOST\_RE\` self-heals legacy \`global-aiplatform\` form; \`vertexHost()\` centralizes construction. X-Lore-Upstream-URL with a non-Vertex host is silently discarded (Vertex can't be proxied this way — intentional). Separately: the GCP OAuth2 bearer token minted for Vertex auth must never leak to non-Vertex requests — \`executeWarmup()\`/\`buildVertexWorkerRequest()\` skip \`resolveAuth()\` and mint the token only on Vertex branches; \`forwardClientHeaders()\` strips \`authorization\` before snapshot storage; \`isVertexHost()\`/\`vertexRegionFromUrl()\` regex constrain the token's destination to \`\*.aiplatform.googleapis.com\` even under misconfiguration.

<!-- lore:019f8666-8573-7d19-a4be-11b9ce20f69f -->
* **Vitest probe test outside packages/\*/test dirs is silently skipped**: Trap: placing a throwaway diagnostic test file at /tmp/opencode/\<probe>.test.ts looks convenient for isolated experiments outside the repo. Fix: vitest config only globs test files under each package's own test dir (e.g. packages/gateway/test/), so an external file produces 'No test files found, exiting with code 1' — looks like a failure but is actually a config/path mismatch, not a real test failure. Place throwaway probe tests inside the target package's real test dir (e.g. packages/gateway/test/\_probe\_\*.test.ts) and delete them after use.

<!-- lore:019f8671-85b6-74e8-9d98-27854fe3ccdb -->
* **worker-health.ts ensureSweepTimer never armed by recordWorkerSuccess — succeededSessions leak**: Trap: arming ensureSweepTimer only inside recordWorkerFailure looks sufficient — sweep exists to clean up stale failure tracking. Fix: recordWorkerSuccess also writes into the succeededSessions map but never called ensureSweepTimer, so a process that only ever sees successful sessions (zero worker failures) never GCs succeededSessions — unbounded memory growth over a long-running gateway. Fix: call ensureSweepTimer() from recordWorkerSuccess too. Found via adversarial review of PR #1439 (CONCERN 1d), a non-blocking finding fixed immediately rather than deferred.

<!-- lore:019f8671-8584-74df-a31c-2ff6fb7b463c -->
* **worker-health.ts sawGenuineReason latches degradation banner past window rotation**: Trap: gating getDegradationWarning's banner-suppression on entry.reasons (worker-health.ts) looks correct — it reflects what was actually recorded. Fix: reasons is cleared on every window rotation (FAILURE\_WINDOW\_MS=5m) while firstFailureAt survives rotations, so a session that saw a genuine (non-credential-class) failure early, then only credential-class failures in later windows, wrongly computes allCredentialClass=true and suppresses the banner even past 30/60min sustained thresholds. Fix: added a latched boolean \`sawGenuineReason\` on SessionHealth, set once a non-credential reason is ever seen for the outage, and gate getDegradationWarning on that instead of the window-local reasons check. Found via adversarial review of PR #1439 (CONCERN 1b); confirmed with a regression test spanning 3 window rotations before evaluation.

<!-- lore:019f86b6-b99c-7708-9ecd-d95dee278515 -->
* **worker-health.ts succeededSessions TTL eviction masks ongoing outage**: Trap: evicting \`succeededSessions\` entries after SESSION\_TTL\_MS (1h) in \`ensureSweepTimer()\` looks like safe memory hygiene. Fix: if a session succeeded once then persistently auth-fails for >1h, its \`succeededSessions\` entry ages out and \`neverSucceeded\` flips true, wrongly suppressing the degradation banner mid-outage. Fix: gate eviction with \`!state.has(sessionID)\` — only evict \`succeededSessions\` once the corresponding failure \`state\` entry is gone (recovered or aged out), so an active outage never loses its 'was working' history. Found via Seer bot review on PR #1439 (worker-health.ts:1024); Seer's 'Resolved in commit b45f730' tag was inaccurate — verified by tracing sweep logic before fixing.

<!-- lore:019f8666-8516-7bfe-8430-5d9ef2a327cb -->
* **worker-health.ts: 5m window rotation resets entry.reasons, can wrongly suppress banner**: Trap: getDegradationWarning's \`neverSucceeded && allCredentialClass\` first-run suppression (PR #1439) looks safe assuming entry.reasons reflects lifetime failure causes. Fix: recordWorkerFailure resets entry.reasons (not firstFailureAt) whenever the FAILURE\_WINDOW\_MS (5min) gap is exceeded. A never-succeeded session that saw a genuine 'upstream-error' then only credential-class failures in later windows ends up with reasons={no-auth} by the 30m mark, so allCredentialClass wrongly evaluates true and the banner is suppressed even though getStatus() correctly reports 'degraded'. Same window-scoped-reason mechanic pre-existed for the Sentry escalation check; PR #1439 extended it to banner suppression without rotation-drop test coverage. Confirmed via throwaway probe test. Narrow (needs neverSucceeded + genuine reason absent from active window); flagged as non-blocking CONCERN on PR #1439, unfixed.

### Pattern

<!-- lore:019f1e66-a06c-7c58-b5ec-6d7cb21eddc3 -->
* **backfillTemporalEmbeddings: per-row checkpoint + backlog/progress logging (PR #1104)**: PR #1104 (\`fix/temporal-rechunk-checkpoint\`) changes two behaviors in \`backfillTemporalEmbeddings\`: 1. \*\*Per-row checkpoint\*\*: \`setKV(TEMPORAL\_RECHUNK\_CURSOR\_KEY, retryFrom ?? cursor)\` moved INSIDE the row loop (was per-page). Crash mid-pass now resumes from last completed row, not start of last page. \`retryFrom ?? cursor\` uses nullish (not \`||\`) — when first row fails, \`retryFrom = ''\` and \`'' ?? cursor\` → \`''\`, correctly resuming at \`id > ''\`. 2. \*\*Backlog + heartbeat logging\*\*: upfront \`SELECT COUNT(\*)\` logs \`'temporal re-chunk: N messages to scan (resuming)'\`; \`PROGRESS\_INTERVAL\_MS = 30\_000\` replaces count-based \`PROGRESS\_INTERVAL = 1000\`; heartbeat shows \`processed re-chunked, scanned scanned…\`. Mutations: per-row checkpoint test and backlog-log test both killed independently. Merged \`25a04e22\` at 2026-07-01T17:30:31Z.

<!-- lore:019f00ae-c762-7067-8197-83d9b70d8dd9 -->
* **cache-warmer evidence gate (Bug C): always fund first probe, never fund second+ cycle with zero lifetime hits**: 🔴 Directive: ALWAYS fund the first warmup of a break (\`cyclesSpent === 0\` — the probe that learns if this slow tool returns within TTL), but NEVER fund a second-or-later cycle for a session that has produced zero confirmed hits over its lifetime. Implementation: \`if (cyclesSpent >= 1 && (state.warmup?.warmupHits ?? 0) < TOOL\_CALL\_MIN\_HITS\_FOR\_CONTINUATION) return false\`. \`warmupHits\` is lifetime/durable across breaks; \`warmupCount\` (cyclesSpent) is per-break (reset on return in pipeline.ts). Rationale: 66 of 74 zero-hit warmups occurred at ≤5 lifetime warmups, below \`MIN\_WARMUPS\_FOR\_ROI\_CHECK\`, so hit-rate guard never engaged.

<!-- lore:019f6047-5672-73ac-a80b-521adbebedce -->
* **Codex-worker cache accounting**: When fixing codex-worker cache accounting, always verify test suite non-vacuousness and apply fixes to parseResponsesWorkerResponse.

<!-- lore:019f8676-b076-7d18-880b-aa685c3c2877 -->
* **Eval #1404-B: per-model absolute-token knee replacement for gradient.ts**: Steps: 1. Wait for MATRIX-V5 single-long cells to finish for all 3 models (deepseek cap200, m3 cap200, sonnet cap200+cap500, both arms) — sonnet needs a second cap point (cap500) to derive fill-fraction conversion. 2. Run \`bun rot-behavioral.mjs --root \<MATRIX-V5 dir> --json\` from packages/core/eval/live in the eval-signals-scorer worktree. 3. Compute per-model absolute-token knees from results. 4. Convert knee to fill fraction = knee / cap-context (the eval's token cap, e.g. 200000 or 500000) — NOT the model's native context window. Gotchas: - Gate replacement of each \`QUALITY\_KNEE\_BY\_FAMILY\` seed value in gradient.ts on Wilson significance + adequate n; where a family isn't statistically solid, keep the existing literature-prior seed rather than overwrite with noisy data. Verify: - \[ ] Standalone PR ships on main (not bundled with other changes). - \[ ] Each replaced knee value cites its Wilson CI and n.

<!-- lore:019f1cc8-b65e-7a6e-97ae-f48d1a7d542b -->
* **forProjectOffloaded / entitiesForSessionOffloaded: timeout must fall back to in-process scan, never return \[]**: All offloaded variants of frozen-baseline builders (\`ltm.forProjectOffloaded\`, \`entities.forProjectOffloaded\`, \`entities.entitiesForSessionOffloaded\`) must fall back to the synchronous in-process scan on \`READ\_JOB\_TIMED\_OUT\` — never return \`\[]\`. Trap: returning \`\[]\` on timeout looks safe (same as \`forSession\` degrade). Fix: unlike \`forSession\` (reruns every turn), these results are frozen into \`stableLtmCache\` + \`saveSessionTracking\` for the entire session lifetime. A spurious empty baseline permanently breaks the knowledge catalog for that session. \`forSession\` can safely return \`\[]\` because it reruns every turn. Mutation test: force timeout branch to return \`\[]\` → timeout test must fail.

<!-- lore:019f8690-87a2-7283-bf2f-8ef6a6b06974 -->
* **Full gate command: typecheck && lint && format:check && test**: The canonical 'full gate' before trusting/pushing a fix in loreai is: \`pnpm run typecheck && pnpm run lint && pnpm run format:check && pnpm test\` run from repo root. Run in a pty and wait for \`\<pty\_exited>\` rather than polling. Confirm real pass/fail counts in output (e.g. '6435 passed, 226 skipped') before trusting the result — a silently-noop'd script (see pnpm --filter gotcha \[\[019f8666-84e9-77aa-81a9-c9f8ecef6a8a]]) can fake a clean exit.

<!-- lore:019eec48-01c5-78bd-b7f9-6cd8b7ddfada -->
* **Lore gateway deploy procedure on production host**: 🔴 Deploy procedure (Burak Yigit Kaya directive): \`git stash save && git pull && git stash pop && pnpm install && pnpm build && pnpm --filter @loreai/gateway run bundle && sudo service opencode restart && journalctl -u opencode -f\`. Plugin file at \`~/.config/opencode/plugins/lore.ts\` contains only \`export { LorePlugin as default } from "opencode-lore";\`. opencode-lore resolves from \`/home/byk/.config/opencode/node\_modules/opencode-lore\` (active built repo) — NOT the cached version at \`~/.cache/opencode/packages/opencode-lore@\*/\`.

<!-- lore:019f8638-c593-76f5-b2db-04ef5f215492 -->
* **Mutation testing practice: throwaway worktree + kill-the-branch verification**: When an expected test value is reachable via multiple code paths, a correct assertion can still pass with the guard under test removed (vacuous pass) — e.g. a test for 'already-cheap session reuses its own model' passed even with the reuse guard deleted, because the session hit an unrelated 'alreadyCheap' fallback path first. Always mutation-verify (delete/invert the specific branch and confirm the test fails) rather than trust a correct assertion value alone. Practical setup: mutation-test a fix in a throwaway git worktree (detached HEAD at the target commit) rather than the main tree — symlink \`node\_modules\` from the main worktree instead of a fresh \`pnpm install\` so vitest runs immediately. Apply/revert one mutation at a time, rerun the target test file, delete the worktree when done, and re-verify sha256 of reviewed files in the main tree before/after.

<!-- lore:019f14f5-5edb-7bd1-bc31-9b10062ce1a3 -->
* **Pi real-runtime e2e: console spy must be installed BEFORE resourceLoader.reload()**: Trap: installing \`console.\*\` spies after \`resourceLoader.reload()\` looks correct — the test body runs after setup. Fix: Pi's extension factory executes during \`reload()\`, so any \`console.\*\` calls in the factory are missed by late-installed spies. Additionally, Vitest intercepts \`console.\*\` away from \`process.stdout.write\`, so \`process.stdout/stderr.write\` spies also miss console output. Both traps look safe because the test passes — it just silently misses the mutation. Fix: install direct \`console.\*\` method spies BEFORE \`resourceLoader.reload()\` so the entire reload + \`createAgentSession\` + prompt window is captured. Mutation A (inject \`console.info("pi: ...")\` at line 99 of index.ts) confirmed this kills the markers test when spies are correctly placed.

<!-- lore:019f147e-7b0d-7ed0-aef5-bb2b8c0bc065 -->
* **standard.site publish script: safety invariants for orphan cleanup**: Three invariants in \`publish-standard-site.mjs\` orphan cleanup (PR #1043, also ported to byk.github.io PR #58): 1. \*\*Upserts-first ordering\*\*: all \`putRecord\` calls complete before any \`deleteRecord\` — a mid-run failure never strands the blog without live records. 2. \*\*Empty-manifest guard\*\*: if \`manifest.documents.length === 0\`, skip orphan cleanup entirely with \`console.warn\` — a broken/empty build cannot wipe the whole collection. 3. \*\*Pagination termination\*\*: \`listAllRecords\` uses dual-condition stop — (a) stop when batch length is 0 even if PDS returns a cursor (PDS returns trailing cursor on last page — confirmed live), (b) stop when cursor is null/undefined. 4. \*\*DID cross-check\*\*: if \`session.did !== recordDid\`, script refuses to publish. 5. \*\*\`validate: false\`\*\* on \`putRecord\` — PDS doesn't host \`site.standard.\*\` lexicons. 6. \*\*Blob uploads\*\*: blobs are content-addressed (CID) → idempotent re-runs; best-effort; size cap \`MAX\_BLOB\_BYTES = 1\_000\_000\`; Astro-optimized webp variants always under 1MB.

### Preference

<!-- lore:019f70c1-6259-7105-b9bc-0c61c230bcf0 -->
* **Admin command never silently promotes wrong account**: User consistently requires that the system never silently promotes the wrong account. This pattern has appeared in 27 prior sessions and is a directive preference.

<!-- lore:019f7af2-a187-7784-bf37-06ede6ad7e76 -->
* **Also check the prose for the results**: User stated: 'Also check the prose for the results'. This is a directive preference.

<!-- lore:019f6b15-462d-79e4-aa39-4c04e1527230 -->
* **Always a remote gateway — it has no shared filesystem with its clients**: Lore Gateway is always remote and has no shared filesystem with its clients. CLI import/export commands (\`lore import\`/\`export\`) always run locally on the client — reading local agent history files (mem0 storage.sqlite, Qdrant scroll, engram export) and producing the normalized doc (\`source.produceDoc\`) never happens on the gateway; only the already-produced \`LoreImportDoc\` travels over the network. Import never proxies a conversation turn, so it requires a dedicated \`LORE\_WORKER\_API\_KEY\` (not a client credential) to run extraction. Any CLI import/export feature must assume client-only filesystem access.

<!-- lore:019f7b95-0983-77a0-9bc8-cfb83b8882c3 -->
* **Always clear the local session**: User stated to always clear the local session.

<!-- lore:019f6fba-0d21-7410-894e-4933b29e8518 -->
* **Always converges**: User stated: 'always converges'. This is a directive preference.

<!-- lore:019f7b4f-e02f-7166-9da3-cf0f0f6d7bb7 -->
* **Always defined in ESM**: User consistently requires that certain values are always defined in ESM modules. This pattern has appeared in 27 prior sessions and is a directive preference.

<!-- lore:019f7766-9f0a-787b-8afd-96740e7d81a4 -->
* **Always drain outstanding embedding promises on shutdown**: User consistently requires that outstanding \`trackDocEmbed\` promises be drained on shutdown to prevent silent recall degradation from incomplete distillation embeddings. This pattern has appeared in 22 prior sessions and is a directive preference.

<!-- lore:019f7155-c3b2-7ddf-8dc5-f9c97f3904e0 -->
* **Always keep pinned segments in distillation**: Always keep head segments in distillation. User stated: 'always kept\*\* (0 >= budget is false for budget>0),'. This is a directive preference.

<!-- lore:019f6fd7-3931-76ae-93b7-cbcec8c69643 -->
* **Always normalize to MIGRATIONS.length for database migrations**: Always normalize to MIGRATIONS.length for database migrations to ensure consistency. This was a directive preference.

<!-- lore:019f7766-9f85-7a5c-b3d3-1917618602bd -->
* **Always position delta block before assistant tool\_use**: User consistently requires that delta blocks be positioned before any assistant \`tool\_use\` in the prompt, never between \`tool\_use\` and its \`tool\_result\`. This pattern has appeared in 36 prior sessions and is a directive preference.

<!-- lore:019f7766-9f5b-72e8-9567-531f800edfd0 -->
* **Always re-index missing distillation\_vec rows on next boot**: User consistently requires re-indexing missing \`distillation\_vec\` rows on next boot to recover from incomplete embeddings due to early session termination. This pattern has appeared in 39 prior sessions and is a directive preference.

<!-- lore:019f7cad-c1f4-7921-875d-978a292ca8bb -->
* **Always record structured tool-call traces — even when the assistant**: Always record structured tool-call traces — even when the assistant. This is a directive preference.

<!-- lore:019f7c6a-f652-7412-a3ae-bd0975f89d1a -->
* **Always record the auth failure so the worker-health ladder sees it even for session-less paths (the adapter is the single owner of transport-failure attribution)**: User stated to always record the auth failure so the worker-health ladder sees it even for session-less paths (the adapter is the single owner of transport-failure attribution).

<!-- lore:019f6fdd-fd1b-7c97-a870-80edf21b66f1 -->
* **Always remove obvious statements from drafted replies**: Always remove obvious statements from drafted replies. User requested removal of sentence about subscription rate window resets from Erica reply draft, stating it's obvious.

<!-- lore:019f7080-f5bc-7da4-8008-44ec5998ff2f -->
* **Always runs locally for lore run**: User stated: 'always runs locally — agent is on the same machine.' This is a directive preference for \`lore run\` operations.

<!-- lore:019f70a4-9381-77f1-bc0d-1485c1e9fbcd -->
* **Always signal — never truncate user's actual prose/instructions**: Always signal — never truncate user's actual prose/instructions. User stated that the leading prose prefix is always kept verbatim during reduction. This is a directive preference.

<!-- lore:019f71b3-f431-7888-b0f9-e87ffeb410de -->
* **Always sit at the front of the transformed**: User consistently requires that certain elements always sit at the front of the transformed output. This pattern has appeared in 27 prior sessions and is a directive preference.

<!-- lore:019f7af2-a12f-79b7-9c28-ddcf89b653b8 -->
* **Always the same: the agent never called the save step in the first**: User stated always the same: the agent never called the save step.

<!-- lore:019f76d7-4108-7e96-95f3-468dca0b097f -->
* **Always use 'document' inputType for backfill to avoid self-gating**: The user consistently requires using 'document' inputType for backfill operations to ensure \`isRecall\` is always false and avoid self-gating. This pattern has appeared in 35 prior sessions and is a directive preference.

<!-- lore:019f6fba-0dc9-787f-a62e-b86b43f939a0 -->
* **Always valid for JSON**: User stated: 'always valid for JSON.' This is a directive preference.

<!-- lore:019f7766-9f32-757d-bbf8-b709eb759894 -->
* **Always warm-start embedding worker at gateway startup**: User consistently requires warm-starting the embedding worker at gateway startup to avoid cold model load delays causing incomplete distillation embeddings. This pattern has appeared in 35 prior sessions and is a directive preference.

<!-- lore:019f86af-91eb-7d77-8ab1-603834ad25e5 -->
* **Always write tests alongside implementation**: Behavioral pattern detected across 33 sessions (action: requested-tests). The user consistently demonstrates this behavior.

<!-- lore:019f6f38-a1ff-7b4b-b941-36b22f5dd4e7 -->
* **Avoid re-embedding identical text every CI run**: Avoid re-embedding identical text every CI run. User stated re-embedding identical text every run is wasteful. This is a CI optimization preference. Directive preference.

<!-- lore:019f70c1-61b7-7c02-823a-aebcac38b39b -->
* **CommandAdmin never reads \_values from OPTIONS map**: User stated that commandAdmin takes \_values and never reads it — so no OPTIONS-map/flag concern applies to admin at all. This is a directive preference.

<!-- lore:019f7af2-a15c-74bf-873d-f2270c47516c -->
* **Do the other runs with DeepSeek and competitors and give a full update to the blog**: User stated: 'Do the other runs with DeepSeek and competitors and give a full update to the blog'. This is a directive preference.

<!-- lore:019f8684-bf3a-743a-9db7-9678488ce0fc -->
* **Don't notify issue reporters until fix is confirmed live, not just merged**: When Burak Yigit Kaya says to respond to a bug reporter "once this commit hits the nightly/release," merging the PR alone is not sufficient — wait for actual confirmation the artifact is published (e.g. GHCR nightly tag live, publish job succeeded) before posting to the issue. Verify via the real publish job result (\`gh run view --json status,conclusion,jobs\`) and the artifact registry (e.g. \`oras manifest fetch ...\`), not just PR merge status. If the publish job fails, investigate instead of posting the reply.

<!-- lore:019f6fde-efd1-7f32-82db-76eb9454a990 -->
* **Invariant-check (judge + CI action): advisory-only, never fails build, judge never false-flags or throws**: The invariant-check action is advisory-only and NEVER fails the build — it reports findings as annotations and a job summary; humans decide. The action should NEVER fail the build: capture exit, always continue. A non-zero exit from the CLI means a TOOLING failure (no range) — we log it and pass. This is a directive preference.

<!-- lore:019f7bb8-b4da-72b7-92a1-079377550ba4 -->
* **Invariant-check judge: never import outside driver**: User stated: never be imported outside driver. This is a directive preference.

<!-- lore:019f7bb8-b4b3-71ad-95e9-7328fccb31cd -->
* **Invariant-check judge: never send more than MAX\_JUDGE\_PAIRS\_PER\_RUN**: User stated: Never send more than this many pairs to the judge in one run. This is a directive preference.

<!-- lore:019f7135-8b8a-7abe-bd3c-29ae8ff474fa -->
* **Junk never embedded in Stage 1 paste-junk filtering**: User stated that 'junk never embedded' is a key requirement for Stage 1 paste-junk filtering. This is a directive preference.

<!-- lore:019f7af2-a1b1-799f-83ea-01e3e46feb85 -->
* **Last time you got a custom build yourself, why not that again?**: User stated: 'Last time you got a custom build yourself, why not that again?'. This is a directive preference.

<!-- lore:019f650e-1593-72e3-8c34-fdbf17c32560 -->
* **Never a skip/conflict**: Never a skip/conflict: User stated never a skip/conflict.

<!-- lore:019f7080-f5e8-7b3b-a8be-274e4f645867 -->
* **Never accumulates background import failures when no auth exists**: User stated: 'never accumulates.' This is a directive preference indicating that background import failures should not accumulate when no authentication is configured.

<!-- lore:019f7155-c3fc-7e42-b047-d4176cd09568 -->
* **Never alert on structural cache busts**: Never fire hook on structural cache busts. User stated: 'never fires the hook)'. This is a directive preference.

<!-- lore:019f7ccd-ede8-7421-9a84-10ab02fd612f -->
* **Never auto-promote to a team**: User consistently requires that the system never automatically promotes users to team membership. This maintains the invite-only model integrity. Directive preference.

<!-- lore:019f7b95-09b2-7577-a9d2-b952b3df0a76 -->
* **Never block login on QR rendering**: User stated to never block login on QR rendering.

<!-- lore:019f7b4f-df9a-7c55-a450-c19f771311d0 -->
* **Never bounces back to native**: User consistently requires that fallback mechanisms never bounce back to native after switching to WASM. This pattern has appeared in 27 prior sessions and is a directive preference.

<!-- lore:019f7155-c3d6-70e8-9030-6a4570e986d1 -->
* **Never bust expensive cache**: Never bust expensive cache. User stated: 'never busts an expensive cache,'. This is a directive preference.

<!-- lore:019f70c1-60ef-71ff-943f-bd1ab0c3f818 -->
* **Never client-driven tombstone reaper**: User stated that the reaper is 'never client-driven' regarding the tombstone reaper pattern. This is a directive preference.

<!-- lore:019f66e9-5fde-7103-a386-f9e5db82613a -->
* **Never compacts -> errors instead**: User stated never compacts -> errors instead.

<!-- lore:019f7155-c46a-71d1-a8da-43efe31d9751 -->
* **Never consume entire maxSegments embed budget**: Never consume entire maxSegments embed budget. User stated: 'never consume the entire \`maxSegments\` embed budget (e.'. This is a directive preference.

<!-- lore:019f70c1-6166-74a9-ac25-8f4257e9a523 -->
* **Never delete another member's real wrap through scope\_keys migration**: User stated that scope\_keys migration has security guarantees: 'NEVER delete another member's real wrap through this'. This is a directive preference.

<!-- lore:019f7b4f-e0ce-7d86-9ada-369d5578820e -->
* **Never delete/redownload**: User consistently requires avoiding delete/redownload cycles. This pattern has appeared in 27 prior sessions and is a directive preference.

<!-- lore:019f702c-6ef3-717a-b329-7a050a717b77 -->
* **Never drop exact evidence in candidate selection**: Never drop exact evidence in candidate selection. This is a directive preference.

<!-- lore:019f7cad-c1c7-75d7-9683-b577f9d3cd39 -->
* **Never exceed the driver's variable limit**: Never exceed the driver's variable limit. This is a directive preference.

<!-- lore:019f749f-147f-7645-a1ec-0821e1476840 -->
* **Never exceed total budget**: Never exceed total budget. This is a directive preference.

<!-- lore:019f7ccd-edbd-7491-a405-3d76962c83c0 -->
* **Never expose or transmit Lore user\_ids**: User consistently requires that Lore user\_ids are never exposed, printed, or transmitted. This is a security requirement for the invite-only model. Directive preference.

<!-- lore:019f749f-1424-7faf-8acc-44fa2494533b -->
* **Never filters bug**: Never filters bug. This is a directive preference.

<!-- lore:019f65ca-91fb-7140-b01f-aeb90540b80d -->
* **Never from within another transaction**: User stated never from within another transaction.

<!-- lore:019f70c1-6116-7798-bde9-7dbaf926a516 -->
* **Never grant UPDATE on profiles to service\_role**: User stated that service\_role was 'never granted UPDATE on profiles' regarding tier guards. This is a directive preference.

<!-- lore:019f7ccd-ee15-739b-bbff-4d4664a4c845 -->
* **Never let two deploys race; newest merge wins**: User consistently requires that deployment processes never race, with the newest merge always winning. This prevents inconsistent deployments. Directive preference.

<!-- lore:019f7b4f-e07e-7962-83e0-85b40dfd63ce -->
* **Never lets N workers sum past the host's**: User consistently requires that the number of workers never exceeds the host's capacity. This pattern has appeared in 27 prior sessions and is a directive preference.

<!-- lore:019f7b4f-e057-7cd5-bab2-2866bf5cb303 -->
* **Never loop unbounded (no event storm)**: User consistently requires preventing unbounded loops to avoid event storms. This pattern has appeared in 27 prior sessions and is a directive preference.

<!-- lore:019f7b4f-e0a6-7249-8ab7-d932661eb85a -->
* **Never match — only the leading string**: User consistently requires that only the leading string is matched in certain contexts. This pattern has appeared in 27 prior sessions and is a directive preference.

<!-- lore:019f81c5-dde6-7ffd-a96f-34e3ccde0a5e -->
* **Never poll pty sessions with sleep+pty\_read loops**: When a pty/background process is spawned for builds, tests, typechecks, installs, lint, format, or mutation verification, wait for the \`\<pty\_exited>\` completion signal rather than polling via sleep+pty\_read loops. Only call pty\_read before exit if live output is needed immediately, the user explicitly requests logs, or the exit notification reports a non-zero/failed status requiring investigation. This is a standing rule for the entire session — apply it consistently to every spawned pty task without needing to be reminded each time.

<!-- lore:019f65ca-91df-708f-a856-e6dd16610be7 -->
* **Never re-read — so excluded**: User stated never re-read — so excluded.

<!-- lore:019f65ca-9231-7baf-a27a-65ee4b6c3a4b -->
* **Never reach the remote**: User stated never reach the remote.

<!-- lore:019f6fba-0e22-7d6f-9f5f-306eeb20e1cf -->
* **Never reaches a fixpoint**: Never reaches a fixpoint — iterateToFixpoint hit its cap (fixed by trimming html-node trailing whitespace). This was a directive preference.

<!-- lore:019f70c1-618e-7f32-9e38-adfe833b06db -->
* **Never reap real member wraps in tombstone reaper**: User stated that 'NEVER a real member wrap 6ms' is a critical security requirement for the tombstone reaper. This is a directive preference.

<!-- lore:019f7b4f-e00a-7ba1-b6a4-8bea4adebc0a -->
* **Never rejects in practice; swallow to be safe**: User consistently requires swallowing errors in practice for safety, even if they should never reject. This pattern has appeared in 27 prior sessions and is a directive preference.

<!-- lore:019f7ccd-ee40-7b83-abcb-4594b6d52ad6 -->
* **Never rely on someone remembering to run**: User consistently requires that critical processes are automated rather than relying on human memory to run them. This ensures reliability. Directive preference.

<!-- lore:019f7cad-c19c-7635-bf05-b29f0ff820e9 -->
* **Never revert a resolved row back to**: Never revert a resolved row back to. This is a directive preference.

<!-- lore:019f70c1-613e-7caf-a1c9-cd7a330cd0c2 -->
* **Never self-upgrade team via client**: User stated that a client can 'never self-upgrade a team' regarding org tier guards. This is a directive preference.

<!-- lore:019f7b4f-dfe5-7592-a5bf-987ca14d2a0d -->
* **Never sets this in production**: User consistently requires that certain flags or values are never set in production. This pattern has appeared in 27 prior sessions and is a directive preference.

<!-- lore:019f6f38-9f83-7c59-b773-9c85fd64d5e5 -->
* **Never ship DEK to CI**: Never ship DEK to CI. User stated shipping the DEK to CI would violate encryption posture. This is a security constraint. Directive preference.

<!-- lore:019f749f-13fe-7823-a5da-cc11a5f5bc69 -->
* **Never surfaced into system\[2] whenever embeddings**: Never surfaced into system\[2] whenever embeddings. This is a directive preference.

<!-- lore:019f749f-144c-7e1c-b1f3-4f64753f976c -->
* **Never surfaced on the vector signal alone**: Never surfaced on the vector signal alone. This is a directive preference.

<!-- lore:019f71b3-f45b-7c8d-b1de-3d96494f11cf -->
* **Never touch the front**: User stated: 'never touches the front,'. This is a directive preference.

<!-- lore:019f66a2-5938-7fd4-b581-90d256a820c4 -->
* **Never TTL-evicted**: User stated never TTL-evicted.

<!-- lore:019f70c1-61df-7837-a22e-2aee503b0b98 -->
* **Never write service-role key to disk**: User stated that service-role key 'never touches that path — it is never written to disk'. This is a directive preference.

<!-- lore:019f7af2-a0ff-7363-bbb3-3802fab59406 -->
* **Never written to any intermediate file**: User stated: 'never written to any intermediate file,'. This is a directive preference.

<!-- lore:019f8126-b8d1-78d6-bc66-9403838dfb13 -->
* **Prefers git\_remote first, only falls back over path**: prefers git\_remote first, only falls back to path

<!-- lore:019f756d-37fe-78d7-9306-40dfe31342ad -->
* **Prefers native ONNX runtime via resolveNativeOrtBindingPath, falls back over WASM if native not resolvable**: prefers native ONNX runtime via resolveNativeOrtBindingPath, falls back to WASM if native not resolvable.

<!-- lore:019f7331-e703-7ae9-aa16-093875aeb481 -->
* **Prefers recall over assuming you don't have the information**: prefer recall over assuming you don't have the information.

<!-- lore:019f7b4f-df75-7bf3-9725-8b4f00c384bb -->
* **Rigorous adversarial verification/review workflow required before trusting, shipping, or merging fixes/PRs — never merge**: Before trusting, shipping, or merging any fix/PR: work only in an isolated worktree, never the real repo. Root-cause via logs/code tracing with a written plan-mode review; reproduce bugs against real tools. Every fix needs a regression test that FAILS when reverted, verified via hash-diff and mutation-kill (revert→RED, restore→match). Grep call sites of changed signatures. Run adversarial/correctness then security review, then full gates (tests, typecheck, lint, format:check). Check CI via \`gh pr checks\`/\`gh pr view --json mergeable,mergeStateStatus\`; CLEAN/MERGEABLE (or UNSTABLE with only optional checks) is merge-ready — distinguish regressions from flakes. Address all bot findings (Seer/Warden/Sentry) against real code; fix cheap SHOULD-FIX now, leave NITs with rationale. Track via a todo list. Squash-merge only once CLEAN, checks pass, bot comments addressed, and diff --stat confirms only intended files changed. Deliver a PASS/FAIL/CONCERN/MUST-FIX verdict ending MERGE/DO-NOT-MERGE. Even after MERGE, wait for explicit user instruction, then delete branch and start fresh off origin/main; rebase if CONFLICTING/DIRTY. Don't write fixes when asked only to review.

<!-- lore:019f70c1-622c-7c8c-96d6-e99ade1d5100 -->
* **Service-role key never matches contains @**: User stated that service-role key 'never match (contains @'. This is a directive preference.

<!-- lore:019f70c1-6205-7d8b-9766-44b11a1e6f51 -->
* **Service-role key never round-tripped through sessionToPersisted**: User stated that service-role key 'never round-tripped through sessionToPersisted'. This is a directive preference.

<!-- lore:019f77f9-b7d6-7f6c-9431-9f4fd9a443a3 -->
* **Shutdown must never block indefinitely — enforce a hard deadline**: Shutdown procedures must never block longer than a hard deadline, to avoid Ctrl+C hangs — including never getting stuck waiting on hung child processes. This pattern has appeared in 36+ prior sessions and is a directive preference.

<!-- lore:019f853b-4cb8-7665-a46f-570c700a1358 -->
* **Treat .lore.md as a generated export — never hand-merge conflicts (git or jj)**: Treat .lore.md as a generated export — never hand-merge conflicts; always keep it in the commit. On a .lore.md conflict during rebase/merge (git or jj), take one side wholesale rather than resolving markers — it's a generated export, lore.db is the source of truth. Git: \`git checkout origin/main -- .lore.md\`. Jj: \`jj file show -r \<mainCommit> .lore.md > .lore.md\` — NOT \`jj file show 'main@origin' .lore.md\` (invalid revset syntax silently fails, writing empty content; jj auto-snapshots as clean, masking the mistake). Sometimes jj auto-resolves the .lore.md conflict in the working copy during rebase with no manual step needed — \`jj resolve\` then reports 'No conflicts found'; still verify size/content before trusting it. Diagnostic tip: if GitHub reports mergeStateStatus=DIRTY/mergeable=CONFLICTING after a clean review, rebase onto new main first and check whether .lore.md is the ONLY conflicting file — that's curator churn, not real code overlap (confirmed on PR #1439). After merging a PR: discard curator's working file, switch to a fresh branch tracking origin/main HEAD, confirm remote branch deleted, delete stray local branches.

<!-- lore:019f7b66-8ff8-7d82-9ee8-fcd3a9521686 -->
* **Warden analysis: acknowledge genuine utility before extracting ideas**: User consistently requires acknowledging Warden's genuine utility before extracting ideas. This pattern has appeared in 3 prior sessions and is a directive preference.

<!-- lore:019f7b66-90ea-7e8c-b238-ce1b2f1e4e7a -->
* **Warden analysis: caution against over-rotating on automation**: User consistently requires caution against over-rotating on automation when analyzing Warden. This pattern has appeared in 3 prior sessions and is a directive preference.

<!-- lore:019f7b66-8fca-7d46-a8a6-c5918d6f1cb3 -->
* **Warden analysis: extract and propose concrete working ideas, not just criticism**: User consistently requires extracting working ideas from Warden analysis, not just criticism. This pattern has appeared in 3 prior sessions and is a directive preference.

<!-- lore:019f7b66-909e-72e9-a518-2cbad5e16348 -->
* **Warden analysis: frame as complementary, not direct competitor**: User consistently requires framing Lore as complementary to Warden rather than direct competitor. This pattern has appeared in 3 prior sessions and is a directive preference.

<!-- lore:019f7b66-9077-7692-bdbf-eab4dff4676e -->
* **Warden analysis: identify structural advantages for Lore**: User consistently requires identifying structural advantages for Lore when analyzing Warden. This pattern has appeared in 3 prior sessions and is a directive preference.

<!-- lore:019f7b66-9021-75fd-8e66-8bb2bfaa179d -->
* **Warden analysis: note model capability requirements**: User consistently requires noting model capability requirements when analyzing Warden. This pattern has appeared in 3 prior sessions and is a directive preference.

<!-- lore:019f7b66-9187-7420-b714-270c2b547199 -->
* **Warden analysis: note reputation opportunity**: User consistently requires noting reputation opportunity when analyzing Warden. This pattern has appeared in 3 prior sessions and is a directive preference.

<!-- lore:019f7b66-9113-732d-bac6-96a43f09ba8c -->
* **Warden analysis: offer actionable next steps**: User consistently requires offering actionable next steps after Warden analysis. This pattern has appeared in 3 prior sessions and is a directive preference.

<!-- lore:019f7b66-913a-754c-98e8-22cd7332e06a -->
* **Warden analysis: prioritize highest-leverage moves**: User consistently requires prioritizing highest-leverage moves after Warden analysis. This pattern has appeared in 3 prior sessions and is a directive preference.

<!-- lore:019f7b66-9162-772a-bdc5-591ba0ddbf86 -->
* **Warden analysis: use benchmark data to show value**: Warden analysis: pull and use benchmark data to make the analysis concrete and demonstrate value. This pattern has appeared in 3 prior sessions and is a directive preference.

<!-- lore:019f77f9-b84c-7c77-9bd6-baa0e69a9bdc -->
* **Warmup embedding never blocks startup**: User consistently requires that embedding warmup never blocks startup. This pattern has appeared in 40 prior sessions and is a directive preference.

<!-- lore:019f77f9-b825-7350-a219-87bda1045e2e -->
* **Warmup embedding never hits network or burns quota**: User consistently requires that embedding warmup never hits the network or burns quota. This pattern has appeared in 34 prior sessions and is a directive preference.

<!-- lore:019f71b3-f4b0-7ddb-91ff-b8a54acc8e99 -->
* **worker-health.ts & WORKER\_DEFAULTS: cross-provider guard, gpt-luna pin, degradation banner**: Never attribute/reuse a different provider's credential across workers (cross-provider poisoning guard). ChatGPT-backend sessions (host chatgpt.com/backend-api) must reuse their OWN live worker/model, not fail over to a sibling (sibling 404s via that host) — discriminate by upstream URL host, not provider name; openai-codex sessions are exempt. WORKER\_DEFAULTS\['openai'] is pinned to family 'gpt-luna' (~$1/M) over lineage-aware 'closest cheaper tier', which would otherwise pick the pricier gpt-5.6-terra — user explicitly chose the pin; \`alreadyCheap\` filter covers \`luna|mini|nano\` so cheap sessions aren't 'downgraded'. \`succeededSessions\` map suppresses the degradation banner for sessions with no prior success when failures are credential-class (indistinguishable from genuine auth break otherwise); banner still fires after a prior success breaks or on real outages — the failure is still always recorded for the health ladder.
