<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019e99ac-28f0-77ef-a5de-01ee769c3d7d -->
* **createGatewayLLMClient: provider mismatch guard, worker auth, and session-aware URL injection**: \`createGatewayLLMClient()\` session-aware wrapper (pipeline.ts:931–999): simplified after same-provider invariant — always injects \`state.lastUpstream.url\` since worker provider IS session provider. No longer needs \`upstreamByProvider\` lookup or proxy/aggregator detection. Provider mismatch guard (llm-adapter.ts:380–394): checks \`cred.scheme==='api-key' && !hasDedicatedKey && !upstreamOverride\`; guard BYPASSED when \`upstreamOverride\` active (which is always true for workers now). \`isOpenAI\` flag (line 362) determines request format: \`model.providerID === 'openai'\` — TRAP: NVIDIA/OpenRouter use OpenAI protocol but have non-'openai' providerID; format should be determined by provider's protocol, not literal providerID.

<!-- lore:019e6904-e0e1-7689-bb52-7e8a0cb0edda -->
* **Gateway recall: Marker-and-Expand strategy — tool injection, interception, and round-trip**: Recall tool injected by gateway pipeline (pipeline.ts), NOT OpenCode plugin. Injection condition: \`modifiedReq.tools.length > 0 && !clientHasRecallTool(tools)\`. Strategy: (1) Response: recall \`tool\_use\` replaced with marker text via \`replaceRecallWithMarker()\`; result stored in \`sessionState.recallStore\`. (2) Next request: \`expandRecallMarkers()\` restores \`tool\_use\`+\`tool\_result\` pairs before forwarding. \`buildRecallFollowUp()\`: assistant message = ORIGINAL \`tool\_use\` block; user message = \`tool\_result\` block with recall results (NOT plain text) — prevents LLM echoing raw recall formatting during streaming. \`hasContinuationAfter\` logic: if blocks follow marker, truncates/splices; else prepends tool\_result to next user msg. \`RecallAwareAccumulator\` suppresses recall SSE blocks. MAX\_RECALL\_DEPTH=10. Follow-up requests: \`cacheConversation: false\`.

<!-- lore:019e7013-a9ea-72e6-8e8c-17ef1848e4e0 -->
* **llm-adapter.ts: retry logic, backoff schedule, and circuit breaker integration**: llm-adapter.ts retry/backoff: \`TRANSIENT\_CODES={429,500,502,503,529}\`; \`AUTH\_ERROR\_CODES={401,403}\`. \`MAX\_RETRIES\_RATE\_LIMIT=3\`; \`MAX\_RETRIES\_SERVER=3\`; \`MAX\_RETRIES\_URGENT=2\`. \`backoffMs()\`: Retry-After present → \`min(retryAfterMs, cap)\`; urgent (no header) → \`min(1000×2^attempt, 4000)\`; 429 non-urgent → \`min(60\_000+attempt×60\_000, 180\_000)\`; 5xx → \`min(1000×2^attempt, 8000)\`. On 429 non-urgent → \`tripCircuitBreaker(pauseSec)\` before retry. Auth errors → \`markAuthStale\`, re-resolve, retry once if \`credentialChanged && attempt===0\`. \`buildAnthropicWorkerRequest()\`: injects billing block only when \`cred.scheme === 'bearer'\`; system sent as block array with \`cache\_control: { type: 'ephemeral', ttl: '1h' }\`; body signed via \`signBody()\`. \`opts.thinking\` intentionally NOT forwarded in bare API fetch. \`activeWorkerCalls=Set\<string>\` tracks worker session IDs.

<!-- lore:019e591d-acda-7f7d-b97b-85ec594a1445 -->
* **LLMClient.prompt() opts: urgent, thinking, workerID, maxTokens, temperature semantics**: Sub-agent sessions detected via \`x-parent-session-id\` header (pipeline.ts:2773-2809). Each sub-agent gets independent session: temporal storage, gradient state, distillation pipeline, LTM injection, cost tracking. Key differential treatment: (1) cache warming ALWAYS skipped — \`shouldWarm()\` returns false unconditionally (cache-warmer.ts:731-733); (2) \`findRotationPredecessor()\` (session.ts:505) skips sub-agents from Tier 1b rotation; (3) idle distillation/curation runs normally (no isSubagent check). \`isSubagent\`/\`parentSessionId\` persisted to DB (migration v26). Dashboard: collapsible cost-rollup tree via \`loadParentChildMap()\` + \`buildLiveSessionRows()\`; \`rollUp()\` propagates grandchildren costs bottom-up. \`isCacheWarm()\` (pipeline.ts:3231–3253): returns false if no \`warmup.lastWarmupAt\`; \`forceKeepWarm\` uses 2× TTL safety margin; purpose: skip post-idle compaction when cache is warm.

<!-- lore:019e5715-cccd-7be2-a979-407066c526d4 -->
* **log.ts: sink bridge, file rotation, and error visibility rules**: \`packages/core/src/log.ts\`: (1) \`log.error()\` always calls \`console.error()\` — always visible, indicates real failures. \`log.info()\`/\`log.warn()\` only print if \`LORE\_DEBUG=1\`. (2) \`sink?.captureException(err)\` called when \`Error\` instance found via \`findError()\` — gateway bridges to Sentry via \`registerSink()\`. (3) File logging: all levels written to \`~/.local/share/lore/lore.log\` regardless of \`LORE\_DEBUG\`; rotated at 5 MB (\`LOG\_MAX\_BYTES\`), check every 1000 writes (\`ROTATION\_CHECK\_INTERVAL\`), single \`.log.1\` backup; disabled in \`NODE\_ENV=test\`. (4) Only one \`LogSink\` supported. TRAP: any \`log.error(..., errorObj)\` auto-forwards to Sentry — including expected shutdown/auth errors. Use typed error subclasses or \`TRANSIENT\_ERROR\_PATTERNS\` filter in instrument.ts \`beforeSend\`. \`log.warn()\` auth errors from batch-queue are never forwarded to Sentry (warn level not captured by sink).

<!-- lore:019e54f5-04be-7a9f-9e62-177d65d5db9f -->
* **LTM forSession() budget, phases, and safety-net constants**: \`forSession()\` (ltm.ts): budgets: \`budget.ltm\`=0.05, \`budget.preferenceLtm\`=0.02. Phase 1: architecture entries up to ARCH\_BUDGET\_FRACTION=0.2. Phase 2: remaining entries by score descending. Phase 3: .lore.md sections. HEADER\_OVERHEAD\_TOKENS=15 subtracted upfront. Hard cap: \`if (used+cost>maxTokens) continue\`. PROJECT\_SAFETY\_NET=5: top-confidence project entries ALWAYS included even with zero relevance score — intentional design, guards against coarse term-overlap scoring excluding important project knowledge. NO\_CONTEXT\_FALLBACK\_CAP=10. Global entries (pid=null) must be cross\_project=1. \`vectorSearch()\` has no project\_id filter; dedup threshold for Nomic v1.5 is 0.935. GOTCHA: \`vectorSearch()\` queries \`knowledge WHERE embedding IS NOT NULL AND confidence>0.2\` — test suites must \`DELETE FROM knowledge WHERE embedding IS NOT NULL\` in beforeEach. Pool SQL: project pool uses \`cross\_project=0\`; cross pool uses \`project\_id IS NULL OR cross\_project=1\`. Promoted entries (cross\_project=1) lose PROJECT\_SAFETY\_NET guarantee in their home project — semantic regression to be aware of.

<!-- lore:1cdad2a4-5637-471b-a5cb-39c919fb1592 -->
* **Multilingual support: FTS tokenizer, Unicode tokenization, and non-English fallbacks**: Lore supports non-English (e.g. Turkish) conversations. Key design: (1) FTS5 uses \`tokenize='unicode61 remove\_diacritics 0'\` (NOT \`porter\` — English-only stemmer; NOT diacritic-folding — Turkish ç/ğ/ı/ö/ş/ü are distinct letters). Migration v32 rebuilds all 6 FTS tables. (2) \`filterTerms()\`/\`extractTopTerms()\` in \`search.ts\` strip via \`/\[^\p{L}\p{N}\_\s]/gu\` (Unicode-aware) so Turkish words don't split at special letters. Underscore kept for snake\_case. (3) \`pattern-extract.ts\` regexes need NO translation — they run on the LLM observer's \*English-normalized\* output. (4) Two raw-user-text regex sites have a structural fallback: \`hasNonAsciiLetters()\` (exported from \`instruction-detect.ts\`) gates fallbacks in \`extractInstructionCandidates\` (emit whole message as candidate) and \`detectAssertions\` (pin first sentence) — requires ≥3 non-ASCII letters to avoid false positives on English loanwords (café, naïve). (5) \`ltm.rerankPreferences()\` no longer demotes entries lacking English directive words to 0.8. KNOWN LIMITATIONS (deferred): English \`STOPWORDS\` not filtered for other languages (vector+RRF compensates); ~3-4 chars/token estimate is English-tuned (fine for Turkish, off for CJK).

<!-- lore:019e99ac-28eb-7a7f-a362-37e2a18eaec5 -->
* **Session auth cross-contamination: per-provider credential and upstream snapshot fix**: Root cause: \`SessionState.lastUpstream\` and \`sessionAuth\` are singular — overwritten when user switches providers mid-conversation. Fix: (1) \`upstreamByProvider: Map\<string, UpstreamSnapshot>\` added to \`SessionState\`; \`lastUpstream\` kept as convenience pointer. (2) \`sessionAuth\` → \`Map\<sessionID, Map\<providerID, AuthCredential>>\`; \`setSessionAuth\`/\`resolveAuth\` take optional \`providerID\`; \`\_default\` slot synced on every write. (3) Session-aware LLM wrapper (pipeline.ts:931–943): looks up \`state.upstreamByProvider.get(workerProviderID)\`, falls back to \`state.lastUpstream\`. KNOWN LIMITATIONS: (a) Provider ID namespace mismatch — \`upstreamByProvider\` keys from \`X-Lore-Provider\` header vs \`WORKER\_DEFAULTS\` providerID; MiniMax-only sessions fall back to \`lastUpstream\`. (b) Staleness is per-session not per-provider. (c) \`getWorkerAuth\` type annotation at pipeline.ts:898 is stale — typed as \`(sessionID?)\` but callers pass \`(sessionID?, providerID?)\`; TypeScript allows due to parameter bivariance.

<!-- lore:019e5922-4280-76aa-9b79-5ebeae433d7a -->
* **Sub-agent session detection, isolation, and differential treatment**: Sub-agent sessions detected via \`x-parent-session-id\` header (pipeline.ts:2773-2809). Each sub-agent gets independent session: temporal storage, gradient state, distillation pipeline, LTM injection, cost tracking. Key differential treatment: (1) cache warming ALWAYS skipped — \`shouldWarm()\` returns false unconditionally (cache-warmer.ts:731-733); (2) \`findRotationPredecessor()\` (session.ts:505) skips sub-agents from Tier 1b rotation; (3) idle distillation/curation runs normally (no isSubagent check). \`isSubagent\`/\`parentSessionId\` persisted to DB (migration v26). Dashboard: collapsible cost-rollup tree via \`loadParentChildMap()\` + \`buildLiveSessionRows()\`; \`rollUp()\` propagates grandchildren costs bottom-up.

<!-- lore:019e4bbd-ade4-7355-8fb1-87df54c8ea69 -->
* **X-Lore-\* headers: project path injection and resolution priority**: X-Lore-\* headers and project path resolution: Priority in \`packages/gateway/src/config.ts\`: 1) \`X-Lore-Project\` header (absolute path, max 1024 chars, strips trailing slashes/control chars), 2) inferred from system prompt (\`cwd\` JSON key, \`Working directory:\` prefix, \`.lore.md\` path, generic \`/home/\`/\`/Users/\` paths), 3) \`process.cwd()\` fallback. Source tagged as \`ProjectPathSource\`: \`"header"|"inferred"|"cwd"\`. Pipeline warns on \`cwd\` source (dedup via \`cwdWarned\` Set). \`agents.ts\` \`envVars()\` injects \`X-Lore-Project\` + \`X-Lore-Git-Remote\` into \`ANTHROPIC\_CUSTOM\_HEADERS\`. Other headers: \`x-lore-session-id\`, \`x-lore-no-store\`, \`x-lore-upstream-url\`, \`x-lore-recall-invoked\` (response). \`lore run\` always runs locally.

### Decision

<!-- lore:019e9ede-fb1c-7a0e-ab8a-1dc1ea878968 -->
* **Migrated to pnpm**: migrated to pnpm.

<!-- lore:019e9eb4-fd09-74d4-8908-7a20bef517b0 -->
* **Migrated to vitest in a follow-up PR**: Migrated from \`bun:test\` to vitest as part of the Node.js runtime migration. Vitest provides better ESM support, more reliable performance, and avoids Bun-specific test runner bugs. The \`bunfig.toml\` preload setup was replaced with vitest config plugins for \`.js\`→\`.ts\` rewriting.

### Gotcha

<!-- lore:019e99ac-28f9-7d1a-ad6d-c4a63431fec6 -->
* **3 recurring timeout-only test failures: entities perf, markdown AST, embedding local model**: Three tests consistently time out (not logic failures): (1) \`entities.test.ts\`: \`truncateToolOutputsInContent — perf regression guards > 100KB payload WITH '/' stays bounded via 64KB scan limit\` — 5s timeout, takes ~6.3s. (2) \`markdown.test.ts\`: \`formatKnowledge > output === normalize(output) — AST serializer produces already-normalized markdown\` — 5s timeout. (3) \`embedding.test.ts\`: \`LocalProvider integration > query and document embeddings have reasonable similarity\` — 60s timeout, uses \`withLocalModel\`. These are infrastructure/performance timeouts, not correctness failures. Don't investigate as logic bugs — increase timeouts or skip in CI if local model unavailable.

<!-- lore:019e1c27-967c-7eb4-bd0e-afb195823970 -->
* **Bun WASM OOM storm: numeric ORT codes are fatal, remote is opt-in only**: Bun WASM OOM storm (PR #545): \`onnxruntime-node\`→\`onnxruntime-web\` (WASM) via \`binaryExternalsPlugin()\`. Bun bugs (#18145, #25677, #31158) cause model-init OOM on healthy machines. \`numThreads=1\` in \`ensurePipeline()\` avoids \`shared:true\`/pthread paths. ORT numeric codes (\`/^\d{6,}$/\`) classified as WASM-fatal via \`isWasmFatalError()\` — single source of truth in \`embedding-worker-types.ts\`. Worker exits once on fatal error; \`localProviderKnownBroken\` latched by both exit and error handlers (no respawn storm). Small-input OOM skips futile retry loop. Remote embedding providers (voyage/openai) are explicit user opt-in via \`search.embeddings.provider\` in \`.lore.json\` — NO automatic fallback from local to remote. When local unavailable, degrades to FTS-only with one-time \`log.info\`. Bun 1.3.14 does NOT fix these.

<!-- lore:019e6a44-dbf1-7df6-ba60-94f75c21b81d -->
* **consolidationUser() batchTarget frame-of-reference mismatch: global target vs batch count produces negative excess**: Trap: \`batchTarget\` in batched consolidation was computed globally (\`Math.max(cfg.curator.maxEntries, entries.length - CONSOLIDATION\_BATCH\_SIZE/2)\` = 1118 for 1143 entries) but \`consolidationUser()\` computed \`excess = count - targetMax\` where \`count\` is the batch size (50). Result: \`excess = 50 - 1118 = -1068\` — prompt tells LLM 'must remove at least -1068 entries', LLM returns empty ops, idle scheduler enters 1h cooldown, system never converges. Fix: \`batchTarget\` must be batch-relative — \`Math.ceil(CONSOLIDATION\_BATCH\_SIZE / 2)\` so \`excess = 50 - 25 = 25\`. Fixed in e8a73f9. CONSOLIDATION\_BATCH\_SIZE=50 (curator.ts:643). Non-batched path (entries ≤ BATCH\_SIZE) correctly uses \`cfg.curator.maxEntries\` as target.

<!-- lore:019e6a1b-3408-7609-ba6b-d4ba9d552afd -->
* **curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields**: curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields: Trap: \`filterOps()\` (curator.ts:109–117) only checks \`op\` is a string — does NOT validate required fields (\`content\`, \`title\`, \`category\`) for \`create\` ops, casting result as \`CuratorOp\[]\` silencing TypeScript. When LLM emits a malformed \`create\` op missing \`content\`, \`applyOps()\` line 226 accesses \`op.content.length\` without null guard → \`TypeError: undefined is not an object\`. Fix: add null guard at line 226 AND field validation in \`filterOps()\` for \`create\` ops. The \`update\` branch at line 252 already has \`op.content !== undefined &&\` guard — \`create\` branch lacks equivalent. \`applyOps()\` calls \`ltm.syncRefs(id)\` and \`entities.syncEntityRefs(id, content)\` after each create/update; \`skipCreate\` flag skips creates (used by consolidation); cross-project guard prevents mutating foreign-project entries. \`cfg\` is in scope in \`runInner\` at line 414.

<!-- lore:019e69f0-a05a-7d48-8e35-76baf13533c5 -->
* **LOREAI-GATEWAY-1E: resp.usage undefined crash in postResponse() for vLLM/OpenAI path**: Trap: \`GatewayUsage\` typed as non-optional on \`GatewayResponse\`, so \`resp.usage.inputTokens\` looks safe. But \`resp.usage\` can be \`undefined\` at runtime (e.g., vLLM returning partial response). All four accumulators construct \`usage\` with \`?? 0\` guards on individual fields — but \`resp.usage\` itself can still be undefined if an exception produces a partial response object. Crash sites: pipeline.ts lines 2069, 2101, 2137-2140, 2150-2153, 2204-2206, 2282-2285, 2341, 2363, 2402; also \`buildOpenAINonStreamResponse\` (openai.ts:296-300). Fix: add \`const usage = resp.usage ?? { inputTokens: 0, outputTokens: 0, cacheReadInputTokens: 0, cacheCreationInputTokens: 0 }\` guard at top of \`postResponse()\` and \`nonStreamHttpResponse()\`, then use \`usage.\*\` throughout.

<!-- lore:019e56fb-b446-759b-aff2-035a83700124 -->
* **LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm — resolveAuth returns same stale token**: LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm. \`resolveAuth(sessionID)\` marks session stale and falls back to \`getLastSeenAuth()\` — same expired token. \`credentialChanged=false\` so retry-once path never taken. Idle scheduler fires 4+ LLM calls/tick (distill, meta-distill, curation, consolidation), each 401ing independently. \`isAuthStale()\` was exported but never called in non-test source. Fix (PR #548): (1) \`isAuthStale(sessionID) && !resolveAuth(sessionID)\` guard in \`scheduleBackgroundWork()\` (pipeline.ts:2785) and \`idle.ts:144\`; (2) \`resolveAuth()\` returns null when global fallback matches stale credential; (3) add \`/Worker upstream auth error/\` to \`TRANSIENT\_ERROR\_PATTERNS\` in instrument.ts. \`resolveAuth()\` is pure read-only (no side effects). Double-call pattern (guard + quota check) is intentional and race-free (JS single-threaded, no awaits between calls).

<!-- lore:019e8d03-3fdf-76cf-b87d-eab142cad8b1 -->
* **OpenAI protocol: split tool messages + recall leak on streaming path**: Two OpenAI-protocol bugs. (1)  (translate/openai.ts) pushed separate user messages per  msg — breaks  Pass 2 which only checks . Fix: coalesce consecutive tool messages into one user message. (2) OpenAI/openai-responses streaming dispatch (pipeline.ts) accumulated upstream SSE then returned early, skipping the recall loop. Fix:  closure called from all three accumulated-response paths. Also:  (recall.ts) must set  — follow-ups parsed as JSON via ; without override, streaming clients forward  causing SSE→JSON parse crash.

<!-- lore:019e4eab-59b4-7fe3-be03-874f682cc340 -->
* **openai.ts: array-format content in system/developer messages silently dropped**: openai.ts / openai-responses.ts parsing traps: (1) \`parseOpenAIRequest\` coerces non-string content to \`""\` — FIXED in PR #526: array-format system/developer content now extracts text parts. (2) \`buildOpenAIMessages\` (openai.ts:523–529): \`tool\_result\` blocks pushed mid-loop before \`textParts\`/\`toolUses\` flushed — tool\_result appears before text/tool\_use of same message. (3) \`buildOpenAIMessages\` always re-emits system as \`role:'system'\`, never \`'developer'\`. (4) \`parseOpenAIResponsesRequest\` reads system prompt from top-level \`instructions\` field. (5) \`item\_reference\` and \`reasoning\` items in \`parseInputItems\` silently dropped. \`store\` field not in extras allow-list — silently dropped. LTM injection for Responses API: \`\[req.system, cache?.stableLtmSystem, cache?.ltmSystem].filter(Boolean).join('\n\n')\`. \`buildOpenAIMessages\`: uses array content form only when \`hasOpaque=true\` (multimodal), plain string otherwise (cache stability). Consecutive role:"tool" messages coalesced into one user message.

<!-- lore:019e6950-8200-7473-87a5-45a861bb84af -->
* **recall.ts: unescaped quotes/ellipsis in marker strings cause store miss and raw marker leak**: Trap: \`buildRecallMarker()\` embeds raw \`query\` between double-quotes and raw \`id\` before \`…\` — looks safe because queries are usually plain text. But if \`query\` contains \`"\`, \`MARKER\_REGEX\` (\`/📚 Searching (.+?) for "(.+?)"…/\`) truncates the captured group at the first inner quote; \`recallStoreKey()\` produces a mismatched key; \`store.get(key)\` returns undefined; the marker is left as raw text in the conversation visible to the upstream LLM. Same issue for \`id\` containing \`…\`. Fix: escape \`"\` → \`\\"\` in \`buildRecallMarker()\` and unescape in \`parseRecallMarker()\`; for IDs, percent-encode or strip \`…\` before embedding.

<!-- lore:019e9d03-8f9f-7811-b504-9d4bc33b009e -->
* **Recurring plan\_exit failure: other:the-user-dismissed-this**: The \`plan\_exit\` tool repeatedly failed with "other:the-user-dismissed-this" across 5 sessions (9 total failures). Sample error: The user dismissed this question. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019e9e6c-9843-7945-b522-c99de8fcea86 -->
* **Recurring read failure: not\_found**: The \`read\` tool repeatedly failed with "not\_found" across 4 sessions (4 total failures). Sample error: File not found: /home/byk/Code/opencode-lore/packages/gateway/src/instrument.ts. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019e2e20-95b3-7a9d-ab38-77d87eafecc4 -->
* **splitSegments() infinite recursion on oversized single messages**: \`splitSegments()\` infinite recursion on oversized single messages in \`packages/core/src/distillation.ts\`: recurses infinitely when a single message exceeds \`maxSegmentTokens\` (16384). \`findSplitIndex()\` returns \`messages.length\` (=1), so \`left = messages.slice(0, 1)\` produces an identical recursive call. Triggered on large tool outputs (~49KB+). Fix: add base case after the \`totalTokens <= maxTokens\` guard — \`if (messages.length <= 1) return \[messages]\`. The oversized message becomes an indivisible segment.

<!-- lore:019e49d5-15c4-78d1-8bbc-f43fde34d9f1 -->
* **startServer() returns plain object, not Promise — use startGateway() for programmatic async use**: Gateway startup traps: (1) \`startServer()\` returns \`{ stop, port, hosts }\` synchronously — use \`startGateway()\` from \`cli/start.ts\` for \`Promise\<GatewayHandle>\`. (2) \`loadConfig()\` in \`config.ts\` is SYNC — calling \`.then()\` throws TypeError. (3) EADDRINUSE fires as async \`'error'\` event — wrap \`server.listen()\` in a Promise. (4) Use port \`0\` (ephemeral) in tests. (5) \`@loreai/core\` exports async \`load(directory)\` — distinct from gateway's sync \`loadConfig()\`. (6) In-process callers MUST pass \`local:true\` — hosted mode disables FS ops in \`@loreai/core\` (breaks \`getGitRemote()\`, \`.lore.md\` import, config loading). (7) \`lore run\` and \`lore import\` always run locally — both set \`opts.local=true\`.

### Pattern

<!-- lore:019e56d6-9cca-7a77-aa1a-a5850daef395 -->
* **Gateway test patterns: unit vs integration vs subprocess isolation**: Gateway test patterns in \`packages/gateway/test/\`. Three patterns: (1) Unit: import functions directly, build fixtures inline — \`makeRequest()\`/\`makeUserMsg()\` factories. (2) Integration: \`createHarness()\` starts real server on random port with replay interceptor, provides \`chat()\` + \`queryDB()\`. (3) Subprocess isolation: \`mock.module()\` calls isolated in subprocess files to avoid global cache pollution. DB isolation via root \`bunfig.toml\` preloading \`packages/core/test/setup.ts\`. \`GatewayRequest\` minimal required fields: \`protocol, model, system, messages, tools, stream, maxTokens, metadata, rawHeaders\`. Use port \`0\` in tests. Session ID regex requires \`\[a-f0-9]\` hex chars only — alphanumeric IDs with non-hex chars (e.g. \`g-z\`) will fail marker extraction.

### Preference

<!-- lore:019e920f-0876-7aad-a306-8129434a3d6b -->
* **Always embed behavioral directives as code comments to assert invariants**: Code quality behavioral directives: (1) Write explicit invariant assertions as imperative code comments at enforcement points. (2) Document source findings as structured bullet points with file path, line ranges, function names. (3) Update comments that misrepresent actual behavior. (4) When a test fails in full suite but passes in isolation, re-run full suite before investigating. (5) Use correct pronouns; never revert to assumed gendered pronouns. (6) 🔴 = durable/identity-level facts; 🟡 = session-scoped findings — never promote 🟡 to 🔴 without explicit user confirmation. (7) After bulk type migrations, always run typecheck. (8) Update ALL affected test files when making breaking type changes. (9) Run full test suite before committing; record exact pass/fail/skip counts; flag new failures. (10) Prefer update over create for knowledge entries.

<!-- lore:019e9e7f-8fc5-7400-9258-0c04c031a666 -->
* **Always enforce same-provider constraint for worker/warmup model selection**: Absolute directives for worker/warmup model selection: (1) \`getWorkerModel()\` ALWAYS returns same provider as session — use session model or cheaper same-provider model with quality floor (e.g., sonnet-4 for Anthropic). (2) NEVER cross-provider batch — only batch when same provider supports it. (3) Session-aware LLM wrapper ALWAYS routes workers to session's upstream. (4) Wire protocol resolved from provider route registry (\`resolveProviderRoute(providerID)?.protocol\`), NOT hardcoded \`providerID === 'openai'\` — NVIDIA/OpenRouter use OpenAI protocol but have non-'openai' providerIDs. Test mock in \`packages/gateway/test/helpers/idle-worker.ts\` returns \`{ providerID: 'anthropic', modelID: 'claude-sonnet-4-20250514' }\` — update when changing worker model logic.

<!-- lore:019e9f0c-0c7f-71fe-822e-2777ca7e708c -->
* **Always fix all occurrences of a pattern before marking a task complete**: When resolving an issue, the user systematically searches the codebase for every file that exhibits the same pattern and applies the identical fix. This includes checking imports, class usage, API consumers, and configuration files. The user expects thoroughness: no instance should be left unaddressed, and no inconsistency should remain between files. Future changes should always be propagated to all relevant locations.

<!-- lore:019e9eb2-02ae-7169-a8c7-38457f047c29 -->
* **Always investigate CI build failures on main branch**: Always investigate CI failures on every push. The user provides the failing workflow run IDs and statuses. If any workflow (CI, eval, etc.) fails, immediately inspect annotations and logs, identify root cause, and propose a fix. Proactively check main CI after merging PRs. Monitor nightly builds when asked. Never proceed with further work while CI is failing or running.

<!-- lore:019e9e73-8cfb-79c0-95f8-47180d7c9534 -->
* **Always investigate specific code paths with file paths and line numbers before drawing conclusions**: When exploring an unfamiliar or complex codebase (especially the 'lore gateway'), the user consistently requests targeted, evidence-based investigation: they name specific files, functions, and fields to trace (e.g., \`getWorkerModel()\`, \`cfg.model\`, \`SessionState\` fields), ask where values are set vs. read, and want exact file paths and line numbers in the findings. They do not want high-level summaries or guesses — they want the assistant to read the actual source and report concrete call sites, conditions, and data flow. Always search the specified files directly, quote relevant lines, and report findings with precise locations before offering any interpretation.

<!-- lore:019e9ead-6692-76f2-93d8-e057649d2865 -->
* **Always migrate projects from Bun to Node.js and pnpm**: Always migrate projects from Bun to Node.js and pnpm. The user consistently follows this pattern, including converting the Lore marketing site (PR #559). Root package.json bun scripts may remain as a pending migration artifact — convert to pnpm scripts. Prior successful migrations (getsentry/cli, spotlight) guide the process.

<!-- lore:019e9e82-5040-71ca-add2-d5aff0f756de -->
* **Always pass full upstream snapshot objects instead of extracting single fields for function parameters**: When updating function signatures that previously accepted a single extracted field (e.g., \`providerID?: string\`), prefer refactoring to accept the full upstream snapshot object. All call sites should pass the full object (e.g., \`state.lastUpstream\`) rather than a single extracted property. Exception: call sites where no session context is available may remain unchanged with no-arg calls. This pattern applies to gateway worker model resolution, compaction summary generation, and similar pipeline functions.

<!-- lore:019e9f06-597e-75c0-8f6a-2d2b4c144bc8 -->
* **Always remove all Bun-specific code and build targets**: The user systematically removes Bun-specific functionality from the codebase. This includes: deleting dist/bun directories, removing Bun API calls (Bun.mmap, Bun.file, Bun.zstdDecompressSync) and replacing with Node.js equivalents (readFileSync, readFile, zstdDecompressSync), removing Bun-specific mocks in tests, removing Bun bundle generation from build scripts, and removing Bun-related test assertions. When encountering Bun-specific code, always replace with Node.js alternatives rather than silencing or skipping. Do not add new Bun-specific code or maintain existing Bun targets.

<!-- lore:019e9ecd-0ac5-7435-ae2c-aab707294330 -->
* **Always require thorough codebase exploration and structural consistency across projects**: The user consistently demands a complete, detailed survey of the project structure, configuration files, and key components before any modification. They explicitly list files and directories to examine, ask for specific aspects (e.g., layout system, content collections, dependencies), and expect comprehensive reports. When adapting changes from one project to another, they insist on replicating the same proven patterns (e.g., Astro + pnpm setup, content collections layout). To follow this pattern, the AI must start each task by systematically exploring the codebase, providing a structured overview, and ensuring that any changes maintain consistency with the user's established templates and conventions.

<!-- lore:019e9ed7-a529-7e5b-9439-12a1ccd25e76 -->
* **Always respect user's task completions and explicit directives**: The user frequently marks tasks as complete based on their own judgment, even if the assistant considered them blocked or requires external dependencies. They also give direct instructions to modify assistant behavior (e.g., calling plan\_exit). When the user provides status updates like git status or CI results, incorporate them immediately. Defer to the user's assessment and follow their explicit commands without questioning to stay aligned with their plan.

<!-- lore:019e9ecb-0afa-7708-b3fa-242aca474618 -->
* **Always review the plan before execution begins**: Always review the plan before execution: present tasks and priorities, wait for explicit confirmation, then begin. For multi-step cleanups/migrations, execute steps strictly sequentially, tracking progress as the user does ('completing Step 1'). Never skip ahead or combine steps without verification. Never start editing files before the plan is approved.

<!-- lore:019e9a1c-6675-7fef-8415-5a36ba3ca84b -->
* **Always run full verification (typecheck + complete test suite) after code changes, extending timeouts when needed**: The user consistently runs \`tsc --noEmit\` to verify TypeScript correctness immediately after applying code edits. This verification step occurs in every session, particularly in the packages/gateway worktree. When typecheck fails (which happens frequently during the Bun→Jest migration), the user iterates by making additional fixes and re-running typecheck until it passes. Always run typecheck after edits to catch TypeScript errors early, especially when working on migrations or making multiple changes.

<!-- lore:019e9dd7-6320-708a-8d60-b21a3b5072ea -->
* **Always run full verification suite and update .lore.md with findings**: After code changes: run typecheck, tests, and lint, then update \`/home/byk/Code/opencode-lore/.lore.md\` with technical findings (architecture decisions, gotchas, credential routing, auth issues) before committing. .lore.md is a living knowledge base — include specific line references, root cause explanations, and routing/config semantics. Proactively offer to update .lore.md when non-obvious technical details are discovered.

<!-- lore:019e9eec-0c17-7da7-ad2e-e0203ec2e828 -->
* **Always separate migration steps into distinct pull requests**: The user consistently breaks down large-scale migrations, such as transitioning from Bun to Node/pnpm/vitest, into multiple small, independent PRs. Each PR addresses a single aspect (package manager, runtime APIs, test runner, CI, cleanup). This incremental strategy allows CI verification for each change and reduces risk. The user signals unfinished parts via comments (e.g., 'Bun is still used as the TS test runner in this PR. Will be removed when we migrate to vitest in a follow-up PR.'). The assistant should always propose or respect this pattern by keeping changes logically separated and ensuring each PR is self-contained and CI-ready.

<!-- lore:019e9eb7-a651-7441-87d3-74d060f4f3db -->
* **Always share multiple related source files from worktree for context**: The user consistently initiates sessions by sharing several source files (Astro, HTML, CSS, Markdown) from their OpenCode worktree. These files are typically interconnected (e.g., layout + theme, or multiple pages). The user prefers to provide full file contents rather than summaries. This pattern indicates that the user expects the assistant to use these files as complete context for reviews, consistency checks, or modifications. After sharing, the user often requests a thorough review of the files, particularly for copy consistency across pages. When the user shares files, the assistant should read all of them, cross-reference for alignment, and base any work on the full context provided.

<!-- lore:019e9f01-152d-7349-9f01-c0b26a130f56 -->
* **Always share outcomes of automated processes and expect assistant to monitor progress**: The user consistently provides status updates from CI runs, edit applications, or other automated checks. They expect the assistant to acknowledge these updates, wait for pending steps to complete, and verify results before moving to the next task. This pattern ensures the assistant stays aligned with the current state of work. The assistant should respond by confirming receipt, monitoring any in-progress items, and reporting final outcomes before proceeding further.

<!-- lore:019e9d06-3be9-7a34-8c32-095bc13e3374 -->
* **Always survey existing code structure before making changes**: Before creating new files, refactoring, or writing code, the user expects thorough codebase reconnaissance: read tsconfig, package.json, migration status, existing export patterns. For the \`lucky-cactus\` Astro marketing site, this means specifically cross-referencing all pages for UI class consistency (e.g., \`.sr\` scroll-reveal) and structural parity before implementing changes.

<!-- lore:019e99ab-3a23-72d5-ab83-ace1222ff7f0 -->
* **Always trace field usages exhaustively before modifying shared state structures**: When planning changes to shared state fields (e.g., SessionState fields like \`lastUpstream\`, \`sessionAuth\`), Burak Yigit Kaya systematically locates every read, write, and type reference across all files before touching any code. This includes grepping for the field name, checking imports, identifying dead code, and noting which call sites use lossy mapping functions. Expects the assistant to enumerate all consumers with file:line precision, classify each as read/write/type, and flag any needing updates — before proposing or implementing any fix. Also: when a proposed refactor has 64+ call sites, prefer a surgical minimal-churn approach (e.g., compound-key existing maps, add parallel field) over a full structural replacement.

<!-- lore:019e9ead-6ed4-7111-9e3d-bf069da53663 -->
* **Always use conventional commit messages and squash merge pull requests**: The user consistently formats commit messages as type(scope): description, using types like fix, ci, docs, and feat with a scope that names the affected module. Descriptions are imperative, lowercase, and omit trailing periods. The assistant should ensure all commit suggestions adhere to this format, and when reviewing changes, propose messages that follow this pattern. This preference applies to every commit in the project, regardless of the kind of change.

<!-- lore:019e9ee1-822f-790a-a7b8-aae94da99f5f -->
* **Always use git worktrees and force push for PR management**: When a PR branch has accumulated unrelated commits, unwanted changes, or complex merge conflicts: prefer creating a fresh branch from origin/main and extracting only the relevant file changes into a single squash commit. Steps: (1) new branch from main; (2) stage relevant changes from diff (not cherry-pick, which may pull unwanted history); (3) restore config files (package.json, .gitignore) from main to avoid old PR cruft, then apply targeted edits; (4) regenerate lockfile against final package.json before committing; (5) force-push to PR branch. Cherry-picking is preferred when history is clean; use diff-extraction when cherry-pick pulls unwanted changes. After force-pushing, restore worktree stashes.

<!-- lore:019e9ef5-73e7-7c40-897f-6a01647e8ccc -->
* **Always wait for all CI checks to pass before merging**: The user consistently shares CI run results, notes every failed or pending job, and waits for complete green CI before merging. They review both successful and failing runs in detail, and when failures occur (e.g., typecheck, lint), they investigate before proceeding. This pattern means you should automatically report full CI status after any push, wait for all jobs to finish, and only merge when all required checks pass. Do not suggest merging or proceeding until CI is fully green, and alert the user immediately if any job fails or is blocked.

<!-- lore:019e9da2-4bbe-704a-8641-f56946c9b5f2 -->
* **Document design rules and invariants as explicit code comments**: Invariant (observed 33+ sessions): Pipeline always returns internal Anthropic-format responses regardless of incoming request protocol. The OpenAI/OpenAI-Responses protocol adapters at the edges handle request/response translation. Never build logic that assumes raw OpenAI format passes through the core pipeline.

<!-- lore:019e9440-0416-7b08-9dfd-8035b6f926a6 -->
* **Follow the established git workflow (branch, PR, review)**: Behavioral pattern detected across 20 sessions (action: enforced-workflow). The user consistently demonstrates this behavior.

<!-- lore:019e9ecc-6ad8-725b-80aa-f2779860bc59 -->
* **Prefers change IDs (letters) over commit IDs (hex)**: prefer change IDs (letters) over commit IDs (hex),

<!-- lore:019e9eb3-5a7c-788d-a290-e9170c04a16a -->
* **Prefers Node.js over Bun for runtime APIs**: prefers Node.js over Bun for runtime APIs.

<!-- lore:019e95b5-075e-7e09-a79c-69f8fa8ef3ed -->
* **Prefers update over create for knowledge entries; (6) Prefer minimal viable scope — gradual throttling over hard blocks**: Prefer update over create for knowledge entries; (6) Prefer minimal viable scope — gradual throttling over hard blocks,

<!-- lore:019e9ec4-9ac7-7f1c-a82a-7bcfb0d4543c -->
* **Remove Bun runtime dependencies and build artifacts**: Remove Bun runtime dependencies and build artifacts: COMPLETED in this session: bundle-exports.test.ts deleted; bun stub removed from script/build.ts; dist/bun/ build target deleted; packages/core/test/db-driver.test.ts deleted; packages/core/test/worker-model.test.ts require path fixed; packages/gateway/package.json 'files' array updated to remove dist/index.bun.js; engines field updated to remove Bun. REMAINING: (1) packages/core/src/db/driver.bun.ts — Bun-only driver using 'bun:sqlite'; delete it. (2) 12 test files using Bun-only \`import.meta.url\` syntax — replace with \`fileURLToPath(new URL('.', import.meta.url))\` for Node compatibility. Affected files: packages/core/test/{agents-file,config,hosted,lat-reader}.test.ts; packages/core/test/import/{aider,claude-code,cline,codex,continue}.test.ts; packages/opencode/test/index.test.ts. (3) Remove @types/bun, @sentry/bun, bunfig.toml. (4) Remove console.error debug line from driver.node.ts. (5) Update build script to remove bun target. (6) Remove \`bun\` export from packages/gateway/package.json exports field — still present as \`"bun": "./dist/index.bun.js"\`. User directive: no silencing or skipping — remove all Bun code entirely.

<!-- lore:019e9d05-c00a-7d84-9585-dc73d76a69be -->
* **Requested Investigation**: Behavioral pattern detected across 5 sessions (action: requested-investigation). The user consistently demonstrates this behavior.

<!-- lore:019e9f05-d778-7200-b2f1-f30daafe8be3 -->
* **Verb Noun**: Behavioral pattern detected across 3 sessions (action: verb-noun). The user consistently demonstrates this behavior.

<!-- lore:019e9d98-c82e-7c98-8f5a-590dbeb71060 -->
* **Wait for all CI jobs green before merging PRs**: Wait for all CI jobs green before merging PRs. Monitor to completion: test, binary smoke (darwin-arm64, windows-x64), changes, preview, CI Status must all pass. Nightly/release jobs skipping is acceptable. Run local typecheck (\`pnpm -r typecheck\`) AND lint (biome, stylelint) as pre-push gates — CI green alone is insufficient if local typecheck fails. Enable auto-merge only after last job flips to pass. Do not merge on partial CI results.
