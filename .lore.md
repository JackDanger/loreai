<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019ecd94-4df4-787b-89f7-31873df5ec0e -->
* **agents.ts: AGENTS registry shape and run.ts explicit-command spawn lookup**: \`AgentDef\` interface (agents.ts:16-33): \`name\`, \`displayName\`, \`binary: string\`, \`detect(): string | null\`, optional \`envVars()\`, optional \`cliArgs()\`. \`binary\` MUST be a constant placeholder string (NOT resolved at module load — avoids stale registry in long-running processes and platform-mutation test issues). \`detect()\` is called fresh each invocation. \`run.ts\` explicit-command branch: if matched by \`agent.name === cmdArgs\[0]\` → use \`detect()\`; if matched by \`agent.binary === cmdArgs\[0]\` → use literal \`cmdArgs\[0]\` (zero behavior change for existing PATH agents). \`main.ts\` gatekeeper MUST use \`AGENTS.some((a) => a.name === command || a.binary === command)\` — NOT \`AGENTS.map(a => a.binary).includes(command)\` (binary is a placeholder, not the resolved path). Current registry (6 agents): \`claude-code\`, \`claude-code-desktop\`, \`codex\`, \`pi\`, \`opencode\`, \`hermes\`.

<!-- lore:019ecd94-4dc6-7687-b087-6bc8e43730bf -->
* **Claude Code Desktop support: agent entry, detection, and safeStorage constraint**: Claude Code Desktop: \`claude\` CLI hardcodes ANTHROPIC\_BASE\_URL, ignoring parent env (anthropics/claude-code#67619). (1) isClaudeDesktopInstalled: /Applications/Claude.app (mac) or %LOCALAPPDATA%\Programs\Claude\Claude.exe (Win). (2) \`claude-code-desktop\` agent: binary is constant placeholder; detect() called fresh per invocation; NEVER injects ANTHROPIC\_BASE\_URL. (3) setupClaudeCodeDesktop writes ~/.claude/settings.json + setx on Win; in-app env editor is manual fallback only. CI mac test always takes 'not detected' branch.

<!-- lore:019eb931-7257-7ef6-87db-b2dd034c3ea7 -->
* **embedding-worker.ts: self-heal retry for corrupt model — skipped for vendored binaries**: embedding-worker.ts self-heal retry for corrupt model — skipped for vendored binaries. \`initPromise\` wraps \`loadPipeline()\`; on catch, if \`!vendorModel && isCorruptModelError(msg)\`, calls \`purgeCachedModel()\` then retries once. NEVER post \`{type:'init-error'}\` before the retry — \`embedding.ts\` sets \`localProviderKnownBroken=true\` permanently on init-error. Only the final \`.catch\` may post \`init-error\`. Error classifiers are INLINED — Node ESM resolver cannot map \`./foo.js\` → \`./foo.ts\` for spawned workers. TRAP: \`/load model .\* failed/i\` is too broad — misclassifies 401/403 → purge→redownload→401 loop. \`vendorModel=true\`: skip all purge/heal paths (model is read-only inside binary).

<!-- lore:019ebdd7-b5a4-7b44-840d-2bd981d368ff -->
* **gradient.ts: selectDistillations() always preserves meta (gen>=1) entries — never evicted by recency scoring**: gradient.ts selectDistillations() always preserves meta (gen>=1) entries — never evicted by recency scoring. distLimit slots = limit - meta.length; gen-0 scored by recency (70%) and content signal (30%). Meta guarantee retains session history (fixes #417). distilledPrefixCached three-branch: (1) no new rows → cache hit, (2) new gen-0 → append delta (prepends headers, causes duplication in messages\[1]), (3) full re-render on meta rewrite. Cache validity: same sessionID, rowCount grew, last cached row unchanged. Layer 0 (warm-cache) does not inject prefix — only Layer 1+ (compression mode).

<!-- lore:019ecab6-f934-757f-a73e-2a14234d8c4f -->
* **lore gateway: thinking parameter handling — never forwarded in bare API calls, lives in req.metadata**: Lore gateway thinking parameter: NOT in KNOWN\_BODY\_FIELDS → parseAnthropicRequest puts it in req.metadata.thinking. buildAnthropicRequest restores metadata. Bare calls NEVER include thinking. cache-warmer detects via \`"thinking" in body\`; uses max\_tokens:1 for thinking sessions. LATENT BUG: max\_tokens=1 violates Anthropic's max\_tokens > budget\_tokens when budget\_tokens>1, causing 400 on every thinking-session warmup (non-fatal, circuit-breaker protected). Meta request passthrough MUST NEVER enable caching. resignBody gate uses hasBillingHeader(req.system) — cache-warmer.ts:1381 calls resignBody directly, bypassing this gate.

<!-- lore:019ebc36-830a-7307-8a10-9ff12dfa5740 -->
* **openai-responses.ts: Codex delta must live in a dedicated wrapper, not inline conditionals**: openai-responses.ts: Codex delta must live in a dedicated wrapper, not inline conditionals. Use \`buildOpenAICodexUpstreamRequest\` and \`applyCodexResponsesDelta\` helper. Codex divergences: URL \`/codex/responses\`, \`store:false\`, control fields passthrough, \`max\_output\_tokens\` suppression. \`applyCodexResponsesDelta\` mutates body post-build. Guard \`if (!extras) return\` runs AFTER setting \`store=false\` — so \`store:false\` always applies even when \`req.extras\` is absent. TRAP: no test that control fields (\`include\`, \`prompt\_cache\_key\`, etc.) are still emitted after refactor — regression could silently drop them. Always add regression tests for Codex builder bugs: (1) suppressed field absent even when client sends it, (2) non-codex path still emits the field, (3) worker-side asserts field absent.

<!-- lore:019ecdf2-310b-77fc-a51f-189ecb397a38 -->
* **pipeline.ts: safeDeltaInsertIndex and durable prompt delta system**: pipeline.ts durable prompt delta (content-anchored): \`appendKnowledgePromptDeltaAt\` persists delta with \`MessageInsertSelector.afterHash\` = fnv1a(\`${role}\x1f${blocksToText(content)}\`) of the message immediately before insert point. \`resolveDeltaInsertIndex\`: tail-first scan for \`afterHash\` match → \`safeDeltaInsertIndex(messages, i+1)\`. Legacy rows (no \`afterHash\`) fall back to clamped absolute \`insertAt\`. TRAP: tail-first scan rebinds to NEWEST hash match — if same content repeats (e.g. duplicate assistant \`"ok"\` or \`"DONE"\` messages, or \`\[tool results provided]\` placeholder injected by \`removeOrphanedToolResults\`), append-only turns shift delta to wrong index (invariant 2 violation). \`blocksToText\` for \`tool\_use\` uses only \`block.name\` (not id/input) → same-name tool calls are indistinguishable. Fix direction: prefer occurrence nearest creation-time \`insertAt\` (exact hit at persisted \`insertAt-1\` before falling back to scan). \`shouldResetDeltaOnCompression\`: true when curLayer>=1 && curLayer!==prevLayer. \`applySessionPromptDeltas\` re-runs \`safeDeltaInsertIndex\` at replay for steady-layer-1 drift.

<!-- lore:019ebc96-83c7-7cd5-98d2-ca85724c2a85 -->
* **pipeline.ts: two curation paths — in-flight vs idle — must be gated separately**: Pipeline two curation paths: in-flight (mid-turn, gated by sessionState.curationScheduled) and idle (flush gen-0 then metaDistill) — must be gated separately. In-flight avoids system\[2] cache busts. System\[2] churn root causes: context-health note appended after diff-pinning, LTM entry selection changes per-turn. In-flight gen-0 distillation mutates messages\[1] in compression mode. LTM pin invariants: NEVER bust cache; ltmCacheText must never diverge from ltmPinText; coalesced seq-0 delta row describes full cumulative frozen→current delta.

<!-- lore:019ecb52-ff7c-7515-a435-be511f6fdde7 -->
* **session.ts: 3-tier session ID strategy, generateSessionID format, and marker functions**: Session ID strategy: Tier 1 priority: x-lore-session-id, x-claude-code-session-id, x-session-id, x-session-affinity. ROTATION\_ELIGIBLE\_HEADERS = ONLY x-session-affinity. x-claude-code-session-id is NOT rotation-eligible — new value = new session, never merge (cross-project contamination). isClaudeCodeClient checks x-claude-code-session-id header; callers should also check hasBillingHeader on system. isCC in pipeline.ts = isClaudeCodeClient OR hasBillingHeader. Tier 2: learned headers after 3 stable turns. Tier 3: SHA-256 of first user msg + auth suffix. generateSessionID: 12 bytes = 8 random + 4-byte BE unix ts, base62. Desktop uses same x-claude-code-session-id as CLI.

<!-- lore:019ec7fc-3bab-7e84-b5b3-e8e9d802a806 -->
* **Supabase auth constraints: email templates blocked on free tier; no device flow**: Supabase auth constraints: Free tier blocks email template customization (HTTP 400); custom SMTP live: \`smtp\_host: mail.smtp2go.com\`, sender \`keeper@withlore.ai\`. Email templates pushed via Management API PATCH (4 fields only). \`supabase config push\` clobbers entire auth config — use Management API PATCH for targeted updates. GitHub OAuth must be enabled separately in Auth → Providers → GitHub. OTP length configurable (not fixed at 6 digits) — observed 8-digit tokens; use \`/^\d{4,12}$/\`. \`lore login --email\` active once custom SMTP configured. Supabase project ref: \`jlwxsrmvomocgngbxmcf\`, region \`us-east-2\`. \`config.toml\` auth settings only affect \`supabase start\` (local stack) — dashboard is authoritative for production. Email templates MUST include \`{{ .Token }}\` in body or users receive only a \`{{ .ConfirmationURL }}\` link. Redirect URLs: must include \`https://\` when not localhost; must include trailing \`/\`.

<!-- lore:019ece12-fa48-7da3-bfe9-38e827a14062 -->
* **sync engine: client writes directly to Supabase PostgREST — all isolation in Postgres**: Sync engine architecture: clients write DIRECTLY to Supabase PostgREST using anon key + user JWT (role \`authenticated\`). NO server gateway in front of writes. ALL isolation, abuse limits, and quota enforcement live in Postgres: RLS (owner-scoped FOR ALL per table), triggers (enforce\_row\_quota BEFORE INSERT OR UPDATE, guard\_profile\_tier BEFORE UPDATE on profiles), CHECK constraints (column size caps), and column-level GRANTs. \`profiles.tier\` (free/pro) drives quotas via \`plan\_limits\` table. SQLite is local source of truth; sync\_outbox captures changes via TEMP triggers per-connection (apply-suppression scoped per connection via \`\_sync\_applying\` TEMP table). Supabase project ref: \`jlwxsrmvomocgngbxmcf\`. User's own ID: \`397d98e5-6fb1-4959-aba7-e521f1ea4e39\` (ben@byk.im, tier=free) — never modify. Remote PKs: composite \`(owner\_user\_id, id)\` for 4 tables; \`(owner\_user\_id, knowledge\_id, entity\_id)\` for join table. Local apply uses \`idColumns\` only. \`authenticated\` role has \`rolbypassrls=false\`; \`service\_role\` has \`rolbypassrls=true\` — RLS genuinely enforced under \`SET LOCAL ROLE authenticated\`.

<!-- lore:019ecb58-7fc8-70ad-85eb-5c27233ec4e3 -->
* **sync engine: Supabase mirror schema (migration 0002) — 5 tables, RLS, triggers**: Sync engine Supabase mirror schema — 6 migrations: \`0001\_accounts.sql\` (profiles table, SECURITY DEFINER trigger handle\_new\_user, profiles\_set\_updated\_at trigger). \`0002\_sync\_basic.sql\` (5 tables: knowledge, entities, entity\_aliases, entity\_relations, knowledge\_entity\_refs; composite PK (owner\_user\_id, id); sync\_set\_updated\_at BEFORE UPDATE trigger; owner-scoped RLS FOR ALL). \`0003\_sync\_limits.sql\` (plan\_limits, quota trigger v1 — BEFORE INSERT, counts ALL rows). \`0004\_sync\_hardening.sql\` (tier lock via column-level GRANTs + guard\_profile\_tier trigger, size CHECKs, DML GRANTs to authenticated, quota trigger v2 — counts LIVE rows, skips existing PKs). \`0005\_sync\_quota\_hardening.sql\` (quota trigger v3 — BEFORE INSERT OR UPDATE, revival gate, advisory lock per (owner,table), id cols ≤64). \`0006\_tier\_upgrade\_path.sql\` (GRANT SELECT, UPDATE ON profiles TO service\_role). Free tier caps: knowledge=500, entities=30, entity\_aliases=300, entity\_relations=300, knowledge\_entity\_refs=2000. NULL cap = unlimited.

### Gotcha

<!-- lore:019eccdc-2a14-7401-9661-20cd5c574a47 -->
* **buildKnowledgeDeltaMessage: skipped-overflow slice is NOT order-stable — pre-existing cache bust risk**: buildKnowledgeDeltaMessage: \`skipped\` overflow slice is NOT order-stable. \`formatKnowledge\` sorts \`rendered\` body alphabetically (byte-stable), but the \`skipped\` overflow array (entries >400-token \`KNOWLEDGE\_DELTA\_TOKEN\_BUDGET\`) preserves \`forSession\` ranking order, driven by per-turn \`contextHint\`. Re-ranking same set between turns → \`skipped\` membership/order changes → different bytes in 'Additional Changed Knowledge (truncated)' \`.slice(0,3)\` → cache bust with no real content change. Trigger: curator burst updating many pinned entries simultaneously. Fix: sort \`skipped\` deterministically (e.g. by entry id) before slicing. Non-blocking but amplified by frozen-baseline fix (material-delta path now fires every turn while diverged).

<!-- lore:019ebc55-ba2e-7271-9906-905e4f5ed9bf -->
* **CCH seed extraction: seed may persist in binary but algorithm/preimage changed across versions**: CCH seed extraction: consult \`quality/CCH.md\`. Seed \`0x4d659218e32a3268\` (SEED\_2\_1\_138) unchanged in 2.1.175. Hash-input ≠ wire body — cch fn scans for \`cch=\` placeholder, splits/transforms before hashing. Binary is EXEC (not PIE), fixed base \`0x200000\`. CCH\_PLACEHOLDER = \`cch=00000\`; signBody replaces with hash; regex \`/cch=(\[0-9a-fA-F]{5});/\` (trailing semicolon required). Work files in stable dir (e.g. \`~/Code/cch-investigation/\`), not \`/tmp\`.

<!-- lore:019ebe5d-132f-7674-a47a-dd41c820fc07 -->
* **cch-seed-check.yml: @loreai/core must be built before scripts run**: cch-seed-check.yml: \`@loreai/core\` must be built before scripts run. \`scripts/check-cc-version.ts\` and \`scripts/extract-cch-seed.ts\` both import \`packages/gateway/src/cch.ts\` → imports \`@loreai/core\` → resolves to \`packages/core/dist/node/index.js\`. Root \`postinstall\` only builds \`@loreai/gateway\`, never \`@loreai/core\`. After \`pnpm install --frozen-lockfile\`, \`dist/\` is missing → scripts crash with \`ERR\_MODULE\_NOT\_FOUND\`. Fix: add \`pnpm --filter @loreai/core run build\` after install in BOTH \`check-version\` and \`extract-seed\` jobs (same pattern as \`ci.yml\` line 501 before \`check:docs\`).

<!-- lore:019eb4d0-6fd9-7ef5-b22f-0f156eeca4b0 -->
* **cli/main.ts OPTIONS map: undeclared flags silently become booleans under strict:false**: cli/main.ts OPTIONS map: undeclared flags silently become booleans under \`strict:false\`. \`parseArgs\` with \`strict: false\` treats any undeclared \`--flag value\` as \`{flag: true}\` with value falling into positionals — \`--flag=value\` (equals syntax) still works so it looks correct. Every flag used in ANY subcommand MUST be declared in the global \`OPTIONS\` map in \`packages/gateway/src/cli/main.ts\` with correct type. Discovered in PR #696: \`--to\`, \`--project\`, \`--dry-run\`, \`--no-children\`, \`--min-confidence\` were missing.

<!-- lore:019ecab6-f928-7927-a39e-d601fb850af6 -->
* **computeMaxTokens: EMA-based floor ignores thinking budget, causing silent agent loop exit**: \`computeMaxTokens\` EMA floor ignores thinking budget → silent agent loop exit. \`computeMaxTokens\` (pipeline.ts) floors \`max\_tokens\` at \`max(8192, 3×outputEMA)\`. Anthropic counts \`max\_tokens\` as combined thinking+visible budget. Model consumes entire 8192 on reasoning → \`finish:"length"\` → no text/tool part → agent loop exits silently. Fix: pass \`thinkingBudget\`; set \`baseFloor = thinkingBudget + THINKING\_OUTPUT\_HEADROOM (8192)\`, clamped by \`modelOutput\`. Gate: \`if (!isCC)\` — CC clients excluded; opencode does NOT send \`x-claude-code-session-id\` so fix applies. EMA not corrupted by thinking truncation (Anthropic includes thinking tokens in output\_tokens).

<!-- lore:019ebafd-32b7-7468-bdb2-5c77ff6c4ccf -->
* **opencode-lore plugin: legacy export path causes getLegacyPlugins() to be invoked**: opencode-lore plugin: never export named functions alongside default — legacy loader invokes them as plugins. \`getLegacyPlugins()\` iterates \`Object.values(mod)\` and treats any function as a server plugin. So \`export default LorePlugin\` with separately-exported helpers (\`applyLoreProviderConfig\`, \`probeGateway\`) causes them to be invoked as plugins; returning undefined pushes them into \`hooks\[]\` → all dispatch loops crash. Fix: \`default export { server: LorePlugin }\` so \`readV1Plugin\` takes the v1 path. NEVER export helper functions from the plugin's top-level index — they leak into the legacy loader.

<!-- lore:019ece09-5df6-7392-b67c-043a729239a1 -->
* **safeDeltaInsertIndex: steady-layer-1 drift NOT fixed by delete/re-anchor predicate**: Trap: \`shouldResetDeltaOnCompression(prevLayer, curLayer)\` returns false when layer stays at 1 (curLayer===prevLayer), so the delete/re-anchor branch never fires for steady-layer-1 raw-window slides — the production bug case. It looks like the fix because it handles layer escalation. The docstring claimed 'steady-state-at-layer-1 raw-window slide is handled by the caller's additional raw-window-repin check' — but that check was NEVER implemented at the call site (pipeline.ts:6078). Fix: extended \`shouldResetDeltaOnCompression\` with third param \`outOfIdle: boolean\` (default false); returns \`true\` when \`outOfIdle===true\` regardless of layer change. Call site passes \`sessionState.lastTurnWasIdle ?? false\`. Replay-time re-validation (\`applySessionPromptDeltas\`) handles non-idle steady-layer-1 drift. One-shot post-idle re-anchor alone doesn't fix steady-layer-1 drift — window start marches forward every turn.

<!-- lore:019ece19-d902-71c9-86d1-f51e74e8c6b9 -->
* **sync.ts drainTimestamp: keyset on idColumns\[0] only drops rows for composite-PK tables with >PAGE same-key entries**: Trap: \`drainTimestamp\` keysets by \`idc = idColumns\[0]\` (= \`knowledge\_id\`) for \`knowledge\_entity\_refs\`. If >200 refs share the same \`knowledge\_id\` + same \`updated\_at\`, rows beyond 200 are silently dropped — pagination terminates after 9 queries. Code comment at lines 322-324 claiming 're-scans the ms from the start … idempotent and loses nothing' is factually wrong. Empirically verified: single-group 250 refs (1 knowledge\_id, 1 ms) all applied correctly with composite keyset; multi-group 480 refs across 3 knowledge\_ids also correct. Fix: keyset on full composite \`(knowledge\_id, entity\_id)\` for composite-PK tables. \`\x1f\` (0x1f) sorts before any printable char — composite row\_id ordering is correct.

<!-- lore:019ece19-d8e5-7e0c-9fbf-fb29368de83c -->
* **sync.ts pushEntry: epoch-ms integers rejected by Postgres timestamptz columns (22008)**: Trap: \`pushEntry\` sends epoch-ms integers into \`timestamptz\` columns. Real Postgres rejects with error code \`22008\`; \`classifyPushError\` maps this to 'transient' → cursor never advances → row retried forever. Fix: \`toRemoteRow()\` converts \`typeof out\[k] === 'number'\` → \`new Date(out\[k]).toISOString()\` for \`TS\_COLS = Set(\['created\_at','updated\_at'])\`. SECOND BUG: \`stripSyncCols\` uses \`Date.parse(v) || Date.now()\` — \`Date.parse\` of epoch-0 ISO string returns \`0\` (falsy) → replaced with \`Date.now()\`. Since \`created\_at\` is NOT in \`HASH\_EXCLUDE\`, this corrupts the content hash → row never settles as 'skip'. Fix: use \`Date.parse(v) !== 0 ? Date.parse(v) : 0\` (or explicit null check). Epoch-0 only occurs if caller explicitly inserts 0 (schema has \`NOT NULL\`, no default).

<!-- lore:019ed09f-854d-7b26-8002-40a13dc0b58b -->
* **sync.ts pushEntry: phantom columns on knowledge\_entity\_refs cause permanent push stall**: Trap: \`pushEntry\` (sync.ts) unconditionally adds \`content\_hash\`, \`revision\`, \`is\_deleted: false\` to ALL tables including \`knowledge\_entity\_refs\`. The join table has no \`content\_hash\`/\`revision\` columns on remote. PostgREST returns HTTP 400 PGRST204 for unknown columns. \`classifyPushError\` only special-cases errcode \`23514\`; PGRST204 falls through to 'transient' → returns 'stop' → push cursor for \`knowledge\_entity\_refs\` never advances → entire join table stuck pending forever. Fix: \`SyncTableMeta.versioned\` flag (defaults true); strip \`content\_hash\`/\`revision\` from upsert payload when \`versioned === false\`. \`knowledge\_entity\_refs\` is the only non-versioned table in \`SYNCED\_TABLES.basic\`.

<!-- lore:019ece19-d8f2-7d7b-8b78-969756c830ae -->
* **sync.ts pushOnce: minCursor=0 from empty-table cursors blocks pruneOutbox forever**: Trap: \`pushOnce\` computes \`minCursor = Math.min(...per-table push cursors)\`. A synced table with zero outbox entries keeps cursor \`0\`. \`pruneOutbox(minCursor)\` early-returns when \`minCursor <= 0\` → outbox NEVER pruned whenever any one synced table has zero pending entries. Fix (implemented): compute \`minCursor\` only over tables where \`hasOutboxEntries()\` is true — empty tables excluded from floor calculation. Empirically verified: empty table does NOT pin floor; higher-cursor table with no peers fully pruned. Push-prune race is benign: rows inserted after cursors are read get \`seq > minCursor\` (AUTOINCREMENT monotonicity) → always survive prune.

### Pattern

<!-- lore:019ed0b5-e3c2-77b1-a8b1-94d56f85a9da -->
* **pnpm.overrides used for transitive vulnerability fixes in root package.json**: Transitive dependency vulnerabilities are fixed via \`pnpm.overrides\` in root \`package.json\`. Current overrides: \`basic-ftp: 5.3.1\`, \`ip-address: 10.2.0\`, \`fast-xml-parser: 5.7.3\`, \`@huggingface/hub: 2.11.0\`, \`yaml: >=2.8.3\`, \`esbuild: >=0.28.1\`, \`@opentelemetry/core: >=2.8.0\`, \`protobufjs: >=7.6.3\`. CI uses \`pnpm-lock.yaml\` as canonical lockfile (all jobs use \`pnpm install --frozen-lockfile\`). \`bun.lock\` was deleted — it was stale and only used by the now-removed \`bun pm pack\` path in \`bump-version.sh\`.

### Preference

<!-- lore:019ed0da-9ae5-73bd-97a9-ac94b9d0e0fd -->
* **Always conduct adversarial pre-merge code reviews scoped to correctness/logic only, explicitly excluding security**: Before merging PRs (especially complex distributed-systems features), the user consistently requests a final adversarial review focused strictly on correctness bugs, logic errors, race conditions, edge cases, resource leaks, and error handling. Security is always explicitly out of scope (handled by a separate reviewer). The user provides precise context: repo path, branch name, HEAD commit hash, PR number, issue number, which commits are new/unreviewed, and specific high-risk areas to focus on. They expect the reviewer to read actual source files rather than rely on summaries, and to reason deeply about specific named functions, cursor logic, and data-flow invariants. After any new commit lands, a fresh review pass is expected covering the new code.

<!-- lore:019ed0dc-cafa-703f-b00b-c68963d088e3 -->
* **Always conduct iterative security reviews with strict cleanup rules on live Supabase projects**: The user repeatedly runs penetration tests and security reviews directly against a live Supabase project (ref \`jlwxsrmvomocgngbxmcf\`) on the \`feat/sync-engine\` branch. Each session follows a strict protocol: create test rows/users as needed, then clean up all test data afterward and restore \`plan\_limits\` to known free-tier values (knowledge=500, entities=30, aliases=300, relations=300, refs=2000). Never alter the real user's (BYK/ben@byk.im) tier or drop real data. Security findings are fixed in numbered migrations (0001–0006+), and each session builds on prior findings. When writing tests or harnesses, ensure they exercise real RLS/triggers/quotas rather than bypassing them.

<!-- lore:019ed0dc-cd14-7ac6-8bf8-8e4b1190ad4a -->
* **Always document design assertions as inline code comments in source files**: The user consistently embeds authoritative design directives and behavioral guarantees directly as comments in source files (e.g., sync.ts, login.ts, sync-data.ts), not just in external docs or PRs. These comments explain \*why\* a design choice was made and what invariants must hold. When writing or modifying code, preserve and add these explanatory comments at the top of functions or files. They serve as enforceable contracts, not just documentation. Never remove or paraphrase them without explicit user instruction. Security findings are fixed in numbered migrations (0001–0006+); each session builds on prior findings.

<!-- lore:019ece05-3058-7c4b-afd7-f668085684cd -->
* **Always enforce tool\_use/tool\_result pair safety as a hard invariant with code comments**: tool\_use/tool\_result pair safety is a hard invariant. safeDeltaInsertIndex is the primary guard: walk back whenever messages\[idx-1] is assistant(tool\_use), regardless of what follows (covers completed pairs AND pending tail). Loop: \`while (idx > 0)\` (no upper bound). removeOrphanedToolResults is last-ditch backstop that 'should essentially never fire.' Always cover both completed pairs AND pending/in-flight dangling tool\_use at tail in tests. Never rely on destructive stripping as primary safety — insertion point must be computed correctly upfront. Invariant documented in code comments referencing bug #747.

<!-- lore:019ecc9f-b2e1-7ccc-9c86-6d594fb297d5 -->
* **Always investigate why tests failed to catch issues, not just fix the issues themselves**: When a bug or production incident is resolved, always follow up with: (1) root cause analysis of why existing tests failed to catch it (structural/design gaps, exact file:line), (2) new regression tests that would have caught the issue, (3) hardening to prevent future misses. Simply fixing the bug is insufficient. Known pattern: tests that don't mock embeddings silently skip embedding-dependent branches — always verify branch reachability in test fixtures.

<!-- lore:019ed073-c91b-7fdb-a45b-0621cea4ec49 -->
* **Always maintain detailed architectural memory of sync engine invariants across sessions**: Sync engine settled invariants (do not re-litigate): push-before-pull ordering, per-table cursors, quota-pause-not-crash, never-fast-forward-over-pending-local-change, apply-suppression via temp table \`\_sync\_applying\`, ON CONFLICT upserts (not INSERT OR REPLACE), keyset pull cursor with >= semantics, PAYLOAD\_EXCLUDE/HASH\_EXCLUDE column sets. Remote composite PKs: \`(owner\_user\_id, id)\` for knowledge/entities/entity\_aliases/entity\_relations; \`(owner\_user\_id, knowledge\_id, entity\_id)\` for knowledge\_entity\_refs. \`onConflict\` MUST include \`owner\_user\_id\` for remote upserts. Local apply (\`sync-data.ts\`) uses \`idColumns\` only (no \`owner\_user\_id\`) — the two sides correctly diverge. \`versioned: false\` tables never get \`content\_hash\`/\`revision\` in payload. \`owner\_user\_id DEFAULT auth.uid()\` fills before conflict check — no duplicate-insert path. Reviews should focus on new bugs or edge cases only.

<!-- lore:019ecdf6-e77f-7847-a915-b240ea62560c -->
* **Always prefer delete-and-recompute over invalidation/validation logic for stale cached state**: When cached state (like a frozen \`insertAt\` index) can become stale due to underlying data reshuffling, the user rejects approaches that validate, patch, or re-derive from the stale state. Instead, they prefer deleting the stale row entirely and recomputing it fresh in the same turn where the invalidating event occurs. This must happen atomically in the compressing turn — not deferred to the next turn — to avoid sending incoherent data upstream. The design principle: no state to track or invalidate, no drift possible, cleaner invariants. When proposing fixes for stale cached positions or indices, always suggest delete-on-compress with same-turn recomputation rather than validity checks or conditional re-derivation.

<!-- lore:019ed0e9-26c1-7a38-91ed-ef9b24d4f472 -->
* **Always run dual-agent review (code quality + security/pentesting) before merging PRs**: Before merging any PR, the user consistently triggers two parallel review agents: one focused on code correctness/logic/edge cases, and one focused on security/pentesting. This pattern applies after every significant commit or batch of fixes — not just once per PR. Each round produces verdicts (SHIP / NEEDS-WORK) with specific blockers, should-fixes, and nits. The user expects the agents to mutation-test or empirically verify findings, not just reason statically. Reviews continue in rounds until both agents return SHIP. When proposing a review, always offer to launch both agents simultaneously rather than sequentially.

<!-- lore:019ed0d6-d3bb-792c-8bde-037c9dc82766 -->
* **Always trigger a new review round after new code lands on a branch**: After any new commit lands on a feature branch (e.g., bug fixes, integration tests, CI wiring), the user consistently asks whether a fresh review has been done covering the new code. They expect each meaningful batch of changes to receive its own adversarial review pass (code quality and/or security) before merge, rather than relying on prior review rounds that predate the new commits. When new unreviewed code is identified, the user expects the assistant to enumerate exactly what changed and propose launching the appropriate review agents.

<!-- lore:019ed034-6c35-7aeb-9281-ed1df0960fad -->
* **Always verify fixes against real production logs after merging**: Always verify fixes against real production logs after merging: Check that the running binary actually contains the fix (not stale), expected log strings appear (e.g., 'upserted' vs 'appended'), and the bug symptom is gone. Investigate when logs show the bug persisting post-merge — trace DB state (row counts, frozen insertAt values), divergence/bust-tracker logs. Pull real log/DB evidence rather than reason theoretically. Identify precisely why a fix didn't cover a specific production case (e.g., steady-state layer-1 not triggering a layer-change predicate).

<!-- lore:019ed0aa-8616-7c54-a083-5535250e733e -->
* **Always verify invariant ordering in code paths during adversarial review**: Always verify invariant ordering in code paths during adversarial review: Guards must be ordered correctly: (1) 'always fund first probe' (\`cyclesSpent === 0\`) must be checked BEFORE any durable/lifetime metric gates (like \`warmupWriteEfficiencyTooLow()\` or hit-rate guards) — durable state can block a per-break reset. (2) Evidence gates must fire before cycle caps. (3) \`writeEfficiencySamples\` is lifetime/durable — NOT reset per-break — placing it before the \`cyclesSpent === 0\` escape hatch causes self-reinforcing lockout. Flag any guard using lifetime state without first checking \`cyclesSpent === 0\` as a MAJOR ordering bug. Hard rule: \`cyclesSpent >= 1 && warmupHits < TOOL\_CALL\_MIN\_HITS\_FOR\_CONTINUATION\` must NEVER be funded (Bug C evidence gate). First warmup cycle of any break MUST always be funded — it is the irreducible probe. Any gate (efficiency, hit-rate, ROI) that blocks this first probe is a critical bug.

<!-- lore:019ed0de-1f8f-79a5-8413-1c077e9677f3 -->
* **Always verify security assumptions empirically before reporting findings**: When conducting security audits or reviewing code with security implications, the user consistently insists on empirical verification rather than accepting theoretical analysis alone. They set up test users, simulate the exact attacker role (e.g., \`SET LOCAL ROLE authenticated\` + JWT claims), reproduce exploits step-by-step, and confirm results with actual data before declaring a finding confirmed or dismissed. They also flag edge cases (like whether \`SET ROLE\` strips owner-bypass) as needing empirical verification rather than assuming the answer. Apply this pattern by always proposing or running concrete test cases to validate security claims, not just reasoning about them.

<!-- lore:019ed0db-e372-79b4-a58f-def7aa674644 -->
* **Always verify tests fail on revert before accepting coverage as valid**: After adding tests, the user explicitly confirms that each new test fails when the guarded logic is removed ('fails-on-revert confirmed'). This applies to behavioral gates, efficiency checks, log-noise suppression, and any other conditional logic. When writing or reviewing tests, always note whether the test is discriminating — i.e., would it catch a regression if the new code were reverted? If a test passes regardless of the new logic (non-discriminating), flag it and recommend fixing the fixture (e.g., adjusting state so the test actually exercises the new branch rather than a prior guard).

<!-- lore:019ecdee-183e-73a4-9e48-a2430114cb84 -->
* **Always write TDD spec tests before implementing fixes, with explicit constraints on scope**: TDD: write tests ONLY first — do NOT implement, do NOT modify src/. Cover all edge cases. Use vitest, mirror style/imports of existing tests. Run tests to confirm red state before implementation. Behavioral invariants use 'never'/'always' rules, each needing a dedicated test. For safeDeltaInsertIndex: NEVER land after assistant tool\_use, NEVER orphan tool pairs, NEVER split tool\_use/result pairs. Loop: \`while (idx > 0)\` (no upper bound). Don't call \`assertNoOrphanedTools\` on pending-tool tails. After implementation: typecheck clean, lint exit 0, targeted tests green, full suite green. Clean up adversarial scratch test files. E2E tests depending on full LTM pinning lifecycle are non-deterministic — replace with focused deterministic integration tests using real \`transform\`. Always validate test discriminability: each test must fail if the specific behavior it guards is reverted. Flag zero-coverage branches as blockers before merge (≥80% patch coverage target).

<!-- lore:019ed058-ae55-7a9f-b6ce-c79717e686af -->
* **Never use Supabase for temporal/high-volume data — treat it as row-level sync only for small structured data**: Hard rule: Supabase (PostgREST row-level) is only appropriate for small structured data AND linked temporal rows (distillation-referenced subset). NEVER use for arbitrary temporal bulk or GB-scale data. Blob snapshots rejected for multi-client: divergent WAL chains, no row-level merge path. Turso rejected for basic/pro tiers: whole-DB replication only, no table/row-level selective sync. Turso deferred to hosted-gateway decision only. Framing Supabase as a live query store for unreferenced temporal data is 'the GB-scale Option-C rewrite trap' and will be immediately rejected. SQLite WAL must NEVER run on network FS (EFS/NFS) — local volume only.

<!-- lore:019ecd73-2479-738e-ae66-932881f40aa4 -->
* **Strict PR Scope Discipline with Explicit Coding Conventions**: Strict PR Scope Discipline and Coding Conventions: State deliverable boundary up front; existing tests/behavior must remain unbroken. Drive-by fixes to unrelated files must be split into separate commits or rejected — user reverts unrelated edits even when correct. Coding conventions: Biome formatting (2-space indent, single quotes, no semicolons), vitest imports, kebab-case filenames, no comments unless documenting invariant/design rationale, ALWAYS/NEVER phrasing for invariant comments. Prefer copy-pasteable one-liners over prose. Always plan before coding: produce a detailed file-and-line-level plan saved as .opencode/plans/\*.md, executed task-by-task with DONE marks. Invariants use 🔴 markers. After implementing, run tsc --noEmit on all affected packages and expect 0 errors. Final verification: typecheck, lint, test, build from repo root — hard constraint, not optional.

<!-- lore:019ebe56-3cfa-7b49-b786-3d603e608a37 -->
* **Test file conventions: Vitest, kebab-case, import style**: Always write new tests with \`import { describe, it, expect } from 'vitest'\`. Use kebab-case file naming for test files. Prefer writing more tests over lowering coverage thresholds or excluding files. Project migrated from Mocha+Chai+ts-node to Vitest (May 2026; 312ms vs 30s startup). Run tests from repo root, not package subdirectory — \`vitest\` with a filter from a subdirectory returns 'No test files found'.
