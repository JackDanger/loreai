<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019e3083-8969-732b-a269-72e6cdd1ff7d -->
* **Background LLM rate limiting: p-limit(2) + 429 circuit breaker in background-limiter.ts**: Global concurrency limit for background LLM work in \`packages/gateway/src/background-limiter.ts\`. Uses \`p-limit(2)\` to cap simultaneous background LLM calls across all idle sessions. Circuit breaker trips on 429 responses and pauses all background work for the \`Retry-After\` duration. Wired into: idle scheduler, pipeline incremental distillation, in-flight curation. Urgent distillation is excluded (client is waiting). Without this, N idle sessions fire N×4 simultaneous background calls causing cascading rate limit failures.

<!-- lore:019e1c62-3208-7836-a531-f92d1bb20733 -->
* **Conversation import system: providers, detection, extraction pipeline**: Core import system in \`packages/core/src/import/\`. Key design: \`AgentHistoryProvider\` interface with \`detect()\`/\`load()\`; providers registered in global registry (\`providers/index.ts\`). Detection scans all providers, returns \`DetectedSession\[]\`. Extraction calls curator LLM sequentially per chunk, deduplicating ops via \`parseOps()\`/\`applyOps()\`. Idempotency via \`import\_history\` table (DB migration v19). Built-in providers: Claude Code (\`~/.claude/projects/\`), OpenCode (SQLite), Aider (markdown), Codex (\`~/.codex/sessions/\` JSONL), Cline (VS Code globalStorage JSON), Continue (\`~/.continue/sessions/\` JSON), Pi (\`~/.pi/agent/sessions/\` tree-structured JSONL). Auto-import triggered in \`lore run\` via \`maybeAutoImport()\`. Copilot Chat skipped (opaque leveldb).

<!-- lore:019e4bda-93ec-70a4-9360-f1f22a5a3769 -->
* **ensureProject() 4-tier resolution and projectId() read-only lookup**: \`ensureProject(path, name?, suppliedGitRemote?)\` in \`packages/core/src/db.ts\` lines 827-907: 4-tier resolution: (1) exact path match + lazy git\_remote backfill, (2) path alias match (\`project\_path\_aliases\` table), (3) git remote match + registers alias, (4) create new project with \`crypto.randomUUID()\`. Without git: skips tier 3, creates path-only project — no worktree dedup or cross-machine unification. \`projectId(path)\` (lines 909-920): read-only, checks \`projects.path\` then \`project\_path\_aliases.path\` — never creates or calls git. \`resolveProjectByRemoteOrPath(gitRemote?, path?)\` (lines 927-941): checks git\_remote first, falls back to \`projectId()\` — never creates. DB schema: \`projects\` table has \`git\_remote TEXT\` (added migration v14).

<!-- lore:019e4ad8-b33b-75b4-8578-b2fce2644bb6 -->
* **getsentry/cli nightly release channel: rolling GHCR-based distribution with bsdiff patches**: (architecture) getsentry/cli nightly release channel: rolling GHCR-based distribution with bsdiff patches: Nightly release: rolling GitHub prerelease tagged \`nightly\`; version format \`0.x.y-dev.\<unix\_seconds>\` (timestamp from \`github.event.head\_commit.timestamp\`, computed once in \`nightly-version\` job output). \`build-nightly-binaries\` builds all 4 targets; \`generate-patches\` uses ORAS + zig-bsdiff against previous GHCR nightly (\`ghcr.io/byk/loreai\`); \`publish-nightly\` uploads to GHCR. \`src/lib/db/release-channel.ts\`: \`getReleaseChannel()\`/\`setReleaseChannel()\` using \`metadata\` table, defaults \`'stable'\`. \`version-check.ts\` is channel-aware. \`nightly-version\` job must always output something (even empty string on PRs). The \`nightly\` tag in the loreai repo is permanently tainted — use GHCR OCI artifacts (mutable tags) instead of GitHub release assets.

<!-- lore:019e458b-fe1a-77fe-886b-37ef1817e7ca -->
* **Gradient tool\_use/tool\_result pairing: reconstruct-after-eviction pattern**: Gradient tool\_use/tool\_result pairing — reconstruct-after-eviction pattern: \`tryFit()\` has NO logic to keep pairs together; safety via: (1) \`resolveToolResults()\` (temporal-adapter.ts:239) merges tool result data onto assistant tool parts, strips user-side \`tool\_result\` blocks; (2) \`loreMessagesToGateway()\` (pipeline.ts:3401) reconstructs pairs from surviving assistant tool parts; (3) \`removeOrphanedToolResults()\` (pipeline.ts:3524) validates BOTH directions. \`sanitizeToolParts()\` (gradient.ts:1071) converts pending/running tool parts to error state. Layer 4 never strips tool parts to avoid infinite loops. Prefix/raw boundary trap: prefix ends with assistant (text-only), rawWindow may start with assistant containing tool\_use → back-to-back assistants → Anthropic rejects. Fix: advance cutoff past leading assistant messages when prefix is present at all 3 assembly points in gradient.ts (tryFit, tryFitStable pinnedIdx adjustment, emergency layer). \`tryFitStable()\` lazy eviction: pins oldest raw message ID; re-pins when highWaterBudget×1.15 exceeded; resets on layer escalation to 2+.

<!-- lore:019e4ba4-7d23-75b4-af92-c5ae831ed7cb -->
* **gradient.ts: transform() layer selection, budgeting, and overflow risks**: gradient.ts \`transform()\` (line 2022) wraps \`transformInner()\` + \`sanitizeToolParts()\`. Budget: \`usable = contextLimit - outputReserved - overhead - sessLtmTokens\`; \`distilledBudget = floor(usable \* 0.25)\`; \`rawBudget = floor(usable \* 0.40)\`. Layers 0–4: 0=passthrough; 1=distilled prefix+raw window (stable lazy eviction); 2=strip old tools, raw=50%; 3=strip all tools, 5 distillations, raw=55% dist=15%; 4=emergency 2K–8K tail. Overflow risk: \`outputReserved\` hardcapped at 32K (line 433) — if SDK sends 128K actual limit, effective input budget stays at 168K but actual max drops to 72K. Token estimator uses \`ceil(len/3)\` — underestimates dense code. \`calibratedOverhead\` EMA (0.7 old + 0.3 new) can lag on sudden system prompt growth. \`sessLtmTokens\` not included in Layer 0 path (line 1652 only in transformInner, not layer 0 passthrough).

<!-- lore:019e4bbd-adf8-7a8f-9592-d9a7fe9dbeb5 -->
* **initIfNeeded() startup sequence and knowledge watcher lifecycle**: \`initIfNeeded(projectPath, config, gitRemote?)\` in \`pipeline.ts\`: idempotent (guarded by \`initialized\` flag). Calls \`enableHostedMode()\` first if \`config.hostedMode\`. Then: \`load(projectPath)\`, \`ensureProject(projectPath, undefined, gitRemote)\`. On \`cfg.knowledge.enabled\`: \`tryImportKnowledge()\` (prefers \`.lore.md\` over agents file; no-op in hosted mode), \`ltm.pruneOversized(1200)\`, \`startKnowledgeFileWatcher()\` (500ms debounce; watches \`.lore.md\` + agents file; returns cleanup fn). Also: \`distillation.backfillMetrics()\`, \`embedding.runStartupBackfill()\`, \`latReader.refresh()\`, pre-populates \`headerSessionIndex\`, \`fetchModelData()\`, starts idle scheduler. \`tryImportKnowledge()\` re-runs on new sessions to catch watcher gaps (file created after gateway startup, network-mounted FS missed events).

<!-- lore:019e4bbd-aded-779a-8441-df55ea3312c0 -->
* **lore run always runs locally — agent is on the same machine as the gateway**: \`lore run\` (\`packages/gateway/src/cli/run.ts\`) always runs locally — the agent process is on the same machine. Flow: 1) start/reuse gateway, 2) \`maybeAutoImport()\` (first run only), 3) \`resolveLaunchTarget()\` (explicit cmd or \`detectAgents()\` + TTY prompt), 4) \`launchChild()\` with \`stdio: 'inherit'\`. Remote mode (line 136-150) delegates to existing remote gateway but still computes git remote locally. \`envVars(gatewayUrl, cwd)\` in \`agents.ts\` injects \`ANTHROPIC\_CUSTOM\_HEADERS\` with \`X-Lore-Project\` and \`X-Lore-Git-Remote\` for claude-code; other agents (codex, pi, opencode) use agent-specific env vars or plugin hooks for header injection.

<!-- lore:019e30a6-ff62-723e-9fd8-56f1f1f60b5a -->
* **LTM confidence field: semantic meaning and rerankPreferences() for legacy entries**: \`ltm.create()\` accepts optional \`confidence\` param (default 1.0, clamped \[0,1]). Semantics: 1.0=unconditional directive, 0.9=strong preference, 0.8=moderate, 0.6=mild. \`CuratorOp\` create type includes \`confidence\`, wired through \`applyOps\`. \`rerankPreferences()\` in \`packages/core/src/ltm.ts\` re-scores legacy entries by directive keyword patterns (\`STRONG\_DIRECTIVE\_RE\`); skips entries whose \`confidence\` was already set to a non-default value — manual overrides are preserved. \`lore data rerank\` CLI command triggers re-ranking; also auto-runs after \`lore data recover\`. Run after deploying to fix existing preferences in DB.

<!-- lore:019e1de2-7650-76c3-9416-40d905dd998f -->
* **OpenAI streaming translation: stateful SSE translators in stream/openai.ts**: (architecture) OpenAI streaming translation — stateful SSE translators in \`packages/gateway/src/stream/openai.ts\` and \`stream/openai-responses.ts\` consume Anthropic SSE events and emit OpenAI-format SSE. The pipeline carries \`effectiveProtocol\` in \`UpstreamResult\` to dispatch to the right translator. All translators must implement: (1) \`cancelled\` flag + \`cancel()\` handler aborting upstream via \`AbortController\`; (2) \`safeEnqueue()\` wrapper that no-ops if \`cancelled\`; (3) error \`catch\` block emitting a protocol-appropriate terminal event (\`response.failed\` for Responses API, \`\[DONE]\` with error for Chat Completions) — otherwise clients hang. OpenAI/Responses API upstreams don't receive LTM injection — \`req.system\` passed through unchanged; only the Anthropic path injects LTM. Fix: apply LTM injection to all upstream paths before forwarding.

<!-- lore:019e4586-82db-796f-a3cd-d65f790276bf -->
* **OpenCode x-session-affinity is a per-process nanoid — not stable across restarts**: OpenCode generates \`x-session-affinity\` natively as a nanoid — random, per-process, NOT stable across restarts. The Lore plugin (\`packages/opencode/src/index.ts\`) never touches this header. \`input.sessionID\` in plugin hooks is OpenCode's persistent DB \`Session.id\` — stable across restarts. When OpenCode restarts, the new nanoid causes Tier 1 in \`identifySession()\` to create a brand-new Lore session, orphaning prior distillations/gradient state. Fix: inject \`input.sessionID\` as \`x-lore-session-id\` in the \`chat.headers\` hook. Tier 1 upgrade-path trap: when plugin starts sending \`x-lore-session-id\` alongside \`x-session-affinity\`, prior sessions indexed under \`x-session-affinity\` are invisible — Tier 1b must check both header names.

<!-- lore:019e1952-398c-753f-b92c-4a5fa5ecf15f -->
* **Pi plugin: which providers can be proxied through the gateway**: Pi plugin gateway proxy compatibility by wire protocol. \*\*Proxiable\*\*: \`anthropic\` → \`/v1/messages\`: \`anthropic\`, \`fireworks\`, \`github-copilot\`; \`openai-completions\` → \`/v1/chat/completions\`: \`deepseek\`, \`xai\`, \`groq\`, \`cerebras\`, \`openrouter\`, \`huggingface\`, \`opencode\`, \`opencode-go\`; \`openai-responses\` → \`/v1/responses\`: \`openai\`, \`azure-openai-responses\`, \`openai-codex\`, \`azure-openai\`, \`lm-studio\`, \`ollama\`. \*\*Cannot proxy\*\*: \`google\`, \`google-vertex\`, \`amazon-bedrock\`, \`mistral\`. \`registerProvider(name, { baseUrl })\` overrides base URL. Gateway routes by URL path only. OpenAI streaming clients receive true incremental SSE (\`stream/openai.ts\`).

<!-- lore:019e4bbd-add8-7ac4-9d92-df02bb642878 -->
* **Project path resolution: no .git traversal — git remote via subprocess with cwd**: Lore has NO explicit \`.git\` directory traversal. Project identity comes from \`getGitRemote(path)\` in \`packages/core/src/git.ts\` — runs \`git remote -v\` with \`cwd: path\` (prefers \`upstream\` > \`origin\` > any). Results cached in \`gitRemoteCache\` (process-lifetime Map). \`ensureProject()\` in \`db.ts\` resolves project by: 1) exact path match, 2) path alias, 3) git remote match (registers alias for O(1) future lookups), 4) create new row. Consequence: sub-projects in a 'fake monorepo' (no \`.git\` at repo root) each get their own project row — no shared identity. \`getGitRemote()\` returns null immediately in hosted mode. Never run git subprocesses with client-controlled cwd.

<!-- lore:019e4a07-0161-7795-961a-38dd0ef8ac69 -->
* **Sentry initialization and error routing in gateway**: (architecture) Sentry initialization and error routing in gateway: Init in \`packages/gateway/instrument.ts:75-88\`. No \`beforeSend\`, \`ignoreErrors\`, or \`denyUrls\` — all errors sent unconditionally. Two paths to Sentry: (1) Direct \`Sentry.captureException()\` in \`llm-adapter.ts:490\` for worker retry exhaustion (429/502). (2) Indirect: any \`log.error(..., errorObj)\` auto-forwards via \`sink.captureException()\` (wired at \`instrument.ts:91-96\`). Transient errors (EPIPE, socket closed, ZlibError, TimeoutError, retry exhaustion) all reach Sentry. To filter: add \`beforeSend\` callback to \`Sentry.init()\` in \`instrument.ts\` — single interception point. \`sentryEnabled\` guard at line 74 controlled by \`SENTRY\_ENABLED\` env var.

<!-- lore:019e4bbd-ade4-7355-8fb1-87df54c8ea69 -->
* **X-Lore-\* headers: project path injection and resolution priority**: Project path resolution priority in \`packages/gateway/src/config.ts\` lines ~246-274: 1) \`X-Lore-Project\` header (absolute path, max 1024 chars, strips trailing slashes/control chars), 2) inferred from system prompt (\`cwd\` JSON key, \`Working directory:\` prefix, \`.lore.md\` path, generic \`/home/\`/\`/Users/\` paths), 3) \`process.cwd()\` fallback. Source tagged as \`ProjectPathSource\`: \`"header" | "inferred" | "cwd"\`. Pipeline warns on \`cwd\` source (dedup via \`cwdWarned\` Set). \`agents.ts\` \`envVars()\` injects \`X-Lore-Project: \<cwd>\` + \`X-Lore-Git-Remote: \<remote>\` into \`ANTHROPIC\_CUSTOM\_HEADERS\`. Other headers: \`x-lore-session-id\`, \`x-lore-no-store\`, \`x-lore-upstream-url\`, \`x-lore-recall-invoked\` (response).

### Gotcha

<!-- lore:019e1c27-967c-7eb4-bd0e-afb195823970 -->
* **Bun NAPI crash on process.exit() — use safeExit() via libc \_exit()**: Bun NAPI crash on process.exit() with fastembed — use safeExit(): Loading fastembed (onnxruntime NAPI bindings) causes a C++ panic on \`process.exit()\` because Bun runs NAPI teardown destructors that throw. Fix: \`packages/gateway/src/cli/exit.ts\` exports \`safeExit(code)\` — uses \`\_exit()\` from libc via \`bun:ffi\` under Bun, falls back to \`process.exit()\` under Node.js. All gateway exit paths must use \`safeExit()\`. Do NOT call \`embedding.resetProvider()\` in test teardown \`resetPipelineState()\` — move \`resetProvider()\` to \`shutdown()\` in \`start.ts\` only. \`resetPipelineState()\` must preserve the 'fastembed unavailable' cached state.

<!-- lore:019e49d2-d096-7dca-81df-04837eb3f247 -->
* **exportLoreFile() missing existsSync guard — fires ENOENT every 30s idle tick**: \`exportLoreFile()\` in \`packages/core/src/agents-file.ts:543\` calls \`writeFileSync(fp, content)\` with no guard. The idle worker calls it every 30s — ENOENT fires when project dir is deleted mid-session. Fix: wrap \`writeFileSync\`/\`statSync\`/\`setCache\` block in try-catch; silently return on \`ENOENT\`. Non-ENOENT errors (EACCES, EIO) must still propagate. Trap: \`existsSync\` pre-check introduces TOCTOU race and unnecessary syscall — rejected approach. Contrast: \`exportToFile()\` uses \`mkdirSync\`; \`data.ts\` callers guard with \`existsSync\` at lines 324/475.

<!-- lore:019e2b12-6ea6-76dc-ab7a-a1532c60b312 -->
* **git remote -v in hosted gateway — skip when header present, never run with client-controlled cwd**: \`LORE\_HOSTED\_MODE=1\` makes all FS-touching functions no-op: \`getGitRemote()\` returns null, \`config.load()\` skips \`.lore.json\`, agents-file/lat-reader/knowledge-watcher are no-ops, \`startKnowledgeFileWatcher()\` returns \`() => {}\`. Activation: \`lore start\` (headless) enables hosted mode by default; opt-out via \`--local\` or \`LORE\_HOSTED\_MODE=0\`. \`lore run\` is always local. Flag set in \`initIfNeeded()\` from \`GatewayConfig.hostedMode\`. Never run \`git remote -v\` with client-controlled cwd. \`LORE\_REMOTE\_URL\` + local CLI: \`lore run\`/\`lore start\` skips local gateway and proxies to remote. Local CLI injects \`X-Lore-Git-Remote\`; remote gateway trusts it. Unsafe ops blocked by hosted mode: git subprocess, \`.lore.json\`/\`.lore.md\` read/write, \`lat.md/\` scan, \`fs.watch()\` on attacker-controlled paths. \`\_resetHostedModeForTest()\` in \`hosted.ts\` — test-only, never call in production.

<!-- lore:019e4ba4-7d2e-718a-8bcb-718a6fa153f2 -->
* **gradient.ts outputReserved hardcap at 32K — silent budget error if SDK reports 128K output limit**: Trap: \`setModelLimits()\` (gradient.ts:433) caps \`outputReserved = Math.min(limits.output || 32\_000, 32\_000)\`. If \`@ai-sdk/anthropic\` is updated to report claude-opus-4's actual 128K output limit, \`outputReserved\` stays 32K but the model's actual max input drops from 168K to 72K — the budget still assumes 168K, causing 'prompt too long' errors. Fix: update the hardcap or add a model-specific override. Also: \`computeMaxTokens()\` (pipeline.ts:476) independently caps \`ceiling = min(modelOutput, 32\_000)\` — two separate 32K caps exist.

<!-- lore:019e4ba4-7d36-7f0e-935b-eb10db28fb93 -->
* **gradient.ts token estimator uses chars/3 — underestimates dense code causing 'prompt too long'**: Trap: \`estimateTokens(text)\` uses \`Math.ceil(text.length / 3)\` (line 15-17). Dense code/JSON averages ~3.5–4 chars/token, so the estimator underestimates by 15–30%. Budget calculations (distilledBudget, rawBudget, overhead calibration) all use this heuristic. Result: \`transformInner()\` believes the context fits within 200K but the actual token count exceeds it — producing the 'prompt is too long: 206029 tokens > 200000 maximum' error. The calibration EMA partially compensates via \`calibratedOverhead\` but only for system overhead, not message content variance.

<!-- lore:019e1de4-cada-77cf-ac3b-79ab96b963e4 -->
* **Query expansion disabled by default AND LLM client not wired in gateway (IF-5 fix)**: Two co-required fixes for query expansion (IF-5): (1) \`queryExpansion\` default in \`packages/core/src/config.ts\` must be \`true\` in BOTH the field-level \`.default()\` AND the section-level \`.default({})\` — the section-level overrides field-level in Zod, so changing only one has no effect. (2) \`executeRecall()\` in \`packages/gateway/src/recall.ts\` must accept an optional \`LLMClient\` param and pass it to \`runRecall()\` — without it, \`if (searchConfig?.queryExpansion && llm)\` is always false. Both pipeline.ts call sites (streaming and non-streaming) must pass \`getLLMClient(config)\`.

<!-- lore:019e2bbf-6dba-72a1-babb-b58a51f07ea5 -->
* **Remote import idempotency split-brain: isImported queries local DB, recordImport writes to remote**: When \`LORE\_REMOTE\_URL\` is set, \`lore import\` has a split-brain bug: \`isImported()\` checks the \*\*local\*\* SQLite \`import\_history\` table, but \`remotePost('/api/v1/import/record')\` writes the record to the \*\*remote\*\* DB. Result: every subsequent run re-detects all sessions as un-imported and double-extracts. Fix: (1) add \`GET /api/v1/import/history\` endpoint and query remote during dedup check when remote URL is set; (2) optionally also write locally as offline resilience. The remote DB is the source of truth for import history in remote mode.

<!-- lore:019e2e20-95b3-7a9d-ab38-77d87eafecc4 -->
* **splitSegments() infinite recursion on oversized single messages**: \`splitSegments()\` infinite recursion on oversized single messages in \`packages/core/src/distillation.ts\`: recurses infinitely when a single message exceeds \`maxSegmentTokens\` (16384). \`findSplitIndex()\` returns \`messages.length\` (=1), so \`left = messages.slice(0, 1)\` produces an identical recursive call. Triggered on large tool outputs (~49KB+). Fix: add base case after the \`totalTokens <= maxTokens\` guard — \`if (messages.length <= 1) return \[messages]\`. The oversized message becomes an indivisible segment.

<!-- lore:019e49d5-15c4-78d1-8bbc-f43fde34d9f1 -->
* **startServer() returns plain object, not Promise — use startGateway() for programmatic async use**: (gotcha) Gateway startup API traps: (1) \`startServer()\` (server.ts:259) returns \`{ stop, port, hosts }\` synchronously — callers doing \`.then()\` get TypeError. Use \`startGateway()\` from \`cli/start.ts\` which returns \`Promise\<GatewayHandle>\` and handles port fallback, port file writing, and existing-gateway detection. (2) Under Node.js, EADDRINUSE fires as async \`'error'\` event, never caught by try/catch, bypassing the port fallback chain (3207→5673→0). Fix: wrap \`server.listen()\` in a Promise resolving on \`'listening'\`, rejecting on \`'error'\`; make \`startServer()\` async. (3) OpenCode/Pi plugins start gateway in-process via \`startInProcess()\`. In monorepo, Bun resolves \`@loreai/gateway\` to \`src/index.ts\`; npm resolves to \`dist/index.cjs\` (only exists post-build). Fix: use variable indirection \`const mod = '@loreai/gateway'; await import(mod)\`. Pi plugin must externalize \`@loreai/gateway\` in esbuild. Use port \`0\` (ephemeral) in tests.

<!-- lore:019e1de2-7639-7b32-b4c1-e64486934c27 -->
* **TTL downgrade hysteresis: downgradeStreak field prevents compounding cache busts**: (gotcha) TTL downgrade hysteresis and session state persistence: Auto-TTL downgrade hysteresis in \`packages/gateway/src/pipeline.ts\`: downgrade from 1h→5m TTL requires 3 consecutive short-gap turns (\`ttlDowngradeStreak\` in \`SessionState\`). Block downgrade if >50% of session tokens are cached. Reset streak on any long-gap turn. Subagent turns and tool-use continuations excluded from gap recording — capture \`prevStopReason\` before line 1667 overwrites it, skip when \`prevStopReason === 'tool\_use'\` or \`isSubagentTurn\`. State persistence: immediate (session identity), per-turn (cost snapshot), 30s periodic (gradient EMAs + cache warming via dirty flag). Max data loss on crash: ~30s. Recall follow-up requests must set \`cacheConversation: false\` — otherwise modified message array triggers full cache write at 5m TTL pricing.

<!-- lore:019e1cd6-05d2-74c5-aea8-fd827a4a45e7 -->
* **vectorSearch() is unscoped — test cleanup must delete all embedding rows**: \`vectorSearch()\` in \`packages/core/src/ltm.ts\` queries \`knowledge WHERE embedding IS NOT NULL AND confidence > 0.2\` with no \`project\_id\` filter (intentional for cross-project search). Two gotchas: (1) Test suites scoped to one project leak embedding rows into other vectorSearch tests — \`beforeEach\` must \`DELETE FROM knowledge WHERE embedding IS NOT NULL\`. (2) \`vectorSearch()\` has no \`excludeCategories\` param — category exclusions from \`forSession()\` callers have no effect; add optional \`excludeCategories\` param and propagate from callers. Also: global entries (pid=null) force \`crossProject=true\`; confidence is clamped to \[0.0, 1.0] in \`update()\`.

<!-- lore:019e49f0-1d65-7737-96da-cb4b04122681 -->
* **vitest-evals requires vitest and tinyrainbow as direct devDependencies**: (gotcha) vitest-evals, buildLoreContext, and createJudgeHarness traps: (1) \`vitest-evals\` lists \`vitest\` and \`tinyrainbow\` as peer deps — not hoisted; add both explicitly to root \`devDependencies\`. (2) \`buildLoreContext(turns, contextWindow = 200\_000)\` — second arg is a token count number, NOT a model string. Passing a model string makes all budget arithmetic yield \`NaN\` → empty lore context. (3) \`createJudgeHarness\` \`run\` callback receives positional args \`(input, options)\` — destructuring \`{ input }\` yields \`undefined\`; fix: \`run: async (input, \_options) => { /\* use input.system, input.prompt \*/ }\`.

### Pattern

<!-- lore:019e21e0-9f09-7dd1-a899-854579e160cc -->
* **Enhanced dedup: title overlap + vector similarity (Nomic v1.5)**: Nomic Embed v1.5 dedup threshold: same-domain cosine similarity spreads 0.46–0.70 (vs BGE Small which clusters at 0.93–0.97+, making dedup unusable). Correct dedup threshold: \*\*0.935\*\* — at-or-above is genuine duplicate. Range 0.85–0.91 contains 'related but distinct' entries; 0.85 produces false positives across project boundaries. \`deduplicate()\` in \`packages/core/src/ltm.ts\` uses both title word-overlap (0.7 Jaccard + 4+ shared words) AND vector cosine similarity. BGE Small embeddings are auto-nulled by \`checkConfigChange()\` on startup; \`backfillEmbeddings()\` re-embeds with Nomic v1.5. \`lore data reindex\` triggers backfill on-demand without gateway restart.

### Preference

<!-- lore:019e4bda-93f6-7ae5-b907-d696761a8efe -->
* **.lore.md is always at project root — no directory traversal**: \`.lore.md\` is always located at \`join(projectPath, '.lore.md')\` — never searched via directory traversal or walk-up. \`LORE\_FILE = '.lore.md'\` constant in \`agents-file.ts\` line 44. All access functions (\`loreFileExists\`, \`exportLoreFile\`, \`importLoreFile\`, \`exportToFile\`) use this fixed join. Never implement walk-up discovery for \`.lore.md\`.

<!-- lore:019e4bea-2764-786a-ae1b-b749fb881c45 -->
* **Always document code with precise file paths, line numbers, and behavioral rationale**: When exploring or discussing code, the user consistently records findings with exact file paths (e.g., \`packages/core/src/ltm.ts\`), specific line numbers or ranges, function signatures, and the \*why\* behind design decisions — not just what the code does. This applies to constants, schema migrations, algorithms, and architectural choices. When confirming behavior, the user adds explicit rationale (e.g., 'always included as a safety net'). The assistant should always include precise file locations and line references in explanations, and capture design intent alongside implementation details.

<!-- lore:019e4bda-940c-74d1-9848-7688c8ef73e0 -->
* **Always explore codebase architecture before proposing solutions**: When encountering a problem in the Lore codebase, the user consistently asks the assistant to first explore and understand the relevant existing code (specific files, functions, headers, data flows) before proposing or implementing solutions. The user identifies specific files and questions to answer, traces code paths across multiple files, and gathers concrete implementation details. Only after this exploration phase does the user move to solution design. Follow this pattern by thoroughly reading and summarizing relevant existing code before suggesting changes, and ask clarifying questions about which files to explore if not specified.

<!-- lore:019e44c8-e3b2-70c1-afb6-d3acf24c531a -->
* **Always fix cache memory leaks with TTL eviction, size cap, and scheduled pruning**: (preference) Cache memory leak fix pattern and locking: (1) TTL check in \`.get()\` — delete and return undefined if expired; LRU eviction in \`.set()\` — delete oldest key when \`store.size >= maxEntries\`; \`setInterval(() => this.prune(), 60\_000)\` in constructor. Defaults: \`maxEntries=10\_000\`, \`ttlMs=300\_000\`. (2) Use \`flock\` advisory locking instead of \`proper-lockfile\` — \`proper-lockfile@4.1.2\` fails in containerized environments where PID namespaces reset on restart, leaving stale locks. \`flock\` is automatically released on process exit. (3) Lock double-acquisition bug (\`binary.ts\`): \`downloadBinaryToTemp()\` acquires lock on \`\<execPath>.lock\`, then \`installBinary()\` tries to re-acquire same lock — fix by allowing re-entry when \`existingPid === process.pid\`.

<!-- lore:019e4ba4-7d5e-777e-b6ae-94d42d05a22b -->
* **Always investigate gradient.ts with exact line numbers, function signatures, and layer behavior details**: When exploring the gradient context management system, the user consistently expects responses that include: exact line numbers for every referenced function/constant, full function signatures with parameter and return types, precise numeric values for all constants and thresholds, and a complete breakdown of each layer's behavior (what it strips, what budgets it uses, what conditions trigger it). The user is building a detailed mental model of the system and cross-references findings across sessions. Always provide exhaustive, citation-style analysis rather than summaries — include the actual values, not just descriptions of what the values represent.

<!-- lore:019e4b97-a6d5-7277-aa5b-d9086019dbcd -->
* **Always investigate root causes with exact line numbers, constants, and formulas before proposing fixes**: When encountering a bug or system issue, the user consistently requests deep, thorough investigation of the relevant source files before any fix is proposed. They want: exact line numbers, key constants, precise calculation formulas, code paths that could cause the issue, and edge cases. They ask about multiple related files simultaneously (e.g., gradient.ts + config.ts together). Investigation must be exhaustive and specific — not high-level summaries. Only after this investigation phase does the user move to planning and execution. This applies to both runtime errors (like token limit exceeded) and architectural issues (like rate limiting bugs).

<!-- lore:019e4baf-f2a6-7943-9dac-0671e1e16b0f -->
* **Always review full file contents before implementing changes**: Before making any code changes, the user consistently requests and reviews the full contents of relevant source files, noting key details like line numbers, exported symbols, interface fields, array entries, and function signatures. This applies to config files, CLI entry points, plugin sources, test files, and package configs. The user expects the assistant to read and internalize complete file contents (not just summaries) and confirm specific implementation details before proceeding with any modifications or plans.

<!-- lore:019e498a-c0e4-70c5-ad40-d4d6d9d26ff5 -->
* **CI/PR cycle: check failing jobs, wait for bots, resolve all comments before merging**: (preference) CI/PR cycle and development conventions: (1) Check failing jobs via \`gh run view --log-failed\`; wait for 'Sentry Seer' and 'Cursor BugBot'; fix all failures; resolve all review threads via \`gh api graphql\`. (2) PR creation: check existing branches, follow repo conventions, base description on implementation plan, add plan as \`git notes\`, create as draft. (3) Run lint, typecheck, full test suite before committing. Use Vitest (\`import { describe, it, expect } from 'vitest'\`; migrated from Mocha+Chai May 2026). Use kebab-case file naming. Document process/workflow decisions in AGENTS.md. (4) Sentry IDs start with \`LOREAI-GATEWAY-\`. Critical+Medium issues fixed before merge. (5) Always create a separate focused branch from \`origin/main\` per distinct CI concern. (6) Adversarial review of CI/CD changes: shell injection (use \`env:\` for \`${{ }}\` in \`run:\` blocks — also applies to unprotected JSON.parse sites), secret exposure, conditional logic, dependency availability, idempotency. (7) When reviewing plans/code: audit actual source, trace call chains, surface real problems with concrete findings.

<!-- lore:019e3cd7-97d3-7053-8f02-bb13d727662e -->
* **Lore eval scores must beat or match tail-window — scoring below it means lost information**: (preference) Lore eval system: scoring, baselines, and fixtures: \`inflateScenario(scenario, opts?)\` — opts is \`{ targetTokens?, excludeKeywords? }\`, NOT positional args. Token estimation: chars/4 (inflate), chars/3 (baselines.ts). 8 replay fixtures, 16 scenarios, 130 questions, 6 baselines in CI. \`--inflate\` incompatible with replay mode. Three baselines: (1) \`tailWindowBaseline()\`: backward scan, 80K token budget, drops prefix silently. (2) \`compactionBaseline()\`: multi-pass LLM summarization at 83.5% autoCompactThreshold. (3) \`buildLoreContext()\`: 25% distilled (40K) + 40% raw (64K). Filler turns (\`isFiller:true\`) skipped during gateway replay but included in \`allTurns\` for baseline context. Scores must beat or match tail-window — scoring below means lost information (treat as bug). QA contamination fixed via \`X-Lore-No-Store\`. Non-deterministic LLM output causes variance: re-run before concluding regression.

<!-- lore:019e2168-2fa4-77bd-a557-9d6dbcb40d81 -->
* **Prefer WASM backend over native onnxruntime-node for compiled binaries**: WASM backend for Bun \`--compile\` binaries with transformers.js: \`binaryExternalsPlugin\` in esbuild redirects \`onnxruntime-node\` → \`onnxruntime-web\` via \`onResolve\` (static imports only — does NOT redirect dynamic \`import()\` calls) and patches transformers.js CDN fallback via \`onLoad\` to read \`wasmPaths\` from \`globalThis.\_\_LORE\_VENDOR\_WASM\_PATHS\_\_\` (object form \`{ mjs, wasm }\` with exact hashed \`$bunfs\` filenames — directory strings fail). WASM files embedded as Bun \`{ type: 'file' }\` assets. For npm/CJS builds, \`onnxruntime-node\` stays external. WASM is ~2x faster on batches than native. Importing \`onnxruntime-web\` explicitly alongside the redirect creates two ort instances — 'cannot register backend cpu using priority 10' error.
