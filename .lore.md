<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019e54f5-04e0-7b47-8058-65742c17c693 -->
* **agents-file.ts: AGENTS.md pointer section + .lore.md full export, hash-cached writes**: LORE\_FILE=\`.lore.md\` (agents-file.ts:44) always at project root — entirely lore-owned, no section markers needed. agentsFile config default path=\`AGENTS.md\` also at project root. exportToFile() writes a pointer section to AGENTS.md (not full entries) and calls exportLoreFile() as side effect — full entries go to \`.lore.md\`. splitFile() handles 3 known historical start-marker variants (ALL\_START\_MARKERS). exportLoreFile() skips write if content hash matches cached hash (kv\_meta keyed by \`'lore\_file\_cache:' + filepath\`). In hosted mode, config.load() never reads \`.lore.json\` from client-controlled paths (config.ts lines 272–274: \`if (!isHostedMode())\` guard) — prevents crafted config from altering gateway behavior.

<!-- lore:019e3083-8969-732b-a269-72e6cdd1ff7d -->
* **Background LLM rate limiting: p-limit(2) + 429 circuit breaker in background-limiter.ts**: Global concurrency limit for background LLM work in \`packages/gateway/src/background-limiter.ts\`. Uses \`p-limit(2)\` to cap simultaneous background LLM calls across all idle sessions. Circuit breaker trips on 429 responses and pauses all background work for the \`Retry-After\` duration. Wired into: idle scheduler, pipeline incremental distillation, in-flight curation. Urgent distillation is excluded (client is waiting). Without this, N idle sessions fire N×4 simultaneous background calls causing cascading rate limit failures.

<!-- lore:019e591d-ace0-7298-9465-627bca20923d -->
* **batch-queue.ts: Anthropic and OpenAI batch provider implementations**: Background LLM call sites (8 total, all \`thinking:false\`, non-streaming JSON): (1) Gen-0 Distillation — distillation.ts:903, workerID \`lore-distill\`; (2) Meta-Distillation — distillation.ts:1116, workerID \`lore-distill\`; (3) Curation — curator.ts:377, workerID \`lore-curator\`, urgent=never; (4) Consolidation — curator.ts:528, workerID \`lore-curator\`, urgent=never; (5) Pattern Echo — pattern-echo.ts:165, workerID \`lore-pattern-echo\`, urgent=never; (6) Query Expansion — search.ts:407, workerID \`lore-query-expand\`, urgent=always (3s timeout); (7) Import Extraction — import/extract.ts:100, workerID \`lore-import\`, urgent=never; (8) Compaction — pipeline.ts:2504, workerID \`lore-compact\`, urgent=always. Batch queue \`prompt()\` (batch-queue.ts:953-1025): if \`opts.urgent===true\` or \`shuttingDown\` → delegates immediately to \`inner.prompt()\`; otherwise snapshots auth credential at enqueue time (line 977) via \`getAuth(opts?.sessionID)\`. Direct path \`llm-adapter.ts:333-589\`: auth at line 334, model at 340, upstream at 342, routes to \`buildAnthropicWorkerRequest()\` or \`buildOpenAIWorkerRequest()\` by \`model.providerID\`. Batch providers: Anthropic max age 1h, OpenAI max age 4h. \`LORE\_BATCH\_DISABLED= \[truncated — entry too long]

<!-- lore:019e1c62-3208-7836-a531-f92d1bb20733 -->
* **Conversation import system: providers, detection, extraction pipeline**: Core import system in \`packages/core/src/import/\`. Key design: \`AgentHistoryProvider\` interface with \`detect()\`/\`load()\`; providers registered in global registry (\`providers/index.ts\`). Detection scans all providers, returns \`DetectedSession\[]\`. Extraction calls curator LLM sequentially per chunk, deduplicating ops via \`parseOps()\`/\`applyOps()\`. Idempotency via \`import\_history\` table (DB migration v19). Built-in providers: Claude Code (\`~/.claude/projects/\`), OpenCode (SQLite), Aider (markdown), Codex (\`~/.codex/sessions/\` JSONL), Cline (VS Code globalStorage JSON), Continue (\`~/.continue/sessions/\` JSON), Pi (\`~/.pi/agent/sessions/\` tree-structured JSONL). Auto-import triggered in \`lore run\` via \`maybeAutoImport()\`. Copilot Chat skipped (opaque leveldb).

<!-- lore:019e54f5-04d5-728e-a92e-25bde9b441fe -->
* **curator.ts: consolidation scope, dedup, action tags, and entry size limits**: curator.ts MAX\_ENTRY\_CONTENT\_LENGTH=1200 chars (~400 tokens); entries exceeding this are truncated with \` \[truncated — entry too long]\` suffix in applyOps(). consolidate() excludes cross-project entries (includeCross=false). applyOps() guards against mutating entries owned by a different project\_id; after ops calls \`ltm.syncRefs(id)\` and \`entities.syncEntityRefs(id, content)\` for all created/updated entries; creates detected entities via \`entities.create()\` (dedup via merge); resolves relation refs by UUID \`\[uuid]\` or canonical name via \`entities.resolve()\`. buildActionTagContext() scans distillation observations for \`\[verb-noun]\` action tags across distinct sessions (excludes current), reports tags appearing in 2+ prior sessions. curator.run() uses p-limit(1) per session via curatorLimiter; skips if already busy. maxEntries default=25. AUTO\_SIGNAL\_MIN\_SIMILARITY=0.80, AUTO\_SIGNAL\_MAX\_PAIRS=50, MIN\_CALIBRATION\_SAMPLES=20. CRITICAL: \`filterOps()\` does NOT validate required fields for \`create\` ops — malformed LLM output (missing \`content\`) crashes at applyOps():226. \`update\` branch has null guard; \`create\` branch does not. See \[\[019e5b1f-7b84-7407-8598-62c9ab0c50fe]] for curator prom \[truncated — entry too long]

<!-- lore:019e596c-e39c-74ff-879f-f04f2bb625f6 -->
* **Daily budget throttle curve: 3 zones + rate-aware EMA design**: Daily budget throttle: rate-aware EMA in \`cost-tracker.ts\`. State: \`dailySpend\`, \`dailySpendDate\`, \`costRateEMA\` (α=0.15), \`costRateLastUpdate\`, \`costRateSeeded\`. \`computeThrottleDelay()\`: \`pressure=(spendFraction-0.50)/0.50\`; \`overshoot=min(costRatePerHour/targetRate-1, 10)\`; delay=\`MAX\_THROTTLE\_DELAY(60s) × pressure² × tanh(overshoot/3)\`. Returns 0 if budget≤0, spendFraction<0.50, or rate sustainable. GOTCHA: returns 0 at 100%+ spend when EMA decayed to ~0 after idle (\`0<=0\` passes) — fix: \`if (spendFraction>=1.0) return MAX\_THROTTLE\_DELAY\`. \`getDailyBudget()\` priority: LORE\_DAILY\_BUDGET env → KV \`'daily\_budget'\` → 0. Worker costs update \`dailySpend\` but NOT EMA. Throttle uses proxy sleep (invisible delay), not 429 — avoids client retry machinery. Only conversation turns throttled; compaction/meta exempt.

<!-- lore:019e550f-29b4-768d-ade6-4e46c014effe -->
* **DB schema current version: 26 migrations, key columns per migration**: DB schema: 29 migrations. Key: v8=knowledge.embedding+kv\_meta; v13=metadata table; v14=projects.git\_remote+path\_aliases; v19=import\_history; v23=session\_state LTM cache/pin; v24=session identity+gradient state; v25=dedup\_feedback; v26=session\_state.parent\_session\_id+is\_subagent; v27=entities+entity\_aliases+knowledge\_entity\_refs+FTS5+triggers; v28=entity\_relations; v29=knowledge +11 cols (created\_by, updated\_by, sensitivity, promotion\_status, promoted\_at, approval\_status, approved\_by, approved\_at, source\_user\_id, source\_entry\_id, last\_accessed\_at) + team\_knowledge + team\_config. \`migrate()\` uses \`MIGRATIONS.length\` as version — \`SCHEMA\_VERSION=16\` constant is dead code (never read). \`recoverMissingObjects()\` must cover v29 tables (\`team\_knowledge\`, \`team\_config\`) — omitting causes silent permanent failure on partial migration. KnowledgeEntry type in ltm.ts includes all 11 new fields; KNOWLEDGE\_COLS and KNOWLEDGE\_COLS\_K updated accordingly. Sensitivity/PromotionStatus/ApprovalStatus types exported from ltm.ts.

<!-- lore:019e5728-916d-7888-9778-224a5fbce34b -->
* **embedding.ts: LocalProvider worker lifecycle — init, crash, restart, and permanent-disable paths**: \`LocalProvider\` worker lifecycle in \`embedding.ts\`: \`workerReady=true\` set at line 404 after Worker constructor returns — NOT after ONNX pipeline loads. \`workerInitError\` set permanently on \`'init-error'\` message (line 353), \`'error'\` event (line 376), or non-zero \`'exit'\` (line 387). Once set, all \`ensureWorker()\` calls throw \`LocalProviderUnavailableError\` — no restart. \`initPromise=null\` only on constructor throw (allows retry). \`shutdown()\` (line 464) resets all fields including \`workerInitError=null\` — allows re-init. \`resetProvider()\` shuts down + sets \`cachedProvider=undefined\` (allows new provider). \`\_shutdownAndDisable()\` sets \`cachedProvider=null\` (prevents new provider — test-only). \`embed()\` auto-fallback: catches \`LocalProviderUnavailableError\`, calls \`pickRemoteFallback()\`, permanently replaces \`cachedProvider\`. Voyage wins ties over OpenAI (higher code search quality).

<!-- lore:019e4bda-93ec-70a4-9360-f1f22a5a3769 -->
* **ensureProject() 4-tier resolution and projectId() read-only lookup**: (architecture) Project identity: ensureProject(), projectId(), git remote resolution, hosted mode: \`ensureProject(path, name?, suppliedGitRemote?)\` (db.ts:827-907): 4-tier resolution: (1) exact path match + lazy git\_remote backfill, (2) path alias match (\`project\_path\_aliases\`), (3) git remote match + registers alias, (4) create new with \`crypto.randomUUID()\`. Without git: skips tier 3, creates path-only project. \`projectId(path)\` (lines 909-920): read-only, checks \`projects.path\` then aliases — never creates. \`resolveProjectByRemoteOrPath()\`: checks git\_remote first, falls back to \`projectId()\`. \`getGitRemote(path)\` (git.ts): runs \`git remote -v\` with \`cwd: path\`, prefers upstream>origin>any; process-lifetime cache. \`LORE\_HOSTED\_MODE=1\`: \`getGitRemote()\` returns null, all FS-touching functions no-op; never run \`git remote -v\` with client-controlled cwd. Sub-projects in fake monorepos each get own project row.

<!-- lore:019e6f1e-e689-753a-8d07-392aa7aadc53 -->
* **Entity list page vs detail page: what's shown and what's missing**: Entity list page (\`pageEntities()\`, ui.ts:2468) shows: type, name, alias count, knowledge count (batch-loaded from \`knowledge\_entity\_refs GROUP BY\`), cross-project, updated. Missing: relationship count and metadata (role/description/notes). Both ARE available — \`entity\_relations\` batch query pattern exists (see knowledge\_entity\_refs query at lines 2494-2501), and \`metadata\` field is on every \`EntityWithAliases\` row from \`listAll()\`. Detail page (\`pageEntity()\`, ui.ts:2523) shows all of the above plus: full aliases table, relationships table (via \`entities.relationsFor()\`), linked knowledge table, metadata display + edit form, delete action. Relationship count batch query pattern: \`SELECT entity\_id, COUNT(\*) FROM (SELECT entity\_a AS entity\_id FROM entity\_relations UNION ALL SELECT entity\_b AS entity\_id FROM entity\_relations) GROUP BY entity\_id\`.

<!-- lore:019e69ae-12cf-7bb2-8e09-881e24af8ef3 -->
* **Gateway auth: single credential per session — background workers inherit client key**: Gateway is a pass-through proxy with no key store. \`resolveAuth(sessionID)\` in auth.ts:131 returns per-session credential → lastSeenAuth fallback. Credentials captured from client's incoming request via \`setSessionAuth\`/\`setLastSeenAuth\` in pipeline.ts. Background workers (distillation, curation, consolidation) always use whatever key the client session is currently sending — there is no mechanism to route workers to a different upstream with a different credential. Consequence: MiniMax integration blocked because background workers get the session's Anthropic key → 401 at MiniMax. Routing plan: repurpose \`LORE\_UPSTREAM\_OPENAI\` for MiniMax (no Anthropic upstream changes needed), but credential routing remains unsolved.

<!-- lore:019e54f5-051c-7641-aaad-7688451d5021 -->
* **Gateway complete HTTP endpoint inventory**: Gateway HTTP endpoints. LLM Proxy (server.ts): POST /v1/messages (Anthropic), POST /v1/chat/completions (OpenAI Chat), POST /v1/responses (OpenAI Responses API), POST /v1/compact ({project\_path, previous\_summary?}→{summary} or 404/502), GET /v1/models (passthrough). Health: GET /health→{status:'ok',version}. REST API (api.ts, /api/v1/): GET projects/stats/knowledge/sessions/distillations/recall/import-history; DELETE knowledge/:id/sessions/:id/distillations/:id/projects/:id; POST projects/merge (disabled hosted), reindex, import/extract, import/record, projects/:id/clear, projects/:id/dedup. UI: GET+POST /ui/\*. Project resolution in api.ts: 1) :id URL param, 2) ?git\_remote=, 3) ?path=. All pipeline handlers return internal Anthropic-format; translation to OpenAI/Responses format happens after pipeline returns.

<!-- lore:019e6904-e0e1-7689-bb52-7e8a0cb0edda -->
* **Gateway recall: Marker-and-Expand strategy — tool injection, interception, and round-trip**: Recall tool injected by gateway pipeline (pipeline.ts), NOT OpenCode plugin. Injection condition: \`modifiedReq.tools.length > 0 && !clientHasRecallTool(tools)\`. Strategy: (1) Response: recall \`tool\_use\` replaced with marker text via \`replaceRecallWithMarker()\`; result stored in \`sessionState.recallStore\`. (2) Next request: \`expandRecallMarkers()\` restores \`tool\_use\`+\`tool\_result\` pairs before forwarding. \`buildRecallFollowUp()\`: assistant message contains ORIGINAL \`tool\_use\` block (NOT marker text); user message contains \`tool\_result\` block with recall results — NOT plain text. This prevents LLM from echoing raw recall formatting (internal IDs, tier labels, scores) during streaming. Marker still emitted to client as user-visible indicator; only the upstream follow-up uses proper tool\_use/tool\_result. \`hasContinuationAfter\` logic: if blocks follow marker, truncates/splices; else prepends tool\_result to next user msg. Streaming: \`RecallAwareAccumulator\` suppresses recall SSE blocks. MAX\_RECALL\_DEPTH=10. Follow-up requests: \`cacheConversation: false\`.

<!-- lore:019e6afa-26e4-772c-a432-5273b52fdb71 -->
* **Gateway session eviction: per-module cleanup functions and memory profile**: Gateway accumulates per-session state in 13+ Maps/Sets across 8 modules with zero eviction. Memory dominated by \`gradient.prefixCache.cachedText\` (10-50KB), \`distillationSnapshot.rows\`, \`cacheAnalytics.lastRequestBody\` (Uint8Array), \`recallStore\`, LTM caches. Eviction is memory-only (no DB deletion) — reconstruction already implemented via \`getOrCreateSession()\` (pipeline.ts:879) and \`getSessionState()\` (gradient.ts:306). Per-module cleanup: \`deleteSessionAuth\` (auth.ts:103), \`deleteSessionCosts\` (cost-tracker.ts:577), \`deleteBillingPrefix\` (cch.ts:237), \`clearWarmupAuthDisabled\` (cache-warmer.ts:239), \`distillLimiter.evict()\`, \`curatorLimiter.evict()\` (session-limiter.ts). Configurable via \`LORE\_SESSION\_EVICTION\_TIMEOUT\` (default 1800s). Sub-agents evict after \`SUBAGENT\_EVICTION\_MS=5min\` via \`Math.min\`. GOTCHA: \`parsePositiveInt\` rejects \`0\` — \`LORE\_SESSION\_EVICTION\_TIMEOUT=0\` silently uses 1800s instead of disabling eviction; use \`parseNonNegativeInt\` for this setting. Map deletion during \`for...of\` iteration is safe in JS.

<!-- lore:019e5544-4bc6-7ae6-ba2c-8d4493f6e1cf -->
* **gradient.ts: consecutiveBusts lifecycle — init, mutation, persistence, and DB-restore skip**: \`consecutiveBusts\` in \`gradient.ts\` SessionState: (1) initialized to 0 in \`makeSessionState()\` line 298; (2) ONLY mutated in \`recordCacheUsage()\` line 144 — increments when \`bustRatio > 0.5\`, resets to 0 otherwise; (3) \`recordCacheUsage()\` called from \`pipeline.ts:postResponse()\` after each successful API response; (4) persisted to DB via \`saveGradientState()\` repurposing dead \`dynamicContextCap\` column (diagnostics only — value written but NEVER read back); (5) NEVER restored from DB on \`getSessionState()\` — explicitly skipped because stale values from prior process (different cache state) cause false \`unsustainable\` warnings. \`getSessionState()\` restore: \`lastTurnAt > 0\` is proxy for 'gradient state was ever flushed'; restores \`lastLayer\`, \`lastKnownInput\`, \`lastTurnAt\` atomically or none. \`cameOutOfIdle\` flag: set by \`onIdleResume()\`, consumed by LTM degraded-recovery branch to always recover LTM on post-idle turn. \`postIdleCompact\`: set by \`onIdleResume()\`, consumed by \`transformInner()\` to activate post-idle compact layer. \`unsustainable\` derived only from \`consecutiveBusts >= 5\`.

<!-- lore:019e5acc-87be-7373-a42e-16e8852dda40 -->
* **Hermes context markers: injection, extraction, sanitization, and stripping**: Hermes plugin injects \`\[lore:session-id=\<hex>]\` and \`\[lore:project=\<path>]\` into the last user message each turn (Tier 2.5 session identification). Gateway extracts markers in \`extractSessionMarker()\`/\`extractProjectMarker()\` before session identification, then must strip them via \`stripContextMarkers()\` before forwarding upstream — markers must never reach the LLM. \`extractProjectMarker()\` sanitizes: strips control chars, enforces 1024-char limit, rejects relative paths and \`..\` traversal components, strips trailing slashes. Session IDs must match \`\[a-f0-9]{8,64}\` — Hermes must always hash to hex via \`hashlib.sha256\` before injecting (Hermes-native session IDs may contain hyphens/uppercase that fail the regex). \`identifySession()\` returns \`tier: 2.5\` (type widened to \`1 | 2 | 2.5 | 3\`). \`stripContextMarkers()\` called in both \`handleConversationTurn\` and \`handleCompaction\`.

<!-- lore:019e571e-834e-7fd9-84dc-9c29506707da -->
* **Hermes package: Python agent integration at packages/hermes/**: \`packages/hermes/\` is a Python package (\`lore\_hermes\`) providing Hermes agent integration. Key files: \`lore\_hermes/engine.py\` (implements \`agent.context\_engine\` ABC), \`lore\_hermes/gateway.py\` (gateway proxy client), \`lore\_hermes/plugin.yaml\` (plugin descriptor), \`pyproject.toml\`. Tests in \`packages/hermes/tests/\` (pytest). Integration tests via \`test-integration.sh\`. Requires Hermes installed — Python tests cannot run in the monorepo without it because \`agent.context\_engine\` ABC is unavailable. End-to-end testing requires a full Hermes installation. The monorepo is \`lucky-cactus\` with packages: \`gateway\`, \`core\`, \`opencode\`, \`hermes\`.

<!-- lore:019e591d-acda-7f7d-b97b-85ec594a1445 -->
* **LLMClient.prompt() opts: urgent, thinking, workerID, maxTokens, temperature semantics**: \`LLMClient.prompt()\` opts (types.ts): \`urgent:true\` = bypass batch queue; blocking callers (compaction, overflow recovery, query expansion) must set it; fire-and-forget background work must NOT. \`thinking:false\` = disable extended thinking — ALL background workers always set this. \`workerID\`: maps to cost bucket. \`maxTokens\`/\`temperature\`: Gateway uses directly; OpenCode silently ignores both. Returns \`Promise\<string | null>\`. Only gateway's \`BatchLLMClient\` honors \`urgent\`; OpenCode and Pi adapters always process immediately regardless of \`urgent\` flag. All background LLM calls are non-streaming JSON. \`generateCompactionSummary()\`: \`compactMaxTokens = max(2048, min(ceil(inputLen/3 \* 0.5), 20\_000))\`; calls \`llm.prompt()\` with \`urgent:true, callType:'direct', temperature:0\`. \`scheduleBackgroundWork()\`: urgent distillation bypasses \`isBackgroundPaused()\`; incremental distillation uses \`callType: batchQueueEnabled ? 'batch' : 'direct'\`; curation multiplier: \`inputCost >= 5 → 3x\`, \`>= 1 → 2x\`, else \`1x\` applied to \`cfg.curator.afterTurns\`.

<!-- lore:019e5715-cccd-7be2-a979-407066c526d4 -->
* **log.ts: sink bridge, file rotation, and error visibility rules**: \`packages/core/src/log.ts\` (170 lines). Key rules: (1) \`log.error()\` \*\*always\*\* calls \`console.error()\` — 'Always visible — these indicate real failures.' \`log.info()\`/\`log.warn()\` only print if \`LORE\_DEBUG=1\`. (2) \`sink?.captureException(err)\` called only when an \`Error\` instance found in args via \`findError()\` — gateway bridges this to Sentry via \`registerSink()\`. (3) File logging: all levels written to \`~/.local/share/lore/lore.log\` regardless of \`LORE\_DEBUG\`; rotated at 5 MB (\`LOG\_MAX\_BYTES\`) with single \`.log.1\` backup; disabled in \`NODE\_ENV=test\`. (4) Only one \`LogSink\` supported at a time. Trap: any \`log.error(..., errorObj)\` auto-forwards to Sentry — including expected shutdown/auth errors. Use typed error subclasses (e.g. \`LocalProviderUnavailableError\`) to filter in \`beforeSend\`.

<!-- lore:019e30a6-ff62-723e-9fd8-56f1f1f60b5a -->
* **LTM confidence field: semantic meaning and rerankPreferences() for legacy entries**: \`ltm.create()\` accepts optional \`confidence\` param (default 1.0, clamped \[0,1]). Semantics: 1.0=unconditional directive, 0.9=strong preference, 0.8=moderate, 0.6=mild. \`CuratorOp\` create type includes \`confidence\`, wired through \`applyOps\`. \`rerankPreferences()\` in \`packages/core/src/ltm.ts\` re-scores legacy entries by directive keyword patterns (\`STRONG\_DIRECTIVE\_RE\`); skips entries whose \`confidence\` was already set to a non-default value — manual overrides are preserved. \`lore data rerank\` CLI command triggers re-ranking; also auto-runs after \`lore data recover\`. Run after deploying to fix existing preferences in DB.

<!-- lore:019e54f5-04be-7a9f-9e62-177d65d5db9f -->
* **LTM forSession() budget, phases, and safety-net constants**: \`forSession()\` (ltm.ts): budgets: \`budget.ltm\`=0.05, \`budget.preferenceLtm\`=0.02 (200k ctx → ~7,650/~3,060 tokens). Phase 1: architecture entries up to ARCH\_BUDGET\_FRACTION=0.2. Phase 2: remaining entries by score descending. Phase 3: .lore.md sections. HEADER\_OVERHEAD\_TOKENS=15 subtracted upfront. Hard cap: \`if (used + cost > maxTokens) continue\`. PROJECT\_SAFETY\_NET=5: top 5 project entries by confidence always included. NO\_CONTEXT\_FALLBACK\_CAP=10. Preference-only fast path skips relevance scoring. IF-2: global entries (pid=null) must be cross\_project=1. \`vectorSearch()\` has no project\_id filter (intentional); dedup threshold for Nomic v1.5 is 0.935. pipeline.ts injects LTM as 3 system prompt blocks: \[0]=host prompt (no cache), \[1]=stable preferences (1h cache\_control), \[2]=context-bound LTM (no cache, diff-pinned, deferred to turn 2+). Turn 1 only injects preferences. \`ltmSessionCache\` always deleted and replaced unconditionally on Layer 4 refresh.

<!-- lore:019e4f22-0872-7dcc-bbba-98447d2c0a59 -->
* **MCP server package plan: @loreai/mcp at packages/mcp/**: MCP server package: \`packages/mcp/\` for Cursor IDE integration. Three tools: (1) \`lore\_recall\` — wraps \`runRecall()\` from \`packages/core/src/recall.ts:1022\`, reuses \`RECALL\_TOOL\_DESCRIPTION\`/\`RECALL\_PARAM\_DESCRIPTIONS\`; (2) \`lore\_knowledge\` — wraps \`ltm.forSession()\` + \`formatKnowledge()\`, 4000-token budget; (3) \`lore\_observe\` — wraps \`temporal.store()\`. MCP Resources: \`lore://knowledge/{id}\`, \`lore://knowledge\`, \`lore://lore.md\`. Session ID: \`cursor-\<sha256(cwd+startup\_time)>\`. DB access: direct via \`@loreai/core\`; gateway API fallback when \`LORE\_REMOTE\_URL\` set. Dependencies: \`@loreai/core\` (workspace:\*), \`@modelcontextprotocol/sdk\` (^1.x). Key hooks: \`sessionStart\`, \`sessionEnd\`, \`preToolUse\`, \`postToolUse\`, \`stop\` (supports \`followup\_message\`, \`loop\_limit=5\`). Exit codes: 0=success, 2=block, other=fail-open. Cursor cannot use custom API base URL — transparent proxy rejected.

<!-- lore:019e1de2-7650-76c3-9416-40d905dd998f -->
* **OpenAI streaming translation: stateful SSE translators in stream/openai.ts**: (architecture) OpenAI streaming translation — stateful SSE translators in \`packages/gateway/src/stream/openai.ts\` and \`stream/openai-responses.ts\` consume Anthropic SSE events and emit OpenAI-format SSE. The pipeline carries \`effectiveProtocol\` in \`UpstreamResult\` to dispatch to the right translator. All translators must implement: (1) \`cancelled\` flag + \`cancel()\` handler aborting upstream via \`AbortController\`; (2) \`safeEnqueue()\` wrapper that no-ops if \`cancelled\`; (3) error \`catch\` block emitting a protocol-appropriate terminal event (\`response.failed\` for Responses API, \`\[DONE]\` with error for Chat Completions) — otherwise clients hang. OpenAI/Responses API upstreams don't receive LTM injection — \`req.system\` passed through unchanged; only the Anthropic path injects LTM. Fix: apply LTM injection to all upstream paths before forwarding.

<!-- lore:019e4586-82db-796f-a3cd-d65f790276bf -->
* **OpenCode x-session-affinity is a per-process nanoid — not stable across restarts**: OpenCode generates \`x-session-affinity\` natively as a nanoid — random, per-process, NOT stable across restarts. The Lore plugin (\`packages/opencode/src/index.ts\`) never touches this header. \`input.sessionID\` in plugin hooks is OpenCode's persistent DB \`Session.id\` — stable across restarts. When OpenCode restarts, the new nanoid causes Tier 1 in \`identifySession()\` to create a brand-new Lore session, orphaning prior distillations/gradient state. Fix: inject \`input.sessionID\` as \`x-lore-session-id\` in the \`chat.headers\` hook. Tier 1 upgrade-path trap: when plugin starts sending \`x-lore-session-id\` alongside \`x-session-affinity\`, prior sessions indexed under \`x-session-affinity\` are invisible — Tier 1b must check both header names.

<!-- lore:019e1952-398c-753f-b92c-4a5fa5ecf15f -->
* **Pi plugin: which providers can be proxied through the gateway**: Pi plugin gateway proxy compatibility by wire protocol. \*\*Proxiable\*\*: \`anthropic\` → \`/v1/messages\`: \`anthropic\`, \`fireworks\`, \`github-copilot\`; \`openai-completions\` → \`/v1/chat/completions\`: \`deepseek\`, \`xai\`, \`groq\`, \`cerebras\`, \`openrouter\`, \`huggingface\`, \`opencode\`, \`opencode-go\`; \`openai-responses\` → \`/v1/responses\`: \`openai\`, \`azure-openai-responses\`, \`openai-codex\`, \`azure-openai\`, \`lm-studio\`, \`ollama\`. \*\*Cannot proxy\*\*: \`google\`, \`google-vertex\`, \`amazon-bedrock\`, \`mistral\`. \`registerProvider(name, { baseUrl })\` overrides base URL. Gateway routes by URL path only. OpenAI streaming clients receive true incremental SSE (\`stream/openai.ts\`).

<!-- lore:019e4c7d-bfae-7c96-a491-e57cef38d84d -->
* **pipeline.ts: per-turn auth binding, billing prefix capture, and session bootstrap sequence**: pipeline.ts key behaviors: (1) \`extractAuth()\` → \`setLastSeenAuth(cred)\` + \`setSessionAuth(sessionID, cred)\` on every request; (2) \`captureBillingPrefix(sessionID, req.system)\` per-session; (3) new sessions: \`fingerprintMessages()\`, \`learnHeaders()\`, \`tryImportKnowledge()\`; (4) \`stripContextWarnings()\` before forwarding; (5) 1h TTL → \`effectiveCacheWriteCost = cacheWriteCost \* 2\`. \`getLLMClient()\` (lines 799-832): lazy singleton; calls \`createGatewayLLMClient({ anthropic: config.upstreamAnthropic, openai: config.upstreamOpenAI }, resolveAuth, defaultModel)\`, wraps with \`createBatchLLMClient()\` unless \`LORE\_BATCH\_DISABLED=1\`; \`upstreams\` and \`resolveAuth\` baked in at creation — no per-call override. \`postResponse()\` (line 2011): \`sessionState.lastAnthropicBeta\` always updated. Temporal storage skipped when \`sessionState.amnesia || req.rawHeaders\['x-lore-no-store'] === 'true'\`. \`outputTokensEMA\` (α=0.3): first turn set directly; subsequent \`Math.round(prev \* 0.7 + outputTokens \* 0.3)\`. Adaptive max\_tokens = \`max(MAX\_TOKENS\_FLOOR, 3 × outputEMA)\`. Pipeline always returns internal Anthropic-format; translation to OpenAI/Responses format happens after pipeline returns.

<!-- lore:019e5b1f-7b84-7407-8598-62c9ab0c50fe -->
* **prompt.ts: curator system prompt structure — 5 categories, 3 preference forms, confidence scale**: \`CURATOR\_SYSTEM\` (prompt.ts): 5 knowledge categories: \`decision\`, \`pattern\`, \`preference\`, \`architecture\`, \`gotcha\`. 3 preference forms: Directive ('always'/'never'), Declarative ('I use X'), Behavioral (repeated actions). Confidence: 1.0=unconditional directive, 0.9=strong explicit, 0.8=inferred/repeated, 0.6=mild/contextual. \`crossProject\` default=true. Gotcha format: 'Trap: X looks right because \[reason]. Fix: Y.' Decision format: 'Chose X over Y because Z.' \`curatorUser()\` rules: prefer update over create, replace content on update (never append), merge same-system entries, extract ALL preference forms, handle preference evolution via update, resolve ambiguous refs to canonical names.

<!-- lore:019e5922-4280-76aa-9b79-5ebeae433d7a -->
* **Sub-agent session detection, isolation, and differential treatment**: Sub-agent sessions detected via \`x-parent-session-id\` header (pipeline.ts:2773-2809). Each sub-agent gets independent session: temporal storage, gradient state, distillation pipeline, LTM injection, cost tracking. Key differential treatment: (1) cache warming ALWAYS skipped — \`shouldWarm()\` returns false unconditionally (cache-warmer.ts:731-733); (2) \`findRotationPredecessor()\` (session.ts:505) skips sub-agents from Tier 1b rotation; (3) idle distillation/curation runs normally (no isSubagent check). \`isSubagent\`/\`parentSessionId\` persisted to DB (migration v26). Dashboard: collapsible cost-rollup tree via \`loadParentChildMap()\` + \`buildLiveSessionRows()\`; \`rollUp()\` propagates grandchildren costs bottom-up.

<!-- lore:019e56c3-c6af-78fd-a312-8ed73905680b -->
* **Team mode knowledge promotion: both explicit sharing and auto-suggest; approval workflow**: Team mode knowledge promotion: explicit sharing + auto-suggest with approval workflow. Architecture: database-per-user/team model (NOT shared DB with visibility column). Personal DB = local SQLite synced to Turso cloud; Team DB = Turso cloud only. Schema v29: \`knowledge\` table gets +11 cols (sensitivity, promotion\_status, source\_user\_id, source\_entry\_id, created\_by, updated\_by, promoted\_at, approval\_status, approved\_by, approved\_at, last\_accessed\_at); new \`team\_knowledge\` and \`team\_config\` tables. \`Sensitivity\`/\`PromotionStatus\`/\`ApprovalStatus\` types exported from ltm.ts. \`create()\` accepts \`createdBy?\` and \`sensitivity?\` params. \`update()\` accepts \`updatedBy?\`. Cross-project knowledge stays personal. \`recoverMissingObjects()\` must include \`team\_knowledge\` and \`team\_config\` tables. RFC documented in GitHub Issue #467. Implemented in feat/team-sync-schema branch (PR pending).

<!-- lore:019e5b1f-7b8c-7b98-9724-ffc5c3442ea3 -->
* **ui.ts: server-rendered dashboard — routes, components, and JS patterns**: ui.ts: pure server-rendered HTML dashboard (~2800 lines). No frontend framework; inline CSS with light/dark mode. PRG pattern for destructive actions. Key: \`renderChatBubble()\` (user=right/blue, assistant=left; chunk separator \`\n\x1f\`), \`renderCostSummary()\` (3 bars), \`renderWarmingSection()\`. Inline JS: table sorting (localStorage keyed \`'lore-sort:'+tableId\`), subagent tree toggle, table filter with tree-aware visibility, chat filter 150ms debounce. Route matching via \`matchRoute()\` with \`:param\` + \`decodeURIComponent\`. \`pageEntities()\` batch-loads knowledge ref counts via single SQL GROUP BY (avoids N+1). Entity metadata POST: \`role\`/\`description\`/\`notes\` must be capped at 500 chars (consistent with curator's \`filterEntities()\` limit).

<!-- lore:019e6f25-51b7-7b3c-8461-dfec7c531902 -->
* **Worker credential routing: LORE\_WORKER\_API\_KEY, LORE\_WORKER\_UPSTREAM, LORE\_WORKER\_MODEL env vars**: Three env vars added in PR #482 for routing background worker LLM calls independently from session credentials. All wired through \`getLLMClient()\` in pipeline.ts (the single construction site): \`LORE\_WORKER\_API\_KEY\` → \`getWorkerAuth\` callback replaces \`resolveAuth\` for all worker calls; \`LORE\_WORKER\_UPSTREAM\` → \`workerUpstreams\` object overrides both anthropic+openai URLs; \`LORE\_WORKER\_MODEL\` → checked first in \`getWorkerModel()\` before config-file override (format: \`providerID/modelID\`). Zero changes needed in \`@loreai/core\`, \`auth.ts\`, \`llm-adapter.ts\`, or \`batch-queue.ts\` — dependency injection at construction time is the key design.

<!-- lore:019e6f1a-ca8f-73f2-b055-c27c3216e0b3 -->
* **worker-model.ts: getWorkerModel() resolution, EXPENSIVE\_MODEL\_THRESHOLD, and validated worker pairs**: \`worker-model.ts\` (gateway): fetches pricing from \`https://models.dev/api.json\` (cached 1h). \`EXPENSIVE\_MODEL\_THRESHOLD=1.5\` ($/M input). \`WORKER\_DEFAULTS\`: anthropic→\`claude-sonnet-4-6\` (alreadyCheap: includes 'sonnet'/'haiku'), openai→\`gpt-5.4-mini\` (alreadyCheap: includes 'mini'/'nano'), github-copilot→\`gpt-5.4-mini\`. \`getWorkerModel()\` resolution: (1) explicit config override, (2) provider-aware cost default, (3) general fallback (GPT-5.4-mini), (4) session model. Validated worker pairs: Anthropic Opus 4.6→Sonnet 4.6 (quality parity, 23/25 obs); OpenAI GPT-5.4→GPT-5.4-mini (exact match, 24/24 obs). Disqualified: Gemini 3.1 Pro→Gemini 3 Flash (-35% drop). \`GENERAL\_FALLBACK\_WORKER={providerID:'github-copilot', modelID:'gpt-5.4-mini'}\` — only used for GitHub Copilot. Call sites: pipeline.ts lines 2369, 2381, 2410, 2466, 2525, 2584, 4173; idle.ts:334.

<!-- lore:019e4c39-882b-7734-a030-a0721201314d -->
* **workspace.ts: discoverWorkspaceRoot() and resolveWorkspaces() — monorepo support**: workspace.ts: \`discoverWorkspaceRoot(startDir)\` walks UP checking markers: (1) \`.lore.json\` with non-empty \`workspaces\`, (2) VCS markers (\`.git/.hg/.jj\`), (3) workspace markers (\`pnpm-workspace.yaml\`, \`nx.json\`), (4) language markers. Stops at \`homedir()\`. Process-lifetime cache. CRITICAL BUG: homedir boundary check comes AFTER marker checks — if \`~/.git\` exists, returns \`$HOME\` as workspace root. Fix: move \`if (current === stopBoundary) break\` BEFORE marker checks. TRAP: glob \`\*\` in \`resolveWorkspaces()\` matches dot-prefixed dirs — add \`if (entry.name.startsWith('.')) continue\`. TRAP: no path traversal guard — \`../\` patterns reach outside root.

<!-- lore:019e4bbd-ade4-7355-8fb1-87df54c8ea69 -->
* **X-Lore-\* headers: project path injection and resolution priority**: (architecture) X-Lore-\* headers: project path injection and resolution priority: Project path resolution priority in \`packages/gateway/src/config.ts\` lines ~246-274: 1) \`X-Lore-Project\` header (absolute path, max 1024 chars, strips trailing slashes/control chars), 2) inferred from system prompt (\`cwd\` JSON key, \`Working directory:\` prefix, \`.lore.md\` path, generic \`/home/\`/\`/Users/\` paths), 3) \`process.cwd()\` fallback. Source tagged as \`ProjectPathSource\`: \`"header" | "inferred" | "cwd"\`. Pipeline warns on \`cwd\` source (dedup via \`cwdWarned\` Set). \`agents.ts\` \`envVars()\` injects \`X-Lore-Project: \<cwd>\` + \`X-Lore-Git-Remote: \<remote>\` into \`ANTHROPIC\_CUSTOM\_HEADERS\`. Other headers: \`x-lore-session-id\`, \`x-lore-no-store\`, \`x-lore-upstream-url\`, \`x-lore-recall-invoked\` (response). \`lore run\` always runs locally; remote mode delegates to existing remote gateway but computes git remote locally.

### Decision

<!-- lore:019e596c-e3a8-7229-bca2-92c0fe79e268 -->
* **Budget throttling uses proxy sleep / gradual slowdown, not hard 429 blocks**: Budget throttling uses proxy sleep (invisible delay) not hard 429 blocks. Rationale: flat 60s/120s Retry-After rejected as 'super annoying'; 429 triggers unpredictable client retry behavior; proxy sleep is invisible — upstream just appears slower; 3-5s sleep imperceptible during normal coding turn. Only conversation turns (Case 3 in pipeline.ts) throttled; meta requests and compaction exempt. Background workers excluded — their fraction of total costs is not meaningful to throttle. The throttle exploits gateway-level cross-request visibility to apply gentle friction without triggering client retry machinery.

### Gotcha

<!-- lore:019e5b19-138a-7e00-ab9e-720769924303 -->
* **bootstrapDailySpend() overcounts multi-day sessions and has double-count risk**: cost-tracker.ts bugs: (1) \`bootstrapDailySpend()\` queries \`session\_state WHERE updated\_at >= midnightMs\` and sums lifetime \`conversation\_cost+worker\_cost\` — multi-day sessions flushed today are overcounted. Fix: add \`daily\_cost\` column reset each UTC day, or restrict to \`created\_at >= midnightMs\`. The live-session loop after DB query double-counts already-flushed sessions (safe at startup only). (2) \`setDailyBudget()\` uses \`Math.max(0, budgetUSD)\` but \`Math.max(0, NaN) === NaN\` — stores \`'NaN'\` string in KV. Fix: add \`if (!Number.isFinite(budgetUSD) || budgetUSD < 0) throw\` before storing. (3) \`computeThrottleDelay\` returns 0 at 100%+ spend when EMA decayed to zero (\`0 <= 0\` check passes). Fix: add \`if (spendFraction >= 1.0) return MAX\_THROTTLE\_DELAY\` before rate check.

<!-- lore:019e5993-5e95-77cb-86e4-e5cb3ff76d4d -->
* **Budget throttle proxy sleep must be capped to avoid cache TTL busts**: Trap: injecting a proxy sleep for budget throttling looks safe because it's invisible to the client. But if the sleep pushes total request time past the cache TTL boundary (5m default, 1h upgraded), the next user turn arrives at a cold cache — causing a cache bust that costs MORE than the throttle saved, making the budget situation worse. Fix: cap the sleep at 50% of the remaining cache TTL window. \`sessionState.resolvedConversationTTL\` is available at the interception point (pipeline.ts ~line 3391); compute \`elapsed = now - lastRequestTime\`, \`remaining = ttlMs - elapsed\`, \`maxSleep = remaining \* 0.5\`. If cache is already expired, sleep = 0. This prevents the throttle from being self-defeating.

<!-- lore:019e1c27-967c-7eb4-bd0e-afb195823970 -->
* **Bun NAPI crash on process.exit() — use safeExit() via libc \_exit()**: Bun NAPI crash on process.exit() with fastembed — use safeExit(): Loading fastembed (onnxruntime NAPI bindings) causes a C++ panic on \`process.exit()\` because Bun runs NAPI teardown destructors that throw. Fix: \`packages/gateway/src/cli/exit.ts\` exports \`safeExit(code)\` — uses \`\_exit()\` from libc via \`bun:ffi\` under Bun, falls back to \`process.exit()\` under Node.js. All gateway exit paths must use \`safeExit()\`. Do NOT call \`embedding.resetProvider()\` in test teardown \`resetPipelineState()\` — move \`resetProvider()\` to \`shutdown()\` in \`start.ts\` only. \`resetPipelineState()\` must preserve the 'fastembed unavailable' cached state.

<!-- lore:019e6a44-dbf1-7df6-ba60-94f75c21b81d -->
* **consolidationUser() batchTarget frame-of-reference mismatch: global target vs batch count produces negative excess**: Trap: \`batchTarget\` in batched consolidation is computed globally (\`Math.max(cfg.curator.maxEntries, entries.length - CONSOLIDATION\_BATCH\_SIZE/2)\` = 1118 for 1143 entries) but \`consolidationUser()\` computes \`excess = count - targetMax\` where \`count\` is the batch size (50). Result: \`excess = 50 - 1118 = -1068\` — prompt tells LLM 'must remove at least -1068 entries', LLM returns empty ops, idle scheduler enters 1h cooldown, system never converges. Fix: \`batchTarget\` must be batch-relative — \`Math.ceil(CONSOLIDATION\_BATCH\_SIZE / 2)\` so \`excess = 50 - 25 = 25\`. Fixed in e8a73f9. The non-batched path (entries ≤ BATCH\_SIZE) correctly uses \`cfg.curator.maxEntries\` as target.

<!-- lore:019e54e4-fe50-71e7-ab69-49ac97cf32f3 -->
* **consumeSSE retries counter never resets after successful reconnection — permanent failure after 10 total disconnects**: Trap: \`retries\` in \`consumeSSE\` (src/commands/local/server.ts:469) increments on each disconnect but never resets on successful reconnection. After 10 total disconnections (even with successful reconnects between them), the client permanently stops. Looks correct because 10 retries sounds like a per-attempt limit. Fix: reset \`retries = 0\` on successful connection (first message or \`open\` event) so it tracks only consecutive failures. Also: initial SSE connection should fail fast on \`'error'\` result rather than retrying — fixed in commit d967edd51.

<!-- lore:019e6a1b-3408-7609-ba6b-d4ba9d552afd -->
* **curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields**: Trap: \`filterOps()\` (curator.ts:109–117) only checks \`op\` is a string — it does NOT validate required fields (\`content\`, \`title\`, \`category\`) for \`create\` ops, casting result as \`CuratorOp\[]\` silencing TypeScript. When LLM emits a malformed \`create\` op missing \`content\` (more likely with richer context like CROSS-SESSION REPEATED INSTRUCTIONS + action tags), \`applyOps()\` line 226 accesses \`op.content.length\` without null guard → \`TypeError: undefined is not an object (evaluating 's.content.length')\`. Fix: add null guard at line 226 (\`if (!op.content) skip/throw\`) AND add field validation in \`filterOps()\` for \`create\` ops. The \`update\` branch at line 252 already has \`op.content !== undefined &&\` guard — \`create\` branch lacks equivalent.

<!-- lore:019e6f25-51ab-788a-9aa9-ce5499a5f7ae -->
* **Edit tool silently fails to persist file changes in OpenCode worktrees — use Python/sed for critical edits**: Trap: the edit tool reports 'Edit applied successfully' but the file on disk is unchanged in OpenCode worktrees (confirmed in \`packages/core/src/ltm.ts\`, \`packages/core/src/db.ts\`, and \`packages/core/test/ltm.test.ts\`). Looks correct because the tool returns success. Fix: use Python scripts or \`sed\` to apply changes directly to disk when the edit tool fails. Verify with \`grep\` or \`head\` after every edit. The issue appears specific to certain files in the worktree — gateway files (\`config.ts\`, \`pipeline.ts\`, \`worker-model.ts\`) persisted correctly while core files did not. Also: stash pop after branch switching can cause merge conflicts that silently revert changes.

<!-- lore:019e5707-9cab-74bb-88c0-b24b83a395d4 -->
* **embedding worker shutdown() must use LocalProviderUnavailableError — plain Error hits Sentry**: Trap: \`shutdown()\` rejecting pending requests with \`new Error('embedding worker shut down')\` (embedding.ts:471) looks correct but causes Sentry noise. \`log.error()\` in fire-and-forget callers (\`embedKnowledgeEntry\`, \`embedDistillation\`, \`embedTemporalMessage\`) calls \`sink.captureException(err)\` for any \`Error\` instance — including expected shutdown errors. Fix: reject with \`new LocalProviderUnavailableError('embedding worker shut down')\` so the error type is consistent with other unavailability signals. Also add 'embedding worker shut down', 'WASM fatal error', and \`LocalProviderUnavailableError\` patterns to \`beforeSend\` in \`instrument.ts\` as defense-in-depth. Sentry issue LOREAI-GATEWAY-Q (29 events, 2 users) was caused by this. LOREAI-GATEWAY-C (92 events, 2 users) is caused by WASM \`Aborted()\` not being caught by \`isOomError()\` — see \[\[019e50e7-6250-779e-a842-b1b5baf87295]] for the fix.

<!-- lore:019e6f25-51c3-738a-bdda-225933169d76 -->
* **ensureSelfEntity() is defined but never called — entity detail pages show no metadata/relationships until curator runs**: Trap: \`entities.ensureSelfEntity()\` (entities.ts:305) is defined but never called anywhere in the codebase — confirmed via grep across all source files. Without a self entity, the curator's entity context lacks 'you (the user)' — the LLM can't anchor relationship extraction. Result: entity detail pages show empty metadata and relationships even after the curator runs. Fix: call \`entities.ensureSelfEntity(input.projectPath)\` before \`entities.forProject()\` in curator.ts (~line 449), so the self entity exists when \`formatForPrompt()\` builds the entity context. Also: empty entity detail pages are expected until curator populates them — the UI code is correct.

<!-- lore:019e69f0-a066-708c-90b2-77ceb461a2d6 -->
* **LOREAI-GATEWAY-1B: TDZ 'Cannot access before initialization' in batch-queue prompt() — root cause unresolved**: Trap: \`Cannot access 'u' before initialization\` in minified \`Object.prompt\` looks like a circular import or esbuild TDZ from \`async function\` → \`const\` conversion. Both hypotheses ruled out: (1) esbuild preserves \`async function\` declarations as hoisted statements (confirmed via test builds); (2) no circular imports in batch-queue.ts dependency chain (auth, cost-tracker, llm-adapter, worker-model, compaction all leaf modules). \`u\` in minified \`flush()\` is a local \`let\` inside an async for-loop body — not module-level. Root cause requires inspecting the actual production bundle (\`dist-bin/bin.js\`). vLLM returns 404 on \`/v1/files\` (no batch API support) → triggers \`fallbackAll()\` path, not the TDZ site directly. Breadcrumb: 'openai file upload failed: 404' precedes the crash.

<!-- lore:019e69f0-a05a-7d48-8e35-76baf13533c5 -->
* **LOREAI-GATEWAY-1E: resp.usage undefined crash in postResponse() for vLLM/OpenAI path**: Trap: \`GatewayUsage\` typed as non-optional on \`GatewayResponse\`, so \`resp.usage.inputTokens\` looks safe. But \`resp.usage\` can be \`undefined\` at runtime (e.g., vLLM at \`http://localhost:8300\` returning partial response). All four accumulators construct \`usage\` with \`?? 0\` guards on individual fields — but \`resp.usage\` itself can still be undefined if an exception produces a partial response object. Crash sites: pipeline.ts lines 2069, 2101, 2137-2140, 2150-2153, 2204-2206, 2282-2285, 2341, 2363, 2402; also \`buildOpenAINonStreamResponse\` (openai.ts:296-300). Fix: add \`const usage = resp.usage ?? { inputTokens: 0, outputTokens: 0, cacheReadInputTokens: 0, cacheCreationInputTokens: 0 }\` guard at top of \`postResponse()\` and \`nonStreamHttpResponse()\`, then use \`usage.\*\` throughout.

<!-- lore:019e6b04-ae79-7df6-b34c-aad06d33c6c0 -->
* **LOREAI-GATEWAY-1G/1H: pickRemoteFallback() uses OPENAI\_API\_KEY=nokey placeholder — causes 401 Sentry noise**: Trap: \`pickRemoteFallback()\` (embedding.ts:633) treats any truthy \`OPENAI\_API\_KEY\` as valid. Users who set \`OPENAI\_API\_KEY=nokey\` as a placeholder (common when using custom \`OPENAI\_BASE\_URL\` with Codex/OpenCode) trigger the local→remote fallback path, which sends \`Authorization: Bearer nokey\` to \`api.openai.com/v1/embeddings\` → 401 → \`log.error()\` → Sentry. Full flow: LocalProvider WASM crash → \`embed()\` catches \`LocalProviderUnavailableError\` → \`pickRemoteFallback()\` finds truthy \`OPENAI\_API\_KEY\` → \`OpenAIProvider('nokey')\` → 401 → fire-and-forget \`.catch()\` → \`log.error()\` → \`sink.captureException()\` → Sentry. Two fixes: (1) add \`/Incorrect API key/i\` to \`TRANSIENT\_ERROR\_PATTERNS\` in \`instrument.ts:78\`; (2) validate key format in \`pickRemoteFallback()\` before constructing \`OpenAIProvider\` (e.g., \`key.startsWith('sk-') && key.length > 20\`). The literal string \`"nokey"\` does NOT exist in the Lore codebase — it comes from the user's shell environment.

<!-- lore:019e56fb-b446-759b-aff2-035a83700124 -->
* **LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm — resolveAuth returns same stale token**: LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm. \`resolveAuth(sessionID)\` marks session stale and falls back to \`getLastSeenAuth()\` — same expired token. \`credentialChanged=false\` so retry-once path never taken. Idle scheduler fires 4+ LLM calls/tick (distill, meta-distill, curation, consolidation), each 401ing independently. \`isAuthStale()\` exported but NEVER called in non-test source. Fix: (1) check \`isAuthStale(sessionID)\` in idle.ts before scheduling LLM work; (2) \`resolveAuth\` returns null when global fallback matches stale credential; (3) add \`/Worker upstream auth error/\` to \`TRANSIENT\_ERROR\_PATTERNS\` in \`instrument.ts\`. Circuit breaker (background-limiter.ts) only trips on 429, not 401.

<!-- lore:019e50e7-6250-779e-a842-b1b5baf87295 -->
* **ONNX OOM error code 284432024: single oversized text bypasses batch budget guard**: ONNX OOM error chain (Nomic v1.5): Nomic v1.5 pads ALL batch texts to longest sequence — one oversized text → huge tensor → OOM. Fix chain: (1) \`LOCAL\_MAX\_CHARS = 16384\` (4 chars/token × 4096 tokens) in \`embedding.ts:38\`; (2) Worker-level OOM-retry loop in \`processEmbed()\` (\`embedding-worker.ts:267-281\`): attempt=0 tries full texts; on OOM, \`maxTokens = OOM\_RETRY\_START\_TOKENS(4096) >> attempt\` → 4096→2048→1024; \`OOM\_MAX\_RETRIES=3\`; (3) \`truncateTexts(texts, maxTokens)\` helper; (4) \`nextBatch()\` adaptive batching caps \`batch\_size×max\_tokens\` at \`MAX\_BATCH\_TOKEN\_AREA=4096\`; (5) \`safeLocalTruncate()\` backs up 1 char on high surrogate boundary. \`isOomError()\` matches \`/^\d{6,}$/\` and \`/out.of.memory|alloc.\*fail|oom/i\`. Known OOM codes: 284432024, 287180544, 144786472. WASM \`Aborted()\` error is NOT caught by \`isOomError()\` — it's a fatal WASM abort. Fix: detect \`isWasmFatalError()\` matching \`/Aborted\\(\\)/i\`, post \`{ type: 'init-error' }\` and call \`process.exit(1)\` to trigger main-thread \`on('exit')\` handler which sets \`localProviderKnownBroken=true\`.

<!-- lore:019e4eab-59b4-7fe3-be03-874f682cc340 -->
* **openai.ts: array-format content in system/developer messages silently dropped**: (gotcha) openai.ts / openai-responses.ts parsing traps: (1) \`parseOpenAIRequest\` (openai.ts:67–69) coerces non-string content to \`""\` — array-format content in \`system\`/\`developer\` messages silently dropped. Fix: check \`Array.isArray(content)\` and extract text parts. (2) \`buildOpenAIMessages\` (openai.ts:523–529): \`tool\_result\` blocks pushed mid-loop before \`textParts\`/\`toolUses\` flushed — tool\_result appears before text/tool\_use of same message. (3) \`buildOpenAIMessages\` always re-emits system as \`role:'system'\`, never \`'developer'\`. (4) \`parseOpenAIResponsesRequest\` reads system prompt from top-level \`instructions\` field — \`developer\`/\`system\` role messages in input array treated as regular user messages. LTM injection for Responses API: \`\[req.system, cache?.stableLtmSystem, cache?.ltmSystem].filter(Boolean).join('\n\n')\`.

<!-- lore:019e6bc9-17f3-780e-82e5-dfa7c11e329a -->
* **parsePositiveInt rejects 0 — 'set to 0 to disable' env var pattern silently falls back to default**: Trap: \`parsePositiveInt(env.LORE\_VAR, default)\` in \`packages/gateway/src/config.ts\` uses \`n <= 0\` guard — rejects \`0\` and falls back to the default with a warning. Any feature documented as 'set to 0 to disable' via env var silently ignores the user's intent. Confirmed: \`LORE\_SESSION\_EVICTION\_TIMEOUT=0\` silently uses 1800s. Fix: use a separate \`parseNonNegativeInt\` (guard: \`n < 0\`) for settings where 0 is a valid sentinel. Tests that bypass \`loadConfig()\` by setting the config field directly (e.g., \`sessionEvictionTimeoutSeconds: 0\`) will miss this bug — always test the env var path too. PR #478 had this exact bug: test at eviction.test.ts:158 set config directly, hiding the parsePositiveInt rejection.

<!-- lore:019e1de4-cada-77cf-ac3b-79ab96b963e4 -->
* **Query expansion disabled by default AND LLM client not wired in gateway (IF-5 fix)**: Two co-required fixes for query expansion (IF-5): (1) \`queryExpansion\` default in \`packages/core/src/config.ts\` must be \`true\` in BOTH the field-level \`.default()\` AND the section-level \`.default({})\` — the section-level overrides field-level in Zod, so changing only one has no effect. (2) \`executeRecall()\` in \`packages/gateway/src/recall.ts\` must accept an optional \`LLMClient\` param and pass it to \`runRecall()\` — without it, \`if (searchConfig?.queryExpansion && llm)\` is always false. Both pipeline.ts call sites (streaming and non-streaming) must pass \`getLLMClient(config)\`.

<!-- lore:019e6950-8200-7473-87a5-45a861bb84af -->
* **recall.ts: unescaped quotes/ellipsis in marker strings cause store miss and raw marker leak**: Trap: \`buildRecallMarker()\` embeds raw \`query\` between double-quotes and raw \`id\` before \`…\` — looks safe because queries are usually plain text. But if \`query\` contains \`"\`, \`MARKER\_REGEX\` (\`/📚 Searching (.+?) for "(.+?)"…/\`) truncates the captured group at the first inner quote; \`recallStoreKey()\` produces a mismatched key; \`store.get(key)\` returns undefined; the marker is left as raw text in the conversation visible to the upstream LLM. Same issue for \`id\` containing \`…\`. Fix: escape \`"\` → \`\\"\` in \`buildRecallMarker()\` and unescape in \`parseRecallMarker()\`; for IDs, percent-encode or strip \`…\` before embedding.

<!-- lore:019e2bbf-6dba-72a1-babb-b58a51f07ea5 -->
* **Remote import idempotency split-brain: isImported queries local DB, recordImport writes to remote**: When \`LORE\_REMOTE\_URL\` is set, \`lore import\` has a split-brain bug: \`isImported()\` checks the \*\*local\*\* SQLite \`import\_history\` table, but \`remotePost('/api/v1/import/record')\` writes the record to the \*\*remote\*\* DB. Result: every subsequent run re-detects all sessions as un-imported and double-extracts. Fix: (1) add \`GET /api/v1/import/history\` endpoint and query remote during dedup check when remote URL is set; (2) optionally also write locally as offline resilience. The remote DB is the source of truth for import history in remote mode.

<!-- lore:019e69c0-1076-7a01-bb5c-b4932fc212bb -->
* **Sentry sourcemap resolution de-obfuscates minified names in error titles — not raw JS engine messages**: Trap: Sentry issue titles like \`ReferenceError: buildOpenAINonStreamResponse is not defined\` look like the original function name survived minification. Fix: Sentry applies server-side sourcemap resolution to issue titles and stack frames — the \`names\` array in \`.map\` files contains original identifiers. The raw JS engine error would say \`ReferenceError: est is not defined\` (minified). LOREAI-GATEWAY-1C (\`log is not defined\`) and LOREAI-GATEWAY-1D (\`buildOpenAINonStreamResponse is not defined\`) both confirmed as NO BUG in source — likely stale sourcemap mismatch between deployed binary and uploaded sourcemaps. \`AOe\` in bundle maps to \`sumTokens\` (distillation.ts:142), NOT the OpenAI translation layer. When investigating Sentry errors with original function names, verify the minified bundle before assuming a missing-import bug.

<!-- lore:019e2e20-95b3-7a9d-ab38-77d87eafecc4 -->
* **splitSegments() infinite recursion on oversized single messages**: \`splitSegments()\` infinite recursion on oversized single messages in \`packages/core/src/distillation.ts\`: recurses infinitely when a single message exceeds \`maxSegmentTokens\` (16384). \`findSplitIndex()\` returns \`messages.length\` (=1), so \`left = messages.slice(0, 1)\` produces an identical recursive call. Triggered on large tool outputs (~49KB+). Fix: add base case after the \`totalTokens <= maxTokens\` guard — \`if (messages.length <= 1) return \[messages]\`. The oversized message becomes an indivisible segment.

<!-- lore:019e49d5-15c4-78d1-8bbc-f43fde34d9f1 -->
* **startServer() returns plain object, not Promise — use startGateway() for programmatic async use**: Gateway startup traps: (1) \`startServer()\` returns \`{ stop, port, hosts }\` synchronously — use \`startGateway()\` from \`cli/start.ts\` for \`Promise\<GatewayHandle>\`. (2) \`loadConfig()\` in \`config.ts\` is SYNC — calling \`.then()\` throws TypeError. (3) EADDRINUSE fires as async \`'error'\` event — wrap \`server.listen()\` in a Promise. (4) Use port \`0\` (ephemeral) in tests. (5) \`@loreai/core\` exports async \`load(directory)\` — distinct from gateway's sync \`loadConfig()\`.

<!-- lore:019e5b1f-7b93-7f5f-8021-7c2ff77d7eee -->
* **UI entity metadata POST bypasses curator's 500-char field length validation**: Fixed in dc4cd9d — no longer actionable gotchas: (1) UI POST handler for entity metadata bypassed 500-char validation on \`role\`/\`description\`/\`notes\`. (2) \`addRelation()\` lacked self-referential guard (\`entity\_a === entity\_b\`). (3) \`entities.create()\` did metadata merge and \`addAlias()\` outside transaction — race condition. (4) \`ensureSelfEntity()\` must pass \`projectPath: undefined\` so self entity gets \`project\_id=NULL\` for cross-project dedup. (5) \`cmdEdit\` double-JSON-stringified metadata — pass raw object to \`entities.update()\`. All fixed.

<!-- lore:019e1cd6-05d2-74c5-aea8-fd827a4a45e7 -->
* **vectorSearch() is unscoped — test cleanup must delete all embedding rows**: \`vectorSearch()\` in \`packages/core/src/ltm.ts\` queries \`knowledge WHERE embedding IS NOT NULL AND confidence > 0.2\` with no \`project\_id\` filter (intentional for cross-project search). Two gotchas: (1) Test suites scoped to one project leak embedding rows into other vectorSearch tests — \`beforeEach\` must \`DELETE FROM knowledge WHERE embedding IS NOT NULL\`. (2) \`vectorSearch()\` has no \`excludeCategories\` param — category exclusions from \`forSession()\` callers have no effect; add optional \`excludeCategories\` param and propagate from callers. Also: global entries (pid=null) force \`crossProject=true\`; confidence is clamped to \[0.0, 1.0] in \`update()\`.

<!-- lore:019e49f0-1d65-7737-96da-cb4b04122681 -->
* **vitest-evals requires vitest and tinyrainbow as direct devDependencies**: (gotcha) vitest-evals / buildLoreContext / createJudgeHarness traps: (1) \`vitest-evals\` lists \`vitest\` and \`tinyrainbow\` as peer deps — add both explicitly to root \`devDependencies\`. (2) \`buildLoreContext(turns, contextWindow = 200\_000)\` — second arg is a token count number, NOT a model string. Passing a model string yields \`NaN\` → empty lore context. (3) \`createJudgeHarness\` \`run\` callback receives positional args \`(input, options)\` — destructuring \`{ input }\` yields \`undefined\`; fix: \`run: async (input, \_options) => { /\* use input.system, input.prompt \*/ }\`. (4) \`inflateScenario(scenario, opts?)\` — opts is \`{ targetTokens?, excludeKeywords? }\`, NOT positional args.

<!-- lore:019e6f40-c79d-7844-be61-fbe22bd0424a -->
* **Worktree branch switching: commits can land on wrong branch after stash/pop operations**: Trap: when working in an OpenCode worktree, stash + branch switch + stash pop can cause subsequent commits to land on the wrong branch. Looks safe because \`git status\` shows the right branch, but the worktree may silently switch branches during stash operations. Symptom: \`git push\` fails with upstream branch name mismatch. Fix: always verify \`git branch --show-current\` immediately before committing. Recovery: \`git stash\`, switch to correct branch, \`git stash pop\`, resolve any conflicts. If branches have diverged significantly, create a fresh branch from main and re-apply changes via Python scripts rather than relying on stash.

### Pattern

<!-- lore:019e21e0-9f09-7dd1-a899-854579e160cc -->
* **Enhanced dedup: title overlap + vector similarity (Nomic v1.5)**: Nomic Embed v1.5 dedup threshold: same-domain cosine similarity spreads 0.46–0.70 (vs BGE Small which clusters at 0.93–0.97+, making dedup unusable). Correct dedup threshold: \*\*0.935\*\* — at-or-above is genuine duplicate. Range 0.85–0.91 contains 'related but distinct' entries; 0.85 produces false positives across project boundaries. \`deduplicate()\` in \`packages/core/src/ltm.ts\` uses both title word-overlap (0.7 Jaccard + 4+ shared words) AND vector cosine similarity. BGE Small embeddings are auto-nulled by \`checkConfigChange()\` on startup; \`backfillEmbeddings()\` re-embeds with Nomic v1.5. \`lore data reindex\` triggers backfill on-demand without gateway restart.

<!-- lore:019e56d6-9cca-7a77-aa1a-a5850daef395 -->
* **Gateway test patterns: unit vs integration vs subprocess isolation**: Gateway test patterns in \`packages/gateway/test/\`. Three patterns: (1) Unit: import functions directly, build fixtures inline — \`makeRequest()\`/\`makeUserMsg()\` factories in recall.test.ts and pipeline-tools.test.ts. (2) Integration: \`createHarness()\` starts real server on random port with replay interceptor, provides \`chat()\` + \`queryDB()\`. (3) Subprocess isolation: \`mock.module()\` calls isolated in subprocess files to avoid global cache pollution. DB isolation via root \`bunfig.toml\` preloading \`packages/core/test/setup.ts\`. \`GatewayRequest\` minimal required fields: \`protocol, model, system, messages, tools, stream, maxTokens, metadata, rawHeaders\`. \`GatewayMessage\` shapes: \`{ role:'user', content:\[{type:'text',text:'...'}] }\`, tool\_use \`{type:'tool\_use',id,name,input}\`, tool\_result \`{type:'tool\_result',toolUseId,content}\`, thinking \`{type:'thinking',thinking,signature}\`. Use port \`0\` in tests. Session ID regex requires \`\[a-f0-9]\` hex chars only — alphanumeric IDs with non-hex chars (e.g. \`g-z\`) will fail marker extraction.

### Preference

<!-- lore:019e6a07-0f56-77ea-957d-f0ec92c93bc4 -->
* **A Z**: Behavioral pattern detected across 4 sessions (action: a-z-). The user consistently demonstrates this behavior.

<!-- lore:019e5975-9590-74b8-a983-35fa3fa00617 -->
* **Always constrain features to minimal viable scope and flag over-engineering risks**: Design philosophy: minimal viable scope, gradual throttling, no over-engineering. (1) Always prefer the simplest implementation that solves the core problem — reject slow/complex alternatives (e.g., \`computeDailyCosts()\` too slow for hot-path), defer non-MVP concerns, avoid extra config surfaces. (2) For rate-limiting/throttling: reject hard blocks or large fixed delays ('too annoying'); prefer EMA-style velocity detection with small incremental delays the user barely notices. Use rate/velocity signals (EMA of cost-per-hour, inter-turn gaps), apply gentle progressive friction, avoid binary allow/block walls. When presenting throttle curves, favor smooth ramps with small minimum delays over step functions with large penalties.

<!-- lore:019e6a44-dc78-7a72-853f-9006db2a5ec9 -->
* **Always document codebase findings as structured observation logs with file paths, line numbers, and key facts**: When investigating a codebase, the user records findings in a structured format: file path with line range, brief description, and bullet-pointed key facts including type definitions, function signatures, constants, and behavioral rules. Observations prefixed with 🟡 (neutral finding) or 🔴 (user assertion/directive). Each bullet includes specific details: exact line numbers, field names, constant values, and algorithmic behavior. The user expects this same structured format when the assistant summarizes or documents code investigations. Always include file paths, line numbers, type shapes, and concrete values rather than vague descriptions. 🔴 directives from code comments (e.g., 'always update including clearing', 'never be evicted', 'first turn') are treated as ground truth.

<!-- lore:019e6914-fa92-764d-bdfa-883f37ec2d46 -->
* **Always feed inner/base accumulator unconditionally before any routing or translation logic**: When implementing stream processing functions (accumulators, translators), always feed the inner/base accumulator unconditionally as the FIRST action in the event handler, before any filtering, routing, suppression, or translation logic. Enforced via code comment in \`stream/anthropic.ts\` line 712: 'Always feed the inner accumulator (it tracks full state)'. The inner accumulator created via \`createStreamAccumulator()\` never scales — \`scaleClientUsage\` only affects client-facing output. Apply this pattern whenever wrapping or layering accumulators: call inner \`processEvent()\` first, then apply conditional logic (suppression, re-indexing, scaling, translation) afterward.

<!-- lore:019e5ac1-fea2-71ff-b900-b50443b120ca -->
* **Always include recoverMissingObjects() coverage for new tables in migration reviews**: When reviewing PRs that add new DB tables via migrations, always verify \`recoverMissingObjects()\` in \`db.ts\` covers every new table with \`CREATE TABLE IF NOT EXISTS\`. If omitted: partial migration leaves tables missing permanently — subsequent runs see the version already set and skip the migration. Flag as CRITICAL. Also: \`SCHEMA\_VERSION\` constant (stuck at 16) is dead code — \`migrate()\` uses \`MIGRATIONS.length\`. Always \`git pull --rebase\` before starting migration work to avoid version number collisions.

<!-- lore:019e6ae6-771d-723f-b16b-5325e7bc2e0d -->
* **Always investigate dead/unwired code paths before designing new features**: When exploring a codebase before implementing a feature, the user consistently traces whether existing functions are actually called/wired up, not just whether they exist. They flag functions like \`deleteSessionCosts\` or \`resetCalibration\` as 'exists but never called' and treat unwired plumbing as a relevant design constraint. When the assistant finds such dead code, the user expects it to be explicitly noted and its implications considered before proceeding with the design. Always check call sites and wiring, not just function existence.

<!-- lore:019e69c0-8a45-721c-9dd5-608caea2e300 -->
* **Always investigate pipeline.ts and related gateway modules through systematic deep-dive exploration**: When working in the opencode-lore gateway codebase, the user consistently performs thorough multi-file investigations of pipeline.ts alongside its translate/, stream/, and server modules. They read large files in full (often 500-4000+ lines), catalog imports and function signatures explicitly, identify architectural invariants (e.g., 'pipeline always returns Anthropic-format'), note potential bugs, and map data flow across module boundaries. Investigations are structured around specific functional areas (auth, routing, translation, streaming, cost tracking). The user expects the assistant to surface implementation details, cross-file dependencies, and gaps (e.g., missing tests) proactively during these explorations.

<!-- lore:019e69cc-74b7-7de6-a789-d8e6782dfbaa -->
* **Always investigate Sentry errors by first retrieving full issue details then doing deep codebase exploration**: When the user mentions Sentry error IDs (e.g., LOREAI-GATEWAY-1E), they expect a systematic two-phase investigation: (1) retrieve full Sentry issue details including stack traces, breadcrumbs, user counts, upstream URLs, and release info, then (2) explore the relevant source files in the codebase (typically in packages/gateway/src/) to identify root cause. The user wants thorough multi-file analysis covering the specific pipeline path implicated by the stack trace. Always map minified function names back to source, check for structural mismatches (e.g., missing fields in upstream responses), and identify the exact line/condition causing the error before proposing a fix.

<!-- lore:019e6a10-abdc-723b-9223-f3d53cb48b89 -->
* **Always investigate Sentry errors by reading full source files and tracing the exact crash path before proposing a fix**: When working on Sentry errors, the user consistently follows a pattern of: (1) listing and prioritizing open Sentry issues, (2) reading relevant source files to understand root cause before suggesting any fix, (3) tracing through stack frames to identify the exact code path, and (4) grouping related issues together when they share a common fix. The user expects thorough investigation (checking auth flows, error patterns, beforeSend filters, etc.) before any code changes are proposed. Do not jump to fixes — always read the relevant source files first and confirm the root cause hypothesis.

<!-- lore:019e69d6-87bd-764d-8b9a-8de594523eb0 -->
* **Always investigate Sentry errors by reading source files directly before concluding root cause**: When investigating production errors (Sentry issues), the user consistently reads the actual source files to verify whether the reported error reflects a real code bug or an artifact (bundler/minification, dead code, caller misuse, etc.). The user expects the assistant to: (1) locate the exact file and line number of the symbol in question, (2) check imports and exports, (3) distinguish between source-level correctness and compiled-output issues, and (4) report findings with file path, line number, and a clear root cause classification (real bug vs. artifact vs. caller error) before proposing any fix.

<!-- lore:019e5723-894e-7cbe-8dbb-e1e2da88a328 -->
* **Always investigate Sentry issues by exploring the full codebase context before proposing fixes**: Sentry investigation and triage rules: (1) Always explore full source files in \`packages/gateway/src/\` (not bundled dist), tracing call chains across auth/adapter/pipeline/idle/instrumentation before proposing fixes. (2) Enumerate ALL contributing root causes (numbered, with file:line) before proposing any fix. (3) Check user count and event count first; skip fixes for issues with 0 real users caused by caller-side misuse. (4) At session start, proactively fetch Sentry issues, triage by severity (user count, event count, regression, fatality), and present a prioritized action list. (5) \`log.error(..., errorObj)\` auto-forwards to Sentry via \`sink.captureException()\` — use typed error subclasses (e.g. \`LocalProviderUnavailableError\`) and \`beforeSend\` in \`instrument.ts\` to filter expected errors.

<!-- lore:019e69a2-99b3-7988-a00b-9ed02d93c164 -->
* **Always maintain detailed context about collaborator Onur Temizkan's setup and issues**: When working on the lore project, track Onur Temizkan's environment: two machines (ThinkPad 8GB headless Linux, ThinkStation 24GB RAM WSL/Ubuntu). Active Sentry issues: LOREAI-GATEWAY-1G/1H: \`OpenAI API 401: Incorrect API key provided: nokey\` — user has \`OPENAI\_API\_KEY=nokey\` in shell env; local ONNX/WASM embedding fails (8GB RAM) → \`pickRemoteFallback()\` picks it up → 401 at OpenAI embeddings API. LOREAI-GATEWAY-1B: \`ReferenceError: Cannot access 'u' before initialization\` in batch-queue \`prompt()\` — vLLM 404 on \`/v1/files\` triggers fallback path; TDZ root cause unresolved without production bundle. LOREAI-GATEWAY-Z escalated: users=21, events=10493 as of 2026-05-27. PR #475 merged (2026-05-27) fixing 1B, 1C, 1D, 1E, 1F. Pending: 1G/1H fix (beforeSend filter + key validation), 1A (getSystemErrorMap Bun compat). Issues Z, 12, Q, C, S, M, G fixed in main, not yet released.

<!-- lore:019e6f34-e024-7ab8-a739-183183011d0f -->
* **Always perform read-only architecture analysis before proposing implementation changes**: Before writing any code, the user consistently requests a thorough read-only investigation of the relevant codebase — tracing call paths, identifying key functions with exact file/line references, and documenting critical findings (including what's missing or broken). Only after this analysis is complete does the user proceed to implementation. The analysis should cover: entry points, data flow, auth/config propagation, existing patterns, and gaps. Findings should be structured with specific file paths and line numbers. The user explicitly flags when investigation should be read-only with no file edits.

<!-- lore:019e6a28-04e9-73e4-8fcd-d647b3585a1e -->
* **Always pre-specify multiple numbered investigation targets before codebase exploration**: When asking for codebase exploration, the user consistently provides a structured list of 4–7 numbered investigation targets upfront, each specifying: (1) the exact file path(s) to examine, (2) the specific symbols, patterns, or keywords to look for, and (3) the question to answer. The user does not ask open-ended 'explore this codebase' questions — they decompose the investigation into discrete, parallel-searchable subtasks before the assistant begins. Follow this pattern by treating each numbered item as an independent parallel search task and reporting findings per-item.

<!-- lore:019e6994-3f8a-74cc-9c47-9f878dce7c4c -->
* **Always prefer semantically correct protocol structures over workarounds when fixing LLM behavior issues**: When the user encounters LLM misbehavior (e.g., echoing internal data, leaking formatting during streaming), they consistently prefer fixing the root cause by using the correct protocol semantics (e.g., tool\_use/tool\_result pairs) rather than brittle workarounds like XML wrapping, prompt instructions, or cache tricks. Explicitly rejected Option B (XML wrapping + prompt instructions) as 'relies on LLM following instructions, doesn't fix the root cause.' When proposing or implementing fixes, always lead with the semantically correct approach and explain why it prevents the issue structurally. Do not suggest prompt-engineering workarounds as primary solutions.

<!-- lore:019e68c5-9ecd-759b-8ef6-3509c1d4a8db -->
* **Always prompt user to create a PR after pushing a branch**: After successfully pushing a branch to a remote, the assistant should prompt the user to create a pull request, typically by surfacing the PR creation URL. This pattern appears consistently across sessions: once a \`git push\` completes and the branch is tracking the remote, the assistant should proactively remind or prompt the user to open a PR (e.g., by sharing the GitHub PR creation link). Do not wait for the user to ask — surface the PR creation URL immediately after a successful push.

<!-- lore:019e6a56-42ce-771f-92f7-fd65330fcc95 -->
* **Always read types.ts early when exploring the gateway package**: When working in the gateway package, the user consistently reads \`packages/gateway/src/translate/types.ts\` early in the session — often as the first or second file — before diving into implementation files. This file defines the core internal representation types (GatewayRequest, GatewayResponse, GatewayMessage, GatewayContentBlock, SessionState, etc.). The user treats it as a required orientation step. When assisting with gateway-related tasks, proactively reference or summarize relevant types from this file rather than waiting to be asked, and assume the user wants to understand the type contracts before looking at implementation details.

<!-- lore:019e6a9b-6376-73b1-93ec-da6801cced43 -->
* **Always request a pre-merge self-review with adversarial/critical eye before merging PRs**: Before merging any PR in the BYK/loreai repository, the user consistently requests a thorough, critical ('adversarial') review of the PR using a subagent for objectivity. The review should read ALL changed files in full and check for: correctness, edge cases, type safety, test coverage, consistency, and accuracy of the PR description. The user expects the subagent to actively look for bugs, not just validate. Critical bugs must be clearly flagged before merge proceeds. Tests that bypass config parsing (e.g., setting fields directly on config objects) must be flagged as hiding real bugs. This pattern applies regardless of PR size or complexity — confirmed across 9+ sessions.

<!-- lore:019e6a48-faf4-7a81-8614-d4643aa9e3c9 -->
* **Always request comprehensive multi-file codebase audits with explicit named targets**: When investigating a codebase, the user consistently structures requests as multi-area audits with 4–7 explicitly named files or modules to examine in parallel. Each target has a specific question or data point to extract (e.g., schema columns, cache structures, config defaults). The user does not ask open-ended 'explore this codebase' questions — they pre-identify the exact files and the precise information needed from each. When responding, read all named files in parallel and report findings per-target in the same order the user listed them, with concrete details (line numbers, variable names, data structures) rather than summaries.

<!-- lore:019e6f10-406f-7127-a426-bf27d5e50c71 -->
* **Always request READ-ONLY investigation before designing or implementing new features**: When planning new features or fixes in the Lore codebase, the user consistently requests a dedicated read-only exploration phase first — explicitly prohibiting file edits — before any implementation begins. The investigation targets specific files and functions relevant to the feature area, organized into numbered lists of key areas to examine. The assistant should treat these as pure analysis tasks: read files, map architecture, identify call sites and data flows, and produce a design-ready summary. Never make edits during an investigation phase unless the user explicitly transitions to implementation.

<!-- lore:019e6efe-dbe4-7e1c-93af-3fed8f44c7f2 -->
* **Always run typecheck and tests before creating a PR**: After completing implementation tasks, the user consistently includes 'run typecheck + tests' as an explicit high-priority step before creating a PR. This appears as a named task in their session task lists. When typechecks or tests fail, the user expects the assistant to identify and fix the errors before proceeding to PR creation. Always treat typecheck and test verification as a mandatory gate between implementation and PR creation, not an optional step.

<!-- lore:019e6b49-cf1f-7e27-adad-0df77da8d9d3 -->
* **Always verify all CI checks pass before merging a PR**: Before merging any PR, the user expects confirmation that all required CI checks have passed. This includes explicitly listing each job's status (pass/fail/skipping) with timing details. Jobs that skip on PRs (Build Nightly Binaries, Generate Delta/Release Patches, Publish Nightly to GHCR) are expected and acceptable. Only after all non-skipping checks are confirmed green should a merge proceed. The user tracks CI runs by URL/run ID and monitors pending jobs until completion.

<!-- lore:019e6af2-d3ea-782d-8d34-3da50abec475 -->
* **Always verify implementation details through direct codebase investigation before asserting facts**: The user consistently uses tool-based codebase investigation (grep, file reads, line-by-line audits) to confirm exact implementation details — field names, line numbers, initialization values, mutation sites, restore logic, and intentional omissions — before making assertions. When the user does assert something (marked 🔴), it is always backed by prior tool investigation (🟡). The user expects the assistant to treat these verified assertions as ground truth and not second-guess them. When exploring unfamiliar territory, the user audits exhaustively: all mutation sites, all return paths, all restore/persist pairs, and cross-file references. The assistant should follow the same rigor: never assume behavior without checking, and always trace the full lifecycle (init → mutate → persist → restore) of any state field.

<!-- lore:019e6b39-54d2-7e42-8915-6985a7846458 -->
* **Always verify typecheck passes across all 4 packages after code changes**: After any code change, the user consistently runs \`bun --filter '\*' typecheck\` to confirm all 4 packages (@loreai/core, @loreai/gateway, @loreai/opencode, @loreai/pi) pass typecheck with exit code 0. This is a mandatory verification step before considering work complete. The assistant should proactively run this command after edits and report the result per-package. If any package fails, fix the errors immediately and re-run until all 4 are clean.

<!-- lore:019e69e6-8d3b-7a73-a84a-9cdb8b82ff78 -->
* **Never let dev shims interfere with production build artifacts**: The user consistently enforces a strict boundary between dev shims and production bundles. Dev shims (e.g., \`export \* from "../src/index.ts"\`) are only written to \`dist/\` when no real bundle exists there — the build script checks if the file starts with \`export \*\` before writing, and skips if a real artifact is present. The user explicitly comments this rule in code ('never interfere with production artifacts'). When diagnosing issues, the user investigates whether a dev shim is being resolved instead of the real bundle (e.g., via the \`bun\` export condition). Always ensure build scripts guard against overwriting real bundles with shims, and flag any situation where a dev shim may be served in production.

<!-- lore:019e2168-2fa4-77bd-a557-9d6dbcb40d81 -->
* **Prefer WASM backend over native onnxruntime-node for compiled binaries**: WASM backend for Bun \`--compile\` binaries with transformers.js: \`binaryExternalsPlugin\` in esbuild redirects \`onnxruntime-node\` → \`onnxruntime-web\` via \`onResolve\` (static imports only — does NOT redirect dynamic \`import()\` calls) and patches transformers.js CDN fallback via \`onLoad\` to read \`wasmPaths\` from \`globalThis.\_\_LORE\_VENDOR\_WASM\_PATHS\_\_\` (object form \`{ mjs, wasm }\` with exact hashed \`$bunfs\` filenames — directory strings fail). WASM files embedded as Bun \`{ type: 'file' }\` assets. For npm/CJS builds, \`onnxruntime-node\` stays external. WASM is ~2x faster on batches than native. Importing \`onnxruntime-web\` explicitly alongside the redirect creates two ort instances — 'cannot register backend cpu using priority 10' error.

<!-- lore:019e68cd-651a-7a1f-86b0-43d36249e915 -->
* **Prefers recall over assuming you don't have the information**: prefer recall over assuming you don't have the information.

<!-- lore:019e5b1a-0620-7016-b6e6-c5bdc392252e -->
* **Prefers update over create**: prefer update over create,

<!-- lore:019e6991-0ad9-7992-8e3f-d2a3df70feab -->
* **Respect explicitly rejected approaches**: Behavioral pattern detected across 6 sessions (action: rejected-approach). The user consistently demonstrates this behavior.

<!-- lore:019e5aa0-1e5f-7da9-a1f4-5b3b3f72b8e9 -->
* **Review code before committing**: Behavioral preferences: (1) Always perform adversarial code review before merging any PR — structured by severity tier (CRITICAL/MEDIUM/LOW), with file:line references and concrete fixes. Use subagent for objectivity. (2) Always investigate Sentry issues by reading full source (not dist), tracing call chains, enumerating all root causes before proposing fixes. Triage by user+event count; skip 0-user issues. (3) Prefer update over create for knowledge entries. (4) Prefer minimal viable scope — reject over-engineering, prefer gradual throttling over hard blocks, EMA-style velocity signals over step functions.
