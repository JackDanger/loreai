<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019eb931-7257-7ef6-87db-b2dd034c3ea7 -->
* **embedding-worker.ts: self-heal retry for corrupt model — skipped for vendored binaries**: embedding-worker.ts self-heal retry: \`initPromise\` wraps \`loadPipeline()\`; on catch, if \`!vendorModel && isCorruptModelError(msg)\`, calls \`purgeCachedModel()\` then retries once. NEVER post \`{type:'init-error'}\` before the retry — \`embedding.ts\` sets \`localProviderKnownBroken=true\` permanently on init-error. Only the final \`.catch\` may post \`init-error\`. Error classifiers are INLINED — Node ESM resolver cannot map \`./foo.js\` → \`./foo.ts\` for spawned workers. TRAP: \`/load model .\* failed/i\` is too broad — misclassifies 401/403 → purge→redownload→401 loop. \`embedding-worker.ts\` cannot be imported in tests (throws at module top-level if \`!parentPort\`). \`vendorModel=true\`: skip all purge/heal paths (model is read-only inside binary).

<!-- lore:019ebdd7-b5a4-7b44-840d-2bd981d368ff -->
* **gradient.ts: selectDistillations() always preserves meta (gen>=1) entries — never evicted by recency scoring**: distillation.ts: selectDistillations() always preserves meta (gen>=1) entries — never evicted by recency scoring. distLimit slots = limit - meta.length; gen-0 scored by recency (70%) and content signal (30%). Meta guarantee retains session history (fixes #417). Dead parameter metaThresholdOverride still plumbed through run() but never passed — removed after PR #751. distilledPrefixCached three-branch: (1) no new rows → cache hit, (2) new gen-0 → append delta (prepends headers, causes duplication in messages\[1]), (3) full re-render on meta rewrite. Cache validity: same sessionID, rowCount grew, last cached row unchanged. Layer 0 (warm-cache) does not inject prefix — only Layer 1+ (compression mode).

<!-- lore:019ebc36-830a-7307-8a10-9ff12dfa5740 -->
* **openai-responses.ts: Codex delta must live in a dedicated wrapper, not inline conditionals**: Chose \`buildOpenAICodexUpstreamRequest\` wrapper (and \`applyCodexResponsesDelta\` helper) over inline \`req.codex\` guards in the shared builder because Codex has 4+ divergences (URL \`/codex/responses\`, \`store:false\`, control fields passthrough, \`max\_output\_tokens\` suppression). The shared \`buildOpenAIResponsesUpstreamRequest\` must be Codex-agnostic; \`applyCodexResponsesDelta\` mutates the body post-build (deletes \`max\_output\_tokens\`, forces \`store:false\`, re-emits control fields, swaps URL). \`applyCodexResponsesDelta\` guards \`if (!extras) return\` AFTER setting \`store=false\` — so \`store:false\` always runs even when \`req.extras\` is absent. Pipeline dispatch site calls shared builder unchanged. \`buildWorkerRequest\` still accepts \`maxTokens\` param but codex branch skips forwarding it; openai/anthropic branches unaffected.

<!-- lore:019ebc96-83c7-7cd5-98d2-ca85724c2a85 -->
* **pipeline.ts: two curation paths — in-flight vs idle — must be gated separately**: pipeline.ts LTM caching: Two curation paths — in-flight (mid-turn, gated by sessionState.curationScheduled) and idle (on idle, flush gen-0 then metaDistill). In-flight avoids system\[2] cache busts during active conversations. System\[2] churn root causes: context-health note appended after diff-pinning fluctuates by layer (1→2→1), LTM entry selection changes per-turn. In-flight gen-0 distillation mutates messages\[1] in compression mode (gradient.ts:1553-1569). Warm-cache DB divergence: when pin reused, ltmCacheText=NULL while ltmPinText populated causes spurious re-pin; fix: persist pinned text. Volatile delta channel (issue #740, PR #747): keep system\[1]/system\[2] pinned (cache-warm), surface changed/new entries as ID-referenced delta in always-rewritten conversation tail after cache breakpoint. Format: \`- \[id] changed: \<content>\` / \`- \[id] new: \<content>\`. Zero cache cost; immediate effect on warm sessions; composes with #738 idle-only refresh.

### Gotcha

<!-- lore:019ebe4e-e788-76c6-818d-faad0018b8eb -->
* **cache-analytics.ts mapOffsetToJsonPath: system\[2] insertion yields path 'system\[1]' not 'system\[2]'**: cache-analytics.ts \`inferDivergenceReason(path, prevLength, currLength, messageCount?, turn?)\`: When \`system\[2]\` is inserted (turn 2), divergence offset lands at array boundary — path stays at \`system\[1]\` (bare, no \`.text\`). Turn-2 branch (line 282): returns 'stable LTM pinned — context-bound LTM (system\[2]) first injected on turn 2 (expected)'; fallback (line 284): 'stable LTM block diverged'. \`analyzeCacheTurn\` increments \`turnCount\` at line 345 (1-based); turn 1 never reaches \`inferDivergenceReason\` (guarded by \`lastRequestBody !== null\`). TRAP: \`grew = currLength - prevLength\` measures full body delta — at system\[1] divergence, ALL bytes after diverge (including messages\[]), so >200 byte threshold mislabels re-curations as insertions. Bare vs \`.text\` path suffix is the only reliable discriminator.

<!-- lore:019ebe56-3d14-7c78-89db-604729f75b58 -->
* **cache-analytics.ts: running vitest from package subdirectory finds no test files**: Trap: Running \`pnpm vitest test/cache-analytics.test.ts\` from \`packages/gateway/\` returns 'No test files found, exiting with code 1' (vitest v4.1.8). Looks right because the path is correct relative to the package. Fix: always run vitest from the repo root with the full relative path or use \`pnpm -r\` recursive exec. Discovered during PR #756 test verification.

<!-- lore:019ebc55-ba2e-7271-9906-905e4f5ed9bf -->
* **CCH seed extraction: seed may persist in binary but algorithm/preimage changed across versions**: CCH seed extraction: consult \`quality/CCH.md\`. Seed \`0x4d659218e32a3268\` (SEED\_2\_1\_138) unchanged in 2.1.175. Hash-input ≠ wire body — cch fn scans for \`cch=\` placeholder, splits/transforms before hashing. Binary is EXEC (not PIE), fixed base \`0x200000\`. CCH\_PLACEHOLDER = \`cch=00000\`; signBody replaces with hash; regex \`/cch=(\[0-9a-fA-F]{5});/\` (trailing semicolon required). Work files in stable dir (e.g. \`~/Code/cch-investigation/\`), not \`/tmp\`.

<!-- lore:019eb4d0-6fd9-7ef5-b22f-0f156eeca4b0 -->
* **cli/main.ts OPTIONS map: undeclared flags silently become booleans under strict:false**: Trap: \`parseArgs\` with \`strict: false\` treats any undeclared \`--flag value\` as \`{flag: true}\` with \`value\` falling into positionals — looks correct because \`--flag=value\` (equals syntax) still works. Fix: every flag used in ANY subcommand MUST be declared in the global \`OPTIONS\` map in \`packages/gateway/src/cli/main.ts\` with its correct type (\`string\` or \`boolean\`). Discovered in PR #696: \`--to\`, \`--project\`, \`--dry-run\`, \`--no-children\`, \`--min-confidence\` were missing, causing \`--to /path\` to parse as \`{to: true}\` with \`/path\` as a positional.

<!-- lore:019eb4d0-6fe3-7dc1-b80b-fad31fa29748 -->
* **distillations table schema: NOT NULL columns narrative, facts, source\_ids (not content)**: Trap: \`distillations\` table has no \`content\` column — looks like it should because other tables use \`content\`. Actual NOT NULL columns: \`narrative\`, \`facts\`, \`source\_ids\`, \`generation\`, \`token\_count\`, \`created\_at\`. Also: \`tool\_calls\` table uses \`tool\` (not \`tool\_name\`) and requires \`message\_id\`. Test helpers that insert into these tables must include all NOT NULL columns or get constraint errors. Discovered while writing \`packages/core/test/data-move.test.ts\`.

<!-- lore:019ebb4a-397c-76aa-89c8-93110713f3d4 -->
* **formatKnowledge: title-collision within a category produces non-deterministic render order**: Trap: \`formatKnowledge\` sorts entries within each category by title for byte-stable rendering. If two entries share an identical title, the sort returns 0 and their relative order is non-deterministic — text can swap between turns, busting the prompt cache. This is an edge case (duplicate titles are rare) but worth guarding. Fix: add a secondary sort key (e.g. entry id) as a tiebreak after title comparison, mirroring the id-tiebreak already used in the preference fast-path packing loop.

<!-- lore:019ebafd-32b7-7468-bdb2-5c77ff6c4ccf -->
* **opencode-lore plugin: legacy export path causes getLegacyPlugins() to be invoked**: Trap: lore's plugin module exports named functions (\`applyLoreProviderConfig\`, \`probeGateway\`, \`LorePlugin\`) plus \`export default LorePlugin\`. OpenCode's \`getLegacyPlugins()\` iterates \`Object.values(mod)\` and treats ANY function as a server plugin (\`isServerPlugin\` = \`typeof === 'function'\`). So \`applyLoreProviderConfig\` and \`probeGateway\` are invoked as plugins; \`applyLoreProviderConfig\` returns \`undefined\` → pushed into \`hooks\[]\` → all dispatch loops (\`config\`, \`event\`, \`dispose\`) crash: \`j.config\` / \`A.event\` / \`j.dispose\` on undefined. Fix: lore's plugin must \`default export { server: LorePlugin }\` (plain object with \`server\` key) so \`readV1Plugin\` takes the v1 path and \`getLegacyPlugins\` is never called. Additionally, do NOT export helper functions (\`applyLoreProviderConfig\`, \`probeGateway\`) from the plugin's top-level index — they leak into the legacy loader.

<!-- lore:019ebe55-c5cb-7bdb-a07f-ba12665d9f02 -->
* **Recurring bash failure: aborted**: The \`bash\` tool repeatedly failed with "aborted" across 8 sessions (9 total failures). Sample error: \[Tool execution was interrupted]. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebe55-c5f8-7922-959a-705bf2fde2e4 -->
* **Recurring edit failure: aborted**: The \`edit\` tool repeatedly failed with "aborted" across 3 sessions (4 total failures). Sample error: Tool execution aborted. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebe55-c5d2-779c-947d-92d5cd1a4c46 -->
* **Recurring edit failure: other:could-not-find-oldstring**: The \`edit\` tool repeatedly failed with "other:could-not-find-oldstring" across 5 sessions (7 total failures). Sample error: Could not find oldString in the file. It must match exactly, including whitespace, indentation, and line endings.. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebe55-c5c0-71f4-9da3-69590899285e -->
* **Recurring plan\_exit failure: other:the-user-dismissed-this**: The \`question\` tool repeatedly failed with "other:the-user-dismissed-this" across 4 sessions (5 total failures). Sample error: The user dismissed this question. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019eb94d-db25-72d2-ba71-f2b1ae570c51 -->
* **Recurring read failure: not\_found**: Recurring tool failures (transient; expect and retry): \`edit\` fails 'aborted' or 'could-not-find-oldstring' (re-read file before edit, match whitespace/indentation exactly); \`write\` fails 'aborted' or 'other:the-user-has-specified' (check permissions/rules); \`bash\`/\`task\` fails 'aborted' (interrupted — retry); \`webfetch\` returns 404 for raw GitHub URLs (check branch/path); \`read\` fails 'not\_found' (verify path with ls/find); \`question\`/\`plan\_exit\` fails 'user-dismissed-this' when dismissed. All are expected transient errors — retry or adjust approach. Bash aborts are the most frequent (8+ sessions).

<!-- lore:019ebe55-c5f4-7244-934c-5397566478e9 -->
* **Recurring task failure: aborted**: The \`task\` tool repeatedly failed with "aborted" across 3 sessions (4 total failures). Sample error: Tool execution aborted. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebe55-c5d9-7fd0-aa86-2d17a8510d6d -->
* **Recurring webfetch failure: other:statuscode-non-xx-status**: The \`webfetch\` tool repeatedly failed with "other:statuscode-non-xx-status" across 4 sessions (10 total failures). Sample error: StatusCode: non 2xx status code (404 GET https://raw.githubusercontent.com/oven-sh/bun/main/src/http/http\_client.zig). Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebe55-c5e7-7052-ab76-2b1f2ffe1750 -->
* **Recurring write failure: other:the-user-has-specified**: The \`write\` tool repeatedly failed with "other:the-user-has-specified" across 4 sessions (4 total failures). Sample error: The user has specified a rule which prevents you from using this specific tool call. Here are some of the relevant rules \[{"permission":"\*","action":"allow","pattern":"\*"},{"permission":"edit","patter. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019ebcab-947e-76d8-8dcd-294c8fd5b164 -->
* **sessionState.curationScheduled typed boolean | undefined — coerce with !! before passing to strict boolean param**: Trap: \`sessionState.curationScheduled\` is typed \`boolean | undefined\` in pipeline.ts session state. Helper functions like \`shouldRunInFlightCuration\` declare their params as \`boolean\` (not \`boolean | undefined\`), so passing the field directly causes TS2322. Looks safe because the value is only ever set to \`true\` explicitly. Fix: coerce with \`!!sessionState.curationScheduled\` at the call site. Applies to any \`sessionState\` field that starts undefined and is only assigned truthy values.

<!-- lore:019eb4d0-6fe8-7e3e-b9dc-46608c201399 -->
* **suggest.ts: RegExp flag deduplication required before appending 'g'**: Trap in \`packages/gateway/src/suggest.ts\`: constructing \`new RegExp(pattern.source, ${pattern.flags}g)\` produces \`'gg'\` if source pattern already has \`g\` flag, causing \`Invalid flags supplied to RegExp constructor\`. Fix: strip \`g\` from \`pattern.flags\` before appending: \`pattern.flags.replace(/g/g, '') + 'g'\`. Applies anywhere a RegExp is reconstructed with forced flags.

### Pattern

<!-- lore:019eb4d0-6fed-71ff-baca-7151d61bf315 -->
* **api.ts resolveProject() priority and parseBody zstd support**: \`packages/gateway/src/api.ts\`: \`resolveProject()\` priority: (1) \`:id\` route param, (2) \`?git\_remote=\`, (3) \`?path=\`. POST dispatch: literal routes first, then parameterized. TRAP: \`moveSessions()\`/\`reassignKnowledge()\` update \`knowledge SET project\_id=?\` but leave \`knowledge\_transfers\` rows pointing to old project. \`moveSessions()\` BFS-expands children before moving; \`sessions\_moved\` return overcounts if BFS includes children from other projects. \`rebindActiveSession\` iterates only user-provided IDs, not BFS-expanded children. \`parseBody\<T>()\` supports optional zstd decompression via \`Content-Encoding: zstd\`.

### Preference

<!-- lore:019ebc36-8316-7816-9936-29c3d1613cd0 -->
* **Always add regression tests when fixing protocol/builder bugs**: When fixing a protocol-level bug (e.g. Codex builder separation, field suppression like \`max\_output\_tokens\`, cache-analytics divergence reason), Burak Yigit Kaya explicitly requests regression tests alongside the fix. Pattern: (1) test suppressed field is absent even when client sends it, (2) test non-codex path still emits the field (positive case), (3) worker-side test asserts field absent. Also applies to \`applyCodexResponsesDelta\` refactor and \`inferDivergenceReason\` label changes (PR #756). Missing: no test that control fields (\`include\`, \`prompt\_cache\_key\`, etc.) are still emitted after refactor — regression could silently drop them.

<!-- lore:019ebe56-3ceb-710c-9238-6f4606c30728 -->
* **Always investigate and report cache analytics measurement artifacts as distinct from real bugs**: When analyzing cache analytics data, always distinguish genuine cache invalidation bugs from measurement/labeling artifacts. Report them separately: a mislabeled divergence reason is a diagnostic artifact, not a cache bust. Conflating the two leads to incorrect root-cause analysis and wasted investigation.

<!-- lore:019ebe56-7b16-7f9b-bca8-da47e9bebc8f -->
* **Always investigate read-only before proposing fixes**: When encountering a bug or unexpected behavior, the user consistently requests a thorough read-only investigation phase first — gathering exact file paths, function names, line numbers, and code snippets — before any modifications are made. The user explicitly states 'read-only' constraints and asks for specific details (e.g., 'find all occurrences of X', 'how does Y resolve Z'). Only after the full picture is established does the user move toward planning fixes. Never jump to proposing or implementing changes during the investigation phase; instead, surface all relevant code locations and trace the full call chain.

<!-- lore:019ebc68-e704-760e-b022-ed1aaf6457b2 -->
* **Always preserve raw bytes when handling HTTP request bodies**: Handle HTTP bodies as Buffer\[] with Buffer.concat, never string concatenation. Use latin1 encoding for text search in binary bodies. Copy bytes before mutation; guard offset writes. Pass as bytes not strings.

<!-- lore:019ebe55-e40a-75bb-83a7-e15c3ee5493e -->
* **Always track design decisions as GitHub issues with structured proposals**: When the user identifies a system limitation or architectural problem, they formalize it as a GitHub issue with a clear problem statement, prior work references, a concrete proposal (often with example formats), explicit benefits, and open design questions. They then verify whether subsequent PRs address the issue. This applies to cache optimization, LTM surfacing, curation gating, and similar architectural concerns. When implementing or discussing features, always reference the relevant issue number, acknowledge prior work, and address the listed design questions explicitly rather than proposing solutions in isolation.

<!-- lore:019ebe56-5ab6-702d-9ffe-998bc63b8d87 -->
* **Always track investigation state with colored bullet points (🔴/🟡) and structured sections**: The user consistently documents investigation sessions using emoji-prefixed bullet points: 🔴 for critical/blocking findings or active issues, 🟡 for resolved/informational findings. Sessions are organized into sections like 'Current State', 'Key Decisions', 'Technical Changes', and 'Investigation Findings'. Entries include timestamps in (HH:MM) format. Rejected approaches are explicitly marked with '🔴 \*\*\[rejected]\*\*'. This pattern applies to all technical investigation sessions involving reverse engineering, debugging, or multi-session research. When summarizing or continuing work, follow this same structured format with colored bullets and section headers.

<!-- lore:019ebe56-3ce4-777f-bb85-1e15c8533756 -->
* **Always track remaining work items explicitly after merging PRs**: After merging a PR, always explicitly track remaining work items — do not treat a merge as 'done'. List outstanding tasks, follow-ups, and known gaps. A merge closes one unit of work but does not close the broader task unless all items are verified complete.

<!-- lore:019ebe51-c489-7fcf-a6de-fea27f030d5c -->
* **Always treat 🔴-marked directives as non-negotiable invariants and explicitly acknowledge them**: 🔴-marked directives in .lore.md and session context are hard behavioral invariants, not documentation. Always explicitly verify and acknowledge them before proceeding. Proposals violating any 🔴-marked constraint are rejected. Core recurring directives: investigate before modifying, adversarial review before merging, CI monitoring, full test suite before closing, prefer proper solutions over hacks, document constraints with strong language. Treat each 🔴 directive as a standing rule unless explicitly revoked.

<!-- lore:019ebe56-3cd7-7530-aa15-80b82b7786e3 -->
* **Always use MIGRATIONS.length for schema version in UPDATE statements**: Always use \`MIGRATIONS.length\` for schema version instead of hardcoded constants in \`UPDATE schema\_version SET version = ...\` statements. Hardcoded version numbers become stale when migrations are added and cause schema version mismatches. Using \`MIGRATIONS.length\` ensures the version is always derived from the actual migration array length.

<!-- lore:019ebe24-4789-789c-bc6b-c6d448aeb325 -->
* **Always verify fixes by restarting the service and checking live logs**: After implementing a fix or merging changes, restart the service (e.g., \`systemctl restart opencode\`) and inspect live logs (e.g., \`journalctl -u opencode\`) to confirm the fix is working. Look for: zero worker failures, stable cache behavior, correct model interleaving, absence of prior error patterns. Note new PID, startup timestamp, and key post-restart metrics as confirmation checkpoints.

<!-- lore:019ebdc7-57dd-7842-9236-b9f9dc3a8736 -->
* **Always verify that detected/computed values are actually used in routing/forwarding logic, not just computed**: When reviewing or implementing code that detects a protocol, parses a body, or computes a routing decision, always verify the detected value is actually wired into the downstream routing/forwarding path — not just computed and discarded. Critical flaw pattern: \`detectProtocolFromBody()\` in fetch-interceptor.ts was computed but never transmitted to the gateway (which routes by URL path + \`X-Lore-Upstream-URL\` header only), making body detection dead code. When this gap is found, fix the wiring (make it functional) rather than remove the dead code. Always trace the full data flow: detection → header/parameter propagation → gateway endpoint selection.

<!-- lore:019ebde2-37a4-78b8-a749-edcd3c40d1da -->
* **Never add provider-specific worker defaults for providers with frequently changing model catalogs**: When discussing worker model defaults, the user explicitly rejects adding WORKER\_DEFAULTS entries for providers like 'opencode' whose model catalogs are large and frequently changing. The rationale is that a hardcoded default becomes stale quickly and doesn't make sense given the variety. Only add WORKER\_DEFAULTS for stable, well-known providers (e.g., anthropic, openai, openai-codex, github-copilot) where a reasonable cheap default model can be identified and is unlikely to disappear. For dynamic providers, leave the defaults map empty and handle fallback differently.

<!-- lore:019ebe56-3cf3-7441-9125-9e5fe3a5c8bb -->
* **Prefers AGENTS.md over CLAUDE.md for agent instructions**: Burak Yigit Kaya prefers using AGENTS.md (not CLAUDE.md) as the agent instructions file in the repository. When creating or referencing agent instruction files, use AGENTS.md.

<!-- lore:019ebcb3-5d82-7591-91dc-c9e587602f24 -->
* **Stop chasing live-log rabbit holes — focus on planned work**: When redirected away from an investigation (e.g. 'stop chasing X; focus on the planned work'), drop it immediately and execute planned tasks in order. Do not theorize about live session data (specific session IDs, cch= content) without first verifying. Directive: verify before theorizing, return to task list when redirected.

<!-- lore:019ebe56-3cfa-7b49-b786-3d603e608a37 -->
* **Test file conventions: Vitest, kebab-case, import style**: Always write new tests with \`import { describe, it, expect } from 'vitest'\`. Use kebab-case file naming for test files. Prefer writing more tests over lowering coverage thresholds or excluding files. Project migrated from Mocha+Chai+ts-node to Vitest (May 2026; 312ms vs 30s startup). Run tests from repo root, not package subdirectory — \`vitest\` with a filter from a subdirectory returns 'No test files found'.
