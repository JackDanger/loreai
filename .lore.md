<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019eb946-848c-7604-ab72-37427ab6e9ab -->
* **cch-seed-check.yml: runner, steps, and extract job structure**: \`cch-seed-check.yml\`: all jobs use \`ubuntu-latest\`. \`extract-seed\` job steps: checkout → pnpm setup → node 24 → \`pnpm install --frozen-lockfile\` → extract (\`node scripts/extract-cch-seed.ts --version $VERSION --output $OUTPUT\` then \`--apply\`) → test → PR creation. No compiler install step — GCC available by default on ubuntu-latest. \`scripts/cch-scan.c\` is committed to repo (not gitignored). Native scan is essential for CI — JS fallback would time out (30min+). \`notify-failure\` job dedupes GitHub issues by version marker to avoid 6-hourly spam. CRITICAL: A single oracle pair yields ~11 false positives (e.g. \`0x2cc8000000000000\`); ALWAYS capture 2+ pairs and scan for a seed matching BOTH to eliminate false positives. Seed table entries may be stale/wrong — never trust the table; always verify from scratch with 2 real captured pairs. Algorithm confirmed: \`xxHash64(body, seed) & 0xFFFFF\`.

<!-- lore:019eb931-7257-7ef6-87db-b2dd034c3ea7 -->
* **embedding-worker.ts: self-heal retry for corrupt model — skipped for vendored binaries**: Self-heal logic (lines 156–196): \`initPromise\` wraps \`loadPipeline()\`; on catch, if \`!vendorModel && isCorruptModelError(msg)\`, calls \`purgeCachedModel()\` then retries once. NEVER post \`{type:'init-error'}\` before the retry — \`embedding.ts\` init-error handler sets \`localProviderKnownBroken=true\` permanently. Only the final \`.catch\` may post \`init-error\`. Error classifiers (\`isOomError\`, \`isWasmFatalError\`, \`isCorruptModelError\`) are INLINED — Node ESM resolver cannot map \`./foo.js\` → \`./foo.ts\` for spawned workers. TRAP: \`/load model .\* failed/i\` is too broad — misclassifies 401/403 → purge→redownload→401 loop. \`embedding-worker.ts\` cannot be imported in tests (throws at module top-level if \`!parentPort\`) — extract pure functions to unit test.

<!-- lore:019e7013-a9ea-72e6-8e8c-17ef1848e4e0 -->
* **llm-adapter.ts: retry logic, backoff schedule, and circuit breaker integration**: llm-adapter.ts retry/backoff: \`TRANSIENT\_CODES={429,500,502,503,529}\`; \`AUTH\_ERROR\_CODES={401,403}\`. \`MAX\_RETRIES\_RATE\_LIMIT=3\`; \`MAX\_RETRIES\_SERVER=3\`; \`MAX\_RETRIES\_URGENT=2\`. \`backoffMs()\`: Retry-After present → \`min(retryAfterMs, cap)\`; urgent → \`min(1000×2^attempt, 4000)\`; 429 non-urgent → \`min(60\_000+attempt×60\_000, 180\_000)\`; 5xx → \`min(1000×2^attempt, 8000)\`. On 429 non-urgent → \`tripCircuitBreaker(pauseSec)\`. Auth errors → \`markAuthStale\`, re-resolve, retry once if \`credentialChanged && attempt===0\`. \`buildAnthropicWorkerRequest()\`: injects billing block only when \`cred.scheme === 'bearer'\`; system sent as block array with \`cache\_control: { type: 'ephemeral', ttl: '1h' }\`; body signed via \`signBody()\`. \`AbortError\` downgraded to \`log.info\`.

<!-- lore:019e54f5-04be-7a9f-9e62-177d65d5db9f -->
* **LTM forSession() budget, phases, and safety-net constants**: LTM forSession() architecture: \`forSession()\` (ltm.ts) budgets: \`budget.ltm\`=0.05, \`budget.preferenceLtm\`=0.02. Phases: (1) architecture entries up to ARCH\_BUDGET\_FRACTION=0.2; (2) remaining by score desc; (3) .lore.md sections. HEADER\_OVERHEAD\_TOKENS=15 subtracted upfront. Hard cap: \`if (used+cost>maxTokens) continue\`. PROJECT\_SAFETY\_NET=5: top-confidence project entries ALWAYS included even with zero relevance score. NO\_CONTEXT\_FALLBACK\_CAP=10. Global entries (pid=null) must be cross\_project=1. Pool SQL: project pool uses \`cross\_project=0\`; cross pool uses \`project\_id IS NULL OR cross\_project=1\`. Promoted entries (cross\_project=1) lose PROJECT\_SAFETY\_NET guarantee in home project. GOTCHA: \`vectorSearch()\` queries \`knowledge WHERE embedding IS NOT NULL AND confidence>0.2\` — test suites must \`DELETE FROM knowledge WHERE embedding IS NOT NULL\` in beforeEach. Dedup threshold for Nomic v1.5 is 0.935.

### Gotcha

<!-- lore:019eb4d0-6fd9-7ef5-b22f-0f156eeca4b0 -->
* **cli/main.ts OPTIONS map: undeclared flags silently become booleans under strict:false**: Trap: \`parseArgs\` with \`strict: false\` treats any undeclared \`--flag value\` as \`{flag: true}\` with \`value\` falling into positionals — looks correct because \`--flag=value\` (equals syntax) still works. Fix: every flag used in ANY subcommand MUST be declared in the global \`OPTIONS\` map in \`packages/gateway/src/cli/main.ts\` with its correct type (\`string\` or \`boolean\`). Discovered in PR #696: \`--to\`, \`--project\`, \`--dry-run\`, \`--no-children\`, \`--min-confidence\` were missing, causing \`--to /path\` to parse as \`{to: true}\` with \`/path\` as a positional.

<!-- lore:019e6a44-dbf1-7df6-ba60-94f75c21b81d -->
* **consolidationUser() batchTarget frame-of-reference mismatch: global target vs batch count produces negative excess**: Trap: \`batchTarget\` in batched consolidation was computed globally (e.g. 1118 for 1143 entries) but \`consolidationUser()\` computed \`excess = batchCount - batchTarget\` → negative excess → LLM returns empty ops → idle scheduler enters 1h cooldown, system never converges. Fix: \`batchTarget\` must be batch-relative — \`Math.ceil(CONSOLIDATION\_BATCH\_SIZE / 2)\` so \`excess = 50 - 25 = 25\`. Fixed in e8a73f9. CONSOLIDATION\_BATCH\_SIZE=50. Non-batched path correctly uses \`cfg.curator.maxEntries\`. \`consolidate()\` NEVER creates entries — only 'update' and 'delete' ops; \`includeCross=false\` excludes cross-project entries.

<!-- lore:019e6a1b-3408-7609-ba6b-d4ba9d552afd -->
* **curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields**: curator.ts applyOps(): create op missing content field crashes at line 226 — filterOps() doesn't validate required fields: Trap: \`filterOps()\` (curator.ts:109–117) only checks \`op\` is a string — does NOT validate required fields (\`content\`, \`title\`, \`category\`) for \`create\` ops, casting result as \`CuratorOp\[]\` silencing TypeScript. When LLM emits a malformed \`create\` op missing \`content\`, \`applyOps()\` line 226 accesses \`op.content.length\` without null guard → \`TypeError: undefined is not an object\`. Fix: add null guard at line 226 AND field validation in \`filterOps()\` for \`create\` ops. The \`update\` branch at line 252 already has \`op.content !== undefined &&\` guard — \`create\` branch lacks equivalent. \`applyOps()\` calls \`ltm.syncRefs(id)\` and \`entities.syncEntityRefs(id, content)\` after each create/update; \`skipCreate\` flag skips creates (used by consolidation); cross-project guard prevents mutating foreign-project entries. \`cfg\` is in scope in \`runInner\` at line 414.

<!-- lore:019eb4d0-6fe3-7dc1-b80b-fad31fa29748 -->
* **distillations table schema: NOT NULL columns narrative, facts, source\_ids (not content)**: Trap: \`distillations\` table has no \`content\` column — looks like it should because other tables use \`content\`. Actual NOT NULL columns: \`narrative\`, \`facts\`, \`source\_ids\`, \`generation\`, \`token\_count\`, \`created\_at\`. Also: \`tool\_calls\` table uses \`tool\` (not \`tool\_name\`) and requires \`message\_id\`. Test helpers that insert into these tables must include all NOT NULL columns or get constraint errors. Discovered while writing \`packages/core/test/data-move.test.ts\`.

<!-- lore:019e69f0-a05a-7d48-8e35-76baf13533c5 -->
* **LOREAI-GATEWAY-1E: resp.usage undefined crash in postResponse() for vLLM/OpenAI path**: Trap: \`GatewayUsage\` typed as non-optional on \`GatewayResponse\`, so \`resp.usage.inputTokens\` looks safe. But \`resp.usage\` can be \`undefined\` at runtime (e.g., vLLM returning partial response). All four accumulators construct \`usage\` with \`?? 0\` guards on individual fields — but \`resp.usage\` itself can still be undefined if an exception produces a partial response object. Crash sites: pipeline.ts lines 2069, 2101, 2137-2140, 2150-2153, 2204-2206, 2282-2285, 2341, 2363, 2402; also \`buildOpenAINonStreamResponse\` (openai.ts:296-300). Fix: add \`const usage = resp.usage ?? { inputTokens: 0, outputTokens: 0, cacheReadInputTokens: 0, cacheCreationInputTokens: 0 }\` guard at top of \`postResponse()\` and \`nonStreamHttpResponse()\`, then use \`usage.\*\` throughout.

<!-- lore:019e6950-8200-7473-87a5-45a861bb84af -->
* **recall.ts: unescaped quotes/ellipsis in marker strings cause store miss and raw marker leak**: Trap: \`buildRecallMarker()\` embeds raw \`query\` between double-quotes and raw \`id\` before \`…\` — looks safe because queries are usually plain text. But if \`query\` contains \`"\`, \`MARKER\_REGEX\` (\`/📚 Searching (.+?) for "(.+?)"…/\`) truncates the captured group at the first inner quote; \`recallStoreKey()\` produces a mismatched key; \`store.get(key)\` returns undefined; the marker is left as raw text in the conversation visible to the upstream LLM. Same issue for \`id\` containing \`…\`. Fix: escape \`"\` → \`\\"\` in \`buildRecallMarker()\` and unescape in \`parseRecallMarker()\`; for IDs, percent-encode or strip \`…\` before embedding.

<!-- lore:019eb952-70a9-7400-9ff8-f48948b38770 -->
* **Recurring bash failure: aborted**: The \`bash\` tool repeatedly failed with "aborted" across 5 sessions (5 total failures). Sample error: \[Tool execution was interrupted]. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019eb952-70ae-792f-bde3-a66e9ca7f5c5 -->
* **Recurring edit failure: aborted**: The \`edit\` tool repeatedly failed with "aborted" across 3 sessions (4 total failures). Sample error: Tool execution aborted. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019eb952-7094-7569-a1d9-24465f8c8ca8 -->
* **Recurring edit failure: other:could-not-find-oldstring**: The \`edit\` tool repeatedly failed with "other:could-not-find-oldstring" across 7 sessions (9 total failures). Sample error: Could not find oldString in the file. It must match exactly, including whitespace, indentation, and line endings.. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019eb952-7086-796a-995b-83e1a959359e -->
* **Recurring plan\_exit failure: other:the-user-dismissed-this**: The \`question\` tool repeatedly failed with "other:the-user-dismissed-this" across 5 sessions (6 total failures). Sample error: The user dismissed this question. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019eb94d-db25-72d2-ba71-f2b1ae570c51 -->
* **Recurring read failure: not\_found**: Recurring tool failures observed across sessions: \`read\` fails with \`not\_found\` when paths are stale or wrong (verify with \`ls\`/\`find\` first); \`edit\` fails with \`could-not-find-oldstring\` when oldString doesn't match exactly (whitespace/indentation must be exact — read file fresh before editing); \`webfetch\` returns 404 for raw GitHub URLs (URLs may be wrong branch/path); \`write\`/\`bash\`/\`edit\` fail with 'aborted' or user-rule blocks. Mitigations: always verify paths before reading, read file immediately before editing, use \`find\`/\`grep\` to locate correct paths.

<!-- lore:019eb952-708e-7ce4-b2e8-cb94573c5cfd -->
* **Recurring webfetch failure: other:statuscode-non-xx-status**: The \`webfetch\` tool repeatedly failed with "other:statuscode-non-xx-status" across 7 sessions (23 total failures). Sample error: StatusCode: non 2xx status code (404 GET https://raw.githubusercontent.com/oven-sh/bun/main/src/http/http\_client.zig). Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019eb952-70a4-7282-8431-54d6f6b1660b -->
* **Recurring write failure: aborted**: The \`write\` tool repeatedly failed with "aborted" across 5 sessions (5 total failures). Sample error: Tool execution aborted. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019eb952-709f-774b-8817-f01d0a1e5312 -->
* **Recurring write failure: other:the-user-has-specified**: The \`write\` tool repeatedly failed with "other:the-user-has-specified" across 5 sessions (5 total failures). Sample error: The user has specified a rule which prevents you from using this specific tool call. Here are some of the relevant rules \[{"permission":"\*","action":"allow","pattern":"\*"},{"permission":"edit","patter. Investigate the root cause — this is a recurring obstacle in this project.

<!-- lore:019e2e20-95b3-7a9d-ab38-77d87eafecc4 -->
* **splitSegments() infinite recursion on oversized single messages**: \`splitSegments()\` infinite recursion on oversized single messages in \`packages/core/src/distillation.ts\`: recurses infinitely when a single message exceeds \`maxSegmentTokens\` (16384). \`findSplitIndex()\` returns \`messages.length\` (=1), so \`left = messages.slice(0, 1)\` produces an identical recursive call. Triggered on large tool outputs (~49KB+). Fix: add base case after the \`totalTokens <= maxTokens\` guard — \`if (messages.length <= 1) return \[messages]\`. The oversized message becomes an indivisible segment.

<!-- lore:019e49d5-15c4-78d1-8bbc-f43fde34d9f1 -->
* **startServer() returns plain object, not Promise — use startGateway() for programmatic async use**: Gateway startup traps: (1) \`startServer()\` returns \`{ stop, port, hosts }\` synchronously — use \`startGateway()\` from \`cli/start.ts\` for \`Promise\<GatewayHandle>\`. (2) \`loadConfig()\` in \`config.ts\` is SYNC — calling \`.then()\` throws TypeError. (3) EADDRINUSE fires as async \`'error'\` event — wrap \`server.listen()\` in a Promise. (4) Use port \`0\` (ephemeral) in tests. (5) In-process callers MUST pass \`local:true\` — hosted mode disables FS ops in \`@loreai/core\` (breaks \`getGitRemote()\`, \`.lore.md\` import, config loading). (6) \`lore run\` and \`lore import\` always set \`opts.local=true\`.

<!-- lore:019eb4d0-6fe8-7e3e-b9dc-46608c201399 -->
* **suggest.ts: RegExp flag deduplication required before appending 'g'**: Trap in \`packages/gateway/src/suggest.ts\`: constructing \`new RegExp(pattern.source, ${pattern.flags}g)\` produces \`'gg'\` if source pattern already has \`g\` flag, causing \`Invalid flags supplied to RegExp constructor\`. Fix: strip \`g\` from \`pattern.flags\` before appending: \`pattern.flags.replace(/g/g, '') + 'g'\`. Applies anywhere a RegExp is reconstructed with forced flags.

### Pattern

<!-- lore:019eb4d0-6fed-71ff-baca-7151d61bf315 -->
* **api.ts resolveProject() priority and parseBody zstd support**: \`packages/gateway/src/api.ts\`: \`resolveProject()\` priority: (1) \`:id\` route param, (2) \`?git\_remote=\`, (3) \`?path=\`. POST dispatch: literal routes first, then parameterized. TRAP: \`moveSessions()\`/\`reassignKnowledge()\` update \`knowledge SET project\_id=?\` but leave \`knowledge\_transfers\` rows pointing to old project. \`moveSessions()\` BFS-expands children before moving; \`sessions\_moved\` return overcounts if BFS includes children from other projects. \`rebindActiveSession\` iterates only user-provided IDs, not BFS-expanded children. \`parseBody\<T>()\` supports optional zstd decompression via \`Content-Encoding: zstd\`.

### Preference

<!-- lore:019eb936-7c9f-7f60-83a0-05442d818862 -->
* **Always enforce lowercase hex literals and symbolic named constants for magic values**: When working with hex literals or repeated magic values (seeds, constants), the user consistently requires: (1) lowercase hex throughout (e.g., \`0x4d659218e32a3268n\`, never uppercase), matching Biome/existing style; (2) repeated hex values extracted into named constants so no literal appears more than once; (3) generated/applied code must reuse existing named constants when the value matches, and only introduce a new named constant (never a bare literal) when the value is new. This applies to both hand-written and script-generated code, including C source files. Violations cause lint/style failures and the user will request immediate correction. CCH oracle \`cch\` values are always lowercase 5-hex by capture+validation — C code must produce lowercase (\`%05llx\`) to match.

<!-- lore:019e9f06-597e-75c0-8f6a-2d2b4c144bc8 -->
* **Always remove all Bun-specific code and build targets**: Always remove all Bun-specific code: delete dist/bun directories, replace Bun APIs (Bun.mmap, Bun.file, Bun.zstdDecompressSync, DecompressionStream('zstd'), @sentry/bun) with Node.js equivalents, remove Bun-specific mocks/test assertions/build targets. Enforce Node.js >= 22.15 + pnpm + Vitest stack. Use \`node:worker\_threads\`, \`node:fs\`, \`node:zlib\`, \`node:https\`, \`vitest run\`, \`npx tsx\`. Bun bugs (#18145, #25677, #31158) cause model-init OOM in WASM paths — numThreads=1 avoids pthread paths. Load workers via \`new URL('./worker.ts', import.meta.url)\`; use \`Float32Array\` structured clone; \`worker.unref()\` for clean exit. Never add new Bun-specific code.

<!-- lore:019eb19e-49a3-7756-9068-eaed09c05063 -->
* **Always request harsh multi-pass code review covering logic flaws, edge cases, and invariant violations**: Code review and commit discipline: (1) Harsh multi-pass self-review — logic flaws, edge cases, invariant violations, exact file:line refs. No edits during review. (2) Fix all failing tests; run full typecheck and vitest suite before committing. (3) Use Biome (\`pnpm biome check --write\`); never ESLint/Prettier. (4) CLI flags used in subcommands MUST be declared in global OPTIONS map in main.ts. (5) Always push branch and create PR; watch CI. (6) Exclude \`.lore.md\` from PR commits. (7) All packages must be devDependencies. (8) Real bugs found in tests: fix in same PR with \`fix(scope):\`. (9) Never modify \`src/recorder.ts\` for test-only SSE needs. (10) Reconnaissance before any code. (11) Run \`pnpm run generate:env-docs\` after touching env-var sites. (12) Before merging, request adversarial subagent review categorized by severity (🔴/🟡/🟢).

<!-- lore:019e9f41-df33-7e3c-a1a5-843a62fcbd64 -->
* **Always use the same provider and never cross-provider**: Same-provider invariant: worker/warmup model ALWAYS uses same provider as session. \`getWorkerModel()\`: effectiveProvider = cfg.model?.providerID ?? sessionProviderID ?? 'anthropic'; fallback chain: (1) cfg.model, (2) sessionModelID only if hasKnownDefaults, (3) WORKER\_DEFAULTS\[effectiveProvider], (4) undefined. Unknown providers → returns undefined. Cross-provider guard in pipeline.ts: detects model/upstream provider mismatch at prompt time, calls \`recordWorkerFailure('cross-provider')\` + returns null. Wire protocol resolved from \`resolveProviderRoute(providerID)?.protocol\`, NOT hardcoded. Workers must NEVER fall back to \`config().model\` — \`const model = input.model\` with NO fallback is correct. Any \`?? config().model\` in worker code is a critical bug.

<!-- lore:019e9d98-e22f-7f0d-87d3-91c4f8b9a5e3 -->
* **OpenCode plugin: per-project state map for project path header injection**: OpenCode plugin per-project state: \`projectState\` Map keyed by \`ctx.project.id\` → \`{ projectPath, gitRemote, lastSeenAt }\`; TTL=24h; \`reapStaleProjectState()\` called on every new entry. \`chat.headers\` hook uses per-project Map entry — correct, no cross-project clobber. BUT module-level globals \`currentProjectPath\`/\`currentGitRemote\` (lines 304-305) are 'most-recently-active wins' and used by fetch interceptor \`getHeaders()\` for requests bypassing \`chat.headers\`. Root cause of git-remote magnet bug: stale remote from prior project leaks into next session's headers. Fix: emit project+remote as an atomic pair in headers; never send remote without its paired path. \`loreInitPromise\` memoization prevents concurrent plugin calls racing on probe→spawn. Gateway discovery order: (0) \`LORE\_REMOTE\_URL\`; (1) \`LORE\_GATEWAY\_URL\`; (2) port file; (3) in-process \`startGateway\`. \`remoteGateway\` auto-detection 4-layer precedence in \`start.ts\`.

<!-- lore:019eb885-9bfe-7c6a-987c-6edda2862119 -->
* **Prefers recall over assuming you don't have the information**: Always prefer recall over assuming you don't have the information. Check memory BEFORE exploring the filesystem. Applies to: people, services, tools, projects, file paths, past decisions, prior approaches. Key paths: opencode binary source at /home/byk/Code/opencode/packages/opencode/src/ (lore's monorepo depends only on @opencode-ai/plugin SDK). @ai-sdk packages live at /home/byk/Code/opencode/packages/opencode/node\_modules/@ai-sdk/. When investigating, enumerate 4–7 specific targets, read actual files, report findings with exact file:line references. Grep/ripgrep sometimes returns empty despite files existing — fall back to direct file reads. Always verify paths with \`ls\` or \`find\` before reading. Before implementing anything, produce exact file:line references, code snippets, and structural understanding. Only after investigation does the user proceed to design or implementation.

<!-- lore:019eb931-7269-752f-8f44-6509316fe725 -->
* **purgeCachedModel and self-heal retry path never called for vendored binaries**: purgeCachedModel() and self-heal retry MUST be skipped when \`vendorModel=true\` — model ships inside binary, path is read-only. Guard every heal/purge path with \`if (!vendorModel)\`. Truncated HF Hub downloads cause onnxruntime 'Protobuf parsing failed' at init (not a download error); fix is delete cached model dir and re-download. Self-heal automates this for non-vendored models. For vendored binaries, manual intervention required. See also: embedding-worker.ts self-heal architecture entry.
