<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019eaee5-6de2-7abb-ac6a-4bcd02ad862d -->
* **craft NpmTarget workspace expansion and OIDC auth strategy**: craft NpmTarget workspace expansion, OIDC auth, and publish flow: \`NpmTarget.expand()\` discovers workspaces via pnpm-workspace.yaml or package.json, topologically sorts (deps before dependents), validates public packages don't depend on private ones. Auth priority: \`oidc=true\` config → force OIDC (does NOT check \`isOidcEnvironment()\` — env vars only checked in auto-detect path); \`NPM\_TOKEN\` set → token auth; no token + OIDC env detected → auto OIDC; else error. OIDC env: \`ACTIONS\_ID\_TOKEN\_REQUEST\_URL\` + \`ACTIONS\_ID\_TOKEN\_REQUEST\_TOKEN\` (GitHub Actions) OR \`NPM\_ID\_TOKEN\` (GitLab). \`publishPackage()\`: path arg ALWAYS pushed last. \`getPublishTag()\`: pre-release → 'next'; new version < current latest → 'old'; else undefined. \`publishMain()\` sequence: artifact download → workspace expand → state resume → publish loop (state written after each target) → merge release branch (non-fatal) → post-release command. State file: \`getPublishStatePath(version)\` uses \`XDG\_STATE\_HOME\`. Legacy cwd state file no longer read (PR #797).

<!-- lore:019ea9c2-23e7-78a3-b559-416743674bc7 -->
* **Docs-site structure: Zod .describe() + generator + check-docs CI job**: Docs-site auto-generation: two pages in \`packages/website/src/content/docs/docs/\`. \`generate-config-docs.ts\` walks Zod \`.shape\`+\`.description\`+\`unwrap()\` directly (NOT zod-to-json-schema — fails on ZodEffects). \`generate-env-docs.ts\` walks gateway/core source for \`process.env.LORE\_\*\`/\`env.LORE\_\*\` refs, extracts JSDoc, parses defaults, groups by subsystem. Both wired via \`pnpm generate:docs\` + \`pnpm check:docs\` (--check exits 1 on drift). CI \`check-docs\` job gates on both. GOTCHA: backticks inside template literals MUST be \`\\\`\` — unescaped backticks cause esbuild parse errors. Zod 4 internals: \`s.description\` is top-level (NOT \`s.\_def.description\`). Default wrapper: \`\_def.type === 'default'\`, \`\_def.defaultValue\` is the value itself. Numeric checks: \`check.\_zod.def.check\`, \`.value\`, \`.inclusive\`. Object: \`\_def.shape\` is direct. Array: \`{ type: 'array', element }\`. Record: \`{ type: 'record', keyType, valueType }\`.

<!-- lore:019e7013-a9ea-72e6-8e8c-17ef1848e4e0 -->
* **llm-adapter.ts: retry logic, backoff schedule, and circuit breaker integration**: \`llm-adapter.ts\` retry/backoff: \`TRANSIENT\_CODES={429,500,502,503,529}\`; \`AUTH\_ERROR\_CODES={401,403}\`. \`DEFAULT\_MAX\_RETRIES=8\` (overridable via \`LORE\_MAX\_RETRIES\`; non-numeric/negative/zero falls back to default). \`backoffMs()\`: Retry-After present → \`min(retryAfterMs, 32000)\`; exponential fallback \`min(500×2^attempt, 32000)\` + 0–25% jitter. On 429 → \`tripCircuitBreaker(pauseSec)\` at most once per call. Auth errors (401/403): calls \`recordWorkerFailure('auth-rejected')\` first, then \`markAuthStale(sessionID, providerID)\` (only THIS provider), re-resolves, retries once if \`credentialChanged && attempt===0\`. A permanently invalid bearer token (always 401) NEVER trips the global circuit breaker — global breaker only trips on 429. \`tripCircuitBreaker()\` only extends open duration, never shortens it. \`recordWorkerSuccess()\` intentionally NOT called at transport layer — parse failures would be invisible.

<!-- lore:019e5715-cccd-7be2-a979-407066c526d4 -->
* **log.ts: sink bridge, file rotation, and error visibility rules**: \`packages/core/src/log.ts\`: (1) \`log.error()\` always calls \`console.error()\` — always visible, indicates real failures. \`log.info()\`/\`log.warn()\` only print if \`LORE\_DEBUG=1\`. (2) \`sink?.captureException(err)\` called when \`Error\` instance found via \`findError()\` — gateway bridges to Sentry via \`registerSink()\`. (3) \`formatArgs()\`: strings pass through, \`Error\` instances use \`.message\`, others use \`String(a)\`. (4) File logging: all levels written to \`~/.local/share/lore/lore.log\` regardless of \`LORE\_DEBUG\`; rotated at 5 MB (\`LOG\_MAX\_BYTES\`), check every 1000 writes (\`ROTATION\_CHECK\_INTERVAL\`), single \`.log.1\` backup; disabled in \`NODE\_ENV=test\`. (5) Only one \`LogSink\` supported. TRAP: any \`log.error(..., errorObj)\` auto-forwards to Sentry — including expected shutdown/auth errors. Use typed error subclasses or \`TRANSIENT\_ERROR\_PATTERNS\` filter in instrument.ts \`beforeSend\`. \`log.warn()\` auth errors from batch-queue are never forwarded to Sentry.

<!-- lore:019e54f5-04be-7a9f-9e62-177d65d5db9f -->
* **LTM forSession() budget, phases, and safety-net constants**: LTM forSession() architecture: \`forSession()\` (ltm.ts) budgets: \`budget.ltm\`=0.05, \`budget.preferenceLtm\`=0.02. Phases: (1) architecture entries up to ARCH\_BUDGET\_FRACTION=0.2; (2) remaining by score desc; (3) .lore.md sections. HEADER\_OVERHEAD\_TOKENS=15 subtracted upfront. Hard cap: \`if (used+cost>maxTokens) continue\`. PROJECT\_SAFETY\_NET=5: top-confidence project entries ALWAYS included even with zero relevance score. NO\_CONTEXT\_FALLBACK\_CAP=10. Global entries (pid=null) must be cross\_project=1. Pool SQL: project pool uses \`cross\_project=0\`; cross pool uses \`project\_id IS NULL OR cross\_project=1\`. Promoted entries (cross\_project=1) lose PROJECT\_SAFETY\_NET guarantee in home project. GOTCHA: \`vectorSearch()\` queries \`knowledge WHERE embedding IS NOT NULL AND confidence>0.2\` — test suites must \`DELETE FROM knowledge WHERE embedding IS NOT NULL\` in beforeEach. Dedup threshold for Nomic v1.5 is 0.935.

<!-- lore:1cdad2a4-5637-471b-a5cb-39c919fb1592 -->
* **Multilingual support: FTS tokenizer, Unicode tokenization, and non-English fallbacks**: Lore supports non-English (e.g. Turkish) conversations. Key design: (1) FTS5 uses \`tokenize='unicode61 remove\_diacritics 0'\` (NOT \`porter\` — English-only stemmer; NOT diacritic-folding — Turkish ç/ğ/ı/ö/ş/ü are distinct letters). Migration v32 rebuilds all 6 FTS tables. (2) \`filterTerms()\`/\`extractTopTerms()\` in \`search.ts\` strip via \`/\[^\p{L}\p{N}\_\s]/gu\` (Unicode-aware) so Turkish words don't split at special letters. Underscore kept for snake\_case. (3) \`pattern-extract.ts\` regexes need NO translation — they run on the LLM observer's \*English-normalized\* output. (4) Two raw-user-text regex sites have a structural fallback: \`hasNonAsciiLetters()\` (exported from \`instruction-detect.ts\`) gates fallbacks in \`extractInstructionCandidates\` (emit whole message as candidate) and \`detectAssertions\` (pin first sentence) — requires ≥3 non-ASCII letters to avoid false positives on English loanwords (café, naïve). (5) \`ltm.rerankPreferences()\` no longer demotes entries lacking English directive words to 0.8. KNOWN LIMITATIONS (deferred): English \`STOPWORDS\` not filtered for other languages (vector+RRF compensates); ~3-4 chars/token estimate is English-tuned (fine for Turkish, off for CJK).

<!-- lore:019e5922-4280-76aa-9b79-5ebeae433d7a -->
* **Sub-agent session detection, isolation, and differential treatment**: Sub-agent session detection via \`x-parent-session-id\` header (\`pipeline.ts\`). Each sub-agent gets independent session: temporal storage, gradient state, distillation pipeline, LTM injection, cost tracking. Key differential treatment: (1) cache warming ALWAYS skipped — \`shouldWarm()\` returns false unconditionally; (2) \`findRotationPredecessor()\` skips sub-agents from Tier 1b rotation; (3) idle distillation/curation runs normally. \`isSubagent\`/\`parentSessionId\` persisted to DB (migration v26). Parent resolved via \`headerSessionIndex\` (Tier 1+2 headers only — Tier 3 fingerprint-only parents unresolvable, warning logged). Dedup via \`subagentParentPendingLogged\` Set prevents 50+ identical log lines. Dashboard: collapsible cost-rollup tree via \`loadParentChildMap()\` + \`buildLiveSessionRows()\`; \`rollUp()\` propagates grandchildren costs bottom-up. \*\*Gap\*\*: OpenCode plugin \`chat.headers\` hook does NOT set \`x-parent-session-id\`. \`x-session-id\` added to \`KNOWN\_SESSION\_HEADERS\` (position 3, above \`x-session-affinity\`); \`ClientType\`/\`detectClientType()\` replaced with \`isClaudeCodeClient()\` boolean.

### Gotcha

<!-- lore:019e69f0-a05a-7d48-8e35-76baf13533c5 -->
* **LOREAI-GATEWAY-1E: resp.usage undefined crash in postResponse() for vLLM/OpenAI path**: Trap: \`GatewayUsage\` typed as non-optional on \`GatewayResponse\`, so \`resp.usage.inputTokens\` looks safe. But \`resp.usage\` can be \`undefined\` at runtime (e.g., vLLM returning partial response). Crash sites: pipeline.ts lines 2069, 2101, 2137-2140, 2150-2153, 2204-2206, 2282-2285, 2341, 2363, 2402; also \`buildOpenAINonStreamResponse\` (openai.ts:296-300). Fix: add \`const usage = resp.usage ?? { inputTokens: 0, outputTokens: 0, cacheReadInputTokens: 0, cacheCreationInputTokens: 0 }\` guard at top of \`postResponse()\` and \`nonStreamHttpResponse()\`, then use \`usage.\*\` throughout.

<!-- lore:019e56fb-b446-759b-aff2-035a83700124 -->
* **LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm — resolveAuth returns same stale token**: LOREAI-GATEWAY-Z: OAuth token expiry causes 401 storm. \`resolveAuth(sessionID)\` marks session stale and falls back to \`getLastSeenAuth()\` — same expired token. \`credentialChanged=false\` so retry-once path never taken. Idle scheduler fires 4+ LLM calls/tick (distill, meta-distill, curation, consolidation), each 401ing independently. Fix: (1) \`isAuthStale(sessionID) && !resolveAuth(sessionID)\` guard in \`scheduleBackgroundWork()\` and \`idle.ts:144\`; (2) \`resolveAuth()\` returns null when global fallback matches stale credential; (3) add \`/Worker upstream auth error/\` to \`TRANSIENT\_ERROR\_PATTERNS\` in instrument.ts. NOTE: auth guard only catches auth failures — \`no-response\`/\`upstream-error\` failures have no scheduler circuit-breaker. A permanently invalid bearer token (always 401) NEVER trips the global circuit breaker — global breaker (\`background-limiter.ts\`) only trips on 429 responses, not 401/403.

<!-- lore:019e4eab-59b4-7fe3-be03-874f682cc340 -->
* **openai.ts: array-format content in system/developer messages silently dropped**: openai.ts / openai-responses.ts parsing traps: (1) Array-format system/developer content: extract text parts (not coerce to ""). (2) \`buildOpenAIMessages\`: \`tool\_result\` blocks pushed mid-loop before \`textParts\`/\`toolUses\` flushed — tool\_result appears before text/tool\_use of same message. (3) Always re-emits system as \`role:'system'\`, never \`'developer'\`. (4) \`parseOpenAIResponsesRequest\` reads system from top-level \`instructions\`. (5) \`item\_reference\` and \`reasoning\` items silently dropped; \`store\` field not in extras allow-list — silently dropped. (6) LTM injection for Responses API: \`\[req.system, cache?.stableLtmSystem, cache?.ltmSystem].filter(Boolean).join(' ')\`. (7) Consecutive role:"tool" messages coalesced into one user message. (8) Array content only when \`hasOpaque=true\`. (9) \`translateAnthropicStreamToOpenAI\`: feeds accumulator unconditionally before per-event logic. (10) \`translateAnthropicStreamToResponses\`: on \`output\_text.done\`, replaces delta-accumulated text with final authoritative value. (11) \`nonStreamHttpResponse()\` scales usage via \`scaleUsage()\`.

<!-- lore:019eb248-b091-795e-acc7-948758e7c119 -->
* **pipeline.ts: TS2774 on function-reference guards, TS2739 on async wrapper return types**: pipeline.ts TS traps: (1) TS2774: \`const hasOutcomes = outcomes?.getKnowledgeImpact; if (hasOutcomes)\` — function references are always truthy, triggers 'condition always true'. Fix: call \`getKnowledgeImpact\` directly without the guard. (2) TS2739: \`runBackground(outcomes.runOutcomeAdjustment({...}))\` fails when \`runOutcomeAdjustment\` returns a plain object but \`runBackground\` expects \`Promise\<unknown>\`. Fix: wrap in \`async () =>\` or make \`runOutcomeAdjustment\` return a Promise.

<!-- lore:019e6950-8200-7473-87a5-45a861bb84af -->
* **recall.ts: unescaped quotes/ellipsis in marker strings cause store miss and raw marker leak**: Trap: \`buildRecallMarker()\` embeds raw \`query\` between double-quotes and raw \`id\` before \`…\` — looks safe because queries are usually plain text. But if \`query\` contains \`"\`, \`MARKER\_REGEX\` (\`/📚 Searching (.+?) for "(.+?)"…/\`) truncates the captured group at the first inner quote; \`recallStoreKey()\` produces a mismatched key; \`store.get(key)\` returns undefined; the marker is left as raw text in the conversation visible to the upstream LLM. Same issue for \`id\` containing \`…\`. Fix: escape \`"\` → \`\\"\` in \`buildRecallMarker()\` and unescape in \`parseRecallMarker()\`; for IDs, percent-encode or strip \`…\` before embedding.

<!-- lore:019e2e20-95b3-7a9d-ab38-77d87eafecc4 -->
* **splitSegments() infinite recursion on oversized single messages**: \`splitSegments()\` infinite recursion on oversized single messages in \`packages/core/src/distillation.ts\`: recurses infinitely when a single message exceeds \`maxSegmentTokens\` (16384). \`findSplitIndex()\` returns \`messages.length\` (=1), so \`left = messages.slice(0, 1)\` produces an identical recursive call. Triggered on large tool outputs (~49KB+). Fix: add base case after the \`totalTokens <= maxTokens\` guard — \`if (messages.length <= 1) return \[messages]\`. The oversized message becomes an indivisible segment. Also: \`distillTokenBudget()\` uses \`10×√N\` formula clamped to \[256, 4096]. \`storeDistillation()\` stores no file path column — paths only in \`temporal\_messages.content\` via \`toolStripAnnotation()\`.

### Pattern

<!-- lore:019e56d6-9cca-7a77-aa1a-a5850daef395 -->
* **Gateway test patterns: unit vs integration vs subprocess isolation**: Gateway/OpenCode test patterns — three isolation levels: (1) Unit: import functions directly, build fixtures via \`makeRequest()\`/\`makeUserMsg()\` factories. (2) Integration: \`createHarness()\` starts real server on random port with replay interceptor, provides \`chat()\` + \`queryDB()\`. (3) Subprocess isolation: \`mock.module()\` calls isolated in subprocess files to avoid global cache pollution. OpenCode plugin tests: \`LorePlugin\` + \`createMockClient()\` stubbing \`client.tui.showToast\` and \`client.session.\*\`. \`initPlugin()\` returns \`{hooks, tmpDir, cleanup}\`. DB isolation via root \`bunfig.toml\` preloading \`packages/core/test/setup.ts\`. \`GatewayRequest\` minimal required fields: \`protocol, model, system, messages, tools, stream\`. TRAP: integration tests that stub \`globalThis.fetch\` to mock \`upstreamFetch\` work only because \`\_originalFetch\` is null in tests. Use \`vi.mock('../src/fetch')\` to stub \`upstreamFetch\` directly. All packages must be devDependencies — CI enforces with \`pnpm run check:deps\`.

<!-- lore:019ead47-995f-7339-955e-548f2b7625ae -->
* **lore setup command: per-agent config writers with idempotent JSON/TOML updaters**: \`packages/gateway/src/cli/setup.ts\` \`SUPPORTED\_APPS\` has a \`ConfigWriter\` per app. Codex: TOML (\`config.toml\`, \`openai\_base\_url\` + \`model\_auto\_compact\_token\_limit=999999999\`). JSON apps (opencode, claude-code): idempotent deep-merge — reads existing JSON, preserves unknown keys. Re-running \`lore setup\` is safe. Port defaults to 3207; override with \`-p\`/\`-r\`. \`opencode\` setup auto-installs \`@loreai/opencode\` npm plugin (skippable with \`--no-plugin\`) and registers it in the \`plugin\` array. \`installPlugin\` shells out via \`spawnSync('npm install -g')\`, gracefully surfaces failures. \*\*Type traps\*\*: JSON updater functions must return the agent's config type (e.g. \`OpencodeConfig\`), NOT \`Record\<string, unknown>\`. \`opencodePluginSpec\` must be declared BEFORE \`SUPPORTED\_APPS\` — forward reference causes TS2448/TS2454. Prefer bundling setup steps into \`lore setup \<agent>\` over new top-level commands.

### Preference

<!-- lore:019eb3ff-f281-7e4e-bf40-b9ec95bdeb49 -->
* **Always annotate behavioral constraints as code comments at the exact implementation site**: The user consistently embeds authoritative behavioral directives as code comments directly at the relevant implementation lines (e.g., entities.ts:1507, 1443-1448, 1672-1674). These comments use imperative language ('never merge', 'always qualifies', 'always forces') and serve as the canonical source of truth for design decisions. When reviewing or implementing code, treat inline comments as binding specifications — not just documentation. When adding new logic, include a comment explaining the invariant or constraint at the exact line where it is enforced. Never rely solely on external docs or PR descriptions for behavioral rules.

<!-- lore:019eb411-9558-7718-95fd-9ad111de16dc -->
* **Always enforce specific CI invariants as non-negotiable directives**: Hard CI invariants — never violate: (1) Full CI always runs on main/release pushes: \`code=true\` forced unconditionally via \`id: force\` step when \`github.event\_name == 'push'\`; dorny/paths-filter only applies to PRs. (2) \`actionlint\` runs unconditionally (no \`needs\`/\`if\` guards), \`-shellcheck=""\` flag always set. (3) Coverage/artifacts always upload even on failure (\`if: '!cancelled()'\`). (4) Placeholder files ensure upload-artifact always creates an artifact. (5) \`ci-status\` job exits 1 on \`failure\` or \`cancelled\` for all required jobs. New CI jobs MUST appear in \`ci-status\` \`needs:\` array or they never block merge. Path filters in dorny/paths-filter must include all relevant files or test jobs silently skip. vitest.config.ts \`resolve.alias\` is top-level, NOT nested under \`test:\`. Delete stale \`.tsbuildinfo\` files to clear phantom type errors.

<!-- lore:019eb401-1846-7d52-b67d-b53d3af1f63f -->
* **Always flag security/data-leak bugs as blocking before other issues**: Cross-project data leakage or security boundary violations are BLOCKING issues, more urgent than correctness bugs, performance issues, or test coverage gaps. When a vector search or query path lacks a project\_id filter while a sibling path correctly filters, treat this as a regression equivalent to previously fixed leaks — call it out explicitly as blocking the PR. Non-blocking issues (stale artifacts, inefficiency, truncation inconsistencies, test masking) are noted separately but not conflated with the blocking security concern.

<!-- lore:019e9f06-597e-75c0-8f6a-2d2b4c144bc8 -->
* **Always remove all Bun-specific code and build targets**: Project has fully migrated to Node.js >= 22.15 + pnpm + Vitest. Never add new Bun-specific code. Replace Bun APIs with Node.js equivalents: \`node:worker\_threads\`, \`node:fs\`, \`node:zlib\`. Sentry: \`@sentry/bun\` → \`@sentry/node\` via \`sentryBunToNodePlugin\`; Bun's \`node:util\` lacks \`getSystemErrorMap()\` — polyfill BEFORE \`Sentry.init()\` in \`packages/gateway/instrument.ts\`. Always use \`vitest run\` (never \`bun test\`). Eval run commands use \`npx tsx\`. Install examples use \`pnpm add\`. TRAP: \`DecompressionStream('zstd')\` is Bun-only — Node.js throws at construction. \`bspatch.ts:157-178\` \`createZstdStreamReader()\` used this broken pattern — fix by replacing with \`createZstdDecompress\` + \`Readable.toWeb\`. Control block in same file correctly uses \`zstdDecompressSync\` from \`node:zlib\`.

<!-- lore:019eb19e-49a3-7756-9068-eaed09c05063 -->
* **Always request harsh multi-pass code review covering logic flaws, edge cases, and invariant violations**: PRs require harsh multi-pass self-review before merging. Check: (1) logic flaws and incomplete allowlists, (2) fragile assumptions/edge cases, (3) invariants preserved (cross-project isolation, scope gating parity between FTS and vector paths), (4) CI/publish path correctness, (5) PR description accuracy vs actual diff. Independently verify claims (grep symbols, trace execution paths). Produce structured PASS/FAIL/NIT verdicts. Fix all failing tests before pushing. Run full typecheck (zero errors) and full vitest suite before committing. Before implementing, enumerate 5–10 distinct targets upfront with exact file:line references. Always push branch and create PR as last step — confirm PR URL. Always stash/exclude \`.lore.md\` from PR commits. Check: flags set in memory but never persisted to DB; flag lifecycle; dead code; missing tests for new lifecycle paths. Verify tests actually assert the claimed behavior. Use Biome (never ESLint/Prettier) for lint/format. Always annotate behavioral constraints as code comments at the exact implementation site.

<!-- lore:019eb3df-2f47-7149-ae81-9b2489fbe095 -->
* **Always verify glob patterns don't accidentally exclude core business logic files**: When reviewing or writing file glob patterns (especially in coverage excludes, lint configs, or build configs), verify patterns don't silently exclude important source files. TRAP: \`\*\*/\*.config.ts\` looks like it only matches config files, but it also matches \`packages/core/src/config.ts\` (686 lines, Zod schema + config loading) and \`packages/gateway/src/config.ts\` (827 lines, env-var config) — 1,513 lines of core business logic silently excluded from coverage. Fix: prefer single-star \`\*.config.ts\` (root-only) over \`\*\*/\*.config.ts\` when intent is to match only root-level tooling config files. Always verify which actual files a glob matches before finalizing.

<!-- lore:019eadd8-c8fd-7785-9764-dfbf885563c9 -->
* **Always verify local CI checks (lint, typecheck, check scripts) pass before committing**: Always verify local CI checks pass before committing. Work sequence: implement → verify checks pass → commit → push. Required CI jobs before merging: \`test\`, \`actionlint\`, \`changes\`, \`preview\`, \`CI Status\`, \`check-docs\`, \`check-links\`, \`binary smoke (darwin-arm64)\`, \`binary smoke (windows-x64)\`. New CI jobs MUST appear in \`ci-status\` \`needs:\` array or they never block merge. Path filters in dorny/paths-filter must include all relevant files or test jobs silently skip. Main/release pushes always run full CI via \`Force full CI\` step; \`dorny/paths-filter\` only matters for PRs. \`actionlint\` runs unconditionally — workflow bugs must never slip through. vitest.config.ts \`resolve.alias\` is top-level, NOT nested under \`test:\`. Delete stale \`.tsbuildinfo\` files to clear phantom type errors.

<!-- lore:019e9d98-e22f-7f0d-87d3-91c4f8b9a5e3 -->
* **OpenCode plugin: per-project state map for project path header injection**: OpenCode plugin per-project state: \`projectState\` Map keyed by \`ctx.project.id\` → \`{ projectPath, gitRemote, lastSeenAt }\`; TTL \`SESSION\_STATE\_TTL\_MS = 24h\`. \`reapStaleProjectState()\` called on every new entry. \`chat.headers\` hook uses per-project Map entry to avoid cross-project clobbering. \`loreInitPromise\` memoizes concurrent plugin calls racing on probe→spawn. Gateway discovery order: (0) \`LORE\_REMOTE\_URL\`; (1) \`LORE\_GATEWAY\_URL\`; (2) port file (dynamic import \`@loreai/gateway\` → \`readPortFile()\`); (3) known ports \`\[3207, 5673]\`; (4) in-process \`startGateway({ quiet:true, local:true })\`. \`remoteGateway\` auto-detection: (1) \`LORE\_REMOTE\_GATEWAY\` env; (2) \`LORE\_HOSTED\_MODE\`; (3) non-loopback bind; (4) default=true. \`--local\` CLI flag ALWAYS wins. In-process callers MUST pass \`local:true\`. Fallback globals \`currentProjectPath\`/\`currentGitRemote\` used by fetch interceptor when \`chat.headers\` hook is bypassed. \*\*Gap\*\*: \`chat.headers\` injects \`x-lore-session-id\`/\`x-lore-agent\` but NOT \`x-parent-session-id\`. \`tool: {}\` at line 286 — empty map, no tool hooks implemented yet.
